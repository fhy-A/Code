# 多标签预览工作区与本轮改动审查：隔离归属评审

> **2026-09-16：用户已确认B，阶段1通过指定STRICT独立复核及用户切换体验验收，本提交本地收口，未发布；阶段2仍未实施。**
> 生效授权：`code042-workspace-R002`及复核修订`R003/R004`，本地收口`R006`，基线`15c7f44eb2c17f865371e1087f74dc15eb87ea1c`。以下“待裁决/纯前端”等原评审文字保留为历史对照；与本节冲突时以本次已确认合同为准。

当前合同：Session（含服务端稳定实例身份）隔离文件标签/活动项/滚动/模式，项目提供组织与工作区上下文，Run归属冻结改动事实；不提供A/B模式开关。阶段1获准新增最小source身份、metadata附加previewIdentity和绑定读取接口；messages JSONL/AgentRun/Goal格式、既有访问权限与旧文件API保持。

- source为`DATA_DIR/preview-workspace/source.json`的`code-preview-source/v1`，UUID及规范化DATA_DIR路径摘要；损坏、未知或搬移不默默重置。Session身份由服务端生成，create/branch/新导入快照为新值，普通保存/原位导入更新/归档恢复保留；旧数据仅当前Session显式POST初始化，保持CAS revision。旧版丢失字段后重新生成身份，不复用浏览器缓存UUID。
- `POST /api/preview/context`提供权威source/Session实例/root/contextRevision；`GET /api/preview/file`验证上下文并冻结解析root，返回canonicalAbsolutePath/fileKey/contentRevision/locator；绑定后路径重定向拒绝，图片/PDF raw同样验证。新解析不mkdir，读取不触发归档/删除事务前滚恢复，未决事务直接拒绝；旧API继续兼容。身份/上下文不是新的授权依据。
- 单击内部槽复用；双击以同手势描述符回滚首次替换后显式新开；Ctrl/Meta、中键、明确入口新开，同受控fileKey去重。并发显式新开（包括回答链接异步目录探测）保留每个不同文件意图并按点击请求顺序排列，仅最新操作取得正文/焦点；显式新开不改变内部槽；关闭槽后下次单击新建；关闭活动项用MRU，关闭整栏保留工作集。任意时刻仅一个活动详情和刷新槽。
- 20标签上限拒绝继续新开并提示，不自动关闭既有项；v1本地store最多50个Session记录、1MiB，容量/存储失败明确提示仅页内保留。正文不持久化，未知/损坏原值保留。草稿用sessionStorage独立UUID，仅对应创建成功的Session迁移一次；新会话/欢迎态不沿用上一会话。
- 初始化失败后普通下一次点击重试，同目标并发初始化合并，旧导航结果不覆盖新scope；正常初始化不提示身份信息，仅确定旧实例非空标签失效时说明需要重新打开。描述符按字段/引用/大小有界校验，坏值保留原字节并降级到空内存工作集。清栏/关闭/导航释放拖拽资源，迟到RAF不能改宽度。
- 预览区分空、加载、成功和不可用；冷打开显示目标标题与加载提示，访问过的同scope文件使用最多8项/8MiB估算响应载荷的内存LRU即时呈现，只重新校验活动项。预算按序列化UTF-8与UTF-16字符串估算取大，不代表整个堆/DOM/媒体内存上限；raw媒体字节不缓存，正文不落盘，不预加载。切scope/失效/pagehide清空，关闭标签释放对应项；等待校验时不另开轮询，语言刷新也不启动新计时器。媒体仍显示真实加载状态，单renderer与单轮询槽保持。
- 旧open/path仅权威scope下受控解析后迁移，Web Locks串行协调浏览器页，一份store内写入迁移归属与成功标记；成功前不消费，旧键不删除。宽度仍全局。失败导航保持空白/不可用；切换立即使正文、错误、mtime、raw加载和其他旧异步结果失效。多页最后保存者规则不抢当前视图。
- 窄屏仍复用同一预览aside与单renderer，提供明确关闭入口；不增加分屏、系统窗口或非活动文件预加载。新标签创建顺序稳定，活动标签滚动入可视区域，长名不能挤出关闭按钮；所有标签同等。阶段1先内部H4后用户验收，候选冻结后独立复核，按授权再提交。
- 阶段2仍未实施。R005只读核查推荐复用现有AgentRun工具结果、不建第二份快照仓库；用户已确认“按一次任务汇总”：根执行组包含已消费补充指令、经核实的所属子任务和自动续接，独立新请求另算；展示逐操作diff、删除事实及覆盖不完整提示。未来Run归属/实际文件凭据、只读接口、限量/revision及兼容回退合同仍待下一条技术实施relay冻结，不能据此提前写入阶段2。阶段1通过不等于整个CODE-042完成，不自动实现阶段3。

下列原评审记录保留其基线与历史判断，不作为当前已实现/已验证声明。

## 1. 当前事实与旧方案需要修正之处

以下代码行号以 `06e0b6d0bda779f507149394fc6a1fd4d32cc151` 为基线，仅是静态核查，不是运行验收。

| 已核查事实 | 证据 | 对设计的影响 |
| --- | --- | --- |
| 预览仍用全局 `state.previewPath/previewContent` 等字段；`loadFile` 请求返回后直接提交全局路径、内容与DOM，没有project/tab/请求序列提交绑定 | [preview.js](../src/features/preview.js) 385–453行 | 需要集中身份与提交校验，不能只在现有DOM上增加标签按钮 |
| 当前3秒轮询从全局path发请求，返回后按当前kind更新；旧响应可能已不对应当前活动对象 | [preview.js](../src/features/preview.js) 348–374行 | 加载、自动刷新、错误与mtime均须按同一对象身份提交；abort不是唯一保护 |
| 宽度仍是全局旧键；加载保存open/path，关闭清除open/path，restore失败也清除这两个键 | [preview.js](../src/features/preview.js) 103、391–392、458–464、544–555行；[app.js](../app.js) 19217行 | 原关闭与恢复实现不能直接作为多标签的目标行为；迁移必须先保护旧值 |
| 文件树和最终回答本地文件入口共享预览加载能力；文本、Markdown、CSV/TSV、图片、PDF和二进制降级已有分支 | [app.js](../app.js) 2156–2166、3654–3668行及[files.js](../src/features/files.js) 385、637行；[preview.js](../src/features/preview.js) 385–453行 | 保留统一打开路由与既有受支持类型，不在两个入口各造标签系统 |
| CODE-071已将文件树浏览与Session归属解耦；显式projectId权威，rootPaths有唯一主源和可共享次源 | [09-03日志](development-log/2026/2026-09-03.md) 25–29、55–61行；[saveProjectRoot](../app.js) 7844行起 | 8/24按浏览根识别项目的假设过时，目录切换不能迁移项目或偷取其他项目状态 |
| `/api/file`调用现有路径解析；先read_bytes再截断展示，另有raw字节流 | [server.py](../server.py) 25680–25707、31438行起 | projectId/root列表不是授权或硬沙箱；展示截断不等于磁盘读取成本上限 |

**路径规则的实际边界**：[resolve_project_path](../server.py) 25680行起先解析绝对路径或按当前有效root解释相对路径；在项目根内直接返回，在项目外但用户home内会扩大到home，某些相对路径还会尝试home fallback，其余情况落到`root/output/basename`并可能执行mkdir。`get_file`在31438行起先尝试附件解析再调用该函数，先`read_bytes()`读入文件、再按`MAX_PREVIEW_BYTES`切片供展示。因此Session/projectId/root集合只提供UI与工作区上下文，**自身不授予或限制文件访问，也不是硬沙箱**；不能声称预览磁盘读取本身有1MiB成本上限。本轮只静态记录，不调用这些路径接口，不修复或调整其现有行为。

“当前项目”必须先冻结其产品含义：沿用已有显式项目上下文，不把文件树临时浏览路径或共享次源反推出另一个项目。现有接口能否提供足够的scope/受控文件身份供纯前端验证，仍须阶段0盘点；本文没有证明这点已经解决。

## 2. 2026-08-24 原确认约束：保留记录，隔离归属待裁决

以下完整保留原已确认行为。UI共享、文件查重及关闭规则中的“同项目”归属均属于候选A；候选B仅在用户选择后改为“同会话工作集”，同时不改变现有服务端路径/权限规则。其余打开手势、同等标签、只读/资源/兼容边界继续保留，不能把B暗中视为当前合同。

- 文件标签按项目隔离，不绑定Session。同项目切换会话保留标签、顺序、活动项与视图状态；跨项目先隐藏旧内容，再恢复目标项目，返回原项目恢复自己的工作区。
- 只有现有右侧栏和一条文件标签栏，不增加左右/上下分栏、独立系统窗口或多个活动renderer；文件预览只读。
- 普通单击复用一个内部浏览槽；双击、Ctrl/中键或明确“在新标签打开”显式新建。同项目中已经打开的同一受控文件一律激活已有标签并应用明确行号，不再建副本。
- 所有可见标签地位相同，都能激活、关闭、重新加载和保留视图状态；不暴露“临时/固定”类别、徽标或不同关闭语义。浏览复用槽只是内部游标。
- 关闭非活动标签不影响活动正文；关闭活动标签优先回到同项目最近访问的剩余项，最后一项关闭时停止轮询并关栏。关闭整栏保留该项目标签，不默认清空。
- 标签顺序按创建顺序稳定保存，不加入拖拽排序、固定、分组、跨项目移动或最近关闭列表。目录导航、右键、`@`插入、显式系统打开和行号定位保持。
- 保留旧open/path/width兼容要求，不把标签状态写入Session JSONL；第一阶段不改服务端文件协议、项目/Session/AgentRun/Goal格式，不新增依赖或后台预加载。
- 任一时刻仅活动文件有内容DOM与轮询。原有文本截断/免高亮门槛、Markdown/表格模式、图片缩放、PDF与二进制降级仍受各自原安全合同约束。

**必须验收的组合手势**：浏览器双击先产生两次click，不能先误覆盖浏览槽、再额外创建错误标签；同路径经双入口同时打开须去重。显式新建后普通单击应覆盖哪个内部槽、槽关闭后的回落、Ctrl的平台等价键、中键默认导航阻止、长同名文件与键盘/390px窄屏均需真实验收。若无法满足原同等标签约束，须停下确认，不能自行改成“临时/固定”两类。

## 3. 隔离归属对比与整合建议（待确认）

### 3.1 A项目共享与B会话独立

| 维度 | A：项目共享UI工作集（8/24原确认） | B：会话独立UI工作集（新版推荐，未批准） |
| --- | --- | --- |
| tabs/active/开关/滚动 | 同项目会话共用，跨会话接续阅读方便 | 各稳定Session分别保存，返回原会话恢复自己的阅读位置 |
| 同项目并行任务 | 在同一份可见工作集中切换时可能互相影响活动标签/阅读位置，需要额外避免抢占 | 切同项目另会话也先隔离，后台完成只更新所属会话 |
| 与改动审查的关系 | 容器虽共享，review仍必须绑定来源Run/版本，不能按project复用事实 | UI选中范围随会话保存，事实仍属于Run/来源版本，较贴合任务归属 |
| 使用代价 | 跨会话少重新打开文件，但不同任务易混在同一阅读集合 | 跨会话需重新打开文件；仍可复用现有项目文件树，无需复制项目 |
| 实施门槛 | 延续原确认的项目标签约束 | 先批准同项目不再共享标签这一变化，并冻结Session/草稿/导入生命周期 |

**推荐B的理由**：任务与改动审查更接近会话上下文，同项目并行任务不应互相改活动文件或滚动位置。项目负责文件组织与工作区上下文；实际访问仍由现有服务端路径/权限规则决定，会话独立不构成新的授权限制或扩大。Codex官方资料中的thread side panel/chat sidebar可作为上下文组织参考，不能证明其持久化或切会话行为与B一致。

第一版只实现用户选定的一种隔离归属，不同时提供A/B切换开关。跨会话分享或复制标签以后可考虑明确动作，不默认互相同步。用户尚未裁决时，A仍是原已确认方案，B仍是推荐候选；本稿其他新建议不构成实施授权。

### 3.2 推荐B的分层与生命周期

- **会话层**：稳定Session身份保存tabs、active、paneOpen、scroll、mode及review选中范围；同项目换会话也先隐藏旧正文并使旧请求失效，返回原会话恢复自己的工作集。文件查重与浏览复用槽仅在该会话内生效，其他会话已打开的同path不能被跨会话激活。Session身份须置于数据源/host命名空间内，来源导入还要核实本地身份及代次，不能只用外部源ID或可重用字符串。
- **项目层**：显式projectId和root集合提供文件组织与工作区上下文，不是访问凭据或硬沙箱；实际访问沿现有服务端路径/权限规则。目录浏览不是归属迁移，项目不负责同步各会话的UI状态。
- **全局层**：宽度、主题等既有UI偏好可继续全局使用，不把文件内容或活动标签也全局共享。
- **Run层**：冻结实际改动事实与来源revision。会话只持有合法review引用，不能因同项目或切会话就把别的Run结果当当前轮。
- **新会话/欢迎态**：默认空工作集。没有Session的临时预览不写回上一会话；如希望保留草稿预览，须使用独立draft身份，只在对应新Session实际落定且scope验证后迁移一次，不广播给后续所有会话。draft的启用和失效方式须在阶段0明确。
- **改归属/导入/生命周期**：Session迁移项目后先核验全部文件scope，不能携带旧项目正文冒充新项目。归档时暂停并隔离其活动侧栏，恢复后再验证；删除后记录按已确认保留策略失效，不能自动继承给重建的同ID；来源导入不自动带入未受信任的路径或外部侧栏状态。具体保留/清理与身份代次仍待确认，阶段1不擅自删除用户原状态。

### 3.3 一个侧栏，两种阅读对象

文件预览用于持续查看项目中的当前文件；改动审查用于回答“这一轮实际改了什么”。共享右侧容器、加载状态、焦点返回、错误呈现和资源释放，分开身份、保留期和生命周期。下表按推荐B描述；若选A，只把文件UI工作集归属改回project，Run事实归属不变。

| 对象 | 归属与身份建议 | 内容性质 | 打开方式 |
| --- | --- | --- | --- |
| 项目文件标签的UI实例 | 数据源/host scope + Session身份/代次 + 经现有API核验的真实文件身份；项目另供组织上下文 | live，沿既有接口读取当前文件 | 文件树、搜索结果、最终回答本地链接；沿用第2节规则 |
| 本轮改动视图 | 数据源scope + 真实请求/AgentRun归属集合 + 结果revision + 文件身份 | 已有执行证据支持的冻结历史结果 | 聊天终态轻量摘要进入，一份review中换文件 |
| 待批准编辑建议 | 原authorization/proposal/tool-call实例身份 | 尚未应用的建议，继续原授权流程 | 原提案卡片和授权定位；不冒充已发生改动 |
| Git工作区变更（扩展） | 明确仓库 + Git范围 + 当前读取revision | 可能含用户及其他写者的现有变更 | 单独标“工作区变更”，不当作本轮Agent归因 |

建议的文字线框（仅供评审，不是新页面原型）：

```text
聊天中的本轮终态
  改动 3 个文件 · 查看改动

现有右侧工作区
  [项目文件] [本轮改动]
  项目文件：README.md  main.py  …  （既有同等标签）
  本轮改动：本次请求 / 时间 / 覆盖说明
             搜索文件…
             新增 a.txt   修改 main.py   删除 old.txt
             选中 main.py 的差异；[查看当前文件]
```

“本轮改动”内部用紧凑可搜索/滚动的文件列表选择一个详情，不为每个文件自动开新标签，不新增可拖拽分栏；当前文件可以显式进入文件标签或既有渲染预览。两种视图在同一侧栏切换时只激活一个详情renderer。

主聊天在完成、失败或取消终态已有实际改动时显示轻量摘要，不把一堆diff展开进聊天、不自动抢焦点。后台任务完成只更新其所属记录/摘要，不切当前项目、会话、标签或页面。仅当覆盖已知完整且确认无改动时不显示；若命令等覆盖未知，即使已记录数为0，也必须提示“改动记录不完整”而非宣称零修改。数值来自执行事实去重后的文件数，不解析模型回答来决定。

## 4. 项目与文件身份：修正共享root和浏览目录假设

以下是UI工作集与文件身份校验的共同约束，A/B都适用，不把UI隔离声称为文件权限隔离；UI工作集按第3节选定归属。具体字段、来源与兼容策略尚待阶段0确认。

1. **先绑定数据源和host。** 同一浏览器来源、同名项目或同一相对路径不足以说明是同一工作区；切换服务数据源/host不得加载旧scope记录。现有产品尚未被本文证明具有可直接复用的稳定scope标识，缺失时标为待解决，不自造不受验证的身份。
2. **显式projectId优先。** 文件树浏览目录与Session归属分开，不因导航、共享root命中或预览文件而迁移Session。无项目的独立文件夹仅在当前scope和受控根验证成功后使用明确fallback，不能借用其他会话或已归属项目的标签。推荐B的无Session预览还必须使用独立draft身份。
3. **文件键不能只是相对path。** 同项目两个root下各有`README.md`时，必须基于API确认的受控真实路径/根身份区分；显示名可用短root提示解歧义。目标必须按现有服务端路径/权限规则解析并核验；projectId和root名单不保证解析结果限于项目内。标签与缓存绑定已验证的真实文件身份，不能只靠会变的全局root重新解释相对路径，也不能把前端规范化当成授权。
4. **物理共享不等于视图共享。** A下两项目共享同一物理文件仍各自保有项目视图；B下即使同项目、同物理文件，不同Session也分别保有标签/滚动/选择状态。两种方案都不能仅凭磁盘路径跨工作集复用DOM。移除root、改主源、项目移除或数据源变化时先重验，失效标签标不可用并保留轻量记录，不静默映射到另一个root同名文件。
5. **历史差异与live文件分开。** 同文件不同轮、同轮不同冻结revision、当前文件和历史差异都不能按path单独去重。历史删除文件可继续查看已有合法差异；“查看当前文件”不存在时说明不可用，不把后来重建的同名文件当旧版本。

阶段1的“不改后端”是边界，不是可实现性结论。若现有接口无法稳定核验UI scope、实际解析文件和root变化后的对应关系，应停在阶段0提出独立合同，不用拼接path或共享缓存猜测身份。任何收窄或扩大现有路径访问规则均另行评估、批准，不能借本稿承诺已实现项目硬隔离或顺带重构路径安全。

## 5. 本轮改动事实：覆盖盘点与数据合同（新增，待确认）

### 5.1 当前工具可提供的证据和不足

[server.py](../server.py) 21868–21912行的`execute_write_file_tool`在普通文件分支返回path/diff/backupPath/replayed；同内容重放可返回空diff。27715–27734行`execute_propose_edit_tool`明确返回`applied=False`，只是建议。实际应用入口是27818行`execute_apply_edit_proposal`，核心在27737–27815行；应用结果包含path/diff/backupPath/applied/replayed，且校验内容hash及mtime。当前源码并非名为`execute_apply_edit_tool`的独立函数。

这些字段能作为盘点起点，但不等于已建立任务级可靠记录：受管记忆等特殊分支返回形态需单独盘点；backupPath可能为空，资源存在期也未由本方案确认。单工具diff不自动覆盖命令/脚本写文件，不自动提供完整before/after链、请求/子任务归属或重启后历史可用性。失败/拒绝的建议不能计为已应用，`replayed`不能再增加一次副作用；没有可靠应用证明时应标未知，而非仅凭“ok”或模型文字归因。

### 5.2 归属、汇总与冻结建议

- 以真实AgentRun和一次实际用户请求的执行关系作为归属证据；明确队列、steer、重试、恢复、子任务和独立后台任务分别如何加入。子任务必须有可核实父子关系，不能因同Session/同时段就合并；Goal跨多轮，未经定义不能把整个Goal算“一轮”。阶段0先冻结这套命名和归属，不由UI自行猜测。
- 每项事实至少需可核对的操作/工具身份、scope与受控文件身份、应用状态、重复回放关系、前后内容证据或其缺口。这里只列信息需求，不冻结sidecar键名/schema、保存位置、保留期或删除策略。
- 同轮同文件多次编辑，只有连续且可核实的首前→末后链才能给净diff；不得简单拼接unified diff或累加增删行数。链断裂、外部写入穿插、资源缺失或中间状态不明时只提供逐操作差异与明确缺口，不能伪造净差。
- 新增/修改/删除类别需文件存在性及操作证据；重命名必须有可靠关联证据，不能靠相似内容或路径猜测。同轮新增后删除等净零情况仍应保留可解释的操作事实，不与“没有发生副作用”混淆。
- 任务开始前已有dirty内容、其他任务和外部并发写入不归本轮。Git工作区diff不能单独证明Agent归属；非Git项目也应能显示受管文件工具已确认的改动，命令/脚本覆盖不足则标范围不完整。
- 成功、失败、取消和恢复都按已有执行事实记录副作用。模型终态文案不能证明文件是否改变；未知执行窗口需保留核对状态，不把丢响应当未写，也不把重复返回算两次。
- 本轮历史差异一旦确认冻结就以其revision阅读，不随当前文件轮询重算。历史无证据不回填编造；合法旧差异即使当前文件已删仍可查看；diff/备份/正文未保留时明确不可用。修正归属或补证据需新revision及原因，不能悄悄覆盖原审查结果。

### 5.3 必须独立批准的边界

若需AgentRun sidecar、新只读接口或持久副作用索引，属于新的数据/恢复合同，按STRICT另批。先确定字段与版本、来源/去重、截断/保留与清理、权限/路径、失败/重启恢复、旧数据读取及降级，再实现摘要和diff。UI整合推荐仍放`CODE-042`；文件改动事实/恢复若需新能力，建议设独立关联事项，但本轮**仅建议，不分配ID、不修改私有计划**。

## 6. 轻量UI持久化、旧键与回退

按推荐B，文件UI可使用版本化registry：scope→Session身份/代次（或独立draft）→标签描述、activeTabId、内部reuseTabId、paneOpen、创建顺序、模式、行号、滚动、图片缩放和review选中引用；projectId/root信息属于文件组织和上下文映射，不是授权凭据。若用户选择A，则UI归属维度改为project，不能在一份实现中混用。历史review仅保留合法记录的引用/选中项；差异事实来自单独批准的数据合同，不能把浏览器localStorage当持久审计账本。

- 不持久化正文、图片字节、PDF DOM、语法高亮HTML或任意服务端响应；每个已选工作集的标签数、序列化字节和view-state范围设确定上限，不能静默丢弃活动项。
- 保留`code-preview-width`作为既有全局宽度偏好，不因迁移重置。新多标签键名仅为实现候选，需版本与严格解析合同，不在评审稿中当成已经发布的格式。
- `code-preview-open/path`只在新记录缺失、当前scope/已选UI归属身份与API实际解析文件的对应关系全部验证成功时惰性导入一次。B仅导入当前已确认Session或独立draft，不广播到同项目全部会话。成功保存新记录后才允许更新或镜像旧键，镜像也只指向当前有效scope的活动文件；不全量扫描或迁入其他项目。
- 未知版本、损坏、过大、存储失败或不可验证旧值都保留原值，不覆盖“修复”、不删除用户状态；呈现该scope无可用预览或明确不可用。原稿“损坏时回退到旧单路径”收紧为：**只有旧路径也通过当前scope完整校验才可使用**，绝不能跨项目回退。
- UI版本回退只承诺有证据验证的旧单文件路径/宽度行为，不承诺旧构建具备项目隔离能力。不能安全证明时停止自动恢复，保留新旧记录；切回新版仍可核对。阶段2事实格式的降级另按STRICT合同，不能以清空凭据作为回退办法。
- 阶段1不删除原用户状态，不修改Session JSONL或后端协议；不新增迁移弹窗。手动清除浏览器状态后的空工作区沿既有规则降级，不恢复已被用户删除的UI数据。

## 7. 异步、焦点与资源管理

共享侧栏控制器应统一接收打开意图，避免文件树、消息链接和review各自管理正文。selectedWorkspaceIdentity在B为Session/draft身份及代次，A为project身份。建议加载身份至少含`scope + selectedWorkspaceIdentity + projectScope + viewKind + tab/reviewId + contentRevision + requestSeq + workspaceEpoch`；具体字段在阶段0冻结。

切换已选UI工作集/标签/视图时先记录轻量状态（B包括同项目切Session，A包括切project）、递增epoch、停止旧轮询并隐藏旧正文，再建立目标壳和活动身份。只有身份、revision、请求序列仍一致的响应能提交正文、mtime、错误、滚动或模式；关闭、替换浏览槽、快速切文件、跨scope和root移除均使旧请求失效。AbortController可节流，但不能代替提交时的身份复核。

仅活动live文件拥有既有刷新轮询；历史diff不轮询当前文件来改变冻结结果。后台标签、非当前会话/项目工作集和未展开review零预加载，不能一次恢复所有文件。任意时刻一个活动renderer/轮询槽，切出即释放图片监听、PDF iframe、Markdown/表格事件、resize帧、pointer capture及可能的Object URL；正文不能因多标签而长期多份缓存。

用户明确打开时才转焦点到对应视图，关闭返回原触发点；后台完成、自动刷新和失败提示不夺取焦点。loading/error/unavailable均必须属于当前对象，不能显示其他项目的标题或内容；失败只影响当前项，不清空其他工作区。B下后台同项目的其他Session也不能切换当前侧栏。

## 8. 与Codex官方能力的对照和取舍

本节复用本轮审批侧已实取的官方资料，不重复联网调研；引用的是产品公开能力，不是对本机Codex点击细节、内部实现或项目保留语义的观察。

| 官方资料支持的方向 | workbar建议借鉴 | 不据此推断或纳入首交付 |
| --- | --- | --- |
| 文件/成品可在chat sidebar中查看，支持可选完成后自动预览和对受支持内容标注：[Artifacts viewer](https://learn.chatgpt.com/docs/artifacts-viewer) | 共用侧栏、从聊天定位产物与定点反馈入口 | 不默认抢焦点；标注另议，不声称所有renderer都支持 |
| Unstaged默认范围还含用户/其他未提交改动；Staged/Commit/Branch/Last turn区分范围，部分能力要求Git，并支持仓库选择、inline评论及stage/revert：[Code review](https://learn.chatgpt.com/docs/code-review) | 明确“本轮”与“工作区”的范围、内容/差异切换；多仓库身份不能省略 | 不复制Git范围作为本轮归因，不加入stage/revert或回滚权限 |
| 4月10日thread side panel中的workspace file tabs、4月12日rich previews的更新条目：[Changelog](https://learn.chatgpt.com/docs/changelog) | 会话上下文中的多文件阅读与共享详情容器 | 不推断单击/双击规则、临时/固定语义、跨项目持久化或内部数据模型 |

现有workbar按实际支持提供文本、Markdown、CSV/TSV、image、PDF与二进制降级。HTML可沿现有文本路由阅读源代码，不等于可运行页面；Office、可交互HTML、浏览器、terminal renderer另阶段评估。首次交付不引入编辑保存、Git stage/revert、批量回滚、自动commit、重型IDE、独立窗口或新依赖。

## 9. 阶段重排与各自完成线（建议，尚待确认）

| 阶段 | 交付 | 进入实现前的门槛 | 不能冒称的完成 |
| --- | --- | --- | --- |
| 0：共享对象/入口与证据盘点 | 裁决A/B，冻结scope/Session或draft/项目/文件/轮次对象、组合手势、工具/命令覆盖和旧键风险 | 用户确认隔离归属与新增整合方向；必要接口缺口先提出合同 | 文档盘点不等于产品已支持、事实已经可靠 |
| 1：多标签基础及选定的隔离归属 | 统一打开路由、同等标签、按选定A/B隔离、轻量兼容及资源释放 | B须先获用户变更确认；其他原约束保留；纯前端可验证身份，不改后端/数据；若做不到先停 | 标签栏和renderer完成不代表已有本轮改动汇总 |
| 2：本轮变更摘要与只读差异最小闭环 | 真实执行归属、有限但明示的证据覆盖、摘要、冻结历史diff、live文件入口及恢复 | 格式/保留/失败恢复先冻结；sidecar或新只读接口按STRICT另批并独立验收 | 单工具diff、Git dirty和模型总结不等于完整净diff |
| 3：覆盖与反馈扩展 | 更完整命令/Git覆盖、多仓库范围、标注等按价值选取 | 分别确认来源、权限、存储成本、失败和回退 | 不自动纳入编辑/回滚/自动提交或Office/浏览器能力 |

CODE-042继续作为UI整合主事项。原私有条目仍写A的项目共享第一阶段，保持原文与未实施事实；用户选择B后才能另行同步其合同。本稿不能自动改变它的隔离归属或扩大完成定义，也不因阶段1通过就关闭阶段2尚未获批的需求。

## 10. 验收矩阵与待决定事项

下表是未来验收要求，本次只核对文档，**没有运行产品测试、浏览器或实现验证**。

| 验收组 | 必须给出的证据 |
| --- | --- |
| UI隔离裁决 | A验证同项目切会话保留共享标签；B验证同项目切会话先隔离、返回恢复原Session、后台不抢侧栏，新会话空工作集、欢迎态独立draft与一次迁移；两种期望不可在未选择时混为同一验收 |
| 身份与root | Session改归属/归档/删除/重建/来源导入的身份与失效策略按所选方案核验；同项目不同root同名文件正确区分；两项目共享物理文件状态隔离；浏览独立文件夹不迁移projectId、不偷取其他项目；root移除/主源变化及host/数据源切换不复活旧标签 |
| 打开与关闭 | 单击复用、双击前两click组合、中键/Ctrl/明确新建、双入口同文件去重、行号；内部浏览槽不误覆盖；关闭活动/非活动/整栏及最近访问回落符合原约束 |
| 异步资源 | 慢响应切项目、快速换文件、加载中关闭/替换、轮询中切view、后台完成；迟到正文/错误/mtime都拒绝，只有一个活动renderer和轮询、后台零加载 |
| 迁移与重启 | 旧open/path/width导入/镜像幂等、成功保存顺序、未知/损坏/越界/超限/写失败保留原值；reload/重启/root变化后重验；旧版回退不跨scope、不删原用户状态 |
| 执行归属 | 旧dirty基线不归本轮；重复、失败、拒绝、已应用但丢响应分开；两run同path、子任务与独立后台不误合并；恢复后原操作不重复计数 |
| 差异与覆盖 | 连续证据首前→末后净差；外部并发/链断裂降为逐操作；新增/删除/重建与有证据重命名；非Git可看已确认受管改动，命令coverage未知不显示“零修改” |
| 历史与可用性 | 同path不同轮/revision、历史diff与live不混用；已删文件可看保留差异；资源过期/缺失、未知格式保留证据并明确不可用，不编造历史 |
| 展示与安全 | bundle/direct classic、390px宽窄、明暗中英、长同名标签、键盘与焦点；受支持文本/MD/CSV/图片/PDF/二进制原安全路由；后台不切页面，无真实模型/数据副作用 |

进入新增范围前的首要裁决是**继续A项目共享，还是采用推荐B会话独立**；这是改变8/24已确认约束，不能仅凭用户要求“考虑隔离”就视为选B。随后确认：同侧栏两种视图与终态轻量摘要；“本轮”对请求/重试/子任务的精确定义；首交付的事实覆盖与不完整提示；历史差异/备份的权限、保留期限和资源上限；是否另立关联的数据/恢复事项。以上均为待确认建议，本次不询问实现授权，也不据此开工。

停止条件：未获A/B裁决就改变工作集归属、把Session/projectId/root身份当成硬沙箱或授权、跨scope共享视图、收窄或扩大现有路径权限、改变全局文件协议/持久格式、删除旧值来迁移、多活动renderer或偷偷引入临时/固定标签时停止；阶段2格式/保留策略未定不得先实现。证据与用户观察冲突、旧数据/安全/双入口回归同样回到对应门禁，不以文档方案代替验收。

## 11. 关联历史待办核查：事实与未知

| 线索 | 可确认的记录 | 与“任务结束后的改动文件预览”的区别 |
| --- | --- | --- |
| CODE-042 | 当前私有清单中承接预览工作区；本次复核仍未实施，未发现独立“任务完成改动文件汇总”条目 | 可承接UI整合，但现条目不证明新数据合同已获批准 |
| CODE-024 | [08-13日志](development-log/2026/2026-08-13.md) 13–21行：终态静默刷新文件树已完成 | 更新可见目录，不是按run冻结改动清单/历史净差 |
| CODE-025 | [08-14日志](development-log/2026/2026-08-14.md) 5–13行：单编辑建议Diff折叠及授权定位已完成 | 建议/授权卡片，不是完成后的跨工具归因汇总 |
| CODE-002 | [08-14日志](development-log/2026/2026-08-14.md) 28–33行：per-run轨迹与副作用manifest方向另行立项 | 是历史方向记录，不构成当前已有稳定ID的文件改动审查事项 |

审批侧本轮提供的检索结论覆盖264个Git版TODO，未找到明确独立条目；开发侧本次复核当前私有条目和上述日志，不把转交的历史检索说成本次重新穷举。私有TODO不在Git中，因此不能断言从未登记、被删除或由谁删除，也不自动重建/分配遗漏ID。若用户提供原记录，再按证据定位。

## 12. 事实源与维护

内部工作区唯一未完成事实源是根目录的`workbar-private/TODO.md`（从本文目录定位为`../../workbar-private/TODO.md`）；本轮只读，CODE-042和其他条目/编号完全保持。公共`code/TODO.md`是用户批准的脱敏非执行摘要，不能据它启动任务或恢复内部路线。外部clone缺少私有源时只按显式授权工作，不重建清单。

本指导原地维护设计，不另建平行计划。实现事实以Git、实际源码及可复现证据为准；设计修订/查证完成可以记日志，但不得写成产品完成或新增范围已确认。未来只有获批阶段完成相应验收后才能按canonical规则收口，不能因本文更新而移动、删除CODE-042或创建关联事项。
