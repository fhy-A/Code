# 2026-09-06 开发日志

## 2026-09-06 13:25 · Codex

### CODE-074 · Skill 阶段检查点与优化延期决定

- 用户明确授权“先提交并记录现状，留作日后优化”。R106 将已审阅的 R098–R105 差量作为一次本地阶段检查点，覆盖总览与详情布局、管理入口收口、描述与详情读取、新 Skill 模式默认值、启停即时反馈及后端元数据重复计算精简；仅纳入22个明确列出的实现、合同、测试和开发日志文件。
- 该授权仅解除本次检查点的暂不提交限制，不表示视觉或真实启停速度验收通过，也不关闭 CODE-074 总体事项。用户已确认处理中提示能立即出现，但完成仍慢；进一步启停性能优化按用户决定延期，剩余执行状态记录在私有 TODO。
- 复用既有验证：R105 最终相关回归 `310 passed, 2 deselected`，同基线合成启停链从15.96秒降至13.89秒、约13%，响应及291个持久文件字节一致；完整对象验证仍为310次。此前总览、默认加载、前端反馈和恢复兼容证据见本日对应条目。本次仅核对既有差量、白名单、Git diff和暂存内容，未重跑测试或扩展验证范围。
- 合成收益有限，后端仍有秒级成本，真实启停写入耗时未测，当前日常实例尚未重新加载此次后端修改。R100两项自然任务未观察到 Skill 加载、既有夹具熔断及 Office 超时边界继续保留，不宣称体验或系统整体通过。
- 本阶段不重启3010/3011、不追加真实模型或 Office 样本、不切换真实 Skill、不修改运行数据，不推送、打标签或发布；受保护和运行时未跟踪数据不纳入提交。

## 2026-09-06 13:11 · Codex

### CODE-074 · Skill 启停后端元数据重复计算精简

- R105 在原分段夹具中补充文件读取、对象校验、元数据规范化和锁获取等待观测。先完成31项/217文件合成状态，再将同一generation38状态逐字节复制到独立前后对照根；一次对照约25秒，避免重复约289秒的完整准备与测量流程。真实3011的generation39由用户控制，本轮未访问或恢复成合成代次。
- 产品仅修改`skill_store_v2.py`的纯计算路径：日志验证已经取得合法base/request后直接重建target，避免再次规范化同一份值；target仍完整验证并与日志逐字段相等比较。返回时复用本调用已经生成的独立嵌套副本，没有缓存、共享可信结果或跳过文件检查。公开`make_target`仍先验证输入。
- 新增副本隔离测试发现旧v1规范化器保留嵌套引用，已为该兼容分支显式保留深拷贝；转换、启停和带包操作均验证修改输出不污染输入，再次读取被篡改输入仍拒绝。初轮该反例失败后修复并保留断言，不以减少安全检查换取计时收益。

| 同一起点的合成服务阶段 | 修改前（ms） | 修改后（ms） | v2 registry规范化次数（前→后） |
|---|---:|---:|---:|
| 列表 | 2492.72 | 2323.60 | 115→78 |
| preview | 4477.18 | 3804.40 | 230→156 |
| apply及附带snapshot | 11484.56 | 10081.29 | 493→334 |
| 启停链合计（preview+apply） | 15961.74 | 13885.69 | 723→490 |

- 本次启停链合计约减少13%，收益有限，仍有秒级等待。该合成无竞争样本中锁获取合计约45ms以内，主要时间在读取/校验与元数据计算；各函数计时是含子调用时间，不能将其全部相加。绝对耗时不能与不同时间段的R103或真实GET/preview直接比较，也不构成真实提交耗时结论。
- 前后数据根及起始registryHash相同；响应字节、最终291个持久文件字节完全一致。文件读取、JSON读取、对象验证、journal/lineage检查和锁获取次数逐项保持；启停链完整对象验证仍为310次。没有更改API、持久格式、owner/锁、CAS、同key幂等、原子提交、来源或运行准入。
- 冻结后的管理/Store/HTTP/前端响应及启动恢复相关回归`310 passed, 2 deselected`；排除两个性能用例，启停分段在本轮单独完成，旧读取性能用例复用R099证据，未删减正确性场景。包含并发、旧代次、同key重试、响应丢失、损坏与请求内变更、持久化边界及重启恢复。语法、frontend freshness与diff检查通过；原15项未涉及的已有tracked差量保持。
- 沿用已验证环境，未额外运行doctor、Office或真实模型样本，没有操作真实Skill/运行数据、重启3010/3011、提交或发布。R103即时反馈与最终状态确认保持；用户已反馈“处理中立即出现，但完成仍慢”，体验未判PASS，当前后端修改尚未在日常实例重新加载。

## 2026-09-06 12:40 · Codex

### CODE-074 · Skill 启停即时反馈与写响应列表复用

- R103 在新合成31项、217文件、generation37数据中先测完整启停链，再仅修改前端：卡片与详情在预览请求前立即显示“正在处理… / Processing…”，保留原开关值，服务端确认后才更新。单操作执行覆盖预览、提交和原操作重试，重复点击共享原操作；处理中不展示尚不需要的重试入口，失败后保留原 operationKey。
- `set-enabled` 成功响应的列表仅在协议、服务实例、数据根、代次、列表结构、目标状态及收据匹配当前请求时复用。operationId和requestHash按现有协议核对；更晚代次可以展示权威新状态，已知旧代次、切根/实例或期间重新检查均不覆盖新状态。缺失/无效列表继续原刷新路径；刷新失败明确提示状态暂不可用，不自动重复提交。
- 后端 API、manager、Store、锁、CAS、原子提交、对象完整性及恢复均未修改。优化省掉写响应之后的冗余全量 GET，同请求链完整对象检查372→310、registry规范化820→705、journal规范化271→233；提交自身的检查次数保持。

| 合成服务调用阶段 | 修改前（ms） | 修改后（ms） |
|---|---:|---:|
| preview | 1251.82 | 1251.18 |
| apply及附带snapshot | 3296.42 | 3418.31 |
| 后续重复列表 | 814.00 | 省略（独立对照766.89） |
| 实际采用的调用分段合计 | 5362.24 | 4669.49 |

- 两次均为同机同规模新合成根、generation37→38的单次对照，计时断言与正确性分离，没有跨请求安全缓存。上述合计不包含真实 HTTP/模型耗时，也不是用户日常实例的启停写入实测；事务本身仍有秒级成本。
- 最终前端/设置/i18n定向 `42 passed`，包含真实管理服务响应跨到JavaScript验证、确认取消、重复/不同点击互斥、预览及提交失败、响应丢失原 key 重试、前后在途 GET、切根/实例/详情和代次变化。doctor10项、构建/freshness、语法及diff检查通过。原11项未涉及的tracked差量保持，受保护运行数据未访问。
- 真实DOM使用隔离的本地合成页面和现有组件/CSS验证中文深色、英文700px、卡片与详情处理中及最终状态，开关在处理期间保持原值；只有一次初始GET、预览和提交各一次，无额外列表读取、页面错误或横向溢出。浏览器context/process正常关闭，没有打开额外服务端口或操作用户内置标签页。初次临时页面遗漏Code命名空间导致夹具错误，补齐后通过；未修改产品来掩盖。
- 没有切换真实Skill、修改运行flags/profile、重启3010/3011或追加模型样本，未提交或发布；同源用户视觉PASS仍待确认，R100自然加载与Office剩余边界保留。

## 2026-09-06 12:13 · Codex

### CODE-074 · 新 Skill 存储准入与主模型加载默认开启

- R102 仅调整 `server.py` 的两个开关解析：immutable admission 与 model loading 未设或空值时默认开启，显式关闭及非空未知值仍关闭。completion enforcement 独立且默认关闭，旧 activation 等其它开关未改；三个启动入口复用原初始化接线，没有新增配置结构、迁移或恢复机制。
- 默认开启仍由现有 DataDirOwner 保护首次初始化；原格式不自动转 managed-v2、不替用户选择冲突来源、不重置启用/选中/修订。合成测试验证本地修改、旧 tombstone、已禁用安装与固定对象保留。打包新安装沿用原 bundled sync 后可用；直接 server / Dev 指向缺少旧 Skill 目录的空根继续保留 `legacy-root-missing` 来源阻断，共享开发目录来源确认要求不变。
- 新进程默认/空值/显式值、模块导入零初始化、直接 server/Dev/launcher 的真实 owner 与 Skill 启动段、旧数据/待恢复/损坏、HTTP 协商和独立 OFF 等最终专项 `52 passed`。监听、托盘及无关后台工作隔离，数据为全新合成临时根。显式关闭新准入仍能读取 v7，旧请求不静默换协议，浏览器旧停用名继续缩小模型目录。
- 相邻回归 `162 passed, 136 deselected, 13 subtests passed`，覆盖 v5/v6/v7、管理 HTTP、激活/完成与入口兼容。首轮79项通过后，两项旧 v6 样本构造被新默认的协议门禁拒绝；仅将旧 v5/v6 专用构造夹具明确设为对应旧模式，v6 构造后即恢复新默认进行恢复断言，没有全局关闭新默认或放宽产品门禁。
- 原 C3 v6 启动专项也显式选择旧加载模式后通过：ON 的 v6 等待授权、OFF 恢复原 v6 并新建 v5、再次 ON 新建同修订 v6；损坏 ON 不发布监听，OFF 可用普通路径、需重启恢复固定对象且可无 reader 取消。受控 ready 超时的子进程与两个端口关闭通过；使用原模拟 HTTP 夹具，没有真实模型请求。
- doctor10项、Python语法、前端构建 freshness及diff检查通过；R098-R101六个既有实现差量逐项比对保持。当前合同已同步默认值与回退边界，未改现有3011 flags/profile/选择、未重启真实服务、未访问保护数据、未提交或发布。R100两个自然任务未观察加载及既有Office限制不因默认化改变。

## 2026-09-06 06:29 · Codex

### CODE-074 · 真实模型短样本与 Skill 设置反馈收口

- 用户手动重启原 3011 后，R099 后端已生效；同源核对新实例、原数据根、generation34、31 项描述及31/31启用。审批侧首次管理 HTTP 观察为目录4479ms、详情3864ms，仍有秒级成本；不能把此前合成数据计时称为真实 Dev 冷启动表现。
- R100 复用原登录、`deepseek-v4-flash-vision-exp`、自动权限与最高推理，在06:08:42至06:17:42完成恰好四个新 Session/Run，均达到 completed，12轮模型用量均有报告，合计116216 Tokens。普通翻译正确且未加载 Skill；计划任务与读取工作单后审查均完成业务回答，但没有自然加载 Skill。两者目录与 `use_skill` 已提供，不据此认定系统故障，也不将未观测结果计为自然选择通过。
- 显式 `/code-review` 审查找到数量未乘入金额的关键问题，保存 `origin=explicit` 的真实加载收据，五次执行的已加载前缀均为1；这证明显式路径，不是主模型自主选择。七个合成输入文件 hash 不变，四个新会话及原始证据保留。模型测试结束后没有补题、修改提示词或复跑；R097 的前置失败及旧夹具熔断事实保持不变。
- R101 按用户截图与补充反馈，将全局“管理 Skills”移至总览顶部，详情不显示导入和浏览器停用偏好；总览隐藏冗余返回入口，详情提供“返回全部 Skills”。补充中英文用途说明，并让管理按钮换行时自适应高度；只调整前端布局、文案和对应测试，原管理操作、确认、API及未结清操作恢复语义保持。
- R101 定向管理/设置测试10项与i18n检查6项通过，构建/freshness、语法及diff检查通过。原内置 Browser 实看普通宽度与700px窄屏管理按钮、总览/详情入口、英文文案和左右独立滚动；未执行导入、偏好迁入、安装、卸载或版本操作。界面已恢复中文、原深色主题、原尺寸及实际 Desktop 浏览目录，空白待输入，停留在31项卡片总览，内置标签页保持打开。
- 本次界面可供用户复核，尚未取得最终视觉 PASS；没有创建提交或发布。真实模型结果与输入恢复证明、R101截图均保留于本轮临时证据根。

## 2026-09-06 05:55 · Codex

### CODE-074 · Skill 管理展示读取性能定向优化

- R099 在3个后端文件内完成管理展示专用的同请求读取：一次持有原读锁并核对 committed、root/registry/lineage 同代，实际返回内容仍通过原固定修订 reader 的前后完整校验，响应前再次核对 root/registry。复用仅存在于当前查询，没有持久或跨请求缓存；模型准入、执行、写入 CAS/幂等、来源确认和恢复路径未放松。
- 目录一次集中投影可选 `description` 字段，兼容原字段；新前端收到描述即不再为31张卡片分别请求详情，旧后端缺失字段时保留回退。HTTP 详情的 root 校验并入同次查询，资源读取路径保持原检查。查询异常清空部分目录投影，未结清事务拒绝展示为可用；没有迁移、删除或重写 Store/Run。
- 直接相关旧 Store、管理 API/HTTP、恢复、安全与前端回归 `224 passed`，最终读取视图/前端/固定31项计数组 `11 passed`。覆盖只读/旧格式、root 与 generation 漂移、非当前保留修订、目标与非目标文件篡改、未完成事务、无跨请求复用和读取零写副作用；原 runtime reader 的全库完整性拒绝仍成立。初轮一项测试误按底层异常通用消息匹配错误码，改为断言公开 `code` 后通过，没有修改产品错误处理掩盖失败。
- 同机合成31项、217文件、generation34样本中新服务对象的首次请求：旧无描述目录933.18ms、旧详情2102.19ms；新含全部描述目录764.69ms、新详情409.26ms，目录与详情并发合计1138.30ms。详情完整对象检查从95次降至2次；新目录对31个实际返回修订各做原 reader 的前后检查，共62次，同请求 lineage 检查一次。没有使用应用跨请求缓存；这些不是原 Dev 重启后的实测，也不是磁盘缓存冷启动压测。
- 构建/freshness、语法与 diff 检查通过，静态结束复核无阻断。原 Dev 仍运行旧后端，尚未重启、提交或发布；原模型短样本仍为0，不把管理查询优化当作模型自然选用已验证。

## 2026-09-06 05:49 · Codex

### CODE-074 · Skill 总览与详情展示、真实模型样本前置阻断

- R097 在原同源 Code Dev 准备四个短样本后，正常页面显示模型列表刷新失败、模型选择为空；公开路由投影的 `credentialsAvailable=false`。尚未发送第一题、未启动观察计时即停止，四项真实模型行为全部未覆盖；没有把该信号推断为 Key 失效或频繁切换的因果。临时项目目录已通过正常配置接口恢复为原 Desktop，87 个会话未新增，合成工作文件不变；不自动复跑。
- R098 按用户追加交互完成默认全部 Skill 卡片总览：展示名称、用途与启用开关，点击进入左右独立浏览的目录/详情；保留搜索和返回全部入口。详情区保留常用操作，高级管理折叠来源、版本、回退和卸载；没有额外依赖时只显示说明，有依赖时保留原检查、安装、修复与未结清操作处理。
- 前端继续使用既有管理 API、确认、CAS、幂等和失败重试；展示内容按服务实例、数据根、安装身份及修订复用，不复用启用/选中状态，也不将展示预取作为模型指令加载。旧接口缺少描述字段仍有回退。已验证 515 项前端/接口相关测试、4 项子测试；新增展示请求共享及启停失败不乐观翻转的定向组 3 项通过，构建/freshness 与语法检查通过。
- 原内置 Browser 完成中文深色、英文浅色、700px 窄屏、默认总览、搜索空结果/筛选、详情切换、高级管理键盘折叠、无依赖及 XLSX 有依赖入口的观察；未执行安装、修复、卸载或回退。主题、语言与窗口尺寸已恢复，内置标签页保持打开。视觉确认尚未交付为最终 PASS，不创建提交。
- 用户反馈加载缓慢后，只读实测旧后端 snapshot 为 8429ms、compact-chat detail 为 27040ms。代码确认管理读取重复进行 registry/lineage 和完整对象检查，逐卡详情请求又放大该成本；前端合并在途请求、单队列和进入详情停止预取仅是缓解，不能称为首次读取性能修复。验收期间现场已到 generation34，31/31 启用；保留现状，不覆盖回 generation32。

## 2026-09-06 04:56 · Codex

### CODE-074 · Code Dev 持久 Skill 启用与零推理检查

- 用户批准复用现有开发实例并明确空闲窗口。R095 自动重启命令在执行前被工具策略拒绝，未停止服务或初始化数据；用户随后自行按启用参数启动原 3011。本轮 R096 复用该实例，没有再重启、复制 profile、新增端口或挂接 HTTP 拦截夹具。
- 用户启动产生 generation 0 的 immutable Store；本轮通过现成管理 preview/operations、CAS 和固定幂等 key 完成 managed-v2 转换及 31 项同修订来源确认。最终 generation 32，31/31 binding ready，安装均 selected/enabled；未编辑、升级、安装或替换任何 Skill。
- 与启用前基线相比，31 项修订和 200 个旧 Skill 文件字节一致；1257 个旧 Session/Run 等文件的路径、大小和修改时间元数据在用户初始化及本轮管理前后均无变化，没有读取历史正文作验收。新存储及其事务数据是运行数据，未暂存。
- 原 `localhost:3011` 页面保持登录及原模型、权限、推理选择。设置页用正常“重新检查”刷新外部管理操作前的缓存后，31 项可见且管理入口可用；点击正常新会话入口后保持空白待输入，87 个可见会话未增加，没有新增记录、Run 或模型消息。零推理检查不等于真实发送或自然选用通过。
- 原 `/api/skills` 在已存在 Store 时按合同返回 `409 management_client_upgrade_required`，最终核验使用既有 managed snapshot 与真实页面，未绕过旧接口门禁。回退应关闭新加载准入并保留 immutable store、reader、固定对象及 v7 历史，不删除/移动 Store，也不伪改旧 Run 版本；本轮未演练回退。

## 2026-09-06 03:46 · Codex

### CODE-074 · 真实前端验收被夹具阻断并熔断收口

- R091 使用 R090 原样冻结内核启动全新隔离配置页；用户经正常 UI 登录后，模型、权限、推理与公开路由核对一致。R092 在同一 host 冻结四个精确自然任务及模型不可见的验收标准后，一次启动 20 分钟执行窗口：北京时间 03:39:44 至原定 03:59:44，配置时间未占用该窗口。
- 首题普通翻译由内置 Browser 自然提交，创建 1 个 Session 后显示 `canary_http_locked`；AgentRun、ModelRuntimeRun、实际模型请求均为 0。随后目录刷新提示保留为原始结果，不据此认定真实账号或 Key 发生变化。仅有 HTTP 拦截计数，具体被拦请求路径未捕获，保持未知，不再扩查或猜定。
- 立即停止本轮，剩余三题未提交，无重试；普通翻译无 Skill、计划任务自然加载、中途业务加载与显式加载四项真实行为均未覆盖，不能称为模型选用失败或通过。R090 的 30 项/373 条断言仅为后端 HTTP 和模拟上游定向覆盖，未跑通真实前端发送入口，不是用户 UI 复跑前的完整等价证据。
- shutdown 后正常清理未在内部预留内完成，硬截止以退出码 3 结束 host，正常清理报告未生成；明确保留非优雅退出。监督器和外部进程/监听核查均确认本次进程已退出、端口关闭，进程内 worker 随进程退出；实际重新取得再释放 DataDirOwner，执行租约经 canonical helper 释放。不得据此声称线程级正常 join 或正常清理通过。
- 七个合成工作文件及冻结内核 hash 未变，原始 Session、计划、逐次状态和退出证据保留。日常实例、默认 OFF、旧资料和受保护数据未操作；无产品/夹具修正、额外 doctor、独立 Chromium、提交或发布，内置标签页保持打开。
- 连续两次用户配置后的真实验收均被夹具阻断，本路径进入熔断，不自动第三次复跑、修夹具、开页或重启；只完成本轮事实归档，后续取舍保留在私有 CODE-074。

## 2026-09-06 03:14 · Codex

### CODE-074 · 验收夹具离线修正与分段计时

- R089 首个真实验收请求在准入前因临时夹具错误的工具信封被拒，未创建 AgentRun、未调用模型；按停止条件结束并保留失败，不能计为真实 Skill 验收通过。R090 在全新离线根复现该错误，修正后保留 canonical `schemaVersion/names` 结构，只收窄名称；移除 task 时复用既有 canonical helper 同步收窄对应提示块，没有修改产品准入、选择器或权限协议。
- 冻结同一份夹具执行逻辑后，30 个定向离线场景通过。真实 HTTP 准入与本地模拟 SSE 走完 v7 Run、普通读取、Skill owner 加载、固定资源读取、重复加载原收据、终态与恢复；加载前后执行前缀分别为 0/1。非法信封、禁止工具、外目录/附件/资源路径、身份与连接漂移均在相应副作用前拒绝；重复请求不新增 Run、worker 或推理。
- 确认与放行绑定非秘密 routeRef、connectionId、catalogRevision、模型、权限、推理投影和连接地址，不以一个布尔值代替实际绑定。每个 ModelRuntimeRun 的用量独立核对，中间轮缺报告不能被后续已知汇总掩盖；16 万报告 Token 仍是软阈值。离线测试证实仅设置 maxRounds 字段不足以限制前台实际请求数，故夹具另在第 6 次模型请求前停止；单 Run 四分钟、最多四个新 Session/Run、输出和上下文上限均有定向证据。
- 配置与登录最多 60 分钟，确认及执行前检查完成后一次性启动 20 分钟模型窗口，总存活最多 80 分钟；重复放行与刷新不重置。清理预留包含在上限内，正常超时和清理卡住时的硬截止均用可控时钟与实际子进程验证。最终批次进程/监听全部退出，数据目录 owner 逐一重新取得再释放，强制退出场景的非零码为预期结果。
- 90 个隔离 app 文件与已提交基线一致，旧失败资料原样保留。未修改共享产品/测试、安装依赖、重跑独立 Chromium、启动新用户页或调用外部模型；原 doctor 关闭超时边界仍保留。此结果只证明夹具的离线链路，不等于自然模型选用、浏览器或生产全量通过；暂不提交，下一步等待离线复核。

## 2026-09-06 01:22 · Codex

### CODE-074 · 主模型按需加载与普通工具轨迹

- 按 R084 完成默认关闭的 `model-driven-v1`：服务端只向主执行模型提供可用 Skill 的名称与真实描述，由该模型选择不加载、首次加载或追加一个 modifier；支持先做普通工作再加载。新链不调用旧格式/关键词语义路由，不增加选择模型、owner 切换、卸载、权限恢复或调度层。仍以单独工具调用、最多一个 owner 加一个 modifier 为界。
- 按用户追加要求与 R085 修订，四个旧仅显式 Skill（dispatching-parallel-agents、subagent-driven-development、executing-plans、writing-plans）在新模式中按普通 Skill 供主模型选择，并去掉当前新模式的“仅显式调用”提示。`/skill` 仍是显式独占；旧协议的静态资格、task/child 限制和权限模式未改写。
- 新 Run 使用 v7 与有界 loading/v1，旧 v5/v6 不迁移。正文、固定对象身份、收窄工具、当前 lifecycle、完成计划和原调用成功收据在一次持久提交中成立；每次工具执行保存已加载集合的前缀，先前普通结果不能满足新 owner 的 evidence/v2 / completion 要求。保留原任务委托限制与依赖保护；预算、目录变更、混批、待决事项、无效追加和写入失败均有明确失败路径。新二进制在 flag OFF 时仍读取 v7；旧二进制回退必须排空，不能改写记录冒充兼容。
- 耗时标题移除 Skill 胶囊。实际加载沿用普通工具轨迹，显示“加载 Skill / Load Skill”和名称；显式加载使用真实操作事件，不伪造模型调用。移除这类工具的重复函数名进度兜底；没有加载事件的旧记录不补造轨迹。协议、失败与回退细节见 [主模型按需加载 Skill 合同](../../skill-model-loading-contract.md)。
- 验证均使用业务 import 前设置的新合成 DATA_DIR / TEMP / TMP。doctor `10/10`；资格调整后的加载与 D2 定向组 `68 passed`，其中包含 v7 离线真实 pip 安装、安装后复检绑定、绑定持久化失败后只复检恢复、继续执行和原授权/late-install/未结清写者门禁；最后的加载定向 `42 passed`。扩大相关 Skill、AgentRun、协议、取消、历史恢复与子任务回归 `781 passed, 458 subtests passed`。前端/i18n 最终 `513 passed, 4 subtests passed`；inventory 与五类 replay 组合 `55 passed, 77 subtests passed`；构建、freshness、语法与 diff 检查通过。没有把这些相邻回归称为生产全量 pytest。
- 内部 H4 使用明确标注的模拟模型选择，执行真实 HTTP / 前端 / Skill / AgentRun 链。中文深色、英文浅色共 10 个运行覆盖中途 owner+modifier、显式加载、失败、无 Skill、旧 v6、刷新与 OFF 展示。完成验收同时等待 UI 停止态、成功行脱离 running/pending、保存请求结清；普通加载行与旧记录无伪造/重复断言通过。原耗时/模型提醒专项的 bundle 与 classic 路径、折叠键盘交互通过。独立浏览器、worker 与监听均关闭，未程序化关闭 Codex 内置标签页。
- H4 早期夹具曾遗漏静态资源、路由/Runtime 轮询，并提前采样完成画面；已在夹具内部修正。早期日志中的连接中止发生在 Session PUT 的响应写回，导航时点支持该解释，但旧日志不具逐请求因果证明。补充请求观测与终态/保存等待后的完整通过根，浏览器中止记录为空、服务 stderr 为空；原日志保留，未修改生产 HTTP 异常处理。
- 新 flag 保持默认 OFF，没有启用日常 profile、改动真实模型/Key/权限选择、操作 3010/3011、push、tag 或发布。模拟上游不是新的真实模型自然任务验收，R081 的自然匹配失败和未覆盖旧 A 连续性不据此改判；既有 Office 超时、后代退出证明及其它未完成边界继续保留。
