# 2026-08-12

## 2026-08-12 09:54 · Codex

### Workflow v2 风险分级审批与长期 Skill 收口

- 根据审批 Agent 长期真实协作复盘，将个人 `$approval-relay` 从统一重型门禁升级为 Workflow v2：新增 `QUICK / STANDARD / STRICT` 三档风险、风险不确定时上调、低风险明确修改请求不重复确认、标准任务一次方案确认、严格任务完整兼容/失败/回退审批。审批层只固定产品行为、范围、不变量、验证等级和停止条件，具体函数、选择器、文件拆分与测试组织默认交给开发 Agent。
- 新接力使用紧凑 `[approval-relay/v2]` 差量信封，记录风险、基线、目标、不变量、范围预算、验证等级和新增停止条件，并引用项目规则而不重复通用安全模板；`ownerTaskId / sourceRef / targetTurnId / instructionKey` 提供确定性防重证据，既有 v1 `relayId` 仍可恢复但绝不重发。协议明确跨任务工具没有原子所有权锁，因此只能宣称 duplicate-resistant，不能虚假外推 transactional exactly-once。正式发布门禁新增冻结基线与带稳定队列项 ID 的 next-cycle 全量快照，新截图、样本、需求或文档不得静默改写候选版本。
- 根目录及 `code` 的 `AGENTS.md` / `CLAUDE.md` 已同步风险分级和发布冻结规则；`code` 规则同时将验证范围按 QUICK 定向检查、STANDARD 相关回归/构建/必要人工 PASS、STRICT 兼容/恢复/安全及阶段冻结扩大回归分级。诊断只读、共享工作区保护、阶段独立提交，以及 push、tag、release、删除和线上变更的明确授权边界均未降低。
- 原 544 行未跟踪 `docs/approval-agent-collaboration-manual.md` 的稳定原则已压缩迁入个人 Skill，随后从仓库目录移除，结束长期“位于仓库内但所有任务必须排除”的中间状态。该文件从未纳入 Git，因此本次没有删除任何已跟踪项目历史。
- 统一验证入口尚未实施，已作为独立 `verify.py quick/ui/runtime/release` 事项写入 `TODO.md`；后续应复用现有 pytest、frontend、Harness 和发布检查并让 `release.py` 共用验证定义，不与本阶段规则调整混合。
- 本阶段未修改业务代码、测试、`release.py`、产品协议、Session/JSONL、持久化或线上配置。个人 Skill 已通过官方 `quick_validate.py`，并用两轮独立只读前向测试覆盖 QUICK 文档修正、STANDARD 主观滚动、STRICT schema/发布冻结、空开发任务首 relay、双审批冲突、标题失败和压缩恢复；测试暴露的审批写入歧义、首指令来源、防重摘要、非原子所有权声明和冻结队列恢复均已修正。四份规则同步、手册不存在和 `git diff --check` 已核对，未运行不相关的代码测试、构建或完整 pytest，也未执行 push、tag 或 release。

## 2026-08-12 08:45 · Codex

### Goal 复杂长任务规划、执行与验收开发指导落盘

- 新增 [《Code Goal 复杂长任务规划、执行与验收开发指导》](../../goal-mode-development-guide.md)，将 Goal 固化为跨多个既有 AgentRun 的持久任务契约：一轮结束后只有在目标未完成且仍能安全推进时才自动续跑，每一步必须以可复核证据满足验收条件，不能以模型口头自报替代完成门禁。MVP 每个 Session 最多一个 active Goal，完成后停止续跑并保留历史，普通后续消息不得静默重启。
- 顶层计划步骤统一为 `pending / in_progress / completed` 三态且最多一个 `in_progress`；阻塞、失败、重试和等待用户只作为内部运行信号。UI 只展示整体步骤及描述、当前阶段和完成情况，不增加编辑、暂停、恢复、清除按钮或预算、耗时、阻塞/失败元数据；用户通过普通消息补充条件、询问状态、暂停、恢复、修改或清除目标，重大范围、验收结果和权限边界变化仍需明确确认。
- 文档明确区分官方公开事实与本项目规范：只引用官方 `Follow goals`、`Long-running work` 和 `Developer commands` 三篇页面作为产品启发，不把官方未公开的状态机、token 预算、工具协议或算法写成事实。Code 选择消息驱动控制和最小三态 UI，不照搬官方页面展示的全部管理命令或控制界面；`/plan` 用于澄清目标、约束和验收，`/goal` 用于持久执行闭环。
- 既有 [Agent 重构执行计划](../../agent-refactoring-execution-plan.md) Stage 3 保留为历史架构草案，但已增加醒目规范说明：旧五态示例、预算字段和控制台式交互在冲突处让位于新指导。`TODO.md` 的 P2 结构化计划事项已链接新文档并准确保留为未完成，明确 `/goal`、Goal 工具、持久化 reducer、自动续跑和最小计划 UI 尚未实施。
- 本次只创建 Goal 指导并同步旧规划、TODO、本日志和索引五份文档；没有修改业务代码、测试、运行时协议、Session/JSONL 或真实数据，没有运行代码测试或构建，也未执行 push、tag、release、线上配置或凭据操作。

## 2026-08-12 06:51 · Codex

### 历史消息“一键到底”与滚动接管收口

- `#messages` 的自动跟随、用户滚动接管、历史位置保持与按钮显隐已集中到 `src/ui/messages.js` 的单一滚动控制器；用户离开真实底部超过 2px 几何容差即交出跟随，离底达到 160px 才显示入口，按钮已出现后持续到真正回到底部。点击会瞬时回到最新消息并以 `preventScroll` 将焦点交回输入框；刷新、重新打开或切换会话仍显式定位最新消息，不持久化历史滚动位置。
- 轻量悬浮按钮保持 40px 固定几何：当前会话前台任务运行中显示严格水平等距、同静态基线且错峰跳动/明暗变化的三点，空闲时显示向下箭头，状态切换不改变按钮尺寸或位置；表面、边界、图标、hover、active 与 focus 均由相对主题混色适配明暗主题，`prefers-reduced-motion` 使用静态三点。动态 composer 安全区、授权面板抑制以及问卷、附件、2～5 行输入和移动尺寸避让保持一致。
- 有效向上 wheel/touch 意图会在浏览器真实 `scroll` 前交出跟随并取消待执行 RAF；新增 `awaitingUserScroll` 保护短会话轻微溢出，在首次真实 `scroll` 到达前，连续流式内容、ResizeObserver、图片或工具布局回调只能重算按钮，不能用旧底部覆盖浏览器已产生的小幅位移。无可滚内容或最大范围仅在 2px 容差内不会进入等待；普通点击、轻点、2px 抖动、横向手势、`Ctrl+wheel` 与向下 wheel 不改变跟随，真实滚动、点击到底、会话重置和 disconnect 均安全清理等待状态。
- 默认 bundle 与 direct classic 复用同一控制器、样式和行为；本阶段没有改变 Session/JSONL、服务端/API、工具协议、持久化、安全或授权边界，也没有加入 smooth、计时延迟、`preventDefault`、全局输入监听、未读计数或滚动位置持久化。
- 最终自动门禁通过：控制器定向为 `3 passed`；`tests/test_frontend_modules.py tests/test_p0_stability.py` 为 `211 passed`；bundle/classic 定向为 `3 passed`；`npm run check:frontend` 完成构建、freshness、classic fallback 与 bundle Node 语法检查；完整 pytest 为 `1138 passed, 751 subtests passed, 3 warnings`，三条 warning 均为既有损坏 TIFF EXIF 负测；`node --check app.js`、`node --check src/ui/messages.js`、`node --check src/core/i18n.js` 与 `git diff --check` 通过。
- 人工验收覆盖长流式输出、运行/空闲图标、按钮位置与明暗主题、首次向上滚动、短会话轻微溢出及完成后布局变化；经过滚动回弹、三点排列、首次输入竞争和短溢出保护修订后，用户最终明确反馈“OK，通过”，据此记录阶段 B 人工 PASS。相邻“新消息发送后的阅读锚点与流式滚动”仍是独立未完成事项，不在本阶段实现或标记完成。
- 本阶段独立提交只收口七个实现/测试文件、`TODO.md`、本日志与日志索引；回退只需撤销该提交。未执行 push、tag、release、线上配置或数据变更，也未开始下一 TODO。

## 2026-08-12 05:21 · Codex

### 输入框主题层级与文件拖放反馈收口

- 输入框表面不再直接复用单一 `--panel-3`，改为由当前主题的 `--bg`、`--text` 与 `--accent` 相对生成 composer 专属令牌：默认表面向文字色混合 6%，hover 为 8%，防御性 `textarea:disabled` 为 3%，真实文件拖放则在默认表面混入轻量 accent tint。普通 composer、empty composer、附件缩略区、2～5 行 textarea 与底部控制栏保持同一连续表面；focus 继续使用既有 accent 边框与 3px 低透明光晕，placeholder 明确使用 `--muted` 且 `opacity: 1`，caret 使用 `--accent`。浅色、深色及非默认主题均通过相对令牌适配，没有为单一主题硬编码固定灰值。
- 文件拖放已收束为 `chatForm` 级唯一 `dragenter`、`dragover`、`dragleave` 与 `drop` 事件链；深度计数避免指针经过 textarea、控制栏或附件缩略图子节点时反复闪烁，只有真实 `Files` 拖放才激活 `drag-active`，移出或 drop 后统一清理。既有 `isImageFileCandidate`、`queueImageFile`、附件接收、单次入队和发送语义保持不变；disabled 仅提供防御性样式，不改变运行中、队列、问卷或授权阶段仍可输入的产品行为。
- 自动验收通过：`node --check app.js`；`python -m pytest tests/test_frontend_modules.py tests/test_p2_coverage.py -q` 为 `235 passed, 4 subtests passed`；`npm run check:frontend` 完成 bundle/classic 构建、freshness 与 bundle Node 语法校验；`git diff --check` 通过。默认 bundle 页面继续加载共享 `/styles.css` 与 `/dist/frontend/code.bundle.js`，direct classic 继续加载同一 `/styles.css` 与 `/app.js`，新拖放逻辑已进入 bundle。
- 用户依据阶段 A 人工验收材料明确反馈“测试通过”，据此记录视觉、拖放与既有发送/附件行为的人工 PASS。本阶段只修改 `styles.css`、`app.js`、`tests/test_frontend_modules.py`、`TODO.md`、本日志和日志索引；不涉及服务端、API、Session/JSONL、持久化、授权、安全边界或数据迁移。回退只需撤销本阶段独立提交。
- 相邻的历史消息“一键到底”仍是未完成的独立阶段，未在本次收口中实施或标记完成；本次也未执行 push、tag、release 或线上配置变更。

## 2026-08-12 02:06 · Codex

### Harness 第一轮综合完成判定与统一文档收口

- 以 H4-8G 最终已提交证据树 `618f876fb7d09f821b302eaf5a731f367fd894ac` 为冻结基础完成综合核对：H4-8D `0e349e3d34264263567d1297732552af5f1f587e`、H4-8E `387a3a3bff5e432f4362998c19680df9ce7e53ac`、H4-8F `8cd6116aa2b7fcc71c5919b90d733a4869a1b12a` 与 H4-8G `618f876fb7d09f821b302eaf5a731f367fd894ac` 分别关闭陈旧编辑冲突、问卷等待态与单项队列刷新顺序、授权请求到达生产 handler 前失败后的单次手动安全重试，以及 detached `/parallel` 编辑授权刷新恢复；五项完成定义均有专题、对等哈希和独立提交支撑。
- 最终门禁沿用 H4-8G 已提交且未重跑、未重基线的真实结果：两轮 H4 为 `67 passed (3.9m)` 与 `67 passed (4.4m)`，均为 1 worker、0 retry、各 67 条 cleanup；相关回归为 `680 passed, 260 subtests passed, 1 warning in 34.74s`，完整 pytest 为 `1126 passed, 751 subtests passed, 3 warnings in 93.48s`；replay 保持 `17/124/25/25/4` 与 suite hash `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`，frontend freshness/verify、Node/Python 语法、diff 与资源审计均按专题闭合。
- [`Harness 第一轮完成线`](../../harness/harness-round-1-completion-line.md) 已从历史“剩余门禁”更新为完成证据矩阵和有限综合判定；统一 [`TODO`](../../../TODO.md) 已移除第一轮已完成差量，只保留 active 跨进程恢复、异构工具、未覆盖失败组合、其他浏览器及其他真实生命周期等后续轮次事项；[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 已增加 2026-08-12 最新状态并撤销过期的“H0-1 是准确下一步”当前指令；本日志和索引同步更新。
- 本次只修改完成线、TODO、工程总览、当日日志与日志索引五份文档，不修改生产或测试文件，不改变 API、AgentRun/Runtime、authorization、Session/JSONL、持久化、安全边界或用户交互，也不需要迁移真实数据。回退只需撤销本次五文件文档提交，不应回退 H4-8D/E/F/G 的已提交测试与专题证据。
- “Harness 第一轮完成”严格限于完成线定义的固定工程边界，不外推为通用 exactly-once、授权恢复完整覆盖、并发/多标签页/多 actor、服务重启或 active 跨进程、Firefox/WebKit、真实外网/模型/凭据、主观视觉或可访问性穷举，也不启动 H4-8H、H5 或任何下一 TODO。
- 文档验收只核对链接、提交、数字、suite hash、五文件限定的 `git diff --check` 与 cached 白名单；没有重跑历史 H4 或其他测试。未读取、修改或暂存既有未跟踪审批协作手册，未读取或覆盖无关活动交接，未清理 ignored outputs 或 `%TEMP%` 证据，也未执行 push、tag、release 或线上配置变更。

## 2026-08-12 01:30 · Codex

### Harness H4-8G detached `/parallel` 编辑授权完整刷新恢复收口

- H4-8G 已为固定同 Session 场景补齐真实浏览器证据：主 `read_file` 进入第二 Runtime 且真实前端 consumer 已附着后，从 composer 提交 detached `/parallel`；background 产生唯一 `propose_edit` 并等待授权，主任务先完成，background checkpoint、authorization、diff 与卡片保持。专题见 [H4-8G detached `/parallel` 编辑授权完整刷新恢复](../../harness/h4-8g-detached-parallel-edit-authorization-refresh.md)。
- consumer fence 只观察 lifecycle boundary 之后、同一第二 Runtime 的真实 GET、numeric cursor、`wait=25` 与 positive wait；它是测试侧只读调度观测，不是 sleep 或产品同步 API，也不覆盖 consumer attach 前的极短窗口。
- waiting reload 严格限定为当前、last-active Session 的完整 page reload，不覆盖 Session 切换、sidebar/list refresh、额外 `refreshSessions` 或非 last-active Session。reload 额外产生 1 次同 `clientRequestId` 的 create transport，但复用同一 background AgentRun；终态为 3 个 durable AgentRun，而 create transport 总数为 4。
- 用户只通过真实 UI approve 一次；authorization/resume 为 `1/1`，proposal/apply/effective write/backup 为 `1/1/1/1`，unsafe 增量为 0，background 两个 Runtime cursor 为 `[4,3]`。终态为 3 AgentRun、5 Runtime、5 upstream chat，usage 通过 waiting、background final 与 follow-up 的阶段 delta 关系证明只累计一次。
- background 只产生唯一 canonical detached final。随后普通前台 follow-up 的实际模型上下文保留主 user、唯一 `read_file` call/receipt、主 final 与 follow-up user，detached marker/state 与 unclassified 均为 0；完整消息只额外扫描七个固定编辑 artifact，`detachedEditArtifactCount=0`，该计数不是通用泄漏扫描。
- bundle/direct classic 共用 lifecycle、投影和十三项冻结哈希；最终 `followupRequestContext` 为 `cb728e9a97ebb86660b16c0089f79ae136847abf36b8f329780789bd94aee8f5`，其余十二项与专题逐项冻结。终态 reload 的模型、工具、授权、resume、apply、write、backup 业务增量均为 0，但仍允许正常 HTTP/GET，不表示零网络请求。
- 相关十文件为 `680 passed, 260 subtests passed, 1 warning in 34.74s`；邻接 H4 十二例为 `12 passed (40.7s)`、1 worker、0 retry；replay 保持 `17/124/25/25/4` 与 suite hash `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。完整 pytest 为 `1126 passed, 751 subtests passed, 3 warnings in 93.48s`，warning 均为既有损坏 TIFF/EXIF Pillow 负测。
- 两轮最终 H4 分别为标准入口 `67 passed (3.9m)` 与独立 infra 后专用 output `67 passed (4.4m)`，均为 1 worker、0 retry、各 67 条 cleanup；frontend freshness/verify、Node/Python AST 与 diff 通过。AST 的既有 `server.py:4949` invalid escape `\C` SyntaxWarning 不计入 pytest 三条 warning。
- H4/isolated-host/Python/Chromium、监听、临时根、fixture、backup 与 pyc 为 0；两枚共享 MCP stdio Node 无监听、不是 H4 残留且未终止。R059M 三个 ignored output 都只含 passed `.last-run.json`，九份 `%TEMP%` 诊断日志继续保留且不入 Git，没有写成“所有临时产物已清理”。
- 本阶段仅修改两份 H4 测试文件，没有生产、协议、Session/JSONL、安全边界或交互变化。证明不覆盖多 proposal/pending/background、多标签、reject、授权/响应丢失、resume/apply/write/backup 失败重试、服务重启/跨进程/crash、真实模型/外网/凭据、Firefox/WebKit、性能 SLA 或人工视觉/可访问性，也不构成通用后台授权恢复、通用 exactly-once 或所有并行失败隔离。
- H4-8G 阶段完成；Harness 第一轮综合完成判定、完成线/TODO/总览同步由用户指定的新审批/开发会话继续。当前按用户要求暂停，本阶段提交不宣告“Harness 第一轮已经完成”，也不启动 H4-8H。
