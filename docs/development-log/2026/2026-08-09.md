# 2026-08-09

## 2026-08-09 00:03 · Codex

### Harness H4-6O forced-final 模型失败终态与刷新唯一性收口

- H4-6O 沿用 H4-6K 的固定同指纹 `read_file({"path":"fixture.txt","startLine":2,"endLine":1})`。前三轮均真实委托生产执行器并失败，第四个新 toolCallId 由生产限流器生成耐久 blocked execution 与唯一 `tool_retry_blocked`、不再真实执行；第五轮 forced-final 请求移除 `tools/tool_choice` 并携带既有恢复指令，隔离假上游随后固定返回 HTTP 502。
- 父 AgentRun 以 `failed/upstream_error` 收敛：24 事件、`nextCursor=24`、`nextSeq=25`、Runtime cursor `[4,3,3,3,0]`、chat 5、生产委托/真实执行 3、耐久 execution 4、blocked 1、unsafe 0；第五 Runtime 独立为 `failed/upstream_error/502/transient=true`，没有第五轮 `model_completed`。
- 终态按既有健康快照回滚为 user 与唯一 `error-recovery` assistant。公共 AgentRun 与 Session 的活动 Runtime 引用清空，v4 原始耐久记录精确不含 `activeRuntimeRunId`；当前页和完整刷新后顶部耗时均恰好一个、主 assistant 页脚无重复时钟，刷新 AgentRun POST/Runtime POST/chat/tool 四项增量全部为 0。
- failed 主任务耗时丢失的根因是先持久化不含耗时的错误 assistant、后在内存 finalize。修正保持 checkpoint 先捕获终态，在同一 `persistRunCheckpoint` / `persistMessages` 保存边界之前对同一个错误 assistant 调用既有 `finalizeRunTiming`，由现有 `meta._responseTime` 序列化路径恢复；没有新增字段、迁移、计时算法、协议或历史回填。
- 完整门禁同时强化两项测试确定性：TIFF 以脱敏 key hash 和主 frame document generation 区分旧页/新页，仍严格要求刷新 GET=1；queue 观察器在主 Runtime/Session checkpoint 有界收敛后启动，仍严格要求唯一 `Control+Enter` 的 transition PUT=1。两者都只调整 Harness 观察窗口，不是生产缓存或队列语义修改。
- bundle/direct classic 九项语义哈希完全一致，H4-6K 旧哈希保持。最终五文件哈希下连续两轮标准 H4 均为 `49 passed`、单 worker、零 retry、exit 0；H4-7B/H4-7C/暂停取消定向 `6 passed`，前端/P0 `232 passed`，完整 pytest `1129 passed, 751 subtests passed`，`check:frontend`、Node/Python 语法、diff 与资源清理通过。文档收口沿用同一哈希的完整矩阵，只重做文档、链接、语法、diff、白名单和资源门禁。
- 完成声明只覆盖固定同指纹行范围失败的三次真实执行、第四次阻断、第五轮 forced-final 固定 502，以及 bundle/direct classic 的 failed 终态回滚、顶部耗时持久化与同进程刷新零重放；不覆盖执行前失败限流、不同参数/工具、其他模型错误或恢复策略、跨进程 active、取消、长输出或外部副作用 exactly-once。长期事实见 [`H4-6O forced-final 模型失败终态与刷新唯一性`](../../harness/h4-6o-forced-final-model-failure.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。
