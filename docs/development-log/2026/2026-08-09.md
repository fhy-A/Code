# 2026-08-09

## 2026-08-09 14:21 · Codex

### workbar 官方文档独立源码仓库与 CI 交付完成

- `../workbar-docs/` 已初始化为独立 Git 仓库，默认分支为 `main`，当前根提交为 `1482fa0 docs: 建立 workbar 官方文档仓库`；首个提交包含 14 篇正文、5 张已脱敏截图、构建器、锁文件、Nginx/发布/回滚资料和编辑计划，共 38 个文件。`dist/`、本地预览 HTML/截图、`node_modules/`、临时目录、环境文件和发布压缩包均不进入源码提交。
- 新增只读权限的 GitHub Actions 文档构建工作流：Node 20 使用 `npm ci` 和 `npm run check` 生成并校验正式站点，再把 `dist/` 根内容打包为带完整 commit SHA 的 `tar.gz`，生成 SHA256 并上传 30 天 artifact；工作流不包含 SSH、服务器凭据或线上部署动作。
- 依赖锁文件已统一到 npm 官方 registry；仓库文本扫描未发现真实密钥、邮箱、Windows 绝对路径、私钥或 JWT。已引用的 API 密钥入口截图改为整栏不透明遮挡，两个仍含掩码密钥片段的旧图在确认无引用后从源码和生产 `dist/` 删除，最终 5 张入库截图人工复核通过。
- 部署手册补齐从 GitHub Actions 下载指定 commit artifact、执行 `sha256sum -c`、解压为 `dist/` 并使用同一 commit 发布脚本的闭环；同时区分 Code 正常需要的 `127.0.0.1:3010` 与不得进入正式文档链接的 8092/8093 本地预览地址。
- 最终 `npm run check` 生成并校验 14 篇正式页面，两个发布脚本通过 Git for Windows Bash `bash -n`，`git diff --cached --check` 和提交后工作区检查通过。已创建仓库 `https://github.com/fhy-A/workbar-docs` 并推送 `main`；根提交在后续作者归属纠正后为 `1482fa0`，原首次构建产物已由新 SHA 的成功构建取代，压缩包根目录仍包含可直接部署的 `index.html` 和快速开始页面。线上 DNS、TLS、Nginx 和后台配置均未操作。

## 2026-08-09 13:11 · Codex

### workbar 官方文档独立生产构建与部署交付完成

- 正式架构确定为 `docs.workbar.ai` 独立静态站：文档仍可部署在现有 VPS，但由独立 Nginx 虚拟主机和发布目录提供，不进入 New API React/Go 嵌入资源、不使用 `FRONTEND_BASE_URL`，文档更新与 API 网关发布、数据库和模型服务解耦。
- 在 `../workbar-docs/preview/` 新增生产构建器和独立交互脚本，继续以 14 篇 Markdown 为内容源，生成 `../workbar-docs/dist/` 的真实目录路由页面、自包含且带内容哈希的 CSS/JS/图片、搜索索引、404、robots、sitemap、canonical、Open Graph、favicon 和 manifest；原有 Hash 单页预览继续可用。
- 生产构建会在替换旧 `dist/` 前完成站内链接、锚点、图片、样式、脚本、搜索索引以及常见真实密钥、JWT 和私钥格式检查；文本扫描不会回显疑似密钥值，失败时保留上一版产物。新增依赖锁文件、根构建说明和 `.gitignore`，`npm ci` 与 `npm run check` 实测通过并生成 14 篇正式页面。
- 在 `../workbar-docs/deploy/` 新增同一 VPS 的 DNS/TLS、Nginx HTTP 引导与 HTTPS 正式配置、安全响应头、HTML/哈希资源分级缓存、版本目录原子发布、回滚、监控和验收手册；两个 Bash 脚本通过 Git Bash `bash -n`，本机未安装 Nginx，因此真实 `nginx -t` 明确保留为服务器生效前门禁。
- 本地 HTTP 核对 sitemap 中 14/14 个页面、搜索索引、品牌 SVG 与关键资源均返回 200，生成站疑似真实密钥位置为 0。内置浏览器实测首页和文章真实深链接、搜索“自动分组”及跳转、代码复制、图片放大/关闭、深浅色、移动目录和 `390×844` 响应式布局；桌面和移动页面均无横向溢出，宽表格继续在自身容器内滚动，页面控制台无站点错误。
- `new-api-source/web/default/public/workbar-homepage/homepage-concept-v1.html` 中顶部“文档”和“查看新手教程”本地链接已分别改为 `https://docs.workbar.ai/` 与 `/getting-started/quick-start/`；没有扩展修改默认页脚、顶部导航行为、服务器、DNS、证书或后台配置，原有 `new-api-source/AGENTS.md` 及 Code 工作区修改均保持未动。
- 剩余线上发布、后台文档链接、真实响应头/404/回滚验收、旧截图脱敏复核和必要的本地 Code 配图继续保留在主 `TODO.md`；`workbar-docs` 当前尚未纳入独立 Git 仓库，正式自动发布前仍需决定源码版本库归属。

## 2026-08-09 11:45 · Codex

### workbar 文档预览内容列宽度统一完成

- 在 `../workbar-docs/preview/build.cjs` 增加单一 `--content-width: 820px` 宽度源，将文章头部、正文、配图、表格容器、可复制代码块与上一篇/下一篇导航统一到同一内容列；宽表格仍保留容器内横向滚动，图片放大和代码复制交互不变。
- 移动端移除了配图、代码块和表格的负边距与额外展宽，这些内容现在与正文使用同一左右边界。重新构建后仍为 14 篇文档，生成页面不再包含旧的 `900px` / `920px` / `1060px` 宽度规则或移动端负边距规则。
- 内置浏览器实测：桌面端正文、标题、配图、代码块和表格均为 `820px`，左右边界一致且页面无横向溢出；`390×844` 移动端各类内容同为左侧 `18px` 起始，宽表格在自身容器内产生横向滚动，文档页本身无横向溢出。代码复制按钮会正常反馈“已复制”，配图大图可正常打开和关闭。
- 本轮只修改本地 HTML 预览生成样式与重建产物，没有修改 Markdown 正文、workbar 线上页面或业务逻辑。

## 2026-08-09 10:48 · Codex

### workbar 官方用户文档本地 HTML 预览完成

- 在 `../workbar-docs/preview/` 新增可重复生成的静态文档站：`build.cjs` 从现有 14 篇 Markdown 构建单文件 `index.html`，默认以文档首页按用户目标分流，左侧使用“开始使用、连接 AI 工具、账号与使用、开发者接口、获取帮助”5 组导航，一次只展示一篇正文。
- 预览页已实现站内搜索、Hash 路由、当前文章二级目录、上一篇/下一篇、代码复制、配图点击放大、深浅色主题、移动端抽屉导航和打印样式；资源完全本地化，无 CDN、线上写入或业务接口依赖。`preview/package.json` 与 `README.md` 记录了重新构建和本地启动方式。
- 最终 QA 将 Markdown 原始 HTML 改为纯文字输出，阻止 `javascript:`、`vbscript:`、`data:`、协议相对地址和未知协议进入生成链接或图片；搜索结果恢复为普通链接列表语义，图片大图和移动端目录增加键盘焦点约束、背景隐藏与关闭后焦点返回。
- 构建过程会阻止失效 Markdown 站内链接和缺失图片进入产物。本轮同时发现 Code 下载、模型广场和钱包三张截图实际为 JPEG 但使用 `.png` 扩展名，已统一改为 `.jpg` 并更新 5 处正文引用，避免静态服务器按错误 MIME 返回后浏览器不显示。
- 自动检查覆盖 14 篇正文和 5 张当前引用图片：每篇一级标题恰好 1 个、代码围栏成对、生成页残留 `.md` 链接 0、疑似真实 `sk-` 密钥 0；全部图片通过 HTTP 200 且 MIME 与扩展名一致。内置浏览器实际验证首页、快速开始、搜索“自动分组”及跳转、文章目录、配图加载与大图、深浅色和 390×844 移动端抽屉，页面无横向溢出。
- 该产物仅是本地信息架构与视觉预览，不等于已选择正式线上文档框架或完成发布。正式公开前仍需按计划补充必要的本地 Code 状态图，并将含模糊用户名或脱敏密钥片段的旧图替换为不透明遮挡或演示数据。

## 2026-08-09 10:16 · Codex

### workbar 官方用户文档首批脱敏配图接入完成

- 使用已登录的线上普通用户账号，以只读方式核对并截取 Code 下载页、模型广场和钱包充值区域；截图期间仅临时隐藏账号头像、余额、累计用量、请求次数和推荐链接，截图完成后立即恢复页面，未创建密钥、未调用模型、未发起支付，也未修改线上配置。
- 新增 `code-download-entry.png`、`model-marketplace-details.png` 和 `wallet-add-funds.png` 三张配图，并分别接入《快速开始》《使用 workbar Code》《模型、分组与自动选择》《余额、价格与使用记录》，共形成 5 处正文引用。模型价格、充值金额和支付方式的图注均明确以页面当前实际显示为准。
- 使用日志中现有记录包含真实请求 ID 和错误详情，因此本轮没有截图；线上个人资料页当前也没有可用于说明 2FA 或 Passkey 的对应入口，本轮没有制造特殊状态或使用不相符的截图。本地 `http://127.0.0.1:3010/` 当前未运行，连接页、主控件和模型同步三张 Code 配图继续保留在后续配图计划。
- 完成图片人工复核和 Markdown 资源检查：新增截图不含用户名、邮箱、头像、余额、推荐链接、完整或部分 API 密钥、订单号及请求正文；全部图片相对路径可解析，缺失资源为 0。`CONTENT_PLAN.md` 已将配图阶段更新为进行中。

## 2026-08-09 09:19 · Codex

### workbar 官方用户文档第一轮全量精修完成

- 完成 `../workbar-docs/` 文档首页与 13 篇正文的第一轮全量精修，并同步收口 `SUMMARY.md` 和 `CONTENT_PLAN.md`。内容覆盖快速开始、workbar Code、Claude Code、其他兼容工具、API 密钥、模型与自动分组、余额与计费、账号安全、常见问题，以及 API 概览和 OpenAI、Claude、Gemini 三类兼容接口；各篇职责、面向新手的动作表达和交叉链接已经统一。
- 按当前产品实现统一关键事实：Code 正常启动或登录时静默同步密钥和模型，“从 workbar 获取”仅打开密钥弹窗，需复制到 Key 列表后重新检测模型；自动分组按后台顺序选择而非最低价；受邀请者奖励直接进入主余额，邀请者奖励需要手动转移；自行注销为软删除且用户名和邮箱不自动释放；系统管理访问令牌不得用于模型调用。
- API 与工具文档已区分网站地址、Base URL、完整接口 URL 和接口路径，并统一三类认证头及客户端自动拼接规则。Claude Code 文档同时记录当前 workbar 已核对基础 `/v1/messages`，尚未提供 `/v1/messages/count_tokens`，依赖该端点或其他新增网关能力的功能可能不可用，不再把基础调用表述成完整功能兼容。
- 交叉审校发现并修正排错页一处旧的“点击即同步”描述，以及 API 概览把相对路径误称为完整接口的问题。最终检查覆盖 16 个 Markdown 文件，其中 `content/` 共 14 篇、2120 行：一级标题和代码围栏异常 0、失效相对链接 0、疑似真实密钥/令牌 0、大写品牌名 0、旧本地网关地址 0、固定价格或奖励数值 0、合并冲突标记 0。
- `CONTENT_PLAN.md` 已将初稿和第一轮精修标记为完成，主 `TODO.md` 只保留脱敏配图、文档站框架接入、桌面端/移动端检查和线上发布；本轮未修改业务代码，也未把配图缺失留成正文占位。

## 2026-08-09 08:45 · Codex

### workbar《管理 API 密钥》首轮精修完成

- 按“平台侧密钥设置、安全与生命周期”的单篇边界精修 `workbar-docs/content/keys/manage-api-keys.md`，将七个分散设置小节和重复问答合并为新手推荐设置表、必要的分组/模型说明、复制分流、生命周期表和泄露处理流程，正文由 181 行收敛为 129 行；本轮不新增截图占位，配图继续按集中计划后置。
- 按当前 `new-api-source` 核对创建行为：未选择具体分组会保存为 `auto`，模型限制候选项随固定分组或自动分组集合动态变化，“无限配额”只取消单把密钥上限，批量创建除第一把外会增加随机名称后缀；自动分组按后台顺序工作，不承诺最低价。
- 按真实列表与后端状态边界核对完整密钥复制、编辑、停用、重新启用和删除：停用密钥只有在未过期且仍有可用额度时才能直接重新启用，删除确认明确不可撤销；正文同步补齐日常轮换与疑似泄露时“先停用、再替换”的不同顺序。
- 明确与 Code 专题的内容边界：workbar Code 首次授权通常静默同步已启用密钥，无需提前复制；手动补充和本地 Key 列表操作统一交给《使用 workbar Code》，Claude Code 与其他兼容工具分别进入对应专题，避免混用 Base URL。
- 最终只读复核为 PASS、无剩余必须修正项；129 行正文含 9 个有效链接、一级标题 1、疑似真实密钥 0、遗留 `TODO/TBD/待补截图` 0。下一篇按计划精修《使用 workbar Code》并修正已识别的三处旧手动同步文案。

## 2026-08-09 08:33 · Codex

### workbar《快速开始》首轮精修完成

- 按“只完成首次闭环”的定位精修 `workbar-docs/content/getting-started/quick-start.md`：保留注册、创建密钥、下载、连接、选择目录/模型/只读模式和首次任务六步，以及两张已经验收的 API 密钥截图；删除前置概念表、自动分组模型集合的高级展开、重复的其他工具板块和过多错误分支，正文由 168 行收敛为 130 行。
- 注册流程改为当前真实行为：固定填写用户名、8～20 字符密码和确认密码，邮箱验证仅在页面显示时处理；注册成功通常直接建立会话并进入控制台，失败回退登录页时只使用预填用户名和刚设置的密码，不再把既有账号的 2FA 流程混入首次注册。
- API 密钥步骤明确未选具体分组会保存为自动分组，到期时间、数量、无限配额和模型限制按首次默认值操作；“无限配额”仍消耗账号余额。正常路径保持“授权后静默同步已启用密钥”，异常路径则按当前 Code UI 写为“从 workbar 获取”弹窗复制所需密钥并粘贴到 Key 列表，不再误写为一键重新同步。
- Code 路径按现行源码核对：授权页点击 `Connect` 后自动返回；连接完成标志为门禁消失并进入主界面，可在“设置 → workbar 账号”查看状态；项目目录位于左侧栏“文件”区域，权限和模型位于输入框下方；首次成功只要求 Code 正常回答，使用日志改为可选复核。
- 最终只读复核为 PASS、无必须修正项；两张图片和全部相对/外部链接存在，一级标题 1、代码围栏成对、疑似真实密钥 0、遗留 `TODO/TBD/待补截图/Base URL` 0。关联的《使用 workbar Code》仍有三处旧的手动补充密钥描述，已写入主 TODO，留待该专题精修时统一修正。

## 2026-08-09 08:08 · Codex

### workbar 官方用户文档全套初稿完成

- 按用户确认的“先完成整体编写，再逐篇优化细节与配图”重排文档工作流，在 `../workbar-docs/` 完成独立的内容规划、文章级结构、统一术语、配图队列、优化顺序和发布前人工复核清单，并新增 `SUMMARY.md` 导航草案与用户文档首页。
- 保留并继续使用已经验收的《快速开始》及两张 API 密钥操作截图；本轮统一“API 密钥”术语，明确 `127.0.0.1:3010` 本地页面与 `https://workbar.ai` 模型服务的区别、自动分组下模型限制留空的含义，并把四处正文截图占位迁移到集中配图计划，补齐真实专题页交叉链接。
- 首轮正文共 13 篇，覆盖《快速开始》、workbar Code、Claude Code、其他兼容工具、API 密钥、模型与自动分组、余额价格与使用记录、账号安全、常见问题，以及 OpenAI、Claude、Gemini 三类兼容接口和 API 接入概览；新文章不含截图占位、固定奖励金额或真实密钥。
- 用户界面与产品事实按当前 `new-api-source`、workbar Code 源码/发布说明及既有真实验收记录核对；三类兼容端点、认证方式和 Claude Code 配置同时按本地网关实现、New API 与 Anthropic 官方资料核对。账号自行注销和系统管理访问令牌已列为发布前重点复核项，不用一般网站经验替代实际规则。
- 自动检查确认全部 Markdown 相对链接可解析、每篇只有一个一级标题、代码围栏和地址写法一致、没有疑似真实 `sk-` 密钥、过期“后续专题”或 `TODO/TBD/待补截图` 标记。业务代码未改动；剩余逐篇事实/文案优化、脱敏配图、最终站点框架选择与部署保留在主 `TODO.md`。

## 2026-08-09 00:03 · Codex

### Harness H4-6O forced-final 模型失败终态与刷新唯一性收口

- H4-6O 沿用 H4-6K 的固定同指纹 `read_file({"path":"fixture.txt","startLine":2,"endLine":1})`。前三轮均真实委托生产执行器并失败，第四个新 toolCallId 由生产限流器生成耐久 blocked execution 与唯一 `tool_retry_blocked`、不再真实执行；第五轮 forced-final 请求移除 `tools/tool_choice` 并携带既有恢复指令，隔离假上游随后固定返回 HTTP 502。
- 父 AgentRun 以 `failed/upstream_error` 收敛：24 事件、`nextCursor=24`、`nextSeq=25`、Runtime cursor `[4,3,3,3,0]`、chat 5、生产委托/真实执行 3、耐久 execution 4、blocked 1、unsafe 0；第五 Runtime 独立为 `failed/upstream_error/502/transient=true`，没有第五轮 `model_completed`。
- 终态按既有健康快照回滚为 user 与唯一 `error-recovery` assistant。公共 AgentRun 与 Session 的活动 Runtime 引用清空，v4 原始耐久记录精确不含 `activeRuntimeRunId`；当前页和完整刷新后顶部耗时均恰好一个、主 assistant 页脚无重复时钟，刷新 AgentRun POST/Runtime POST/chat/tool 四项增量全部为 0。
- failed 主任务耗时丢失的根因是先持久化不含耗时的错误 assistant、后在内存 finalize。修正保持 checkpoint 先捕获终态，在同一 `persistRunCheckpoint` / `persistMessages` 保存边界之前对同一个错误 assistant 调用既有 `finalizeRunTiming`，由现有 `meta._responseTime` 序列化路径恢复；没有新增字段、迁移、计时算法、协议或历史回填。
- 完整门禁同时强化两项测试确定性：TIFF 以脱敏 key hash 和主 frame document generation 区分旧页/新页，仍严格要求刷新 GET=1；queue 观察器在主 Runtime/Session checkpoint 有界收敛后启动，仍严格要求唯一 `Control+Enter` 的 transition PUT=1。两者都只调整 Harness 观察窗口，不是生产缓存或队列语义修改。
- bundle/direct classic 九项语义哈希完全一致，H4-6K 旧哈希保持。最终五文件哈希下连续两轮标准 H4 均为 `49 passed`、单 worker、零 retry、exit 0；H4-7B/H4-7C/暂停取消定向 `6 passed`，前端/P0 `232 passed`，完整 pytest `1129 passed, 751 subtests passed`，`check:frontend`、Node/Python 语法、diff 与资源清理通过。文档收口沿用同一哈希的完整矩阵，只重做文档、链接、语法、diff、白名单和资源门禁。
- 完成声明只覆盖固定同指纹行范围失败的三次真实执行、第四次阻断、第五轮 forced-final 固定 502，以及 bundle/direct classic 的 failed 终态回滚、顶部耗时持久化与同进程刷新零重放；不覆盖执行前失败限流、不同参数/工具、其他模型错误或恢复策略、跨进程 active、取消、长输出或外部副作用 exactly-once。长期事实见 [`H4-6O forced-final 模型失败终态与刷新唯一性`](../../harness/h4-6o-forced-final-model-failure.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。
