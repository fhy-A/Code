# 2026-09-05 开发日志

## 2026-09-05 17:23 · Codex

### CODE-074 Stage 4D D1 · 不可变 Skill 内部管理事务与测试隔离收口

- 新增显式、默认不对外暴露的 `SkillStoreManager` 与纯 v2 元数据合同。管理必须同时满足当前进程持有同根 `DataDirOwner`、Store 显式可写和线程 / 跨进程 registry 排他锁；必须先有合法 v1 bootstrap，再显式 `convert-v2`，启动、导入或新建请求均不会自动转换。物理目录、旧对象字节、根身份、既有 Skill / installation / revision ID、v1 source observations、tombstones 与 bootstrap 收据原样保留。
- 内部引擎支持 local 新建 / 导入 / 编辑 / 改名、bundled fork / 离线 catalog 升级、保留修订回退、profile 启停、逻辑卸载 / 恢复、显式同名候选选择与确认后的浏览器偏好迁入。选择身份与当前可准入状态分离；被选 local 停用、卸载或改名后不自动回落到 bundled。bundled 不能被 local edit 原地覆盖；停用偏好只缩不扩，catalog / provenance 不授予 trust、工具、资源、validator 或权限。
- 每个 operation key 固定规范化请求，先查原收据再检查新操作 CAS；A→B→C 后重试 A 仍返回 A 的原收据，同 key 异请求拒绝，不同 key 的相同内容仍产生新 generation。v2 journal 固定完整 base / target 与包 manifest，只有 registry 原子替换是提交点；提交后只向前完成，回退也是新操作。早期恢复只接受原捕获，晚期不重读来源；abort 仅清理经归属 / 内容验证的提交前暂存，未知或字节不匹配的对象 / temp 不删除。容量达到既有上限时稳定拒绝，D1 不做旧对象 GC、历史 Run 或共享依赖删除。
- 只读 Reader 与写者共用有界锁，管理期间读取只见完整旧 / 新状态，跨代准入捕获拒绝漂移；startup 仅恢复已经显式开始的 v2 journal，不主动转换。旧 v6 Run 保持冻结 installation / revision、原物理资源路径和已完成 receipt；测试既覆盖原 bundled Run，也覆盖 fork 后同一 local installation 在改名、停用、卸载后的等待恢复与已完成调用。AgentRun / Session 外层版本、旧 v1/v5 读取和终态无 Reader 展示未改变，公开 API/UI 与旧客户端管理写入门禁未接入。
- 冻结前 Store / management / recovery / security / concurrency / Skill / startup / v6 / layout 相关扩大组为 `551 passed, 180 subtests`（796.12 秒），包含真实多进程竞争、持久边界故障、短写、畸形输入与相同身份恢复。doctor `10/10`、catalog 31 项、frontend freshness / bundle syntax、Harness replay、quick `6/6` 与语法 / diff 检查通过；catalog hash 仍为 `sha256:50301114d8b3b822a9abb51ae2b3177ca9b7cae67ab8ea7afc51bafc190dcb77`，replay 仍为 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries` 及 hash `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。
- 本阶段唯一 canonical `python -m pytest tests -q` 为 `1 failed, 2519 passed, 1 skipped, 4 warnings, 1449 subtests`（1596.86 秒）。唯一失败位于 `test_skill_completion_continuation_does_not_use_normal_nonaction_recovery` 的 tearDown：`WinError 145` 指向临时 profile 的 `.nonterminal-index`；测试正文未失败。旧夹具只等待 Run worker、提前恢复全局目录后才删除临时根，没有等待独立索引 writer。三条 PIL/EXIF warning 来自损坏 TIFF 输入用例，另一条是 PPTX 资源测试的 PyPDF2 弃用提示；全量摘要未出现未处理线程或 ResourceWarning，未通过忽略 warning 掩盖失败。
- 第八测试文件现等待 Run worker 与同 profile 的两类索引 writer，在路径补丁仍生效时完成清理，再恢复默认目录；超时抛出 runner 级中断并阻止下一夹具，保留补丁与文件，停用 TemporaryDirectory 的退出时自动清理。正常写入 / 退出、清理顺序和 fatal 路径均有确定性证明。原 Windows 子 pytest 文件收集方案因超时 / access violation 撤回，改用显式 unittest TestSuite 子进程；继承合成根与显式子根两路都证明不进入下一夹具、受控 writer 真实退出后才清理并恢复合成默认根。子证明自行构造 / 恢复环境，不给原套件新增 CODE_DATA_DIR 必须存在的先决条件。
- R070 后全部测试和 Python 辅助验证均在进程启动前用命令级 `CODE_DATA_DIR`、`TEMP`、`TMP` 指向独立临时根；父层在首次 server import 前后确认默认根，嵌套 child 继承或显式覆盖到合成根，正常退出再确认补丁恢复与已知索引 writer 退出。AgentRuntime 整文件为 `151 passed, 93 subtests`（114.25 秒，在最后的环境兼容 / finalizer 微调前）；最终差量的原失败项、夹具与两类索引相关组为 `18 passed, 14 subtests`（13.97 秒）。另在合成默认根已经完成 import 后临时移除环境变量，单独执行子证明仍为 `1 test OK`（2.045 秒），并恢复变量缺省状态和合成 DATA_DIR；最终 quick 与语法 / diff 检查保持通过。原 13 个冻结文件 SHA-256 全部不变；按审批未跑第二次 canonical，不将本阶段表述为全量全绿。
- 历史隔离证据有明确限制：审批侧独立元数据复核记录真实默认非终态索引为 175 bytes、修改时间 2026-09-05 15:52:23，session 索引为 111112 bytes、修改时间 2026-09-03 21:50:27；缺少测试前快照，不能确定写者，也不能排除先前测试对真实默认索引的读取或写入。用户接受该有限不确定性，停止进一步访问 / 修复或扩大调查；R070 只保证后续验证采用上述外层隔离，不追溯宣称真实 profile 零 I/O。原失败临时夹具按审批保留，没有清理其内容。
- 生产差量为 5 文件、净增 939 行；测试差量为 8 文件、净增 1293 行；公共文档仅内部管理合同、当天日志和索引 3 文件，私有计划只更新 CODE-074 当前 / 下一步差量。未做真实 profile 的显式转换、启用、CRUD、真实模型或 3010/3011 操作；受保护来源 / 用户数据不纳入修改与提交，无关活动 handoff 不变。没有 push、tag、release、物理清理接口或 D2/D3 实现，阶段在 D1 边界停止。

## 2026-09-05 14:00 · Codex

### CODE-074 Stage 4C3 · 默认关闭的不可变 Skill 启动接入与回退

- 新增无 import-time I/O 的 `skill_runtime_startup.py`，只接受当前进程已持有且根身份一致的 `DataDirOwner`，并以 `CODE_SKILL_IMMUTABLE_ADMISSION_V1` 独立控制新 v6 准入，默认关闭。空 store + flag off 保持零 store 写，flag on 才执行首次 bootstrap；既有 committed store 只做 root / registry / 单 committed journal / 精确对象集与空 staging 复验，不读取 catalog、bundled source 或 legacy sync；可恢复 store 始终 journal-first，late phase 不被外层 catalog/source 读取阻断，early phase 仍按既有来源冲突语义保留 journal、拒绝推进。初始化同身份幂等且零新增 I/O，失败会以稳定 code 复现，热切换根或模式 fail-closed。
- `server`、`launcher` 与 `dev_server` 现统一遵守 owner → 必要目录/既有 legacy sync → immutable startup → route services → listener 的顺序；启动失败不发布监听器，committed store 不受后续 legacy sync 失败影响，也不存在按请求 lazy bootstrap / lazy repair。恢复 reader 与 admission reader 明确分离：flag off 仍可恢复既有 v6、同 `clientRequestId` 找回原 v6，但所有新无握手请求继续写 v5；flag on 且 reader ready 才允许新 v6。对象暂时缺失或损坏时 GET 维持 `waiting_recovery`，恢复必须经过重启或显式 reader 重接；真实取消会清除 authorization / input / evidence gate 与 `skillRecovery`，终态保持 reader-free。
- C2 已存在的 `skill_recovery_restored` producer 现补入 Agent event v1 exact known-event 合同，`restoredStatus` 与 `dataRootId` 均为必需字段；纯诊断状态表允许 `waiting_recovery` 回到已实现的 authorization、user input、skill evidence 或 credentials 等待态，未知事件/字段的兼容策略与终态禁止回跳保持不变。该同步不改变 producer、持久记录、执行状态、权限或公开 API/UI。
- 模型无关、跨进程 HTTP selfcheck 在隔离临时 profile 上通过三代启动：flag on 创建并冻结 v6，flag off 恢复 v6 且新请求仍为 v5，再次 flag on 保持同 root/revision 并创建新 v6；并验证第二 owner 被拒、committed startup 忽略 sync-failed、损坏对象时 flag on 零 listener、flag off legacy v5 可用而 v6 稳定等待恢复、修复字节不会在线 lazy-repair、取消终态无 reader。受控 ready-timeout 与正常链都确认 child 真实退出、两个端口关闭、host/child 登记清空后才删除临时根；Python host 的 owner 与真实入口一样只在进程退出时释放。既有 bundle/direct classic Browser H4 的 canonical / legacy 链也通过，且未关闭内置 Browser 标签页。
- 冻结前专项结果包括 startup `14 passed`、v6 AgentRun `34 passed`、相邻 Store/Skill/Goal/Agent/server/routes 扩大组 `803 passed, 1 skipped, 262 subtests`、入口顺序组 `26 passed, 3 subtests`、activation `18 passed`；doctor `10/10`、catalog 31 项且 hash 仍为 `sha256:50301114d8b3b822a9abb51ae2b3177ca9b7cae67ab8ea7afc51bafc190dcb77`、layout `3 passed`、frontend freshness / bundle syntax、quick `6/6`、py_compile 与 diff check 均通过。Harness replay 保持 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`，hash 仍为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。
- 本阶段唯一一次 canonical `python -m pytest tests -q` 为 `1 failed, 2401 passed, 1 skipped, 1415 subtests`（1152.17 秒）；唯一失败精确暴露上述既有恢复事件已在 producer / inventory 中存在、却漏登 `AGENT_EVENT_SPECS`。R064 最小合同同步后，event protocol + inventory 为 `17 passed, 177 subtests`，四个真实恢复等待态为 `4 passed`，server shadow 为 `9 passed, 25 subtests`，完整 v6 AgentRun 再次 `34 passed`，Harness replay 与 quick 保持通过；按审批边界未运行第二次全量，因此不把 canonical 结果表述为全绿。
- 生产差量严格为 6 文件、净增 `356/850` 行；测试差量严格为 8 文件、净增 `1501/1600` 行。flag 仍默认关闭，没有操作真实 DATA_DIR/profile、真实 3010/3011 实例、真实模型或外部网络，也未增加公开 API/UI、CRUD/GC/delete、child Skill 解禁或 Goal 跨 Run 放行；未读取、哈希、修改或暂存受保护的 `data/generated-assets/`、`data/image-route-registry.json`、`claude-skills` 与 `output/r035-isolated`，未执行 release、push 或 tag。Stage 4D 未启动，真实 profile canary / 启用仍需独立 STRICT 操作授权。

## 2026-09-05 11:56 · Codex

### CODE-074 Stage 4C2 · 固定修订运行接入与可恢复 AgentRun v6

- 在仍默认不可达、只允许受信调用方显式注入预建 `SkillStoreReader` 的链路中接通 AgentRun v6 dual reader/writer：无 reader 的现有路径继续写 v5；有 reader 时 admission 以单请求 registry 双读、候选有界捕获与选中 revision 末端复验建立 `skillLifecycle/v2`，v6 record 固定写入 lifecycle、active names、`dependencies/v2`、`evidence/v2`、`runtimeBindings/v2` 与 `skillOutcome/v3`。loader 对 v5/v6 精确分派，缺失 version 的历史记录维持既有兼容，终态 v6 可无 reader 只读展示。
- 新增纯 `skill_runtime_v2` consumer，use/check/read、文本 access、dependency 状态、runtime binding、`run_command` preflight、execution context 与 resource path binding 全部只从 `{dataRootId, installationId, revisionId}` 指定的已复验对象读取；文本在 decode、binding 与返回前受 512 KiB 上限约束。`skillOutcome/v3` / receipt v2 与 completion v2 只接受同一 exact context；sidecar normalized contract、工具缩减、Windows 路径别名、完成态 receipt 复用及持久终态重验均 fail-closed。
- 非终态 v6 在对象缺失、损坏或 reader 不可用时进入独立 `skillRecovery/v1`，保留原 authorization / user input / evidence gate 并在同一 revision 恢复后原子清除；generic 非幂等调用若重启时处于 dispatching 会以 unknown/notReplayed 闭合。旧 immutable resource 路径绝不改写或重新分发：completed 调用复用原 receipt，prepared/pending 调用以稳定失败闭合并由模型从当前路径新建调用；闭合写盘失败会回滚 execution、pending、authorization、status、messages、events 与 seq。v2 child 暂时剥离 Skill 工具，Goal 跨 Run 暂时以一次 waiting-user gate 和零 successor 稳定暂停，均保留给后续独立设计。
- 聚焦验证通过：admission `54 passed`，C2 纯运行时 `9 passed`，v6 AgentRun `26 passed`，相邻 lifecycle/runtime binding/completion/outcome/Goal/runtime/recovery 为 `242 passed, 194 subtests`，Stage 4A/B/C1 与 admission 组合为 `164 passed`；旧路径闭合的 persist-failure 定向故障注入 `1 passed`。doctor `10/10`、project layout `3/3`、catalog 31 项且 hash 仍为 `sha256:50301114d8b3b822a9abb51ae2b3177ca9b7cae67ab8ea7afc51bafc190dcb77`、quick `6/6`、py_compile 与 diff check 通过。
- 本阶段唯一一次 canonical `python -m pytest tests -q` 完整运行结果为 `2 failed, 2376 passed, 1 skipped, 1442 subtests`（680.94 秒）：两项失败均为 Harness 事实清单尚未登记 `skill_recovery_restored`，以及旧断言把 `_agent_run_record` 版本假设为单一字面量。随后仅同步实际 producer/零前端 consumer 事实，并把断言精确收紧为历史基线 v4、当前 dual writer v5/v6；定向 inventory 为 `5 passed, 12 subtests`，Harness replay 保持 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`，hash 仍为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`，修正后 quick 再次 `6/6`。按审批边界未机械重跑全量，故不把本阶段表述为“最终全量全绿”。
- 生产差量严格为 7 文件、净增 `1849/1900` 行；测试差量净增低于 `2200`。没有接入 launcher/dev_server/startup、真实 DATA_DIR、现有 flag、公开 API/UI、模型、Browser/H4、CRUD/GC/delete、发布、push 或 tag；未读取、哈希、修改或暂存受保护的 `data/generated-assets/`、`data/image-route-registry.json`、`claude-skills` 与 `output/r035-isolated`。Stage 4C3/4D 与 `CODE-063/064/068` 均未启动。

## 2026-09-05 09:36 · Codex

### CODE-074 Stage 4C1 R053 · 返回字节、严格输入与 canonical 路径修正

- 审批完成复核在尚未接入生产的 C1 基础层发现三处可复现缺口并原边界修正。`SkillStoreReader.read_pinned` 现在对准备返回的每份 bytes 直接重算 canonical content mode、size 与 digest，再做第二次完整对象复验；即使对象验证观察到 A、返回读取瞬间为 B、随后又恢复 A，也以 `object_changed` 拒绝 B，不能把未绑定 manifest 的内容交给后续准入。
- `skillLifecycle/v2` builder 现在把 captures 严格限制为最多两项的 list，只有真实空 list 才可形成纯 `outcome=none`；lifecycle / activation / access / evidence enforcement 的版本字段均要求精确整数，拒绝 Python 等值的 float / bool。资源路径先要求输入就是 canonical Posix 形式并且确实包含文件 path parts，再执行正文隔离：空串、`.`、`./SKILL.md`、重复分隔符与 `references/./guide.md` 均拒绝，既有普通相对路径及反斜杠到正斜杠兼容保留；测试通过实际 `bind_text_resource` 覆盖，不只调用 helper。
- fail-first 为 `10 failed, 1 passed`，精确命中 A→B→A、三种畸形 captures、四种错误版本类型及两个仍被接受的路径别名；首轮修正后 `11 passed`，C1 与相邻 Stage 4A/B、registry / activation / lifecycle / dependency / resource / layout 组合为 `355 passed, 140 subtests`。审批随后追加 `.` / 空路径边界，最终精确修正组 `13 passed`、受影响 lifecycle / access / layout 复验 `108 passed, 119 subtests`；pycompile、diff、quick 与预算检查通过，按 R053 明确边界未机械重复 doctor 或九分钟全量。
- 修正只改 `skill_store.py` 与 `skill_lifecycle_v2.py`，C1 累计生产差量最终为 `+693/-3`、净增 `690/700`；测试仍严格为原 3 文件。未修改 v1、admission matcher、生产 consumer、AgentRun、flag、startup、真实数据或公开协议，Stage 4C2 未启动；未触碰受保护路径，未运行 Browser/H4、模型、网络、Office、发布、push 或 tag。

## 2026-09-05 09:23 · Codex

### CODE-074 Stage 4C1 · 不可变读取、准入捕获与 lifecycle/v2 纯基础层

- 在仍无生产调用方的 `skill-store-v1` 上新增纯只读 `SkillStoreReader`：active capture 只接受 canonical registry 中 ready binding 的 `activeCandidate`，返回 registry 根身份、`skillId / installationId / revisionId` 与完整复验后的对象 manifest / canonical 相对路径 / bytes；pinned read 仅按 `{dataRootId, revisionId}` 定位对象，绝不查询当前 alias / pointer。读取前后复验 root、registry、全部引用对象与目标对象；blocked / tombstoned / shared / missing、根错配、registry 漂移及 missing / corrupt / extra object 均以稳定错误 fail-closed，不创建 lock、skeleton、temp、journal，也不返回绝对 store 路径或回退 legacy。
- 新增无生产 import 的 `skill_admission.py`，只接受显式注入 reader、消息意图、工具上限与 token estimator。候选 Descriptor 完全由已复验对象 bytes 构造，直接复用现有纯 `resolve_skill_shadow`、frontmatter normalizer 与 `project_skill_tools`，保持 explicit / automatic owner+modifier 顺序、最多两项及工具只缩不扩；选中后再次读取同一 active identity。捕获只保留同一 revision 的 Skill 正文、精确安装身份，以及有界 evidence、去除 presentation-only `installHint` 的 normalized dependency manifest、对象内相对 resource contract 与逐文件 hash；任一 malformed / oversize / 越权 evidence、资源缺失 / 过大 / hash 不符或读取竞态均拒绝或降为既有兼容 invalid evidence，不查询 mutable `DATA_DIR/skills`、bundled fallback、host paths、环境、probe、凭据或任意 discovery 结果。
- 新增独立 `skillLifecycle/v2` 纯 schema：固定 `mode=immutable-v1`、activation/v2 的 registry identity 与 selected exact authority、access/v3 的 `{installationId, revisionId, file, contentHash}` 文本绑定。normalizer 精确约束字段、版本、枚举、hash / ID、owner→modifier 顺序、唯一 name / alias / skillId / installationId、组件跨 identity、相对路径、合同子上限及总计不高于既有 256 KiB；build 会丢弃 ephemeral body / instruction，existing-compatible projection 仍只给 active names、依赖 capability、evidence 与 content hash，不公开 immutable IDs。`outcome=none` 仅是纯 schema 能力；本阶段未实现或决定 AgentRun v6、runtimeBindings/v2、outcome/v3、receipt/v2、completion/v2 或 writer 选择。
- 闭合 R048 的部分空骨架 carry：bootstrap 现在按内容区分 absent / non-authoritative empty，而非只看 store 根是否存在。只含任意空子集的 `registry.lock`、`objects[/sha256]`、`transactions`、`staging` 且没有 root / registry / journal / temp / object / staged 内容时，必须先完成与全新 store 相同的稳定 live preflight；无效 catalog / legacy state 保持整棵现有树逐字节不变。预检有效后才加锁、锁内重分类并补 skeleton；未知、非空、权威或可恢复状态继续沿 Stage 4B 规则，journal-first late recovery 未弱化。Windows 锁占用时分类只读文件元数据，不读取被锁字节，既有 bounded-busy 合同保持。
- fail-first 精确得到缺失 `StoreReader` 与五类空骨架改写共 `6 failed`，两个新模块导入各自 `ImportError`。实现后 C1 三文件组合 `139 passed`，补齐 tombstone / missing store、2 MiB executable resource 与交叉身份后新模块 `42 passed`；相邻 Stage 4A/B、registry、activation、lifecycle/v1、dependencies 与 resources 为 `338 passed, 140 subtests`。doctor `10/10`、quick `6/6`、catalog check（31 项，hash `sha256:50301114d8b3b822a9abb51ae2b3177ca9b7cae67ab8ea7afc51bafc190dcb77`）、py_compile、diff 与零生产 import 检查通过。首次 canonical 全量仅因 repository layout 白名单尚未登记两个获批模块而为 `1 failed, 2328 passed, 1 skipped`；将 lifecycle/v2 测试合并进同一 C1 foundation 测试文件以保持测试文件上限 3，并更新白名单后，最终原样 `python -m pytest tests -q` 为 `2329 passed, 1 skipped, 1442 subtests`，4 条 warning 仍为既有 TIFF corrupt EXIF 与 PyPDF2 deprecation。
- 生产严格为 3 文件：`skill_store.py +120/-3`、新 `skill_admission.py 269` 行、新 `skill_lifecycle_v2.py 296` 行，净增 `682`，低于硬上限 700；测试严格为 `test_skill_store.py`、合并后的 `test_skill_admission.py` 与 `test_project_layout.py` 3 文件，未以压行或 minify 换预算。现有 server / launcher / dev_server / frontend、API、Skill activation/lifecycle/outcome/completion/runtime consumer、AgentRun v1-v5 reader/writer、flags 与 startup bootstrap 均未修改或导入新模块；未接触真实 DATA_DIR、Browser/H4、模型、网络、Office、PyInstaller、release、push、tag 或线上配置，受保护路径未读取、哈希、修改或暂存。Stage 4C2 必须另经独立 STRICT 门禁，且在所有 v2 consumers 原子闭合前不得出现可达 v6 writer/admission；Stage 4C3 与 Stage 4D 继续未启动。

## 2026-09-05 07:52 · Codex

### CODE-074 Stage 4B R046 · 恢复边界与状态码闭集修正

- 按审批复核修正 bootstrap 恢复顺序：全新 store 仍在创建任何 store 文件前完成零写稳定预检；既有 store 则先核验布局、取得锁并读取 journal。`prepared` / `root-bound` / `copying` 仍必须重读 legacy source，来源变化或不可读时以 `bootstrap_recovery_source_conflict` 保留原 journal 并拒绝推进；达到 `staged-verified` 后只使用 journal 已捕获的 staging / final objects 与 target registry 向前恢复，不再依赖可变或已删除的 legacy source，恢复提交后再把当前不同请求稳定拒绝为 `bootstrap_already_committed_conflict`。
- 将持久 registry 的 `binding.reasonCode` 与 `sourceObservation.errorCode` 收紧为按状态绑定的闭集：blocked binding 只接受实现定义的 8 个原因；ready / missing observation 的 error 必须为 null，invalid / unsafe / unreadable 分别只接受 `source-invalid` / `source-unsafe` / `source-unreadable`。未知、错误类型、跨状态或超长字符串即使重封 hash 也 fail-closed；Stage 4A 的细粒度诊断只在 bootstrap 边界映射为该闭集，不把不受限文本写入持久状态。
- fail-first 精确得到 `13 failed, 4 passed, 39 deselected`，覆盖三个 late crash point、两个 early crash point 及 reason / error 畸形值；修正后新增组 `17 passed, 39 deselected`，Stage 4B / revision / layout 聚焦组 `96 passed`，相邻 Skill / bundled / DATA_DIR owner 组合 `302 passed, 369 subtests`，quick `6/6`、catalog check、py_compile 与 diff check 通过。第一次全量 `-q` 样本在约 5% 出现一个未定位单点后于 9% 主动中止以隔离；随即完整 `-x -vv` 与最终原样 `python -m pytest tests -q` 均为 `2244 passed, 1 skipped, 1442 subtests`，4 条 warning 仍为既有 TIFF corrupt EXIF 与 PyPDF2 deprecation，未复现该瞬态样本。
- 独立修正的生产差量仅 `skill_store.py` `+64/-30`、净增 34 行，测试 `test_skill_store.py` `+105/-0`；Stage 4B 相对 Stage 4A 基线的当前累计生产差量为 `+634/-8`、净增 626，仍低于审批上限 700。未修改 server / launcher / runtime / API / UI，未执行真实 DATA_DIR bootstrap，未读取、哈希、修改或暂存受保护路径；Stage 4C 仍需新的独立 STRICT 门禁。

## 2026-09-05 07:17 · Codex

### CODE-074 Stage 4B · 不可变 Skill 存储与可恢复 bootstrap 引擎

- 新增独立、默认拒绝写且无生产调用方的 `code_runtime/skill_store.py`，只接受调用方显式传入的数据根与 bundled 根；在 `DATA_DIR/skill-store-v1/` 合同下建立 `code-skill-data-root/v1` 根身份、`code-skill-install-registry/v1` canonical snapshot、复用 `code-skill-revision/v1` 的内容寻址对象，以及 `code-skill-store-transaction/v1` 恢复 journal / 同卷 staging。对象内容为独立 canonical bytes，文本固定 LF、二进制保持原字节；store 不使用 symlink / junction / reparse / hardlink，不依赖 chmod，既有对象只有完整 manifest / 文件集 / 内容复验通过才可复用。
- 事务遵守“零写稳定预检 → 创建有界 skeleton / lock → 锁内稳定复扫 → prepared journal → root marker → staging copy / file fsync / verify → 同卷对象 rename → registry CAS / file fsync / `os.replace` / reopen verify → committed journal → 精确 staging cleanup”。正式运行时未来只能复用 CODE-066 已持有的 DATA_DIR 外层 owner，内部绝不重取；当前引擎另用按路径 `RLock` 与经身份核验、有限等待的 `registry.lock` 串行化线程 / 独立进程。Windows 只承诺普通文件 fsync 与可观察的同卷 rename / replace，不宣称目录 fsync 或绝对断电持久性；未知、错配或损坏状态保留字节并 fail-closed。
- R044 三项修正已成为实现合同：logical `requestHash` 只绑定 operation、catalog / plan / legacy snapshot 与显式 identity hints，CAS base 独立存于 journal；`operationId` 再绑定持久 `dataRootId`，prepared 后的随机 root / local skill / installation ID 不再重生，同请求提交后直接返回同一 receipt / result，generation、journal、对象与 ID 均不增加，后续不同 bootstrap 明确冲突。未匹配 v1 tombstone 只保留 `skillId=null` 的惰性事实；同名唯一 local custom 继续 ready，不被无关删除意图遮蔽。无效 catalog / legacy state 在首次建库前失败且零新文件，只有已通过预检的空 skeleton / lock 才是可恢复初态。
- 非破坏映射覆盖 exact bundled、local custom、same-name modified、bundled missing、matched / unmatched tombstone、tombstone conflict、invalid、missing root 与 shared-development：唯一 exact / local 可形成 registry 内 shadow pointer；modified、missing、tombstone conflict 与 shared-development 一律 blocked 且 `activeCandidate=null`，无效项不导入对象。真实 `skills/` 与 `bundled-skills-state.json` 始终逐字节不变；完整数据根移动保留 `dataRootId`，完整 clone 只共享 lineage 而非全局实例锁，分叉必须同时比较 generation 与 registry hash，Stage 4B 不发现、合并或旋转 clone。
- Registry / journal 使用精确字段、canonical hash 与硬上限：最多 512 installations / bindings、1024 observations / candidate refs / objects、256 tombstones、512 receipts / journals、单一 nonterminal transaction、单 revision 继续为 4096 文件 / 64 MiB、单事务新增对象 1 GiB、store 8 GiB；达到上限只拒绝后续写，不做 GC、purge 或 compaction。Catalog / source / revision 只证明身份与完整性，不授予 trust、工具、资源、validator 或权限，也不改变 Stage 3C 语义边界；引用对象损坏时不得回退别的 revision 或 legacy。
- `skill_revisions.read_skill_revision` 在既有双重稳定读取上同时返回 manifest 与独立 canonical bytes；Stage 4A 维护 CLI 现会以 `catalog_skill_id_reassignment` 硬拒绝已登记目录改绑不同 stable skillId，同 ID 重跑保持幂等，rename 仍必须同次显式 `--drop old --assign new=oldStableId`，无 force 逃生口。checked-in `data/skills/catalog.json` 保持原字节，check 仍为 31 项与 `sha256:50301114d8b3b822a9abb51ae2b3177ca9b7cae67ab8ea7afc51bafc190dcb77`。
- fail-first 为 `tests/test_skill_store.py` collection 的预期 `ImportError`。最终 Stage 4B / revision / layout 聚焦组 `78 passed`，相邻 Skill / bundled / DATA_DIR owner 组合为 `284 passed, 369 subtests`；doctor `10/10`、quick `6/6`、catalog check、py_compile 与 diff check 通过。阶段冻结的 canonical `python -m pytest tests -q` 为 `2226 passed, 1 skipped, 1442 subtests`，4 条 warning 仍为既有 TIFF corrupt EXIF 与 PyPDF2 deprecation。
- 生产严格为 `skill_store.py` 580 行、`skill_revisions.py` `+12/-7`、catalog CLI `+8/-1`，3 文件合计 `+600/-8`、净增 592；测试 3 文件合计 `+493/-0`。未修改 server / launcher / dev server、activation / lifecycle / AgentRun、依赖 / 资源、API / UI 或默认 flag，未让真实 DATA_DIR 执行 bootstrap，也未读取、哈希、修改或暂存受保护路径。Stage 4C 仍需新的独立 STRICT 门禁，才可讨论真实 profile bootstrap、生产 reader / active pointer、lifecycle v2 / AgentRun v6、drain / rollback、canary、CRUD/API/UI 或默认启用；本阶段未执行 Browser、H4、模型、网络、Office、PyInstaller、发布、push、tag 或线上配置。

## 2026-09-05 06:14 · Codex

### CODE-074 Stage 4A · 不可变 Skill 修订身份与只读迁移影子

- 新增独立、未接入现有生产决策的 `code_runtime/skill_revisions.py`，定义 `code-skill-revision/v1`、`code-skill-bundled-catalog/v1` 与 `code-skill-migration-shadow/v1`。修订身份覆盖 Skill 包内按 canonical 相对路径排序的全部普通文件及其规范化大小 / SHA-256；有效 UTF-8 文本统一 CRLF / CR 为 LF 后计算，二进制保持原始字节，目录名、绝对根、时间戳、来源、展示名、主机事实和信任均不进入 `revisionId`。唯一排除项与现有打包规则一致，仅为 `__pycache__` 与 `*.pyc`。
- 遍历拒绝 symlink / reparse、特殊文件、逃逸 / 非 canonical / Unicode 或大小写别名路径、尾随点空格、超深、超量、单文件 / 总量超限、不可读及文件或整树不稳定重读；要求完整包存在 `SKILL.md`。输出只含有界的路径、大小与 digest，不含文件正文、命令、绝对路径、凭据或 trust 字段。
- 新增根级紧凑 `data/skills/catalog.json`，为当前 31 个 bundled Skill 显式分配稳定 `code.bundle/*` skillId 并记录完整树 `revisionId`。`scripts/skill_revision_catalog.py` 只作为维护 / 校验入口：默认 check，`--write` 复用目录到稳定 ID 的显式映射并原子刷新 revision；新目录、重命名、缺失、重复 ID / 目录、修订或任一 sidecar / resource 漂移均令 freshness fail-closed，不能在运行时从可变名称或路径自动生成新 ID。
- 只读迁移 planner 对当前 bundled catalog 与 legacy installed 根进行双重稳定读取，保守区分 exact bundled、same-name modified、local custom、tombstoned bundled、tombstone conflict、missing、invalid / unsafe / unreadable 与 shared-development；未知历史内容不猜为 bundled，同名 custom 与 bundled tombstone 保持不同事实，未匹配旧 tombstone 也保留在输出。planner 不写、移、删、合并、隔离、重标或覆盖任何目录；当前源码共享根的真实影子结果为 `31 shared-development`。
- 换行审计确认仓库 `core.autocrlf=true` 且既有 Skill 文件同时存在 CRLF / LF 工作树形态，专项验证两者获得相同修订，二进制 CRLF 不归一化。目录 rebuild / check 连续得到 31 项、catalog hash `sha256:50301114d8b3b822a9abb51ae2b3177ca9b7cae67ab8ea7afc51bafc190dcb77`；`--write` 前后 catalog 文件 SHA-256 同为 `B691F29C9EC57211173B3515268EE9DA1C7714CFEE641704C43FF7D02EAB2C9B`，幂等且无额外差量。
- fail-first 为新模块尚不存在时专项 collection `ImportError`；实现后新专项 `34 passed`，结构 / 专项组合 `37 passed`，既有 registry / activation / lifecycle / runtime / resource / dependency / bundled sync / packaging 相关矩阵 `240 passed, 333 subtests`。doctor `10/10`、quick `6/6`、py_compile 与 catalog check 通过；最终 canonical `python -m pytest tests -q` 为 `2185 passed, 1 skipped, 1442 subtests`，4 条 warning 仍为既有 TIFF corrupt EXIF 与 PyPDF2 deprecation。
- 生产 / 产物严格为新 revision 模块、catalog 维护入口、checked-in catalog 3 文件，共 `+550/-0`；测试为新专项与 project layout 登记 2 文件，共 `+475/-0`。现有 `DATA_DIR/skills`、`bundled-skills-state.json` / tombstone、launcher、registry、activation、runtime、AgentRun、API 与 UI 路径均未导入或调用 Stage 4A 模块，语义保持不变；未新增 trust、权限、执行、迁移或 default-enable 行为。
- Stage 4B 仍需新的独立 STRICT 门禁，才可设计 / 实现本地不可变 store、registry state、事务、CAS、恢复与非破坏 bootstrap；本阶段未开始 lifecycle v2 / AgentRun v6、active pointer、CRUD/API/UI、canary / 默认启用、`CODE-063/064/068` 领域 validator 或通用语义 judge。未访问受保护目录，未运行外网、模型、Browser、Office、PyInstaller、发布、push、tag 或线上配置。

## 2026-09-05 05:12 · Codex

### CODE-074 Stage 3C · 通用语义边界收口

- 完成最终回答 / 结果语义的只读价值与设计审计，并确认 Stage 3 通用核心在冻结激活、资源 / 依赖运行时身份、执行归属 receipt 与可选 owner 完成门禁处收口。通用核心明确不增加正则、文案、LLM judge 或逐 Skill 最终 JSON 作为真实性裁判；这些方案只能检查表达或结构，无法可靠证明领域结果正确，还会引入模型 / provider 漂移、自定义 Skill 信任、权限扩张、重启重放、迁移、UI 与长期维护负担。
- 当前 `run_command` receipt 只证明命令在绑定的 Skill 依赖环境中成功执行：`skillRuntime` 没有绑定指定 executable resource、protocol、operation、输入产物或输出结果，而 completion contract 也没有声明期望 fingerprint。因此，同一单 Skill runtime 下的无关成功命令可能满足普通 `run_command` requirement；它只能作为执行证据，永远不得外推为语义 validator 证明。`write_file` receipt 同样只证明结构性文件写入，不证明 XLSX / DOCX / PDF / PPTX 或最终文案正确。
- 领域正确性继续由 `CODE-063/064/068` 等独立事项中的确定性 validator 负责。未来若由单一领域 canary 证明价值，最小可信扩展必须走专用 exact-operation receipt：绑定 activation-pinned resource / protocol / runtime、canonical typed input、输入 / 输出 digest 与服务端解析的结构化结果；不得以 shell 命令字符串、stdout、最终文案或通用 `run_command` receipt 匹配，不得因自定义 Skill 被激活就自动执行其代码，也不得绕过 `read` / `plan` / `accept` / `bypass` 的既有授权边界。旧 `evidence/v1/v2` 不重解释、不迁移；失败或未知副作用不得盲目重放。
- Stage 3B1b 继续只在出现可复现且真实影响 enforcement 的归属歧义时另行评估；`CODE-063/064/068` 保持独立，本阶段不启动领域 canary。`CODE-074` 下一独立阶段转入 Stage 4 provenance / upgrade 设计，不从本结论推断实现授权或默认启用权限。
- 本次仅修改开发日志、日志索引与仓库外私有 TODO；生产、测试、flags、contracts、bundled Skills、公开协议与 UI 均未改动。因代码未变，按审批只执行 diff / 暂存范围 / 工作区状态核对，未运行测试；不 push、不打 tag、不发布。

## 2026-09-05 04:56 · Codex

### CODE-074 Stage 3B2 · 默认关闭的 owner 完成门禁引擎

- 新增严格版本化的 `evidence/v2` completion policy：enforcement 固定为 `schemaVersion: 2`、`mode: owner_completion_once` 与 canonical、非空、有序的 `activationKinds`。只有默认关闭且独立于 activation 的 `CODE_SKILL_COMPLETION_ENFORCEMENT_V1` 显式开启、canonical foreground root Run 的 owner 合同为 v2 且本次 intent 命中时才在准入期冻结内部 plan；当前 bundled Skill 没有 opt-in。准入同时要求 requirement 工具来自冻结公开工具集，排除 interaction / Goal metadata，禁止 owner 内重复工具及 modifier 冲突，`minCount` 总和不超过 8，side effect 只能为 1 次，工具预算足够，`run_command` 不得存在外来或歧义 runtime identity。
- 完成候选只在既有 normal / forced 两个真实终点前由服务端检查同一 canonical lifecycle 派生的 `skillOutcome/v2`。已满足时继续兼容既有 `skillEvidence/v1` code-review gate；缺失但可恢复时仅追加一次 server-owned continuation，将下一模型轮工具严格缩到仍缺失的 owner requirement。整批 tool call 在任何 dispatch 前一次性校验并把 plan、call IDs 与 pending calls 同记录持久化；只允许 `not_executed` 与 failed idempotent read 修复，ambiguous / rejected、failed side effect、重复引用及非法归属直接失败。批次完成后重新投影，满足才进入一次无工具 finalizing 轮；phase、trigger round 与 batch IDs 跨重启约束最多一个 repair batch、最多两个额外完成轮，不复用普通 non-action recovery。
- restart reconcile 覆盖候选已落盘但 continuation 未写、repair model round 已落盘但 pending batch 未冻结、batch 已完成但 finalizing 未写三类窗口；pending steer 在 plan 进入 passed / failed 或终态 / legacy wait 前同锁仲裁，发布被拒绝时不遗留假终态。repair 自身命中既有 identical-tool retry limit 时保留 `repeated_tool_failure`。first candidate 继续按既有 `model_completed` 可见，不新增事件、弹窗、公开 snapshot 字段或 UI；内部可选字段仍在 AgentRun v5，Session、权限、授权、旧记录和通用工具 schema 均不迁移。
- 兼容性保持：flag-off、v1 / missing / invalid contract、automatic / explicit 未命中 policy、legacy、background / internal / child 均不创建 plan；旧 `skillEvidence/v1` modal / continue / skip / cancel 原样独立，v2 satisfied 后仍经过旧 gate。关闭 completion flag 只影响新 Run，已冻结 plan 可按 v5 记录恢复；回退无需数据迁移，先关闭 flag 并排空已有 active plan 即可。本阶段未启用任何 bundled v2 合同，未开始 3B1b、3C、领域 validator 或 `CODE-063/064/068`。
- 生产严格为 `code_runtime/skill_completion.py`、`code_runtime/skill_outcome.py`、`server.py` 3 文件，精确 `+708/-15`、净增 `693` 行；测试 6 文件净增 `1096` 行。新增 unit / integration 覆盖 exact contract、admission、fatal / recoverable、all-or-none、side-effect no-replay、显式 target、steer、forced / Goal precedence、重复失败、非法 phase / round / batch 篡改和两类 restart crash window；扩大 Skill / server / AgentRuntime 为 `599 passed, 1 skipped, 318 subtests`，完整 `python -m pytest tests -q` 为 `2151 passed, 1 skipped, 1441 subtests`，4 条 warning 均为既有 TIFF corrupt EXIF 与 PyPDF2 deprecation。完整套件后仅补强 empty effect 拒绝及非终态 Run 不得携带 unsatisfied passed / failed plan，两项最终差量以聚焦 `17 passed, 35 subtests`、bundle / direct H4 与 quick 复验通过；按分层规则未机械重跑完整套件。
- doctor `10/10`、quick `6/6`、Harness replay `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries` 通过，replay hash 仍为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。新增模型无关隔离 H4 在 bundle / direct classic 各自完成 3 轮 candidate → repair → final，repair 仅暴露 `read_file`、final tools 为空、仅 1 次 execution，子进程、端口、临时根与 active child 全归零。一次从仓库根运行 pytest 在 collection 阶段被禁止目录 `output/r035-isolated` 的 ACL 拒绝，未进入测试或读取其内容，随后改用 canonical `tests/` 范围完成全量；未运行真实模型、外网、Office 人工操作、发布、push、tag 或线上配置，也未读取、哈希、修改或暂存两项受保护 data 路径。

## 2026-09-05 03:37 · Codex

### CODE-074 Stage 3B1a · `skillOutcome/v2` 服务端归属 receipt

- 将新 canonical AgentRun 每次重算的内部 shadow 升级为 `skillOutcome/v2`；权威仍只来自冻结的 `skillLifecycle/v1`、同一份将要落盘的 JSON-safe `toolExecutions`、Run ID 与状态，不新增独立可变账本。旧 `skillOutcome/v1`、缺失、陈旧或畸形 v2 在恢复时均被忽略并从权威事实重算，AgentRun 外壳继续为 v5，历史记录不迁移；无 canonical lifecycle 的 legacy / flag-off / background / internal / child Run 仍不产生 outcome。
- 每个 completed execution 在全部 selected Skill requirement 中最多生成一张确定性 receipt。候选先按 tool 与 artifact 结构资格过滤，再依次采用三种服务端已有信号：`use_skill` / `check_skill_dependencies` 的 `name` 和 `read_skill_resource` 的 `skill` 形成 `explicit_skill_target`；`run_command` 结果中恰好一个、版本合法且命中 active Skill 的 `skillRuntime.skills` 形成 `runtime_single_skill`；没有前两类信号时，只有全局唯一 requirement 才形成 `server_unique`。同 Skill 多 requirement、跨 Skill、多 runtime identity、inactive / malformed / conflicting identity、重复或不安全 call reference、缺失 / 非法 execution fingerprint 均保守诊断为零 receipt，绝不按声明顺序猜测。
- receipt 固定包含版本、由 Run 与冻结 Skill / evidence / requirement / call / fingerprint / outcome / source 共同绑定的 `sr1_` ID、最小 Skill identity、requirement、tool、安全 call reference、execution fingerprint、outcome、qualification 与 source；不复制 arguments、result、path、content、command 或其他正文。成功 receipt 才满足 `minCount`，失败 receipt 只可审计；`artifact/file` 继续只做 `write_file` 成功结果的结构资格，不读文件、不宣称办公产物或最终答案语义正确。每个 requirement 的 receipt / diagnostic 总数仍最多 16，并记录确定性截断。
- 本阶段没有新增 claim 工具、模型 prompt、通用工具 schema、事件、公开 snapshot / route、UI、权限、完成 gate 或默认开关，也没有改变旧 `skillEvidence/v1` 与 code-review pilot。Stage 3B1b 仅在出现真实且影响后续 enforcement 的归属歧义时另行评估；Stage 3B2 强制策略、Stage 3C 结果语义以及 `CODE-063/064/068` 继续开放且不得由本 receipt 外推。
- 生产改动仅 `code_runtime/skill_outcome.py` 与 `server.py`，净新增 178 行。纯投影与 lifecycle 定向组 `37 passed, 83 subtests`，相邻 AgentRun / Session / project / activation / access / runtime-binding 回归通过，完整 `python -m pytest tests -q` 为 `2135 passed, 1 skipped, 1407 subtests`（4 条既有依赖 warning）；doctor `10/10`、quick `6/6`、Harness replay `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries` 且 hash 保持 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。前端未改，故不机械运行 freshness；完整 H4 继续按 `CODE-075` 不运行。

## 2026-09-05 02:59 · Codex

### CODE-074 Stage 3A · `skillOutcome/v1` durable shadow

- 新增 canonical-only、`mode=shadow` 的 `skillOutcome/v1`：唯一权威仍是 `skillLifecycle/v1` 中冻结的 selected Skill / evidence contract 与 AgentRun 自身的 `toolExecutions`，shadow 每次写 AgentRun v5 durable record 时由两者纯派生，不保存独立可变状态。`toolExecutions` 先经过与落盘完全相同的 JSON-safe clone，再同时用于记录和 outcome 计算；旧记录缺失、陈旧或畸形的 `skillOutcome` 在恢复时一律忽略，运行中断的 execution 先按既有规则归一为 failed，下一次正常持久化再从恢复后事实重算。shadow 不参与恢复准入、运行状态或完成判断，旧二进制可安全忽略并在后续写入时丢弃。
- 投影按 canonical 最多 2 个 Skill 及冻结 requirement 顺序输出，但 execution 按安全 call reference 确定排序。只有 completed execution 才形成 claim；同一 call 只命中全局唯一 requirement 时才接受，成功计一次、失败只记录且不满足。若同一 call 命中同 Skill 多 requirement 或跨 Skill requirement，则分别以稳定 `same_skill_multiple_requirements` / `cross_skill` 原因记入每条 claim，全部计为 ambiguous 且不满足任何 requirement；规范化 call reference 冲突整组隔离。每项 requirement 明确给出 bounded `actual` 引用 / outcome / claim state、accepted succeeded / failed / ambiguous / rejected 计数及 `missingCount` / `gap`，实际引用最多 16 条并记录截断数，非安全 call ID 仅保存 SHA-256 引用。
- `artifact/file` 本阶段只认可已成功且 result 结构为 `ok != false`、`action=write_file`、非空 path 的 `write_file`；投影不复制 path、arguments、result、command、content 或其他可能敏感正文，也不读取文件系统。aggregate 将 observing、satisfied、terminal gaps、missing / invalid contract 与 failed / cancelled 分开表达，但绝不修改 AgentRun status。没有新增 XLSX / DOCX / PPTX 或任何领域验收算法，也没有检查最终回答语义。
- 新字段只写 canonical AgentRun 的内部 durable record，不进入公开 AgentRun snapshot、UI、模型消息、事件、提示、通知或路由；无 lifecycle / flag-off legacy Run 不产生该字段。既有 `skillEvidence/v1` 单数 / 复数兼容记录、公开 snapshot、`code-review + explicit_only` pilot 的 `waiting_skill_evidence`、continue / skip / cancel、恢复与幂等 action 全部保持原实现和行为，`skillLifecycle/v1` 精确形状 / 版本也未改变。Stage 3B 仍需另行解决 generic tool 的显式 Skill 归因、领域产物正确性、最终结果验收与可选 enforcement 迁移，本阶段不把 shadow 当作完成证明。
- 生产改动为新建 `code_runtime/skill_outcome.py` 及 `server.py` 最小接线，numstat `+362/-1`、净增 `361` 行，低于批准的净增 450 行停止线；测试新增纯 projector 覆盖并扩展 lifecycle、route 与 layout 合同。直接组最终为 `30 passed, 68 subtests`，扩大 AgentRuntime / route / lifecycle / access / resource 组为 `277 passed, 209 subtests`。首次完整 Python 套件唯一失败是新模块尚未登记到精确 runtime module 布局白名单（其余 `2127 passed, 1 skipped`），只补测试清单后定向 `33 passed, 68 subtests`，最终 `python -m pytest tests -q` 为 `2128 passed, 1 skipped, 1392 subtests`（438.35 秒），仅 4 条既有 TIFF / PyPDF2 warning。
- `python verify.py doctor` `10/10`、`verify.py quick` `6/6` 与 Harness replay `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries` 均通过，replay 哈希仍为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。前端零改动，按批准的比例边界未重复 frontend freshness；未运行 `CODE-075` 所辖完整 H4，也未运行真实外部模型、外网、安装、push、tag、release 或线上配置。未读取、哈希、修改或暂存受保护的 `data/generated-assets/`、`data/image-route-registry.json`、`claude-skills` 与 `output/r035-isolated`，`CODE-063/064/068` 均保持开放。

## 2026-09-05 02:06 · Codex

### CODE-074 Stage 2B2b · runtime binding 连续性

- canonical lifecycle 的前台根 AgentRun 现在只在 active Skill 的显式 capability-specific ready check，或唯一 capability 的 `use_skill` 成功后建立既有 `skillRuntimeBindings/v1`；绑定身份直接来自 Stage 2B2a 冻结 lifecycle 的 Skill / manifest，不再按名称重新发现目录。canonical record、恢复、内存、公开投影与命令预检统一对 binding envelope、Skill 名、正文哈希、manifest 哈希、ready capability 和精确非布尔整数版本做交叉校验；畸形、额外字段、重复、nonactive 或身份冲突均 fail-closed，缺失 binding 记录仍表示尚未检查，旧 / 无 lifecycle AgentRun 保持宽容兼容。
- binding 必须在结果进入下一轮模型前先持久化；新建或替换 binding 的持久化失败会返回稳定 `skill_dependency_binding_persist_failed`，清除该目标 Skill 的新旧授权、保留其他 Skill 的合法 binding，并由外层失败结果持久化该清权状态，随后只有真实重试成功才能重新授权。binder 后置冻结快照的文件、Unicode、dependency 或 lifecycle 失败均收敛为不建 binding，不向 Agent loop 泄漏异常。
- `run_command` 的 canonical preflight 复用 Stage 2B2a 精确冻结目录，重验 Descriptor、Skill、dependency manifest、resource source / contract / content，并只从该目录取得 capability 状态；随后比较规范化 managed runtime 及 Python / Node 精确安全路径。任一 lifecycle、dependency、resource、runtime 或路径漂移都会在执行器前清除目标 binding 并以 stale 错误阻断；成功时才投影最小 Skill / capability runtime 摘要。前端、工具 / AgentRun 外层 schema、权限、Session、flag-off 与 direct legacy 行为均未改变，`CODE_SKILL_ACTIVATION_V1` 继续默认关闭。
- 生产修改仅为 `server.py` 的 `+153/-18`、净增 `135` 行，低于批准的 2 文件 / 净增 180 行上限；其余改动仅扩展 lifecycle access 合同与隔离 H4。定向验证为 access `28 passed, 45 subtests`，相邻 lifecycle / resource `31 passed, 68 subtests`；完整 `python -m pytest tests -q` 为 `2117 passed, 1 skipped, 1392 subtests`（465.21 秒），仅 4 条既有 TIFF / PyPDF2 warning。一次误用未限定 `tests` 的根目录收集在 0 tests 时被历史保留 `output/r035-isolated` 的访问权限阻断，该用户输出未读取内容、修改或清理，随后 canonical 全量一次通过。
- Harness replay 保持 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`，哈希为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；frontend freshness / bundle syntax 与 `verify.py quick` `6/6` 通过。更新后的 access H4 在 bundle / direct classic 各完成 `use → read → check → harmless command → nonactive` 五轮，命令仅命中隔离宿主精确白名单且 cleanup 全归零；首次运行因夹具缺少已有 Auto 风险确认标记而在创建 AgentRun 前被 UI 门禁拦截，完整审计后只补 `v1` 确认标记，首次重跑通过。既有 activation handshake 四链与 infrastructure self-check 同样通过；按 `CODE-075` 未运行完整 164 项 H4。
- 本阶段未运行真实外部模型、外网、安装、push、tag、release 或线上配置；完整 pytest 内既有受控 Office 用例按原合同执行。未读取内容、哈希、修改或暂存受保护的 `data/generated-assets/`、`data/image-route-registry.json` 与 `output/r035-isolated`，也未启动 Stage 3 或关闭 `CODE-063/064/068`。

## 2026-09-05 01:04 · Codex

### CODE-074 Stage 2B2a · active-only 访问与资源冻结

- `skillLifecycle/v1` 的访问子合同升级为 `access/v2`：仅 canonical lifecycle 的前台根 AgentRun 使用新路径，旧 `access/v1` 空记录继续只读恢复，并在首次成功读取普通文本 reference 时原子升级。绑定只保存 active Skill 名、规范相对路径和内容哈希，最多 64 项、唯一且确定排序；文件身份以跨平台保守的大小写不敏感键判重和匹配，同时保留首次路径文本供审计，因而同字节 case-only alias 幂等、变化后拒绝，持久记录中的 alias duplicate 也 fail-closed。首次绑定必须在内容返回模型前持久化，注入持久化失败时同步回滚内存状态、不泄漏正文，恢复后重试必须重新完成真实持久化。
- `use_skill`、`check_skill_dependencies` 与 `read_skill_resource` 在任何 Skill 目录枚举、正文、manifest 或资源解析前先核对 lifecycle 已选名称；随后只从冻结的相对目录重验 Descriptor、`SKILL.md`、有效 dependency manifest 及 executable resource 的有效来源与合同哈希。`use_skill` 只返回紧凑 active 状态、完整依赖状态和经 activation 固定的可信 runtime resources，不再重复正文；提示词只允许正文确实需要可信运行资源时为当前 active Skill 调用，禁止动态激活其他 Skill。普通 reference 按精确文件读取，绝对路径、遍历、隐藏路径、`SKILL.md` 大小写别名、reparse 与超限文件均不进入绑定。
- 本阶段按审批拆为 2B2a / 2B2b：canonical `check_skill_dependencies` 仅提供冻结身份后的只读状态，并显式跳过既有后置 runtime binder；因此不会新建或更新 `skillRuntimeBindings`，依赖命令在没有既有合法绑定时继续以 `skill_dependency_check_required` 阻断。runtime binding 的单向建立、持久记录交叉校验与命令预检精确路径连续性全部保留给 2B2b，`CODE-074` 仍开放。flag-off、无 lifecycle 的旧 AgentRun、direct `/api/tools`、background、child / subagent、权限、Session、AgentRun 外层 v5、公开 snapshot 与前端行为均不变。
- 生产修改严格限于 `code_runtime/skill_activation.py`、`code_runtime/skill_lifecycle.py`、`code_runtime/skill_resources.py` 与 `server.py`，最终 numstat 为 `+409/-26`、净增 `383` 行，低于批准的 400 行上限。新增 19 项 access 合同 / AgentRun 测试覆盖 nonactive / no-match 零 Skill IO、同名替代目录与 Descriptor、Skill / manifest / resource source / contract / content 漂移、compact use、持久化顺序与失败回滚、重启、重复、并发、上限 / 排序 / 唯一性、case-only alias、v1 空访问升级，以及 legacy / direct / background / child 对照。
- 自动验证为 Skill 扩大组 `143 passed, 118 subtests`、AgentRuntime 串行 `137 passed, 81 subtests`；R013 大小写身份修正前的唯一完整 pytest 为 `2107 passed, 1 skipped, 1372 subtests`（700.96 秒）。并行扩大组曾出现既有 0.12 秒首响应时限、Windows 临时目录锁、假上游脚本串扰和 updater 5 秒交接超时，四项逐一串行隔离复验均通过，完整 pytest 未复现。R013 只改 `skill_lifecycle` 的 binding 判重 / 查找 / 排序键并新增测试，相关 access + lifecycle 最终为 `38 passed, 93 subtests`；按审批允许复用此前全量证据，未机械重跑 700 秒套件。Harness replay 保持 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`，哈希仍为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；R013 后 frontend freshness、bundle / 变更 Python 语法与 `verify.py quick` `6/6` 通过。
- 新增隔离 access H4：bundle 与 direct classic 均真实完成 `use → read → check → nonactive` 四轮工具链，持久记录只含一个文本哈希绑定、无 runtime binding，直连 legacy `use_skill` 仍返回正文；子进程、双端口、临时根和 active child 全部归零，R013 后已再次通过。既有 activation handshake 的 bundle / classic canonical 与 flag-off legacy 四链及 H4 infrastructure self-check 同样通过。最初两次开发运行在浏览器链开始前由 H4 旧工具网络围栏确定性拒绝新增 direct 对照；第二次取得安全错误体后已停止原夹具并改为精确 payload 白名单，重设计后的最终链通过。按 `CODE-075` 边界未运行已知超过 600 秒的完整 164 项 H4，未宣称 `verify.py runtime` 全档通过。
- 本阶段未运行真实外部模型、未批准外网、安装、push、tag、release 或线上配置；完整 pytest 内既有受控 Office 用例按原合同执行。未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，也未开始 Stage 2B2b、Stage 3 或关闭 `CODE-063/064/068`。
