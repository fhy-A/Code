# 2026-08-01

## 2026-08-01 23:55 · 用户验收

### Code v0.5.32 正式更新与 bundle 入口验收通过

- 用户完成 v0.5.32 正式版更新并确认全部检查项正常：3010 能正常启动、刷新和对话，3011 开发版可同时运行，开发/正式端口隔离保持有效。
- 正式版默认 bundle 页面正常加载，经典回退地址 `/dist/frontend/index.classic.html` 也可正常打开；本轮未发现更新、页面资源或运行时兼容问题。
- 首个 bundle 正式版本的上线与初次人工验收至此收口；经典页面和原始脚本继续至少保留一个稳定版本，后续观察无回退需求后再评估 6D 清理兼容层。

**涉及记录**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 23:30 · Codex

### OpenAI、DeepSeek 与 xAI 官方价格采集器首批验证

- 在独立仓库 `模型定价/渠道采购体系` 新增 `工具/official_pricing_collector.py`，由既有 Workbar 模型快照精确派生 OpenAI 6 个、DeepSeek 2 个和 xAI 2 个模型，不在采集器中维护代表模型清单，也不接受别名猜测。
- 采集器只读访问 HTTPS 官方公开页面，不调用需 Key 的接口；固定官方域名白名单，禁止认证查询参数和跨域跳转，并限制超时、响应大小和内容类型。模型字段不全时来源降级为 `partial`，整源不可达时标记为 `failed`，只保存脱敏错误码并交由既有差异引擎续用上一有效价格。
- 标准记录保留输入、输出、缓存读取及实际存在的缓存写入价格，同时结构化保存 OpenAI 长上下文倍率、xAI 长上下文分档与批处理折扣，以及 DeepSeek 已公告但尚未生效的北京时间峰谷规则；本轮没有修改 Workbar 基础价、分组倍率或线上渠道配置。
- 新增最小冻结来源样本和 9 项测试，覆盖精确模型身份、字段失败、整源故障、上一有效值续用、URL 安全和重复采集零误报；渠道采购体系完整回归 46 项通过。两次独立实时只读采集均得到 10 条记录、3 个来源全部成功，接入现有差异引擎后语义变化和数据质量问题均为 0。
- 与 2026-07-29 Workbar 快照按固定汇率 `6.7` 反算后，产生两个明确候选告警：`gpt-5.6-terra` 当前官方输入/输出/缓存读取均较旧值下降 20%，`gpt-5.6-luna` 均下降 80%。DeepSeek 当前 USD 价格换算后与现有人民币快照存在 2.93%–6.61% 差异，当前归为币种与舍入口径待核，不直接认定调价；三类复核均已写入 `TODO.md`，线上值保持不变。
- 后续继续接入其余官方厂商、12 条上游来源、当前 Workbar 映射与基础价快照、倍率/毛利计算及每日 09:00 调度；以上未完成项继续保留在 `TODO.md`。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`、`../../模型定价/渠道采购体系/工具/official_pricing_collector.py`、`../../模型定价/渠道采购体系/tests/test_official_pricing_collector.py`、`../../模型定价/渠道采购体系/tests/fixtures/official-pricing/frozen-sources.json`、`../../模型定价/渠道采购体系/outputs/20260801/official-pricing-collector-validation.md`、`../../模型定价/渠道采购体系/模板/pricing-monitor-sources.example.json`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/规范/07_模型价格监控与变更提醒.md`、`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/MANIFEST.sha256`

## 2026-08-01 23:28 · Codex

### Code v0.5.32 首个 bundle 正式版本发布

- 使用项目正式发布脚本和自动检测到的 Windows 代理完成 `v0.5.32` 发布；脚本依次通过版本一致性、前端强制构建与新鲜度门禁、全量测试、JavaScript/Python 语法检查、EXE 构建、Windows 版本元数据和 SHA-256 校验，再生成无占位内容的中文发布说明。
- 发布元数据提交为 `9928a61`，远端 `master` 和标签 `v0.5.32` 均指向该提交；GitHub Release 已创建且不是草稿或预发布：[Code v0.5.32](https://github.com/fhy-A/Code/releases/tag/v0.5.32)。
- 发布资产 `Code-v0.5.32.exe` 为 31,482,337 bytes，GitHub 记录与本地一致的 SHA-256：`3A44E010C1D5BF7EDA6F0B692E95817DC44480CF5606149245AF0FB52AA3A672`。EXE 元数据确认 `ProductVersion`、`FileVersion` 和 `OriginalFilename` 均为 `0.5.32` 对应值。
- 本次仅完成构建、推送、标签和 GitHub Release，没有替用户停止、更新或启动 3010 正式实例。下一步由用户执行正式版更新，并验证 3010/3011 端口隔离、默认 bundle 页面和经典回退；兼容资源至少保留一个稳定版本后再评估 6D。

**涉及文件与远端**：`VERSION`、`file_version_info.txt`、`README.md`、`Code-v0.5.32.spec`、`docs/releases/v0.5.32.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`、Git 标签与 GitHub Release `v0.5.32`

## 2026-08-01 23:03 · Codex

### app.js 阶段 6C-3：EXE 资源与发布门禁

- `build_exe.py` 在 PyInstaller 开始前依次强制构建 bundle、校验源码/产物哈希并执行 bundle 语法检查；默认 `index.html`、`dist/frontend/code.bundle.js`、`dist/frontend/index.classic.html` 以及原始 `src/`、`app.js`、`agent-runtime.js` 一起进入 EXE，source map、metafile 和构建状态不打包。打包环境需要 Node，生成后的 EXE 运行时不依赖 Node。
- `release.py` 在代码质量阶段增加独立前端门禁：正式模式先构建再校验，`--dry-run` 只执行新鲜度和语法检查；`build_exe.py` 在真正打包前再次复核，防止两阶段之间产物被改动。任何命令失败都会在 PyInstaller、Git 提交、标签、推送和 GitHub Release 之前终止。
- 真实执行 `python build_exe.py` 成功生成本地 `dist/Code-v0.5.31.exe`，大小 31,481,686 bytes；PyInstaller 归档查看器确认包含 bundle、经典回退页、`app.js`、`agent-runtime.js` 和 `src/core/namespace.js`，且不包含 `code.bundle.js.map`、`code.bundle.meta.json` 或 `code.bundle.state.json`。`Code-v0.5.31.spec` 已按真实构建结果刷新资源清单。
- 只读调用发布前端门禁前后六类 `dist/frontend` 产物 SHA-256 完全一致；前端/发布定向回归 152 项及 5 个子测试通过，完整回归 899 项及 236 个子测试通过，Python 语法、bundle 新鲜度及 `git diff --check` 通过。构建仍出现 `server.py` 既有的 `invalid escape sequence '\\C'` SyntaxWarning，本阶段未扩大范围处理。
- 本阶段没有启动新 EXE、停止或替换 3010、修改线上配置、提交版本号、打标签、推送或发布；当前正式版本仍使用此前入口。本地构建验证不能视为正式发布，下一次发版及更新/端口人工验收仍需用户明确授权。

**涉及文件**：`build_exe.py`、`release.py`、`Code-v0.5.31.spec`、`tests/test_frontend_modules.py`、`tests/test_release_script.py`、`docs/APP_JS_SPLIT_PLAN.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 22:56 · Codex

### 渠道采购与定价体系建立独立本地仓库

- 在根目录 `模型定价/渠道采购体系` 初始化独立 Git 仓库，默认分支为 `main`，首个本地提交为 `e6dbd04 chore: 初始化渠道采购与定价仓库`；纳入规范、模板、工具、测试、脱敏证据包、定价快照和既有分析输出，共 394 个文件。
- 新增仓库级 `.gitignore` 与 `.gitattributes`：本地渠道注册信息和其他 `本地配置` 文件、真实环境文件、每日价格监控运行快照、缓存及锁文件不进入版本控制；`本地配置/README.md` 作为安全说明保留。提交前按路径和内容完成敏感信息审计，没有发现字面量凭据。
- 初始提交前复验采购体系全部 37 项测试、`MANIFEST.sha256`、暂存差异格式及禁入路径，结果均通过。仓库当前工作区干净，未配置远程地址，也未执行 Git 推送、标签、发布或线上配置修改。

**涉及文件**：`docs/development-log/2026/2026-08-01.md`、`../../模型定价/渠道采购体系/.gitignore`、`../../模型定价/渠道采购体系/.gitattributes`、`../../模型定价/渠道采购体系/MANIFEST.sha256`、`../../模型定价/渠道采购体系/本地配置/README.md`

## 2026-08-01 22:42 · Codex

### app.js 阶段 6C-2：开发默认 bundle、自动重建与经典回退

- 将开发默认 `index.html` 从 31 个本地经典脚本切换为单一 `dist/frontend/code.bundle.js`；`src/frontend-entry.js` 在全部依赖与 `app.js` 同步初始化完成后写入就绪标记，页面在 bundle 加载失败或同步入口初始化失败时自动跳转到 `dist/frontend/index.classic.html`。
- 构建脚本不再从默认页面复制经典脚本标签，而是以唯一入口的 31 条导入清单生成回退页，固定回退资源无重复、顺序一致且末尾仍为 `agent-runtime.js`、`app.js`。预览页继续支持相对 bundle 与同目录回退，默认页和回退页可共用当前静态服务。
- `dev_server.py` 在 3011 启动及 `/`、`/index.html` 刷新时串行执行 bundle 新鲜度检查；过期时按“检查、构建、复核”自动恢复，失败返回 503，避免继续运行半成品。Node 缺失和 60 秒超时会给出明确启动错误；Windows 子进程使用 `CREATE_NO_WINDOW` 与隐藏启动参数，修复每次刷新短暂闪现终端窗口的问题。
- 本批只切换 3011 源码开发入口；3010、当前正式 EXE、`build_exe.py`、发布脚本、API、JSONL、配置和工具协议均未修改。回退只需撤销本阶段提交，经典脚本资源继续完整保留；下一步 6C-3 才接入正式 EXE 与发布链。
- 自动验证通过：`npm run check:frontend` 与 `npm run verify:frontend`；前端和开发服务定向回归 155 项及 3 个子测试；完整回归 895 项及 236 个子测试；3011 根页、bundle 和经典回退页均返回 HTTP 200，`git diff --check` 通过。用户重启 Code Dev 后确认默认页与回退路径可正常使用，并确认修复后连续刷新不再出现终端窗口。

**涉及文件**：`dev_server.py`、`index.html`、`src/frontend-entry.js`、`scripts/build-frontend.mjs`、`tests/test_dev_server.py`、`tests/test_frontend_modules.py`、`docs/APP_JS_SPLIT_PLAN.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 19:07 · Codex

### workbar 全模型价格监控首批只读引擎

- 用户确认当前“渠道与定价”任务同时覆盖渠道采购、正式准入和定价；价格监控第一阶段固定检查模型厂商官方 API 定价与当前 12 条启用上游渠道，并按 workbar 实际启用映射动态覆盖全部模型，不使用首轮硬门槛代表模型的固定清单。计划运行时间为北京时间每日 09:00，无变化静默并保留每周健康摘要。
- 在根目录 `模型定价/渠道采购体系` 新增价格监控规范、三份 JSON Schema、12 条渠道来源登记模板和 `工具/pricing_monitor.py`。首批工具只接收已经脱敏的本地 JSON，使用十进制规范字符串生成不可覆盖历史快照，忽略抓取时间和数值格式差异，识别价格、计费规则及模型记录的语义变化并生成中文报告；未读取真实 Key，未执行实时网页或 New API 请求，也未修改线上 workbar。
- 增加来源健康状态与故障保护：采集源为 `partial` 或 `failed` 时保存脱敏错误码、续用上一份有效模型价格并标记数据质量异常，不会把来源故障误报为模型下架；恢复后继续与最后有效价格比较。新增两份冻结样本和 9 项定向测试。
- 自动验证通过：价格监控定向测试 9 项；采购体系完整回归 37 项；三份生成契约通过 Draft 2020-12 Schema 验证；工具源码隔离临时路径编译；三份 Schema、来源模板 JSON 解析；完整性清单逐文件 SHA-256 校验。一次直接编译测试模块时因 Windows 占用既有 `__pycache__` 目标返回访问拒绝，改用隔离临时输出后源码编译通过，不属于实现失败。
- 后续阶段保留在 `TODO.md`：逐个接入官方和 12 条渠道价格适配器、读取当前启用模型映射、补统一倍率与毛利影响计算，然后再创建每日 09:00 调度和通知。任何基础价、分组倍率或线上配置修改仍需用户明确授权。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/MANIFEST.sha256`、`../../模型定价/渠道采购体系/规范/07_模型价格监控与变更提醒.md`、`../../模型定价/渠道采购体系/schema/pricing-*.json`、`../../模型定价/渠道采购体系/模板/pricing-monitor-sources.example.json`、`../../模型定价/渠道采购体系/工具/pricing_monitor.py`、`../../模型定价/渠道采购体系/tests/`

## 2026-08-01 18:11 · Codex

### app.js 阶段 6C-1：bundle 哈希与经典回退护栏

- 扩展 `scripts/build-frontend.mjs`，构建时除 bundle、source map、metafile 和显式预览页外，同时生成可直接访问的经典脚本回退页 `dist/frontend/index.classic.html`；回退页使用根路径加载现有 31 个源码脚本，当前默认 `index.html` 和 3011 根页仍保持经典加载方式。
- 新增确定性的 `code.bundle.state.json`：记录 31 个入口依赖及构建脚本、页面模板、package 元数据的规范化输入列表和 SHA-256 源码指纹，并记录五类构建产物的大小与 SHA-256。构建开始前先删除旧状态；构建期间源码变化会使构建失败，不会留下可通过校验的半成品状态。
- 构建脚本新增只读 `--check` 模式；`npm run verify:frontend` 组合执行状态/产物哈希校验和 bundle 语法检查，`npm run check:frontend` 继续先构建再验证。本批尚未接入开发启动、`build_exe.py` 或发布脚本，也未修改 3010、EXE、API、会话或 UI 行为。
- 回归测试在隔离临时目录验证两次构建的 bundle、预览页、经典回退页和源码指纹一致，并确认正常状态通过、bundle 篡改、伪造源码指纹及回退页缺失均被阻断。3011 默认根页保持 31 个经典脚本，bundle 预览与经典回退页均返回 HTTP 200。
- 自动验证通过：`npm run check:frontend`；前端模块测试 143 项；完整回归 889 项及 236 个子测试；JavaScript 语法检查和 `git diff --check` 通过。下一步 6C-2 才切换开发默认入口并接入刷新时自动重建。

**涉及文件**：`package.json`、`scripts/build-frontend.mjs`、`tests/test_frontend_modules.py`、`docs/APP_JS_SPLIT_PLAN.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 18:04 · 用户确认

### workbar 官方用户文档方向与协作方式

- 用户确认采用独立文档站方向，面向普通 AI 工具使用者和初学者组织内容；文档以“完成一个具体任务”为主，不照搬面向开发者的传统 API 手册，完整 API 参考后置。
- 文档按单一主题逐篇推进：每轮先核对生产环境真实界面、路由和配置值，再编写正文；必要时补充经过脱敏和裁切的操作截图，用户验收当前主题后才进入下一篇，避免一次铺开后集中返工。
- 每篇统一采用“能完成什么、开始前准备、分步操作、成功标志、常见问题、下一步”的结构；使用新手语言并解释首次出现的术语，示例不得包含真实密钥或其他敏感信息。
- 第一篇确定为《5 分钟快速开始》，优先覆盖注册登录、创建和保护连接密钥、填写服务地址、选择模型以及确认首次使用成功；文档首页、其他工具教程、模型分组、余额费用、故障排查和 API 参考按后续独立主题推进。

**涉及记录**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 17:45 · Codex

### 问卷工具执行轨迹去重与本地化

- 修复模型仅发出 `request_user_input` 工具调用时生成原始 `→ request_user_input` 过渡行的问题：新工具轮改用本地化的运行提示，消息投影同时兼容识别旧 JSONL 中已有的原始箭头行，不重写历史会话数据。
- 问卷工具组运行中显示“询问用户”，完成后显示“询问了用户”；多次调用显示“多次询问了用户”，英文对应为 `Ask user`、`Asked the user` 和 `Asked the user multiple times`。原始工具调用及结果继续保留在可展开组内，不减少审计信息。
- 本批只调整工具进度文案、消息投影分类、工具标签和完成统述；未修改 `request_user_input` 工具协议、问卷状态、提交与恢复时序、AgentRun、JSONL schema 或持久化行为，旧会话无需迁移且可通过回退本提交恢复原投影。
- 自动验证通过：前端模块测试 143 项；完整回归 889 项及 236 个子测试；`npm run check:frontend` 重新生成 bundle 预览并通过语法校验；JavaScript 语法检查和 `git diff --check` 通过。用户在 3011 bundle 预览中完成旧会话、新问卷工具组及相关界面人工验收并确认无问题。

**涉及文件**：`app.js`、`src/ui/messages.js`、`src/core/i18n.js`、`tests/test_frontend_modules.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 17:44 · 用户验收

### workbar 新版生产环境完整验收通过

- 用户继续完成其余上线项目验收并确认均无问题：首页导航、登录、注册、双重身份验证、模型广场分组、令牌自动分组与模型限制、钱包充值、付款确认、推荐奖励说明及奖励转入余额流程均正常。
- 用户同时确认生产环境原有支付、邮件、域名及其他运营配置未因本次镜像部署被覆盖；本轮 workbar 用户端与品牌部署的生产验收正式收口。
- `TODO.md` 已移除对应的生产环境上线验收事项，尚未完成的官方用户文档和其他长期工作继续保留。

**涉及记录**：`TODO.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 17:42 · 用户验收

### workbar 生产环境主页与标签页 Logo 生效

- 生产环境已将 Logo URL 配置为同源 workbar SVG 路径，并将首页内容配置为内置 workbar 首页路径。
- 用户确认 `https://workbar.ai/` 已显示新版主页，浏览器标签页 Logo 也已更新；对应静态资源和生产后台配置均已生效，不需要为这两项重新部署镜像。
- Edge 曾短暂保留旧标签图标，Codex 内置浏览器已先显示新图标；最终更新后确认属于浏览器 favicon 缓存差异，不是服务器资源缺失。

**涉及记录**：`TODO.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 17:25 · Codex

### app.js 阶段 6B：3011 显式 bundle 预览与人工验收

- 扩展 `scripts/build-frontend.mjs`，在 `dist/frontend/` 同时生成 bundle 专用 `index.html`：移除 30 个经典模块/Runtime 标签，在原 `app.js` 的页面底部位置加载单一 bundle，并将样式、图标改为根路径；KaTeX / marked 外部资源保持不变，避免提前改变依赖边界。
- 预览只通过 3011 的显式 `/dist/frontend/index.html` 地址访问，利用现有静态文件服务即时生效，无需重启 Code Dev；默认 `/` 仍加载 29 个 `src/` 模块以及 Runtime / app 经典脚本，3010、`build_exe.py`、发布链和当前 EXE 均未引用或包含该旁路产物。
- 自动验证固定 bundle 预览标记、资源路径、单一脚本、执行位置、经典内部脚本完全移除和重复构建一致；预览 HTML 与 bundle 通过当前 3011 返回 HTTP 200。前端/开发服务定向回归 148 项及 3 个子测试通过，`git diff --check` 通过。
- 用户完成 bundle 预览人工验收并确认页面、会话、模型列表、只读工具、问卷刷新恢复、最终回答与工具轨迹、设置中英文切换及 `/compact` 取消流程均无问题。人工验收同时发现独立样式优化点：执行轨迹中的原始 `→ request_user_input` 行与下方工具组重复且暴露内部英文名，已记入 `TODO.md`，不混入本次构建链提交。

**涉及文件**：`scripts/build-frontend.mjs`、`tests/test_frontend_modules.py`、`docs/APP_JS_SPLIT_PLAN.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 17:00 · Codex

### app.js 阶段 6A：esbuild 构建链与兼容顺序护栏

- 固定 `esbuild 0.28.1` 为开发依赖，新增唯一入口 `src/frontend-entry.js`，按 `index.html` 当前 31 个内部经典脚本的实际顺序导入全部模块、`agent-runtime.js` 与 `app.js`；外部 KaTeX / marked CDN 和现有页面结构不进入 bundle。
- 新增 `scripts/build-frontend.mjs`、`npm run build:frontend` 与 `npm run check:frontend`，生成浏览器 IIFE bundle、外置 source map 和 metafile。6A 采用兼容优先配置：不压缩、保留函数名、关闭 tree-shaking，默认输出到已被 Git 忽略的 `dist/frontend/`，不删除或覆盖 `dist/` 中其他发布文件。
- 强化 `tests/test_frontend_modules.py`：经典加载顺序与 bundle 入口必须逐项相同且无重复，补入旧护栏遗漏的 `theme-engine.js` 和 `subagents.js`；测试在临时目录连续构建两次，固定 bundle 字节一致、语法有效和关键输入完整，同时断言 `index.html` 与 `build_exe.py` 尚未引用 bundle。
- 本批仅建立旁路构建能力，没有修改 3011、3010、`index.html`、开发启动、PyInstaller、发布脚本、API、持久化或 UI 行为。首个产物约 914,903 bytes，外置 source map 约 1,394,137 bytes；它不参与当前运行，回退可按本批单提交完整撤销。
- 自动验证通过：`npm run check:frontend`；前端模块定向回归 142 项通过；33 个 JavaScript/MJS 文件语法检查通过；完整回归 888 项及 236 个子测试通过；`git diff --check` 通过。下一步 6B 仅建立 3011 显式预览入口，人工冒烟前不切默认页面或正式 EXE。

**涉及文件**：`package.json`、`package-lock.json`、`src/frontend-entry.js`、`scripts/build-frontend.mjs`、`tests/test_frontend_modules.py`、`docs/APP_JS_SPLIT_PLAN.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 16:26 · Codex

### app.js 第五阶段 5G-2 与阶段 5 正式收口

- 在 5G-1 状态矩阵和源码所有权护栏基础上完成收口验证：`runServerAgentLoop()` 同时编排 AgentRuntime 创建与监听、凭据恢复、游标与事件投影、问卷、授权、模型能力刷新和终态清理，唯一可继续迁移的状态标签映射没有独立消费者或副作用隔离价值。
- 正式决定不创建 `src/agent/agent-loop.js`，保留 `runServerAgentLoop()` 作为 `app.js` 的跨领域入口编排层；该决定减少跨文件接口和初始化顺序风险，不改变 AgentRun、JSONL、API、工具协议、权限、持久化、UI 或运行时行为，也不以目标目录或行数要求制造薄模块。
- 阶段 1 至 5 至此按职责所有权全部完成，`TODO.md` 移除 5G 待办。阶段 6 esbuild 会改变资源加载、调试、EXE 打包与回退方式，继续后置并要求单独分析、方案确认和阶段验证；导入语义、模型真实上下文上限等产品兼容议题继续保留在各自待办中。
- 自动验证通过：`node --check app.js`；Agent 主链定向回归 280 项及 15 个子测试通过；完整回归 887 项及 236 个子测试通过；`git diff --check` 在文档更新后通过。本阶段未修改业务代码。

**涉及文件**：`docs/APP_JS_SPLIT_PLAN.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 16:10 · Codex

### Hermes PATH 冲突诊断与 Code Dev 托盘恢复

- 确认本次问题起因：Hermes Agent 安装脚本为了让 `hermes` 命令全局可用，将其内部 `venv\Scripts` 永久放到了 Windows 用户 PATH 前部；该目录同时提供 `python` / `pythonw`，但 Hermes 的裁剪环境不含 pytest、Office 工具依赖和 `pystray`，因此裸解释器被替换后先后表现为测试入口不可用、Office 工具测试缺依赖，以及 Code Dev 3011 可正常对话但托盘图标消失。正式版 3010 使用打包后的可执行文件，未受本次解释器顺序影响。
- 完成本机环境恢复：新增独立的 `hermes.cmd` / `hermes-agent.cmd` 启动器，将 Hermes 内部 `venv\Scripts` 保留但移至用户 PATH 末尾，并在 `C:\Users\Admin\AppData\Local\hermes\path-backups\20260801-152746-user-path.txt` 留存修改前备份；系统 Python、pytest 与 Hermes 命令均已重新验证，未向 Hermes 内部环境安装项目依赖。
- 仅停止并重启了已核对的 3011 Code Dev 进程树，改由系统 `pythonw` 启动，3010 正式版保持运行且未操作；`/api/version`、项目目录和浏览器能力检查正常，用户已确认开发版托盘图标恢复。Code 业务代码与数据均未修改。
- 将同类环境污染保留为后续观察项：关注其他 Agent / 开发工具安装、Hermes 更新和系统重启是否再次改变 PATH 或解释器所有权；以 `where python/pythonw/hermes`、pytest/托盘依赖导入、3011 托盘和 Office 命令冒烟作为巡检信号。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 15:52 · Codex

### app.js 第五阶段 5G-1：服务端主循环状态矩阵与所有权护栏

- 只读审计 `runServerAgentLoop()`、`AgentRuntime.watchAgentRun()` 及现有前端回归覆盖，确认 `watchAgentRun()` 在 `running` 时自行长轮询，前端返回点只负责 `waiting_credentials`、`waiting_user_input`、`waiting_authorization`、`completed`、`cancelled` 和失败终态；将各状态的既有动作、后续流向与副作用所有权写入 `docs/APP_JS_SPLIT_PLAN.md`。
- 扩展 `tests/test_frontend_modules.py` 的源码所有权护栏，固定凭据恢复、事件监听、cursor 提交、问卷、授权、完成清理、取消错误、失败分类与 `model_access_denied` 模型刷新顺序；同时断言本批不创建或加载 `src/agent/agent-loop.js`。未修改 `app.js`、AgentRun、JSONL、API、工具协议、权限、持久化或 UI 行为。
- 审计结论为不建议在 5G-2 创建薄模块：唯一可迁移候选只是单消费者的状态标签映射，不能隔离 AgentRuntime、网络恢复、事件投影、问卷、授权、模型刷新或终态清理副作用，跨文件接口成本高于维护收益。5G-2 继续保留待决，等待用户确认是否按“保留入口编排层”完成阶段 5 收口。
- 自动验证通过：`node --check app.js` 与 `git diff --check` 通过；Agent 主链定向回归 303 项及 13 个子测试通过；Office 专项 18 项通过；在重新载入持久化用户 PATH 的新进程环境中，全量回归为 882 项及 236 个子测试通过。首次全量尝试的 13 个 Office 失败已定位为当前 Codex 进程仍继承 Hermes 安装时置顶的内部 Python 路径，未修改依赖或测试代码。

**涉及文件**：`docs/APP_JS_SPLIT_PLAN.md`、`tests/test_frontend_modules.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 04:44 · Codex

### v0.5.31 端口隔离人工确认与刷新闪烁待办

- 用户完成 v0.5.31 发布后的首次端口隔离检查并确认正常；本次只记录 3010 正式版与 3011 开发版端口隔离结果，不扩大描述为尚未逐项回传的数据目录、登录态或重启入口验收。
- 新发现页面刷新时模型列表会重新获取，并在请求完成前短暂显示为空；已作为独立 P1 UI 状态待办记录，后续采用保留上一次成功列表、区分刷新中状态、成功后原子替换和失败保留旧数据的方案评估。本阶段未修改业务代码。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 04:02 · Codex

### v0.5.31 发布前状态与文档收口

- 核对 app.js 职责拆分主线：阶段 1 至 4、阶段 5A 至 5F 已完成，下一步为 5G `agent-loop.js` 只读职责审计；同步修正拆分计划顶部状态和“推荐的下一步”，不以行数目标强行迁移高耦合主循环。
- 修复开发日志迁移后 README 仍引用已移除 `docs/GUIDE.md` 的失效链接；确认原 `CHANGELOG.md` 与冻结归档 `docs/development-log/archive/CHANGELOG-through-2026-07-29.md` 的 Git blob 哈希一致，历史没有因迁移丢失。
- 将 `data/session-archive/` 纳入本地运行数据忽略范围，避免 310 个测试会话归档、约 137 MB 数据被发布提交误收录；未删除或移动现有会话归档。
- 发布前自动验证通过：33 个已跟踪 JavaScript 文件语法检查通过，Python 入口可解析；Agent 主链定向回归 302 项及 13 个子测试通过，完整测试 881 项及 236 个子测试通过；当前 3011 开发实例主页和 `app.js` 均返回 HTTP 200。`server.py` 仍只有既有的 `invalid escape sequence '\\C'` SyntaxWarning。
- 本记录仅确认开发版本可继续使用；正式 EXE 更新、3010 正式端口、3011 开发端口及两套数据目录隔离将在 v0.5.31 发布后由用户执行首次人工更新验收。

**涉及文件**：`.gitignore`、`README.md`、`TODO.md`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 03:27 · Codex

### AgentRun 自动上下文压缩与任务中途续行

- 前端在创建前台及后台 AgentRun 时按模型族传递 `contextLimit`；服务端对旧客户端和旧持久记录保留相同的模型族默认值，并在每次模型请求前估算包含消息、工具定义与请求选项的完整上下文。默认达到上下文上限的 90% 时触发压缩，中文与其他非 ASCII 文本使用保守估算，避免只按英文字符比例低估。
- 自动压缩保留所有系统消息、当前真实用户任务原文，以及当前任务内最近一组完整的 assistant 工具调用和对应 tool 结果；其余较早过程交给同一模型生成检查点摘要。压缩请求不计入 Agent 模型轮次，但 usage 会并入任务累计用量；成功后在同一 AgentRun、同一任务中继续下一次模型请求。
- 上游实际返回 `context_window_exceeded`、`maximum context length` 等上下文错误时，同一模型轮次最多执行一次响应式压缩与重试，第二次仍超限则保留原错误终止，避免无限循环。压缩失败不会替换原始消息；取消任务时会通过现有活动 runtime 取消链路停止压缩请求。
- AgentRun 持久记录以向后兼容的新增字段保存 `contextLimit`、压缩记录和响应式恢复轮次；旧 version 2 记录无需迁移即可恢复。压缩开始、完成和失败均写入持久事件流；前端按事件原位显示“正在自动压缩上下文…”并在完成后更新为“上下文已自动压缩”，该记录与阶段说明和工具组保持时序，最终回答出现后一起收进“已处理”执行轨迹。
- 自动压缩不弹确认框；手动 `/compact` 继续保留确认流程与最近 3 个完整用户轮次，同时修复消息替换后未显式 `persistMessages: true` 的持久化缺口。上下文用量提示已从未实现的 95% 文案改为实际的 90%。会话 JSONL、工具协议和既有手动压缩 API 均未做破坏性修改。
- 自动验证通过：`python -m pytest tests -q -p no:cacheprovider` 共 881 项及 236 个子测试通过；Agent 运行时、前端模块与 P0 定向阶段回归均通过；Python/JavaScript 语法检查和目标文件 `git diff --check` 通过。`server.py` 仍报告既有的 `invalid escape sequence '\\C'` SyntaxWarning，本批未扩大范围处理。按用户约定未操作浏览器或系统界面，真实页面视觉验收留待用户手动重启 Code 后确认。

**涉及文件**：`server.py`、`agent-runtime.js`、`app.js`、`src/ui/messages.js`、`src/core/i18n.js`、`styles.css`、`tests/test_agent_runtime.py`、`tests/test_frontend_modules.py`、`tests/test_p0_stability.py`、`tests/test_p2_coverage.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 02:30 · Codex

### workbar 用户端、品牌资源与部署整合

- 完成 `new-api-source` 中的 workbar 用户端改造并提交至 `fhy-A/new-api` 的 `custom-local-dev` 分支；远端提交为 [`a00b81b`](https://github.com/fhy-A/new-api/commit/a00b81b9607193b3b401c0ca484ad2c5a67956c6)，GitHub Actions [镜像构建任务](https://github.com/fhy-A/new-api/actions/runs/30653600191) 已成功完成，发布 `ghcr.io/fhy-a/new-api:latest` 与 `ghcr.io/fhy-a/new-api:a00b81b`。
- 品牌侧新增 workbar 通用 SVG Logo，并设为默认 Logo 与浏览器页签图标；Logo 改用同源路径 `/workbar-brand/export-v1/workbar-symbol-universal.svg`。首页作为内置资源 `/workbar-homepage/homepage-concept-v1.html` 随前端构建发布，不依赖 `localhost`、固定域名或额外静态文件服务器。
- 首页和公共导航已完成 workbar 内容、响应式布局、模型接口示例、复制交互、站内路由与滚动紧凑样式；登录、注册和双重身份验证页面完成统一视觉改造，密码显隐与验证码输入交互已优化，注册成功进入登录流程时可带入刚填写的信息。
- 模型广场已修正模型卡片分组展示；令牌未选择具体分组时使用自动分组行为，模型限制选项按当前分组动态过滤，未选具体分组时取自动分组顺序内可用分组的模型集合。
- 钱包充值区域、付款确认和奖励转入窗口已统一金额表达并读取系统默认结算货币；推荐奖励说明与后台配置数值同步，并区分邀请人待转入奖励和受邀人自动到账奖励。
- 部署说明已写入 `new-api-source/docs/workbar-deployment.md`，构建部署包会携带该文档。生产环境后台值、支付、邮件和域名等运营配置未写入代码，也未记录任何密钥或敏感值；部署后的后台配置和人工验收继续保留在 `TODO.md`。
- 根目录、`code` 与 `new-api-source` 协作文档已补充跨仓库统一归档规则：所有 workbar 产品级完成事实集中写入本开发日志，未完成事项集中写入 `code/TODO.md`，子仓库不再建立平行状态源；`new-api-source/CLAUDE.md` 继续通过 `@AGENTS.md` 自动继承同级规则。
- 自动验证通过：`bun run typecheck`；`bun test` 共 41 项、0 失败；`go test ./controller ./model -count=1`；`bun run build`；变更文件格式和差异检查。仓库全量版权检查仍有 25 个与本阶段无关的历史文件问题，本次 workbar 变更未新增版权问题。

**涉及仓库与记录**：`../AGENTS.md`、`../CLAUDE.md`、`AGENTS.md`、`CLAUDE.md`、`../new-api-source/AGENTS.md`、`../new-api-source`、`../new-api-source/docs/workbar-deployment.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 01:52 · Codex

### `/compact` 手动压缩命令与斜杠菜单分组

- 在本地斜杠命令路由中新增 `/compact`，复用现有 `compactConversation()` 规划、确认框和 `/api/compact` 流程；命令不会作为用户消息发送给模型，也不会进入前台队列或后台并行任务。确认框仍是执行压缩前的安全门禁，不做静默压缩。
- `/compact` 固定排在内置命令首位；斜杠下拉改为“命令 / Skills”两个独立分组，分组标题不参与上下键选择，原有点击、方向键、Enter 和前缀筛选行为保持不变。新增中英文命令描述、分组标题和压缩前置条件提示。
- 修复原压缩流程对已不存在 `compactBtn` 的引用，改由确认按钮承载执行中状态；确认、取消、关闭和遮罩点击改用可成对解绑的具名监听器，避免反复打开弹窗后监听累积或重复提交。
- 未修改 JSONL、会话 schema、压缩 API、服务端保留公式、摘要结构或归档协议；旧会话无需迁移。用户确认不处理“下拉已打开时切换语言后立即重绘”的瞬时时序，重新输入 `/` 时会按当前语言正常生成菜单。
- 自动验证通过：全部已跟踪 JavaScript 文件语法检查和 `git diff --check` 通过；前端模块、消息队列和 P2 定向回归 202 项及 4 个子测试通过；完整测试为 873 项及 236 个子测试通过。完整测试中的本地 HTTP 400 为既有非法请求用例的预期日志，不是失败。
- 浏览器验收通过：中文和英文重新输入 `/` 均显示独立命令/Skills 分组，`/compact` 为首项且上下键跳转正常；空会话显示中文“当前消息太少，无需压缩”；72 条消息的既有测试会话打开压缩确认框并显示压缩 47 条、保留 12 条，随后取消，未触发压缩或改动会话数据。

**涉及文件**：`app.js`、`src/features/skills-memory.js`、`src/core/i18n.js`、`styles.css`、`tests/test_frontend_modules.py`、`tests/test_message_queue.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 01:13 · Codex

### app.js 第五阶段 5F-2：手动压缩规划与三轮完整上下文

- 扩展 `src/agent/compaction.js`，新增 `buildManualCompactionPlan()`、`createCompactSummaryMessage()` 及服务端保留数兼容纯函数；统一完成导入边界分组、API 消息映射、完整轮次起点、待移除/保留消息、摘要请求尾部、Token 节省估算和摘要消息 schema。`app.js` 只注入 `mapMessageForApi`、消息文本和 detached 判定，并继续负责模型/Key、确认框、`POST /api/compact`、全量归档、状态替换、DOM、保存和错误提示，由 10,152 行降至 10,117 行。
- 按用户确认，默认至少保留最近 3 个非 detached 用户轮次；每轮从真实用户消息开始，包含其后 assistant、`tool-call`、`tool-result` 和最终回复。若最近 6 条 API 可见消息落入更早轮次，保留边界继续向前对齐到该轮用户消息，因此不会从工具调用与结果之间切断；少于 3 个用户轮次或没有可移除前缀时不执行无意义压缩。
- 为继续复用现有服务端公式，摘要请求只包含持久导入边界、真正待移除的 API 前缀和服务端需要保留的 2 至 6 条最新尾部；服务端实际摘要前缀与待移除内容对齐，最近三轮完整原始消息不依赖摘要重建。`/api/compact` 路径、请求字段和服务端实现均未修改，旧前端、旧会话和旧协议无需迁移。
- 直接 Node 行为测试覆盖旧压缩摘要再次压缩、导入边界持久保留、`skipApi` 归档轨迹不进入摘要请求、detached 用户不计作主轮次、三轮完整保留、工具调用/结果成组、服务端摘要数量公式、摘要消息计数/时间/节省量覆盖、短会话拒绝和输入数组不变；源码护栏确认纯模块不拥有 Date、DOM、网络、状态或持久化副作用。
- 自动验证通过：全部 33 个 JavaScript 文件语法检查和 `git diff --check` 通过；压缩最小测试 7 项通过；前端模块、P0/P2、并发、子 Agent 和消息队列定向回归 276 项及 4 个子测试通过；完整测试为 876 项及 236 个子测试通过。完整测试中的本地 HTTP 400 为既有非法请求用例的预期日志，不是失败。
- 本批不实现界面所写的“超过 95% 自动压缩”，也不改变导入边界在最新压缩摘要后的模型可见语义；两者继续作为独立待确认事项。下一批只读完成 5F 所有权护栏与收口，再决定是否进入 5G `agent-loop.js`。

**涉及文件**：`src/agent/compaction.js`、`app.js`、`tests/test_frontend_modules.py`、`tests/test_p2_coverage.py`、`TODO.md`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 00:54 · Codex

### app.js 第五阶段 5F-1：压缩上下文策略纯边界

- 新增 `src/agent/compaction.js`，迁移 `isCompactSummaryMessage()`、`getModelContextMessages()` 和 `getModelContextLimit()`；`app.js` 仅向上下文选择函数注入既有 `isDetachedFromMainContext()` 谓词，并继续把同一纯策略提供给模型请求和用量面板。`app.js` 由 10,176 行降至 10,152 行。
- 直接 Node 行为测试固定无摘要保持顺序、多个摘要只取最新摘要及其后消息、detached 后台/排队投影不进入主模型上下文、输入数组不变，以及 Claude 4.5/4.6/5、GPT 4.1/5.1/5.2、DeepSeek v3/v4、Gemini 和未知模型的原有上下文上限。
- 源码所有权护栏确认纯模块不读取全局 `state`、DOM、localStorage、网络或持久化状态；`index.html` 在 `subagents.js` 后、`model-stream.js` 前加载新模块。未修改 JSONL、API、SSE、工具协议、AgentRun、压缩按钮、消息替换、usage、持久化格式或界面时序，旧会话无需迁移且无需浏览器视觉验收。
- 用户补充 5F-2 的硬性验收条件：压缩后必须保留最新几轮完整上下文，工具调用与结果不得在保留边界处拆开。审计同时确认当前“超过 95% 自动压缩”只有界面文案，没有自动触发实现；导入边界在最新摘要后的模型可见语义也存在待确认项，本批均不借纯拆分静默修改。
- 自动验证通过：全部 33 个 JavaScript 文件语法检查和 `git diff --check` 通过；压缩最小测试 8 项通过；前端模块、P0/P2、并发、子 Agent 和消息队列定向回归 275 项及 4 个子测试通过；完整测试为 875 项及 236 个子测试通过。完整测试中的本地 HTTP 400 为既有非法请求用例的预期日志，不是失败。
- 下一批 5F-2 只拆手动压缩规划和结果数据；模型请求、归档、DOM、消息替换、保存及错误提示继续留在 `app.js`，完成后再做 5F 所有权收口。

**涉及文件**：`src/agent/compaction.js`、`app.js`、`index.html`、`tests/test_frontend_modules.py`、`tests/test_p2_coverage.py`、`tests/test_concurrency.py`、`tests/test_subagent_frontend.py`、`tests/test_message_queue.py`、`tests/test_p0_stability.py`、`TODO.md`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 00:34 · Codex

### app.js 第五阶段 5E-3：后台结果投影纯边界与子 Agent 收口

- 扩展 `src/agent/subagents.js`，新增 `hasBackgroundResult()` 和 `buildBackgroundResultMessage()`，把成功、异常和 reload 三处后台结果严格判重，以及成功/异常两处消息 schema 统一到纯模块；`app.js` 继续提供最终内容、模型、ISO 时间、格式化耗时和 usage，并负责一次性 usage 合并与真实消息写入。`app.js` 由 10,202 行降至 10,176 行。
- 直接 Node 行为测试固定只匹配 assistant `background-subagent`、job ID 严格类型比较、空消息安全回退、成功消息 usage 引用与 `_usageScope`、异常消息不含 usage、AgentRun/父任务排序/模型/时间/耗时字段及两侧输入不变；源码护栏确认纯模块不拥有 AgentRuntime、AbortController、Promise、消息写入、授权、持久化或 dispatcher。
- 收口审计确认 `createBackgroundServerContext()`、`runBackgroundSubAgentJob()`、`pumpBackgroundDispatcher()`、dispatch、restore 和 resume 剩余逻辑均为运行时适配或副作用编排，不再为减少行数继续迁移。5E 正式结束，服务端模型委托与前端用户并行任务仍保持独立链路。
- 按用户确认，拆分完成度以后以职责所有权、可维护性、稳定接口、测试护栏和无循环依赖为准；`app.js` 行数仅作为观察指标，不再作为强制迁移高耦合代码的门槛。
- 未修改 JSONL、API、SSE、工具协议、AgentRun、服务端 Child AgentRun、并发上限、权限、持久化格式或界面；本批没有视觉与交互时序变化，因此未操作浏览器。
- 自动验证通过：全部 31 个 JavaScript 文件语法检查和 `git diff --check` 通过；195 项前端/子 Agent/并发/消息队列定向测试通过；高风险组合为 342 项及 17 个子测试通过；完整测试为 869 项及 236 个子测试通过。完整测试中的本地 HTTP 400 为既有非法请求用例的预期日志，不是失败。
- 下一批只读审计 5F `compaction.js` 的职责边界，先区分 token 预算、消息选择和结果规范化纯计算与模型请求、消息替换、DOM 和持久化副作用。

**涉及文件**：`src/agent/subagents.js`、`app.js`、`tests/test_frontend_modules.py`、`tests/test_subagent_frontend.py`、`TODO.md`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`

## 2026-08-01 00:14 · Codex

### app.js 第五阶段 5E-2b：后台 reload job 默认值纯边界抽离

- 扩展 `src/agent/subagents.js`，新增 `buildRestoredBackgroundJobData()`，把 reload checkpoint 到 runtime job 的字段规范化迁出 `app.js`；复用已有 checkpoint 默认值边界，集中处理 ID、session、文本、模型、工具配置、AgentRun/cursor、时间、强制 `pending` 和 `restored`。`app.js` 由 10,213 行降至 10,202 行。
- `app.js` 继续显式提供用户消息文本、当前模型和 queued/deadline 两个时间回退，并保留原有短路读取时机；Promise/resolver、用户消息补建、既有结果去重、checkpoint 移除、dispatcher 注册、session 保存和后台调度均未迁移。
- 直接 Node 行为测试覆盖完整 checkpoint、旧最小 checkpoint、数字字符串规范化、未知字段保留、`rootPaths` 数组隔离、输入对象不变、默认权限/工具/思考配置、文本与模型回退、时间回退以及强制 pending；源码护栏固定纯模块不读取 `Date.now()`、DOM、state、AgentRuntime 或持久化状态。
- 未修改 JSONL、API、SSE、工具协议、AgentRun、服务端 Child AgentRun、并发上限、权限或界面，旧 checkpoint 无需迁移；本批没有界面与交互时序变化，因此未操作浏览器。
- 自动验证通过：全部 31 个 JavaScript 文件语法检查和 `git diff --check` 通过；193 项前端/子 Agent/并发/消息队列定向测试通过；高风险组合为 340 项及 17 个子测试通过；完整测试为 867 项及 236 个子测试通过。完整测试中的本地 HTTP 400 为既有非法请求用例的预期日志，不是失败。
- 下一批先只读收口 5E 剩余所有权；若无新的独立纯边界，则进入 5F `compaction.js` 审计。原计划之后仍有 5F、5G `agent-loop.js` 和阶段 6 esbuild 三个主要部分，其中后两项风险显著高于当前纯函数拆分。

**涉及文件**：`src/agent/subagents.js`、`app.js`、`tests/test_frontend_modules.py`、`tests/test_subagent_frontend.py`、`TODO.md`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-01.md`
