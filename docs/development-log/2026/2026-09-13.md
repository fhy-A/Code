# 2026-09-13 开发日志

## 2026-09-13 13:48 · Codex

### CODE-103 上下文移入高级设置、单次输出留空自动（本提交，未发布）

- **呈现与兼容**：按用户已确认的 `code-context-settings-R001/R002` 及 R003 文案一致性补充，日常参数只保留表达详略、交流语气、单次输出；“上下文上限 / 留空自动”与温度放入默认折叠的高级设置。单次输出仍就地展开，自动时输入为空、提示留空自动，数字快捷项不再列出 auto，相关错误提示中英文同步改为留空自动；旧 auto 可兼容归一为空框，清空/纯空白仍保存 canonical auto，旧手动数字原样保留。上下文输入只搬迁，原 ID、保存键、格式校验、隐藏时生效及压缩/服务端算法不变；原运行详情/诊断保留，未新增面板。复用既有两列及窄屏单列局部样式。
- **验证**：R001 定向前端 **4 passed / 471 deselected**；合入 R002 后前端相关两模块 **475 passed / 48 subtests passed、0 failed/skip**，两项同源既有转义警告。前者为后者子集，不相加；`npm run check:frontend` 通过。真实模板、全部 CSS、i18n/风格模块、原上下文解析/预算模块和实际保存/恢复函数在隔离 Chromium 的中英文 × 明暗主题 × 桌面/窄屏 **8 组**通过：默认隐藏、展开/键盘、40px 对齐及无溢出；400000 旧上下文隐藏仍生效、128K/空白/400000 保存恢复、非法值回退；单次输出 16384 保留、清空/纯空格/旧 auto 保存为 auto 且恢复空框。最终截图核对收起、自动空框及高级展开状态；非法输入演练后经真实有效输入事件恢复正常状态再截图。未重跑预算业务全量。
- **证据与清理**：本机 `%TEMP%/code103-evidence.json` 绑定本提交文件、命令、报告和截图哈希；报告 `code103-frontend-targeted.log`、`code103-frontend-regression.log`、`code103-build.log`、`code103-r002-visual-v2.log` 及 `code103-r002-visual/` 截图保留，未纳入公共仓库。最终浏览器 context/browser 已关闭，BrowserServer 关闭确认曾超时，定向核验该夹具自建 PID 35136/35488 已不存在，见 `code103-visual-cleanup.json`；没有操作用户浏览器/服务或未知进程。前期同阶段浏览器八组也通过，结果有重叠不累计。
- **收口与边界**：依已确认 STANDARD 阶段授权一次归档、本地提交后交回只读核对，不冒称另有独立技术复跑。仅提交本阶段七文件及日志/索引；仓库外只分配后移除已完成 CODE-103，游标保持 CODE-103，不重开 CODE-102 或其他事项，不动公共 TODO、无关 handoff 和受保护数据。未重启/刷新真实3011、未访问用户Run/发模型请求，隔离验证不等于真实实例验收；未 push/tag/release，提交后释放本人租约并停止。

## 2026-09-13 13:29 · Codex

### CODE-102 单次输出预算与截断停止第一阶段、开发标题及参数布局经指定独立复核通过（本提交，未发布）

- **预算与兼容**：真实设置页默认只显示“单次输出：自动/自定义”，高级输入保留旧手动正整数、快捷值和实际保存路径；回答详略、语气独立。新自动请求由服务端依据已准入模型/连接证据采用推理 65536、已确认非推理或关闭推理 32768、未知 16384 的候选，再受独立模型/连接输出上限约束；不从 contextWindowHard 推导输出能力，不暗改手动值。上下文不足、不兼容推理及固定 thinking 余量不足明确拒绝。可选 v1 预算/诊断字段校验版本、类型与实际请求，恢复不重算冻结预算；旧 Run 保留具体请求与旧压缩合同。新前端自动请求须确认服务端协议，配套前后端须正常加载。详见[输出预算与截断合同](../../output-budget.md)。
- **截断停止与历史边界**：完整终止流的 length/max_tokens 独立区分仅推理耗尽、正文截断和工具截断；截断整批工具零执行，即使部分 JSON 合法。取消、原生结构/DONE/finish/thinking 签名校验保持；不新增截断重试、换 key/模型或推理降级。压缩截断注明该内部请求实际预算并停止业务请求。仅记录诊断计数，不归档不完整 native 回放；前端已展示截断内容标记 skipApi，此前成功工具结果、Goal/Skill/权限/幂等边界保留。
- **标题的真正原因与追加修正**：前一提交 `41c2c59` 仅改主 HTML 初始标题，用户重启后仍看到 Code。基线进一步复现真实 `applyI18n` 在中英文均无条件把 Code Dev 重写为 Code，左上产品名又依赖较晚 heartbeat；本阶段按 `code-dev-title-R002` 让服务端主/bundle/classic 入口输出既有 instanceMode 和初始标题/产品名，前端立即应用同一身份，i18n/欢迎页重绘不再改标题，原通知恢复逻辑保持。此前首轮证据没有覆盖实际 i18n 重绘链，不能外推为完整标题修复；保留上条日志，不改写历史。
- **两次截图反馈与最终布局**：R003 针对三列裸摘要及远离字段的高级标题，改为模型参数局部桌面两列：首行表达详略/交流语气，次行单次输出/最大上下文窗口；单次输出采用 40px 原生 summary 入口并就地展开。R004 核对真实类名 model-parameters-section（复数），修正此前 CSS/视觉夹具误用单数导致未命中或目标隐藏的错误；全零 boundingBox 不作为控件高度证据。最终局部 grid align-items:start，展开不拉伸其他字段；700px 及以下单列，高级采样保留在下方，手动 16384 等值及校验语义不变。
- **验证分层（本次归档复用，不重跑产品测试）**：追加 R003/R004 前的相关回归 **1115 passed / 205 subtests passed，0 failed/skip**，3 条同源既有转义 SyntaxWarning。追加局部布局后仅补 UI 回归 **475 passed / 48 subtests passed，0 failed/skip**，2 条同源警告；两组有重叠，不相加。前端 build/freshness/语法通过。实际模板、全部 CSS、真实 i18n/回答风格模块和输出事件/保存函数在隔离 Chromium 的中英文 × 明暗主题 × 1100px/375px **8 组**关闭/展开检查通过：40px 等高、桌面其他控件位置不变、无横向溢出、Enter/Space/Tab、16384 保留、54321/auto 保存恢复及非法 1.5 拒绝；16 张最终截图保留。标题验证另含真实身份/i18n/通知函数及 dev/release 根/bundle/classic HTML handler。
- **早期失败与修正保留**：旧解析器的两项截断反例先失败；早期大回归 **988 passed、3 failed、200 subtests passed**，对应旧 length 允许执行/纠正与旧文案预期，修正后定向通过；非 length 参数错误原验证逻辑及完整 UTF-8 工具执行控制仍保留。下一轮 **1113 passed、2 failed、205 subtests passed**，两个抽取式前端夹具漏载新增检查函数，加载真实函数后定向通过，再得到上述 1115 结果。R003 UI 首轮 **474 passed、1 failed、48 subtests passed**，旧摘要位置断言按获批就地展开结构更新。早期视觉目标因单复数错配被隐藏，随后真实 CSS 和夹具同步修正；截图中的虚拟风格选项改为真实风格模块后重验。阶段 doctor 和早期 BrowserServer 关闭曾超时；早期强制清理仅针对夹具自建 PID，后续确认这些 PID 不存在，最终八组 context/browser/server 均正常关闭。
- **冻结及指定复核证据**：本机 `%TEMP%/code102-review-manifest.json` 的 SHA-256 为 `5c718c5e125b34baf46a022bebb9deb06fe1757378acb673d90c613e40fe35ee`，绑定基线 `41c2c591b12a64685b5c1c7389d567371a410ea2`、18 个候选文件、18 份报告、17 个视觉产物和 3 个构建产物及命令。最终原始报告为 `code102-final-regression-v2.log`、`code102-r004-ui.log`、`code102-r004-build.log`、`code102-r004-visual.log`，失败报告也在清单；这些 TEMP 证据未纳入公共仓库。审批任务 `01a094f0-5494-7fd1-b9f3-f5fdf363fa04` 通过 R005 明确完成指定独立复核：文件/报告/产物哈希匹配，已读入场/恢复、能力隔离、原生截断/失败、前端保存/历史与旧夹具差量，已查看最终中文深色桌面展开和英文浅色窄屏展开截图，未发现阻断。审批复核与开发侧测试执行证据分开记录，不声称审批侧复跑全部测试。
- **覆盖边界与本次收口**：未进行自然模型成本/效果对比，候选数值不代表最优；未发付费模型请求，未读取/操作用户真实会话、测试项目或 3011 页面/Run，未重启/刷新真实服务，隔离浏览器不等同真实实例初始化或自然任务验收。测试临时环境变量已恢复，最终隔离浏览器已清理。R005 收口只核冻结哈希、归档准确性和 Git/lease，产品代码、测试及既有合同文档不再修改；本提交为18个冻结候选文件加本日日志/索引。仓库外仅删除私有 TODO 的 CODE-102 已完成第一阶段整项，游标保持 CODE-102；公共 TODO、CODE-080/CODE-055、其他事项、WB-003 handoff 及三项受保护未跟踪数据保持。本人持 canonical lease 本地提交后释放并停止，未 push/tag/release，未启动或捏造第二阶段计划；不为纯记录复核结论再生成日志或审批。

## 2026-09-13 11:37 · Codex

### 开发实例初始浏览器标签显示 Code Dev（本提交，未发布）

- **原因与修复**：主页面静态标题原为 `Code`，前端直到 heartbeat 返回 `instanceMode` 才调用 `applyInstanceIdentity`。本次仅在 `server.py` 提供主 `index.html` 时依据既有 `INSTANCE_MODE == "dev"` 将初始标题输出为 `Code Dev`；普通实例仍为 `Code`，不按端口猜模式。前端通知及标题恢复逻辑、HTML 源文件、配置和 Goal/AgentRun 协议均不改。
- **验证**：doctor 通过；从当前源码 AST 提取实际 `CodeHandler.do_GET`，使用内存响应替身验证 dev/release × `/`、`/index.html`、带查询参数的主页面和 CSS 共 **8 个场景**，初始标题、Content-Length、no-store 及非页面资源保持均通过。Node 执行现有通知/身份函数，确认两种实例在 heartbeat 前和权限通知期间/之后均按原逻辑恢复基准标题。`npm run verify:frontend`（构建 freshness + bundle 语法）和 `git diff --check` 通过；Python AST 仅提示原有文档转义 SyntaxWarning。未增加测试系统、未跑全量。
- **观察与生效边界**：没有进行真实浏览器视觉观察；没有刷新用户页面、访问或操作当前 3011 的用户 Run，也没有重启真实服务。已启动 Python 服务须在用户下次正常重启加载该服务端代码后，随页面自然重新加载取得新初始标题；本轮不替用户执行这些动作。
- **收口**：用户直接授权的 QUICK 小改，接力 `code-dev-title-R001`；本提交仅含服务端三行修复、本日日志及索引。私有 TODO、CODE-080 已完成实现及其他事项、三项受保护未跟踪数据保持。本人持 canonical lease 完成本地提交后释放并停止，未 push/tag/release。

## 2026-09-13 02:52 · Codex

### CODE-080 同 Session Goal 收尾闭环：实现与隔离验证经指定独立复核通过（本提交，未发布）

- **范围与结果**：针对同一 Code Session 多轮相关任务已结束而 Goal 仍活跃的问题，服务端每次请求重建有界 Goal 投影，提供稳定 ID、条件、证据摘要和同 Session 的 `goal_read`；相关/状态/无关关系可审计，最终回答漏收尾时进入一次有界补查。完成仍依原条件和成功工具回执；可选改进、未授权发布不作为硬验收条件。实现与兼容细节见[同 Session Goal 收尾合同](../../goal-closeout.md)。
- **收尾、取消与等待**：补查只开放受限 Goal 控制工具，允许显式取消及按原授权解除等待，禁止业务副作用或改计划；原补查/续跑预算保持有界。旧 gate 不能代替本轮等待声明，当前等待确认绑定输入、revision 和调用 ID。新 steer 在恢复旧 final 前消费，不被旧候选吞掉；恢复、重复 steer/回执不重置窗口或重复写事件。Skill 完成检查及 CODE-074 不可变 Skill 跨 Run 限制保持。
- **负向证据**：R001 的隔离 worker 复现直接宣布完成或只说缺条件却不记 gate 时仍接受 completed；实际 JS 格式化函数把十条条件输出为八条且缺少稳定 ID/截断说明。R002 对冻结 R001 产品源码另以真实 worker 复现两处阻断：补查耗尽后取消 steer 留下 active Goal；旧 waiting_user + 新相关验收输入漏动作时仅一次模型请求便接受 final。修正用例覆盖取消到达补查/旧 final 的时机、状态/无关/当前等待/解除完成、Skill 链、worker 重启及取消事件/等待读取回执提交后的崩溃恢复。
- **开发侧验证分层（复用已有证据，本次未重跑产品测试）**：R001 扩展回归 **1100 passed / 494 subtests passed**，3 条既有 SyntaxWarning。R002 相邻首轮 **303 passed / 162 subtests passed，另有 1 个旧夹具失败**；第 50 轮边界夹具补为真实 `goal_read(decision=wait)` 后，保留越过旧固定轮次边界、原 gate 和无 successor 的核心断言，相关两模块复验 **92 passed**，1 条既有 SyntaxWarning。上述集合有重叠，**不得相加为唯一总数，也不表示 R002 首轮全量无失败**。前端构建 freshness/语法、diff hygiene 已通过；环境未变，归档阶段不重复 doctor 或产品测试。
- **原始证据与候选绑定**：本地 `%TEMP%/code080-review-manifest.json` 与 `code080-final-regression.log` 保存 R001 清单/报告；`%TEMP%/code080-r002-review-manifest.json` 保存 R002 七文件 SHA-256、验证命令及三份报告 SHA-256，报告为 `code080-r002-negative.log`、`code080-r002-regression.log`、`code080-r002-final-targeted.log`。这些是本机临时证据文件，未纳入公共仓库；当前提交绑定的源码和[隔离用例](../../../tests/test_goal_closeout.py)可重新检查。
- **指定复核结论**：审批任务 `01a094f0-5494-7fd1-b9f3-f5fdf363fa04` 在 `code080-goal-closeout-R003` 明确通过 R002 独立复核，已核对七文件/三份报告哈希、真实 worker 负控与修正用例、旧边界夹具核心断言、恢复/取消/等待权限和预算；审批侧未复跑整套。执行者 `01a094ef-dcb7-7742-9375-7a343138e50e` 按获批 R003 只核对归档事实、链接、冻结哈希及暂存范围。独立复核结论与开发侧测试执行证据分开记录。
- **兼容与覆盖限制**：旧 Run 缺少新控制字段时保留旧收尾默认；新增可选 Run 元数据按合同恢复，未知/损坏状态拒绝恢复。旧 Goal 事件、生命周期/reducer/CAS/幂等未迁移；没有批量结束历史 Goal。**自然模型任务未实测，真实服务未重启**；证据真实性仍按原合同，模型声明与工具来源不等同于独立验真或确定性自然语言意图分类。
- **本次收口**：本提交仅包含已核验且字节冻结的七个候选文件，以及本日日志和索引。仓库外仅移除私有 TODO 的 CODE-080 整项，CODE-101 编号游标、CODE-074、其他事项及采购 handoff 保持原状。三项既存受保护未跟踪数据保持；未 push/tag/release，未启动下一事项。按本人 canonical lease 完成本地提交后释放并停止，不为纯日志收口递归生成新审批或日志。
