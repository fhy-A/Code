# 2026-09-15 开发日志

## 2026-09-15 23:50 · Codex

### CODE-072 第一阶段：项目会话批量归档与项目筛选（本提交，未发布）

- **已实现范围**：项目菜单“全部归档”使用服务端固定 ID/计数预览；活动项明确确认停止后逐项归档。稳定操作 ID、逐项结果及失败项新预览保持已完成项不重放；区分冲突、停止失败与已停止但归档失败。归档页按项目 ID 与搜索组合筛选，保留单项恢复/永久删除，不实现批量恢复或第二阶段批量永久删除。
- **状态与兼容合同**：独立生命周期代次绑定项目位置、归档/恢复和新增工作，冻结有限 Run 身份；原 Run 普通输出、revision/mtime、模型轮次和首次正常检查点不使确认反复失效。复用既有 paused/Goal 判定、停止 fence 与生命周期/AgentRun/JSON 锁，最终移动继续使用 v1 bundle/manifest；仅批次短期 v2 journal 绑定操作和 archive token，完成凭据在删除 journal 前写入并独立于归档 bundle 保留。损坏或缺失凭据拒绝操作，404 不冒充成功。恢复、兼容、回退及证据等级见 [阶段协议](../../project-session-archive.md)。
- **界面独立复核修正**：预览显示项目名称与 ID，同名筛选项可区分；权威列表加载后同步选项，选中项目最后一项消失时先复位筛选再绘制。只在本批报告当前会话成功、服务端明确返回仍已归档且导航代次未变时切走，不以分页缺失判断归档；无关/已恢复会话保持。确认响应丢失后，原弹窗可直接读取同一批次结果；逐项展示可理解原因。
- **后端和规定回归**：最终隔离 `project_archive_cases.py` + 既有归档生命周期集合 **82 passed / 30 subtests**（22 新增批次用例 + 60 既有用例，68.96s）；早期 11/15/20/21 项为迭代子集，不叠加。规定 `test_frontend_modules.py`、`test_p2_coverage.py`、`test_projects.py` 使用临时数据根，**601 passed / 81 subtests**（65.00s），与其他集合分别记录，不生成合并通过总数。doctor 本阶段一次通过；JS 语法、构建/bundle/classic freshness、diff 检查与无 CODE_DATA_DIR 的安全收集通过。R004/R005 没有重复未变前端、H4 或 doctor，收口只复核冻结字节及暂存边界。
- **真实进程补证与模拟边界**：原 `.clear()` restart 场景只代表进程内模拟。R004 新增两文件测试，真正 A `os._exit(73)` → 等待退出 → 全新 B，**3 passed，8.25s**：成功项 SHA/size/mtime 不变且只继续 pending；stopped 续归档不重复 stop/cancel、不改写 cancelled Run；bundle 已提交但无凭据时恢复凭据，随后单项恢复的 Session 不被旧批次两次 resume 重归档。这 3 场景不与早期同场景模拟叠加。停止夹具是实际 durable 准入/取消、`start_worker=False`，不声称真实模型或执行线程停止覆盖；R002 另有真实 Agent worker 连接本地 SSE 模拟上游，连续输出 3 秒再确认成功，及同期新增 Run/项目迁移往返冲突证据，不属于付费/自然模型验收。
- **浏览器及环境边界**：最终 bundle/中文/1280 与 classic/英文/390 两条 H4 路径通过，包含冷筛选、已删除/无项目区分、选项消失复位、无关/已恢复导航保护、丢响应原批读取、单项恢复和重载历史；自建浏览器/子进程/端口/临时根全部清理。初期夹具曾修正 Fetch `ok` 属性、Playwright URL 字符串及跨场景选项计数后才最终通过，失败轮次不计通过。R004 六个进程退出、三个根移除，审计禁止 socket bind/connect，未开监听端口。保留既有 `server.py:13457` invalid escape SyntaxWarning；未操作真实会话/模型、3010/3011、用户 Browser、打包或线上配置，不宣称外部文件系统写者的强隔离。
- **独立复核与一次收口**：审批任务 `01a094f0-5494-7fd1-b9f3-f5fdf363fa04` 在 R005 对固定 10 文件候选 `f57cf3ece913e20be05ac1593d4eb86600f61e4fc976d08d6e1d455f1502ad00` 及 R004 两项新测试的实际代码/指纹、锁/凭据路径、UI 和原始结果独立核验通过，授权一次本地阶段提交。收口除 R006 明确批准测试包装文件末尾空行修正外保留产品/测试字节；该文件 930→928 字节，新 SHA-256 为 `9e1a500da151a29cedd2f49578fcebe562b366d4349162215546c55d23040add`。新增文件在未跟踪状态不进入普通 `git diff --check`，首次 `git diff --cached --check` 才发现这个 EOF 问题，经限定批准修正后暂存检查通过，不复跑测试。协议验证段仅补充 R004 入口，加入本日日志与精确本任务索引行。私有 CODE-072 仍进行中，下一动作是已批准第二阶段等待审批另行派发，游标 CODE-107/WB-014 不变。09-14 WB-014 索引行/未跟踪日志、既有 data 差量、真实 memory lock/receipts 元数据及无关采购 handoff 保留；不 push/tag/release，提交后释放租约并停止，不自行开始第二阶段，也不为收口复核再追加日志。
- **证据位置**：本机 `%TEMP%/code072-stage1-candidate-mtiywctl/` 保存 10 文件候选与回执，`code072-final-backend.txt` 为最终 82 项原始输出；601 项原始输出在开发任务 `01a094ef-dcb7-7742-9375-7a343138e50e` 的 exec session 98899 最终结果。`code072-ui-GdVsNh/result.json` 保存最终 H4，`code072-r004-evidence-wsxjq4vf/` 保存新增指纹、差量、真实进程完整输出与 collection 审计；TEMP 工件不进入仓库，日志汇总不等于复跑。

## 2026-09-15 22:20 · Codex

### CODE-107 默认记忆用户视角补正（本提交，未发布）

- **用户纠正**：按 `architecture-memory-refresh-R002` 提供的最终全文，改为面向最终用户任务的“Code 使用环境参考”，替代上一版偏源码开发者的导航。明确默认记忆不是用户项目架构或实时能力清单，不预设路径、端口、Shell、依赖或执行设备，不引导查找安装目录/内部数据；已有 Goal、记忆和单次输出均按当前可核实状态理解。
- **定点核验**：通过已确认的 3011 受管接口绑定最新 revision 与 legacy scope 更新；写后复读逐字匹配给定 description/正文，索引一致、文件名与原有其他元数据不变，原 receipts/lock 保留。未再次运行 doctor、模型或产品回归，不声称 token 节省或模型效果已验证。
- **同项收口**：仍为 CODE-107 内容补正，不新分配 ID，私有 TODO 及 CODE-107/WB-014 游标未改。仅提交记忆正文、派生索引、本日日志与本任务索引行；09-14 WB-014 既有索引行/未跟踪日志、保护运行数据及无关 handoff 保留。未处理打包运行数据问题，未改功能、打包/更新策略或用户 `.code` 副本。写后版本与最终全文见本机 `%TEMP%/architecture-memory-r002-receipt.json`；完成本地提交后停止，未 push/tag/release。

## 2026-09-15 21:53 · Codex

### CODE-107 默认架构记忆精简更新（本提交，未发布）

- **内容与事实核对**：按用户明确授权 `architecture-memory-refresh-R001`，将 `data/memory/code-architecture.md` 的英文长说明改为约 **674 个中文字**的中文架构导航。开头明确描述的是 Code 宿主而非用户当前项目；保留 Python HTTP / 原生 JS / 模型网关分层，说明 `server.py`、`code_runtime/`、`app.js` / `src/` 与 `agent-runtime.js` 的职责，区分单轮模型、AgentRun 和 Goal 生命周期。补充按请求读取受管记忆、包括 bypass 在内的删除确认，以及单次输出预算不等于整个任务预算。删除固定工具全清单、并发数字、旧迁移细节和过强安全保证，改用 README、记忆/输出预算/Goal 收尾验收文档与开发日志入口。承重事实以当前实现和这些文档逐项核对，未修改产品代码。
- **真实受管写入**：先读取已运行 Code Dev 的 `127.0.0.1:3011/api/memory?file=code-architecture`，其 revision 与仓库同名文件的实际文件身份、mtime、大小和字节摘要一致，确认没有访问用户 `.code` 副本。写前再次检查目标未变化，再用既有管理接口提交原 revision、`scope: legacy` 与唯一 operationId；只更新 description/body，不增加 project。响应及写后读取均保持 legacy，revision 更新；正文与同根派生 `MEMORY.md` 完全对应，无遗留事务日志，原有 receipt 文件元数据和 lock 字节保持不变。
- **定点结果**：目标文件 **6892 → 2854 字节**，减少 **4038 字节（58.59%）**；名称不变、仅保留 description 元数据、scope/正文/索引/字节核验通过。文内六个文档入口存在。此次为已授权 QUICK 内容维护，不跑模型、全量产品测试或真实自然召回验收，不重启服务、不操作用户浏览器、不同步其他数据目录或修改其他记忆/协议；没有新增缓存或后台任务。
- **索引来源与收口**：任务开始前 `MEMORY.md` 相对 Git 的 `test-memory → code-architecture` 差量来自用户刚完成的真实验收；本次在更新描述后保留该正确派生索引，不恢复不存在的 test-memory。提交只包含记忆正文、派生索引、本日日志及本任务公共索引行共四文件。09-14 WB-014 原公共索引行与未跟踪日志、三处保护 data、无关采购 handoff 及现有 receipts/lock 保留，后两者不暂存。私有清单按契约分配 CODE-107，完成后移除该条目，游标保持 CODE-107/WB-014，其他事项不动。不 push/tag/release，不启动下一项。
- **本机证据**：`%TEMP%/architecture-memory-before.json` 保存原目标版本，`architecture-memory-draft.json` 保存最终文案，`architecture-memory-update-receipt.json` 保存受管写后全文、revision、scope、索引与字节指标；`architecture-memory-preservation.json` 保存原共享差量与 receipt 元数据。TEMP 证据和运行时 receipt/lock 不进入本提交。

## 2026-09-15 20:39 · Codex

### CODE-084 持久记忆事务、删除授权及请求上下文一致性（本提交，未发布）

- **实现与兼容**：统一 Markdown 正文、同根派生 `MEMORY.md` 索引、事务恢复日志和操作回执；保存及语义幂等返回均修复索引，设置页重命名使用单次事务，普通受管文件写入、删除及编辑提案接入同一协调器。保留旧 frontmatter、BOM、未知元数据及既有项目可见性；受管编辑提案保留已解析的绝对目标，避免项目外但原有允许范围内的记忆被错误重解释为相对路径。索引及内部文件受保护，链接、目录联接、并发版本冲突和回执目标重建均有拒绝或恢复证据。
- **删除授权**：`delete_memory` 使用确切名称与 scope，由服务端冻结文件版本并绑定实际授权 ID；自动模式也须确认，模型自填 `confirmed` 不构成授权，后台/子运行不提供专用删除工具。普通 `delete_file` 命中记忆也走同一确认与版本检查；旧回执遇到后来修改或重建的同名目标不会再次应用或伪称当前目标已删除。可见记忆上下文附带最小 name/scope 元数据，模型可据此发起首次预览而不用猜测作用域。
- **上下文与 B 方案**：新前端不再冻结记忆正文，服务端按 Run 固定 cwd 在每次模型及压缩请求组装时读取当前正文。界面缓存只作展示，当前前台 Run/Session/项目的受管工具完成后先同步清空旧正文和数量，再事件驱动读取一次；请求、导航、项目及旧会话/后台隔离防止旧结果覆盖。旧合同存在不可分离的精确内联记忆标记时，在下次出站前暂停且不可恢复；需确认服务更新并刷新加载匹配的新前端，再在同一 Session 开启新轮次。前后端必须同步生效，旧 Run、历史、已发送请求及已有摘要不改写，已完成工具不重放。
- **R003 已通过证据（本次收口复用）**：记忆专项与完整前端/P2 集合为 **555 passed / 1 skipped / 52 subtests**；相关运行时为 **30 passed / 131 deselected / 11 subtests**。R003 后以真实前台 Run 和独立 clientRequestId 强化的记忆专项为 **32 passed / 1 skipped**，是相关子集，不能相加。单次 doctor、JS 语法、构建与 freshness 已通过。本次纯日志收口不重新运行产品检查。
- **R004 差量证据**：常规入口不导入服务，完整记忆用例在临时 `CODE_DATA_DIR` 子进程执行；无该环境变量的新模块与完整 `pytest tests --collect-only` 在受控进程中通过，审计守卫禁止访问真实产品 data。外层 **3 passed**、内层 **34 passed / 1 skipped**；受影响前端 **68 passed / 400 deselected / 48 subtests**，最终后端差量 **9 passed / 26 deselected**。这些集合重叠且执行层次不同，均不相加。最终 bundle/1280 与 classic/390 两条 UI 链通过，覆盖真实设置 CRUD、单 POST 重命名、取消、工具实际返回到完成回调后的缓存失效/读取、乱序、语言刷新、旧版提示及窄屏确认按钮；自建 child、端口和临时根均清理成功。早期夹具误收集已中止而不计通过；新增工具事件曾被原固定白名单拒绝，最终仅准入两条精确合成保存载荷。最终语法、diff hygiene、构建 freshness 通过。
- **验证与运行边界**：Windows 符号链接创建权限导致的既有 1 项 skip 保留，硬链接与目录联接有实际通过证据，不将它们冒称符号链接实测。既有 `invalid escape` SyntaxWarning 未修改。未读取真实产品记忆/配置/会话、未重启真实实例或操作用户 Browser，也未进行真实模型自然验收；任意命令及外部文件系统写者仍在协调器外，不宣称文件系统强隔离。`consolidate-memory` 旧直接写索引步骤本轮未适配、未运行/自然验收，程序索引维护与 Skill 内容整理职责见 [协议说明](../../memory-consistency.md)，不宣称该 Skill 端到端通过。
- **独立复核与收口**：R005 已对基线 `969a0e2d66f9cd4cf28983bafc41a01dd4208c3b` 上的 17 文件候选 `e5afe9231203346071e5173abe5613ff2c00e648e5a0814f47aaa6d1531808dd` 完成 STRICT 独立复核并批准一次本地提交。收口仅增加本日日志与本任务索引行，共 19 个仓库文件；09-14 WB-014 原索引行、未跟踪日志、三处受保护 data 与无关采购 handoff 保留。私有 CODE-084 完成移除，游标保持 CODE-106/WB-014，其他事项不动。此前 `%TEMP%/code_mem_gum1ptp2` 递归清理被自动审批以 `blocked by policy` 拒绝，目录保留，未重试或绕过；不将该旧残留计为已清理。未 push/tag/release，不开始下一事项。
- **证据定位**：本机 `%TEMP%/code084-candidate-receipt.json` 保存 R003，`code084-r004-candidate-receipt.json` 保存 R004 精确候选；相关 `code084-frontend-final.log`、`code084-runtime-final.log`、`code084-r004-python.log`、`code084-r004-regression.log`、`code084-r004-delta.log` 与 `code084-memory-ui-lHwrrE/result.json` 为对应原始证据。上述 TEMP 工件不进入仓库，汇总不代表新的复跑。

## 2026-09-15 18:36 · Codex

### CODE-106 新建会话及欢迎页用量残留修复（本提交，未发布）

- **原因与修复**：普通/项目新建入口 `beginNewConversation` 和 `clearCurrentSession` 已清空会话 ID、消息及累计统计，却残留上一会话的 `state.lastUsage`，无 ID 的读取又回退到该旧值，导致上下文比例/圆环沿用旧会话。两个入口在缓存旧会话后清空当前最近用量；`getSessionLastUsage` 对无会话返回 null。产品改动为两个赋值与一个读取兜底，旧会话统计/最近用量缓存不删除，后台回报仍按旧 ID 归属。
- **验证**：实际 state accessors、session navigation、panels 模块与 app 的缓存/clear 函数在 Node 隔离夹具联动，最小 DOM 替身检查数值、比例、圆环属性、警示类及缓存率未知显示。基线四项负控均复现旧 900 token 残留，修正后四项通过；空会话保留系统提示估算 13 token，不强制归零。覆盖普通/项目新建、clear、随后创建新会话对象、返回旧会话恢复、旧会话延迟 usage/stats 到达时不污染欢迎页。最终定向前端集合 **17 passed / 454 deselected / 0 failed**；早期四项及13项报告均为最终集合子集，不相加。未运行模型、用户项目或完整浏览器 UI 验收。
- **环境与构建**：本阶段 doctor 单次通过，健康探针的 Chromium context 关闭且临时根移除；`node --check` 检查三个改动 JS 文件通过，`npm run check:frontend` 的构建、bundle语法及freshness通过，diff检查通过。原计费累计、缓存率未知口径、空会话上下文估算、样式布局、后端与持久协议均不改。
- **证据与收口**：按已授权 QUICK 接力 `welcome-usage-reset-R001` 完成实现、定点验证和一次本地提交，未另设方案门禁。本机 `%TEMP%/code106-baseline-negative.log`、`code106-targeted.log`、`code106-related-frontend.log`、`code106-final-frontend.log`、`code106-doctor.log`、`code106-build.log` 与收口回执保存真实证据，TEMP 文件不进入仓库。提交仅三个产品文件、新回归文件、本日日志、索引中的本任务新增行，共六文件；09-14 WB-014 原索引行保持未暂存，未跟踪09-14日志及三处保护data原样保留。私有 CODE-106 完成移除、游标保留106，WB游标/其他事项及采购handoff不动。本轮没有重启3011、操作用户浏览器、push/tag/release；提交后释放本人租约并停止，不启动下一事项。
