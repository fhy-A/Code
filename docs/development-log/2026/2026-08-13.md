# 2026-08-13

## 2026-08-13 19:12 · Codex

### CODE-024 对话终态后静默刷新文件树完成

- 左侧文件树现已绑定真实用户轮次生命周期：前台主轮在成功、失败或取消终态后调度静默刷新；queue 消息只有真正执行并进入主轮终态时触发，steer 合并请求随所属主轮统一收口；detached `/parallel` 后台任务在自身终态触发。等待授权、等待问卷和仅入队等中间态均不会提前刷新，同一轮也不会因外层收口重复请求。
- 静默路径复用既有 `/api/files`，但与导航和手动刷新语义分离。刷新只作用于发起时仍可见的项目根与当前目录，不切换项目、目录、会话或预览；搜索输入、预览选中和文件树 `scrollTop` 保持不变，现有文件项展示结构继续沿用。
- 控制器以 `32ms` 短窗口合并相邻终态，并提供同轮去重、generation/latest-wins 和响应上下文复核；过期响应不得覆盖较新文件树。请求失败安静保留最后一次正常树，不显示 loading/toast、不清空旧内容，也不打断对话。
- 本阶段仅修改前端共享链与直接契约测试，没有修改服务端/API、AgentRun、Session schema、JSONL、持久化、安全或授权边界。默认 bundle 与 direct classic 继续共享同一行为。
- 同一人工验收现场的自动证据为：静默刷新控制器与终态接线定向 `3 passed`；`python verify.py ui` 为 `217 passed`；`npm run check:frontend`、`node --check app.js`、`node --check src/features/files.js`、bundle 语法与 `git diff --check` 均通过。临时 pytest 隔离目录已在验证后删除。
- 用户分别在 Code Dev 与 direct classic 页面完成新建、修改、删除及状态保持复核，并确认双入口均为 `PASS`。据此将 `CODE-024` 从唯一未完成事项清单移除；其他 Code/workbar 待办均未改写、选择或启动。
- 回退只需撤销本阶段独立提交；本阶段未执行 push、tag、release、线上配置或数据变更。

## 2026-08-13 17:33 · Codex

### CODE-004 用户长文本省略与收起完成

- 输入框继续以现有约 5 行高度作为紧凑草稿视口，只有内容真实溢出时才显示“展开输入”；展开后的最大高度取 `45vh` 与 `420px` 中较小值，更长内容继续在输入框内滚动。“收起输入”只改变展示高度，不改变草稿全文、光标、选区、焦点或后续发送内容。
- 已发送的用户文字在真实溢出时默认收起，桌面约 8 行、移动尺寸约 6 行，并以轻量渐隐及“展开全文 / 收起”控制切换完整 Markdown；短文本、纯图片消息和附件展示保持不变，图片与附件始终完整显示。所有控制均有中英文文案、键盘焦点和 `aria-expanded / aria-controls` 语义。
- 展示状态与原文严格分离：发送、steer、queue、`/parallel`、Session JSONL、模型上下文、复制和后续编辑始终使用完整原文，没有新增持久化字段。当前页面内已展开的消息在普通流式、工具或队列重绘时保持；刷新或切换会话后重新默认收起。展开/收起通过既有布局变化入口通知滚动控制器，跟随底部时继续跟随，用户阅读历史时不抢回滚动所有权。
- 默认 bundle 与 direct classic 继续复用同一前端共享链；本阶段没有修改服务端/API、AgentRun、Session schema、持久化、安全或授权边界。自动门禁在同一六文件人工验收现场通过：长文本控制器定向 `3 passed`；`python verify.py ui` 为 `214 passed`；i18n/P2 定向为 `56 passed, 4 subtests passed`；`npm run check:frontend`、`node --check app.js`、`node --check src/ui/messages.js`、`node --check src/core/i18n.js` 与 `git diff --check` 均通过。
- 用户按桌面/移动、明暗主题、长草稿与长用户消息、展开/收起、复制/发送全文、刷新/会话切换、附件/Markdown 及滚动矩阵完成实机复核，并明确反馈 `PASS`。据此将 `CODE-004` 从唯一未完成事项清单移除；相邻 `CODE-003`“新消息阅读锚点与流式滚动”及其他待办均未改写、选择或启动。
- 回退只需撤销本阶段独立提交；本阶段未执行 push、tag、release、线上配置或数据变更。
