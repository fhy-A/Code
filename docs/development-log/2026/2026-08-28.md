# 2026-08-28 开发日志

## 2026-08-28 10:57 · Codex

### CODE-006 · 重启后生图目录一次性重绑定修复

- 修复新 AgentRun 创建前的独立生图目录代际差异：页面仍持有稳定 `routeRef` 的旧 revision，而服务重启后的运行时目录为空、凭据尚未重绑或 revision 已推进时，仅对 `image_route_catalog_unavailable`、`image_route_stale`、`image_route_credentials_unavailable` 执行至多一次受控 `refresh/rebind`。重绑必须按原 `routeRef + connectionId + modelId` 精确找到同一路由，只更新公开 revision，并使用原用户消息、原 `clientRequestId` 和同一 optimistic dispatch 再创建一次；路由消失、禁用、仍无凭据、刷新失败或第二次创建失败均继续 fail-closed。
- 一次性恢复只位于尚无 `agentRunId` 的创建分支；已创建、等待授权、恢复中或已派发的 AgentRun 继续冻结原 image route，不换连接、不重放模型、授权或生图请求。图片服务 socket/HTTP 超时的公开表述改为“联系图片服务超时，送达与结果均未知”，不再声称已提交或上游已收到，同时保留 `outcomeUnknown + notReplayed`、同 Run 付费熔断和零自动重试。

### 验证

- fail-first 确认旧前端在创建前目录错误后直接终止，且旧 timeout 文案会声称 `after dispatch`。修正后 image runtime/AgentRuntime 定向组为 `35 passed + 20 subtests`，Goal 生图 continuation 为 `1 passed`，frontend 为 `280 passed`；R002 继续补齐 clientRequestId AgentRuntime `2 passed`、AgentRun route `1 passed`、server 路由冻结/旧运行兼容 `7 passed + 3 subtests`。
- bundle/direct classic 的最终可重复 H4 为 `2 passed`：两个入口均覆盖新会话与既有会话的 `AgentRun 409 → image route refresh 200 → AgentRun 201`，两次创建使用同一非空 `clientRequestId` 和同一稳定 routeRef，每条仅一条用户消息、一个 AgentRun、一次授权和一次图片请求，刷新与既有 Run 恢复零重放。此前 localhost 假服务的应用内 Browser 观察也确认相同 409/200/201 链、刷新零 AgentRun POST 与唯一资产稳定；R002 按审批边界未再打开、导航、关闭或控制 Browser，该观察只作补充，H4 为可重复主证据。
- frontend build/freshness、变更 Python/JavaScript/H4 语法和 `git diff --check` 通过。所有新增非 Browser 回归使用隔离 `CODE_DATA_DIR`；未调用真实模型、外网、生产网关或凭据。

### 状态与边界

- `CODE-006` 保持完成且无需迁移 Session、AgentRun 或 ImageRouteRegistry；聊天 route、图片付费边界、SSRF、授权、资产所有权/删除和 Child 不继承均未改变。不启动 `CODE-009`、`CODE-044` 或 `CODE-049`。
- 受保护 `CODE-049` section SHA-256 保持 `FD3179A8617DDFAB7075F6F90740834E2C61BF601DE7A7F86E3E78755034578A`。用户验收产生的未跟踪 `data/image-route-registry.json` 只核对文件系统元数据，未读取正文、修改、删除或暂存；审批要求保留的临时诊断根也未手工递归清理。

## 2026-08-28 09:27 · Codex

### CODE-006 · imagegen Skill 与付费运行时收紧

- 按 Codex 官方内置 `imagegen` 的分层原则收紧项目 Skill：Skill 只负责区分新图/当前 Session 单参考图编辑并组织 prompt；明确一次 `generate_image` 调用、失败即停止、由运行时自动校验与缓存，不再让模型传 `size`、`quality`、`outputFormat`，也不允许用文件工具复制/移动缓存或虚构路径。真实 Skill 改用官方兼容的 `allowed-tools + metadata` frontmatter，Code 现有关键词/工具发现保持不变，`skill-creator quick_validate` 通过。
- 新 AgentRun 的前后端 `generate_image` 模型工具定义只公开 `prompt`、受控 `reference` 与 `count`。模型或旧冻结工具定义携带的已知 size/quality/outputFormat 会在持久化前被服务端忽略，授权与执行统一规范为 `auto/auto/png`；未知额外字段仍严格拒绝。有效 fingerprint、授权 identity 与 operationId 均基于规范参数，默认请求不再向上游发送 size/quality；安全返回可为任意实际尺寸，只要通过既有格式、损坏、单图 20 MiB、40 MP、总量与所有权限制，即以真实尺寸写入 Session-scoped 元数据。
- 新增同 AgentRun 付费熔断：任一 `generate_image` 已跨过 durable dispatched 边界后得到 `retryable=false`、`outcomeUnknown=true` 或 `notReplayed=true`，后续即使改变 prompt 或 legacy 参数，也在授权卡和上游请求前持久返回脱敏 `image_retry_blocked + notReplayed`。刷新/服务重启继续依据权威 tool execution 阻断；只有新的用户消息创建新 AgentRun 后才允许再派发一次。既有 route、授权、SSRF、资产所有权/删除、Goal continuation 与 Child 不继承合同不变。

### 验证

- fail-first 以本地三次调用证据建立：旧实现会把 `hd/standard/1024x1024` 透传并允许变参继续付费；修正后的确定性用例证明假模型即使提交这些字段，假上游只收到一次不含 size/quality 的请求，3×2 安全 PNG 正常缓存并保存实际尺寸。非重试 400、格式结果未知/notReplayed、Accept 前置授权、刷新/磁盘重载、旧冻结参数、未知字段拒绝与新 AgentRun 单次恢复均已覆盖。
- image runtime、AgentRuntime、routes、server、视觉刷新、frontend 与 Goal 相关扩大回归为 `801 passed + 161 subtests`；`skill-creator quick_validate`、Python/JavaScript 语法、frontend build/freshness 与 `git diff --check` 通过。bundle/direct classic H4 最终 `2 passed`：两个入口均证明模型工具表面无执行控制、generation/edit 上游请求无 size/quality、唯一 generation + 成功 edit + unsupported edit 三次付费请求、同 Run 第四次变参尝试零新增授权/上游、刷新零重放、禁用 fail-closed、唯一 Session 删除和两张资产 404；受管进程、端口和临时根均清理。canonical 重建后的中间复核曾在全部 hardening 断言通过后出现一次 bundle Session DELETE 400；完整脱敏附件审计后只增加错误类别观测，未改生产行为，最后一次双入口复核全绿。

### 状态与边界

- 不读取或复刻 Claude Desktop 能力，只以 Codex 官方 imagegen/skill-creator 为规范参考；未调用真实模型、供应商、外网或凭据，也未启动 `CODE-009`、`CODE-044`、`CODE-049`。`CODE-006` 保持完成。
- relay 给出的用户验收 registry 基线为 397 bytes / `08:38:36`，但本阶段首次只读预检已发现它在本阶段写入前由外部更新为 398 bytes / `09:03:43`；未读取其正文，随后所有测试显式使用隔离 `CODE_DATA_DIR`，收口时元数据仍为 398 bytes / `09:03:43`，文件未修改、删除或暂存。受保护 `CODE-049` section SHA-256 保持 `FD3179A8617DDFAB7075F6F90740834E2C61BF601DE7A7F86E3E78755034578A`。

## 2026-08-28 08:56 · Codex

### CODE-006 · 单参考图编辑验收修正

- 生图模型配置不再展示或保存“支持单参考图编辑”开关；设置说明收敛为“生图模型独立于 Agent 聊天模型。”及对应英文。旧设置、公开 route 与 AgentRun 中的 `supportsEdit=false`、缺失或 `true` 均可安全读取并被兼容忽略，不批量迁移或重写旧数据，新公开投影不再携带该能力字段。
- 当前 Session 中存在一张受控参考图时统一进入既有 `/v1/images/edits` multipart 流程，继续经过独立 image route、`image_generation` 授权、Session 所有权和付费调用 exactly-once 边界，不回退为文生图。第三方端点明确返回 404/405/501 时稳定映射为脱敏的 `image_edit_unsupported`，同时标记 `outcomeUnknown=true`、`notReplayed=true` 且不自动重试。

### 验证

- fail-first 在旧实现上得到 `5 failed + 1 passed + 2 subtests`，分别命中 false/missing capability 阻断、服务端二次阻断、通用 HTTP 误分类与设置投影残留；修正后最小组为 `4 passed + 6 subtests`。旧 durable registry 夹具另验证加载时不改写、刷新 revision/routeRef 稳定且旧字段不再决定编辑可用性。
- 相关 image runtime、AgentRuntime、Goal、routes、frontend 与 i18n 回归为 `463 passed + 39 subtests`；通用 AgentRuntime/server 回归为 `372 passed + 115 subtests`。前端 build/freshness、Python/JavaScript 语法和 `git diff --check` 通过。
- bundle/direct classic H4 最终 `2 passed`：设置页开关为 0、中英文简短说明准确、公开目录及 AgentRun 不含 capability 字段；同一独立 route 依次完成 generation、成功 edit 与 404 unsupported edit，三次均只有一个幂等上游请求，失败项固定为 `image_edit_unsupported + notReplayed`。刷新零重放、禁用 fail-closed、唯一 Session 删除与资产 404 继续通过，两个入口的受管端口、进程和临时根均清理。首轮 bundle 仅因构建产物未刷新仍显示旧开关，classic 则在终态折叠后使用了旧预览 locator；完整附件审计后重建 canonical 前端并在测试内等待 completed trace、显式展开，第二轮全绿。

### 状态与边界

- 本修正不改变授权、计费副作用、路由冻结、SSRF、资产所有权/清理、Child 不继承或第一阶段恢复协议；不调用真实模型、外网或凭据。`CODE-006` 保持完成，不启动 `CODE-009`、`CODE-044` 或 `CODE-049`。
- 用户验收产生的未跟踪 `data/image-route-registry.json` 未修改、删除或暂存；其文件系统最后写入时间保持为本阶段开始前的 `2026-08-28 08:38:36`。

## 2026-08-28 08:13 · Codex

### CODE-006 · 独立生图模型 API 产品面完成

- 在第一阶段独立 `ImageRouteRegistry`、`generate_image`、付费调用 exactly-once/outcome-unknown 和 Session-scoped 资产地基上，完成与聊天配置彻底分离的生图设置区：稳定 connection ID、名称、base URL、Key、启用状态、显式模型、编辑能力和默认模型均独立管理；前端只把运行时凭据重绑定到服务端，公开目录、Session、AgentRun、事件、日志和 DOM 只保留脱敏身份，不借用聊天 Key 或模型。前台 AgentRun 与队列在派发/入队时冻结公开 image route，Child/parallel v1 不隐式继承。
- 按 system `skill-creator` 作者流程新增短小、可确定发现且与本地绘图 Skill 互斥的 `imagegen` Skill，并以 observer-only evidence 合同观察成功 `generate_image` 回执。`image_generation` 保持独立副作用权限：Accept 使用脱敏授权卡，Read/Plan 不获得工具，Bypass 仍受服务端格式、尺寸、数量、SSRF、所有权和不重放边界。
- 权威工具结果投影为 Session-scoped 图片卡，支持受控预览、下载、刷新/服务重启恢复、文生图与单参考图编辑；客户端不接收上游 URL、服务器绝对路径或秘密。禁用/未配置独立路由时准确 fail-closed，Session 删除会清理其生成资产，旧记录继续无迁移兼容。

### 验证

- 定向 image/AgentRuntime/routes/frontend 回归为 `404 passed + 29 subtests`，扩大 AgentRuntime/server 回归为 `372 passed + 115 subtests`；`python verify.py doctor`、前端 build/freshness、Python/JS 语法和 `git diff --check` 通过。
- bundle/direct classic CODE-006 H4 最终 `2 passed`：分别验证独立设置/公开目录、脱敏 Accept 授权、唯一 generation 与 edit 请求及幂等键、Session-scoped 资产读取/预览、Escape 关闭、刷新零重放后显式展开 execution trace、终态 image route 与 Skill evidence、禁用路由零上游请求、权威 Session PUT 落盘边界、唯一无 body DELETE 及两张资产随后均为 404。H4 infra 的跨 generation Skill 夹具复制改为幂等覆盖，基础设施自检随后通过且受管进程/端口归零。
- 应用内 Browser 只连接确定性 localhost 夹具：验证独立生图设置与默认模型、脱敏授权卡、两张 2×2 PNG 的生成/单参考图编辑资产卡、同源下载、刷新后两卡稳定、禁用连接的准确提示；真实 overlay 交互由上述双入口 H4 覆盖。未读取浏览器存储或真实凭据，未调用真实模型、外网或供应商渠道。

### 状态与回退

- `CODE-006` 完成定义已满足并从 `TODO.md` 删除；第一阶段提交 `4f19de6` 与纠正提交 `206278f` 保留，第二阶段作为独立本地提交收口。未自动启动 `CODE-009`、`CODE-044` 或 `CODE-049`。
- 回退不需要迁移或改写旧 Session/AgentRun：旧版本忽略新增可选字段，禁用独立生图 connection 即可让新运行安全回到无可用 image route；聊天路由、聊天 Key、对象存储和多实例边界未改变。R007 H4 受管根已由 canonical cleanup 清理；Browser 包装器中断遗留的合成夹具根及此前已登记的无 owner 测试根未手工删除，但对应 helper/isolated_host/Playwright 进程均已退出。

## 2026-08-28 03:34 · Codex

### CODE-006 · 独立生图运行时基础复核纠正

- `ImageRouteRegistry` 的 `routeRef` 改为只对稳定的 `connectionId + modelId` 身份做服务端加盐 HMAC；Key 与 baseUrl 的运行时轮换不再改变 `routeRef` 或公开目录 revision，已有 `prepared` / `waiting_credentials` AgentRun 可在同一冻结公开身份下使用最新运行时绑定继续。模型、能力、启用状态或 label 等公开目录变化仍按原合同推进 revision，磁盘继续不保存 Key、baseUrl 或可复核的凭据指纹。
- 上游结果解码现在兑现工具请求的输出合同：实际 PNG/JPEG/WebP MIME 必须与 `outputFormat` 一致，非 `auto` 尺寸必须与请求宽高精确一致。格式或尺寸不符分别返回稳定的 `image_response_format_mismatch` / `image_response_size_mismatch`；因付费请求已派发，结果同时标记 `outcomeUnknown=true`、`notReplayed=true`，且不会保存不合约资产。文生图、单参考图编辑、base64 与受控 URL 分支均纳入确定性覆盖，JPEG 映射和固定尺寸正向样本也已验证。
- `image_generation` 已进入 Goal continuation 的成功副作用保护集合；Goal successor 只继承冻结的公开 image route 身份。相同 fingerprint 的已完成生图调用被 `notReplayed` 阻断且不增加上游调用，不同 prompt 产生不同 fingerprint 并可正常执行。背景 Child/队列仍不会隐式继承或猜测 image route。

### 纠正验证

- 先在旧实现上新增失败回归，稳定暴露凭据轮换推进 revision、输出格式/尺寸未校验以及 Goal successor 未继承成功生图 fingerprint 三类缺口。
- 生图 registry/upstream/资产/AgentRun/Goal continuation 直接回归：`55 passed + 14 subtests`；AgentRuntime、routes、server 与聊天 Model Route Registry 隔离相关回归：`449 passed + 133 subtests`。
- Python 语法与 `git diff --check` 通过；仅保留 `server.py` 既有 batch 字符串 `SyntaxWarning` 和相关回归中的一条预期损坏 TIFF 警告。本次为 R001 未发布基础提交上的定向纠正，未迁移或改写 Session/AgentRun，未调用真实 Key、外部模型或网络渠道，也未启动第二阶段。

## 2026-08-28 03:10 · Codex

### CODE-006 · 独立生图模型 API 第一阶段完成

- 新增与聊天 `ModelRouteRegistry` 完全分离的 `ImageRouteRegistry` v1。连接名称/ID、模型、启用状态与能力进入版本化公开目录；经 03:34 复核纠正后，`routeRef` 由服务端盐值对稳定的 connectionId/modelId 身份生成不透明 HMAC，Key 和 baseUrl 仅存在运行时内存，凭据或端点轮换不会改变公开 route identity/revision。目录、Session、AgentRun、事件、快照和错误均只保留非秘密身份；服务重启后未重新绑定凭据时稳定进入可恢复的凭据等待，不借用聊天 Key、workbar 路由或聊天模型。
- 服务端新增唯一 `generate_image` 工具和独立 `image_generation` 副作用。`read`/`plan` 不获得该工具，`accept` 复用可恢复授权门禁且仅显示模型、数量、尺寸、质量、格式和是否有参考图，不显示 prompt/Key/baseUrl；`bypass` 仍经过全部服务端限制。模型参数只允许 prompt、当前 Session 附件/生成资产身份、尺寸、质量、数量和格式；额外路由、header、Key 或本地路径字段在持久化前被脱敏拒绝。AgentRun 只冻结公开 image route 身份，背景 Child/队列未显式传入时不继承或猜测路由。
- 上游基础协议限定为 OpenAI-compatible `POST /v1/images/generations` 与单参考图 multipart `POST /v1/images/edits`，固定请求 `b64_json` 并发送确定性 `Idempotency-Key`。URL 回退仅在服务端内部使用：限定 HTTP(S) 默认端口、公网 DNS 全量校验与连接 IP 绑定、TLS 主机名验证、最多 3 次重定向、禁止 HTTPS 降级，并共享唯一总超时。URL/查询、Authorization、base64 和供应商域名均不进入工具结果、客户端、模型上下文或诊断。
- 生成图像限定 PNG/JPEG/WebP、每张 20 MiB、每批 1–4 张、每张 40 MP，以真实 signature、HTTP MIME、Pillow verify/decode、尺寸、响应总量和损坏校验 fail-closed。资产保存于单机 `code/data/generated-assets` 专用仓库，仅用高熵 asset ID 和 Session-scoped 受控 URL；旁车仅保留 session/run/tool call、hash、MIME、像素、字节和创建时间，不暴露服务器绝对路径。读取复核 Session 所有权和文件完整性，删除 Session 仅联动删除该 Session 资产。本阶段明确不支持多实例或对象存储。
- 外部请求前先持久化 operationId 与 `prepared`，在付费请求前原子切到 `dispatched`。已持久资产按 operation/session/run/call/count 确定性复用；重启发现 `dispatched` 但无完整资产时固定转为 `image_outcome_unknown` + `notReplayed`，不再自动请求上游，部分资产证据也不删除。拒绝、取消、超时、网络失败、401/403、429、无凭据、陈旧/禁用路由、非法响应与未知诊断均使用稳定且不含秘密的错误码。

### 验证

- `python verify.py doctor`：通过；当前/H4 Python、Node/npm、esbuild、Playwright 与受控 Chromium 均可用，Chromium 临时目录与相关进程前后归零。
- 生图 registry/upstream/SSRF/图片/资产/AgentRun/routes 直接回归：`24 passed + 8 subtests`；Agent 事件协议合并定向：`35 passed + 166 subtests`；聊天 Model Route Registry 隔离回归：`7 passed + 3 subtests`。
- AgentRuntime + routes + server 相关回归最终为 `442 passed + 130 subtests`。首轮仅出现一次既有 Windows 命令取消测试的临时目录句柄竞态，附件审计后该用例单独复跑通过，同组完整复跑全绿，未扩大修复范围。
- 扩大回归排除仓库已登记的三份陈旧冻结 harness（AgentRun v1–v4 哈希、旧手工压缩可见历史、旧压缩失败注入）后为 `1532 passed + 1025 subtests`。未排除的完整扫描中，其他用例 `1541 passed + 1042 subtests`，红项仅限上述三份已知冻结证据。Python/JavaScript 语法与 `git diff --check` 通过；仅保留既有 batch 字符串 `SyntaxWarning` 及三条预期损坏 TIFF 警告。
- 本阶段未增加最终设置页或 Skill UI，按审批不运行 Browser/H4；全部上游使用本地确定性假响应，未读取或调用真实 Key/外部生图模型、聊天模型或网络渠道。

### 状态与回退

- `CODE-006` 仍保留在 `TODO.md` “Code 产品与工程 / 当前进行”；下一动作是另行 STRICT 审批设置页、独立模型选择、`imagegen` Skill、前端授权/预览和 bundle/direct classic H4，本阶段不自动继续。
- 回退无需迁移或重写旧 Session/AgentRun：旧版本忽略可选 `imageRoute`、新工具与额外事件字段；不调用 image route refresh 或移除本阶段生产入口即可使新运行回到无生图工具状态。未修改聊天连接/Key 设置、发布定义或真实数据，未启动 `CODE-009`、`CODE-044`、`CODE-049`。

## 2026-08-28 02:00 · Codex

### CODE-014 · Skill 执行证据校验正式完成

- observer v1 以服务端事实为唯一权威：AgentRun 创建时冻结该请求系统提示实际注入的 Skill 集合、每项 `SKILL.md` 内容哈希及经校验的可选 `evidence.json` v1 合同。显式 Skill 为单项，自动匹配沿用既有确定性排名、禁用排除和最多三项，不在恢复时重新匹配；客户端只能提交名称意图，不能提交合同或扩张 `allowedTools`。
- 观察结论只统计唯一的权威 execution/result 与成功产物事实，模型正文、自述、重复事件、原始命令、文件内容和 stdout/stderr 均不能证明成功，也不会进入快照或诊断。`satisfied`、`partial`、`failed`、`unsupported_completion`、`legacy_unverified` 五态保持 observer-only；旧 v5、单 Skill 旧形状及无合同 Skill 无迁移兼容读取。
- 第二阶段只为用户显式激活 `/code-review` 且冻结合同明确 opt-in 的运行启用低风险门禁，最低证据为一次成功且唯一的 `read_file`。自动匹配 `code-review`、其他 Skill、无合同/无效合同与旧记录继续 observer-only，门禁不改变工具权限、授权策略或执行顺序。
- 证据不足时，候选回答和脱敏缺口原子持久化为 `waiting_skill_evidence`。Continue 在同一 AgentRun 恢复并复用既有 execution，已完成工具不重放；Skip 不发模型请求，直接完成并记录 `userOverride` 与未验证状态；Cancel 复用既有取消终态。三种动作使用持久幂等身份，刷新、快速切换和服务重启后可恢复；关闭 `code-review` enforcement 后，新运行立即回到 observer-only，既有等待运行仍可 Skip 或 Cancel。
- `loadSession()` 在既有导航序列守卫内恢复完整 `skillEvidenceRequest`，只有当前会话渲染三按钮面板；非当前会话只显示侧栏状态。紧凑 Session 索引/API 新增可回退的 `interactionState`，值仅能是 `waiting_user_input`、`waiting_authorization`、`waiting_skill_evidence` 或空，由服务端从权威 pending request 推导；旧索引在现有加法回填锁内一次性补齐，metadata-only 写入保持已有值，完成、跳过或取消后清空。列表不暴露 request、gate、AgentRun、Skill、路径、命令、URL、凭据或错误正文，完整面板仍必须显式加载 Session，不能由摘要伪造。

### 验证

- `python verify.py doctor`：通过；当前/H4 Python、Node/npm、esbuild、Playwright 与受控 Chromium 均可用，Chromium 临时目录和相关测试进程前后归零。
- CODE-014/R006 定向、AgentRuntime、路由、Session、前端与兼容回归：`536 passed + 115 subtests`。扩大回归在明确排除三份基线已陈旧冻结 harness 后为 `1508 passed + 1017 subtests`；旧 harness 分别冻结 AgentRun v4、旧手动压缩可见历史与旧压缩失败注入，不属于本阶段产品回归。
- bundle 与 direct classic 的等价拆分 H4：`6 passed`。Continue 在未更换假上游的同一代内保持同一 AgentRun、唯一用户消息、唯一 continuation 和唯一成功 `read_file`；另两条等待门禁跨服务重启恢复侧栏/面板/gate/candidate，Skip 与 Cancel 均不产生模型或工具请求，动作和终态幂等。
- 应用内 Browser 技能在隔离本地假上游完成无真实模型验收：当前会话显示 Continue、Skip、Cancel 三按钮；切到另一持久会话后面板隐藏、原会话侧栏为 `Waiting for Skill evidence`；普通刷新和服务重启后两个等待摘要仍在，按已知 Session ID 显式切回后恢复同一候选与三按钮。Continue 仅保留一条用户消息、一条最终回答和一次 `read_file`；重启代 Skip/Cancel 前后均为 0 模型请求、0 工具执行，刷新后侧栏恢复 idle。
- `npm run build:frontend` 与 `npm run verify:frontend`、Python/JavaScript 语法检查、`git diff --check` 全部通过。未读取浏览器存储或凭据，未调用真实模型、外网或线上配置；隔离 H4 端口、子进程和临时根全部清理归零。

### 状态与回退

- `CODE-014` 的 observer v1 与显式 `/code-review` 试点完成定义均已满足，事项从 `TODO.md` 删除；已验证方案作为正式低风险试点保留。
- 回退无需迁移或重写 Session/AgentRun：关闭 Skill 的 enforcement opt-in 即可让新运行回到 observer-only；旧版本会忽略可选 AgentRun 字段和 Session 索引额外枚举。未批量为其他 Skill 增加合同，未启动 `CODE-006`、`CODE-049`、PPT 或 `output/tmp` 阶段。
