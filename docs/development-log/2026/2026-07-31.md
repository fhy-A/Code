# 2026-07-31

## 2026-07-31 05:34 · Codex

### 新会话首次发送关键路径与首帧反馈优化

- 只读诊断确认首条消息卡顿由两段关键路径叠加：前端在渲染用户消息前串行等待 `createSession()` 完成会话创建、项目根同步与侧栏刷新；后端 `/api/sessions` 又会针对索引中的每条会话重复解析元数据路径。184 条会话时，列表请求实测约为 3–5 秒。
- 第一阶段将会话列表读取改为一次性建立元数据文件快照，保留扁平旧格式与现有 JSONL 数据兼容，不修改会话协议。184 条会话时请求降至约 0.48–0.90 秒，平均约 0.63 秒。
- 经用户授权，将 150 条旧测试会话可恢复地转移到 `data/session-archive/latency-test-20260731-040804`，保留原始索引备份与清单，没有删除数据；活跃会话降至约 34–35 条后，列表请求稳定在约 0.17–0.20 秒。归档属于本地测试数据，不纳入代码提交。
- 第二阶段为首次发送增加乐观投影：提交后立即显示同一个用户消息对象并进入运行状态，随后再完成真实会话创建与持久化；非关键侧栏刷新延后执行，并避免迟到的列表响应覆盖当前运行状态。创建失败时仍保留用户输入，成功时不会产生重复消息。
- 3011 开发版真实验证中，点击发送后用户气泡约 177 ms 出现，运行状态约 280 ms 出现；页面只显示一条用户消息，模型约 5 秒返回 `OK`，对应 JSONL 恰好写入一条用户消息和一条助手消息。首次验证遇到一次上游模型 120 秒超时，复测成功，确认与前端首帧优化无关。
- 自动验证通过：`app.js` 与全部 18 个 `src` JavaScript 文件语法检查通过；阶段二相关测试 149 项通过；完整测试为 848 项及 236 个子测试通过。用户完成页面验收并确认“没问题”。
- 本次仅调整首条消息关键路径和会话列表扫描方式；JSONL 会话结构、工具协议及旧会话格式均未改变，无需迁移，回退对应前后端代码即可恢复旧行为。

**涉及文件**：`app.js`、`server.py`、`tests/test_frontend_modules.py`、`tests/test_projects.py`、`docs/development-log/2026/2026-07-31.md`

## 2026-07-31 03:20 · Codex

### Codex 风格执行轨迹、稳定任务状态与回答切换

- 顶部任务状态现在只在首个模型响应前显示等待、延迟或恢复提示；首个模型响应开始后固定显示“已处理 + 任务总耗时”，不再随模型轮和工具轮反复切换文案，计时刷新频率统一为每秒一次。
- 阶段说明与工具调用按模型实际执行顺序投影：去除“正在读取/执行……”等重复操作播报，同一阶段的工具记录合并为一个轻量折叠组；运行中标题显示当前工具或命令，完成后改为“查看了多个文件”“运行了多个命令”“编辑了文件”等统述表达。
- 工具阶段外层状态圆点已经移除，单项工具内部仍保留成功、失败、运行中状态；顶部状态横线已经移除，正式模型回答的模型名分隔线继续保留。
- 当首段有效正式回答进入 `answer` 投影时，在同一次消息列表 DOM 提交中先建立正式回答投影并把此前的阶段说明、工具组整体折叠到“已处理 + 计时”下；思考中、工具执行中、空白响应、等待恢复和占位内容不会提前触发折叠。
- 用户在最终回答流式输出期间或完成后手动展开执行轨迹时，后续 Token 更新、完整消息重投影和运行结束切换均保留其展开选择；无执行轨迹的普通问答继续使用普通完成状态，不生成空折叠区域。
- 改动仅涉及前端状态和消息投影，没有修改 JSONL 会话结构、工具协议或历史数据；旧会话通过现有消息内容直接投影，无需迁移，可以通过回退前端文件恢复旧展示。
- 自动验证通过：`app.js` 与全部 `src/**/*.js` 语法检查通过，`tests/test_frontend_modules.py` 110 项通过，`tests/test_p0_stability.py` 25 项通过，完整测试为 843 项及 236 个子测试通过，`git diff --check` 通过。
- 3011 开发版完成真实多阶段工具任务验证：工具期间轨迹可见，首段正式回答出现时直接切换为默认折叠轨迹和可见回答；历史会话默认折叠，手动展开后触发新一轮完整重渲染和运行结束仍保持展开。用户完成视觉验收并反馈“没太大问题”。
- `TODO.md` 中“新会话首次发送用户消息短暂渲染卡顿”仍是独立未完成事项，本阶段未改动；当前渠道正式准入活动交接也与本阶段无关，保持原样。
- 本地提交 `0c636de feat: 优化执行轨迹折叠与任务状态` 只包含已经验收的消息渲染代码、i18n 文案和自动化测试；开发日志索引仍参与既存未提交的文档迁移，本阶段同步结果保留在工作区，未顺带提交其他协作者的修改。

**涉及文件**：`app.js`、`src/core/i18n.js`、`src/ui/messages.js`、`styles.css`、`tests/test_frontend_modules.py`、`tests/test_p0_stability.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-07-31.md`
