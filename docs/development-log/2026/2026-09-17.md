# 2026-09-17 开发日志

## 2026-09-17 04:27 · DSH 开发会话（发布接手）

### v0.6.13 发布阻断最小修复：归档备份误判会话身份（code0613-release-R003）

- 事实：R001 canonical prepare 在 pytest_full 门禁失败（27 失败 / 3793 通过 / 6 skip）；R002 定性为 4 项真产物缺陷、11 项过期测试契约、4 项检查出行尾伪影、7 项 clone 缺宿主受管运行时、1 项渲染超时。
- 修复（code_runtime/project_archive.py）：ArchiveService.note_meta_write 仅把 SESSIONS_DIR 下的会话元数据（扁平 1 段或迁移后 YYYY/MM/DD 层级 4 段，与 preview() 的 depth 0/3 一致）当作会话元数据；该判定是**深度筛选**：只区分 1 段与 4 段，并不校验 YYYY/MM/DD，因此不冒称排除所有任意子目录；按名显式排除的唯一子树是压缩备份 archive（SESSIONS_DIR/archive/**，其自身嵌套也可能是 4 段），不进入 session_id()（R006 修正，R005 的“仅深度”写法会接受 archive/YYYY/MM/<sid>.json）。不做目录扫描、不吞直属非法身份的异常。R003 曾把规则收窄为直属父目录，导致层级元数据不再 bump generation（批量 move_back 冲突用例失败），R005 已修正为上述布局判定。该误判由范围内提交 17695ef（CODE-072 归档）引入。
- 新增回归（tests/test_project_archive_meta_write.py，R005/R006 扩展为 9 项）：真实 write_json 钩子下扁平与日期层级的 generation 递增、项目移走再移回三次位置变化均登记 generation、archive 备份 JSON 与 JSONL 内容一致且不触碰 generation、非法身份仍抛 project_archive_invalid_state、SESSIONS_DIR 外文件忽略；sidecar 使用记录型 stub，仅证明调用边界，不代表真实 sidecar 持久化验收。R006 增补负例：archive 子树四段路径（archive/YYYY/MM/<sid>.json）不得进入 session_id()；同一脚本把旧的“仅深度”守卫复原后对该路径实测 bump=1（位置变化被登记），证明该负例在修复前为红；生产 write_json 下扁平与 2026/09/17 层级各 bump=1 且 generation 变化、move_back 三次得 3 个不同 generation、两类 archive 备份（2 段与 4 段）bump=0 且 generation 不变。
- 过期测试契约修正（11 项，未改产品行为）：摘取式 do_GET 沙箱补真实模块全局（re/managed_memory）；app.js 切片补 getEffectiveMaxTokens 真实实现与 maxTokens DOM 位；补 previewFeature.newDraft 依赖；RUNTIME_MODULES 明确列出范围内新增的 11 个模块；SERVER_TOOL_REGISTRY 期望补 delete_memory；safe_memory_name 5 项改为断言 managed_memory.MemoryError 且 code=memory_name_invalid（MemoryError 为 ValueError 子类），未修改产品文案。
- 环境：clone 按 Git 原始 LF 字节重新检出（HEAD tree 与 index tree 同为 477c711c，blob/index 不变）；仅从原库复制 data/runtime/python 作为 clone 独立受管运行时（解释器 sys.prefix 指向 clone、所需依赖可解析），原库未改。
- 定向结果（R003）：首轮 27 项中 22 通过、5 失败；4 个身份失败 node-id 在 R003 未通过（R003 未声称其通过）。R004 新增归档回归 5 项通过；合计 32 项 27 通过 5 失败。R006 完成行尾配对归因（隔离工作副本、两份源码树逐字节校验“仅行尾不同”）：① 切片长度/摘要属行尾派生——同一 LF 切片 23427/SHA bd6684bb…，仅把 752 个 LF 换成 CRLF 即 24179/SHA da6ae098…，与旧冻结值逐字一致；v0.6.12 与 HEAD 的 LF 切片相同。② 渲染摘要不是行尾差异——把全部 40 个源码文件（app.js + src 全部文件）转成 CRLF 后，ui.htmlHash、19 项 scenarioHashes 完全不变；二分定位：febf62a 全部命中旧值、6fd84a4 全部翻新，其间唯一改动文件是 src/ui/messages.js；用 febf62a 其余源码 + 6fd84a4 的 messages.js 即复现新 ui.htmlHash 6b1f194d…；用 febf62a 源码 + 全 CRLF 检出可逐字段复现旧 ui.htmlHash 8c7bc211…、旧 replayHash 1e573283… 与旧 D3 suiteHash e09df3a3…（19/19 场景哈希一致）。③ 因此旧期望＝冻结时（b1aa02c/febf62a）源码内容 + CRLF 检出，新期望＝HEAD 源码 + LF 检出；冻结值在 HEAD 上本就无法由任何行尾组合复现（messages.js 已在 6fd84a4 变更），R002 把两项渲染夹具失败一并归因于“行尾伪影”不准确，本条更正。④ 字段级因果：D2 证据中仅 $.ui.htmlHash 与 $.replayHash 变化；D3 中仅 $.uiHash（19/19）、$.scenarioHash（19/19）与 2 项 $.pendingUiHash 变化，其余行为字段零差异。派生关系：ui.htmlHash=sha256(projection.html←src/ui/messages.js 渲染)；uiHash=sha256(html)、pendingUiHash=sha256(pendingRetryHtml)、scenarioHash=canonical(result 含二者)；replayHash/suiteHash=canonical(证据树含切片与上述摘要)。⇒ R004 刷新后的期望值与当前产品行为一致，无需再改预期；但“全部为 LF 派生”的表述不成立，已按上述更正。
- 覆盖限制：R006 按名排除 archive 备份子树并补负例；meta-write 9 项通过，meta-write/批量归档/批量删除/整库归档删除/两份 manual-compaction 相关集合合计 25 项 + 38 子测试通过，六项旧失败（4 身份 + 2 批量 move_back）均已通过；仍未跑全量 pytest 或 prepare；配对实验的完整原始输出、19 场景 raw_outputs、原始 HTML、逐字段因果表与 guard 验证结果均落在 clone 外的 r006 证据目录；发布正文仍待最终审定；运行中审查/卡片缓存未纳入本版本。
## 2026-09-17 02:40 · Codex

### CODE-042 R028：用户验收标签拖动，本地提交后交DSH发布

- 用户明确“没问题，接着发布v0.6.13，可以用DSH”；审批R028确认R027独立425项SHA核对及42项专项已通过，授权本开发仅做本地验收收口。R027候选SHA `2cb80f47329209d548c241e68e29d623ea00a83e05a1b69d05cc27eb0aae6d5e`与33项公共/私有/构建接手一致，产品和测试保持冻结；历史“待验收/未提交”描述各轮当时状态，以本条用户PASS为准。
- 本提交仅纳入R026/R027 preview/CSS、拖动H4以及指导、9月17日志和索引的批准差量；原WB014/9月14索引行留工作区，9月14未跟踪日志、data、采购handoff、公共TODO等保持。复用已通过验证，不重复测试；执行定向diff、精确暂存及提交后Git核对。会话94.166MiB正常复用。
- 本开发完成提交后释放本人租约并停止自动写入，发布由审批侧交DSH既有`session-31ee4868-5d0a-4ada-a983-57f169e552f9`，DSH须以本人runtime重新接手；本轮不向DSH发送消息，不运行release/push/tag，不代写发布说明，不隐藏共享差量。现有dirty索引与未跟踪文件应由发布接手核对，不能冒称工作树全净；此前被拒删的临时目录继续保留。卡片缓存/可见自动加载和运行中阶段保持排队，发布不自动启动下一开发事项。

## 2026-09-17 02:09 · Codex

### CODE-042 R027：完整标签跟手、同宽占位及平滑让位（双入口定向通过，待体验验收，未提交）

- **授权与范围**：用户明确“整个标签跟随鼠标”“适合放入时显示占位”“动画要丝滑”，审批`code042-preview-review-R027`按STANDARD视觉差量直接续接。R026冻结SHA `006adf6b896b592bb2daff9a701ed39be0d616594fd352e256c7c3ed4cd84f3c`源/私有/构建接手一致，基线仍`e873b90`；原视觉不算用户PASS。仅preview/CSS、既有拖动H4和事实源变化，后端/打开协议/排序提交语义/v1保持。
- **呈现**：保留抓取点偏移的完整标签浮层直接跟随最新指针，包含原图标、标题截断及关闭外观；aria-hidden/inert/无重复id/role/data且pointer-events:none，专用主体class保持原布局。固定各标签原宽度，以原几何与scroll判断目标，同宽占位、相邻项140ms transform让位；原语义节点保留capture，合法pointerup才排序/persist。关闭命中仅在指针事件重算，避免RAF被滑过指针的邻项反复翻转；无外部/跨Session拖入。
- **结束与交互**：越界撤占位并恢复邻项，原取消/语义变化清理浮层、占位、临时尺寸/位移/capture/RAF；松手浮层140ms transform/opacity落位，真实目标立即可交互，下次pointerdown清理未完动画。减少动态效果完全关闭此交互邻项过渡并跳过落位动画。活动正文、MRU/reuse、文件读取和v1沿原合同；拖动中不写存储。
- **最终证据**：`%TEMP%/code042-tab-drag-z68g1w/result.json` bundle/classic **42项/10图**通过。新增实际鼠标5点分段/快速跨项/反向跟手、原抓取偏移、浮层主体/关闭宽度与占位宽高位置、语义唯一、越界撤销/取消零写入与零读文件；18个静止鼠标帧占位不变，每入口邻项记录8组不同位置、7帧活动过渡；落位140ms keyframe、立即开始下次拖动及无遗留、减动画均通过。原36项排序/异步/复用/Session/root/v1与250中文深色两端实际换位、390横滚及720混合标签一起复跑通过。代表宽屏与250深色图已目视；合成pointercancel/blur/pagehide覆盖限制保持，不把截图/帧样本冒称用户“丝滑”PASS或设备帧率。
- **修正与诊断**：`uhHjbj`首轮旧目标偏后一项，改起始原几何阈值后通过；`3nm5aO`抓取点断言漏计button内侧1px边框，按真实鼠标起点修测试；`xjiHup`减动画遇既存全局`.01ms !important`规则，局部明确none，并修placeholder继承preview-head span上边距导致下移4px。浮层剥role后专用class复用视觉规则，采纳独立只读检查提示。`dIoa0S`40项通过仅中间证据，最终以42项及增加的过渡位置/连续重拖证据为准，失败结果保留。
- **验证与收口**：最终构建freshness、JS语法/diff通过；复用R026 doctor、469/48及22/20/63、39和旧后端等证据，未重跑无关全集。会话93.781MiB正常复用。本轮所有自建宿主退出/端口关闭/root移除均通过；R026及更早拒删目录保留未重试，不冒称旧根已清理。公共TODO、采购handoff、data、WB014/9月14日志等共享差量保持，私有CODE042只保留待验收差量。候选待独立复核/主观体验PASS，未暂存/提交/push/tag/release，不开始卡片缓存或运行中阶段。

## 2026-09-17 01:50 · Codex

### CODE-042 R026：已验收阶段独立提交，标签鼠标换位候选内部通过（待独立复核/手感PASS）

- **阶段边界**：R018–R025精确25公共文件已提交`e873b90b78b29f6c0f067eb9d8edbe770302873e`（功能：完善多标签审查与空白页文件树预览）；索引原WB014/9月14行通过局部暂存保持在工作区。本轮拖动仅preview/CSS与新增H4及事实源差量，未暂存/提交，不混入已验收提交，不push/tag/release。
- **产品**：文件/空白/审查统一鼠标拖动，6px阈值、插入标记、两端32px区域持续横滚；不切当前正文、不读文件、不改变MRU/复用槽。关闭/＋/列表不参与拖动；短点击、键盘Home/End按当前视觉顺序及原关闭回落保持。Escape/取消/失焦/pagehide/resize、Session/root和标签语义变化取消，重复无变化重绘保持原capture，释放后抑制误点击。
- **顺序与兼容**：沿用v1文件order，无新增持久字段，空白/审查页内顺序；保存失败保留原v1字节并提示。异步空白转文件按当前槽位，选择已打开文件只消耗空白；手动排序后重复/迟到请求不拉回旧requestedOrder，原打开token有效，新文件按意图先后追加；普通单击继续使用原复用槽。回退至本轮基线前端无需迁移/删数据。后端及容量20文件/空白+1审查保持。
- **最终专项**：`%TEMP%/code042-tab-drag-NZX97p/result.json` bundle/classic **36项/8图**通过；真实鼠标验证混合非活动换位、两端持续滚动、250中文深色实际首尾互换、720/390视口、关闭非handle/短点击、MRU/复用、重复/逆序响应、移动空白同槽/去重、成员增删、Session/root、保存失败及reload/缺order旧v1。pointercancel、window blur、pagehide使用合成事件，非操作系统输入覆盖；代表宽屏插入标记和250深色图已目视，截图不替代用户PASS。
- **适用回归**：本轮`code042-r026-regression.txt`完整前端 **469 passed/48子测试**；`code042-preview-evidence-8OjbM6/result.json` **22主/20状态/63恢复**及连续审查`code042-continuous-review-ZABFeC/result.json` **39项**通过。前三套早于最终无变化重绘/capture目标守卫微调，最终双入口专项覆盖该差量；不冒称全集在最终微调后复跑。doctor全通过，最终freshness、JS语法及diff检查通过，阶段末92.907MiB正常复用。
- **诊断边界**：原目标靠后时metadata扫描触时间上限（`v3Mghd`与最终结果有reasons=time），独立`dN8joC --picker-probe`可完整返回；拖动专项改用排序靠前的真实唯一文件`a-blank-target.txt`，未修改后端扫描预算，也不冒称始终完整枚举。`cnHbJn/result.json`记录pointer实际落在保存失败的warning toast，后续等待真实提示消失再发新手势，不修改toast或放宽拖动断言。早期`tUUzTR`30项、`VGUFyj`34项仅增量诊断，最终以36项为准。
- **临时状态与保护**：首轮`code042-tab-drag-Ib78wk`因未处理waitForResponse拒绝掩盖原失败，后续为等待Promise即时附拒绝处理。遗留`%TEMP%/code-h4-e2e-s5krp5`经只读进程核对无匹配isolated_host，但未保留端口独立证明；精确清理被自动审批`blocked by policy`拒绝，目录保留且不重试，说明见该证据目录cleanup-note.json。其后含最终各轮宿主退出、端口关闭、root移除通过；此前拒删根继续保留。data、WB014/9月14日志、采购handoff和公共TODO保持；卡片加载、运行中阶段未开始，拖动待独立异步/持久顺序复核及用户手感验收。

## 2026-09-17 01:10 · Codex

### CODE-042 R026前置收口：预览/审查/空白文件树已获用户PASS并授权本地提交

- 用户明确“可以了，再加标签页拖动换位”，审批通过`code042-preview-review-R026`确认R018–R025当前阶段验收，并授权按R025冻结清单先做独立本地提交；R025候选SHA `cd5ff2b1bbf2594b588d35e427fedd351e4499478a678f887740018a25664176`及335项独立核对作为事实来源。历史条目中的“未提交/待验收”表示各轮当时状态，以本条后续验收为准。
- 本次归档包括单轮连续审查/紧凑diff、单行标签/溢出列表、页内空白标签与有界文件选择树、加载布局和普通箭头呈现；所有原测试/失败/skip/覆盖边界按原轮证据保留，不冒称本轮重跑。新阶段doctor全通过（`%TEMP%/code042-r026-doctor.txt`），R025公共/私有/构建hash接手一致，90.500MiB正常复用。
- 仅精确暂存R025的25公共文件及本次验收记录，索引中的既存WB014/9月14行保持未暂存，9月14日志/data/采购handoff及公共TODO不纳入；不push/tag/release。拖动换位属于后续单独候选，不混入本次提交，运行中审查仍未验收。

## 2026-09-17 00:59 · Codex

### CODE-042 R025：删除目录加载圆圈（QUICK定向通过，未提交）

- 用户明确不需要短暂加载圆圈，按`code042-preview-review-R025`直接授权删减：preview目录图标始终使用展开/收起箭头，删除spinner及其专用动画CSS；固定16px槽、行高、无障碍busy/加载名称、失败重试、取消/迟到、搜索和选中文件状态均保持，无调度/后端/i18n变化。
- `%TEMP%/code042-picker-loading-ZytyZw/result.json`的`--icon-only`双入口 **4项/6图**通过：持有目录响应时普通箭头、无spinner元素或动画、可访问busy、几何/scroll/focus稳定、完成正常及收起取消后迟到不插子项；无正文读取。构建freshness与diff检查通过，R024其他18项几何/10定向及既有后端/预算证据明确复用，不跑无关全集或重复主观方案确认。
- 隔离宿主退出、端口关闭、root移除通过；租约内仅本差量与事实源，未暂存/提交，拒删根及其他共享差量保持。用户删减要求不等于整个R023交互PASS；下一阶段拖拽/卡片加载/运行中仍未启动。

## 2026-09-17 00:48 · Codex

### CODE-042 R024：目录加载提示移入固定图标槽，消除树上下跳动（内部验证通过，待体验确认，未提交）

- **授权与复现**：用户明确展开文件夹时整体弹动并短暂显示加载文字，依`code042-preview-review-R024`按STANDARD反馈修复。先在R023原产品上运行`%TEMP%/code042-picker-loading-GmCVzE/result.json`的observe模式，双入口根展开的树视口与触发行Y均为**220→264→220**，证实顶部`treeLoading`状态块新增44px再消失；保留坐标与4图，不把预修复observe算PASS。
- **最小产品修订**：仅preview/CSS去掉普通目录加载对顶部状态的占位，使用固定16px目录图标槽和12px加载环，目录行`aria-busy`及既有中英加载文案可访问名称；减少动画时静态显示。搜索/文件打开的顶部状态和取消、每目录错误/重试保持。原树请求、配额、队列、controller/迟到围栏、root/Session/v1及后端不变，未修改其他标签、拖拽或运行中行为。
- **焦点补修**：首轮几何检查通过至Escape分支后，发现取消时搜索框聚焦又被app外层通用INPUT Escape处理blur；只在本选择器已处理Escape后停止冒泡，使其保留原取消与焦点归属。未修改app全局快捷键或取消调度。失败诊断`code042-picker-loading-KLSgeT/result.json`保留，最终不放宽焦点断言。
- **最终定向证据**：`code042-picker-loading-1NOlg7/result.json` bundle/classic **18项/12图**通过，记录标题、搜索、结果视口、触发行rect、scrollTop和焦点；根相同坐标为**220→220→220**。根/250px嵌套/720px并行兄弟、非零滚动、失败重试、加载中收起及迟到、Escape取消、无正文预读、搜索/选中文件状态、中英390深色可访问加载通过。几何断言容差0.75px，下方兄弟随真实子项插入的自然移动不作抖动。代表加载中/完成图已目视；未将内部截图当用户体验PASS。
- **验证与边界**：最终`code042-r024-final.txt`预览定向 **10 passed**，构建freshness、JS语法及diff检查通过。复用原i18n键，未改i18n文件；中英通过实际页面切换核验，不机械重跑525或未变更后端/diff/预算全集。R023其他证据保持并明确复用，物理后端SHA仍保持。所有本轮隔离宿主退出、端口关闭、root移除，Python指定数据根未创建；旧拒删根未重试。阶段末90.175MiB正常复用，公共TODO/采购handoff/data/WB014及其他共享差量保持，未暂存/提交；等待本轮体验确认，后续拖拽→卡片加载→运行中阶段继续排队。

## 2026-09-17 00:06 · Codex

### CODE-042 R023：＋紧邻标签与空白页懒加载文件树（内部验证通过，待独立复核/用户验收，未提交）

- **授权与产品差量**：按`code042-preview-review-R023`及用户两处截图反馈，只修改preview/i18n/CSS：少量标签＋跟随最后项，溢出仍保留列表/＋与原关闭/滚动；选择器移除项目文件/上一级导航与重复根路径，根文件夹名一处、完整路径tooltip，目录原地展开收起与层级缩进。只读取展开目录的一层元信息，文件仍走原槽/fileKey去重。`index.html`无需改动，server与preview_picker后端SHA与R022完全一致。
- **树与资源**：根首次读取一层；兄弟/两层按需展开，收起释放该子树，节点错误就地重试，文件失败的重试刷新原父目录元信息而不自动读正文。搜索/树各自保持页内滚动，清空搜索恢复展开；搜索固定在结果滚动区上方。全页所有空白共享2000目录状态/条目、2MiB保留估算；实际未完成展开调用2个、队列16项，5秒包括排队。超限明确提示，收起/关闭后重试；底层忽略Abort不释放实际并发槽直到settle。字节估算非完整堆上限，沿用R022搜索/后端限额，不全项目预载或接入真实左侧树状态。
- **异步与兼容**：scope/epoch、节点及每次controller身份校验；收起、搜索、切换、关闭、Session/root和pagehide取消并拒绝迟到填槽/展开/抢焦点，树只在原空白页内、v1不变。Keyboard上下/Home/End、左右展开/父子导航及Enter选择，重绘焦点保留、重试后回目录。旧R022后端/链接/越界/只读证据和R019差异证据复用；未重新跑无关后端全集或创建链接，也未修改Session协议。
- **最终专项**：`%TEMP%/code042-blank-picker-XurlOO/result.json` bundle/classic **50项、20图**通过，真实250/390/720侧栏、少量＋紧邻/溢出可用、根名一处、两层/兄弟按需展开、实际键盘搜索/清空恢复600px树滚动、无正文预读、节点失败与文件失败父目录重读、同槽/去重/20槽、A取消→B后A迟到、树关闭/root变更/搜索隔离、Session快速导航负控、中英明暗均通过。2000节点跨两个空白共享且关闭释放重试、长名称先触2MiB、2并发/16队列、队列等待计入5秒均有确定性负控；**容量/队列使用保留权威context的合成metadata，只证明前端预算，不冒称物理文件系统扫描**。代表250浅色、390中文深色及宽树已目视，最终图片供独立复核，不代表用户PASS。
- **必要回归**：`code042-r023-i18n.txt`规定完整i18n集 **525 passed/52子测试**；之后仅树排队截止、独立滚动、文件失败重读父目录微调由最终上述双入口与`code042-r023-final-targeted.txt` **10项预览定向**覆盖。`code042-tab-strip-oNWuiS/result.json` **22项/24图**覆盖1/2/多/20文件与18文件+2空白+1审查、混合MRU、列表焦点、手动滚动/resize/后缀可见；其后树内部变化不改标签布局。最终构建freshness、JS语法、diff检查和后端SHA核对通过；同环境沿用R022 doctor，不机械重复。
- **诊断与修正事实**：R022曾用networkidle等待Session，本轮`rgCiT8/result.json`显示B行已选中且tabs清空时生产SID仍A，证明该等待不足；正常操作现等精确B上下文及恢复，另独立hold导航中快速按预览、释放后断言B无A标签，双入口通过，未用延长等待代替负控。旧替身对取消的B树metadata也忽略Abort且遗留未settle Promise，妨碍新2并发负控；在撤销替身时结清所有held，关键迟到断言先完成，未放宽并发。滚动负控发现搜索聚焦把共享滚动从600改92，产品改为固定搜索/独立结果滚动后通过；并保留失败结果。早期通过目录仅诊断，不计最终候选证据。
- **收口与未完成**：自建隔离H4宿主均退出/端口关闭/root移除，测试root配置恢复；指定Python数据根未创建，以前拒删根未重试。阶段末会话89.638MiB正常复用；保护data、WB014索引、9月14日志、采购handoff及公共TODO。R022独立核对通过不等于用户接受，R023仍待独立复核和两处新交互验收，未暂存/提交；后续仍为拖拽/边缘横滚→卡片缓存/可见自动加载→运行中阶段独立合同。
