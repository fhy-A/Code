# 2026-09-17 开发日志

## 2026-09-17 01:10 · Codex

### CODE-042 R026前置收口：预览/审查/空白文件树已获用户PASS并授权本地提交

- 用户明确“可以了，再加标签页拖动换位”，审批通过`code042-preview-review-R026`确认R018–R025当前阶段验收，并授权按R025冻结清单先做独立本地提交；R025候选SHA `cd5ff2b1bbf2594b588d35e427fedd351e4499478a678f887740018a25664176`及335项独立核对作为事实来源。历史条目中的“未提交/待验收”表示各轮当时状态，以本条后续验收为准。
- 本次归档包括单轮连续审查/紧凑diff、单行标签/溢出列表、页内空白标签与有界文件选择树、加载布局和普通箭头呈现；所有原测试/失败/skip/覆盖边界按原轮证据保留，不冒称本轮重跑。新阶段doctor全通过（`%TEMP%/code042-r026-doctor.txt`），R025公共/私有/构建hash接手一致，90.500MiB正常复用。
- 仅精确暂存R025的25公共文件及本次验收记录，索引中的既存WB014/9月14行保持未暂存，9月14日志/data/采购handoff及公共TODO不纳入；不push/tag/release。拖动换位属于后续单独候选，不混入本次提交，运行中审查仍未验收。

## 2026-09-17 00:59 · Codex

### CODE-042 R025：删除目录加载圆圈（QUICK定向通过，未提交）

- 用户明确不需要短暂加载圆圈，按`code042-preview-review-R025`直接授权删减：preview目录图标始终使用展开/收起箭头，删除spinner及其专用动画CSS；固定16px槽、行高、无障碍busy/加载名称、失败重试、取消/迟到、搜索和选中文件状态均保持，无调度/后端/i18n变化。
- `%TEMP%/code042-picker-loading-ZytyZw/result.json`的`--icon-only`双入口 **4项/6图**通过：持有目录响应时普通箭头、无spinner元素或动画、可访问busy、几何/scroll/focus稳定、完成正常及收起取消后迟到不插子项；无正文读取。构建freshness与diff检查通过，R024其他18项几何/10定向及既有后端/预算证据明确复用，不跑无关全集或重复主观方案确认。
- 隔离宿主退出、端口关闭、root移除通过；租约内仅本差量与事实源，未暂存/提交，拒删根及其他共享差量保持。用户删减要求不等于整个R023交互PASS；下一阶段拖拽/卡片加载/运行中仍未启动。

## 2026-09-17 00:48 · Codex

### CODE-042 R024：目录加载提示移入固定图标槽，消除树上下跳动（内部验证通过，待体验确认，未提交）

- **授权与复现**：用户明确展开文件夹时整体弹动并短暂显示加载文字，依`code042-preview-review-R024`按STANDARD反馈修复。先在R023原产品上运行`%TEMP%/code042-picker-loading-GmCVzE/result.json`的observe模式，双入口根展开的树视口与触发行Y均为**220→264→220**，证实顶部`treeLoading`状态块新增44px再消失；保留坐标与4图，不把预修复observe算PASS。
- **最小产品修订**：仅preview/CSS去掉普通目录加载对顶部状态的占位，使用固定16px目录图标槽和12px加载环，目录行`aria-busy`及既有中英加载文案可访问名称；减少动画时静态显示。搜索/文件打开的顶部状态和取消、每目录错误/重试保持。原树请求、配额、队列、controller/迟到围栏、root/Session/v1及后端不变，未修改其他标签、拖拽或运行中行为。
- **焦点补修**：首轮几何检查通过至Escape分支后，发现取消时搜索框聚焦又被app外层通用INPUT Escape处理blur；只在本选择器已处理Escape后停止冒泡，使其保留原取消与焦点归属。未修改app全局快捷键或取消调度。失败诊断`code042-picker-loading-KLSgeT/result.json`保留，最终不放宽焦点断言。
- **最终定向证据**：`code042-picker-loading-1NOlg7/result.json` bundle/classic **18项/12图**通过，记录标题、搜索、结果视口、触发行rect、scrollTop和焦点；根相同坐标为**220→220→220**。根/250px嵌套/720px并行兄弟、非零滚动、失败重试、加载中收起及迟到、Escape取消、无正文预读、搜索/选中文件状态、中英390深色可访问加载通过。几何断言容差0.75px，下方兄弟随真实子项插入的自然移动不作抖动。代表加载中/完成图已目视；未将内部截图当用户体验PASS。
- **验证与边界**：最终`code042-r024-final.txt`预览定向 **10 passed**，构建freshness、JS语法及diff检查通过。复用原i18n键，未改i18n文件；中英通过实际页面切换核验，不机械重跑525或未变更后端/diff/预算全集。R023其他证据保持并明确复用，物理后端SHA仍保持。所有本轮隔离宿主退出、端口关闭、root移除，Python指定数据根未创建；旧拒删根未重试。阶段末90.175MiB正常复用，公共TODO/采购handoff/data/WB014及其他共享差量保持，未暂存/提交；等待本轮体验确认，后续拖拽→卡片加载→运行中阶段继续排队。

## 2026-09-17 00:06 · Codex

### CODE-042 R023：＋紧邻标签与空白页懒加载文件树（内部验证通过，待独立复核/用户验收，未提交）

- **授权与产品差量**：按`code042-preview-review-R023`及用户两处截图反馈，只修改preview/i18n/CSS：少量标签＋跟随最后项，溢出仍保留列表/＋与原关闭/滚动；选择器移除项目文件/上一级导航与重复根路径，根文件夹名一处、完整路径tooltip，目录原地展开收起与层级缩进。只读取展开目录的一层元信息，文件仍走原槽/fileKey去重。`index.html`无需改动，server与preview_picker后端SHA与R022完全一致。
- **树与资源**：根首次读取一层；兄弟/两层按需展开，收起释放该子树，节点错误就地重试，文件失败的重试刷新原父目录元信息而不自动读正文。搜索/树各自保持页内滚动，清空搜索恢复展开；搜索固定在结果滚动区上方。全页所有空白共享2000目录状态/条目、2MiB保留估算；实际未完成展开调用2个、队列16项，5秒包括排队。超限明确提示，收起/关闭后重试；底层忽略Abort不释放实际并发槽直到settle。字节估算非完整堆上限，沿用R022搜索/后端限额，不全项目预载或接入真实左侧树状态。
- **异步与兼容**：scope/epoch、节点及每次controller身份校验；收起、搜索、切换、关闭、Session/root和pagehide取消并拒绝迟到填槽/展开/抢焦点，树只在原空白页内、v1不变。Keyboard上下/Home/End、左右展开/父子导航及Enter选择，重绘焦点保留、重试后回目录。旧R022后端/链接/越界/只读证据和R019差异证据复用；未重新跑无关后端全集或创建链接，也未修改Session协议。
- **最终专项**：`%TEMP%/code042-blank-picker-XurlOO/result.json` bundle/classic **50项、20图**通过，真实250/390/720侧栏、少量＋紧邻/溢出可用、根名一处、两层/兄弟按需展开、实际键盘搜索/清空恢复600px树滚动、无正文预读、节点失败与文件失败父目录重读、同槽/去重/20槽、A取消→B后A迟到、树关闭/root变更/搜索隔离、Session快速导航负控、中英明暗均通过。2000节点跨两个空白共享且关闭释放重试、长名称先触2MiB、2并发/16队列、队列等待计入5秒均有确定性负控；**容量/队列使用保留权威context的合成metadata，只证明前端预算，不冒称物理文件系统扫描**。代表250浅色、390中文深色及宽树已目视，最终图片供独立复核，不代表用户PASS。
- **必要回归**：`code042-r023-i18n.txt`规定完整i18n集 **525 passed/52子测试**；之后仅树排队截止、独立滚动、文件失败重读父目录微调由最终上述双入口与`code042-r023-final-targeted.txt` **10项预览定向**覆盖。`code042-tab-strip-oNWuiS/result.json` **22项/24图**覆盖1/2/多/20文件与18文件+2空白+1审查、混合MRU、列表焦点、手动滚动/resize/后缀可见；其后树内部变化不改标签布局。最终构建freshness、JS语法、diff检查和后端SHA核对通过；同环境沿用R022 doctor，不机械重复。
- **诊断与修正事实**：R022曾用networkidle等待Session，本轮`rgCiT8/result.json`显示B行已选中且tabs清空时生产SID仍A，证明该等待不足；正常操作现等精确B上下文及恢复，另独立hold导航中快速按预览、释放后断言B无A标签，双入口通过，未用延长等待代替负控。旧替身对取消的B树metadata也忽略Abort且遗留未settle Promise，妨碍新2并发负控；在撤销替身时结清所有held，关键迟到断言先完成，未放宽并发。滚动负控发现搜索聚焦把共享滚动从600改92，产品改为固定搜索/独立结果滚动后通过；并保留失败结果。早期通过目录仅诊断，不计最终候选证据。
- **收口与未完成**：自建隔离H4宿主均退出/端口关闭/root移除，测试root配置恢复；指定Python数据根未创建，以前拒删根未重试。阶段末会话89.638MiB正常复用；保护data、WB014索引、9月14日志、采购handoff及公共TODO。R022独立核对通过不等于用户接受，R023仍待独立复核和两处新交互验收，未暂存/提交；后续仍为拖拽/边缘横滚→卡片缓存/可见自动加载→运行中阶段独立合同。
