# 2026-08-30 开发日志

## 2026-08-30 20:21 · Codex

### 十天前 Code 会话数据清理完成

- 本次按用户确认的滚动十天策略冻结截止时间 `2026-08-20T20:09:13.844497+08:00`，只以 session meta 的 `updatedAt` 判定年龄；旧无时区时间使用现有 canonical 本地时区归一化，不使用文件 mtime 或 createdAt。R001 在零删除全量预检中发现 `c3b44d19d5da407a` 的 Goal 为 `ready_for_acceptance`、`de0df1de52164180` 的 Goal 为 `active`，因此按门禁停止；R002 明确固定保留这两项后才重新开始。
- R002 在首次 archive 前重建出与 R001 完全相同的 `176` 个旧 `source=code` meta 候选，核心文件合计 `14,842,410` bytes；排序 ID 的 SHA-256 为 `6cbdcde12e8f14af0a30642b95acfe32e9f968739b1a367c8ce0603362357baf`（UTF-8、按 ID 排序后以 LF 连接、末尾无 LF）。固定排除两条 Goal 会话后，`174` 个目标全部通过既有 archive eligibility，目标核心文件为 `14,711,218` bytes，排序 ID 摘要为 `df217d01becabada509d4fd27f9032571df864299115c4dea91cfd2be16cf04f`。
- `2026-08-30T20:16:41+08:00` 至 `20:19:40+08:00`，174 个目标按稳定 ID 顺序逐项调用 3011 Code Dev 的事务化 session archive lifecycle；每项仅在 archive 成功、返回本次 `archiveToken` 且 token 与持久 manifest 一致后，才调用 archived permanent delete。每个目标成功并确认 active core 与临时 archive bundle 均不存在后才进入下一项；没有直接活动 DELETE、直接文件删除、重试或绕过 token、锁、索引、回滚与 tombstone。

### 后置验证、保留集合与不可恢复边界

- 全量完成后 active meta 为 `120`，原旧 Code 范围只保留 `21` 个：`19` 个 paused 与上述 `2` 个未结束 Goal；另有 `4` 个旧 codex / claude-code 导入会话、`95` 个近期活动会话全部保留。174 个目标在 active 与 archive 中均为 `0`；active meta 与消息核心文件 `120 / 120` 一一配对，没有缺失或孤立核心文件。
- 既有近期归档继续恰好 `1` 个，排序 ID 摘要保持 `1b3d780824f91a66ef7d4f612880ed55dfb943c1c3af5d0f8346fb66d8fdf875`。3011 的归档列表与磁盘归档集合一致；活动列表投影为 `118`，后置只读审计发现两个被保留的旧 `claude-code` meta（`claude-1ce1657fd8f3cf33`、`claude-94af0ac64eb7c179`）不在现有 session index，因此不在列表中。本轮能证明其 meta 与消息核心仍保留，但无法事后证明该索引缺口是否在清理前已经存在；没有擅自重建索引或把它们纳入删除。
- 永久删除不可恢复；消息、索引、Goal、终态 Agent Run 与 session-owned generated assets 只由既有删除事务级联。本阶段没有单独枚举、读取或哈希 generated-assets 内容，也没有读取或修改 image-route-registry；未修改产品、测试、配置或线上数据，未运行 doctor / H4 / Browser / 模型 / 外网，也未 push、tag 或 release。

## 2026-08-30 19:49 · Codex

### CODE-043 · 人工介入界面扁平化完成

- 第二阶段本批次收敛授权队列、请求用户问卷与问卷回答摘要。授权队列改为与消息流一致的透明扁平表面：只有存在非主任务来源时才显示分组，纯主任务队列不再保留空分组层；同组多项继续提供“全选”，单项选择、批量批准 / 拒绝、风险说明、来源与既有授权语义不变。
- 请求用户面板将阶段标题、进度与原因并入同一层级，问答区使用进度 / 原因与问题内容两栏布局；多选标题只显示模型原始 prompt，不再追加可见“（多选）”。“其他”输入初始一行并按内容自动增长或回缩，最多三行后内部滚动，保留 `2000` 字计数、IME、Enter 提交、Shift+Enter 换行、焦点恢复及原回答序列化语义。
- 问卷回答摘要只依据答案判定长度：答案总字符数超过 `240`，或任一答案含至少三条显式文本行时，默认折叠为最多三个视觉行；短答案完整显示且无多余控件。展开 / 收起按钮带本地化文案、箭头、ARIA 与键盘语义，展开状态按 `requestId` 仅保存在当前页面实例中，同页重绘保持、刷新后恢复默认折叠；原消息的 role、content、meta、requestId、标题与答案均未改写。

### 自动证据、用户验收与剩余边界

- R005 新增的自动增长、多选标题与长摘要披露三组合同先稳定失败，修正后 CODE-043 人工介入直接组为 `17 passed`；完整 `tests/test_frontend_modules.py` 为 `345 passed`。`npm run check:frontend` 的 build / freshness 与 bundle syntax、相关 JavaScript 语法、Python AST 及 diff 检查均通过。
- 用户对 R001-R005 当前草稿明确 `PASS`。开发任务没有运行 Browser / H4 / selfcheck / doctor、模型或网络，也没有操作 3010 / 3011；收口阶段复用上述自动证据和用户验收，没有机械重跑测试、构建或 Browser。
- 本批次只修改前端投影、局部样式、i18n 与直接测试，没有改变授权、问卷结果、Session / AgentRun、server / API、持久化、权限或协议。未读取、哈希、修改或暂存受保护用户 data，未覆盖无关 handoff，未 push、tag 或 release；CODE-043 保持开放，第二阶段只剩普通消息最终审计，第三 / 第四阶段仍未启动。

## 2026-08-30 17:50 · Codex

### CODE-043 · Diff 编辑建议扁平化完成

- 第二阶段本批次只调整 PC Web 的 Diff 编辑建议：外壳与标题行改为透明、无边框和阴影，Diff / 新文件代码区成为唯一持久视觉表面，保留代码背景、适度圆角以及新增 / 删除 / hunk 的既有语义色。文件名为 `14px`，状态、统计和控制为 `12px`，Diff 代码为 `14px`；长文件名继续安全省略且右侧控制保持可见。
- pending / applied / rejected 状态只保留既有语义文字色，不再使用 pill 背景或边框；展开控制收敛为无底无框文字与小箭头，hover / focus-visible 仍清晰。既有 Diff DOM、复制、展开、ARIA、键盘、状态、持久化与恢复语义未变；授权项的临时定位焦点环继续保留，应用或拒绝不会给整张建议着色。

### 自动证据、用户验收与剩余边界

- 直接 CSS 合同先以缺失新阶段规则稳定得到 `6 failed`，修正后为 `6 passed`；完整 `tests/test_frontend_modules.py` 为 `328 passed`。`npm run check:frontend` 的 build / freshness、Python AST 与 diff 检查均通过。开发任务没有运行 Browser / H4，也没有操作 3010 / 3011。
- 用户在现有 3011 Browser 手动刷新后明确 `PASS`，验收只覆盖本批次 Diff 编辑建议的 PC Web 视觉结果，不外推为授权队列、普通消息、窄屏或其他 CODE-043 表面已验收。收口阶段复用上述自动证据与人工验收，没有重跑 doctor、测试、构建、Browser、H4 或 selfcheck。
- 本批次仅修改 `styles.css` 与直接前端测试，作用域限定于 `.edit-suggestion`；没有修改 JavaScript / DOM、server / API / 数据 / 协议 / 持久化 / 依赖，也没有处理或归档 workbar 429、API Key 名称、生图连接或路由。未读取、哈希、修改或暂存受保护用户 data，未覆盖无关 handoff，未 push、tag 或 release；CODE-043 保持开放，后续仍包括授权队列扁平化、普通消息最终审计，以及既有第三 / 第四阶段，本阶段均未启动。

## 2026-08-30 17:13 · Codex

### CODE-043 · API Key 连接名称语义优化完成

- API Key 的可选名称明确为纯显示标签，设置字段更新为“连接名称（可选）” / “Connection name (optional)”。Key-only 输入、旧无名称配置、重名配置、批量导入提示、保存格式和 parser 均保持原合同，不迁移、不重写也不强制补值；生图连接仍使用独立配置和路由。
- 手动连接刷新 payload、公开 route state 与持久 Route Registry 对空名称统一保留 `""`；“未命名连接” / “Unnamed connection”只在前端目录渲染时按当前语言投影。重名或多个未命名连接继续追加稳定 `connectionId` 后六位；语言切换只重绘标签，不调用路由刷新，也不改变 catalogRevision、routeRef、当前模型选择或可用性。
- `routeRef` 仍只由 `connectionId + modelId` 生成。显式重命名继续触发现有目录刷新并可递增 catalogRevision，但 routeRef 保持稳定；从旧持久目录中的本地化占位首次收敛为空标签时，同一路由、当前选择及 credentialsAvailable 均保持。删除确认的未命名回退改用 i18n，显示参数进入确认 HTML 前完成转义。

### 确定性验证与边界

- 新合同先以 `2 failed` 稳定复现 payload 本地化占位和缺失字段 / 删除 / 语言边界，修正后转为 `2 passed`；包含 parser、旧配置、显式重命名、目录分组、空标签、选择恢复、刷新所有权与即时撤销的定向矩阵为 `9 passed`。最终完整 `tests/test_frontend_modules.py` 为 `322 passed`，既有 Route Registry 身份测试为 `11 passed + 5 subtests`，frontend build / freshness、相关 JavaScript / Python 语法与 diff 检查均通过。
- 本阶段按批准边界没有运行 Browser、H4，也没有 reload 或操作 3010 / 3011；收口前用户已报告 workbar 429 恢复，但本阶段未对该外部状态进行复验，也不把恢复归因于本提交。未读取浏览器存储、真实 Key 或用户 data，未调用 workbar、模型、生图或外网；未修改 server、schema、ModelRouteRegistry、image route、认证、限流或 workbar 配置，未处理 429 及逐连接检测状态，未覆盖无关 handoff，也未 push、tag 或 release。

## 2026-08-30 16:52 · Codex

### CODE-043 · 三层折叠控件交互统一完成

- 第二阶段首个局部差量统一了执行轨迹、阶段与工具项三层折叠控件：三层标题保持 `fit-content`、左对齐与至少 `32px` 有效点击高度，标题右侧空白不再触发展开；箭头统一为收起向右、展开向下，最外层“用时”箭头进一步收敛为与中 / 内层一致的 `6×6px`、`1.4px` 线宽和约 `7px` 文字间距。既有 DOM、ARIA、键盘、焦点、展开状态、重绘恢复和事件所有权未变，长目标路径继续在名称、状态与箭头可见的前提下省略。

### 自动证据、熔断边界与人工验收

- 直接合同先锁定三层紧凑范围、方向、顺序、路径优先级与语义边界；阶段完整 `tests/test_frontend_modules.py` 为 `319 passed`，frontend build / freshness 与语法 / diff 通过。最外层箭头微调另以缺失新规格的稳定 fail-first 起步，修正后 CODE-043 直接组为 `7 passed`；该轮只运行直接静态与 frontend build / freshness，没有重跑任何浏览器 selfcheck。
- 原 `code043-compact-disclosures-selfcheck.cjs` 首次因启动渲染移除过早注入的夹具而失败，完成时序修正后第二次又因在既有 `140ms` 过渡起点读取箭头而失败；按两轮熔断撤回且未第三次运行。重新设计的 `code043-compact-disclosures-final-state-selfcheck.cjs` 仅在隔离页面为四个目标箭头关闭 transition，冻结终态而不改变产品 CSS；它只运行一次并通过 bundle / classic，覆盖三层空白不触发、鼠标 / 键盘展开、ARIA / focus、方向、长路径省略、深浅主题、零横向溢出、零产品写与完整进程 / 端口 / 临时根清理。
- 用户在现有 3011 Browser 复核最外层 `6px` 箭头后明确 `PASS`，确认其与 `12px`“用时”文字及中 / 内层箭头视觉重量一致；三层紧凑范围、收起 / 展开方向和既有交互没有可见回归。收口阶段复用上述自动证据与人工验收，没有重跑 H4、任何 selfcheck、Browser 或无关全量回归。
- 本阶段没有修改 server / API / 数据 / 协议 / 持久化 / 依赖，也没有处理 Key 名称、限流或其他新发现；未读取、哈希、修改或暂存受保护用户 data，未覆盖无关 handoff，未 push、tag、release，也未启动 CODE-043 后续会话内容卡片、辅助表面或全面审计。

## 2026-08-30 15:30 · Codex

### CODE-043 · 主界面字号层级第一批完成

- 在高频主界面建立作用域明确的 `12 / 14 / 16 / 18 / 20px` 语义字号：Caption 辅助信息为 `12px`，Interface 常规界面与密集内容为 `14px`，Content 主要阅读与输入为 `16px`，Section 区块标题为 `18px`，Page 页面标题为 `20px`。普通助手正文、模型名、普通用户文本内部 bubble 与主输入框统一为 `16px`；commentary、思考 / 工具组主要内容、composer 模型 / 思考控制、顶部栏、侧栏、项目 / 会话与文件树常规文字统一为 `14px`；时间、Token、状态、路径等元信息统一为 `12px`。Markdown `H1 / H2 / H3-H6` 分别映射 `20 / 18 / 16px`，表格与代码主体为 `14px`。
- 映射仅落在显式主界面选择器；设置、搜索 / 归档、其他弹窗、通知、onboarding 与预览正文没有被全局替换。纯 SVG / icon-only 控件继续使用既有图标尺寸，`#attachFile` 的遗留 `18px` 只作为图标尺寸例外，不被误作界面文字。用户气泡只命中普通 `.user-message-text > .bubble`，图片批次、后台 / 队列消息与持久执行轨迹均显式排除；既有 padding、宽度、边框、底色、阴影、圆角、行高和交互没有变化。
- 同一提交保留富文本 admonition 首段多余空行修复：只在解析后的 admonition body 首个 `<p>` 开头移除解析器引入的 leading `<br>`，不改普通 blockquote、嵌套块或段内换行。

### 自动证据、熔断边界与人工验收

- 字号体系初始直接合同以 `4 failed` 稳定锁定五级 token 与缺失映射，修正后转绿；用户气泡真实选择器另以 `2 failed` 复现 wrapper 命中但内部 bubble 仍为 `14.5px`，修正后 `2 passed`。最终完整 `tests/test_frontend_modules.py` 为 `314 passed`，frontend build / freshness、相关 JavaScript / Python 语法与 `git diff --check` 均通过。
- 新建的 model-free bundle / classic 字号 selfcheck 首次因 viewport 重投影清空测试夹具而失败；完成时序审计并把夹具安装与 computed-style 读取合并到同一浏览器原子段后，第二次仍因测试把 `#attachFile` 的 icon-only `18px` 误判为文字 `14px` 而失败。按两轮熔断未进行第三次运行，失败 selfcheck 及其专属读取断言在收口时删除；本日志不把该场景记为通过，也没有为通过测试而改变已验收图标尺寸。
- 用户在现有 3011 Browser 明确 `PASS`：普通用户消息、普通助手正文、助手模型名与主输入框 computed font-size 均为 `16px`，composer 模型控制与会话行文字为 `14px`，response 辅助信息为 `12px`；其余已验收字号、布局和图标表现保持稳定。收口阶段复用上述自动证据与人工验收，没有重跑 Browser、H4、熔断 selfcheck 或无关全量回归。
- 本阶段未修改 JavaScript DOM / 交互、server / API / 数据 / 协议 / 持久化 / 依赖，没有读取、哈希、修改或暂存受保护用户 data，也未覆盖活动 handoff；未 push、tag、release，未启动字号第二批或 CODE-043 其他阶段。

## 2026-08-30 12:20 · Codex

### CODE-043 · Code Web 视觉系统与组件尺度优化第一阶段完成

- 第一阶段以“紧凑但更清晰”为方向，只在 PC Web 高频外壳接入局部 typography、line-height、icon、spacing、radius、control、elevation 与交互状态尺度，覆盖侧栏、项目 / 会话列表、文件区、顶部栏、空页面和 composer；11 个关键 shell 操作改用现有 SVG registry，并保持原 id、ARIA、键盘、focus-visible、拖拽、折叠、滚动和业务所有权。独立 phase-one readiness 先于 bundle frontend readiness，未激活包含设置、预览正文和通用弹窗候选的宽泛图标升级路径。
- 预览分栏改为按实时 `.workbench` 宽度统一夹紧：会话工作区至少保留 `520px`，预览至少 `250px`，默认 `420px`；打开、恢复、拖拽和窗口 resize 复用同一边界，陈旧超大 `code-preview-width` 会被安全夹紧并写回，CSS 同时以 `520px` 聊天列和受容器余量约束的预览列提供硬保护。欢迎标语更新为“把想法变成结果” / “Turn ideas into results”，composer 提示更新为“输入想法，或使用/命令” / “Enter an idea, or use /commands”，启动 fallback 与运行时中英文投影一致。

### 自动证据、熔断边界与人工验收

- 最终直接组为 `15 passed`，完整 `tests/test_frontend_modules.py` 为 `307 passed`；frontend build / freshness、相关 JavaScript / Python 语法和 diff 检查均通过。R004 的独立 model-free bundle / classic selfcheck 曾在精确拦截既有启动初始化请求后通过，证明每入口 readiness、11 个 SVG、桌面尺度、深浅主题、焦点、零实际产品写和完整资源清理；这份通过证据早于后续范围校正。
- 原 `exerciseCode043PhaseOneVisualShell` H4 同场景连续两轮失败后已撤回，未执行第三轮；随后为 390px 增加的窄屏 selfcheck 也按熔断停止，没有继续第三跑。用户最终决定第一阶段聚焦 PC Web，并将 390px / 手机窄屏移至第四阶段；精简后的 PC-only selfcheck 与原 H4 均未再运行，最终自动完成线只采用上述直接回归、构建 / freshness、语法和静态边界，不把未运行场景记为通过。
- 用户在现有 3011 Browser 明确 `PASS`：中文深色恢复态显示“把想法变成结果”和“输入想法，或使用/命令”，英文与浅色也已复核。左侧栏和右侧预览同时展开、预览接近上限时，`workbench≈1369px`、`preview≈843px`、`chat≈526px`、`composer≈445px`，可见控件 bbox 无相交且 document / body 零横向溢出；关闭预览后 `chat≈1369px`、`composer=760px`、`welcome=680px`，布局正常恢复。
- 本阶段未修改 server / API / 数据 / 协议 / 依赖，没有读取、哈希、修改或暂存受保护用户 data，也未覆盖活动 handoff；未 push、tag、release，未启动 CODE-043 第二阶段或其他事项。CODE-043 保留稳定 ID、P1 / STANDARD 与四阶段路线，下一步仅在另行批准后规划会话内容视觉尺度，窄屏继续留在第四阶段。

## 2026-08-30 09:15 · Codex

### CODE-035 · 多分组 API Key 的模型路由与分组一致性完成映射

- `CODE-035` 的未完成边界已由 `CODE-046` 连接中心 Route Registry 及提交 `6655055` 完整覆盖：服务端以稳定非秘密 `connectionId + modelId` 生成不透明 `routeRef`，每个 Workbar `platformTokenId` 形成独立 `wc1_` 连接；同名模型存在于多个连接时必须选择精确 route，公开目录、Session 和新 AgentRun 只保存非秘密路由身份，用户选择可跨 reload 持久化。
- 禁用目标连接不得跨连接或未经授权的价格分组回退；既有 bundle / direct classic H4 已覆盖重复模型精确绑定、刷新 / 重载和禁用连接零跨组回退。2026-08-28 的后续 Key 删除 / 禁用即时撤销证据进一步确认：目标 route 会从目录与 selection 消失，只有权威刷新后同模型恰好剩余唯一有效连接时才可安全迁移；空目录、刷新失败、歧义、禁用或无凭据均保持无选择。
- 现存 `modelKeyMap` 只属于 `routing_v1` 回退兼容，不是 `routing_v2` 的权威路由；本次文档收口无需删除该兼容路径，也不迁移或改写历史记录。

### 完成依据与边界

- 完成判断复用既有自动测试、bundle / direct classic H4 和用户已通过的产品验收：`CODE-046` 的 H4 由 `10 passed / 4 failed` 加修正后定向 `4 passed` 组合覆盖完整 14 场景，routing_v2 / 终态漂移 `151/151`、Route Registry / server `250/250`、前端非构建 `261/261`；后续路由即时撤销另有定向 `34 passed + 9 subtests`、相关扩大回归 `484 passed + 86 subtests` 和双入口 H4 `2 passed`。
- 本阶段仅删除已完成 TODO 并记录映射，没有调用真实 Key、真实模型或外网，没有运行 doctor、测试、构建、Browser、模型、H4 或回归；未修改产品、测试、依赖、协议、活动 handoff、`WB-003` 或用户 data，未 push、tag 或 release，也未启动路由开发或下一事项。
