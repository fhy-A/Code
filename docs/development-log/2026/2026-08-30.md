# 2026-08-30 开发日志

## 2026-08-30 09:15 · Codex

### CODE-035 · 多分组 API Key 的模型路由与分组一致性完成映射

- `CODE-035` 的未完成边界已由 `CODE-046` 连接中心 Route Registry 及提交 `6655055` 完整覆盖：服务端以稳定非秘密 `connectionId + modelId` 生成不透明 `routeRef`，每个 Workbar `platformTokenId` 形成独立 `wc1_` 连接；同名模型存在于多个连接时必须选择精确 route，公开目录、Session 和新 AgentRun 只保存非秘密路由身份，用户选择可跨 reload 持久化。
- 禁用目标连接不得跨连接或未经授权的价格分组回退；既有 bundle / direct classic H4 已覆盖重复模型精确绑定、刷新 / 重载和禁用连接零跨组回退。2026-08-28 的后续 Key 删除 / 禁用即时撤销证据进一步确认：目标 route 会从目录与 selection 消失，只有权威刷新后同模型恰好剩余唯一有效连接时才可安全迁移；空目录、刷新失败、歧义、禁用或无凭据均保持无选择。
- 现存 `modelKeyMap` 只属于 `routing_v1` 回退兼容，不是 `routing_v2` 的权威路由；本次文档收口无需删除该兼容路径，也不迁移或改写历史记录。

### 完成依据与边界

- 完成判断复用既有自动测试、bundle / direct classic H4 和用户已通过的产品验收：`CODE-046` 的 H4 由 `10 passed / 4 failed` 加修正后定向 `4 passed` 组合覆盖完整 14 场景，routing_v2 / 终态漂移 `151/151`、Route Registry / server `250/250`、前端非构建 `261/261`；后续路由即时撤销另有定向 `34 passed + 9 subtests`、相关扩大回归 `484 passed + 86 subtests` 和双入口 H4 `2 passed`。
- 本阶段仅删除已完成 TODO 并记录映射，没有调用真实 Key、真实模型或外网，没有运行 doctor、测试、构建、Browser、模型、H4 或回归；未修改产品、测试、依赖、协议、活动 handoff、`WB-003` 或用户 data，未 push、tag 或 release，也未启动路由开发或下一事项。
