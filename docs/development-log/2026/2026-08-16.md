# 2026-08-16 开发日志

## 2026-08-16 10:59 · Codex

### 发布流程两阶段凭证与断点续发完成

#### 流程与兼容

- `release.py` 在保留 `python release.py X.Y.Z --yes` 一次性正式入口、原 release 门禁集合/顺序/超时/fail-fast 和 `--dry-run` 外部语义的基础上，新增 `python release.py X.Y.Z --prepare`、`--publish-prepared`、`--resume` 三条版本优先显式路径；动作在前的三条子命令形式保留为等价兼容别名。H4 继续只属于 runtime profile，没有进入正式 release 定义或凭证。
- `prepare` 在首次发布元数据写入前只读核对 gh 登录、仓库身份、origin/master 祖先关系及目标 tag/Release 冲突；随后按共享正式门禁完整验证、构建并严格核对 EXE PE 元数据/SHA-256，成功只保留本地候选和 Git 内部凭证，不 commit、tag、push 或创建 Release。失败会恢复本次发布白名单元数据并使凭证无效。
- 非 dry-run 的 `--skip-tests` 不再接受“刚跑过”的人工信任，兼容入口会转入 prepared 凭证校验；没有当前有效凭证即 fail-closed。原 `--dry-run --skip-tests` 仍保持 dry-run 的只读子集语义。

#### 凭证与恢复

- 新增 `release_state.py`，将 `code-release-prepared/v1` 凭证原子写入 `.git/code-release/vX.Y.Z.json`。凭证以稳定 JSON/SHA-256 封印，绑定版本/tag、基线 HEAD、master、origin HEAD、index tree、发布白名单外 tracked 差量摘要、五个发布文件的大小/原始 SHA-256/Git blob、共享验证定义指纹、发布说明、EXE 大小/SHA-256/PE 元数据及必要的 Git/gh/Python/平台/仓库摘要；不记录 token、业务数据或不必要的绝对路径，中文工作区与 `.git` 文件式 worktree 路径可安全解析。
- `publish-prepared` 仅在所有绑定证据仍完全一致时开始发布事务；`resume` 只接受同一凭证已进入 publishing/published 的状态。发布提交、master、本地/远端 tag、Release 标题/正文和资产逐项以真实状态审计：一致则跳过、缺失才补做、不同立即停止；禁止 force-push、删除/重建 tag 或 Release、`--clobber` 覆盖资产，Release 已建但资产缺失时可安全补齐。
- 每个外部步骤后原子刷新进度；提交、推送、Release 创建或资产上传已经成功但响应/进度写入丢失时，下一次 resume 先核对真实对象再继续。发布提交使用精确 changed whitelist 和 Git blob 校验，既有 cached/tracked 差量、任务专用交接与 Goal sidecar 均不读取、不恢复、不暂存、不提交。

#### 验证与边界

- 发布/验证定向测试为 `50 passed, 9 subtests passed`，覆盖 CLI 互斥与 legacy 路由、凭证 schema/确定性摘要/损坏/篡改、定义/环境/EXE 漂移失效、远端预检顺序、prepare 成功证据与失败原子回滚、隔离临时仓库 + bare origin + 状态化假 gh 的 commit/tag/master/Release/资产矩阵、资产首次上传失败后的 resume、重复 resume 幂等及分支/tag/Release/资产冲突 fail-closed。
- 真实 `python verify.py release` 通过：`1304 passed, 765 subtests passed, 3 warnings`；Harness replay 为 `17 fixtures / 124 events / 25 checkpoints / 25 recoveries / 4 explicit recoveries`，哈希 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；frontend freshness/bundle syntax、diff 和原 JS/Python 语法门禁全部通过，H4 明确跳过。
- 真实 `python release.py 0.6.4 --dry-run --no-proxy` 退出 0，运行前后 VERSION、HEAD、cached 与完整 status 摘要一致；既有无效 `GH_TOKEN` 只触发原 dry-run gh 登录诊断，没有发布写操作。另行 `py_compile` 覆盖 `release.py`、`release_state.py`、`verification.py` 和新测试。
- 本阶段没有执行真实 EXE 发布候选、push、tag、GitHub Release、线上配置或 Goal 测试，也没有修改 TODO 或启动 `CODE-033`。

## 2026-08-16 08:50 · Codex

### CODE-019 · Goal v2 持久执行闭环完成

#### 产品与事实源

- 用户最终选择方案 B：Goal v2 使用独立的 `data/goals-v2/<sessionId>.jsonl` 作为当前事实源，不要求兼容旧 Goal v1 或过渡期 `data/goal-workflows/`。旧目录和用户实机 sidecar 原地保留，本阶段不读取、不迁移、不改写也不清理，且全部排除在提交之外。
- Goal v2 采用 Session 隔离的 append-only 版本化事件、严格 reducer、单调 revision、CAS、稳定幂等键、跨进程锁、损坏 fail-closed 与 clear tombstone。`goal_created` 可先记录无步骤目标，并由服务端绑定不可伪造的来源消息、顶层 Run、权限档和 `explicit` / `autonomous` 来源；步骤公开状态始终只有 `pending`、`in_progress`、`completed`。
- 显式 `/goal` 先落目标事实，再启动同一个普通前台 AgentRun；复杂、多步骤、可能跨 Run 的普通任务也可由模型通过受控内部 Goal 操作自主创建。Child、detached、parallel、后台或跨 Session Run 不能创建、接管或推进顶层 Goal，Goal 操作不提升文件、命令、授权、模型轮次或其他副作用权限。
- 普通前台 AgentRun 可通过窄操作设置/修订 3～8 步计划、开始步骤、提交与验收条件逐项对应的有界证据、暂停/恢复及提出 `waiting_user` / `blocked` / `failed` 门禁。最后一步的有效完成在同一 CAS/幂等链中直接写入 `goal_completed`；用户实操验收属于尚未完成步骤的门禁，不再为每个 Goal 强制增加通用“待验收”终态。旧 `ready_for_acceptance` 事件保持可读，新的正常流程不再写入。
- 顶层 Goal Run 在约 40 轮进行服务端软交接，50 轮保留单 Run 硬熔断。后继 Run 复用同一 Session、Goal、来源、模型连接和权限，使用有界公开 checkpoint 继续；稳定身份、准入顺序、受保护副作用指纹与连续无进展门禁防止刷新、多标签、重连或重启重复创建后继 Run、重放成功副作用或无限空转。公开助手过程、工具结果和逐 Run 审计继续保留，私有 reasoning 不投影。

#### 前端与交互

- Goal 的唯一持续投影位于 composer 上方：紧凑条显示目标摘要、生命周期/门禁、当前步骤的 1-based 序号与总步数；悬停、聚焦、点击或触摸展开的详情仅显示步骤序号、三态状态和描述，外部点击、焦点离开或 Escape 收起。刷新、切 Session、桌面/移动端及 bundle/direct classic 保持一致。
- 只有显式 `/goal` 的可信来源用户消息显示 Goal 标记；只有显式 Goal 真正完成时，完成它的最终助手消息显示“已在 xm xxs 内达成目标”。自主 Goal 不显示这两类历史标记。完成后活动条隐藏，历史可信标记保留。
- 内部 `goal_*` 操作不进入工具组、工具计数、标题、高光或消息正文；普通工具和模型明确输出的公开执行过程仍实时流入本轮“处理中/已处理”折叠组。工具以 `(agentRunId, toolCallId)` 跨 steer/取消闭合真实 `succeeded` / `failed` / `cancelled` 终态，终态不残留运行高光。
- 同一顶层用户消息触发的多个续接 AgentRun 只在最终可见助手消息显示一份去重汇总用量；全局用量与后端逐 Run 审计不变。助手 footer 从左依次排列用量、显式 Goal 完成标记，悬停/键盘聚焦/触屏时再显示复制和消息时刻。
- Goal 工具轮的公开文字只承担进展、证据发现和阶段性小结；最后一步完成后的既有无工具终态轮输出唯一、完整、面向用户的最终总结，逐分片流式呈现，不以“见上方总结”代替，也不增加额外模型轮次。

#### 验证与验收

- 最终职责收口定向测试为 `33 passed`；相关 Goal、前端、Session 与 AgentRun 回归为 `400 passed + 66 subtests`；统一 UI 为 `231 passed`。
- Harness replay 保持 `17 fixtures / 124 events / 25 checkpoints / 25 recoveries / 4 explicit recoveries`；bundle/direct classic 核心 H4 `2 passed`，另有显式/自主、跨 Run、用户门禁等 Goal 真实路径 `7 passed`。前端 build/freshness、Node/Python 语法和 diff 门禁通过。
- 多轮实机验收覆盖显式与自主 Goal、跨 AgentRun 续接、当前步骤进度、详情交互、外部点击收起、来源与完成标记、单轮汇总用量、公开过程折叠、工具终态以及最终总结单一出口。用户最终确认 Code Dev 与 direct classic 均 PASS。
- 本次事实收口复用上述冻结证据，不再改动已验收产品/测试文件，也不机械重跑完整门禁。真实 `data/goals/`、`data/goal-workflows/`、`data/goals-v2/`、旧 spec 删除和并行研究/指导文档均未进入提交。

#### 状态

- `CODE-019` 当前完成定义已经覆盖，事项从 canonical TODO 移除；Code 最后已分配编号游标仍为 `CODE-032`，不得回退或复用。
- Goal v2 尚未发布。本地完成提交审计后，v0.6.3 将按用户单独授权进入新的发布冻结与正式门禁；本阶段不修改版本元数据、不 push、不 tag、不创建 Release，也不自动启动下一 TODO。
