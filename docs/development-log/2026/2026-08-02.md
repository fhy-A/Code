# 2026-08-02

## 2026-08-02 21:35 · Codex / 用户测试

### 运行中刷新后的状态栏首帧恢复

- 用户在 H1-3 开发实例测试题 2 中确认任务、事件与工具投影均能正常恢复，但刷新后“已处理 + 计时”会短暂消失，直到下一次工具状态变化才出现。定位为会话消息先渲染、内存 `_sessionRuns` 后接管的显示时序空档，不是 v1 事件丢失或游标异常。
- 会话切换与刷新现在会对持久化状态为 `running / waiting-network / resuming`、由服务端 Agent 持有且仍有 `agentRunId` 的任务立即恢复纯显示态：状态栏在首帧出现，并恢复模型轮、事件游标和“已处理”判断。已完成任务、旧浏览器 Agent 和缺少耐久 Run 的记录不会被误判为活动任务。
- 恢复接管前，计时冻结在最后一次持久化的 `elapsedMs`，不把页面初始化、账号同步或模型列表刷新耗时计入任务；真实恢复接管后复用现有逻辑继续累计。该阶段不启动额外模型、不生成虚假事件、不修改 AgentRun/会话 JSONL，也不改变 H1-3 服务端灰度开关。
- 自动验证通过：前端模块为 `157 passed`，刷新与稳定性定向为 `39 passed, 7 subtests passed`，完整回归为 `955 passed, 427 subtests passed in 70.61s`；`npm run check:frontend`、JavaScript 语法和 `git diff --check` 均通过。开发实例刷新人工复测仍保留为下一步验收。

**涉及文件**：`app.js`、`tests/test_frontend_modules.py`、`docs/harness/h1-3-versioned-event-grey-rollout.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 20:51 · Codex

### Harness H1-3 版本化事件灰度写入与前端兼容

- 在 H1-2 影子观察通过后，将普通耐久事件和终态事件统一收口到一个服务端构建边界；开发实例默认给新事件写入 `protocolVersion: 1`，正式实例仍默认写入无版本旧信封。`CODE_AGENT_EVENT_PROTOCOL_V1` 支持显式开关和按实例安全回退，无法识别的值回到实例默认值。
- v1 事件时间按真实时区换算为严格 UTC `Z`，不直接给本地时间拼接 `Z`；旧模式时间、全局 `now_iso()` 及 AgentRun 元数据时间表达保持不变。旧 AgentRun 恢复后可继续追加 v1 事件，不重写历史文件；关闭开关后立即恢复 `{seq, type, data, createdAt}` 四字段写入。
- 前端 Agent 运行时增加单一事件适配边界：无版本旧事件、v1 和未来版本统一归一到当前信封；未来字段与未知事件不会令现有投影崩溃，合法序号但无法投影的畸形事件会跳过并推进游标，避免轮询卡死。适配诊断不写回 AgentRun、会话 JSONL 或浏览器持久状态。
- 本阶段未改变 AgentRun `version: 3`、会话 JSONL、工具协议、模型请求或现有 UI 投影；H0 无版本轨迹继续作为旧协议基线，默认 bundle 与经典回退均通过。服务端协议定向结果为 `96 passed, 179 subtests passed`，前端与 H0 定向结果为 `169 passed, 18 subtests passed`；完整回归为 `954 passed, 427 subtests passed in 68.58s`，`npm run check:frontend`、Python/JavaScript 语法与 `git diff --check` 均通过。
- 下一步先在开发实例执行真实多工具任务、刷新恢复和旧 Run 续接人工验收，并观察 H1-2 影子诊断；确认正常后再单独决定正式实例是否提升 v1 默认值及 H1 是否收口，本阶段不进入 H2 reducer 或 UI 投影重构。

**涉及文件**：`agent-runtime.js`、`server.py`、`tests/test_agent_runtime.py`、`tests/test_frontend_modules.py`、`docs/harness/h1-1-agent-event-contract.md`、`docs/harness/h1-2-server-shadow-validation.md`、`docs/harness/h1-3-versioned-event-grey-rollout.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 20:23 · Codex

### Harness H1-2 服务端事件协议影子验证

- 将 H1-1 纯事件契约接入 `server.py` 的普通耐久事件和终态事件产生边界，新增只存在于 AgentRun 内存中的兼容模式影子观察器；开发实例默认开启、正式实例默认关闭，可用 `CODE_AGENT_PROTOCOL_SHADOW` 显式控制，无法识别的值回到实例默认值，不会因拼写错误意外开启正式实例。
- 影子入口观察 v1 兼容归一、事件序号和 Run 状态迁移，任何观察异常均 fail-open，不改变事件追加、持久化和任务终态。恢复旧 AgentRun 时从已有最大序号和恢复后的实际状态继续，不回放或改写旧文件；原事件信封仍为 `{seq, type, data, createdAt}`，未增加 `protocolVersion`。
- 诊断只在内存保留代码、级别、来源、已知事件类型和序号，拒绝保存错误文本、路径或载荷；每个 Run 最多保留 64 个诊断代码/样本和最近 256 个事件指纹。影子状态不进入 `_agent_run_record()`、AgentRun 磁盘 JSON、HTTP 快照、会话 JSONL、工具协议或前端。
- 自动验证覆盖开关默认值与异常值回退、普通/终态事件、完整假上游终态链、15 条 H0 脱敏轨迹共 106 个事件、服务重启游标续接、故障注入非阻断、脱敏与内存上限，以及公开/持久结构零新增字段；H1-1/H1-2 与 Agent 运行时定向结果为 `92 passed, 171 subtests passed`，完整回归为 `949 passed, 419 subtests passed in 67.55s`；前端 bundle、构建新鲜度、JavaScript 语法、经典回退、Python 语法和 `git diff --check` 均通过。
- 下一小阶段 H1-3 保持独立：先核对影子观察结果，再决定是否给新写入显式增加 `protocolVersion: 1` 并增加前端版本/未知事件兼容；本阶段未进入 reducer、UI 投影或 H2。

**涉及文件**：`agent_protocol.py`、`server.py`、`tests/test_agent_protocol.py`、`tests/test_agent_runtime.py`、`docs/harness/h1-2-server-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 20:05 · Codex

### Harness H1-1 Agent 事件契约纯函数基线

- 在不接入生产运行时的前提下新增纯函数 `agent_protocol.py`，将 H0 冻结的 22 类耐久事件固化为版本 1 契约，定义规范信封、允许载荷、最低必需字段、v0 无版本事件兼容归一、未来版本和未知字段策略；当前 `server.py` 未导入该模块，持久事件、HTTP 快照和前端投影均保持原行为。
- 未知事件类型会保留原类型与载荷并产生诊断，信封未知字段被忽略、载荷未知字段被保留；兼容模式不阻断旧事件，严格测试模式会拒绝缺失必需字段、未来未声明版本、非法迁移和序号间隙。凭据字段、Bearer 与 `sk-` 形态文本在所有模式下递归拒绝，`authorizationId` 等业务 ID 不会因子串误判。
- 定义单 AgentRun 事件序号与内容指纹检查：相同序号和相同内容重复投递幂等，不同内容复用序号始终失败；定义 Run、模型轮和工具执行三组迁移表，`completed/failed/cancelled` 及模型/工具终态不可回流。该模块不读取文件、网络、配置、会话或时钟，不生成消息、不改变状态、不执行工具。
- 自动验证通过：H1-1 为 `9 passed, 135 subtests passed`，H0/H1-1 联合为 `22 passed, 153 subtests passed`，旧 AgentRun/JSONL 定向兼容为 `3 passed, 3 subtests passed`；完整回归为 `936 passed, 396 subtests passed in 69.77s`；前端 bundle、构建新鲜度、JavaScript 语法、经典回退、Python 语法和 `git diff --check` 均通过。
- 下一小阶段 H1-2 只考虑可关闭、非阻断、脱敏且有上限的服务端影子验证；是否给新写入事件增加 `protocolVersion: 1`、以及前端适配，继续保持独立阶段和独立回退。

**涉及文件**：`agent_protocol.py`、`tests/test_agent_protocol.py`、`docs/harness/h1-1-agent-event-contract.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 19:51 · Codex

### Harness H0-2 脱敏轨迹、兼容夹具与测量基线

- 按已确认边界完成 H0-2，只增加离线测试资产和说明，不改 API、AgentRun 写入、会话 JSONL、工具协议、前端状态、UI 或生产数据。H0-1 的 15 条候选已固化为版本 1 合成轨迹套件，共 106 个现有耐久事件和 4 个页面刷新、轮询断线或服务重启恢复点；事件序号、信封、检查点前缀、终态和固定套件哈希均由独立脚本验证。
- 根据真实 Git 历史建立 AgentRun v1/v2/v3 最小兼容样本：v1 覆盖无工作区根和上下文字段的旧记录，v2 覆盖 `cwd/workspaceRoots`，v3 覆盖 `contextLimit/contextRecoveryRound/compactions`；同时加入含有效旧消息和异常尾部半行的 JSONL 样本，以及锁定入口导入顺序、经典回退末尾脚本和必要产物的前端兼容清单。测试直接调用当前恢复读取器，旧样本不做原地改写。
- 新增只读夹具校验器，默认只读取仓库内 H0 资产，拒绝凭据字段、Bearer/`sk-` 值、私有用户路径、真实服务 URL、过长正文、未知事件、重复场景、非连续序号和错误检查点。当前 15 条轨迹和 5 份兼容样本全部通过；伪凭据、私有路径和非示例域名探针能被主动拦截。
- 建立 100/1000/10000 合成事件规范化工作量及固定哈希基线，单次本机采样分别约为 `0.274ms / 2.392ms / 16.331ms`；明确这不是 UI/reducer replay 性能。首次发送和刷新恢复当前只记录自动守卫命令墙钟与可验证边界，真实首帧和刷新 DOM 恢复耗时留到 H4 隔离浏览器 E2E，避免用测试进程耗时冒充用户体验。
- 自动验证通过：H0-1/H0-2 定向测试为 `13 passed, 18 subtests passed`；完整回归为 `927 passed, 261 subtests passed in 93.32s`；`npm run check:frontend` 通过 bundle、构建新鲜度、JavaScript 语法和经典回退门禁；Python 语法与 `git diff --check` 通过。H0 已具备进入 H1 的离线事实和兼容基线，H1 事件契约仍需下一阶段单独确认。

**涉及文件**：`docs/harness/h0-2-fixture-baseline.md`、`scripts/verify_harness_fixtures.py`、`tests/fixtures/harness/`、`tests/test_harness_fixtures.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 19:27 · Codex

### Harness H0-1 事件与状态事实清单

- 按已确认路线完成 H0-1，只冻结现状、不修改业务行为：从 `server.py`、`agent-runtime.js`、`app.js`、`src/core/state.js` 与 `src/ui/messages.js` 提取 AgentRun、模型 Runtime、工具、权限、恢复、前端投影和计时事实，形成中文说明与机器可读 JSON 清单。
- 当前 AgentRun 记录版本为 3，顶层实际有 8 个状态、22 类耐久事件；其中 8 类由前端按事件直接投影，6 类等待/终态主要由快照驱动，8 类仅保留耐久记录而没有独立前端消费者。清单明确区分耐久 AgentRun 事件与内存模型 SSE，并标记重复含义、无消费者字段和前端推断状态。
- 冻结 15 条首批脱敏轨迹候选，覆盖纯文本、单工具、多工具、问卷、授权、压缩、取消、断线、刷新和服务重启；本阶段不创建最终轨迹夹具，H0-2 再单独确认夹具格式、旧版本兼容样本、脱敏检查与性能测量方式。
- 新增只读清单一致性测试，通过 Python AST 自动核对源码中的 8 个状态、22 个事件、AgentRun 记录版本、前端消费者名称和轨迹覆盖。定向测试为 `5 passed, 12 subtests passed`；完整回归为 `919 passed, 255 subtests passed in 97.28s`，PowerShell 墙钟 `99.30s`；`npm run check:frontend` 在 `2.90s` 内通过 bundle、freshness、语法和经典回退门禁，`git diff --check` 通过。
- 未修改 API、AgentRun、会话 JSONL、工具协议、前端 UI、服务端运行时或生产持久数据，也未启动浏览器和真实模型。

**涉及文件**：`docs/harness/h0-1-fact-inventory.md`、`docs/harness/h0-1-fact-inventory.json`、`tests/test_harness_inventory.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 19:05 · Codex / 用户确认

### Code Harness 工程优化路线立项

- 基于当前服务端 AgentRun、工具协议、持久化恢复、前端执行轨迹、测试与发布链完成只读工程评估，确认 Code 已具备稳定的本地 Agent Harness 底座；下一阶段主要风险不再是功能缺失，而是事件、运行状态、计时、恢复和 DOM 投影之间的跨层时序一致性。
- 新增 `docs/harness-engineering-optimization-plan.md`，明确单一事实源、确定性投影、副作用分离、版本化事件契约、Run/模型轮/工具轮状态机、耐久事件与瞬时流事件边界、轨迹 replay、浏览器 E2E、职责拆分、安全、可观测性、兼容和发布门禁。
- 路线按 H0～H7 分阶段推进：先冻结事实清单与脱敏轨迹基线，再建立事件契约、影子 reducer、replay 和关键浏览器 E2E；上述护栏稳定后才扩大 Context Envelope、结构化计划、生命周期 Hook 与 Workflow。用户确认先执行 H0-1，不直接进入协议改造、运行时重写或大规模拆分。
- 本阶段只建立长期指导与 TODO 入口，没有修改业务代码、API、AgentRun、会话 JSONL、工具协议或正式/开发端运行行为。已完成的图片格式与历史污染修复从 TODO 移除；独立生图模型 API 配置与统一调用规则作为新的未完成事项保留。

**涉及文件**：`docs/harness-engineering-optimization-plan.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 18:25 · Codex / 用户验收

### 用户图片批次横排与统一视觉高度

- 用户消息中的同批图片由“每张图片一个独立消息行”改为单个批次容器：图片位于文本上方，组内右对齐并优先横向排列，超过聊天内容区宽度后自动换行；文本继续作为同一用户消息的一部分显示在图片组下方。
- 图片使用 `120 CSS px` 目标高度，宽度按原图比例自适应；极端超宽图片受消息区最大宽度保护，避免横向溢出。输入框原有 `48×48` 缩略图、逐图点击放大、图片加载后的滚动跟随、队列与后台任务状态均保持不变。
- 本次只调整消息投影和样式，模型请求中的内容块顺序、会话 JSONL 与历史数据均未改变；旧消息重新渲染后自动按原用户消息批次分组。Windows 或浏览器 150% 缩放下，`120 CSS px` 在截图物理像素中约为 180px，属于正常 DPI 映射。
- 自动验证通过：前端模块 `155 passed`，JavaScript 语法、前端 bundle、经典回退页构建新鲜度与 `git diff --check` 均通过。用户以三张不同比例图片完成横排、尺寸与文本顺序验证后确认没有问题。

**涉及文件**：`src/ui/messages.js`、`styles.css`、`tests/test_frontend_modules.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 17:50 · Codex / 用户验收

### 工具阶段标题时序与展开状态保持

- 修正活跃工具阶段在工具刚返回、下一轮模型尚处于 `pending` 或无明确阶段说明的 `thinking` 时过早切换成统述标题的问题：当前阶段继续显示最近的具体工具与目标，只有出现明确阶段说明、正式回答或整轮任务结束后才显示“运行了多个命令”“查看了文件”等统述；单项与多项文案继续按实际数量区分。
- 为工具阶段增加仅在当前页面任务内使用的稳定标识；消息区流式重绘前读取用户手动展开的工具组，后续工具事件、模型等待与回答流期间恢复其展开状态。用户主动收起后保持收起，整轮任务完成后不再恢复内部展开状态，并随外层“已处理”轨迹默认折叠。
- 未修改工具协议、会话 JSONL、本地配置或历史消息；展开状态只来源于当前 DOM。自动验证通过：前端模块 `155 passed`，JavaScript 语法、前端 bundle、经典回退页构建新鲜度与 `git diff --check` 均通过。用户用多工具任务验证标题与展开时序后确认没有问题。

**涉及文件**：`app.js`、`src/ui/messages.js`、`tests/test_frontend_modules.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 17:11 · Codex / 用户验收

### 前端图片附件格式归一与入口时序收口

- 新增 `src/features/image-attachments.js` 作为前端图片 MIME、扩展名与实际字节识别的统一事实源：PNG、JPEG、WebP 保持对应编码；浏览器可解码的非通用模型格式转为 PNG；实际 MIME 从编码结果读取，不再出现 Canvas 编码与声明类型不一致。
- 浏览器无法解码但服务端可转换的 ICO、BMP、GIF、TIFF 等格式保留原始字节，交给已验证的模型请求边界转换；增加 10 MB 上限、图片解码失败处理和 10 秒截止时间，并补齐中英文失败提示。转换后的存储扩展名与真实 MIME 对齐，原始展示名称和旧 JSONL 均不改写。
- 剪贴板、拖放与 `@` 图片引用统一复用同一附件管线；普通发送、排队消息和后台子任务均在分流前等待图片任务与 `@` 解析完成，避免立即发送时漏图或先发后补。
- 自动验证通过：完整回归为 `919 passed, 243 subtests passed`，JavaScript 语法、前端 bundle、经典回退页构建新鲜度与 `git diff --check` 均通过。用户完成实际图片发送测试后确认没有问题。

**涉及文件**：`app.js`、`scripts/build-frontend.mjs`、`src/core/i18n.js`、`src/features/image-attachments.js`、`src/frontend-entry.js`、`tests/test_frontend_modules.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 16:46 · Codex / 用户验收

### 模型图片请求边界保护与恢复任务计时修正

- 模型请求边界新增非破坏性图片投影：PNG、JPEG、WebP 按实际字节修正 MIME 后直接使用，ICO、BMP、GIF、TIFF 在本次请求内转换为 PNG；无效、超限或无法转换的历史图片仅从当次模型请求中省略并追加说明，不改写会话 JSONL，界面仍保留原始图片。
- 浏览器直连 `/proxy/chat`、服务端 Agent 运行时及 `read_file` 视觉注入最终统一经过相同保护；历史污染会话续聊、换模型、队列和后台任务不再重复向上游提交 `image/x-icon`。纯文本兼容重试改为请求级投影，不再把最后一条用户图文消息永久改成纯文本。
- 修复页面恢复持久化任务时把离线间隔计入顶部耗时的问题：检查点新增可选 `elapsedMs`，恢复后按“此前已累计活跃耗时 + 本次恢复后的耗时”继续计时；旧检查点用 `updatedAt - startedAt` 兼容推算，原始 `startedAt` 继续用于任务身份与后台结果排序，新消息仍从零开始。
- 自动验证通过：PNG、JPEG、WebP、ICO、BMP、GIF、TIFF 请求投影矩阵、旧会话非改写、计时恢复兼容及前端稳定性定向测试通过；完整回归为 `917 passed, 243 subtests passed`，JavaScript/Python 语法、前端 bundle 与经典回退页构建/新鲜度、`git diff --check` 均通过。用户以旧 ICO 污染会话续聊并换模型，确认图片重新可见；刷新恢复后计时显示正常，不再出现跨离线时段的十几小时累计。
- 本阶段只完成模型请求边界和旧数据保护；前端附件入口的实际编码/MIME 一致性、异常兜底，以及打包环境的 Pillow 格式插件仍按后续阶段继续推进。

**涉及文件**：`app.js`、`server.py`、`src/agent/model-request.js`、`src/core/state.js`、`tests/test_frontend_modules.py`、`tests/test_image_vision_and_browser_refresh.py`、`tests/test_p0_stability.py`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 11:40 · Codex / 用户验收

### 发送前置提示与新会话标题 i18n 收口

- 普通发送和任务中排队发送的缺少模型、缺少 Key 提示统一接入中英文 i18n，删除遗留的 “New API sub key” 产品术语；发送前先检查模型，再检查该模型对应的可用 Key，避免未选择模型时误报为 Key 缺失。
- 会话保存、自动创建及纯图片首条消息的空标题回退统一复用现有“新会话 / 未命名会话”翻译，不再在中文界面生成 `New session` 或 `Untitled`。
- 用户确认 Mode Prompt 继续作为内部机制，不需要在设置页向用户展示；本阶段不增加预览区、不修改权限执行逻辑，并移除已不再需要的预览同步 TODO。
- 自动验证通过：普通发送、排队发送、提示顺序和标题回退定向测试通过；完整相关回归为 `235 passed, 4 subtests passed`，JavaScript 语法、前端 bundle 构建与新鲜度检查、`git diff --check` 均通过。用户刷新、切换中英文并完成正常发送后确认没有问题，人工验收通过。

**涉及文件**：`app.js`、`src/core/i18n.js`、`tests/test_frontend_modules.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 11:14 · Codex / 用户验收

### workbar Key 同步状态与弹窗布局收口

- workbar Key 同步现在区分账号暂无 Key、令牌存在但完整 Key 暂不可读、部分 Key 暂不可读、暂无可用 Key、已全部添加及已移除等结果；交互调用返回结构化状态，静默同步保持无 Toast，原有授权失效和请求失败处理不变。
- 平台返回空列表或不可读 Key 时不再写入或清空本地 Key 配置；同步状态只反映当前请求结果，不持久化为跨端口或跨浏览器结论，3010/3011 及不同来源中既有本地 Key 继续独立保留。
- 获取弹窗标题和底部状态文案改为更简短的中英文表达，宽度扩展为响应式 720px，令牌名称、脱敏 Key 与操作区采用单行三列对齐；长名称通过省略和悬停标题查看完整内容。
- 自动验证通过：JavaScript 语法、`git diff --check`、前端 bundle 构建与新鲜度检查均通过；相关回归为 `233 passed, 4 subtests passed`。用户刷新后确认弹窗宽度、单行布局及简化文案均无问题，人工验收通过。

**涉及文件**：`src/core/i18n.js`、`src/features/settings.js`、`styles.css`、`tests/test_frontend_modules.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 10:33 · Codex / 用户验收

### 模型列表热刷新缓存与失败保留

- 模型列表增加仅包含网关地址、模型 ID 和更新时间的本地展示缓存；页面再次打开时先恢复上次成功列表和当前选择，并以独立的“正在刷新可用模型”状态等待后台请求，避免刷新期间列表短暂清空。缓存不保存 Key、账号或模型到 Key 的路由关系。
- 后台刷新完成后原子替换列表和实时路由映射；全部请求失败时保留旧列表并明确标记为上次结果，真实空结果才清除缓存和失效选择。Key 或账号变化后旧列表标记为待刷新且清空实时映射，全部 Key 被移除或退出登录时清除缓存；旧映射只有对应 Key 仍启用时才允许使用。
- 设置页与主模型选择器共享同一模型目录状态，补充加载、警告和失败的中英文轻量提示；刷新按钮、模型数量和当前选择保持同步。
- 自动验证通过：JavaScript 语法与 `git diff --check` 通过，相关回归为 `232 passed, 4 subtests passed`，`npm run check:frontend` 的 bundle 构建、新鲜度和语法检查通过；用户连续刷新页面并检查模型设置后确认没有问题，人工验收通过。

**涉及文件**：`app.js`、`src/core/i18n.js`、`src/core/state.js`、`src/features/settings.js`、`styles.css`、`tests/test_frontend_modules.py`、`tests/test_p0_stability.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 09:58 · Codex / 用户验收

### 状态文案 i18n 与斜杠命令发现性收口

- 删除会话确认已统一使用现有中英文 i18n；本阶段继续清除 Memory 提取不足、对话扫描和更新重启三处用户可见英文硬编码，动态状态统一通过 `t()` 读取同一翻译事实源。
- `/remember` 与 `/help` 原有执行分支此前未注册到命令候选，本阶段只补齐命令区注册和中英文说明，`/compact` 继续保持第一项；两个命令的执行与持久化行为未改变。
- 将 `/parallel` 说明明确为“主任务运行时，启动共享当前项目的后台子 Agent”，避免误解为普通队列消息并发；仅修改中英文描述，不改变共享目录、并发限制或后台任务行为。
- 自动验证通过：JavaScript 语法、直接硬编码 Toast/确认调用扫描、`git diff --check`、前端 bundle 构建与新鲜度检查均通过，完整相关回归为 `204 passed, 4 subtests passed`；用户刷新后确认命令候选、描述及中英文显示没有问题，人工验收通过。

**涉及文件**：`app.js`、`src/core/i18n.js`、`src/features/settings.js`、`src/features/skills-memory.js`、`tests/test_frontend_modules.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 09:21 · Codex

### 上游缓存支持语义与定价总表计算修正

- 定位并修复渠道总表出现大额负毛利的口径错误：上游价格源未提供缓存读取价时，旧采集器会默认用输入价回填，导致不支持缓存的渠道被误算为高价缓存采购。现改为显式记录 `cache_read_support`；缺少缓存字段时标记 `unsupported` 且采购缓存价保持空值，只有来源明确提供缓存价时才标记 `supported`，即使该值等于输入价或为 0 也继续保留。
- 总表新增输入、输出、缓存读取三维毛利、最低毛利、限制维度和所需倍率。`unsupported` 缓存维度不参与最低毛利和所需倍率计算；旧快照保持兼容，存在缓存价时推断为 `supported`，无价格且无状态时记为 `unknown`，不静默伪造价格。
- 09:13 完成一次真实只读全量流水线：九家官方与 12 条启用渠道共得到 94 条记录，15 个来源全部 `success`。渠道模型中 56 条明确支持缓存、3 条明确不支持；三条被纠正的记录为 `ApiNebula-Gemini-Antigravity / gemini-2.5-flash-lite`、`ApiNebula-Gemini-Antigravity / gemini-3.5-flash`、`ApiNebula-Gemini-GCP / gemini-3.5-flash`，纠正后的最低毛利分别约为 16.04%、16.04%、17.08%。
- 两条仍为负毛利的记录均来自上游明确缓存价，属于真实复核信号而非缺字段误判：`ApiNebula-Codex / gpt-5.4-mini` 约 -286.95%，`ApiNebula-Gemini-GCP / gemini-3.1-flash-lite` 约 -729.19%；后续需结合供应商实际扣费确认，不自动调整 workbar 线上倍率。
- 自动验证通过：采购体系完整回归 68 项通过；Schema 定向测试 11 项通过；冻结样本流水线、最终工作簿公式错误扫描均通过，四张工作表完成视觉检查。全过程未读取或输出真实 Key，未修改 workbar 基础价、分组倍率、渠道或其他线上配置。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/pricing_monitor.py`、`../../模型定价/渠道采购体系/工具/upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/工具/pricing_master.py`、`../../模型定价/渠道采购体系/工具/pricing_master_workbook.mjs`、`../../模型定价/渠道采购体系/schema/pricing-snapshot.schema.json`、`../../模型定价/渠道采购体系/tests/`、`../../模型定价/渠道采购体系/outputs/20260802/RUN-20260802T011341Z/`

## 2026-08-02 09:00 · Codex / 用户验收

### 导入会话结果反馈区样式收口

- 将导入会话弹窗的结果反馈区与底部操作栏设为不可压缩的独立布局，避免批量处理完成后状态文案被列表挤压、贴边或与局部滚动条争抢空间。
- 成功、警告和失败结果改为轻量状态条，统一内边距、边框、背景和换行；空结果与空失败详情不再占位，较长反馈仍由反馈区在限定高度内滚动。
- 本阶段未修改导入流程的 DOM 结构、JavaScript 行为、数据协议或中英文文案，仅收口样式并补充 CSS 回归约束。
- 自动验证通过：前端相关测试 `203 passed, 4 subtests passed`，`npm run check:frontend`、bundle 构建/新鲜度检查与 `git diff --check` 均通过；用户刷新 Code Dev 后确认界面没有问题，人工验收通过。

**涉及文件**：`styles.css`、`tests/test_frontend_modules.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 08:44 · Codex / 用户验收

### 手动上下文压缩显示、历史保留与图文兼容收口

- `/compact` 手动压缩改为在消息流中显示独立的轻量状态行，依次呈现“正在压缩上下文…”、成功或失败状态；成功不再重复弹出 Toast，隐藏的 `compact-summary` 只承担模型上下文边界，不再显示旧式分支提示。
- 修复压缩成功后错误地以“摘要 + 最近消息”覆盖活动会话的问题：完整历史继续保存在 `state.messages` 和会话 JSONL 中，界面、刷新和后续查找均保留旧消息；后续模型请求只从最新隐藏摘要开始，重复压缩也只处理当前有效模型上下文。
- 修复手动压缩调用固定相对 URL 以及图文消息 `content` 数组触发 `'list' object has no attribute 'strip'` 的问题：请求使用当前网关地址，多模态内容只提取文本块，纯图片安全跳过，图片 URL/Base64 不进入摘要请求。
- 删除会话确认正文及无标题回退文案改用统一中英文 i18n；旧的压缩摘要时间线投影与对应无用文案已经移除。导入会话弹窗结果反馈区的布局问题按用户截图另记入 `TODO.md`，本阶段未修改其样式。
- 自动验证通过：完整测试 `902 passed, 236 subtests passed`，相关 JavaScript/Python 语法、前端 bundle 构建与新鲜度检查、`git diff --check` 均通过；仅保留 `server.py` 既有的 `invalid escape sequence '\\C'` SyntaxWarning。
- 用户重启 Code Dev 后在原图文会话重新执行手动压缩，确认功能恢复正常，本阶段人工验收通过。

**涉及文件**：`app.js`、`server.py`、`src/core/i18n.js`、`src/ui/messages.js`、`src/ui/timeline.js`、`tests/test_frontend_modules.py`、`tests/test_p2_coverage.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 05:10 · Codex

### 定价历史归档与模型/渠道双视图精简总表

- 在独立仓库 `模型定价/渠道采购体系` 将完整流水线补齐为可审计历史：每轮为官方页面和上游模型广场保存 gzip 原始响应、SHA-256、字节数、归一记录和运行清单；本轮 `RUN-20260801T205935Z` 共归档 29 个原始/归一记录并附 1 份运行清单，原始证据不覆盖旧运行，也不包含真实 Key。
- 新增精简总表生成器，以同一批 `渠道 × 分组 × 模型` 事实同时生成模型总览和采购渠道视图，并附变化摘要、采集批次；初版只保留官方价、Workbar 基础价、采购价、分组倍率、销售价、毛利、所需倍率、来源状态等核心字段，避免一开始扩展为难维护的宽表。
- 本轮采集得到官方 35 条、12 条启用渠道 59 条，共 94 条归一记录，15 个来源状态全部 `success`。工作簿中的启用渠道数、最低采购价、销售价、最低毛利和所需倍率均由公式计算；公式错误扫描为 0，四张工作表完成视觉检查。
- 变化摘要正确记录用户已完成的 Terra 基础档/长上下文档六项价格下降 20%，以及 `GPT-Codex-优质` 从 `0.08` 调至 `0.09`；这些均标记为“已配置”历史，不生成重复线上操作。当前仍需处理的信号为 Luna 基础价复核、DeepSeek 基础价取整口径、Qwen 限时折扣候选，以及渠道缓存价导致的负毛利风险；后续继续结合实际扣费判断。
- 本阶段没有修改 Workbar 基础价、分组倍率、渠道或其他线上配置。每日 09:00 调度、异常通知、无变化静默和每周健康摘要仍保留在 `TODO.md`。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/pricing_pipeline.py`、`../../模型定价/渠道采购体系/工具/pricing_master.py`、`../../模型定价/渠道采购体系/工具/pricing_master_workbook.mjs`、`../../模型定价/渠道采购体系/tests/test_pricing_master.py`、`../../模型定价/渠道采购体系/tests/test_pricing_pipeline.py`、`../../模型定价/渠道采购体系/历史/运行/RUN-20260801T205935Z/`、`../../模型定价/渠道采购体系/outputs/20260802/RUN-20260801T205935Z/`

## 2026-08-02 02:56 · Codex

### UU API 上游可访问范围与 Workbar 启用范围拆分

- 用户通过 Workbar 模型广场截图指出，当前“国产模型”分组只有 `deepseek-v4-flash`、`deepseek-v4-pro`、`glm-5.2`、`kimi-k2.6`、`qwen3.7-max` 五个模型；进一步核对 2026-07-30 分组定价快照，确认 `minimax-m3` 与 `qwen3.7-plus` 已从 UU API 渠道取消。此前登记册的 7 个 `accessible_models` 表示上游令牌能力，并不等于 Workbar 当前启用范围，因此 02:35 的 5/7 `partial` 是范围语义混淆造成的误报。
- 定价采购体系将 `accessible_models` 与 `enabled_models` 拆分：前者只记录上游能力，后者作为 Workbar 实际采集范围，并强制校验为前者子集且存在于当前精确模型快照中；旧登记册保留兼容回退。新增 `token-scope` 安全命令，可在不读取或修改真实 Key 的情况下维护启用范围，`token-save` / `token-sync` 会保留范围。
- 使用安全命令把本地 UU API 登记状态修正为“可访问 7 个、Workbar 启用 5 个”。两次连续实时完整流水线均得到 59 条渠道记录、94 条官方与上游合并记录，UU API 为 5/5，14 个来源全部 `success`，第二轮语义变化为 0、异常来源为 0；采购体系完整回归 62 项通过。
- 本次只修正本地脱敏登记范围与确定性采集逻辑，没有读取或输出真实 Key，没有修改 Workbar 基础价、分组倍率、渠道、模型分组或其他线上配置。后续继续进行实际扣费复核、分组倍率与毛利核算，再按明确授权执行线上调整。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/upstream_channel_tool.py`、`../../模型定价/渠道采购体系/工具/upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/模板/upstream-registry.example.json`、`../../模型定价/渠道采购体系/tests/test_upstream_channel_tool.py`、`../../模型定价/渠道采购体系/tests/test_upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/outputs/20260802/upstream-pricing-collector-validation.md`

## 2026-08-02 02:42 · Codex

### app.js 阶段 6D-4：正式版经典回退恢复层决策与阶段 6 收口

- 完成默认页、3011 开发刷新、前端构建、3010 正式 EXE 打包、应用内更新和失败恢复链路的只读审计。经典回退页由 `src/frontend-entry.js` 的同一导入清单自动生成，不存在第二份手工脚本顺序；页面自身约 50 KiB，随其保留的 `src/`、`app.js` 与 `agent-runtime.js` 原始脚本合计约 834 KiB，相对当前约 30 MiB 的 EXE 成本有限。
- 明确正式版回退语义：bundle 资源加载失败或同步初始化未完成时，默认页转到随同一 EXE 打包的原始脚本启动页；应用版本、服务端、会话和用户数据均不回退，地址中的 `fallback=bundle-load` 或 `fallback=bundle-init` 只记录本次前端启动失败类型。该恢复层不能处理 EXE、服务端或原始脚本本身的故障。
- 3011 开发实例能够检查并重建过期或损坏的 bundle，3010 正式 EXE 不能在用户机器上重建。当前更新流程依赖正常前端发起下载与重启，安装脚本会清理旧版本且没有独立自动回滚；因此删除经典资源会把单一 bundle 故障放大为整个 UI 无法进入，也同时失去应用内更新入口。
- `window.Code` 当前由 29 个实际模块作为注册命名空间使用，不是只供过渡的空代理。用户确认按审计建议保留经典回退、原始脚本及模块命名空间；6D-1 至 6D-3 已删除确认无必要的内联事件和顶层临时全局，6D-4 不修改运行代码、打包清单、协议或持久化数据，阶段 6 至此完成。
- 未来只有在具备无界面更新或回滚入口、启动完整性检测和可验证的故障恢复后，才重新评估移除经典页面与原始脚本；真正的 ES Module / TypeScript 迁移作为独立架构阶段处理，不与本轮兼容层清理混合。

**涉及文件**：`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 02:35 · Codex

### UU API Kimi 渠道级模型映射校正

- 用户指出 `kimi-k2.6x` 即 UU API 对 `kimi-k2.6` 使用的供应商模型 ID；进一步核对 2026-07-29 多个 UU API 准入、流式、工具和稳定性证据包，均已明确记录“`kimi-k2.6` 使用快照中的 `kimi-k2.6x` 价格映射”，因此修正 02:25 将其仅列为候选的临时判断。
- 在定价来源配置中固化渠道级 `kimi-k2.6 -> kimi-k2.6x` 显式映射。采集记录继续使用 Workbar 模型 ID，并在规则中同时保存供应商模型 ID；未启用任何全局前后缀、相似度或相邻型号猜测。
- 冻结样本与配置 Schema 测试通过；两次连续实时只读流水线均得到 59 条上游记录、94 条官方与上游合并记录，第二次语义变化为 0。UU API Kimi 经 `0.65` 供应商分组折算为输入/输出/缓存读取 `4.225 / 17.55 / 0 CNY/MTok`。
- UU API 由 4/7 提升到 5/7 价格覆盖，仍因 `minimax-m3`、`qwen3.7-plus` 未出现在 42 模型目录中保持 `partial / expected_model_missing`；本次未修改 Workbar 基础价、分组倍率、渠道或其他线上配置。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/模板/pricing-monitor-sources.example.json`、`../../模型定价/渠道采购体系/schema/pricing-source.schema.json`、`../../模型定价/渠道采购体系/tests/test_upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/outputs/20260802/upstream-pricing-collector-validation.md`

## 2026-08-02 02:25 · Codex

### UU API 登录价格响应缺失复核

- 经用户明确授权，将其复制的 UU API 登录后 `/api/pricing` 完整 Response Body 先以 UTF-8 原样保存到定价仓库 Git 忽略的本地目录，再从落盘文件解析；原始文件为 40,870 字节，SHA-256 为 `9461c972d238e56a6ae7fe58d399005075f0eb6b767f397ea23aad7e7a5d12e0`，敏感字段扫描为 0，未读取请求头、Cookie、Authorization、HAR 或 Key。
- 登录响应与自动公开采集完全一致，均为 `pricing_version=f7244f5d`、42 个模型，排除匿名接口导致漏数。登记的 7 个精确模型中，目录只匹配 `deepseek-v4-flash`、`deepseek-v4-pro`、`glm-5.2`、`qwen3.7-max`。
- `kimi-k2.6`、`minimax-m3`、`qwen3.7-plus` 仍精确缺失；目录另有不同模型 ID `kimi-k2.6x`，只能作为映射候选，不能在缺少渠道映射和真实调用证据时自动替代。目录中没有任何 `minimax` 模型或 `qwen3.7-plus`。
- UU API 来源继续保持 `partial / expected_model_missing`。后续需要确认渠道模型映射、上架状态或修订登记令牌的可访问模型范围；本次未修改 workbar 线上配置。

**涉及记录**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/outputs/20260802/upstream-pricing-collector-validation.md`、`../../模型定价/渠道采购体系/CHANGELOG.md`

## 2026-08-02 02:09 · Codex

### ByteCatCode 原始价格响应确定性导入

- 经用户明确授权，通过本机 PowerShell 剪贴板接口读取其已复制的 ByteCatCode `/api/pricing` 完整 Response Body，先将 31,667 字节 UTF-8 原始文本保存到定价仓库 Git 忽略的 `本地配置/clipboard-imports/`，再从落盘文件解析；原始文件 SHA-256 为 `171c5c4e80d52a3bb2be2f9028415ea8440e99852b88e8554f537ce76eac1b5d`，敏感字段扫描为 0，未读取请求头、Cookie、Authorization、HAR 或 Key。
- 新增 `bytecat_pricing_importer.py`，固定执行原始 JSON、敏感字段、精确模型集合、供应商分组、计费表达式和有效期校验；ByteCatCode 当前 `codex=0.2`，登记令牌与 workbar 快照交集的 6 个 GPT 模型全部存在，生成 7 天有效的本地脱敏快照。
- 导入时识别 `gpt-5.6-sol` 的 `len < 272000` 双档价格和 `priority=2.5` 倍服务档，补齐原解析器只识别 `<=` 的边界；自动采购价固定采用默认服务档，同时保留第二上下文档和 priority 倍率证据。
- 完整回归 58 项通过。修正解析器后两次连续实时上游采集均为 58 条记录、5 个来源成功、UU API 1 个部分成功、0 个失败，第二次语义变化为 0；ByteCatCode 提供 6 条记录并从 `manual_price_missing` 恢复为 `success`。
- 本次只建立本地采购价格证据，没有修改 workbar 基础价、分组倍率、渠道或其他线上配置；ByteCatCode 目录价仍需结合实际账户余额扣减确认后才能用于正式采购核算。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/bytecat_pricing_importer.py`、`../../模型定价/渠道采购体系/工具/upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/tests/test_bytecat_pricing_importer.py`、`../../模型定价/渠道采购体系/tests/fixtures/upstream-pricing/bytecat-raw-pricing.json`、`../../模型定价/渠道采购体系/模板/pricing-monitor-sources.example.json`、`../../模型定价/渠道采购体系/outputs/20260802/upstream-pricing-collector-validation.md`

## 2026-08-02 01:38 · Codex

### 12 条上游渠道确定性价格采集与统一流水线

- 在独立仓库 `模型定价/渠道采购体系` 新增上游价格采集器，按来源登记把 12 条启用渠道归并为 APINebula、BoxYing、Unity2、UU API、ByteCatCode 和 DeepSeek 官方六个来源；渠道模型由安全脚本脱敏扫描登记册中的 `accessible_models` 后，与当前 workbar 35 个精确模型 ID 求交集，不在代码中维护代表模型列表。
- APINebula、BoxYing 与 UU API 通过既有 New API 安全脚本访问公开模型广场接口，Unity2 通过 HTTPS 同源白名单访问公开模型与分组接口，DeepSeek 官方复用既有官方采集结果；ByteCatCode 保留 7 天有效的脱敏人工 JSON，不尝试读取登录态、Cookie 或模型调用 Key。
- 固化四种确定性归一规则：优先解析 New API `billing_expr`、无表达式时使用标准倍率、UU API 绝对每 Token 转每百万 Token、Unity2 每 Token 目录价乘分组倍率；供应商展示汇率只保存证据，不参与未经确认的采购价推算。公开来源在实际供应商账户扣费复核前统一保持 `provisional`。
- 新增一键 `pricing_pipeline.py`，顺序执行九家官方采集、12 条上游采集、记录合并、不可覆盖历史快照、语义差异和中文报告；日常运行不调用模型、不依赖 AI，单源异常会生成降级报告并续用上一有效记录，调度器可通过 `--fail-on-source-error` 获取异常退出码。
- 采购体系完整回归 55 项通过；两次冻结完整流水线均为 48 条记录且第二次语义变化为 0。两次实时上游只读采集均为 52 条记录、4 个来源成功、UU API 部分成功、ByteCatCode 因缺人工快照失败，连续语义变化为 0；UU API 登记的 7 个模型中当前模型广场只返回 4 个对应价格，系统保留 `expected_model_missing` 数据质量提醒，未误报下架或零价格。
- 本阶段没有读取或输出真实 Key，没有修改 workbar 基础价、分组倍率、渠道配置或其他线上数据。仍需由用户提供 ByteCatCode 脱敏模型广场价格，完成供应商上游账户实际扣费复核、统一分组倍率与毛利影响计算，再配置每日 09:00 系统调度和通知。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/工具/pricing_pipeline.py`、`../../模型定价/渠道采购体系/tests/test_upstream_pricing_collector.py`、`../../模型定价/渠道采购体系/tests/fixtures/upstream-pricing/`、`../../模型定价/渠道采购体系/模板/pricing-monitor-sources.example.json`、`../../模型定价/渠道采购体系/模板/bytecat-pricing-manual.example.json`、`../../模型定价/渠道采购体系/schema/pricing-source.schema.json`、`../../模型定价/渠道采购体系/outputs/20260802/upstream-pricing-collector-validation.md`

## 2026-08-02 00:48 · 用户决策

### 官方价格变更执行范围收窄

- 用户确认阿里云百炼的限时折扣只列入变更候选，`qwen3.7-max` 和 `qwen3.7-plus` 当前不修改 workbar 基础价；监控继续同时保留原价、折扣价和后续到期回退信号。
- 当前待执行的官方基础价更新仅包括 `gpt-5.6-terra` 与 `gpt-5.6-luna`。按固定定价汇率 `6.7`，输入/输出/缓存读取候选值分别为 `13.4 / 80.4 / 1.34` 与 `1.34 / 8.04 / 0.134` CNY/MTok。
- 本轮只记录决策，不修改线上配置；两项 GPT 基础价将在 12 条上游渠道价格核对、分组倍率和毛利影响计算完成后，与渠道相关调整同批执行，并在实际线上修改前再次取得明确授权。

**涉及记录**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 00:40 · Codex

### 九家官方模型价格采集全量验证

- 在独立仓库 `模型定价/渠道采购体系` 将官方价格采集器从 OpenAI、DeepSeek、xAI 三家 10 个模型扩展到九家 35 个模型，新增 Anthropic、Google、智谱、Moonshot、阿里云百炼和 MiniMax；模型身份继续从既有 workbar 快照精确派生，不用别名猜测，也不读取真实 Key。
- 针对官方页面的实际结构补齐基础输入、输出、缓存读取、缓存写入、上下文分档、批处理折扣、币种、限时优惠和已公告未来价格等结构化字段；采集仍只允许 HTTPS 官方域名、无认证公开来源和有界响应，来源失败继续由既有引擎降级并续用上一有效值。
- 两轮独立实时只读采集均得到 35 条记录、九个来源全部 `success`、零 `partial`、零 `failed`；第二轮相对第一轮的语义变化和数据质量问题均为 0。采购体系完整回归 50 项通过，冻结来源样本、Python 编译、JSON 解析、格式检查和完整性清单均纳入收口验证。
- 与 2026-07-29 workbar 快照比较，新增两个待复核候选告警：`qwen3.7-max` 当前限时优惠输入/输出/缓存读取均低 50%，`qwen3.7-plus` 基础档和长上下文档均低 20%。其余新增 23 个模型与旧快照一致；本阶段没有修改 workbar 基础价、分组倍率或线上渠道配置。
- 官方模型价格全量采集阶段完成；后续仍需接入当前 12 条启用上游渠道的公开或人工价格来源，生成当前启用映射与基础价新鲜快照，补统一分组倍率和毛利影响计算，最后启用北京时间每日 09:00 调度、无变化静默和每周健康摘要。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/official_pricing_collector.py`、`../../模型定价/渠道采购体系/tests/test_official_pricing_collector.py`、`../../模型定价/渠道采购体系/tests/fixtures/official-pricing/frozen-sources.json`、`../../模型定价/渠道采购体系/outputs/20260801/official-pricing-collector-full-validation.md`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/规范/07_模型价格监控与变更提醒.md`、`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/MANIFEST.sha256`

## 2026-08-02 00:41 · Codex

### app.js 阶段 6D-1：消息交互移除内联事件与散落全局桥接

- `src/ui/messages.js` 新增幂等的消息列表事件委托，统一处理消息复制、图片预览和图片加载后的滚动回调；复制函数保留为模块内部实现，不再暴露 `window.copyMessageText`。
- 消息投影与 Markdown 图片使用数据属性表达交互意图，输入区图片缩略图由 `renderImageThumbs()` 直接绑定预览事件；相关 `onclick` / `onload` 内联脚本已移除。`app.js` 只负责注入图片预览与滚动副作用，消息模块继续负责自身交互所有权。
- 本批没有删除经典回退、原始脚本资源或 `window.Code` 模块命名空间，没有修改模型请求、JSONL、API、工具协议、AgentRun、权限和持久化结构；回退可通过单独撤销本阶段提交完成，旧会话无需迁移。
- 自动验证通过：`app.js`、`src/ui/messages.js` 和 `src/ui/markdown.js` 语法检查；前端模块测试 144 项；`npm run check:frontend` 的 bundle 构建、新鲜度与语法门禁；此前同一实现还通过完整回归 900 项及 236 个子测试。用户确认图片显示正常后继续拆分主线。
- 人工验证过程中发现的 ICO 模型输入失败与本批显示事件调整无关：浏览器可显示 `image/x-icon`，但上游模型不接受该 MIME，后续轮次还会随历史上下文重复发送。该兼容问题已单独记录在 `TODO.md`，本批没有修改会话文件或扩大修复范围。

**涉及文件**：`app.js`、`src/ui/messages.js`、`src/ui/markdown.js`、`tests/test_frontend_modules.py`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 01:08 · Codex

### app.js 阶段 6D-2：bundle 启动临时全局收口

- 默认页面改为局部启动闭包创建 bundle 脚本，并以一次性 `error` / `load` 监听分别处理资源加载失败和初始化失败；不再通过内联 `onerror` 或 `window.__codeUseClassicFrontend` 暴露回退入口。
- `src/frontend-entry.js` 在全部依赖与 `app.js` 完成同步初始化后写入 `data-code-frontend-ready` DOM 属性，替代仅供启动检查使用的 `window.Code.frontendBundleLoaded`。加载完成但就绪属性缺失时仍按既有 `bundle-init` 原因跳转，资源失败仍使用 `bundle-load`。
- 经典回退页继续由构建脚本用 31 个原始脚本替换完整启动块；默认页、预览页和经典页的资源路径转换规则不变。本批没有删除经典资源、原始脚本或 `window.Code` 模块命名空间，也没有修改 EXE 资源、API、JSONL、工具协议和持久化数据；可通过单独撤销本阶段提交回退。
- 新增可执行启动闭包行为测试，覆盖 bundle 正常就绪、资源加载失败、初始化失败、一次性监听、同步顺序属性和回退原因；前端模块测试 145 项通过，`npm run check:frontend` 的构建、新鲜度和 bundle 语法门禁通过，完整回归 901 项及 236 个子测试通过，3011 默认页、bundle 和经典回退资源均返回 HTTP 200。
- 用户刷新默认页面并验证经典回退页面后确认均正常，6D-2 人工验收通过。

**涉及文件**：`index.html`、`src/frontend-entry.js`、`tests/test_frontend_modules.py`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 01:57 · Codex

### app.js 阶段 6D-3：AgentRuntime 顶层全局收口

- `agent-runtime.js` 在既有 `Code.agent` 命名空间中注册冻结运行时对象，缺少命名空间时明确失败；删除独立的 `window.AgentRuntime`。`app.js` 在初始化顶部取得单一 `agentRuntime` 局部引用，原 24 个运行时消费点全部迁移到该引用。
- 运行时对象公开的九个方法及其实现没有变化，`agent-runtime.js` 文件名、`namespace.js → … → agent-runtime.js → app.js` 加载顺序、bundle 入口、经典回退的 31 个脚本和 EXE 资源清单均保持不变。本批没有修改 AgentRun、SSE、API、JSONL、工具协议、权限或持久化数据；可通过单独撤销本阶段提交回退。
- 新增运行时所有权行为护栏，验证缺少 `Code.agent` 时给出稳定错误、正常加载只注册 `Code.agent.runtime` 且不产生顶层全局；相关运行时、主 Agent、后台子 Agent 与 P0 定向测试 202 项通过，JavaScript 语法和 `git diff --check` 通过。
- `npm run check:frontend` 的构建、新鲜度与 bundle 语法门禁通过，生成 bundle 无旧全局残留；完整回归 902 项及 236 个子测试通过。3011 默认页、bundle 与经典回退资源均返回 HTTP 200，生成经典页保持 31 个脚本和既有末尾顺序。
- 用户分别在默认 bundle 页面和经典回退页面完成包含工具调用的真实对话，确认均无问题，6D-3 人工验收通过。

**涉及文件**：`agent-runtime.js`、`app.js`、`tests/test_frontend_modules.py`、`tests/test_subagent_frontend.py`、`docs/APP_JS_SPLIT_PLAN.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`

## 2026-08-02 03:38 · Codex

### GPT 基础价与九个分组倍率联动分析

- 基于最新完整组合快照完成可复现核算：共 94 条记录，其中官方 35 条、12 条启用渠道 59 条，14 个来源全部 `success`，无失败和重复记录键；候选倍率继续按固定汇率 `6.7`、目标毛利 15%、覆盖模型/渠道/档位/输入输出缓存读取后取最大值并向上取整两位计算。
- `gpt-5.6-terra` 可分阶段处理：基础档候选为 `13.4 / 80.4 / 1.34`、长上下文档为 `26.8 / 120.6 / 2.68` CNY/MTok；普通 `GPT-Codex` 维持 `0.06`，优质组需先从 `0.08` 调至 `0.09`。该方案下优质组其他 GPT 模型售价上涨 12.5%，Terra 在该组最终售价较当前下降 10%。
- `gpt-5.6-luna` 暂不建议录入：若 Terra/Luna 同时更新且沿用现有共享分组，`GPT-Codex` 与 `GPT-Codex-优质` 安全倍率候选将分别升至 `0.19`、`0.36`，会让同组其他未改基础价 GPT 模型售价约上涨 216.7%、350.0%。两个候选均由 BoxYing 暂定公开价驱动，必须先用供应商账户实际余额扣减复核，不能视为正式线上值。
- Claude、Claude-Max、Gemini-反重力、Gemini-GCP、Grok、国产模型当前倍率保持不变；DeepSeek 常态反算候选约为 `1.15`，本轮仍保持 `1.18`，等待高峰 2 倍扣费单独核算；Qwen 限时折扣继续只列候选。
- 分析产物包含执行完成的 Jupyter 笔记、中文决策报告和已通过结构校验的报告数据；本阶段没有读取或输出真实 Key，也没有修改 Workbar 模型基础价、分组倍率、渠道或其他线上配置。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/outputs/20260802/workbar-base-group-pricing-analysis.ipynb`、`../../模型定价/渠道采购体系/outputs/20260802/workbar-base-group-pricing-analysis.md`、`../../模型定价/渠道采购体系/outputs/20260802/workbar-base-group-pricing-artifact.json`

## 2026-08-02 04:05 · 用户操作 / Codex 复核

### Terra 分阶段调价与模型广场反算验收

- 用户已在 Workbar 后台把 `gpt-5.6-terra` 基础档更新为输入/输出/缓存读取 `13.4 / 80.4 / 1.34`，长上下文档更新为 `26.8 / 120.6 / 2.68` CNY/MTok；同时把 `GPT-Codex-优质` 从 `0.08` 调至 `0.09`，普通 `GPT-Codex` 保持 `0.06`。
- 模型广场详情页逐项反算通过：普通组基础档为 `0.804 / 4.824 / 0.0804`、长上下文档为 `1.608 / 7.236 / 0.1608`；优质组基础档为 `1.206 / 7.236 / 0.1206`、长上下文档为 `2.412 / 10.854 / 0.2412`，全部与基础价乘对应分组倍率一致。
- 模型广场总览同时确认 `gpt-5.6-luna` 仍按旧基础价计费：普通组显示 `0.402 / 2.412`、优质组显示 `0.603 / 3.618`，本次没有把 Luna 候选价提前录入。
- 当前配置已消除 Terra 优质组低于 15% 目标毛利的已知风险；`0.09` 已通过 BoxYing-Codex-Premium 输入/输出实扣确认，缓存读取连续两次未观测到，后续随正常使用补证，不再用重复短测阻塞本次变更。
- 04:10:26 的首笔定向请求已同时取得 Workbar 与 BoxYing 消费记录：渠道、模型和 Token 数均一致，输入/输出为 `6165 / 12`；Workbar 收费 `¥0.007522`，BoxYing 实扣 `¥0.006238`。按其基础目录价 `2.5 / 15 CNY/MTok` 与上游分组倍率 `0.4` 计算，理论采购成本为 `¥0.006237`，与实扣仅差 `¥0.000001`；按显示金额计算实际毛利约 17.07%，确认 `GPT-Codex-优质=0.09` 对基础档输入/输出满足 15% 目标毛利。连续两次普通请求均未命中缓存；由于输入、输出和目录缓存读取相对官方基础价对应同一倍率比例，缓存暂记为“未观测”，不影响本轮 `0.09` 的安全性结论。

**涉及记录**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/快照/模型定价/workbar-model-pricing-20260802.json`、`../../模型定价/渠道采购体系/快照/分组定价/workbar-gpt-group-pricing-20260802.json`

## 2026-08-02 10:17 · Codex

### Workbar 缓存回退毛利修正与价格变化处理规则

- 根据用户展示的当前 Workbar 配置确认，`gemini-3.1-flash-lite` 没有显式启用缓存读取价格；后台禁用字段中的 `$0.3` 只是表单占位值，不是有效售价。New API 未配置模型 `CacheRatio` 时按默认值 `1` 结算，因此缓存读取销售价应回退为普通输入基础价乘分组倍率。
- 采购体系总表和工作簿公式已按上述口径修正：`ApiNebula-Gemini-GCP / gemini-3.1-flash-lite` 的基础缓存价保持空值，实际缓存销售价为 `1.675 × 0.27 = 0.45225 CNY/MTok`；输入、输出、缓存读取与最低毛利率均约 `17.08%`，15% 目标毛利所需倍率约 `0.2634`，当前 `Gemini-GCP=0.27` 可覆盖，不需要线上改价。
- 价格变化处理已固化为三条主线：官方价变化而上游不变时调整模型基础价并重算共享分组倍率；官方价不变而上游变化时保持基础价、只按实际采购成本重算倍率；添加、删除或调整上游渠道时重新核对准入、模型范围、故障域和最坏成本包络。渠道只要仍可能承接正常或故障切换流量，就继续参与倍率计算。
- 同批官方/上游事实生成了独立的缓存回退修正版总表，四张工作表公式错误扫描为 0，采购体系完整回归 69 项通过、1 项因本环境缺少可选 `jsonschema` 依赖而跳过。本轮没有读取真实 Key，也没有修改 Workbar 线上配置。

**涉及文件**：`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/pricing_master.py`、`../../模型定价/渠道采购体系/工具/pricing_master_workbook.mjs`、`../../模型定价/渠道采购体系/tests/test_pricing_master.py`、`../../模型定价/渠道采购体系/快照/模型定价/workbar-model-pricing-20260802.json`、`../../模型定价/渠道采购体系/规范/04_成本核算与定价.md`、`../../模型定价/渠道采购体系/规范/07_模型价格监控与变更提醒.md`、`../../模型定价/渠道采购体系/outputs/20260802/RUN-20260802T011341Z-CACHE-FALLBACK/`

## 2026-08-02 10:44 · Codex

### 定价流水线操作文档与跨 Agent 自助接手收口

- 新增定价流水线唯一运行手册，统一新会话和不同 Agent 的最短读取顺序、事实优先级、当前有效快照与运行、权限边界、环境依赖、运行前后清单、ByteCatCode/Workbar 快照流程、失败恢复和阶段收口；已完成阶段不再依赖上一会话重复编写交接。
- 当前明确基线为 `RUN-20260802T011341Z-CACHE-FALLBACK`：35 个模型、59 条渠道模型映射、94 条归一记录和 15 个来源状态，全部 `success`。未提交、未在日志中引用或来源不完整的输出不得自动替代该基线。
- README 已消除两套路径不一致的完整流水线命令，日常采集统一指向新手册；官方/上游/监控器独立命令仅保留为冻结样本回归和来源故障诊断。工作簿生成被明确为 `pricing-master.json` 的独立确定性步骤，JSON/CSV 和历史原始证据仍是长期事实源。
- 渠道准入流程已移除不存在的旧工作簿页签引用，改为使用人工协作 SOP、标准测试模板、严格证据汇总和 3/5/7 天扩样门槛；价格采集不能替代模型身份、跨时段稳定性、SSE、Code 工具闭环、账单一致性与故障切换证据。
- 同步修正旧缓存预警叙述：`gemini-3.1-flash-lite` 的上游缓存采购价保留，但 Workbar 未启用独立缓存收费时按 `CacheRatio=1` 回退普通输入基础价，当前最低毛利约 17.08%；`gpt-5.4-mini` 的缓存负毛利预警仍保留。本轮只修改文档、索引和完整性清单，不读取真实 Key，不修改采集逻辑或线上配置。

**涉及文件**：`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/MANIFEST.sha256`、`../../模型定价/渠道采购体系/规范/01_证据与数据治理.md`、`../../模型定价/渠道采购体系/规范/02_技术评测与准入.md`、`../../模型定价/渠道采购体系/规范/04_成本核算与定价.md`、`../../模型定价/渠道采购体系/规范/07_模型价格监控与变更提醒.md`、`../../模型定价/渠道采购体系/规范/08_定价流水线运行与AI接手手册.md`

## 2026-08-02 11:30 · Codex

### 定价监控确定性触发状态机与调度/通知契约

- 在独立仓库 `模型定价/渠道采购体系` 新增只消费现有完整记录、语义差异和总表的确定性触发状态机，不改写九家官方/12 条启用渠道采集器，也不接触真实 Key。语义差异首次出现后会保存当前记录指纹，下一轮即使滚动差异为“无变化”仍能完成跨轮确认。
- 固化 P0/P1/P2：受本次变化影响的渠道模型行最低毛利小于 0 或删除后无启用渠道时，P0 立即产生待确认动作并在 5 分钟复核；P1 在 10 分钟确认；P2 在下一探测窗口确认。最低毛利只核算本次变化的受影响行，避免无关历史风险重复报警。
- 固化来源第一次/第二次失败后 10/30 分钟重试、连续三次或超过 60 分钟确认告警、恢复只通知一次；同时覆盖 ByteCatCode 到期前 24 小时与到期后提醒、未来价格生效动作、确定性事件去重、完整状态转换历史和单实例并发合并重跑。
- 默认策略定义北京时间每日 01/05/09/13/17/21 点上游探测、09:00 全量运行（覆盖同刻探测）和周一 09:30 健康摘要。当前实现只输出供外部调度器/通知器消费的待执行动作；未创建 Windows 计划任务、未接入通知渠道、未修改 Workbar 或其他线上配置，这两项继续保留在 `TODO.md`。
- 10 项触发器定向测试及采购体系完整回归 79 项全部通过；使用当前有效历史基线完成只读集成验证，基线摄取为 0 个变化事件，下一工作日 09:31 只产生 09:00 全量与周一 09:30 摘要动作，没有重复生成 09:00 上游探测。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/pricing_trigger.py`、`../../模型定价/渠道采购体系/模板/pricing-trigger-policy.example.json`、`../../模型定价/渠道采购体系/schema/pricing-trigger-event.schema.json`、`../../模型定价/渠道采购体系/schema/pricing-trigger-state.schema.json`、`../../模型定价/渠道采购体系/规范/09_定价监控触发与通知契约.md`、`../../模型定价/渠道采购体系/tests/test_pricing_trigger.py`

## 2026-08-02 13:40 · Codex

### 定价固定任务离线补跑与小时探测不补跑

- 固化离线恢复口径：每日 09:00 全量和周一 09:30 摘要在电脑或调度器恢复后只补最近一个已到期槽，不逐日、逐周回放更早缺口；显式回写实际完成槽，避免补跑完成时间被误记为当前日期。
- 01/05/09/13/17/21 点小时探测不补跑，只允许在对应时刻后的短执行窗口内启动，默认窗口为 15 分钟；超过窗口视为本次跳过，等待下一个探测时刻。09:00 全量仍覆盖同刻探测。
- 新增每日、每周补跑和探测超窗三类回归，触发器定向测试 13 项、采购体系完整回归 82 项全部通过；尚未创建 Codex 或 Windows 计划任务，仍需用户另行授权。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/pricing_trigger.py`、`../../模型定价/渠道采购体系/模板/pricing-trigger-policy.example.json`、`../../模型定价/渠道采购体系/规范/08_定价流水线运行与AI接手手册.md`、`../../模型定价/渠道采购体系/规范/09_定价监控触发与通知契约.md`、`../../模型定价/渠道采购体系/tests/test_pricing_trigger.py`

## 2026-08-02 14:20 · Codex

### 定价 Codex 计划任务与确定性调度入口部署

- 新增 `pricing_scheduler.py` 作为唯一计划执行入口：每日任务实时采集官方与上游；小时探测复用上一轮官方标准记录，只实时采集上游；找不到官方缓存时安全降级为完整采集；同一唤醒同时存在固定任务补跑和当前探测时只执行一次采集并回写两个槽。
- 每周健康摘要由脚本根据最新总表、来源状态和触发事件确定性生成。调度入口增加最长 1 小时的独立互斥租约，防止每日长任务与周报或相邻探测重叠采集。
- 已在本地 `api中转站` 项目创建并启用三个 Codex 任务：01/05/13/17/21 点上游时段唤醒、每日 09:00 全量唤醒、周一 09:30 健康摘要唤醒；09:00 探测仍由全量覆盖。任务只调用统一入口，不读取或输出真实 Key，不操作网页，不修改 Workbar 线上配置。
- 采购体系完整回归 86 项、Python 语法、差异格式及 463 项清单校验全部通过。独立通知器和通知成功后的动作 `ack` 尚未实现；当前由计划任务结果页展示执行结果和待处理事件。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-02.md`、`../../模型定价/渠道采购体系/工具/pricing_pipeline.py`、`../../模型定价/渠道采购体系/工具/pricing_scheduler.py`、`../../模型定价/渠道采购体系/tests/test_pricing_pipeline.py`、`../../模型定价/渠道采购体系/tests/test_pricing_scheduler.py`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/MANIFEST.sha256`、`../../模型定价/渠道采购体系/规范/08_定价流水线运行与AI接手手册.md`、`../../模型定价/渠道采购体系/规范/09_定价监控触发与通知契约.md`
