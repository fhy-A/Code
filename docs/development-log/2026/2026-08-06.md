# 2026-08-06

## 2026-08-06 00:33 · Codex

### Harness H3-2C1 上游失败与 non-action 恢复证据收口

- 新增独立、严格版本化的 `h3-2c1-upstream-failure-non-action` v1 evidence profile、schema 与 suite；事件部分继续使用现有单 Run fixture v1 payload，扩展的 `sourceFacts` 不进入默认 fixture v1 通用协议。H3-2C1 独立基线为 6 条轨迹、26 个事件、16 个检查点、16 次检查点恢复和 0 个显式 `recoveryPoints`，不并入默认单 Run或 multi-run 计数。
- 六条轨迹分别冻结普通 401 `config_error`、429/502 `upstream_error` transient、首响应超时 `model_response_timeout`、empty 恢复与 reasoning-only 恢复。两条 non-action 轨迹均按当前生产顺序从 `model_recovery` 直接进入下一次 `model_started`，不插入 `model_pending`，成功轮 outcome 为 `completed`；默认 suite 中原 `model-non-action-recovery` 保留为历史投影样本。
- `sourceFacts` 不是说明性字段：本地假上游输入使用独立 `runtimeInput.httpStatus/httpMessage`，Model Runtime 与 AgentRun 集成测试直接消费同一 case 数据，再核对实际 `upstreamStatus`、`runtimeErrorCode`、`runtimeTransient`、AgentRun `errorCode` 和规范耐久事件顺序。401 模型无权正文仍优先分类为 `model_access_denied`；429/502 虽得到相同 AgentRun 投影，其 HTTP 差异由 Runtime 集成层独立证明。
- 代表性 pre-event fallback 仅覆盖 502：两个合成 Key、两次本地调用，首个失败发生在 Runtime 产生事件前，第二个 Key 完成；没有扩展为所有 HTTP 状态、真实供应商或网关重试结论。全部 fixture 均为脱敏合成数据，测试只访问本地假上游，不调用真实模型、工具、外部网络或浏览器。
- fixture suite hash 为 `3278b2cfed32b5d06d8c0e0b4c07f84b065b3c0f41803d13bbbe211c8879a6c1`，suite replay hash 为 `caa4f57850729a6d9452030418f914b42061fae0c64e410e839d892d38b97177`；六条轨迹状态 hash 依次为 `4ee23358e8e2263316e27fbfd2d55ab703dcb555f40b5412fe0a0899157d049e`、`5f76190e174310644acca65d82ece61d5334a5039ed45af6d2aa003e42db4088`、`2c924318f4433e84503fe3b4ff744075f9590dd87b9bb59c65aaecc1ca05bc9e`、`9920c097c0a31748b5b48a30eccd78b402a7add99b3928de77c85cc728569293`、`186644bcc433bd0fe9e16fc965ef7a9dfdc45cd4b7b6b9fff1fc5f8b15f1c0c5`、`29257088abe58ed97c3e9d4c9b337815556e38db4b6f0fc83e1007f4eb686456`。
- 默认单 Run仍为 17 条轨迹、124 个事件、25 个检查点、25 次检查点恢复和 4 个显式恢复点，fixture/replay hash 仍为 `c5136af7d5abe6f055e76e230939f7aa5d4cf1c4b2bb832c492819d5604eeffc` / `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；B1/B2 的 fixture、suite replay、复合状态及各 Run hash 也保持不变。定向 fixture/replay 为 `15 passed, 56 subtests passed`，Harness/投影组合为 `96 passed, 199 subtests passed`，Model Runtime/Agent 协议为 `21 passed, 160 subtests passed`，完整 AgentRun 集成为 `98 passed, 60 subtests passed`；四套 CLI replay、Node 语法和 `git diff --check` 均通过。
- 本阶段没有修改生产代码、默认单 Run schema/suite/runner、B1/B2、`package.json`、H4 或发布脚本。完成声明仅覆盖合成 replay 与本地假上游集成，不覆盖真实网络、浏览器/DOM、页面刷新、Runtime 原始事件恢复或发布门禁。

**涉及事实源**：`../../harness/h3-2c1-upstream-failure-non-action-replay.md`、`../../harness/h3-1-replay-runner.md`、`../../../tests/fixtures/harness/upstream-failure-recovery-trace-suite.json`、`../../../tests/test_harness_upstream_failure_replay.py`、`../../../tests/test_model_runtime.py`、`../../../tests/test_agent_runtime.py`、`../../../TODO.md`

## 2026-08-06 00:27 · Codex

### 定价采集与计划任务操作报告优化验收归档

- 用户确认记录本阶段结果。已完成事实以 2026-08-05 日志中的“定价官方价变化探测与计划任务可操作报告收口”为详细依据，本条只记录阶段验收，不重复复制实现说明。
- 验收基线保持为：CC Switch 本地社区缓存只承担 35 个精确模型的变化探测；官方价采用“复用已确认记录、按受影响厂商复核、异常完整回退、每 168 小时成功完整审计”策略；失败的完整采集不替换确认缓存或重置审计周期。
- 三类 Codex 计划任务继续使用结构化 `operator_report`，区分本轮新增与历史持续事项，按结论、影响、用户动作、安全保护、下次检查和证据路径呈现；时间表、启用状态和唯一调度入口未改变。
- 最终自动验证仍为 95 项单元测试通过，Python 编译、`git diff --check` 和 `MANIFEST.sha256` 校验通过；本地 CC Switch 文件中的 35 个监控模型全部存在。下一次每日 09:00 任务因尚无探测历史基线，将先执行一次九厂商完整官方采集。
- 定价独立仓库已创建本地提交 `a3ef37a feat: 优化定价变化探测与任务报告`，只包含本阶段 12 个实现、测试、规范和清单文件；既有运行输出与历史目录未纳入提交。
- 本阶段未读取真实 Key，未修改 workbar 线上基础价、分组倍率或渠道配置，未进行 Git 推送、标签或发布。渠道正式准入活动交接继续保留，不被本次定价流水线收口覆盖。

**涉及事实源**：`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/规范/08_定价流水线运行与AI接手手册.md`、`../../模型定价/渠道采购体系/规范/09_定价监控触发与通知契约.md`、`TODO.md`
