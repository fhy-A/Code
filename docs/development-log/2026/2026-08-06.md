# 2026-08-06

## 2026-08-06 22:53 · Codex

### Harness H4-2 模型流式刷新恢复收口

- 默认 bundle 新增三条确定性 Chromium 生命周期：首个正文增量前刷新、已有两段正文后刷新并继续追加、已有两段正文后刷新再取消。三条场景复用真实生产 CodeHandler、AgentRun、Model Runtime 和前端投影；每例 AgentRun POST 1、Runtime POST 0、上游 chat 1、工具 0，刷新前后脱敏 AgentRun/Runtime ID 保持唯一且相同，没有第二次模型请求、重复 assistant 或工具副作用。
- `app.js` 只在父 AgentRun 快照和耐久 `model_started` 确认同一 active Runtime 时复用已有 assistant 并恢复内存流式投影；非 active、其他 Run、普通重放和终态消息不误复活。`agent-runtime.js` 对既有 Runtime 先用当前快照累计 `result` 追赶，再从快照 `nextCursor` 继续轮询，completed/failed/cancelled 分别收敛，不创建新 Runtime 或合成耐久事件。
- 活动 Session JSONL 按现有设计不保存部分 assistant 正文，也不写 `streaming/_streamProjection`；after-second 检查点由 Runtime 快照证明前两段已经到达且 `nextCursor=2`，刷新可见前缀由累计 `result` catch-up 恢复。成功终态才持久化完整三段正文，临时字段始终不存在。
- 三条场景共同冻结 DOM 正文不得回退：首增量前从空前缀继续，两段后刷新逐样本验证严格 `startsWith`，取消后保留已有两段并只追加一个 `[Output paused]`。取消场景固定唯一 DELETE、AgentRun/Runtime cancelled、无 `model_completed` 或成功终答；本地合成上游释放阻塞闸门时可能有一个在途第三片段先于取消收敛落盘，该诊断不要求产品必须追加第三段，也不支持“点击取消后绝无新字节”的更强结论。
- R004 两轮标准入口均先通过 infra 自检，再以 `6 passed`、`retries=0` 完成 H4-1 三条基础冒烟与 H4-2 三条刷新场景；两轮取消延迟为 242 ms / 243 ms。`npm run check:frontend`、前端模块与 P0 `199 passed`、Node/Python 语法和差异检查通过；完整 Python `1113 passed, 739 subtests passed` 的生产及 pytest 输入哈希未改变。每轮子进程、两端口、临时根和 output 都清理归零。
- 本阶段只证明默认 bundle 的同进程 Runtime 快照恢复；classic 仍只通过 H4-1 纯文本基础冒烟。不覆盖服务重启、Runtime 原始事件持久化、跨进程恢复、工具流式刷新、问卷/授权、队列/并行/Child、图片、压缩、真实 CDN/完整 Markdown、浏览器崩溃、EXE 或发布门禁；没有修改 `server.py`、AgentRun/Runtime 协议或 Session JSONL 格式。

**涉及事实源**：`../../harness/h4-2-model-stream-refresh-recovery.md`、`../../harness-engineering-optimization-plan.md`、`../../../app.js`、`../../../agent-runtime.js`、`../../../tests/e2e/h4/`、`../../../tests/test_frontend_modules.py`、`../../../TODO.md`

## 2026-08-06 21:02 · Codex

### Harness H4-1 浏览器 E2E 基础设施与首批冒烟收口

- 以精确开发依赖引入 `@playwright/test 1.62.1`，Playwright 管理的 Chromium revision 为 `1234`、浏览器版本为 `151.0.7922.34`；Firefox、WebKit、`node_modules`、浏览器缓存和测试输出不进入提交。唯一标准入口 `npm run test:h4:e2e` 串行执行 infra 自检和三条 Playwright smoke，自检失败会短路且不启动 smoke。
- 每条测试使用独立临时 `data/project/artifacts/home/tmp`，真实生产 `CodeHandler` 与本地假上游仅绑定 `127.0.0.1:0`；Python 子进程使用最小白名单环境和隔离 home/temp，父环境合成 secret 及敏感变量不进入子进程，浏览器阻断全部非 loopback 请求。
- 工具边界只允许生产 `read_file("fixture.txt")` 读取临时 project；未知 action 或路径在委托生产执行器前拒绝。host 控制与退出等待均有界，失败诊断、context 关闭及 host 停止使用可靠 `finally`，`stop()` 幂等并验证子进程退出、活跃子进程为零、两端口关闭和临时根删除；初始化故障注入覆盖相同清理路径。
- 首批三条冒烟为默认 bundle 纯文本、默认 bundle 单只读工具和 classic fallback 纯文本。两轮标准入口均先通过 infra 自检，再以 `3 passed`、`retries=0` 完成 smoke，耗时约 `6.0s` / `6.2s`；文本场景各 1 次模型请求，工具场景 2 次模型请求、1 次工具执行，用户/阶段/工具/结果/最终回答 DOM 计数和顺序均固定，bundle/classic 标记正确。
- Playwright 内置 trace 保持关闭；失败时只 best-effort 写入脱敏截图、console 和自定义 `sanitized-diagnostics.json`。生产 Markdown CDN 被非 loopback 策略阻断，测试注入最小 `marked` API 适配器，因此本阶段不证明真实 CDN 或完整 Markdown 行为。
- 最终验证包括两轮 H4 标准门禁、`npm run check:frontend`、前端模块 `169 passed`、完整 Python `1111 passed, 739 subtests passed`、Node/Python 语法、差异及退出清理审计。H4-1 未修改生产代码、AgentRun/Runtime、Session JSONL、Harness replay 或 `release.py`；不覆盖自动 bundle-load 降级、页面刷新/重连、问卷、授权、停止、队列/并行/Child、图片、压缩、EXE 或发布门禁。

**涉及事实源**：`../../harness/h4-1-browser-e2e-smoke.md`、`../../harness-engineering-optimization-plan.md`、`../../../playwright.h4.config.cjs`、`../../../tests/e2e/h4/`、`../../../package.json`、`../../../TODO.md`

## 2026-08-06 11:38 · Codex

### Harness H3 最终覆盖与 replay 发布门禁收口

- 完成总体方案第 6.3 节十五类必备轨迹的最终证据映射，逐项区分 Run replay、multi-run replay、生产 Runtime/AgentRun 集成、真实临时持久化、生产 HTML 投影与隔离人工显示证据。H3 完成声明限于非浏览器状态、replay、生产集成、持久化和投影；第 4 类真实工具展开生命周期，以及问卷、压缩、图片和队列的真实刷新/DOM 行为明确保留给 H4，未被误报为通过。
- `release.py` 新增独立 `run_harness_replay_gate()`，正式、非 `--skip-tests`、非 `--dry-run` 路径按“前端门禁 → 完整 pytest（180 秒）→ `npm run verify:harness-replay`（30 秒）→ diff → 语法 → EXE”执行。replay 返回非零、超时或无法启动均立即阻断且不会进入 EXE 构建；`--skip-tests` 继续同时跳过 pytest/replay，`--dry-run` 不新增 replay 执行。
- 发布测试新增精确命令、30 秒超时、成功继续、非零/超时/启动失败、顺序、`--skip-tests`、`--dry-run` 与失败不进入构建的定向契约。B1/B2/C1 独立 CLI 不在发布脚本中重复执行，继续由默认完整 pytest 收集；独立门禁专门验证默认单 Run npm 命令接线与 Node 进程。
- 默认 replay CLI 基线保持为 17 条 fixture、124 个事件、25 个检查点、25 次检查点恢复和 4 个显式恢复点，suite hash 为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`，本轮耗时约 730 ms。发布测试定向结果为 `16 passed, 7 subtests passed`；新增门禁后的完整回归已完成 `1111 passed, 739 subtests passed`，纯文档收口没有改变生产或测试文件，沿用该结果。
- H4 浏览器 E2E 必须使用独立门禁、独立超时和可靠的临时 data/服务/浏览器进程清理，并分别覆盖默认 bundle 与经典回退。本阶段未安装 Playwright、未启动 H4、未构建或发布 EXE。

**涉及事实源**：`../../harness/h3-final-coverage-release-gate.md`、`../../harness/h3-1-replay-runner.md`、`../../release-guide.md`、`../../../release.py`、`../../../tests/test_release_script.py`、`../../../TODO.md`

## 2026-08-06 10:27 · Codex

### Harness H3-2D3 手动压缩失败与持久化边界收口

- 新增独立、严格版本化的 `h3-2d3-manual-compaction-failure-boundaries` v1 evidence schema、脱敏 fixture 和定向测试，固定执行 19 个 compact/summary/archive/apply/save、Session 切换、响应丢失、pending 阻止、显式重试与操作锁异常场景；suite hash 为 `50ff1567e7477d6438bfc7e8175a3936f04177a089a4b4ae5acc0a93a0a2a657`。
- 手动压缩固定绑定发起时的目标 Session；messages、stats、lastUsage、marker、archive、save 和 retry 都通过目标 Session 访问器处理。`cacheActiveSessionState()`、`saveCurrentSession()` 与 checkpoint 清理复用统一 per-Session 保存链，保存 payload 同时包含最新 messages 与 runState；页面切换不取消目标操作，也不让后台目标覆盖当前页面。
- compact 请求、拒绝、summary 构造和 apply 失败均保留原历史并写入稳定、脱敏 failed marker；archive 失败保持非阻断并持久化警告；正常终态保存首次失败后自动重试最新状态一次，两次失败才记录 persistence pending。显式重试只执行当前目标 Session 的全量保存，不再 compact、archive、生成 summary、创建 marker或清零统计；响应丢失场景经真实临时 `save_session` 与 JSONL 复读证明最终收敛且 summary/marker 不重复。
- 修正 `status: failed + persistenceStatus: failed` 的组合投影：中文为“上下文压缩失败，且失败状态尚未保存”，英文为 `Context compaction failed, and the failure state has not been saved`，保留“重试保存 / Retry save”按钮且不显示“上下文已压缩”。定向场景固定 compact 1、archive 0、summary 0、save 3；显式重试成功后 marker 仍为 failed，只清除 persistence 失败字段，原历史、后续消息、stats 与 usage 不被旧快照覆盖。
- `compactConversation()` 与 `retryManualCompactionPersistence()` 在操作登记后的准备、渲染和保存路径使用可靠 `try/catch/finally`。首次 render 注入异常后，操作锁归零、未处理 Promise rejection 为零，同一 Session 可重新打开唯一确认处理器，取消后所有监听器清理且不增加 compact/save 调用。
- H3-2D2 只受控更新结构证据：源码切片 SHA `1264f4f9…052b8` → `6df39b85…102f5`、长度 `5732` → `20676`、replayHash `36e0206d…d4c26` → `db2a14b1…e967a`、fixture 文件 `56584599…a586` → `1fac9a92…0384`、test 文件 `f681fab3…c85b` → `79ad0a04…b328`。源历史、最终 messages、summary/marker、archive payload/record、save/JSONL、model context/API payload、UI HTML/可见文本全部领域哈希不变。
- 最终验证为 D2+D3 `12 passed, 38 subtests passed`、前端模块 `169 passed`、保存链/旧结构 `128 passed, 4 subtests passed`、全部 Harness `109 passed, 272 subtests passed`、完整 Python `1109 passed, 737 subtests passed`。默认单 Run `17/124/25/25/4` 和 C1/B1/B2 CLI 哈希不变；`npm run check:frontend`、独立 `npm run verify:frontend`、经典回退与 bundle 语法通过。隔离本机临时页面确认中英文即时切换、切回中文和重试按钮处理中禁用/完成后恢复，compact/archive/summary 均未增加；临时页面、服务和脚本已清理，`dist/` 不进入提交。
- 本阶段没有修改 `server.py`、Session JSONL 顶层结构、HTTP endpoint、AgentRun/Runtime、Run replay runner/schema、`beforeunload`/`sendBeacon`、`package.json`、H4 或发布脚本。完成声明不覆盖真实网络、真实摘要质量、页面刷新、完整 DOM 生命周期、Runtime 原始事件恢复、H4 隔离浏览器 E2E 或发布门禁。

**涉及事实源**：`../../harness/h3-2d3-manual-compaction-failure-boundaries.md`、`../../harness/h3-1-replay-runner.md`、`../../../tests/fixtures/harness/manual-compaction-failure-boundaries-evidence.json`、`../../../tests/test_harness_manual_compaction_failure_boundaries.py`、`../../../TODO.md`

## 2026-08-06 07:39 · Codex

### Harness H3-2D2 手动压缩成功路径与完整可见历史契约收口

- 新增独立、严格版本化的 `h3-2d2-manual-compaction-visible-history` v1 evidence schema、脱敏 fixture 与一份定向测试。测试执行当前 `app.js` 中从唯一 `compactConversation()` 起点到唯一 `projectOptimisticFirstMessage()` 边界的精确源码切片，不是公开模块 API 导入；切片包含真实 `hideCompactConfirm()`，先在隔离 Node VM 中编译再执行，SHA-256 为 `1264f4f9d7d46c15012b4d0d092d52819f2e8c41daf5c4561ec0286b733052b8`。该哈希只用于提示人工复审，生产实现变化时不得机械更新。
- `apiJson()` 只返回固定脱敏摘要并捕获 compact/archive 请求，`saveSessionState()` 只捕获参数和完成信号，render/streaming/toast/DOM stub 只记录调用或管理最小监听器；它们不修改消息、marker 或 stats。原历史前缀、summary、completed `manual-context-compaction` marker、统计清零和一次性 confirm cleanup 均来自真实源码切片。
- 一份带 `meta.evidenceMessageId` 的 13 条合成历史贯穿完整链路。实际 `/archive` `PUT` payload 与压缩前历史深度相等，随后交给真实 `CodeHandler.archive_session()` 并用生产 JSON/JSONL 入口在临时目录复读；切片传给 save 的最终 15 条 messages 再经生产 `serializeSessionMessages()/buildSessionSavePayload()`、临时 JSONL 往返，保持原 13 条历史为精确前缀，只追加一个 summary 和一个 completed marker。
- 后续模型上下文先由生产 `getModelContextMessages()` 按 evidence ID 核对范围，再经 `mapMessageForApi()` 以 `role/content` 规范哈希验证旧消息没有泄漏；同一最终消息由生产 `renderUserProjection()/projectMessages()` 生成 HTML，12 个可见文本哨兵各出现一次且顺序不变，summary 不作为普通可见正文，completed marker 只出现一次。HTML 字符串证据不等同于真实 DOM 或浏览器验收。
- fixture/schema SHA-256 为 `56584599d968fd7cfcdfad17cc56ed59a7ec8d18c7ab27b5fbe33d5ec3eda586` / `3b1426da4a914ec89d72988636a18b515c2cbf3a72e66043664b4021fc52eba5`；确定性 replay 状态为 `36e0206d1be87e80bade8a0d6a568fa1f6e2e19747e858964727e0f21b1d4c26`，最终 JSONL/model context/UI HTML 哈希分别为 `3a8f9f210d9e5daccc56789cd4a1ef8c144d89a829f6f55f68eff8d32e58779f`、`8d5972e4b0b9548099d2efbb95ecb1f14ff8d9dbe1bf62c603d3ec93d3108958`、`7253fa07caca7bb89087ce706d45889ada1a305492f2b2bd9204a027a85f651c`。
- 定向测试为 `7 passed, 19 subtests passed`，相关 compaction/model-request/persistence/UI/Session/archive 为 `405 passed, 48 subtests passed`，全部 Harness 为 `104 passed, 253 subtests passed`，完整 Python 为 `1102 passed, 718 subtests passed`；默认单 Run `17/124/25/25/4` 及 H3-2C1、B1/B2 的旧哈希保持不变，CLI replay、Python/Node 语法、空白和差异检查通过。
- 本阶段没有修改生产代码、既有测试、会话 JSONL、Run replay runner/schema、`package.json`、前端或发布脚本。完成声明只覆盖成功路径，不覆盖 compact API失败、archive 失败、最终保存失败、真实摘要质量、真实 HTTP、浏览器、DOM/滚动、页面刷新、Runtime 或发布门禁；手动压缩失败与持久化边界仍待独立确认。

**涉及事实源**：`../../harness/h3-2d2-manual-compaction-visible-history.md`、`../../harness/h3-1-replay-runner.md`、`../../../tests/fixtures/harness/manual-compaction-visible-history-evidence.json`、`../../../tests/test_harness_manual_compaction_visible_history.py`、`../../../TODO.md`

## 2026-08-06 06:41 · Codex

### Harness H3-2D1 七格式图片 MIME 保留与模型投影契约收口

- 新增严格版本化的 `h3-2d1-image-mime-preservation` v1 evidence schema、合成 fixture 和一份数据驱动定向测试；scope 固定为 `seven-format-production-chain`，按 PNG、JPEG、WebP、BMP、GIF、ICO、TIFF 的固定顺序执行 7 个案例、7 条消息、7 个 `content` 图片、7 个 `_images` 源图片和 7 条临时 JSONL 往返消息，3 个原样送模、4 个转 PNG，全部实际执行且没有 skip。
- 同一批规范消息依次经过生产 `serializeSessionMessages()`、`buildSessionSavePayload(persistMessages=true)`、临时 `write_jsonl()/read_jsonl()`、`_project_model_payload_images()` 以及 `renderUserProjection()/projectMessages()`；模型 payload 直接使用 JSONL 读取消息原有的 `content`，不从 `_images` 重建。序列化、往返和重复投影后，case 身份、源 MIME/base64、数量、原 data URL、源哈希和源消息均保持稳定，UI HTML 仍引用原始格式图片。
- PNG/JPEG/WebP 的模型输出 MIME、data URL 和解码字节与源完全相同；BMP/GIF/ICO/TIFF 输出可解码 PNG，并通过源选择帧/首页与模型 PNG 的尺寸、规范 RGBA 直接相等证明语义转换。GIF/TIFF 只覆盖第 0 帧/首页，ICO 只覆盖单尺寸。转换后 PNG 编码 SHA-256 仅作当前 Pillow 环境诊断，不作为跨版本语义协议；正式门禁为 MIME、可解码性、尺寸和 RGBA 相等。
- fixture/schema SHA-256 为 `51606a657d95a8c421feef3a8b5415d178efc66e72bfc53dac64a19d009288e1` / `ac367f82b9df44a858186c02e2f6b90cd8150bcfb128ce544ced77defc94c39c`。定向测试为 `6 passed, 32 subtests passed`，相关图片测试为 `12 passed, 7 subtests passed`，完整 Python 回归为 `1095 passed, 699 subtests passed`；持久化、前端模块、既有 Harness、各 CLI replay、Python/Node 语法、空白和差异检查均通过，默认单 Run `17/124/25/25/4` 及 H3-2C1、B1/B2 的旧哈希全部保持不变。
- 本阶段没有修改生产代码、依赖、既有测试、会话 JSONL、Run replay runner/schema、前端、`package.json` 或发布脚本。完成声明不覆盖 SVG/AVIF/HEIC、恶意或损坏输入、GIF 动画、TIFF 多页内容、ICO 多尺寸选择、真实浏览器/DOM、页面刷新、模型、外部网络或发布门禁；第 6.3 节下一项剩余缺口是手动压缩保留完整可见历史，尚未实施。

**涉及事实源**：`../../harness/h3-2d1-image-mime-preservation.md`、`../../harness/h3-1-replay-runner.md`、`../../../tests/fixtures/harness/image-mime-preservation-evidence.json`、`../../../tests/test_harness_image_mime_preservation.py`、`../../../TODO.md`

## 2026-08-06 05:10 · Codex

### Harness H3-2C2 旧 AgentRun 持久化恢复契约收口

- 新增严格版本化的 `h3-2c2-legacy-agent-run-recovery` v1 manifest/schema 与一份定向测试；manifest 只引用既有 `agent-run-v1.json` 至 `agent-run-v4.json` 四份最小 compatibility fixture，不复制或修改其 `record`。它显式冻结 39 个 v4 持久化字段、四份源哈希及 v1/v2/v3/v4 的 19/12/8/6 个缺失字段。
- 四个版本案例分别调用生产 `_agent_run_from_record()`、`_agent_snapshot()` 和 `_agent_run_record()`，独立核对 loader 内部规范化、当前公共投影和 v4 persisted 三层结果，不假定字段集合相同。输入不变性结论只限这四份合成最小 fixture。
- 每个案例均真实执行“旧记录写入临时 `DATA_DIR` → 首次 `_get_agent_run()` → 缓存命中 → v4 持久化 → 清空 `_agent_runs` → 第二次磁盘 `_get_agent_run()` → 再次缓存命中”，合计 4 次完整往返、8 次磁盘加载。所有测试写入位于临时目录，生产 workspace helper 仍被执行。
- 活动 v1 首次加载只追加一个 `waiting_credentials(reason=server_restarted, resumeStatus=tools)`；该事件经 `_append_agent_event()` 时已自动调用 `_persist_agent_run()` 并写成 v4，后续显式 `_persist_agent_run()` 只是重复确认。v2～v4 首次加载不追加重启事件，由显式 persist 完成规范化写回；再次磁盘加载不重复事件，终态 v3 往返前后公共快照稳定。
- H3-2C2 manifest/schema hash 为 `9acdf241c211ccff9528f30a17ad03ce7ddeae16c263f5de4494fdd61c6bddeb` / `a66792fa46e7844479b9320ad93e9dac4b610b664ad6e4e4a4d14d761add26f4`；四份源 fixture hash 原样保持。定向测试为 `5 passed, 20 subtests passed`，H3-2C2 与既有 Harness 为 `101 passed, 219 subtests passed`，完整 AgentRun/协议为 `108 passed, 215 subtests passed`；compatibility、四套 CLI replay、Python/Node 语法与差异检查均通过，所有旧基线不变。
- 本阶段没有修改生产代码、既有 compatibility fixture、AgentRun/JSONL 协议或任何 replay runner/schema。测试确认不启动 worker，不调用模型 Runtime、工具或网络；完成声明不覆盖损坏记录、Session JSONL、worker/工具外部状态、模型、网络、浏览器/DOM、刷新、Runtime 原始事件或发布门禁。

**涉及事实源**：`../../harness/h3-2c2-legacy-agent-run-recovery.md`、`../../harness/h3-1-replay-runner.md`、`../../../tests/fixtures/harness/legacy-agent-run-recovery-suite.json`、`../../../tests/test_harness_legacy_agent_run_recovery.py`、`../../../TODO.md`

## 2026-08-06 00:33 · Codex

### Harness H3-2C1 上游失败与 non-action 恢复证据收口

- 新增独立、严格版本化的 `h3-2c1-upstream-failure-non-action` v1 evidence profile、schema 与 suite；事件部分继续使用现有单 Run fixture v1 payload，扩展的 `sourceFacts` 不进入默认 fixture v1 通用协议。H3-2C1 独立基线为 6 条轨迹、26 个事件、16 个检查点、16 次检查点恢复和 0 个显式 `recoveryPoints`，不并入默认单 Run或 multi-run 计数。
- 六条轨迹分别冻结普通 401 `config_error`、429/502 `upstream_error` transient、首响应超时 `model_response_timeout`、empty 恢复与 reasoning-only 恢复。两条 non-action 轨迹均按当前生产顺序从 `model_recovery` 直接进入下一次 `model_started`，不插入 `model_pending`，成功轮 outcome 为 `completed`；默认 suite 中原 `model-non-action-recovery` 保留为历史投影样本。
- `sourceFacts` 不是说明性字段：本地假上游输入使用独立 `runtimeInput.httpStatus/httpMessage`，Model Runtime 与 AgentRun 集成测试直接消费同一 case 数据，再核对实际 `upstreamStatus`、`runtimeErrorCode`、`runtimeTransient`、AgentRun `errorCode` 和规范耐久事件顺序。401 模型无权正文仍优先分类为 `model_access_denied`；429/502 虽得到相同 AgentRun 投影，其 HTTP 差异由 Runtime 集成层独立证明。
- 代表性 pre-event fallback 仅覆盖 502：两个合成 Key、两次本地调用，首个失败发生在 Runtime 产生事件前，第二个 Key 完成；没有扩展为所有 HTTP 状态、真实供应商或网关重试结论。全部 fixture 均为脱敏合成数据，测试只访问本地假上游，不调用真实模型、工具、外部网络或浏览器。
- fixture suite hash 为 `3278b2cfed32b5d06d8c0e0b4c07f84b065b3c0f41803d13bbbe211c8879a6c1`，suite replay hash 为 `caa4f57850729a6d9452030418f914b42061fae0c64e410e839d892d38b97177`；六条轨迹状态 hash 依次为 `4ee23358e8e2263316e27fbfd2d55ab703dcb555f40b5412fe0a0899157d049e`、`5f76190e174310644acca65d82ece61d5334a5039ed45af6d2aa003e42db4088`、`2c924318f4433e84503fe3b4ff744075f9590dd87b9bb59c65aaecc1ca05bc9e`、`9920c097c0a31748b5b48a30eccd78b402a7add99b3928de77c85cc728569293`、`186644bcc433bd0fe9e16fc965ef7a9dfdc45cd4b7b6b9fff1fc5f8b15f1c0c5`、`29257088abe58ed97c3e9d4c9b337815556e38db4b6f0fc83e1007f4eb686456`。
- 默认单 Run仍为 17 条轨迹、124 个事件、25 个检查点、25 次检查点恢复和 4 个显式恢复点，fixture/replay hash 仍为 `c5136af7d5abe6f055e76e230939f7aa5d4cf1c4b2bb832c492819d5604eeffc` / `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；B1/B2 的 fixture、suite replay、复合状态及各 Run hash 也保持不变。定向 fixture/replay 为 `15 passed, 56 subtests passed`，Harness/投影组合为 `96 passed, 199 subtests passed`，Model Runtime/Agent 协议为 `21 passed, 160 subtests passed`，完整 AgentRun 集成为 `98 passed, 60 subtests passed`；四套 CLI replay、Node 语法和 `git diff --check` 均通过。
- 本阶段没有修改生产代码、默认单 Run schema/suite/runner、B1/B2、`package.json`、H4 或发布脚本。完成声明仅覆盖合成 replay 与本地假上游集成，不覆盖真实网络、浏览器/DOM、页面刷新、Runtime 原始事件恢复或发布门禁。

**涉及事实源**：`../../harness/h3-2c1-upstream-failure-non-action-replay.md`、`../../harness/h3-1-replay-runner.md`、`../../../tests/fixtures/harness/upstream-failure-recovery-trace-suite.json`、`../../../tests/test_harness_upstream_failure_replay.py`、`../../../tests/test_model_runtime.py`、`../../../tests/test_agent_runtime.py`、`../../../TODO.md`

## 2026-08-06 00:27 · Codex

### 定价采集与计划任务操作报告优化验收归档

- 用户确认记录本阶段结果。已完成事实以 2026-08-05 日志中的“定价官方价变化探测与计划任务可操作报告收口”为详细依据，本条只记录阶段验收，不重复复制实现说明。
- 验收基线保持为：CC Switch 本地社区缓存只承担 35 个精确模型的变化探测；官方价采用“复用已确认记录、按受影响厂商复核、异常完整回退、每 168 小时成功完整审计”策略；失败的完整采集不替换确认缓存或重置审计周期。
- 三类 Codex 计划任务继续使用结构化 `operator_report`，区分本轮新增与历史持续事项，按结论、影响、用户动作、安全保护、下次检查和证据路径呈现；时间表、启用状态和唯一调度入口未改变。
- 最终自动验证仍为 95 项单元测试通过，Python 编译、`git diff --check` 和 `MANIFEST.sha256` 校验通过；本地 CC Switch 文件中的 35 个监控模型全部存在。下一次每日 09:00 任务因尚无探测历史基线，将先执行一次九厂商完整官方采集。
- 定价独立仓库已创建本地提交 `a3ef37a feat: 优化定价变化探测与任务报告`，只包含本阶段 12 个实现、测试、规范和清单文件；既有运行输出与历史目录未纳入提交。
- 本阶段未读取真实 Key，未修改 workbar 线上基础价、分组倍率或渠道配置，未进行 Git 推送、标签或发布。渠道正式准入活动交接继续保留，不被本次定价流水线收口覆盖。

**涉及事实源**：`../../模型定价/渠道采购体系/CHANGELOG.md`、`../../模型定价/渠道采购体系/README.md`、`../../模型定价/渠道采购体系/规范/08_定价流水线运行与AI接手手册.md`、`../../模型定价/渠道采购体系/规范/09_定价监控触发与通知契约.md`、`TODO.md`
