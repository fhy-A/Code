# 2026-09-01 开发日志

## 2026-09-01 14:53 · Codex

### 发版元数据自动同步与一致性门禁

- 修正 v0.6.7 发布后 README 版本徽章的历史陈旧 alt：可见 badge URL、`Version 0.6.7` 无障碍文本和 `Code-v0.6.7.exe` 下载名现保持同版；已发布的 release commit、tag、GitHub Release、EXE、`VERSION`、PE 元数据、spec 和发布说明均未修改。
- canonical `update_readme()` 现在只识别恰好一个 `img.shields.io` 版本徽章及具体 `Code-vX.Y.Z.exe` 名称，在该范围内同步目标版本的 badge URL、对应 alt 和全部具体 EXE 名；其他图片 alt、链接、正文及通配 `Code-v*.exe` 不参与替换。相同版本重复执行保持内容幂等，canonical 徽章或具体 EXE 名缺失时 fail-closed。
- dry-run 一致性门禁校验当前旧版本，正式门禁校验目标版本；badge URL、alt、EXE 任一缺失、陈旧或相互不一致都会给出稳定失败并阻断构建。有效 fail-first 为 `11 failures`，覆盖 dry-run 六类漏检、正式四类 badge 漏检和 alt 未同步；修复后直接组 `3 tests`、两套 release 定向 `42 tests` 全部通过，doctor `10/10`、release.py 语法与 Git diff 检查通过。
- 本阶段只形成发布后的本地修复，不运行真实 release CLI 的 dry-run / prepare / publish，不创建或修改远端对象，不触碰受保护用户 data、TODO 或无关 handoff。

## 2026-09-01 13:50 · Codex

### v0.6.7 · manual-compaction Harness UI 证据受控重基线

- v0.6.7 首次 `--prepare` 的完整 Python 门禁在产品代码已通过后仅剩 manual-compaction 冻结 UI 证据漂移：定向失败前基线为 `6 failures / 10 passed / 35 subtests`，完整门禁为 `6 failed / 1877 passed / 1212 subtests`。prepare 已按事务边界回滚，未生成候选 EXE、凭据、tag 或 Release；本阶段没有重新进入 prepare / publish。
- D2 连续两次完整生成逐字段相同，结构化差异精确限定为 `expected.ui.htmlHash` 与派生 `expected.replayHash`；12 个可见哨兵、`visibleTextHash` 以及 counts、source、slice、plan、execution、dispatch、archive、completed、save、model、sideEffects 全部保持。D2 fixture 文件哈希更新为 `01b824a3a2bc8b7bbe0570304e3bcdf00bda520dea8e5345c14f9452055ba496`。
- D3 连续两次完整生成逐字段相同，19 个固定场景只刷新 `uiHash`、既有两个 `pendingUiHash` 及派生 `scenarioHash`，suite hash 更新为 `bf795a33b981e7034196b003088a5a82a73b7221d5ec6784680e7f5ea3dc4985`。以 v0.6.6 只读临时导出独立生成后，19 个移除 UI 派生字段的规范哈希逐项一致，case 顺序、dispatch 身份和 mutation 首差异路径保持；临时导出已清理。
- 更新后 D2+D3 定向测试连续两轮各 `13 tests` 通过；全部 Harness `110 tests` 通过，fixture/schema、精确 slice、19 场景派生链和变异首差异门禁均保持。文档同步为当前确定性基线，未修改生产代码、测试语义、schema、断言数量或发布脚本。
- 本阶段只收口批准的 7 个 Harness fixture / 测试常量 / 专题 / 日志文件。冻结的 `docs/releases/v0.6.7.md` 草稿保持未暂存且哈希不变；受保护用户 data、无关 handoff、3010/3011、Browser、真实模型、网络、push、tag 和 release 均未触碰。

## 2026-09-01 02:05 · Codex

### CODE-043 · 全局表单焦点样式统一

- 文本输入、搜索、文本域和原生下拉的最终焦点反馈统一为现有强调色 `1px` 边框，移除外扩 outline / box-shadow。会话搜索、归档搜索、导入搜索和项目名称编辑由 focus-within 外壳唯一承担边框，内层控件保持透明 / 零宽边界；Settings、Skills 搜索和生图设置中旧的 `1–2px` 外圈也已被同一最终规则收敛。
- System Prompt 只保留强调色底线；composer 的 textarea 不自行描边，外壳聚焦时仅保留单层边框和既有中性表面阴影，不再叠加蓝色外圈。拖拽强调、disabled、checkbox / radio / range / file / button、菜单、错误 / 警告和授权 / 问卷状态均不在新规则内；未修改 DOM、JavaScript、主题 token、数据或协议。
- 四项新直接合同先稳定 fail-first，修复后与 composer / 设置 / 生图及旧阶段兼容组合计 `15 tests` 通过，完整 `tests/test_frontend_modules.py` 为 `413 tests` 无失败；`npm run check:frontend` 的 bundle / classic build、freshness 与 bundle 语法，以及 Git diff 检查通过。
- 现有 3011 内置 Browser 在 Notion 深色 / 浅色抽查文件搜索、会话搜索外壳、composer、Skills 搜索、System Prompt、生图文本输入和原生下拉：可见控件均为单层强调边框且 outline / 外扩 shadow 为 `none`，嵌套 input 的计算边框透明；System Prompt 只有底边，composer 只有中性阴影。非表单模型按钮的点击焦点未被新规则改写，键盘 focus-visible 选择器由直接合同确认保留。最终恢复中文、Notion 深色、默认桌面视口与原普通会话，设置已关闭并保持 Browser 标签打开；未读取或输出 API Key 值。

## 2026-09-01 01:24 · Codex

### CODE-043 · 浅色输入框表面与工具栏分割线收口

- 输入框正文区与底部工具栏之间的顶部分割线已移除，空会话与普通会话共用同一边界。浅色模式下输入容器的静止、hover 与 disabled 底色收敛为当前页面底色，静止态仅保留一层轻阴影；现有 focus ring、拖拽强调、disabled 弱化和深色输入表面 / 静止无阴影语义未改。未修改 DOM、输入 / 发送 / 附件 / 模型 / 权限 / 思考档位逻辑、全局主题令牌或响应式尺寸。
- 直接合同先以缺失新局部规则稳定 fail-first，修复后 composer / 旧阶段兼容组 `6 tests` 通过，完整 `tests/test_frontend_modules.py` `409 tests` 无失败；`npm run check:frontend` 的 bundle / classic build、freshness 与 bundle 语法，以及 Git diff 检查通过。
- 现有 3011 内置 Browser 完成不依赖模型的局部复验：Codex 浅色、Notion 浅色和 Notion 深色均覆盖空会话与普通会话。两种浅色主题的 composer 计算底色与 chat pane 完全一致、静止阴影非 `none`；全部场景的 composer bar 顶边为 `0px`，Notion 深色静止阴影仍为 `none`，各场景 composer 宽度均为 `760px`。最终恢复中文、Notion 深色、默认桌面视口与普通会话，设置已关闭且标签页保持打开。

## 2026-09-01 00:47 · Codex

### CODE-043 · 导入会话窗口优化完成

- “导入会话”收敛为单一高密度窗口：桌面宽度约 `760px`，固定标题区与仅含选择摘要、单一“导入所选”主按钮的 footer，中间列表独立滚动；`740px / 390px` 使用局部响应式规则，不改变全局移动端外壳。关闭与重新扫描复用共享 `close / refresh` 线性 SVG，刷新在所有宽度均为本地化纯图标；来源数量保留为无胶囊底色的普通数字，筛选仅保留“可导入 / 全部”，“显示 x / y”只在搜索或筛选确实缩小结果时出现。
- 来源切换使用 `tab / tabpanel`、动态 `aria-labelledby` 与左右方向键；右上关闭、Escape、遮罩关闭、焦点圈定和关闭后回焦继续保持。来源或刷新触发扫描时，来源、搜索、筛选、选择框、全选、刷新及导入入口统一锁定；键盘切换来源或点击刷新导致触发器被禁用时，焦点暂交关闭按钮，扫描完成后归还原触发器。
- 首次无缓存扫描显示 5 行有限骨架；重开、切换来源或重扫有缓存时继续显示旧列表，以旋转刷新和轻遮罩标明后台扫描，但旧行完全不可选择，成功后才用当前 generation 的新结果原子替换。扫描失败时旧缓存仍只读，可继续搜索、切换来源和重扫，但选择、全选与导入保持锁定，避免把陈旧快照变为执行输入。
- 普通 `available` 行不再显示冗余状态徽标，特殊的已导入、已继续、可更新、更新冲突和旧版状态继续显示；标题 / 日期统一为 `14 / 12px`，长标题 tooltip、选中轻强调、去重、更新冲突、批处理、取消、失败详情与重试业务语义保持。导入 API、扫描来源、缓存数据、持久化与真实导入协议均未改变。

### 自动证据、Browser 验收与边界

- doctor `10/10` 通过。四项直接合同先稳定得到 `4 failures`；缓存失败只读边界后续也先单独 fail-first，再转绿。最终导入相关组 `19 passed`，完整 `tests/test_frontend_modules.py + tests/test_p2_coverage.py` 为 `464 passed + 4 subtests`；`npm run check:frontend` 的 bundle / classic build、freshness 与 bundle 语法，以及相关 JavaScript 语法和 Git diff 检查通过。
- 现有 3011 内置 Browser 完成 model-free H4：中文 / 英文、Notion 深色 / notion 浅色、`1686 / 740 / 390px`（390px 折叠侧栏）均确认窗口、列表、footer 和控件零横向溢出；首次骨架、缓存重扫只读锁定、来源键盘切换与焦点归还、搜索 / 筛选、单项 / 全选、条件摘要、普通行无徽标、共享 SVG、Escape 关闭回焦均通过。H4 只执行扫描 GET 和本地选择投影，从未点击“导入所选”，未执行任何真实导入。
- 最终 Browser 已恢复中文、Notion 深色、默认 viewport、侧栏展开、Claude Code 来源、零选择、导入窗口关闭，控制台无 warning / error，并保持标签页打开交还用户。未修改 server / API / 协议 / 数据格式、`CODE-042` 或其他窗口；未读取、哈希、修改或暂存受保护用户 data，未覆盖无关 handoff，未 push、tag 或 release。
