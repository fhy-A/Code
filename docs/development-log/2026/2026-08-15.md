# 2026-08-15

## 2026-08-15 09:39 · Codex

### CODE-031 系统提示词工程化与任务快照完成

- 将主 Agent 的系统提示词整理为 11 段显式段表，并把纯组装器迁入 `src/agent/system-prompt.js`；安全层继续位于首段、权限说明继续位于末段，原有工具、Skill、记忆、项目上下文和项目指令条件不变。`src/frontend-entry.js` 在 `app.js` 前加载该模块，bundle 与 generated classic 共用相同的 36 模块顺序。
- 系统提示词以首次持久化 AgentRun 为任务边界建立不可变快照；同一任务内的模型续轮和图片兼容重试复用原字节，新消息、真正开始执行的 queue、detached `/parallel` 与 Child 各自建立新快照。时间标签改为本地 IANA 时区名与 UTC 偏移并带安全回退，Child 控制提示词统一中文而输出语言继续跟随委派任务。没有新增 Session/AgentRun schema、API、协议、持久化字段或敏感全文副本。
- 固定输入迁移前后提示词逐字节一致；CODE-031 定向契约分别为 `5 passed` 与 `2 passed`，统一 UI 为 `227 passed`，前端 build/freshness、JavaScript/Python 语法和 diff-check 通过，既有 replay 四组结果为 `17/124/25/25` 通过。
- 扩大门禁仍存在与本阶段无关的冻结基线债务，未被掩盖或重基线：runtime 为 6 个失败且在纯净 HEAD 同现；H4 为 21 个失败，当前分支与纯净 HEAD 的测试 ID、失败类别、阶段及关键断言/哈希完全同现，当前分支没有独有红项。因此本阶段不能表述为全量 runtime/H4 全绿。
- 据此从 canonical TODO 移除 `CODE-031`；Code 最后已分配编号游标继续保留 `CODE-032`，不回退或复用，已确认推进序列只剩 `CODE-019` 且未启动。本阶段未执行 push、tag 或 release。

## 2026-08-15 08:25 · Codex

### CODE-032 运行中工具组标题提示与展开层级完成

- 运行中的外层工具组只在当前工具标题文字上显示间歇式主题高光，整行背景、箭头、内层工具项、Diff 和正文保持静止；高光随浅色/深色主题令牌变化，`prefers-reduced-motion` 下改为无持续动画的低强调静态提示。当前工具完成即停止高光，不等待整个对话任务结束。
- 工具组运行期间以最后一个当前工具为标题，工具间隙不提前切换为终态总结；多工具组全部结束后才显示汇总标题，单工具组保留该工具标题。首次展开外层工具组只显示工具列表，不再自动展开第一个工具详情；用户随后可独立展开具体工具项，普通重绘保持其当前页面选择，真实终态或刷新仍按既有折叠语义收口。
- 本阶段复用现有运行态与稳定 DOM，没有新增服务端、AgentRun、Session JSONL、协议或持久化字段；CODE-005 的节点复用、选择/焦点保护及 CODE-025 的 Diff 折叠和授权定位保持不变。
- 最终自动证据为：相关定向前端契约 `196 passed`，统一 UI 门禁 `225 passed`，bundle/direct classic H4 代表性生命周期 `6 passed`；前端构建、freshness、JavaScript 语法和 `git diff --check` 均通过。用户在 Code Dev 与 direct classic 两个入口完成实机复核并明确反馈 `PASS`。
- 据此从 canonical TODO 移除 `CODE-032`，但最后已分配编号游标继续保留为 `CODE-032`，不回退或复用；已确认推进序列缩短为 `CODE-031` → `CODE-019`。本阶段未启动下一 TODO，也未执行 push、tag 或 release。

## 2026-08-15 00:32 · Codex

### TODO 双域编号游标维护契约

- 在 `TODO.md` 的 canonical 维护契约中增加 workbar 与 Code 两套独立编号游标，分别初始化为最后已分配的 `WB-012` 与 `CODE-032`。提交历史和当前事项标题扫描均未发现更大编号。
- 新增事项必须在写入前重读对应主域游标并核对现存标题，以两者较大值加一、三位补零，并把游标与新条目放在同一次修改中更新；并行写入已推进游标时重新计算，避免重复分配。
- 游标表示历史上的最后已分配编号，事项完成移除后不回退，编号不复用、不改义；workbar 与 Code 各自单调递增。本阶段只同步 TODO 契约和日志事实，没有修改协作规则、产品代码或测试，也没有实施仍待方案确认的 `CODE-032`。

## 2026-08-15 00:05 · Codex

### CODE-002 顶部工具日志移除完成

- 顶部工具日志入口和面板已移除，并同步清理 `app.js` 中重复汇总当前 `state.messages` 的渲染/详情逻辑、`src/ui/panels.js` 的专属开关与关闭分支、专属样式和中英文文案。顶部工具栏删除后由既有 flex 布局自然收拢，没有新增替代入口，也没有扩展为 per-run 轨迹或副作用 manifest。
- 对话流内联执行轨迹继续承担普通工具轮、失败后继续、取消、queue 真正执行、detached `/parallel`、Child/Subagent 结果、授权/问卷等待和刷新恢复等可见状态；Session 统计中的工具数量、Branches、Preview 与公共 top-panel 行为保持不变。无工具任务不会生成空工具容器。
- 本阶段只改前端 DOM、共享面板接线、样式、i18n 和直接契约测试，没有修改工具执行、滚动所有权、AgentRun、Session JSONL、服务端/API、协议或持久化；旧会话无需迁移，回退只需撤销本阶段独立提交。
- 自动证据为：三份相关 JavaScript 文件的 Node 语法检查通过；普通/失败/取消、panel、queue、parallel、refresh、steer、稳定工具组与终态收口定向测试 `8 passed`；`python verify.py ui` 为 `224 passed`，其 frontend freshness、bundle 语法、pytest UI、diff-check 和 app 语法全部通过；`npm run check:frontend` 的 build、freshness 和 bundle 语法通过；生产源码及 rebuilt bundle/direct classic 均无顶部工具日志残留符号。
- 用户在 Code Dev 与 direct classic 两个入口完成实机复核并明确反馈均为 `PASS`。据此将 `CODE-002` 从唯一未完成事项清单移除，已确认推进序列缩短为 `CODE-032` → `CODE-031` → `CODE-019`；本阶段未启动下一 TODO，也未执行 push、tag 或 release。
