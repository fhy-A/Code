# 2026-08-05

## 2026-08-05 21:40 · Codex

### Harness H3-2B1 多 Run replay 关系基础设施收口

- 新增独立、严格版本化的 multi-run schema、合成 suite、runner 与定向测试，只包含 `queue-parallel-multi-run-relations`：1 个场景、3 个 Run、12 个事件、15 个 schedule 步骤、3 个 fact-marker、4 个检查点及 4 次序列化恢复。该计数独立记录，没有并入单 Run 基线。
- 每个 Run 继续分别调用生产 `reduceRunProjectionInput` 与 `projectRunViewModel`，独立维护事件序列、cursor、检查点恢复、重复投递和状态哈希；组合层只维护 `scheduleCursor`、各 Run 状态/cursor 与不可变身份图，fact-marker 不修改 Run 业务状态，没有建立测试专用 queue、background、UI messages 或 usage ledger。
- fixture suite hash 为 `710a3e5677281d3554f33a2b0fa11fd84a52270c6b90e0aa96e9b93da534880a`，suite replay hash 为 `095537b72121478d1ef35a143aa6ecd361c0ec557a4fcc9594e3b024e86aabf6`，复合状态 hash 为 `5bfc185b1f31979e3802b1908d1b908c6f64d4ccc5ca882da158e7b840504e85`；Run `F1 / B1 / F2` hash 分别为 `996792e15f8182f3b7c7f04acd862efdb5fd5ba590cca34a20d03ab3708b112a`、`2de637407b7156c3d32a94f95fa4e824c0a089c86436f9d78c600fd1340b817a`、`2959cee047a402407bd8b8d29cc2b46fa8b00b7b3e2c17abb647cebc967e1d6a`；重复投递结果与上述哈希一致。
- 复合恢复只由序列化恢复出的 `state.scheduleCursor` 决定续播位置；恢复入口验证其为 `0..schedule.length` 内整数，根据对应 schedule 前缀推导每个 Run 的预期 cursor，核对 `runCursors` 与 `runStates[*].cursor`，并要求两者 Run key 集合分别与场景精确匹配。
- 主要变异首差异路径包括：错误 Run 投递 `$.schedule[6].agentRunId`，无效 schedule 引用 `$.schedule[0].eventSeq`，身份冲突 `$.identities.agentRuns.B1.agentRunId`，Session 错连 `$.identities.agentRuns.B1.sessionId`，创建/终态顺序 `$.orders.creation[0]` / `$.orders.terminal[1]`，F2 恢复污染 `$.checkpoints[2].resume.runs.F2.status`，schedule cursor 向前/向后/越界污染 `$.checkpoints[0].resume.runCursors.B1` / `$.checkpoints[0].resume.runCursors.F1` / `$.checkpoints[0].resume.scheduleCursor`，Run key 缺失/多出 `$.checkpoints[0].resume.runStates[2]` / `$.checkpoints[0].resume.runCursors[1]`，Run state cursor 污染 `$.checkpoints[0].resume.runStates.B1.cursor`，重复事件 `$.schedule[7].eventSeq`。
- 旧单 Run 基线原样保持为 17 条轨迹、124 个事件、25 个检查点、25 次检查点恢复和 4 个显式恢复点；fixture suite hash `c5136af7d5abe6f055e76e230939f7aa5d4cf1c4b2bb832c492819d5604eeffc` 与 replay hash `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2` 均未变化。
- 收口验证：单 Run fixture/replay 验证均通过；Harness 组合回归为 `52 passed, 72 subtests passed`；既有 queue/background 相关回归为 `64 passed`；Session accessor、后台 helper 与确定性 AgentRun ID 所在的既有生产纯函数契约测试为 `2 passed, 165 deselected`；Node 语法和 `git diff --check` 通过。
- 完成声明只覆盖多 Run 独立生产投影、静态身份关系、固定顺序、复合恢复和幂等，不证明真实 queue 提升、background 调度/完成、消息/usage exactly-once、真实线程竞争、DOM、刷新、Runtime 原始事件恢复或发布门禁。本阶段未修改任何生产代码、旧单 Run runner/schema、AgentRun、会话 JSONL 或 Runtime；H3-2B2 Child AgentRun replay 尚未开始，仍需单独确认。

**涉及文件**：`scripts/replay-agent-multi-run-traces.cjs`、`tests/fixtures/harness/multi-run-trace-suite.json`、`tests/fixtures/harness/multi-run-trace-suite.schema.json`、`tests/test_harness_multi_run_fixtures.py`、`tests/test_harness_multi_run_replay.py`、`docs/harness/h3-1-replay-runner.md`、`docs/harness/h3-2b1-multi-run-replay-relations.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-05.md`

## 2026-08-05 15:29 · Codex

### Harness H3-2A 单 Run replay 夹具扩展收口

- 新增 `cancel-multi-tool-terminal-closure` 与 `command-failure-model-recovery` 两条严格脱敏的单 Run 合成轨迹，分别包含 8 个事件/4 个阶段检查点和 10 个事件/4 个阶段检查点；前者固化同轮命令与未开始读取工具在取消时的终态闭合，后者固化命令单次失败、结果进入第二模型轮、唯一最终回答及 Run 正常完成。
- replay / View Model 检查点覆盖状态、`terminalStatus`、模型轮次、工具数量、工具 ID、工具状态与 outcome、事件顺序及 `pendingKind`；原始夹具契约测试独立核对 `model_completed` 声明与 `tool_completed` 集合完全对应、未开始读取工具没有开始事件、`result.cancelled / cancelledBeforeStart`，以及固定退出码 `23` 和 stderr 标记 `synthetic-command-failure-marker`。
- 为两条新轨迹增加专属变异测试：删除取消闭合事件首先定位 `$.events[6].seq`，改写取消工具 ID 定位 `$.tools[1].toolCallId`，乱序命令恢复事件定位 `$.events[6].seq`，改写命令失败 outcome 定位 `$.tools[0].outcome`；没有用旧轨迹的通用变异测试替代新路径证据。
- 最终统一基线为 17 条轨迹、124 个事件、25 个检查点、25 次检查点恢复和 4 个显式恢复点；fixture suite hash 为 `c5136af7d5abe6f055e76e230939f7aa5d4cf1c4b2bb832c492819d5604eeffc`，replay hash 为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。
- 本阶段没有修改生产 reducer、前端、服务端、AgentRun、会话 JSONL、Runtime、replay runner 或夹具 schema，也没有进入刷新恢复、多 Run、排队、并行、Child AgentRun 或发布门禁。`pendingKind: ""` 只证明 View Model 没有投影等待态，不能证明服务端 `pendingToolCalls` 清空；后者仍以 H2-3 既有真实服务端验证为证据，本阶段不为此扩展投影或 schema。
- 收口验证：`python scripts/verify_harness_fixtures.py` 通过；`npm run verify:harness-replay` 通过；`python -m pytest tests/test_harness_fixtures.py tests/test_harness_replay.py tests/test_run_projection.py -q -p no:cacheprovider` 为 `30 passed, 41 subtests passed`；`git diff --check` 通过。
- H3-2A 完成后移除临时 `docs/harness/h3-active-handoff.md`；H3-2B 仍需另行设计、确认，本阶段未开始。

**涉及文件**：`tests/fixtures/harness/trace-suite.json`、`tests/test_harness_fixtures.py`、`tests/test_harness_replay.py`、`docs/harness/h3-1-replay-runner.md`、`docs/harness/h3-2a-single-run-replay-fixtures.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-05.md`

## 2026-08-05 13:46 · Codex

### Harness H2-3 刷新恢复实验撤回与已知限制归档

- 人工验收确认两条刷新恢复场景未通过：模型已有部分正文后刷新，已输出正文会消失并在模型终态后一次性出现；刷新后的暂停请求也不能立即反馈，暂停说明延迟出现且此前正文未恢复。
- 现有自动回归与影子零差异只能验证离散事件、快照及父 `AgentRun` 的规范投影一致性，不能覆盖真实页面中父 `AgentRun` 观察与子 Runtime 流消费互相阻塞的并发时序，因此自动测试通过不得作为正文、暂停反馈或顶部状态恢复正确的证据。
- 决定暂停当前刷新恢复路线，不继续设计并发控制器。本轮尚未提交的 `app.js` 与 `tests/test_frontend_modules.py` 实验改动已精确恢复到当前 `HEAD`，内容哈希与 `HEAD` 一致；没有创建提交、标签、发布或活动交接，未跟踪的 `docs/approval-agent-collaboration-manual.md` 保持原状。
- “首个模型增量到达前刷新”和“已有正文后刷新/刷新后暂停延迟”统一保留为已知限制，延后至 H4 隔离浏览器 E2E 或未来统一控制面重构。该限制不再阻塞 H2-3 当前主线，但这些刷新场景仍明确为未通过，不得以影子零差异或自动测试结果标记完成。
- 撤回后验证：`node --check app.js` 通过；`python -m pytest tests/test_frontend_modules.py -q -p no:cacheprovider` 为 `167 passed`；`git diff --check` 通过。写入事实源前，业务工作区无已跟踪差异，仅保留上述未跟踪审批手册。
- H2-3 下一项恢复正常主线：继续为已通过的关键路径采集独立重复零差异样本，优先补普通前台多工具任务的第二个真实样本；刷新恢复不再混入该批验收。

**涉及文件**：`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-05.md`

## 2026-08-05 14:42 · Codex

### Harness H2-3 关键路径重复零差异样本收口

- 在同一开发页面内对已通过路径连续采集了独立重复样本，覆盖普通前台工具、`request_user_input`、命令授权接受/拒绝、排队、`/parallel` 前后台隔离、模型取消、同轮多工具取消与命令失败。最终页面累计 12 条脱敏摘要、137 个事件、66 个快照和 203 次比较，`mismatches`、`observerErrors` 与 `diagnosticsDropped` 均为 0；用户逐项确认界面表现正常。
- `/parallel` 重复样本进一步核对服务端耐久事实：本次时间窗只新建一个前台 AgentRun 与一个带 `background-*` 幂等键的后台 AgentRun，均为 `completed`；前台观察 14 个事件、7 个快照和 21 次比较，后台观察 9 个事件、10 个快照和 19 次比较，两者轨迹、正文、计时与用量没有串组。总览中一条额外前台摘要来自先前任务的稍后归档，不是本次创建第三个服务端 Run。
- 模型取消重复样本为 `cancelled`，观察 3 个事件、2 个快照和 5 次比较；部分流式正文、暂停说明、顶部累计计时与唯一最终内容均通过人工确认。
- 命令失败重复样本使用独立的 stderr 标记 `h2-3-repeat-failure` 和退出码 `9`；命令仅执行一次，工具失败、结构化错误进入下一模型轮、最终回答唯一且无残留运行状态；Run 为 `completed`，观察 14 个事件、7 个快照和 21 次比较，均零差异。
- 本批收口不改变刷新恢复的事实：“首个模型增量到达前刷新”、“已有正文后刷新”与“刷新后暂停延迟”仍是未通过的已知限制，继续延后到 H4 隔离浏览器 E2E 或未来统一控制面。
- H2-3 当前真实影子采样批次收口；下一步进入 H3 脱敏 replay 夹具扩展，优先将排队/并行、同轮多工具取消、上游失败和旧记录恢复等真实跨层边界转为确定性夹具，再接入发布门禁。

**涉及文件**：`TODO.md`、`docs/harness/h2-3-shadow-validation.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-05.md`
