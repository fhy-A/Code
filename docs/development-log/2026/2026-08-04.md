# 2026-08-04

## 2026-08-04 08:18 · Codex

### Harness H2-3 已验收路径收口与模型首增量刷新延后

- 保留并固化前端 Agent Runtime 投影的单消费者所有权、父 AgentRun 快照 `activeRuntimeRunId` 权威恢复、已消费/未消费 `model_started` 两类接续路径，以及首次模型增量仅保存一次 metadata-only `runState` 检查点；没有新增 Runtime、模型请求、事件类型、协议字段或消息 JSONL 写入，也没有改写旧 AgentRun/会话数据。
- 已验收的模型/工具取消、同轮多工具取消终态闭合、命令失败、授权接受/拒绝、问卷、排队、前后台隔离、工具刷新恢复和影子零差异行为继续保留；撤回了本轮未改善人工结果的启动期即时挂接增量，避免把无效复杂度带入提交。
- 人工重复样本确认“首个模型增量到达前刷新”仍停留在“已提交模型，正在等待响应”，直到终态后一次性显示正文。该场景明确作为已知边界延后至刷新恢复专项/H4 隔离浏览器 E2E，不标记完成，也不以影子零差异替代用户可见验收；H2-3 其余已验收路径可独立提交，H2 仍保持未收口。
- 定向 Agent/投影/前端回归 `241 tests`、全量 `1006 tests` 与 `468 subtests`、前端构建/语法校验和 15 条 replay 夹具（106 个事件、17 个检查点恢复、4 个显式恢复点）均通过；`git diff --check` 通过。未跟踪的 `docs/approval-agent-collaboration-manual.md` 不属于本阶段，不纳入暂存或提交。

**涉及文件**：`app.js`、`tests/test_agent_cancel_handshake.py`、`tests/test_frontend_modules.py`、`tests/test_p0_stability.py`、`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-04.md`

## 2026-08-04 05:41 · Codex

### Harness H2-3 命令失败真实路径零差异

- 使用真实 AgentRun 仅执行一次 `node -e "console.error('h2-3-intentional-failure'); process.exit(7)"`，命令按预期失败并保留退出码 `7`、stderr 标记与空 stdout；界面没有残留“执行中”或“等待执行”，模型收到失败工具结果后只生成一次最终说明，整个 Run 以 `completed` 正常结束。
- 脱敏影子摘要为 1 个前台 `completed` Run、14 个事件、6 个快照和 20 次比较，`mismatches`、`observerErrors`、`diagnosticsDropped` 均为 0，诊断列表为空。
- 新增 AgentRun 完整链路回归，真实启动非零退出命令并验证：命令与 `tool_completed` 均只出现一次，工具 outcome 为 `failed`，退出码和 stderr 同时写入耐久执行结果、事件及下一模型轮工具消息，待处理工具清空且 Run 可以正常完成。业务实现、事件类型、协议版本、持久化格式和前端渲染均未修改。
- 单条定向测试、相关 Agent/投影/前端 `267 tests` 及全量 `1001 tests` 均通过。H2-3 命令失败路径已经补齐，整体仍需继续采集已通过关键路径的重复零差异样本。

**涉及文件**：`tests/test_agent_runtime.py`、`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-04.md`

## 2026-08-04 05:25 · Codex

### Harness H2-3 同轮多工具取消终态闭合

- 修复同一模型轮含多个工具调用时，Run 取消后未开始或未闭合工具残留为“等待执行”的问题。取消提交现由服务端在同一临界区闭合工具事实：正在执行的命令沿用原有 `result.cancelled: true` 结果且只写入一次 `tool_completed`；尚未开始的工具写入带 `cancelledBeforeStart: true` 的耐久取消结果，不执行工具逻辑，也不产生 `tool_started / command_started`；全部工具闭合后才最后写入 Run `cancelled`。
- 工作线程在领取工具、写入开始事件、提交工具结果及模型轮交接时都会与取消终态重新核对，避免并发取消、重复取消或工作线程竞态产生重复事件和副作用。取消后 `pendingToolCalls` 为空，且不保留 `pendingInput`、`pendingAuthorization`、活动命令或任何 waiting/running 工具；持久化重载后保持相同终态。
- 保持现有事件类型、AgentRun v4 和 outcome 兼容语义：取消仍使用 `result.cancelled: true`、`ok: false` 并映射为 `outcome: failed`，未新增 `outcome: cancelled`、协议字段或数据迁移；没有改写旧 AgentRun、会话 JSONL 或前端业务投影，也未把工具残留通过前端隐藏。
- 新增双工具取消、第二工具零执行、事件唯一性与顺序、重复/并发取消幂等、持久化重载、前端取消显示和影子零差异回归。相关 Agent/投影/前端模块共 `266 tests` 全部通过；使用工作区 Python 3.12 与现有依赖运行全量 `1000 tests` 全部通过，`git diff --check` 通过。
- 人工验收在同一模型轮并发请求长命令与读取 `VERSION`，命令执行中点击停止后两个工具均显示“已取消”，未出现“等待执行”。脱敏影子摘要为 1 个前台 `cancelled` Run、8 个事件、3 个快照和 11 次比较，`mismatches`、`observerErrors`、`diagnosticsDropped` 均为 0，诊断列表为空。
- H2-3 尚未整体收口：命令失败真实路径已在后续验收中通过，当前仅需继续采集已通过关键路径的重复零差异样本。

**涉及文件**：`server.py`、`tests/test_agent_runtime.py`、`tests/test_frontend_modules.py`、`tests/test_run_projection.py`、`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-04.md`
