# 2026-08-08

## 2026-08-08 15:38 · Codex

### Harness H4-6N 同指纹成功打断失败链收口

- H4-6N 只扩展隔离宿主和 Playwright。三个唯一 toolCallId 使用完全相同、字段顺序固定的 `read_file({"path":"h4-success-reset-fixture.txt"})`；每例独立 project 内的专用路径按 `missing → present → missing` 切换，第二次写入并核对与 `fixture.txt` 相同字节，三次均经过既有安全 wrapper 后真实调用原生产执行器，没有 mock、改写结果或修改生产协议/持久化/安全边界。
- 三次 fingerprint 相同，耐久 outcome 为 `failed → succeeded → failed`、`failureSignature=A → absent → A`、`failureCount=1 → absent → 1`。成功 execution 顶层及 result 均不存在 `failureCount`，第三次同失败不继承第一次计数；全程无 retry-limit/block、`tool_retry_blocked` 或 forced-final，第四模型轮保持正常 `tools/tool_choice` 并产生唯一普通终答。
- bundle/direct classic 均闭合 19 事件、`nextCursor=19`、`nextSeq=20`、Runtime cursor `[4,3,3,3]`、chat 4、生产委托/执行 3、Session 11 条消息与三对 tool-call/result、单组 failed/succeeded/failed 三项，以及完整刷新 AgentRun/Runtime/chat/tool 四项零增量；九项去随机化哈希完全一致。
- 最终断言增强后的同一测试文件哈希下，H4-6K/L/M bundle/classic `6 passed` 且旧哈希保持，H4-6N bundle/classic 对等，infra 通过；连续两轮标准 H4 均为 `47 passed`、单 worker、零 retry、exit 0。断言增强前的两轮因最终文件哈希变化而作废，不作为最终证据。
- 相关 limiter/read_file 根契约 `6 passed`，前端/P0 `230 passed`，完整 pytest `1127 passed, 751 subtests passed`，`check:frontend`、Node/Python 语法、diff 与资源清理通过。首次 `undefined/false` DOM 投影差异和一次无匹配标题 grep 均不计通过，也未改变产品行为或放宽语义门禁。
- 完成声明只覆盖固定字段顺序、同一工具和完全相同 arguments 指纹下 `missing → success → missing` 对失败链的打断，以及 bundle/direct classic 同进程终态刷新零重执行；不覆盖不同工具/参数、阈值后行为、任意 key 顺序、执行前失败、权限/编码/大文件、跨进程 active、取消或副作用 exactly-once。长期事实见 [`H4-6N 同指纹成功打断失败链`](../../harness/h4-6n-identical-fingerprint-success-reset.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。

## 2026-08-08 14:40 · Codex

### Harness H4-6M 同指纹错误签名交替隔离收口

- H4-6M 只扩展隔离宿主和 Playwright。三个唯一 toolCallId 使用完全相同、字段顺序固定的 `read_file({"path":"h4-signature-alternation-fixture.txt","startLine":2,"endLine":1})`；每例独立 project 内的专用路径按 `present → missing → present` 切换，三次均经过既有安全 wrapper 后真实调用原生产执行器，没有捕获后改写错误、复制 limiter 或修改生产协议/持久化/安全边界。
- 三次工具指纹相同，生产错误签名关系为行范围/缺文件/行范围 `A → B → A`，耐久 `failureCount=[1,1,1]`。三项均无 `retryLimitReached`、`retryBlocked`，没有 `tool_retry_blocked` 或 forced-final；第四模型轮继续携带正常 `tools/tool_choice` 并产生唯一普通终答，父 AgentRun completed。
- bundle/direct classic 均闭合 19 事件、`nextCursor=19`、`nextSeq=20`、Runtime cursor `[4,3,3,3]`、chat 4、生产委托/执行 3、Session 三对 tool-call/result、单组行范围/缺文件/行范围三项，以及完整刷新 AgentRun/Runtime/chat/tool 四项零增量；九项去随机化哈希完全一致。
- 首次取证时，测试 wrapper 把脱敏 timeline 放在生产委托正常返回之后，而真实执行器失败以异常传播，导致 timeline 未执行；修订只把只读取证与专用文件状态复核移入 `finally`，原异常仍原样传播。该首差异轮不计通过。一次 pytest 定向使用不存在的类名、未进入收集，同样不计产品通过或失败；随后使用真实测试定位取得有效结果。
- 有效矩阵为：H4-6K/L 旧场景及原哈希保持，H4-6M bundle/classic 通过，infra 通过，连续两轮标准 H4 均为 `45 passed`、单 worker、零 retry、exit 0；相关生产失败限流与 read_file 路由定向 `6 passed`，前端/P0 `230 passed`，完整 pytest `1127 passed, 751 subtests passed`，`check:frontend`、Node/Python 语法、diff 和资源清理通过。文档收口沿用同一实现哈希下的完整矩阵，仅重做文档、语法、链接、diff、白名单和资源门禁。
- 完成声明只覆盖固定字段顺序、同一工具和完全相同 arguments 指纹下 `present → missing → present` 形成的 `A → B → A` 签名交替、`[1,1,1]` 连续计数隔离，以及 bundle/direct classic 同进程终态刷新零重执行；不覆盖阈值后交替、不同工具/参数、任意 JSON key 顺序、执行前失败、权限/编码/大文件、跨进程 active、取消或副作用 exactly-once。长期事实见 [`H4-6M 同指纹错误签名交替隔离`](../../harness/h4-6m-identical-fingerprint-signature-alternation.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。

## 2026-08-08 13:52 · Codex

### Harness H4-6L 不同 arguments 失败身份隔离与稳定门禁收口

- H4-6L 只扩展隔离宿主与 Playwright。固定 A→B→A 三次调用分别使用 `startLine/endLine=2/1、3/1、2/1`，均通过 schema 和既有只读安全边界并真实进入 `read_file` 生产执行器；三次 `failureCount=[1,1,2]`，证明 B 不继承 A 的计数且不打断 A 同指纹链。三项均无 retry-limit/block，第四模型轮保持普通 tools/tool_choice 终答，父 Run completed。
- bundle/direct classic 均闭合 19 事件、`nextCursor=19`、`nextSeq=20`、Runtime cursor `[4,3,3,3]`、chat 4、生产委托/执行 3、Session 三对 A/B/A tool-call/result、单组有序三项和完整刷新 AgentRun/Runtime/chat/tool 四项零增量；九项去随机化哈希完全一致，完整值见专题。
- 完整门禁中的 Q1 只增加真实一次 Control+Enter 的脱敏因果观察：`keydown=1 → submit=1 → queue-save=1 → queued DOM=1`，没有重复提交、保存完整请求体或修改生产队列语义。首版观察器曾把单独 Control keydown 计入目标事件，修订后只记录目标 Enter；该轮不计通过，本项是 Harness 因果门禁，不是生产竞态修复。
- TIFF 门禁不再用受 `loadSession` 300 ms 防抖影响的“点击当前 Session”触发重绘，而是通过现有设置 UI 真实切换语言，沿 `onLanguageChanged → renderMessages` 证明旧节点断开、当前预览可解码及重绘新增 GET 为 0；恢复原语言后同样闭合。产品行为、POST 2/GET 2/总计 4、原 TIFF SHA、`image/tiff` 持久化和刷新四项零增量均未改变，也不宣称修复生产竞态。
- 同一最终测试哈希下，TIFF bundle 连续三次/classic/合并、Q1 bundle/classic、H4-6L 与 H4-6K 定向、infra 均通过；连续两轮标准 H4 均为 `43 passed`、单 worker、零 retry、exit 0（约 151.2 秒/150.8 秒），Agent Runtime 定向 `3 passed`，前端/P0/background persistence `230 passed`，完整 pytest `1122 passed, 751 subtests passed`，`check:frontend`、语法、diff、哈希和资源清理通过。文档收口沿用同一实现哈希下的完整矩阵，没有写成收口后重跑。
- 完成声明仅限固定字段顺序、同 action/path/错误签名下不同 arguments 的计数隔离与 A 链延续，以及 bundle/direct classic 同进程终态刷新零重执行；不覆盖任意 key 排序、不同工具、错误交替、阈值后交替、forced-final 失败、跨进程 active、取消或副作用 exactly-once。长期事实见 [`H4-6L 不同 arguments 失败身份隔离与同指纹连续性`](../../harness/h4-6l-different-argument-failure-isolation.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。

## 2026-08-08 11:10 · Codex

### Harness H4-7C detached `/parallel` 模型失败隔离与终态刷新唯一性收口

- H4-7C 只扩展隔离测试：主任务单 `read_file("fixture.txt")` 工具结果已形成、第二轮终答尚未发送时提交 detached `/parallel` 固定 marker，假上游只返回一次 HTTP 502。默认 bundle 与 direct classic 复用同一生产链、既有两个终答闸门和同一参数化 helper，没有修改生产、协议、持久化或并发交互。
- background 独立 AgentRun 以 `created → model_started → failed` 闭合；Runtime 精确为 `failed/upstream_error/502/transient=true` 且零 SSE。主任务在 background 失败时仍 active，toolCall/result、稳定 process key、展开状态与工具归组不变，释放闸门后正常 completed。
- detached 用户与唯一 error assistant 通过 jobId/agentRunId/reply reference 闭合，沿用 `execution-trace-persistent` 规则可见但不取得主任务工具或顶部完成状态所有权；background 自身页脚耗时 1，主任务顶部完成耗时 1、页脚耗时 0。首次计数为 AgentRun POST 2、Runtime POST 0、chat 3、tool execution 1；生产 Runtime GET 只是观察，不记作 POST。完整 reload 后四项增量全 0，消息、终态、身份、耗时和工具轨迹保持唯一。
- bundle/direct classic 八项语义 SHA-256 一致：mainToolTrace `6599ebee...23c4`、backgroundAgent `1319a246...4545`、backgroundRuntime `6b71bc4b...76d1`、sessionRoleContent `9846dc82...9ab7`、backgroundMeta `e26b1c4a...5baa`、domOwnership `0c317c7f...d584`、requestCounts `e90926ff...ba1e`、refreshLifecycle `0cb60f32...7681`；完整值见专题。
- 连续两轮标准 H4 均为 `41 passed`、单 worker、零 retry、exit 0，耗时约 161 秒/159.8 秒；H4-6A 两条、H4-7B 两条、background/concurrency/frontend `265 passed`、完整 pytest `1122 passed, 751 subtests passed`、`check:frontend`、Node/Python 语法、diff 和资源清理均通过。一次 240 秒外层等待预算关闭输出管道、无 Playwright 摘要的 EPIPE 轮不计产品通过或失败；后续只增加外层等待预算，没有修改内部门禁或实现。
- 每次 Playwright 成功标记仅在核验 `passed/[]` 后精确清理，最终进程、端口、临时根与 output 均归零。本阶段只证明确定性 502 下 detached `/parallel` 的同进程失败隔离和完整 reload 唯一性；不覆盖显式取消、超时、排队失败/取消、Child、active/cross-process background 恢复、真实外部副作用 exactly-once 或新并发产品语义。长期事实见 [`H4-7C detached parallel 模型失败隔离与终态刷新唯一性`](../../harness/h4-7c-detached-parallel-failure-isolation.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。

## 2026-08-08 09:51 · Codex

### Harness H4-7B 主任务完成计时唯一投影收口

- H4-7B 修正完成状态收集器与可见消息投影的 turn 所有权不一致：detached 用户消息仍正常显示，但不再更新主任务 `currentUserIndex` 或接管完成状态/工具轨迹；detached assistant 仍保留自身页脚耗时。主任务和实际执行完成的普通排队任务各只在用户消息后的顶部完成状态展示一次耗时，对应 assistant 页脚只保留 Token。
- 中文完成态新增专用 `completedElapsedLabel=用时`，英文为 `Worked for`；运行中首个模型正文后的 `processedLabel=已处理` 及等待模型、工具继续、恢复、暂停、取消和失败文案均未改动。实现继续复用既有 `_responseTime` 与 `taskElapsedTitle`，没有修改计时算法。
- 统一后台结果构造入口只规范化一次 `responseTime`，并把同值写入顶层 `_responseTime` 与 `meta._responseTime`，分别服务当前页面和既有 Session 序列化/刷新恢复。成功、失败后台终态以及重复保存、读取、重投影均保持单一页脚耗时；`src/services/persistence.js`、Session JSONL 格式、AgentRun、Runtime 与事件协议均未修改，已经丢失耗时的旧持久化消息不迁移、不补算。
- bundle/direct classic H4 均闭合顶部完成状态 2 个（主任务、排队任务）、主任务/排队任务页脚耗时 0、parallel 页脚耗时 1、主任务/排队任务 Token 指标各 2，以及完整刷新 AgentRun POST、Runtime POST、chat、tool 四项零增量。用户在 3011 开发实例人工确认 bundle 案例 1、2 和 direct classic 案例 3 全部通过，完整刷新和中英文切换正常。
- 同一实现文件形态已完成前端相关 `230 passed`、连续两轮标准 H4 各 `39 passed`（单 worker、零 retry）、完整 Python `1122 passed, 751 subtests passed`，并通过 `check:frontend`、语法、diff 和资源清理。文档收口只在七个实现/测试哈希不变时执行定向轻量门禁；完整矩阵沿用同哈希有效结果，不写成收口后重跑。
- 本阶段只证明修正后新产生的主任务、普通排队任务与 detached/background/`/parallel` 终态耗时唯一投影和同进程刷新恢复；不迁移旧缺失耗时，不改变队列/并行调度、消息顺序、持久化格式或协议，也不证明发布包行为。长期事实见 [`H4-7B 主任务完成计时唯一投影`](../../harness/h4-7b-primary-completion-elapsed-projection.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。

## 2026-08-08 08:16 · Codex

### Harness H4-7A TIFF 派生浏览器预览与页面生命周期缓存收口

- H4-7A 保持原 TIFF 为唯一持久化附件：磁盘附件、消息 `_images`/content 与 Session JSONL 均继续保存 `image/tiff`，不含 Blob URL、失败状态或派生 preview 字段；模型请求继续使用既有 TIFF → PNG 投影。用户已在真实浏览器人工确认发送前、发送后、点击放大和完整刷新后的 TIFF 预览均正常。
- 同源 preview POST/GET 复用现有嗅探、10 MB、像素/尺寸限制和 TIFF → PNG 转换；GET 只允许 `ATTACHMENTS_DIR` 内安全解析的 path。成功仅返回内存 `image/png` 和 `nosniff`，不写预览文件、磁盘缓存或持久化字段；错误 base64/签名、损坏、超限、超像素、绝对路径、穿越和非附件路径均被有界拒绝。预览失败显示附件卡片，不阻止发送或模型识别。
- 已保存 TIFF 的页面缓存以规范化 MIME + 原 attachment path 为 key，状态固定为 `pending`、`ready(blob URL)`、`failed`。同 key 并发和重绘复用 pending，ready 复用同一 Blob URL，failed 同页不重试；完整刷新自然重建并允许一次 GET。异步完成只安全重绘仍含该 path 的当前会话，页面卸载时每个 Blob URL 只撤销一次，不写入消息或持久化。
- bundle/direct classic 的 method + path + phase 计数完全一致：composer 成功 POST 1、composer 故障 POST 1、发送后首次失败 GET 1、模型重绘新增 GET 0、完整刷新 GET 1、刷新后真实会话重绘新增 GET 0、放大新增 GET 0，总计 POST 2、GET 2、请求 4。原 TIFF SHA、单一磁盘附件、Session `image/tiff`、模型识别与刷新 AgentRun/Runtime/chat/tool 四项零增量均保持。
- 实现完成时缓存定向 `1 passed`、TIFF 路由/安全定向 `7 passed, 12 subtests passed`、前端模块 `172 passed`、H3-2D1 `6 passed, 32 subtests passed`、两条 TIFF H4 各 `1 passed`、infra 通过、连续两轮标准 H4 各 `37 passed`、相关组合 `98 passed, 58 subtests passed`、完整 Python `1121 passed, 751 subtests passed`，并通过 `check:frontend`、语法、diff 和资源清理。文档收口只在十个实现/测试哈希不变时执行轻量复验；两轮标准 H4 与完整 pytest 明确沿用同哈希结果，没有在文档收口后重跑。
- 本阶段只证明有效 TIFF 的 bundle/direct classic 展示、失败卡片、刷新恢复与页面生命周期请求去重；不证明 TIFF 多页浏览、完整格式矩阵、真实外部模型或发布门禁。长期事实见 [`H4-7A TIFF 派生浏览器预览与页面生命周期缓存`](../../harness/h4-7a-tiff-derived-browser-preview.md) 与 [`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md)。

## 2026-08-08 06:15 · 人工验收

### Harness H3-2D3 上下文压缩失败持久化恢复真实浏览器验收

- 使用独立会话分支和浏览器本地请求阻断进行真实浏览器人工验收，没有修改代码、配置、凭据或线上环境。
- 同时阻断 `/api/compact` 与目标 Session 保存后，界面显示“上下文压缩失败，且失败状态尚未保存”和“压缩请求未完成，原历史已保留”，提供“重试保存”，且不显示成功提示；请求计数为 compact 1 次、Session 保存阻断 2 次。
- 恢复 Session 保存并只点击一次“重试保存”后，仅执行保存，不重复压缩请求；界面显示“会话保存已恢复”，marker 收敛为唯一“上下文压缩失败”。
- 关闭阻断并完整刷新后，原历史保持完整，失败 marker 唯一，不再显示重试按钮，也没有重复摘要。
- 该结果确认 H3-2D3 在“压缩请求失败且失败状态首次保存受阻”时保留原历史、明确区分未保存与已恢复状态，并以单次保存重试闭合持久化；本条仅记录人工浏览器验收事实，不包含生产代码或测试修改。

## 2026-08-08 04:54 · Codex

### Harness H4-6K 相同 `read_file` 失败限流与强制终答收口

- H4-6K 只修改测试侧，复用 H4-6G 的 schema-valid 只读行范围失败。生产重复身份由工具名与规范 arguments 指纹确定，不含 toolCallId；连续失败还要求 `errorCode` 与规范化错误文本签名一致。解析/schema 校验早于限流，因此 H4-6E/I/J 的执行前失败不触发本限流。
- 前三次相同调用真实进入执行器并产生 `failureCount=1/2/3`，第三次带 `retryLimitReached=true`；第四次以新 toolCallId 声明同一指纹，由 `repeated_tool_failure/retryBlocked` 阻断且不进入执行器；第五轮不携带 tools/tool_choice，包含脱敏恢复指令事实并固定终答，父 Run completed。真实计数为 25 事件、`nextCursor=25`、`nextSeq=26`、Runtime cursor `[4,3,3,3,3]`、模型请求 5、生产委托/执行 3、耐久 execution 4、`tool_retry_blocked=1`、unsafe=0。
- Session 精确绑定四对 tool-call/tool-result；UI 只有一个工具组和四个 failed 项。DOM 只冻结前三项行范围失败与第四项同一调用三次失败后被阻断的用户可见语义；`action/failureCount/retryLimitReached/retryBlocked/repeated_tool_failure` 仍由 AgentRun、模型回执与 Session meta 严格闭合，不宣称逐字显示在页面。完整 reload 后 AgentRun POST、Runtime POST、chat、工具执行四项增量均为 0。
- 九类 SHA-256 为 eventProjection `1d02e735...aa07`、retryExecutionProjection `c4f4a843...31fe`、modelToolReceiptProjection `4d029400...b721`、forcedFinalProjection `30387bd5...8ced`、runtimeProjection `cfe81b1d...c1c`、sessionRoleContent `b9a2d2b5...67a2`、sessionToolMeta `76c4d8cf...b211`、terminalDom `062793b9...36f7`、refreshLifecycle `ae09c60e...fc44`；完整值见专题，bundle/direct classic 完全一致。
- 有效结果为 bundle 固化前/后与 classic 通过、infra 通过、连续两轮标准 H4 均 `35 passed`（单 worker、`retries=0`、exit 0）、H4-6A～J `20 passed` 且旧哈希保持、重复失败生产定向 `3 passed`、前端/P0 `199 passed`、完整 Python `1113 passed, 739 subtests passed`，以及 `check:frontend`、语法、diff 与资源清理通过。首次沙箱内 Chromium `spawn EPERM` 未进入产品路径；最初 DOM 机器字段过度约束失败不计通过，修订未削弱耐久/协议/Session meta 门禁。
- 文档收口后保持两份实现哈希不变，轻量复验 H4-6K bundle/direct classic `2 passed`、重复失败生产定向 `3 passed`、正式 H4 infrastructure 通过；两轮标准 H4、H4-6A～J、完整 Python 与前端/P0 沿用同一实现哈希下的上述有效结果，没有把未重跑项写成收口后重跑。
- 本阶段只证明合成 fixture 上同一 schema-valid、只读行范围失败的三次真实执行、第四次阻断、第五轮强制终答，以及 bundle/direct classic 同进程终态刷新零重执行；不覆盖执行前 schema/parse 失败限流、不同参数/工具、错误交替、强制终答失败分支、跨进程 active、取消、长输出或真实外部副作用 exactly-once。长期事实见 [`H4-6K 相同 read_file 失败限流与强制终答`](../../harness/h4-6k-identical-read-file-failure-bound-and-forced-final.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。

## 2026-08-08 03:36 · Codex

### Harness H4-6J `read_file` 缺少必填 `path` schema 失败收口

- H4-6J 只修改测试侧，为默认 bundle 与 direct classic 各增加一条固定原始 JSON 字符串 `"{}"` 的真实 Chromium 场景。参数成功通过 `json.loads()`，随后命中生产 `read_file` schema 的唯一 `path/required/is required`；这不是 H4-6I `parseError`，也不是 H4-6E `additional_property`。结果保持 `ok=false/action=read_file/errorCode=invalid_tool_arguments/failureCount=1`，生产委托、host 工具执行与 unsafe 均为 0。
- AgentRun 仍持久化唯一 `status=completed/outcome=failed` execution，原始 `"{}"` 在模型声明、`tool_started` 与 execution arguments 中闭合；失败 receipt 进入第二轮，父 Run 以九事件 completed、`nextCursor=9`、`nextSeq=10`、空 pending 收敛。Runtime 为 `4/0 → 4/3`，Session 为五角色链；Session/UI 显示 `read_file` action、没有 path，并保持失败 DOM 与刷新四项零增量。
- 八类 SHA-256 为 eventProjection `8e0d1a69...6ce5`、missingPathReceiptProjection `989616f6...fda7`、modelToolReceiptProjection `027989be...e497`、sessionRoleContent `73166b50...524b`、sessionToolMeta `b912f584...a6da`、activeDom `30ec16a7...72da`、terminalDom `ee55a3e8...9c88`、refreshLifecycle `04f95460...d3a4`；完整值见专题，bundle/direct classic 完全一致。
- 完整矩阵曾在共享失败 helper 中暴露活动态节点重绘竞态：具体旧节点取证得到 `Element is not attached to the DOM`，证明生产重绘替换 `<details>/<summary>`。修正只在重新取得的当前具体节点仍 open 时点击并立即证明关闭；旧节点断开且新节点已 closed 时不再误点，最终仍严格验证当前可见 locator 为 closed。没有修改生产、超时、worker、retry、产品状态或断言强度。修正前 `32/33` 失败轮和节点诊断失败轮均不计通过。
- 有效结果为原 H4-6I direct classic 独立连续两次通过、H4-6E/I/J bundle/classic `6 passed`、infra 通过、连续两轮标准 H4 均 `33 passed`（单 worker、`retries=0`、exit 0，完整命令 151.143/126.391 秒）、invalid-tool-arguments `1 passed`、前端/P0 `199 passed`、完整 Python `1113 passed, 739 subtests passed`，以及 `check:frontend`、Node/Python 语法、diff 和资源清理通过。
- 文档收口后保持两份实现哈希不变，轻量复验 H4-6J bundle/direct classic `2 passed`、原 H4-6I direct classic `1 passed`、正式 H4 infrastructure 通过；两轮标准 H4、完整 Python 与前端/P0 沿用同一实现哈希下的上述有效结果，没有把未重跑项写成收口后重跑。
- 更早一次正式入口的 infrastructure `/api/ping` `fetch failed` 仍不可复现、未定位唯一根因；R003 外层临时诊断载体超时没有形成产品结论。随后三次未经包装的正式 infra、最终 infra 门禁和两轮标准入口均通过；这些历史失败不计产品结果，也未通过放宽门禁掩盖。
- 本阶段只证明固定空对象缺少 `path` 的 bundle/direct classic schema required 生命周期、第二轮失败回执、父 Run completed 与同进程刷新零执行；不覆盖其他 required 字段、其他工具、真实模型/网络、跨进程 active、取消或发布。长期事实见 [`H4-6J read_file 缺少必填 path 的 schema 失败`](../../harness/h4-6j-read-file-missing-path-schema-failure.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。

## 2026-08-08 01:58 · Codex

### Harness H4-6I 工具参数 JSON 解析失败生命周期收口

- H4-6I 只修改测试侧，为默认 bundle 与 direct classic 各增加一条固定 malformed JSON 工具参数场景。假上游将原始字符串 `{"path":"fixture.txt"` 直接送入模型工具调用，真实进入生产 `json.loads()` / `parseError`，并在 `execute_registered_tool` 前以 `ok=false/action=read_file/errorCode=invalid_tool_arguments/fieldErrors=[]/failureCount=1` 失败；decoder error 只断言非空，不冻结全文。
- 原始坏 JSON 只在 AgentRun 的 `model_completed.toolCalls`、`tool_started.arguments` 和 execution arguments 中耐久闭合；Session/UI 经生产规范化后保留 `read_file` action、没有 path，也不保留原始坏 JSON。bundle/classic 均保持 `productionToolDelegations=0`、`toolExecutions=[]`、unsafe=0、九事件 completed、`nextCursor=9`、`nextSeq=10`、Runtime `4/0 → 4/3`、Session 五角色、失败 DOM 与刷新四项零增量。
- 八类 SHA-256 为 eventProjection `7bd9960c...bff6`、parseErrorReceiptProjection `4408c0ef...3558`、modelToolReceiptProjection `d86effa5...8e5a`、sessionRoleContent `dc6b9bee...497`、sessionToolMeta `f460fea9...c1da`、activeDom `e54e3de5...6cac`、terminalDom `0c10ac98...72e3`、refreshLifecycle `04f95460...d3a4`；完整值见专题，两种入口完全一致。
- 首轮唯一失败是测试把 DOM 无 path 参数误建模为字面 `{}`；真实格式保留 `action=read_file` 元数据但无 path。最终断言精确检查 action 存在、path 和原始坏 JSON 不存在，没有改变产品行为或放宽 timeout、retry、身份、失败结果和清理门禁。
- 哈希固化后 H4-6I `2 passed`，infra 通过；连续两轮标准 H4 均为 `31 passed`、单 worker、`retries=0`、exit 0。H4-6A～H 定向 `16 passed` 且旧哈希保持；invalid-tool-arguments AgentRun `1 passed`；前端/P0 `199 passed`；完整 Python `1113 passed, 739 subtests passed`；`check:frontend`、Node/Python 语法、diff 与资源清理通过。
- 本阶段只证明这一固定 malformed JSON 在 bundle/direct classic 中的 parseError 生命周期与同进程刷新零执行；不覆盖缺 path、其他解析错误、执行器失败、取消、长输出、跨进程 active 或真实外部副作用 exactly-once。长期事实见 [`H4-6I 工具参数 JSON 解析失败生命周期`](../../harness/h4-6i-tool-arguments-json-parse-error.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。

## 2026-08-08 00:53 · Codex

### Harness H4-6H `read_file` 缺文件生产执行器失败收口

- H4-6H 只修改测试侧，为默认 bundle 与 direct classic 各增加一条固定 `read_file({"path":"h4-missing-fixture.txt"})` 真实 Chromium 场景。该参数通过生产 schema；测试 wrapper 只新增这一个固定相对路径，拒绝绝对路径、`..`、额外字段及其他 action/path，满足前置条件后仍调用原 `execute_registered_tool → execute_read_file_tool`，由生产执行器在 `exists()/is_file()` 分支抛出“文件不存在”。当前 `resolve_project_path` 直接返回 project target；isolated home 同名不存在只作额外隔离审计，不是生产失败因果链。
- bundle/classic 均闭合单 AgentRun/单 toolCall、`productionToolDelegations=1`、`toolExecutions=1`、unsafe=0、九事件 completed、Runtime cursor `4/0 → 4/3`、Session `user → assistant → tool-call → tool-result → assistant`、失败 DOM 与刷新四项零增量。首次耐久 result 精确为 `ok=false/action=read_file/error=文件不存在/failureCount=1`，不含参数校验或 retry-limit 字段；固定目标在执行前、执行后及刷新后始终不存在。
- 八类 SHA-256 为 eventProjection `4dd3fb7c...314f2`、missingFileReceiptProjection `bef7e203...b498`、modelToolReceiptProjection `4204a133...2b8d`、sessionRoleContent `450f5347...c923`、sessionToolMeta `d8e40c3d...2c7e`、activeDom `b6ae61b6...c983`、terminalDom `2aed54d3...848f`、refreshLifecycle `04f95460...d3a4`；完整值见专题，两种入口完全一致。
- H4-6H 首轮 bundle/classic 各 `1 passed`，固化后合并 `2 passed`，infra 通过；连续两轮标准 H4 均为 `29 passed`、单 worker、`retries=0`、exit 0，完整命令 100.9/101.6 秒。H4-6A～G 定向 `14 passed` 且旧哈希保持；缺文件/路径穿越/AgentRun 定向 `4 passed`；前端/P0 `199 passed`；完整 Python `1113 passed, 739 subtests passed`；`check:frontend`、Node/Python 语法、diff 与资源清理通过。
- AppX Python 在沙箱内不可执行是产品收集前的环境限制；沙箱外以 Python 3.12.10/pytest 9.1.1 完成正式回归，不计产品失败，也未安装依赖、修改 PATH 或切换第三个解释器。
- 本阶段只证明固定缺失相对路径在 bundle/direct classic 中真实进入生产 `read_file` 执行器并闭合失败生命周期与同进程刷新零执行；不覆盖权限、编码、大文件、其他工具、取消、长输出、跨进程 active 或真实外部副作用 exactly-once。长期事实见 [`H4-6H read_file 缺文件生产执行器失败`](../../harness/h4-6h-read-file-missing-file-executor-failure.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。
