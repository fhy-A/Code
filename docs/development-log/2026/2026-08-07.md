# 2026-08-07

## 2026-08-07 09:52 · Codex

### Harness H4-6D direct classic 同轮双工具对等收口

- H4-6D 只修改 Playwright smoke：把 H4-6C 同轮双工具活动态与刷新态流程参数化为 runtime 驱动，并新增两条 direct classic 变体。classic 通过生成页直接入口运行，精确标记为 `classic-fallback` 且无 bundle ready；它复用同一 `MULTI_TOOL_USER`、两个受限 `read_file("fixture.txt")`、三个既有闸门及生产 AgentRun/Runtime/Session/DOM 链，不是自动 bundle 故障降级，也没有修改 host、白名单、协议、持久化或生产逻辑。
- classic 与 bundle 同样闭合单一 AgentRun、两个 Runtime、两个 toolCall、11 事件、T1→T2 声明/执行/结果、Session 两对 tool-call/tool-result、单组双项重绘保持和终态折叠。完成态 reload 后父 trace、工具组、T1/T2 均默认折叠，刷新增量 AgentRun POST、Runtime POST、chat、工具执行全部为 0。
- classic 精确匹配 H4-6C 八类语义 SHA-256：lifecycle `f5445145...307d`、refreshLifecycle `9421bf68...73de`、eventProjection `6e81cc9a...1342`、receiptProjection `35e8de41...42c1`、sessionRoleContent `033743ab...e2f7`、sessionToolMeta `23956f1c...842e`、activeDom `3fd7fd47...a0d0`、terminalDom `9efb0070...753cb`；H4-6A/H4-6B 六类旧哈希也保持原值，完整值见专题。
- bundle 两例 `2 passed`、classic 活动态/刷新态各 `1 passed`、infra 通过；连续两轮标准 H4 均为 `21 passed`、零 retry（完整命令约 69.7/69.2 秒），既有 H4-6A/H4-6B 四例 `4 passed`、bundle 再验证 `2 passed`、多工具 AgentRun 定向 `2 passed`、前端/P0 `199 passed`、完整 Python `1113 passed, 739 subtests passed`，`check:frontend`、语法、diff 与资源审计通过。首个带首尾锚点的 `--grep` 未匹配 Playwright 完整标题且未启动浏览器 case，不计入产品失败或通过轮次；唯一标题子串随后正常通过。收口时 smoke、host 与生产哈希不变，因此文档后只复跑 bundle/classic 四例和 infra，其余完整矩阵沿用同一实现/测试输入的已完成结果。
- 本阶段只覆盖 direct classic、同轮两个成功受限读取与同进程刷新；不覆盖自动 fallback 后工具任务、工具失败/取消/长输出、异构工具、跨进程 active、外部副作用 exactly-once 或主观视觉闪烁。长期事实见 [`H4-6D direct classic 同轮双工具对等`](../../harness/h4-6d-direct-classic-same-round-multi-tool-parity.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。

## 2026-08-07 09:06 · Codex

### Harness H4-6C 同轮双工具生命周期与顺序收口

- H4-6C 只在测试侧为默认 bundle 增加同一模型轮两个受限 `read_file("fixture.txt")`：T1 只含 path，T2 另含 `startLine=1/endLine=1`，没有扩大 action/path 白名单。`before-second-tool-execute` 只命中 T2，位于 T2 `tool_started` 已持久化而生产执行器尚未委托之间；闸门时 T1 completed/succeeded、T2 running、六事件前缀、一次 chat 和一次生产工具执行闭合，释放后 T2 只执行一次。
- 终态 AgentRun 固定为 11 事件，Session 角色链为 `user → assistant → tool-call(T1) → tool-result(T1) → tool-call(T2) → tool-result(T2) → assistant`；两对消息以 toolCallId、agentRunId、arguments、lineRange/size/truncated/result 精确配对。浏览器始终只有单工具组和 T1→T2 两项，运行中用户展开跨 T2 结果与最终正文生产重绘保持，终态折叠；完整 reload 后父 trace、工具组和两个单项均默认折叠，刷新增量 AgentRun POST、Runtime POST、chat 与工具执行全部为 0。
- 首轮定向暴露的是服务端事实已闭合而前端 DOM 尚未消费完毕的短暂窗口；R002 只在稳定语义采样前用 Playwright 等待既有单组、双项、T1 succeeded/T2 running 和结果唯一信号，没有 sleep、手写 retry、额外超时、结果放宽或生产修改。该自动证据只证明结构、顺序、身份和唯一性，不能宣称人眼感知的整片闪烁、布局跳动或滚动问题已经解决。
- 八个固定语义 SHA-256 为 lifecycle `f5445145...307d`、refreshLifecycle `9421bf68...73de`、eventProjection `6e81cc9a...1342`、receiptProjection `35e8de41...42c1`、sessionRoleContent `033743ab...e2f7`、sessionToolMeta `23956f1c...842e`、activeDom `3fd7fd47...a0d0`、terminalDom `9efb0070...753cb`；H4-6A/H4-6B 六个既有领域哈希全部保持原值，完整值见专题。
- 场景 A/B 在冻结哈希前后各 `1 passed`，infra 通过；连续两轮标准 H4 均为 `19 passed`、零 retry（Playwright 54.7/53.7 秒，完整命令 62.8/61.8 秒）；既有 H4-6A/H4-6B 四场景 `4 passed`，多工具 AgentRun 定向 `2 passed`，前端/P0 `199 passed`，完整 Python `1113 passed, 739 subtests passed`，`check:frontend`、语法、diff 与资源清理通过。收口文档后两个测试文件哈希不变，因此仅复跑 H4-6C A/B、既有四场景和 infra，其余结果按同一实现/测试输入沿用。
- 本阶段不覆盖 classic 多工具、异构 action/path、工具失败/取消/长输出、跨进程 active 恢复、工具外部副作用 exactly-once、问卷/授权、压缩、图片、队列/并行/Child，也不构成人工视觉闪烁验收。长期事实见 [`H4-6C 同轮双工具生命周期与顺序`](../../harness/h4-6c-same-round-multi-tool-lifecycle.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。

## 2026-08-07 08:02 · Codex

### Harness H4-6B direct classic 工具详情对等收口

- H4-6B 仅修改 Playwright smoke：把 H4-6A 两条 bundle 流程整理为 runtime 参数化 helper，并新增两条 direct classic 变体。classic 通过生成页直接入口运行，精确标记为 `classic-fallback` 且无 bundle ready；它复用相同生产模块、双闸门与唯一 `read_file("fixture.txt")`，不是自动 bundle 故障降级场景。
- 两条 bundle 基线的名称、断言和六个领域哈希保持不变。classic 活动态同样证明第二轮正文重绘后稳定 key 与 open 保持，AgentRun `model`、第二条 `model_started.runtimeRunId`、`activeRuntimeRunId` 和第二个 Runtime 身份闭合，第一/第二轮 Runtime 分别为 completed cursor 4 与 running cursor 0；前端终态四项信号闭合后工具组自动折叠。
- classic 完成态按父 execution trace→工具组→单项三层真实展开/折叠；完整 reload 后三层默认折叠，Session 角色链、toolCall/AgentRun 身份、九事件和 DOM 唯一保持，刷新增量 AgentRun POST、Runtime POST、chat、工具执行均为 0。六个语义 SHA-256 与 H4-6A 完全一致，完整值见 H4-6B 专题。
- 参数化后的 bundle 两条定向、classic 活动态和 classic 刷新定向均通过，infra 通过；连续两轮标准 H4 均为 `17 passed`、零 retry（Playwright 58.5/57.3 秒，完整命令 66.8/65.5 秒）。前端/P0 `199 passed`、完整 Python `1113 passed, 739 subtests passed`、`check:frontend`、Node 语法、diff 和资源清理通过。收口时 smoke 哈希保持 `33c1e2ca...76c5`，因此完整矩阵沿用同一实现/测试输入的已完成结果。
- 本阶段不覆盖自动故障降级后的工具任务、多工具、失败/取消、长输出、问卷/授权、压缩、图片、队列/并行/Child、跨进程 active 恢复或工具副作用 exactly-once；未修改生产代码、host、协议、JSONL 或持久化。
- 长期事实同步至 [`H4-6B direct classic 工具详情对等`](../../harness/h4-6b-classic-tool-detail-parity.md)、[`Harness 工程优化指导`](../../harness-engineering-optimization-plan.md) 与 [`TODO`](../../../TODO.md)。

## 2026-08-07 07:08 · Codex

### Harness H4-6A 工具详情生命周期收口

- H4-6A 只在测试侧为默认 bundle、单个 `read_file("fixture.txt")` 增加 `before-tool-final-delta` 与 `before-tool-terminal` 两个固定闸门及两条真实 Chromium 场景。活动链证明工具组初始 closed、用户真实展开后，第二轮终答正文 delta 触发生产 DOM 节点替换时同一 `data-tool-process-key=0:1` 和 open 状态保持；此时 AgentRun 为 `model`，第二条 `model_started.runtimeRunId`、`activeRuntimeRunId` 与第二个 Runtime 身份闭合，第一轮 Runtime 为 completed cursor 4，第二轮为 running cursor 0。
- 终态链先独立核对服务端 AgentRun `completed`、九事件、`nextCursor=9` 与空 `activeRuntimeRunId`，随后等待前端 active banner 消失、stop 禁用、active trace 0 和 completed trace 1；四项闭合后单次 DOM 读取证明工具组自动折叠。服务端 terminal 早于前端 terminal 是正常收敛窗口，`Worked for` 本身不作为前端终态信号。
- 完成态按真实三层交互先展开 completed execution trace，再展开工具组和单工具详情；参数与格式化结果包含 `fixture.txt`、`26 B` 和恰好一份受控正文，随后按单项、工具组、父 trace 反向折叠。刷新场景在三层曾展开后执行完整 reload，并通过生产 Session 恢复同一会话；刷新后三层均默认折叠，重新展开仍可查看，说明展开偏好为页面瞬时状态而不持久化。
- 刷新保持 Session 角色链 `user → assistant → tool-call → tool-result → assistant`、同一 toolCall/AgentRun 身份、九事件、稳定 key 与 DOM 唯一；刷新增量 AgentRun POST、Runtime POST、chat、工具执行均为 0。六个固定语义 SHA-256 为 lifecycle `de27ce93...1487`、refreshLifecycle `0712a70b...2ec8`、eventProjection `36658361...9d48`、sessionRoleContent `c6b7c90b...442a`、sessionToolMeta `587b9b63...3fe9`、terminalDom `71a1ebdf...0712`，完整值见 H4-6A 专题。
- 场景 A/B 在冻结哈希前后各 `1 passed`，infra 通过；连续两轮标准 H4 均为 `15 passed`、零 retry（完整命令 52.1/50.1 秒）；前端/P0 `199 passed`，完整 Python `1113 passed, 739 subtests passed`，`check:frontend`、Node/Python 语法、`git diff --check` 和资源清理通过。收口文档完成后测试文件哈希保持不变，因此仅复跑 A/B 与 infra，其余完整矩阵沿用同哈希验证结果。
- 本阶段不覆盖 classic、多工具、失败/取消、长输出、队列/后台/Child、跨刷新持久化展开偏好、跨进程 active 恢复或工具副作用 exactly-once；未修改生产代码、协议、JSONL 或持久化。

**涉及事实源**：`../../harness/h4-6a-tool-detail-lifecycle.md`、`../../harness-engineering-optimization-plan.md`、`../../../tests/e2e/h4/isolated_host.py`、`../../../tests/e2e/h4/smoke.spec.cjs`、`../../../TODO.md`

## 2026-08-07 05:55 · Codex

### Harness H4-5B1 终态工具轨迹跨进程重载与零重执行收口

- H4-5B1 复用 H4-5A 的真实 generation A→B 进程边界：A 以两轮模型交互完成唯一 `read_file("fixture.txt")`，AgentRun 进入 completed 并持久化后完全退出；B 以新 PID、新随机 loopback 端口和新 origin 从同一受控 root/data/project/home 启动，经生产 Session 列表和可见 UI 打开同一 Session。A 为 AgentRun POST 1、Runtime POST 0、chat 2、工具执行 1；B 四项增量均为 0，两个旧 Runtime 按各自 ID 返回 404且不创建替代 Runtime。
- AgentRun 九事件固定为 `created → model_started → model_completed → tool_started → tool_completed → model_pending → model_started → model_completed → completed`，`nextCursor=9`、耐久 `nextSeq=10`、`pendingToolCalls` 为空且 terminal 唯一。第一轮阶段 Runtime 对应 cursor 4，第二轮最终 Runtime 对应 cursor 3；Session 角色链为 `user → assistant → tool-call → tool-result → assistant`，两条工具消息以同一 toolCallId/agentRunId 闭合。DOM 精确为 ordinary assistant 2、tool-process 1、assistant 总数 3，user/阶段/工具/结果/final 各一次且有序。
- 固定八个无随机 ID/时间的语义 SHA-256：工具结果 `1895281c...691c`、execution `1783025d...7c`、事件 `85dfc1ee...a1e`、耐久投影 `b1c30c05...6122`、Session role/content `ecfbdadd...a9a`、工具 meta `587b9b63...fe9`、DOM `37d1870e...8e91`、最终 result `e40fb4ba...bdef`；完整值见 H4-5B1 专题。
- 同阶段移除一个已确认不安全的 H4 测试基础设施共享 stdout 边界：Python host 使用仅覆盖 `log_message()` 的 H4 专用 CodeHandler 子类，使生产访问日志不再与控制 JSONL 共用 stdout；生产 `server.py`、业务处理、Node parser、5 秒上限、ack 顺序与控制协议均未改。infra 以固定 8 个 loopback HTTP 请求和 8 个 `metrics` 控制命令并发自检连续 5 轮通过，pending、进程、端口和临时根归零。该结果不得写成已唯一定位或修复历史 `release-model` 超时根因，Windows pipe/readline、Node event loop 与 pending 竞态仍未排除。
- 最终测试文件形态验证：plain-text 与 H4-5B1 定向各 `1 passed`；连续两轮标准 H4 均为 infra 通过、`13 passed`、零 retry（完整命令 48.05/48.00 秒）；H3-2C2 为 `5 passed, 20 subtests passed`，AgentRun 工具回执恢复定向 `6 passed`，前端/P0 `199 passed`，完整 Python `1113 passed, 739 subtests passed`（89.53 秒）；`check:frontend`、Node/Python 语法和 `git diff --check` 通过，H4 子进程、端口、临时根、output 与暂存区归零。收口时测试文件哈希未变，因此文档后仅复跑 infra 与 H4-5B1 精确定向，其余完整矩阵沿用该同哈希验证结果。
- 本阶段只证明 terminal 工具轨迹跨进程重载与 B 零重执行，不证明 active worker 消费 completed receipt、工具外部副作用 exactly-once、Runtime 持久化、非终态自动恢复或真实进程崩溃。active receipt consumption 仍须先明确 explicit/auto resume 及是否允许新模型请求，再单独审批。

**涉及事实源**：`../../harness/h4-5b1-terminal-tool-trace-cross-process-reload.md`、`../../harness-engineering-optimization-plan.md`、`../../../tests/e2e/h4/isolated_host.py`、`../../../tests/e2e/h4/infrastructure-selfcheck.cjs`、`../../../tests/e2e/h4/smoke.spec.cjs`、`../../../TODO.md`

## 2026-08-07 04:40 · Codex

### Harness H4-5A completed AgentRun 跨进程终态重载收口

- H4-5A 为隔离 host 增加受 owned root 约束的 generation A/B 生命周期：A 完成纯文本任务并持久化后完全退出、两个随机 loopback 端口关闭，B 以不同 PID、新随机端口和新 origin 启动，同时复用同一临时 root/data/project/home。普通 host 的 stop/root 删除语义不变；B 启动失败、浏览器失败和重复 cleanup 仍须最终清理当前记录 PID、两代端口与唯一根目录。
- 新浏览器场景通过生产 Session 列表和可见 UI 在 B 中选择同一 Session，没有注入私有 state、localStorage 或 reducer。A 为 AgentRun POST 1、Runtime POST 0、chat 1、tool 0；B 四项均为 0。B 读取同一 completed AgentRun，事件为 `created → model_started → model_completed → completed`、terminal 唯一、cursor 4，旧 Runtime GET 返回 404 且不创建替代 Runtime；Session role/content 哈希、result 哈希及 DOM user/assistant/final 唯一性均保持。普通前台 `clientRequestId` 当前为空，本阶段只证明 A/B 保留同一实际值。
- 基础设施自检固定 A/B PID 不同、A 端口关闭先于 B ready、跨代 owned path 相同、A 停止后 root 保留、最终删除和 B 启动失败清理；环境白名单、父 secret 隔离、只读工具边界、5 秒命令上限、退出分类和 stop 幂等没有放宽。
- 历史两次标准矩阵分别出现 `release-model` 与 `release-model-catalog` 控制响应 5 秒超时，命令副作用和任务 terminal 随后成立。临时有界诊断的正常样本未复现，无法唯一归因 Python emit、Windows pipe/readline、Node event loop 或 pending 竞态；R003/R004 诊断已完整回退，没有迁移 HTTP、放宽超时、增加 retry/sleep、吞错或把副作用发生视作命令成功。该问题保留为低概率测试基础设施风险，不宣称已经修复，也不描述为产品跨进程失败。
- 最终文件形态验证：H4-5A 定向 `1 passed`（5.1 秒）、infra 通过；连续两轮标准 H4 均为 `12 passed`、零 retry，Playwright 45.6/41.6 秒、完整命令 55.2/51.0 秒；H3-2C2 为 `5 passed, 20 subtests passed`，completed/tool receipt 定向 `3 passed`，前端/P0 `199 passed`，完整 Python `1113 passed, 739 subtests passed`（144.40 秒）；`check:frontend`、Node/Python 语法和 `git diff --check` 通过，H4 子进程、端口、临时根、output 与暂存区归零。
- 本阶段只证明 completed/terminal 纯文本 AgentRun 的真实跨进程重载，不证明 active Run 自动续流、部分正文跨进程保留、同 origin/原标签页重连、Runtime 持久化、completed 工具回执 exactly-once，或问卷/授权、压缩、图片、队列/并行/Child 等其他 DOM 生命周期。

**涉及事实源**：`../../harness/h4-5a-completed-agentrun-cross-process-reload.md`、`../../harness-engineering-optimization-plan.md`、`../../../tests/e2e/h4/isolated-host.cjs`、`../../../tests/e2e/h4/infrastructure-selfcheck.cjs`、`../../../tests/e2e/h4/smoke.spec.cjs`、`../../../TODO.md`

## 2026-08-07 02:44 · Codex

### Harness H4-4 自动 bundle 失败降级收口

- H4-4 在默认根入口的真实 Chromium 中新增 `bundle-load failure automatically falls back to classic` 与 `bundle-init failure automatically falls back to classic` 两条场景。每例只对 `code.bundle.js` 注入一次页面级故障，随后由生产 `location.replace()` 进入带精确 fallback 原因的生成 classic 页面；最终 runtime 标记、导航序列、classic 脚本单次初始化和无重定向循环均已固定。
- 降级后的生产纯文本任务保持 AgentRun POST 1、Runtime POST 0、上游 chat 1、工具 0，AgentRun/Runtime 身份各唯一，user/assistant/final 各一次；load 失败只冻结目标 bundle 的 `requestfailed` 类别与次数，init 失败无该事件，不冻结 Chromium 完整错误正文，也不以 `history.length`/`goBack()` 作为门禁。
- 首次完整矩阵第二轮曾在既有 classic plain teardown 暴露 shutdown 成功响应与正常 exit(0) 的 Node host 竞态：资源均已清理，但 pending shutdown 被旧 child-exit 处理误报为 `H4 isolated host exited (0)`。阶段按门禁停止后，以 pending 命令身份和纯退出分类修正；仅 `stop()` 发起的 shutdown + exit 0 + signal null 可由退出收敛，非零、signal、pending 非 shutdown 和非 stop shutdown 仍明确拒绝，没有过滤错误字符串或清空 `cleanupErrors`。
- 修正后 infra 真实自检连续 5/5 通过，两条 H4-4 定向为 `2 passed`，两轮全新标准 H4 均为 infra 通过、`11 passed`、零 retry（Playwright 约 31.1/33.1 秒）。随后提交前复验又独立出现一次 `H4 host command timed out: metrics`：浏览器功能断言已经通过，资源最终归零，但阶段仍按门禁停止，没有把失败轮靠重跑计为通过。
- 只读审计没有发现明确锁反转，历史输出也无法区分 production snapshot 与 Session JSONL 阶段，因此在 Python host stderr 增加只含固定 phase、递增序号、单调耗时和结果类别的 metrics breadcrumb，并由 smoke 记录测试体 AgentRun/Runtime 终态及 cleanup 首错诊断；stdout 控制协议、5 秒上限、stop-time metrics、`cleanupErrors` 和严格资源清理均未放宽。诊断后一次 bundle-init 定向通过，累计三轮标准 H4 均为 infra 通过、`11 passed`、零 retry；两条自动降级测试体均为 AgentRun `completed(cursor 4)` 且终态事件存在、Runtime `completed(cursor 3)`，stop-time metrics 每例请求 2 次、最大 15～16 ms，主要耗时为 Session JSONL，其余阶段约 0 ms。历史单次 metrics 超时仍未复现、根因未定位，不宣称已经通过等待终态、调整 teardown 或修改快照锁修复。
- `npm run check:frontend`、三个 H4 Node 与 Python host 语法、`git diff --check` 通过；R003 的前端模块 `171 passed` 与完整 Python `1113 passed, 739 subtests passed` 在相关生产和单元测试输入哈希未变化的前提下沿用；H4 子进程、端口、临时根、output 和暂存区归零。
- 本阶段只证明同进程默认根入口的自动 bundle-load/bundle-init 降级和降级后纯文本闭环。direct classic 刷新仍由 H4-3 单独证明；服务重启/跨进程、工具、问卷/授权、压缩、图片、队列/并行/Child 及其他 H4 生命周期继续保留为后续事项。

**涉及事实源**：`../../harness/h4-4-automatic-bundle-fallback.md`、`../../harness-engineering-optimization-plan.md`、`../../../tests/e2e/h4/smoke.spec.cjs`、`../../../tests/e2e/h4/isolated-host.cjs`、`../../../tests/e2e/h4/infrastructure-selfcheck.cjs`、`../../../tests/e2e/h4/isolated_host.py`、`../../../TODO.md`

## 2026-08-07 01:23 · Codex

### Harness H4-3 classic fallback 刷新兼容收口

- H4-3 将 H4-2 三条刷新流程整理为 runtime-aware 共用辅助逻辑，在不修改生产代码、隔离 host、Playwright 配置或协议的前提下，为 classic fallback 直接入口新增首增量前刷新、两段后刷新继续追加、两段后刷新再取消三条浏览器变体。classic 与 bundle 继续加载同一 `app.js`、`agent-runtime.js` 以及 Session/AgentRun/Runtime 恢复链，没有复制第二套刷新状态机。
- 六条 bundle/classic 刷新场景共同固定同一 AgentRun/Runtime、AgentRun POST 1、Runtime POST 0、上游 chat 1、工具 0、模型目录闸门返回前已重附着、user/assistant 唯一、DOM 前缀不回退、成功终答唯一，以及取消唯一 DELETE、cancelled、唯一暂停说明且无 `model_completed`。成功与取消终态 JSONL 均不包含 `streaming/_streamProjection`，页面错误为 0，测试资源清理归零。
- 标准 H4 入口连续两轮均为 infra 自检通过、`9 passed`、零 retry；Playwright 约 26.5 秒，完整命令约 31.1/31.3 秒。classic 三场景 Runtime GET 两轮观测为 8/5/4 与 8/6/4，取消延迟为 238/231 ms；这些仅是本地合成环境的诊断值，不作为跨环境性能阈值。
- 同轮 `npm run check:frontend`、smoke spec Node 语法与 `git diff --check` 通过；前端/P0 `199 passed` 和完整 Python `1113 passed, 739 subtests passed` 的实现与测试输入哈希未变化，沿用为本阶段回归证据。
- 本阶段只证明 direct classic fallback 的同进程模型流式刷新，不证明自动 bundle-load 失败降级、服务重启/跨进程恢复、工具刷新或其他 H4 DOM 生命周期。

**涉及事实源**：`../../harness/h4-3-classic-fallback-refresh.md`、`../../harness-engineering-optimization-plan.md`、`../../../tests/e2e/h4/smoke.spec.cjs`、`../../../TODO.md`

## 2026-08-07 00:55 · Codex

### Harness H4-2 真实刷新恢复补充修正与人工验收

- H4-2 原隔离浏览器回归已证明 Runtime 快照/游标追赶，但真实 Code Dev 人工验收仍可复现两条历史问题：首个正文增量前刷新或已有部分正文后刷新，页面会恢复运行状态与累计计时，但正文要等到终态才一次性出现；刷新后暂停也会因重附着过晚而错过实时窗口。不刷新的对照任务流式正常，排除了上游本身不流式。
- 真实页面时间线将根因定位为启动顺序：Session 恢复后仍等待 9 次串行 `/proxy/models` 模型目录请求，目录阶段约从 1.56 秒持续到 20.48 秒，首个 AgentRun GET 约在 20.61 秒才出现，此前没有 Runtime GET。`app.js` 现在保持 `platformSyncPromise` 和 `authExpired` 检查不变，但在 `refreshModels()` 之前立即调用既有 `sessionStartup.startRecovery()`，避免活动 AgentRun/Runtime 被模型目录阻塞。
- H4 隔离上游增加有界模型目录闸门与 arm/reached/released 时间线。三条 bundle 刷新场景都要求目录闸门仍阻塞时已产生新 Runtime GET：首增量前能恢复流式正文，两段后能恢复原前缀并在 terminal 前追加，取消能发出唯一 DELETE。本轮标准 H4 结果为 infra 通过、`6 passed`、零 retry；三例 Runtime GET 分别为 7/6/4，每例 AgentRun POST 1、Runtime POST 0、上游 chat 1，取消延迟 121 ms，不存在第二 Run、Runtime、assistant 或模型请求。
- 修正后的真实 Code Dev 人工复验为：发送后立即刷新，短暂等待后恢复正常流式；已有正文后刷新，短暂等待后恢复已有正文并继续流式追加。用户确认“基本能满足需求”。这一完成声明保留短暂重附着延迟，不推定为正文瞬时恢复；当前人工复验重点是两条成功恢复，刷新后取消仍以自动 H4 的唯一 DELETE、cancelled 终态和 121 ms 延迟为证据。
- 完整回归首轮同时发现 D3 固定时钟仅作用于隔离 VM，生产消息 HTML 投影仍使用宿主日期，导致跨日后 19 个场景的 UI 哈希漂移。测试现在仅在 `projectMessages()` 调用边界使用已有 `FixedDate` 并在 `finally` 恢复宿主时钟；D3 定向为 `5 passed, 19 subtests passed`，19 个场景哈希和 suite hash `50ff1567e7477d6438bfc7e8175a3936f04177a089a4b4ae5acc0a93a0a2a657` 原样恢复，未更新 fixture 或掩盖语义变化。
- 收口验证为前端构建/新鲜度通过、前端与 P0 定向 `199 passed`、完整 Python `1113 passed, 739 subtests passed`、Node/Python 语法和 `git diff --check` 通过。H4 子进程、临时根、output 文件和暂存区均为 0。修正不修改 `server.py`、AgentRun/Runtime 协议、Session JSONL/runState、`beforeunload`/`sendBeacon`、classic 刷新、release 或其他 H4 场景。

**涉及事实源**：`../../harness/h4-2-model-stream-refresh-recovery.md`、`../../harness-engineering-optimization-plan.md`、`../../../app.js`、`../../../tests/e2e/h4/smoke.spec.cjs`、`../../../tests/test_frontend_modules.py`、`../../../tests/test_p0_stability.py`、`../../../tests/test_harness_manual_compaction_failure_boundaries.py`、`../../../TODO.md`
