# 2026-09-02 开发日志

## 2026-09-02 01:50 · Codex

### CODE-062 · XLSX Skill 自有重算资源与路径闭环

- 以 Code 产品合同、公开 XLSX / LibreOffice 接口和 clean-room 实现补齐自有 `recalc.py` 与受控 `soffice.py`，整个阶段未读取、哈希、搜索或复制受保护的专有 Skill / Office 脚本来源。`use_skill` 现在通过服务端白名单 `code-resources.json` 返回经身份、tombstone、普通文件 / reparse、大小与 SHA-256 校验的绝对 `runtimeResources` 路径，并明确要求模型直接使用而不得自行搜索或复制脚本。
- 新安装使用安装目录中的精确资源；旧安装若仍是已知旧版 Skill / 依赖身份且自有资源缺失，则只读回退到当前 bundled 资源，不覆写旧目录、不批量迁移。未知同名 Skill、部分冲突资源、路径穿越、符号链接 / reparse、哈希不符或 tombstone 均稳定 fail-closed；旧安装的依赖声明同样只读映射到当前兼容 manifest。
- XLSX 能力拆分为普通 `create-edit` / `inspect` 与需要公式重算的 `create-edit-formulas` / `render`。只有后两者要求严格验证真实 LibreOffice：解析到的 `soffice.exe` 必须具备同目录 `soffice.bin` 与 `fundamental.ini`，PATH 包装脚本不能冒充安装；其他 Skill 的既有 command 依赖解析保持不变。
- 重算脚本只接受受控 `.xlsx`，以参数数组、独立 LibreOffice profile、有限超时和进程树清理运行；默认阻断 externalLinks、connections、queryTables、外部关系、外部工作簿公式以及 `WEBSERVICE` / DDE / RTD / URL / UNC 等外部引用，只有显式 `--force` 才放行。结果在隔离副本中验证后以同目录临时文件、`fsync` 与 `os.replace` 原子发布；失败保留原文件并清理临时资源，JSON 区分 `success`、`errors_found` 与稳定错误码。
- fail-first 先稳定得到缺失资源模块的 `1 error`；完成后资源 / 布局直接组 `19 tests`、Skill 依赖 / builtin / 同步 / runtime binding `86 tests`、AgentRuntime 与 P2 Skill CRUD `151 tests`、完整 frontend `417 tests`、server / security / P2 `406 tests` 通过，doctor 在任务开始与重要运行时冻结后均为 `10/10`，frontend build / freshness、Python / JavaScript / JSON / 资源哈希与 diff 检查通过。
- 受控临时目录中的真实 LibreOffice 验证覆盖正常公式重算、错误公式汇总、外部引用阻断、超时与发布失败恢复；最终严格依赖合同定位 `C:\Program Files\LibreOffice\program\soffice.exe` 并成功重算。非发布 PyInstaller 首次仅因测试侧归档路径分隔符比较错误未通过断言，修正断言后的独立最终构建确认 5 个 XLSX 资源进入 EXE（32,761,369 bytes，SHA-256 `e683402158291097a5f7374dbc83e4b78b3a058e9f13c909e5420462de075f05`），临时根完整清理；该构建未替换用户安装或进入发布。
- `CODE-062` 已从私有 TODO 删除且 Code 游标保持 `CODE-062`；`CODE-055` 当前主机后续 `bypass` 评测仍暂停，资源闭环和既有自动模式范围引导都不自动解除门禁，须先完成审批侧独立复核并由用户再次明确授权。未运行模型、H4、Browser、外网、安装、push、tag 或 release；受保护用户 data、3011 页面和无关活动 handoff 均未触碰。

## 2026-09-02 00:53 · Codex

### CODE-061 · 自动模式范围引导与风险校准

- 按用户确认的产品边界，不为自动模式增加项目外路径硬沙箱，也不把工作区误述为不可越过的安全边界。`bypass` 模型提示现在明确：工作区是默认操作范围；用户明确提供的外部路径可在任务需要时访问；未经明确要求不得自行扩张到父目录、相邻目录、用户主目录或磁盘根，也不得为寻找工具、依赖或 Skill 脚本递归扫描用户目录或磁盘；工作区外写入、覆盖或删除需要明确用户意图。发现范围风险时应先缩小范围或改用更安全策略，仍无法完成则在最终回答说明，不以中途提问打断自动任务。
- Auto 选项提示和一次性确认弹窗的中英文文案同步说明项目外路径可能被访问、自动模式并非完全安全；保留危险 / 系统级命令、系统安装、破坏性 Git、工具预算及参数校验等现有基础限制。文案不再声称“越界路径统一被服务端拦截”。
- 工具白名单、`request_user_input` 工具可用性、`resolve_project_path`、文件 / 命令工具、授权状态、`pendingAuthorization`、一次性 Auto 确认、协议与持久化均未改变；本阶段只是模型范围引导与风险披露，不是安全沙箱，也未证明任意 shell 子进程受到项目路径隔离。
- 直接合同先稳定得到 `2 failures`，实施后权限提示 / 风险弹窗 / 工具集 / 状态机核心组 `4 tests` 通过，完整 `tests.test_frontend_modules` `417 tests` 通过；doctor `10/10`，frontend build / freshness、bundle 语法与后续静态 diff 检查通过。未运行 H4、Browser、真实项目外路径访问或评测恢复。
- 同步核对到独立 XLSX 缺口：`data/skills/xlsx/SKILL.md` 引用自身 `scripts/recalc.py`，但现行可分发目录只有 `SKILL.md` 与 `dependencies.json`，构建 / 安装又只同步 `data/skills`。私有 TODO 游标推进到 `CODE-062`，单独登记 Code 自有重算资源、路径、依赖与打包闭环；未读取或复制受保护的 `claude-skills` 专有来源。`CODE-055` 的当前主机 `bypass` 评测继续暂停，待 `CODE-062` 完成、独立复核并由用户再次明确授权后才可恢复。
- 受保护 `data/generated-assets/`、`data/image-route-registry.json` 未读取、哈希、修改或暂存，私有活动 handoff 未覆盖；未执行 push、tag、release、外网或其他外部操作。
