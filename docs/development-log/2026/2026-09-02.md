# 2026-09-02 开发日志

## 2026-09-02 22:14 · Codex

### CODE-066 · DATA_DIR 单写者与更新器精确交棒收口

- 针对“新建 AgentRun 首次读取不可用”的多进程 AgentRun / `readContext` / admission 实验经严格复核后已完整回退：该方向并非既定产品场景，且曾引入兼容回归；最终代码不包含新增 AgentRun 读取协议、诊断上下文、admission 模块或数据迁移，既有 Session / AgentRun 格式与恢复语义保持不变。
- 最终方案收敛为同一 `DATA_DIR` 的单写者合同。新增稳定的目录外 sidecar 内核锁，拒绝目录、reparse、硬链接、路径 / 句柄身份变化及不可用存储；同目录第二实例 fail-closed，不同数据目录可独立运行，进程退出后由操作系统释放锁而永久 sidecar 保持。`server.py`、`launcher.py`、`dev_server.py` 三个正式入口都在任何目录创建、旧目录迁移、route catalog / image service 初始化、installer 或 Browser 启动前取得 owner；busy / unavailable 返回稳定错误且不会先触碰数据目录。
- 正式 updater 不再按名称枚举或强杀任何 `Code-*` 进程，只等待触发它的精确 source PID 有界退出。父进程退出后，helper 以绝对词法 sidecar 路径取得同一 Windows byte-range 锁，并在锁前 / 锁后校验 reparse、目录、硬链接和 FileId；只有持锁后才允许写 `update.log`、完成 `.part` 移动和旧 EXE 清理，随后先释放 / dispose 再启动替代进程。timeout `20`、busy `22`、unsafe / unavailable `24` 均零目标写入且零替代启动。
- STRICT 证据包含真实 batch 启动临时 Windows replacement EXE，replacement 只有取得同一 `DATA_DIR` owner 后才写 ready marker；失败路径以真实 executable sentinel 验证零启动，全部 holder / updater / launcher / helper、batch 与 TEMP 根均以 deadline、精确 PID 和独立清理汇总闭合。最终 `TestUpdaterHelpers` 为 `26 passed + 7 subtests`，唯一 `1 skipped` 是当前 Windows 缺少文件符号链接权限（`WinError 1314`）；`data_dir_owner`、dev、updater、server startup、launcher 与 existing-browser-refresh 组合为 `60 passed + 10 subtests`，产品冻结时 server / security 为 `361 passed + 47 subtests`，AgentRuntime 为 `137 passed + 81 subtests`，legacy recovery 为 `5 passed + 20 subtests`，doctor `10/10`，Python compile 与 diff 检查通过。
- `CODE-066` 已从私有 TODO 删除，`CODE-068` 的悬空 related 引用同步移除，Code 编号游标保持 `CODE-068`。公共 `code/TODO.md` 未更新；受保护的 `data/generated-assets/` 与 `data/image-route-registry.json` 未读取、修改或暂存，未执行 push、tag、release 或线上操作。

## 2026-09-02 11:49 · Codex

### CODE-065 · 可迁移 Skill 资源闭环与 PPTX clean-room 修复

- 先以当前分发目录稳定复现 use_skill(pptx) 缺少 runtime resource binding；随后只基于公开 OOXML / LibreOffice 接口完成 Code 自有 PPTX validate-deck 与 render-pdf helper、版本化 code-resources.json 和依赖声明。模型只取得经服务端白名单、普通文件 / reparse、大小与 SHA-256 校验后的精确 runtimeResources，无需也不得搜索、复制或替换 helper；本阶段未读取、哈希、导入或复制受保护的专有来源。
- 资源合同现在同时覆盖 pptx 与 xlsx：新安装从 active Skill 目录绑定资源；仅已知旧版 bundled 身份且资源缺失时只读使用当前 bundled fallback，不覆写旧安装；同名 custom Skill 必须携带自己的 manifest 和相对路径 / 哈希，随目录移动仍可重定位，未知、缺失、篡改、冲突或 reparse 均稳定 fail-closed。没有声明资源的 custom Skill 不会获得 helper 路径；解析器识别并投影无资源 custom 条件时才返回 custom-no-resources 的不猜测路径指引；这不改变自动模式的 full access、通用工具权限、Session / AgentRun 持久化或协议。
- 独立复核发现默认 Code Dev 中 active Skills 根与 bundled 根同为 data/skills 时，缺少 compatibleInstalled 的有效 custom manifest 曾被误按 bundled 解析。R003 已将该同根拓扑收敛为显式 custom contract 的 local 资源 / dependency 投影；带 compatibleInstalled 的 Code-owned 合同仍走 installed / bundled-fallback，PPTX / XLSX 名称缺失或篡改 identity / resource 时继续 fail-closed，绝不把 Code-owned 损坏推断为 custom，也不把 bundled helper 或 dependency 注入 custom。
- 新增 Code 自有 bundle 审计只检查当前 data/skills 的 runnable code-resources.json，忽略 Markdown / 示例文字；当前 31 个 bundled Skill、pptx / xlsx 合同均无 finding。PPTX validator 对受控普通 .pptx 做 bounded OPC / PresentationML 结构校验并拒绝宏，renderer 以已校验输入、独立 LibreOffice profile、有限超时和进程树清理生成 PDF；依赖检查确认真实 C:\Program Files\LibreOffice\program\soffice.exe，不接受 PATH wrapper 伪装。
- 直接 clean-room PPTX 证据覆盖临时一页 deck 的校验、真实 LibreOffice PDF 渲染与 PDF / JPG 可读性；R003 同根 custom 通过真实 execute_use_skill_tool 入口、带空格路径、资源缺失 / 篡改、移动重定位、local dependency 及 PPTX / XLSX bundled identity / resource gate 回归。资源合同 / PPTX / 静态组复跑 35 tests，server / security / Skill 兼容组复跑 410 passed；此前 AgentRuntime 137 passed + 81 subtests、frontend 417 passed、doctor 10/10、frontend build / freshness、AST / JavaScript 语法和 diff 检查仍保持适用。非发布 PyInstaller 构建的归档精确含 5 个 PPTX 资源且零 __pycache__，仅重建仓库 dist\Code-v0.6.7.exe（SHA-256 7E84A778A6549AB523C34EA142BB50345F9A8001713FBF5DDC97DFA373AC415C），未替换用户安装或进入发布。
- CODE-065 已从私有 TODO 删除且 Code 游标保持 CODE-065；CODE-055 的原 PPTX AgentRun 继续暂停。用户已确认重启后此前卡死的运行状态恢复，但该确认不构成恢复真实模型评测的授权，也不能倒推此前受保护来源是否被读取；剩余评测须经用户新的 STRICT 恢复授权。未运行模型、Browser、外网、安装、push、tag 或 release，未触碰受保护用户 data 或无关 handoff。

## 2026-09-02 01:50 · Codex

### CODE-062 · XLSX Skill 自有重算资源与路径闭环

- 以 Code 产品合同、公开 XLSX / LibreOffice 接口和 clean-room 实现补齐自有 `recalc.py` 与受控 `soffice.py`，整个阶段未读取、哈希、搜索或复制受保护的专有 Skill / Office 脚本来源。`use_skill` 现在通过服务端白名单 `code-resources.json` 返回经身份、tombstone、普通文件 / reparse、大小与 SHA-256 校验的绝对 `runtimeResources` 路径，并明确要求模型直接使用而不得自行搜索或复制脚本。
- 新安装使用安装目录中的精确资源；旧安装若仍是已知旧版 Skill / 依赖身份且自有资源缺失，则只读回退到当前 bundled 资源，不覆写旧目录、不批量迁移。未知同名 Skill、部分冲突资源、路径穿越、符号链接 / reparse、哈希不符或 tombstone 均稳定 fail-closed；旧安装的依赖声明同样只读映射到当前兼容 manifest。
- XLSX 能力拆分为普通 `create-edit` / `inspect` 与需要公式重算的 `create-edit-formulas` / `render`。只有后两者要求严格验证真实 LibreOffice：解析到的 `soffice.exe` 必须具备同目录 `soffice.bin` 与 `fundamental.ini`，PATH 包装脚本不能冒充安装；其他 Skill 的既有 command 依赖解析保持不变。
- 重算脚本只接受受控 `.xlsx`，以参数数组、独立 LibreOffice profile、有限超时和进程树清理运行；默认阻断 externalLinks、connections、queryTables、外部关系、外部工作簿公式以及 `WEBSERVICE` / DDE / RTD / URL / UNC 等外部引用，只有显式 `--force` 才放行。结果在隔离副本中验证后以同目录临时文件、`fsync` 与 `os.replace` 原子发布；失败保留原文件并清理临时资源，JSON 区分 `success`、`errors_found` 与稳定错误码。
- fail-first 先稳定得到缺失资源模块的 `1 error`；完成后资源 / 布局直接组 `19 tests`、Skill 依赖 / builtin / 同步 / runtime binding `86 tests`、AgentRuntime 与 P2 Skill CRUD `151 tests`、完整 frontend `417 tests`、server / security / P2 `406 tests` 通过，doctor 在任务开始与重要运行时冻结后均为 `10/10`，frontend build / freshness、Python / JavaScript / JSON / 资源哈希与 diff 检查通过。
- 受控临时目录中的真实 LibreOffice 验证覆盖正常公式重算、错误公式汇总、外部引用阻断、超时与发布失败恢复；最终严格依赖合同定位 `C:\Program Files\LibreOffice\program\soffice.exe` 并成功重算。非发布 PyInstaller 首次仅因测试侧归档路径分隔符比较错误未通过断言，修正断言后的独立最终构建确认 5 个 XLSX 资源进入 EXE（32,761,369 bytes，SHA-256 `e683402158291097a5f7374dbc83e4b78b3a058e9f13c909e5420462de075f05`），临时根完整清理；该构建未替换用户安装或进入发布。
- `CODE-062` 已从私有 TODO 删除且 Code 游标保持 `CODE-062`；`CODE-055` 当前主机后续 `bypass` 评测仍暂停，资源闭环和既有自动模式范围引导都不自动解除门禁，须先完成审批侧独立复核并由用户再次明确授权。未运行模型、H4、Browser、外网、安装、push、tag 或 release；受保护用户 data、3011 页面和无关活动 handoff 均未触碰。

## 2026-09-02 00:53 · Codex

### CODE-061 · 自动模式范围引导与风险校准

- 按用户确认的产品边界，不为自动模式增加项目外路径硬沙箱，也不把工作区误述为不可越过的安全边界。`bypass` 模型提示现在明确：工作区是默认操作范围；用户明确提供的外部路径可在任务需要时访问；未经明确要求不得自行扩张到父目录、相邻目录、用户主目录或磁盘根，也不得为寻找工具、依赖或 Skill 脚本递归扫描用户目录或磁盘；工作区外写入、覆盖或删除需要明确用户意图。发现范围风险时应先缩小范围或改用更安全策略，仍无法完成则在最终回答说明，不以中途提问打断自动任务。
- Auto 选项提示和一次性确认弹窗的中英文文案同步说明项目外路径可能被访问、自动模式并非完全安全；保留危险 / 系统级命令、系统安装、破坏性 Git、工具预算及参数校验等现有基础限制。文案不再声称“越界路径统一被服务端拦截”。
- 工具白名单、`request_user_input` 工具可用性、`resolve_project_path`、文件 / 命令工具、授权状态、`pendingAuthorization`、一次性 Auto 确认、协议与持久化均未改变；本阶段只是模型范围引导与风险披露，不是安全沙箱，也未证明任意 shell 子进程受到项目路径隔离。
- 直接合同先稳定得到 `2 failures`，实施后权限提示 / 风险弹窗 / 工具集 / 状态机核心组 `4 tests` 通过，完整 `tests.test_frontend_modules` `417 tests` 通过；doctor `10/10`，frontend build / freshness、bundle 语法与后续静态 diff 检查通过。未运行 H4、Browser、真实项目外路径访问或评测恢复。
- 同步核对到独立 XLSX 缺口：`data/skills/xlsx/SKILL.md` 引用自身 `scripts/recalc.py`，但现行可分发目录只有 `SKILL.md` 与 `dependencies.json`，构建 / 安装又只同步 `data/skills`。私有 TODO 游标推进到 `CODE-062`，单独登记 Code 自有重算资源、路径、依赖与打包闭环；未读取或复制受保护的 `claude-skills` 专有来源。`CODE-055` 的当前主机 `bypass` 评测继续暂停，待 `CODE-062` 完成、独立复核并由用户再次明确授权后才可恢复。
- 受保护 `data/generated-assets/`、`data/image-route-registry.json` 未读取、哈希、修改或暂存，私有活动 handoff 未覆盖；未执行 push、tag、release、外网或其他外部操作。
