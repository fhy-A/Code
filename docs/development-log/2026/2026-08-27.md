# 2026-08-27 开发日志

## 2026-08-27 20:19 · Codex 开发会话

### CODE-050 · 最终回答本地链接绝对路径完成

#### 完成内容

- 新生成的最终回答只把完整规范化绝对路径投影为本地可点击目标：Windows 盘符、`/C:/`、UNC、POSIX、正反斜杠、空格、中文、目录及可选行号统一保留完整 `data-path` identity；项目内显示标签仍可缩短。描述项目结构或实现时仍可使用相对路径、裸文件名、`output/tmp/` 等普通文本或行内代码，但它们不可点击，也不按当前项目根猜测。
- Markdown 本地链接、行内代码、代码块行号引用和本地图片共用绝对路径判定；外部 `https` 链接及 favicon 行为保持不变。项目安全范围外的绝对 code/link/image 在回答 HTML 插入前即降级为普通 code/span，移除 `data-path`、链接、焦点、菜单和打开事件；工具卡继续由独立适配器保留项目相对路径协议，`read_file`、`write_file`、`propose_edit` 等工具 schema 与执行语义没有变化。
- 默认系统提示和共享子 Agent 提示明确要求最终回答使用完整绝对本地路径，同时声明工具参数仍使用项目相对路径。越界降级只注入助手的流式、过程与最终回答；用户消息继续使用原有通用 `renderMarkdownLite` 投影，不改变其链接、图片、折叠或复制语义。没有迁移或重写旧 Session/AgentRun；旧回答中的相对路径按新渲染规则自然降级为不可点击文本。
- 文件树与最终回答入口保持隔离：文件树协议和 item identity 未改；文本与大文本继续进入右侧内部预览，项目内绝对目录先经既有只读 `/api/files` 确认后才沿 `/api/open-file` 的 Explorer 目录合同打开，项目内无法确认或失效目标 fail-closed，项目外目标直接降级为非交互内容；二进制维持既有不可预览路由，绝对路径本身不增加访问授权。

#### 验证与边界

- 前端确定性测试覆盖绝对路径规范化、CJK/空格/行号、相对与裸路径负例、Markdown 本地链接与图片、项目内外路由与预插入降级、用户/助手 renderer 职责隔离、工具相对路径适配、外部 URL 及提示合同；相关回归为 `387 passed, 28 subtests passed`。
- bundle/direct classic H4 共 `6 passed (21.6s)`：两入口均验证完整项目内绝对文件/目录 identity 与行号在刷新后稳定、三个相对引用可见但不可点击；项目外 absolute code/link/image 的交互 identity、href、焦点和菜单均为 0，文件/目录/图片请求也为 0，外部 HTTPS 不回归。1.2 MB 中文大文本从文件树和回答链接均内部预览，刷新后行数保持 `8202`；每个入口的回答目录只产生 1 次 `/api/open-file`，`explorerRequests` 精确为 `selectFile=false / targetKind=directory`，默认程序请求为 0；文件树 Explorer 合同不变，模型请求与工具执行均为 0。
- 前端 build/freshness、bundle/源码 JS 语法、Python 语法与 `git diff --check` 通过；H4 两入口端口、子进程与受管临时根全部清理归零。没有修改 `server.py`、持久化格式、允许根、连接/Key、发布定义或活动 handoff，也没有启动 `CODE-049` 或清理 `output/tmp`。

## 2026-08-27 19:06 · Codex 开发会话

### CODE-048 · AgentRun 中途故障恢复与无固定轮次上限完成

#### 完成内容

- 自动上下文改以服务端 `connection + model` 能力为权威：Auto 不再被前端目录尚未就绪时的 legacy `contextLimit=128K` 收紧，`deepseek-v4-flash-vision-exp` 可按既有 family 能力解析为 1M；只有用户显式预算、可信硬元数据和同 key+model 校准可向下约束，首条消息不等待模型目录刷新。
- 前端 200、服务端 50 与 Goal 40 的固定轮次终止/接续语义已经移除；旧 `maxRounds` 继续兼容读取但不参与强制结束。用户取消、单工具超时、Skill/权限/工具预算、重复失败、重复副作用与无新进展暂停保护保持不变。
- 新 AgentRun 首次模型调用仅在连续 120 秒没有任何有效正文、推理或工具证据时硬中断；已有进展后的流中断、断网、后续超时或服务重启会持久化同一 AgentRun 的消息、`routeRef`、模型 checkpoint、恢复位置及权威 execution/result，并进入 `waiting_recovery`。恢复只允许原 routeRef，已完成工具不重放，未完成工具不误执行；前台、后台、队列、Child 与 Goal 均沿同一持久恢复合同继续。
- AgentRun 仍使用既有 v5 记录，只增加可选 `modelCheckpoint`、`recoveryState` 与 `compactionRecovery` 字段，不提升 schema、不批量迁移或原地改写旧 Session/AgentRun。公开快照只暴露脱敏恢复摘要，不保存私有推理正文；旧记录缺少新字段时继续按兼容默认值读取。
- 自动压缩改用禁止工具调用的专用最终摘要合同；空摘要只立即重试一次，仍失败则持久退避，避免逐轮重复压缩。只有非空、结构有效且确实缩小上下文的候选才原子替换历史；失败保留原消息，真实窗口耗尽时进入可恢复状态并给出准确错误。

#### 验证、熔断与边界

- 确定性回归覆盖 1M 能力不受 legacy 128K 收紧、显式预算/硬上限/校准、越过旧 40/50/200 边界、首次 120 秒与中途故障区分、部分流/私有推理脱敏、服务重启同 AgentRun 恢复、completed tool exactly-once、旧 `maxRounds`、普通/后台/队列/Child/Goal，以及空摘要一次重试、退避持久化和原历史原子保留。
- 最初的浏览器“部分流后服务进程重启”组合场景连续两轮未在重启后发起 resume；按 H4 两轮熔断停止第三次同场景复跑。完整审计确认启动恢复扫描早于连接目录就绪且目录成功后未重扫，修复为成功刷新后的幂等恢复扫描；服务进程重启由确定性 Python 测试闭合，浏览器 H4 重设计为同页面 reload 边界。
- 重设计后的 bundle/direct classic H4 共 `2 passed (20.9s)`：两入口各记录 2 个 `waiting_recovery` 事件、1 次 completed tool execution、reload 后工具执行增量 0、同一 AgentRun、唯一用户消息；端口、子进程和受管临时根均清理归零。没有调用真实外部模型或读取真实 Key。
- 扩大回归在排除 3 个已由基线 `e020812` 证实陈旧的冻结 harness 文件后为 `1487 passed + 1009 subtests`；排除项分别仍冻结 AgentRun v4/baseUrl，而基线已写 v5/不落盘 baseUrl，以及手工压缩 VM 仍只注入旧 `getFallbackKeys`，而基线已依赖 `getModelDispatchCredentials`。本阶段没有改写这些与 CODE-048 无关的历史证据。
- 前端 build/freshness、bundle 语法和 `git diff --check` 通过。回退可撤销本提交；因为没有磁盘迁移，旧记录和既有工具执行证据保持可读。本阶段未实现本地链接绝对路径、未清理 `output/tmp`、未修改连接配置、真实凭据、release 定义或无关 handoff，也未开始 `CODE-049` 的 PPT Master 集成。

## 2026-08-27 14:15 · Codex 开发会话

### CODE-047 · 两类真实渠道验收完成并正式收口

#### 真实渠道证据

- 在修复提交 `cd3427dd31b5c7c21272efab47ea48c29d062725` 已加载的新鲜 3011 服务上，通过 in-app Browser 分别新建并完成两条真实会话：`Code-QA-国产模型 / deepseek-v4-pro` 会话 `523c1c6cd75243d8`、AgentRun `b05ce9e9193bdc313fe5a53db08d26f2`；独立严格 OpenAI-compatible `Code-QA-gpt codex / gpt-5.6-sol` 会话 `e4b4eba0c0924d9b`、AgentRun `3985fc60da526152387a72d82350d7c8`。两条渠道均首次形成同一 assistant 的两个并行 `read_file`，没有使用重试，只读取 `assets/code-logo-white.png` 与 `assets/code-logo-black.png`。
- 两个 AgentRun 的脱敏持久顺序均为 `assistant(A,B) → tool(A) → tool(B) → visual(A) → visual(B)`；每条会话都只有 2 个 execution，两个 call ID 的 `tool_started`、`tool_completed` 和 completed execution 均各 1 次。Session JSONL 均为 7 条，按 `user → assistant → tool-call → tool-result → tool-call → tool-result → assistant` 投影，同一 call 的 call/result 一一对应。
- 两条 UI 均展示两张 `image/png` 及最终成功答复，没有错误卡、`config_error` 或 API Key/模型授权误导。分别刷新会话后，UI 仍保留 2 张视觉结果且无活动运行；对应会话仍各只有 1 个 AgentRun，`model_started=2`、`rounds=2`、execution 与 tool 事件计数均未增加，确认刷新恢复零重执行。

#### 收口结论

- 真实 DeepSeek 与独立严格 OpenAI-compatible 两类外部渠道已补齐 `f0ff7781a6dfa02ce5b105dba6624c726c41dd10` 所记录的剩余完成定义；结合此前 bundle/direct classic 隔离 H4、旧 v4/v5 非破坏性请求投影、fail-closed 预检和 `tool_protocol_error` 分类回归，CODE-047 正式完成并从 `TODO.md` 删除。
- 验收未读取或输出真实 Key、Authorization、routeRef、base64、连接内部地址或完整敏感正文；未修改连接设置、产品代码、测试、构建产物或测试会话数据，也未 push、tag 或 release。

## 2026-08-27 12:41 · Codex 开发会话

### CODE-047 · 实现与隔离验收完成，正式收口待真实渠道授权

#### 状态说明

- 本条只确认实现阶段与本地隔离验收已由提交 `cd3427dd31b5c7c21272efab47ea48c29d062725` 完成，不代表 CODE-047 的整体完成定义已经满足。真实严格 OpenAI-compatible 与代表性 DeepSeek 渠道尚未获授权、尚未执行，事项已恢复到 `TODO.md` 的 Code 主域“外部等待”，待用户另行授权后完成真实闭环再正式收口。

#### 根因与修复

- 已确认的旧持久序列为同一 assistant 声明两个 `read_file` 后写成 `tool A → visual A → tool B → visual B`；严格上游因此以工具协议 400 拒绝请求，既有宽泛 400 分类又把它落为 `config_error`，前端最终误提示检查 API Key 或模型授权。
- AgentRun 新写入现在按 assistant 声明顺序插入每个唯一 `tool_call_id` 回执，只有同批全部工具进入终态后才按原 call 顺序统一写入成功视觉标记；失败工具只写唯一失败回执。授权等待/拒绝、问卷、部分失败、取消与恢复继续沿用既有终态和 exactly-once 执行证据，不因整理消息顺序重放工具。
- 请求投影新增非破坏性批次规范化与发送前预检：旧 v4/v5 Session/AgentRun 不迁移、不原地改写；缺失回执仅从权威 completed execution 恢复，否则生成同 call ID 的 `unknownState + notReplayed` 失败回执；相同重复回执去重，冲突重复、重复声明或孤立工具/视觉证据在本地 fail-closed，且不创建上游请求。classic/direct 的 `tool-result` 投影遵守相同连续块合同。
- 新增非瞬态 `tool_protocol_error`。本地预检失败或明确的工具协议 400 不轮换 Key，并显示“工具调用历史异常 / Tool-call history error”及“历史已保留、工具未重放”的处置提示；真实模型权限 403、通用 400/401、上下文、429 与 502 等既有分类保持原行为。

#### 兼容、回退与验证

- 未提升 AgentRun schema、未批量迁移数据、未写回旧历史；回退已完成实现只需撤销 `cd3427dd31b5c7c21272efab47ea48c29d062725`，持久化的原始 messages 与 toolExecutions 仍可读取。旧历史的恢复只发生在当次内存请求投影，execution/fingerprint/operationId 仍是副作用复用事实源。
- 最终相关回归：`tests/test_agent_runtime.py` 在排除 5 条与本补丁无关、因未传 `routeRef` 而基线即返回 409 `route_not_found` 的 HTTP 创建夹具后为 `113 passed + 71 subtests`；前端模块 `263 passed`；图像桥接与模型运行时 `32 passed + 16 subtests`。上述 5 条失败均在 AgentRun 创建前退出，没有模型请求或工具执行，本阶段未扩大为 Route Registry 测试修复。
- 前端 build/freshness、JS/Python 语法、`git diff --check` 与 H4 基础设施 self-check 通过。复合 H4 在第二轮仍受 bundle reload 后测试连接目录不可用影响，按两轮熔断规则撤回并拆成独立合同；重设计后 bundle/direct classic 共 `4 passed`：两入口均验证严格 `assistant → tool A → tool B → visual A → visual B`、两次 read_file exactly-once、页面刷新与服务进程重启零重执行，以及明确协议 400 显示 `tool_protocol_error`、零工具执行且不出现 Key/模型授权提示。每例端口关闭、子进程退出、临时根删除。
- 本阶段只使用本地严格假上游，没有读取真实 Key、调用真实 OpenAI/DeepSeek 渠道或改变连接配置。真实严格 OpenAI 与代表性 DeepSeek 闭环仍需另行外部渠道授权，不属于本地提交通过声明。

## 2026-08-27 11:01 · Codex 开发会话

### Code 开发工作流优化方案 B · 第二阶段协作契约完成

#### 完成内容

- 根级与 `code` 级两对 `AGENTS.md` / `CLAUDE.md` 同步固化五项效率规则：doctor 只在新任务接手、环境变化或重要运行时改动边界运行一次；验证按 doctor → 定向确定性测试 → 相关回归/构建 → 阶段冻结/重要运行时/发布才扩大或全量的阶梯推进；H4 优先由开发任务内部闭环，用户同一场景最多两轮且第二轮失败后禁止第三轮；每阶段默认只保留开始控制合同、真实范围/风险变化和完成复核三个协调点；发布完成、上下文压缩、混入多个无关阶段或事实源无法快速恢复时在阶段边界轮换开发任务。
- `docs/approval-relay-protocol.md` 新增 Code 项目覆盖层，明确效率规则只消除重复步骤，不改变 approval-relay/v2 的风险、授权、阻塞、唯一自动发送 owner、release freeze 或 owner lease。批准边界内的实现与夹具修正由开发任务在同一 turn 自行闭环，产品行为、协议、持久化、数据、安全、权限、迁移、发布或外部操作变化仍立即重新审批。
- 任务轮换只发生在阶段边界：旧任务停止接收新 relay 和作为写者，释放 lease 后才可绑定新任务；已有用户创建授权时由唯一审批 owner 创建/绑定，缺少授权时只建议并等待确认。新任务重新从 Git、开发日志、TODO 和相关 handoff 自助接手，不复制过期任务历史或受保护差量。

#### 验证与边界

- 写入前真实 `python verify.py doctor` exit 0：Python/H4 Python、四项模块、Node/npm、esbuild、Playwright 与 Chromium 均通过；Chromium context/临时根已清理，运行前后临时目录和最终相关进程均为 0。
- 根级 `AGENTS.md` / `CLAUDE.md` 修改前 SHA-256 同为 `5FE0CAE2786E164CCC5880B74BD6824D0FB4EAE3BC571794B48F83E987979064`，修改后同为 `816D5C71884BEDC181FE277D92623B84119B19DDC9FD8112BE63510776A1D515`，两文件逐字一致；code 两文件的新增核心区块逐字一致，并与根级区块一致。协议关键标题、风险保留声明、文件白名单和 `git diff --check` 通过。
- 受保护 `TODO.md` 的 `CODE-048 + CODE-049` 差量未修改，写入前后 diff hash 均为 `b955d08dce16ede85915b86563d70c089dfa7c09`，暂存区保持隔离。本阶段只修改规则和事实文档，没有修改个人 approval-relay Skill、产品代码、测试配置、release 定义或无关 handoff；按纯规则阶段边界未运行产品 pytest、H4、前端 build、push、tag 或 release。

## 2026-08-27 10:39 · Codex 开发会话

### Code 开发工作流优化方案 B · 第一阶段 doctor/preflight 完成

#### 完成内容

- 新增独立入口 `python verify.py doctor`，不加入既有 `quick/ui/runtime/release` profile 或正式 release check IDs。预检会汇总而非首错即停地报告当前 `sys.executable`、H4 裸 `python` 的真实解释器与 `pytest/requests/yaml/jsonschema`，以及 Node/npm、真实但不落盘的 esbuild transform、Playwright 包和 Chromium 启动能力；解释器不一致以明确 warning 呈现，缺失、启动失败、超时和清理失败均稳定返回整体 exit 1。
- Node 探针不访问网络或写入 `dist/`：esbuild 仅作内存转换；Chromium 使用唯一受控的系统临时目录，禁用后台网络行为，只加载内存 HTML，并在所有成功/失败路径关闭 context、删除临时根。README 同步源码依赖和新开发任务/环境变化后的 doctor 用法。
- 现有四档验证映射、H4 仅属于 runtime、release 正式顺序与 `release.py` 接线均未改变；`package.json`、`release.py`、协作协议、无关 handoff 和受保护 `TODO.md/CODE-048` 均未修改。

#### 验证与边界

- doctor 确定性测试为 `20 passed`，覆盖成功、缺失 Python 模块/可执行文件、H4 解释器不一致、Chromium `EPERM` 类启动失败、Node 探针超时和清理失败；release 相关扩大定向为 `59 passed + 229 subtests`。
- 真实 `python verify.py doctor` exit 0：当前 Python 与 H4 裸 `python` 均解析为 Python `3.12.10` 的同一解释器，四项模块齐全；Node `v24.14.0`、npm `11.9.0`、esbuild `0.28.1`、Playwright `1.62.1` 与 Chromium `151.0.7922.34` 均通过。Chromium context 已关闭，`code-doctor-chromium-*` 临时根运行前后均为 0。
- 当前解释器下正式 release definition fingerprint 复核仍为 `7be91010c77bf59f09e98fcd4c9490b81134da6f2f46f47a6d97c83cede3485b`，check IDs、顺序和超时保持原定义。本阶段没有运行完整 pytest、完整 H4、前端 build、push、tag 或 release，也未开始第二阶段协作协议优化。

## 2026-08-27 09:55 · Codex 开发会话

### CODE-045 · favicon/tooltip 部分收口并转回待决策

#### 本轮保留的改进

- favicon 继续只通过同源 `/api/favicon` 进入服务端安全代理；保留 HTTP/HTTPS、DNS/IP、逐跳重定向、TLS、MIME/魔数、大小和总时限边界。候选顺序先覆盖精确域名与父域名直连，再进入既有固定 provider；1×1 退化占位图会被拒绝并继续后续候选，不进入正缓存。
- 服务端将容量、DNS、传输、上游临时状态和总时限失败与稳定无图标分开：临时失败不写稳定负缓存，返回 `503`、`Cache-Control: no-store` 与有界重试提示；所有失败响应不再由浏览器缓存五分钟。客户端使用每 host 共享任务、4 槽 FIFO、一次延迟重试和 30 秒冷却，避免大量链接同时加载时挤满服务端槽位或形成无界轮询。
- 外链、普通本地文件和图片文件卡统一使用单例自定义 tooltip；document/window 委托只初始化一次，本地卡不再保留与自定义提示重叠的原生 `title`，样式明确无边框和阴影，原有 href、复制、右键和预览语义保持。

#### 验证与未完成边界

- 直接相关服务端安全/缓存与前端 favicon/tooltip/Markdown 确定性单元共 `22 passed`；用户实跑既有 CJK 裸 URL与 structured rich text 的 bundle/direct classic H4 共 `4 passed`，覆盖同源代理、临时失败恢复、退化图后续候选、tooltip singleton、外链 href/右键/刷新和本地文件提示。
- 为多 host 公平队列新增的两条 H4 先后受离线 Markdown 夹具与 Session 切换编排影响，未稳定进入 FIFO/全部图片断言，已撤回；其失败不记为产品失败，也不记为通过。9-host、4 槽、FIFO、重试尾排和 stale consumer 的确定性单元仍保留。
- 用户对真实 10+ 站点的人工截图显示部分站点已恢复真实 favicon，但仍有多个站点稳定回落地球图标，人工矩阵仅为“部分改善、未完整”，本轮不宣称 CODE-045 完成或 PASS。剩余问题转回 `待决策/调研`，后续需区分站点无有效图标、既有 provider 覆盖、上游超时与安全策略，再决定是否增加受控 provider 或脱敏诊断；不预设放宽 SSRF、浏览器直连、HTML discovery、图片校验或无限重试。
- 未执行 push、tag、release，也未自动启动后续 `CODE-006`。

## 2026-08-27 02:26 · Codex 开发会话

### CODE-046 · 首条消息即时反馈与连接中心 Route Registry（用户 H4 组合 PASS）

#### 完成内容

- 将新任务发送拆为可恢复的唯一意图：用户提交后先在当前 Session 持久化并展示唯一用户消息和待发送状态，再异步完成可信路由。路由成功只创建一个 AgentRun；失败保留原消息与可操作错误，重试复用原消息而不重复 AgentRun。Session revision/CAS、权威恢复顺序和共享终态 ownership 发布共同阻止跨页面迟到写、恢复误报与完成后下一条消息误分流。
- 路由事实收敛到服务端连接中心 Route Registry：稳定非秘密 `connectionId + modelId` 由服务端生成不透明 `routeRef`，目录按现有连接名称展示该连接实际支持的模型；同名模型可属于不同连接，但不得跨连接回退。公开目录、Session 和新 AgentRun 只保存 `routeRef`、`catalogRevision`、`modelId` 等非秘密身份，Key、Authorization 与内部 baseUrl 仅存在于服务端运行期解析层。
- manual Key 与 workbar Token 均通过现有连接配置自动探测模型；服务端逐后端隔离收集，部分后端失败时仍保留其他健康连接，全部失败才 fail-closed。前端目录刷新按连接快照与意图 singleflight，同一在途操作去重，配置变化或显式刷新最多排一个最新快照尾随刷新；已有有效 `routeRef` 和运行时凭据时直接派发，不等待后台目录刷新。
- `routing_v2` 新请求使用 `routeRef/catalogRevision` 且与 legacy `keys[]` 互斥；旧 v3 目录缓存仅作非权威展示，旧 modelId 仅在唯一匹配时无感升级，歧义进入显式路由选择。旧 AgentRun 与 legacy Key 字段继续只读兼容，不重写历史 JSONL；队列、后台、Child、手动压缩、标题和恢复路径统一继承或提交精确连接身份。

#### 验证与环境边界

- 用户先运行完整同组 bundle/direct classic H4，结果为 `10 passed / 4 failed`；四个缺口经独立模型响应 gate 与禁用连接语义断言修正后，用户定向复跑为 `4 passed (14.5s)`。两轮组合覆盖完整 14 场景，包括重复模型精确绑定、刷新/重载、有效路由不等待后台刷新、终态恢复、fail-closed/重试、禁用连接零跨组回退和旧工具消息配对；没有把两轮结果表述为单次 14 passed。
- R020 兼容收口中，真实 routing_v2/终态漂移簇 `151/151`、Route Registry/server `250/250`、前端非构建 `261/261` 通过；H4 discovery 为 `14/14`，Node/Python 语法与 `git diff --check` 通过。
- 当前开发环境的完整 `unittest discover` 实际运行 `1175 tests`，仍有 15 个由缺少 `requests`、`yaml`、`pytest`、`jsonschema` 导致的导入错误，以及 1 个 esbuild 目录访问沙箱失败。它们单独记录为环境边界，本阶段未宣称全量 pytest 或 frontend build 通过。
- 未执行 push、tag、release，也未启动下一事项。
