# 2026-08-29 开发日志

## 2026-08-29 19:02 · Codex

### CODE-049 · 用户验收通过

- 用户实际打开 PPT Master 离线运行时验收样例后明确回复 `PASS`。最终实现提交为 `5f25cb15c13174e0c022a9a397175942ab400f4c`，最终加固提交为 `92b3a3d7973ed6ea53c1dd55cc2893488e126032`；验收样例 SHA-256 为 `d67134c3092114c46e910e2c2600bb0925d2b4d8cb8d48639384c7a6b1916997`。
- CODE-049 的供应链准入、静态共存、受管依赖、离线运行时、安全加固与人工验收全部完成，canonical TODO 中的对应未完成事项已按维护契约整项移除。本次只做文档收口，未重跑测试/doctor/Browser/LibreOffice/H4，未修改运行时、依赖、Skill、vendor、样例或默认 `/pptx`。

## 2026-08-29 18:41 · Codex

### CODE-049 · 离线运行时最终复核加固

- 提交 `5f25cb1` 的 STRICT completion review 已确认 Git 范围、官方 wheel 来源、受管环境与五页样例视觉均通过，但指出五个必须闭合的安全/可编辑输出缺口。R003 在同阶段内加固，不重新下载或安装依赖，不扩大首阶段能力。
- 工具投影、capability 状态与每次 execute 现在都在 Code 父进程重验 canonical receipt digest、lock 绑定、两个 exact dist-info/version/RECORD 及 RECORD 声明的 `23` 个安装文件字节；路径必须留在受管 venv 且每层均为常规非 reparse 文件/目录。RECORD、其声明文件、receipt、缺失/额外目标任一不一致时，capability 与 Skill 工具 fail-closed，执行返回稳定无秘密错误且 worker 零启动。
- final 的 `receipt.json`/deck 与 existing staging 的 prepared/request/result/deck 在任何 JSON 读取、哈希或 ZIP 解析前，先完成 run-owned containment 及全路径 symlink/reparse 核验；任一冲突都不读外部目标、不重放 worker、不发布。ZIP 还拒绝重复、加密与 symlink member。
- Markdown 连续普通行合并为一个段落；段落+列表使用上下分区并对密度 fail-closed，表格/图表与任何其他 block 混排明确拒绝，不再静默丢内容。运行时生成的段落+列表专测直接核验两个文本框几何不相交。
- PPTX 发布改为首阶段内部 part/Content-Type allowlist：拒绝 VBA/宏、OLE、ActiveX、音视频、custom XML 及其他未批准部件。原生图表唯一必需的 `Microsoft_Excel_SheetN.xlsx` 只在由 chart package relationship 独占引用时允许，其嵌套 workbook 再经独立 part/Content-Type/关系 allowlist；现有文本、形状、表格和原生图表仍通过。
- locked install 的部分失败分支在 cleanup 返回后必须重新快照并与完整 before distribution/version/RECORD 基线精确相等；cleanup 失败或任一残留/无关变化都 fail-closed，只有精确恢复才报告“已恢复但安装失败”，不伪称安装成功。

### 修正验证与边界

- 五组确定性 fail-first 在修正前为 `17 failed + 3 passed`；修正后 PPT runtime/dependency/Skill/builtin 为 `38 passed + 212 subtests`。模块隔离的 routes、server、Skill dependency、Session 与 AgentRuntime 相关扩大回归合计为 `603 passed + 399 subtests`；AgentRuntime 仍只排除 HEAD 已有的单个 gpt-5.6 context-limit 矛盾断言。
- 样例 `output/ppt-master/code049-offline-runtime-pilot/presentation.pptx` 字节未变，SHA-256 仍为 `d67134c3092114c46e910e2c2600bb0925d2b4d8cb8d48639384c7a6b1916997`；它已在 R003 复核中通过双路逐页视觉，现又通过更严格的外层 OOXML 和嵌套 chart workbook 校验，因样例本身未重生成，未机械重跑 LibreOffice/slides_test。
- 本修正未改 wheel lock 版本、受管 `79` distributions、vendor 66 blobs、默认 `/pptx`、`app.js`、`build_exe.py`、持久化 schema 或活动 handoff；不读取/哈希/修改/暂存用户 data，不修改或暂存受保护 `TODO.md`，不运行 Browser/H4，不 push/tag/release。

## 2026-08-29 18:03 · Codex

### CODE-049 · PPT Master 离线运行时试点完成

- 依赖准入只从 PyPI 官方主源下载 CPython 3.12 / Windows x64 匹配 wheel，且固定为 `skia-pathops==0.9.2` 的 `skia_pathops-0.9.2-cp310-abi3-win_amd64.whl` / SHA-256 `9ae72d801b5f4c5dbb937fb25476a12fa2da70860f28dd637da2b529dbd8271c` 与 `uharfbuzz==0.50.0` 的 `uharfbuzz-0.50.0-cp312-cp312-win_amd64.whl` / SHA-256 `f1746fcc800c627b7dc2b89841cbf032738c506b3db4a374b86a33ba2333b59b`。Code 自有审计器核验 PyPI JSON、tag、METADATA/WHEEL/RECORD、路径与压缩属性、BSD-3-Clause/Apache-2.0 许可、Requires-Dist 与 PE 导入；未解释原生依赖为 0，未下载 sdist，未让包管理器解析或更改其他包。
- 两个 wheel 只通过既有 Skill dependency 受管机制进入 `data/runtime/python`；distribution 快照为 `77 → 79`，完整回滚演练为 `79 → 77`，再安装为 `77 → 79`，三次只改变目标两个 distribution。无秘密 lock/receipt 锁定来源、wheel 哈希、审计结果、安装 RECORD 与回滚命令；环境变化后 doctor `10/10`，`ppt-master/offline-core` 仅在精确版本命中时为 ready。
- 新增 Code-owned `create_ppt_master_deck` 结构化工具与隔离 worker：只接受 inline UTF-8 Markdown 或当前项目内不超过 1 MiB 的相对 `.md`/`.txt`，输出固定为 `output/ppt-master/<AgentRunId>/presentation.pptx`。目录、扩展名、URL/远程 HTML、symlink/reparse、旧文件覆盖、任意脚本/命令/输出路径均被拒绝；执行前重验阶段 2 的 66 blobs / `1,840,608` bytes / manifest `b624171959890a6c2d663332bf709c8b05c266272cd6e632e7dc4dbb970e13cf` 及依赖 receipt。
- 运行时纳入既有 `file_mutation` 授权、AgentRun execution/result 幂等与 30 秒进程树取消；完成 execution 重放只读 receipt，已 prepare 但结果不可证的恢复 fail-closed 且不再执行。worker 使用 `-I -B`、最小环境、socket/subprocess audit hook、run-owned staging 与原子目录发布；发布前拒绝非预期残留和重解析点。
- `/ppt-master` 仍只能显式匹配，只有 `offline-core` ready 才投影该一个工具；普通“生成 PPT”仍只匹配 `/pptx`，既有 `data/skills/pptx`、Session 和旧 PPT 文件均未改动。首阶段仅开放离线文本、形状、表格、原生图表与 CJK；图片、模板/品牌/Icon/Sound、PDF/DOCX/XLSX、编辑现有 PPTX、Image-to-PPTX、网络、动画/旁白/视频与本地服务仍隔离。

### 验证与开发态产物

- 定向授权/恢复/运行时回归为 `31 passed + 202 subtests`；按模块隔离的 Skill、dependency、routes/tool、server、AgentRuntime、Session、run projection 与 project 扩大回归共 `628 passed + 402 subtests`。AgentRuntime 全文件为 `133 passed + 79 subtests`，另有 1 个 HEAD 已有、与本阶段无关的 gpt-5.6 context-limit 断言自相矛盾（同文件定义 `1,000,000` 却期待 `1,050,000`），本阶段未越界修改。vendor validator、`verify.py quick` 6 项、frontend freshness/bundle syntax、Python compile 与 diff 门禁均通过；不使用 Browser/H4。
- 开发态验收产物为 `output/ppt-master/code049-offline-runtime-pilot/presentation.pptx`：5 页、33 个形状、1 个原生表格、1 个原生图表，含 CJK。OOXML/schema/关系与无 external/media 审计通过，artifact-tool 渲染和 LibreOffice `26.2.4.2` headless 直接打开/导出均成功，5 页逐页图检无裁切、重叠或明显溢出；原生对象保持可编辑。未自动打开 PowerPoint，产物保留等待用户实际打开验收。
- 受保护 `TODO.md` 字节与 SHA-256 `96516f29f1191ab71260e55d66997e1efd12e6faa8d38e37f9b83e2cbb7aa812` 保持不变且不进入 staged；未读取、哈希、导入、修改或暂存用户 data，未 push/tag/release。

## 2026-08-29 16:36 · Codex

### CODE-049 · PPT Master 固定静态共存包完成

- 官方 `hugohe3/ppt-master` `v5.1.0` 再次只读固定为 commit `d6bcaf96b7946667f4a8871b0688b903181db527`、tree `cd5fad962cf856ccaf0a5101695439cbff30635a`；全树 `13,008` 文件、`115,940,805` bytes 与阶段 1 manifest SHA-256 `0ebe6478337139a98f0f616a84f611238a8e50469ea06f95d22e8bf35ed602ea` 精确复核一致，无 symlink/submodule，Git strict connectivity 通过。
- 新增独立 `/ppt-master` 静态 Skill：扁平 frontmatter、无自动关键词、无工具声明，正文固定 `STATIC_ONLY_DO_NOT_EXECUTE`；普通 `创建 ppt` 仍只匹配既有 `/pptx`，原 `data/skills/pptx` 两文件字节 manifest `244147f3830a24502c2b6e9f1efe4006f9974929fb1d3c529e7dc0aca39b6e9c` 不变。现有 `build_exe.py` 已递归打包 `data/skills`，因此未修改打包实现。
- vendor 只纳入 `49` 个离线 DrawingML/native object/OPC 源文件与 `17` 个许可、preset-shape、schema/scaffold/reference 资源，共 `66` 个 exact Git blobs、`1,840,608` bytes；逐文件 SHA-256/Git blob OID manifest digest 为 `b624171959890a6c2d663332bf709c8b05c266272cd6e632e7dc4dbb970e13cf`。上游 MIT、SPONSORS、Apache POI Apache-2.0 NOTICE 与 Open XML SDK MIT 许可闭包均保留。
- 自动更新、`config.py`/`.env`、网络/provider、网页/图片下载、TTS/音频/旁白/视频/动画、Confirm UI、SVG Editor、Image-to-PPTX，以及 brands/icons/sounds/decks/AI comparison 资产均未进入包。Code 自有校验器只做字节、Git identity、AST、路径、许可与 import closure 静态检查，不 import 或执行 vendor。
- `offline-core` 明确声明 PyYAML、python-pptx、XlsxWriter、openpyxl、skia-pathops 与 uharfbuzz；当前仅 `skia-pathops`、`uharfbuzz` 缺失，Skill/capability 均稳定为 `unavailable`。本阶段未安装或升级依赖，也没有运行上游 guard、模块、脚本、Browser/H4、模型、图片或供应商。

### 验证与边界

- fail-first 在静态包尚未存在时为 `5 failed`；完成包与修正实际 import closure 后，定向/相关 Skill 与依赖回归为 `45 passed + 195 subtests`，Skill API 覆盖为 `14 passed + 4 subtests`。Code 自有 vendor validator、Python compile、`verify.py quick` 六项、frontend freshness 与 `git diff --check` 均通过；doctor 入场为 `10/10`。
- 66 个 vendor 文件在 TEMP 清理前再次与固定导出字节及 Git tree OID 全量比对，均一致；自建 TEMP 根通过 containment/reparse/content 预检后删除且无同前缀残留。未修改或暂存受保护 `TODO.md`、默认 `/pptx`、`server.py`、`app.js`、Skill 协议、`build_exe.py` 或无关 WB-003 handoff；未读取、哈希、导入、修改或暂存用户 data。
- 本阶段只建立静态共存基线，不生成 PPTX，也不授权依赖安装或运行时适配；实际 Markdown/TXT → 可编辑 PPTX、输出目录、取消/超时/恢复、产物验收与人工打开仍须新的 STRICT 阶段。

## 2026-08-29 14:01 · Codex

### CODE-044 · 未归档与归档会话双域搜索完成

- Logo/产品名右侧新增全局搜索入口，只投影未归档 `state.sessions`：空查询按 `lastMessageTime → updatedAt → createdAt` 确定性倒序展示跨项目最近 10 条，非空查询对标题及完整/部分 Session ID 做不区分大小写包含匹配并展示全部命中。结果仅显示既有权威 streaming/active 映射的“运行中”或统一“空闲”、标题与项目；点击或 Enter 复用 `loadSession`，关闭、背景与 Escape 收口并恢复触发器焦点，不改会话排序、未读/运行事实、当前选择或持久化数据。
- “设置 → 已归档会话”增加独立本地搜索，只过滤已加载归档记录并保留项目分组、恢复和永久删除动作；活动与归档结果互不混入。中英文、键盘焦点循环/上下键/Enter、空结果、清空及 390px 窄屏均覆盖。搜索输入框最终按用户反馈取消局部 input 内层边框/阴影，只保留外层 `:focus-within` 单层焦点环，其他表单焦点样式不变。
- 归档搜索验收依赖独立提交 `65f6815` 的 HTTP/1.1 请求边界修复：归档/恢复 POST 在生命周期 mutation 前有界精确消费请求体，避免同一 keep-alive 连接的下一请求被残留 body 污染；该修复未混入本阶段产品提交，也未改变归档 API、payload 解析、事务或幂等语义。

### 验证、熔断与边界

- 通用搜索 H4 两轮都在搜索断言前被模型目录前置阻断：第一轮模型刷新返回 503 导致 `modelPill` 为空，第二轮 `refreshModelsBtn` 在当前产品页不可见。按熔断规则移除本阶段新增 smoke 注册和死 helper，未运行第三轮，也未用应用内 Browser 换壳重演；因此不声称原通用 H4 通过。
- 经差量批准的独立 model-free selfcheck 对 bundle 与 direct classic 各加载一次真实页面，以权威 `GET /api/sessions` 的 12 条跨项目未归档 Session 为 readiness，并验证最近 10、非空全 12 命中、标题/完整与部分 ID、双域隔离、点击/Enter 导航、焦点与键盘、窄屏、i18n、归档动作保留及 Session 事实前后相等。两入口的 AgentRun、runtime、chat、tool、model catalog 与 model refresh 请求均为 0，页面错误为 0；完整前端模块 `294 passed`，frontend build/freshness、JavaScript 语法与 diff 门禁通过。
- 应用内 Browser 在合成 localhost 宿主完成宽屏与 390px 主观检查，确认入口位置、dialog 布局、双域隔离、打开聚焦、焦点恢复及单层焦点环；input 计算样式为无 `box-shadow`/outline/边框，外层仍有 3px 可见焦点提示，用户最终 `PASS`。设置页既有窄屏横向滚动不属于本次局部焦点修正，未扩展范围；内置 Browser 标签页保持打开，合成宿主的子进程、端口与临时根已规范清零。
- 未读取、哈希、修改或暂存用户 data，未调用真实模型、Key、工具或外网；未改变 server/API/schema、归档生命周期、运行状态事实或侧栏每项目预览上限，也未启动其他 TODO。

## 2026-08-29 12:06 · Codex

### CODE-009 · 归档永久删除确认文案易懂化

- 中英文确认文案改为面向用户的等价表达：明确会删除该会话的消息、生成内容及相关运行记录，同时明确已保存到项目目录的文件不受影响；不再暴露 Goal、generated asset 等内部术语。
- 本阶段只修改 i18n 文案和精确前端断言，不改变永久删除范围、API、`archiveToken`、事务、派生索引、缓存/转存归属或确认交互流程。

### 验证

- 直接确认文案与归档设置动作测试 `2 passed`，完整前端模块 `293 passed`；JavaScript 语法、frontend build/freshness 与 diff 门禁通过。
- 本轮为 QUICK 文案修正，按批准边界未运行 H4、Browser、doctor 或后端扩大回归；未读取、修改或暂存用户 data，未改受保护 `TODO.md` / `CODE-049`，未启动下一事项。

## 2026-08-29 11:42 · Codex

### CODE-009 · 归档永久删除动作状态、派生索引与跨代启动证据修正

- 设置页恢复/永久删除改为互斥且各自专属的动作状态：确认前、Cancel 或 Escape 均不进入 pending、也不派发请求；恢复中只有恢复按钮显示“正在恢复”，删除中只有删除按钮显示“正在删除”。只有权威 DELETE 成功才立即移除归档行并显示成功，后续活动列表与分支刷新转为后台收口，刷新失败只告警且不反转已经成功的删除事实。
- 新增版本化、无秘密、可重建的 Session→AgentRun 派生索引，内容只含 `runId/sessionId`。永久删除由该索引有界选择目标终态 Run，不再扫描全部 AgentRun 历史；旧历史启动重建并增量登记，缺失、building、损坏、陈旧或身份冲突均 fail-closed，且不回退到无界扫描。并发 create/persist/delete、崩溃、回滚与重建统一锁序，迟到 persist 不能在归档或 tombstone 后复活 Run。
- `dev_server.py` 与正式 `server.py` 均在监听前启动非终态和 Session→AgentRun 两类索引。H4 隔离宿主同步生产启动语义：每一代先启动两类 worker、确定性等待并核验 process-freshness ready，再创建 code HTTP listener；首次启动和 `restartGeneration` 的注入损坏均在 listener 前失败，子进程、端口和临时根完成清理。

### 验证与熔断事实

- fail-first 与修正后证据覆盖 1001 个无关 AgentRun：目标归档永久删除读取 0 个无关 Run 文件；同时覆盖旧历史启动重建、增量登记、服务重启、索引缺失/building/损坏/陈旧/冲突、并发登记、create/persist/delete、崩溃/回滚、锁序、Goal/asset/Run/分支清理、重复删除与脱敏。
- 扩展后的 bundle/direct classic 归档删除 H4 连续两轮均在释放延迟 DELETE 后失败，第二轮精确响应为 503。两轮完整附件确认 Cancel/Escape 为 0 DELETE、专属 loading 与互斥成立、页面错误为 0，且失败时 bundle/Goal/asset/Run 均安全保留；根因是隔离宿主重启代未建立 Session→AgentRun 的进程级 freshness，服务端按合同正确 fail-closed。依 H4 熔断规则未运行第三轮，也未用应用内 Browser 换壳重演永久删除场景，因此不声称原 H4 通过。
- 经 STRICT 差量批准，非浏览器等价链在第一代创建并归档目标、seed 目标与无关终态 Run，重启到双索引 ready 的新进程后只发出一次原始 HTTP DELETE：返回 200，目标 core/Goal/asset/Run 全部清理，无关 Run 读取数为 0 且事实保留；重复 DELETE 稳定返回 410 `session_deleted`，全程无 503、无 retry、无 sleep。独立 infrastructure self-check 首次仅因最终汇总变量名错误 exit 1，业务断言与 cleanup 已完成；修正报告字段后完整通过并归零子进程、端口与临时根。
- 相关 Session/AgentRun/archive/dev server/routes/server 回归为 `546 passed + 172 subtests`，前端模块为 `293 passed`；frontend build/freshness、Python/JavaScript 语法和 diff 门禁通过。环境未变化，沿用本任务入场时 `doctor 10/10`，未机械重复。当前 HEAD 基线自带且与本差量无关的 `test_explicit_context_calibration_commits_before_accepting_model_result` 仍以期望 1,050,000、实际 1,000,000 失败，本阶段未修改 context calibration，相关扩大回归明确排除该一项。

### 状态与边界

- 不改变公开 API、Session/AgentRun 记录 schema、archive manifest/bundle、删除成功语义、事务回滚、ownership 或 exactly-once 边界；索引不含提示词、消息、路径、URL、Key、Authorization 或工具内容。
- 未读取、哈希、导入、修改、删除或暂存用户 `data/generated-assets/` 与 `data/image-route-registry.json`，未改受保护 `TODO.md` / `CODE-049`，未启动 `CODE-044`、模型、生图、Key、发布或线上配置事项。

## 2026-08-29 02:41 · Codex

### CODE-009 · 会话可恢复归档产品面与归档资格性能完成

- 活动会话菜单的主删除动作改为归档；归档成功后会话即时退出活动与置顶列表，当前会话安全回到保留草稿的新会话态。设置页新增“已归档会话”，按项目展示标题、来源和归档时间，支持恢复以及带当前 `archiveToken` 的单条永久删除；归档态仍不能直接打开或回复，恢复本身不会创建 AgentRun、聊天、工具或上游请求。
- 归档资格不再为每次请求扫描全部 AgentRun 历史。版本化、无秘密的非终态派生索引只记录 run/session 身份；AgentRun admission 先登记，终态事实持久化后才清除。每个服务进程都会执行兼容重建，重建中、缺失、损坏、落后或身份冲突均保守返回不可用；统一锁序避免 rebuild 与 AgentRun 持久化的 ABBA 死锁，并覆盖旧版本 writer、崩溃窗口及并发登记不丢项。
- 归档成功后顶部短提示为“已成功归档会话，可在 设置－已归档会话 进行恢复”，仅目标短语使用强调色且不可点击；非终态、索引构建/恢复不可用和位置冲突均映射为不含原始异常的顶部错误提示。权威 archive POST 完成即结束 pending，列表刷新不延长处理中状态，重复点击只产生一次动作请求。
- 永久删除确认层提升到设置页之上，保留原不可逆文案、token 和唯一 DELETE 语义；Confirm、Cancel、背景、Escape、焦点恢复均不关闭设置页或穿透到底层控件。H4 fixture 同步以具名 cleanup 释放两代 Page observers、归档 audit listener 与 Context route，classic 入口等待 `settingsFeature.bind()` 之后已有的权威运行时语义，Playwright worker 可自然退出。

### 验证

- fail-first 证明旧归档资格路径会读取 1001 个无关 AgentRun JSON；修正后 ready 索引只检查当前 Session，旧 ready 索引加 legacy writer 的启动重建、building/corrupt/missing、登记/终态崩溃窗口、并发 register、ABBA 事件门控、archive 与 AgentRun admission 竞态均闭合且 fail-closed。
- Session、AgentRuntime、dev server、frontend、routes、server 与 Goal 冻结回归：`936 passed + 171 subtests`；frontend build/freshness 与 JavaScript/Python 语法、diff 门禁通过。
- 新生命周期专测 `1 passed`：restartGeneration、replacePage、addInitScript、audit listeners 后 `trackedPageObservers=0`、`registeredPageCleanups=0`、Context route 已解绑、两张 Page 与 Context 关闭，隔离端口/子进程/临时根归零且命令自然 exit 0。最终 bundle/direct classic 归档 H4 `2 passed`、命令 exit 0：两入口均覆盖 409/503 安全门禁、成功提示、pending、无关 AgentRun 零读取、双击一次请求、物理归档、restart/restore、pin/父子关系、第二次归档、确认永久删除和 core/Goal/asset/终态 AgentRun 清理，模型/图片/工具/上游副作用为 0。
- 应用内 Browser 只使用隔离 localhost 夹具：实测非终态错误提示、成功提示精确文案、蓝色强调且交互元素数为 0、设置页归档列表与恢复；永久删除确认层计算层级为 `40 > 20`，Escape 只关闭确认层并保留设置页和归档行。标签页按协作规则保持打开，隔离服务随后 canonical cleanup，未读取浏览器存储或真实 Key。
- `python verify.py doctor` 的 Python/Node/npm 7 项通过，esbuild/Playwright package/Chromium launch 3 项命中 doctor 短超时；同一现场真实 frontend build/freshness、完整 H4 和应用内 Browser 均已成功，进程、端口和本阶段临时根归零，故按环境能力边界披露而不机械重跑。

### 状态与边界

- 本阶段不改变物理归档 bundle/manifest/journal、公开 API、Session schema、权限或 legacy DELETE/压缩 archive 语义；不读取、修改或暂存用户真实 Session、generated assets、image route registry 或凭据，也未调用真实模型/外网。
- `CODE-009` 的产品实现与本地验收已完成；canonical TODO 继续保留用户实际验收边界，本阶段不自动启动 `CODE-044` 或其他事项，未 push、tag 或 release。
