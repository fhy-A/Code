# 2026-09-11 开发日志

## 2026-09-11 18:04 · DSH

### v0.6.10 prepare 收口：全量 pytest 上限 1500→3600 秒、v4/v5 过期断言增强与整机吞吐归因

- 承接本日 16:15 条目（R058 差量提交 `b1aa02c`，13 文件 +108/−44）。随后 `python release.py 0.6.10 --prepare --yes` 在阶段 2 失败：`pytest_full` 以 `status=timeout exit=124 elapsed=1500.015s` 结束（本次收集 3454 项，1500 秒时约 80%），另有 1 项 pptx clean-room 渲染失败与 2 项 v4/v5 子测试失败。失败后 canonical 流程原子回滚：`VERSION` 仍 0.6.9、`docs/releases/v0.6.10.md` 无残留改动、`.git/code-release/v0.6.10.json` 不存在，未签发任何凭证。本条将 16:15 条目所述的全量预算 1500 秒更新为 3600 秒。
- 只读诊断（单次完整套件运行）：`python -m pytest tests -v --tb=short --durations=50` 为 **3 failed, 3452 passed, 1 skipped, 4 warnings, 1523 subtests passed in 2609.46s（0:43:29）**。排除 LibreOffice 相关时间后仍约 2547 秒（pptx 渲染 62.12 秒；`test_xlsx_skill_resource.py` 单独 16 passed in 1.50s、不做真实转换），不可能回到 1500 秒内。最慢项为 `test_skill_management_read_view.py::test_31_skill_toggle_snapshot_and_integrity` 的 229.06s setup + 52.22s call，其后是 4×约 30 秒的 skill 依赖安装与 56.72 秒的 skill 运行时线性读计数。
- 耗时对照（本机历史检查记录与本次实测）：09-08 18:12 同命令 2836 passed / **994.79s**；09-08 22:46 同命令 **1256.61s**（同代码同命令当天内部即波动 26%）；09-11 两次 1500 秒超时（prepare 阶段）与本次诊断 **2609.46s**。同一批最慢项由 09-08 的 setup 53.49s、pptx 16.02s 变为本日的 229.06s、62.12s，全项普涨约 2 至 4 倍，指向整机吞吐下降而非个别用例。
- 校准探针（单跑 `tests/test_skill_management_read_view.py --durations=0`）：**9 passed in 266.98s**，其中 `test_31_skill_toggle_snapshot_and_integrity` **setup 186.15s + call 42.48s = 228.63s**，为 09-08 基线 65.92s 的 **3.47 倍**。探针前后本机采样：CPU 四采样 36/22/18/24（均值 25.0%）→ 18/19/8/1（均值 11.5%），可用内存 15.72 GB → 12.98 GB，League 家族进程 0。
- 根因边界（本人采样与审批侧只读核对合并记录）：内存与分页假说被否证——Pages/sec 0.0、pagefile 已用 167 MB/7168 MB（2.3%）、commit 27.96/38.82 GB、空闲磁盘延迟 0.38 ms 且队列 0。186 秒 setup 的真实内容是约 4000 个小文件的创建、复制与两次哈希快照（31 个 skill、build_bundled_catalog、bootstrap、convert-v2 升到 generation 32、逐项 select-candidate 与整树哈希），属 I/O 延迟受限；最大嫌疑为实时防护对大量小文件抖动的扫描，验证需管理员权限。探针期间仍有 SlayTheSpire2 于 17:56:42 启动，steam 与 8 个 steamwebhelper 自 10:42、wemeetapp 10 个自 15:17 常驻，另有 msedge 18、msedgewebview2 12、ChatGPT 11、WeChat 13、electron 9。16:42 那次 prepare 虽无该游戏，同样在 1500 秒只跑到 80%，故结论不依赖单次负载波动：本机当前吞吐无法在 1500 秒内跑完完整套件。
- v4/v5 过期断言（分类与修正）：`tests/test_agent_runtime.py` 的 `test_v4_and_v5_records_project_interleaved_visual_tools_without_rewriting` 断言最后更新于 `cd3427d`（2026-08-27），此后两次已批准变更各向模型负载插入一条请求期 system 投影——`9df515c`（2026-09-10 14:42，每轮工作区）与 `518b80a`（2026-09-10 18:19，可选行动状态）。实测插入前角色序列仍为 assistant/tool/tool/user/user，`record["messages"]` 投影前后逐字节不变（SHA-256 前后均为 `3a5df89a…`）；两条 system 分别匹配 `_agent_workspace_message(restored)` 与 `"[Optional action status]\n" + _ACTION_STATUS_GUIDANCE`，第二条由 `_agent_action_status_enabled(run)` 门控（该 run 的 read_file 为广告态，服务端 canonical 定义不含该字段）。断言由 4 条增强为 14 条：保留原两条意图，新增「投影 system 恰好 2 条且位于负载前两位」、两条投影的前缀与完整内容，以及移除 `_actionStatus` 后负载退化为 1 条 system 的启用条件检查。变异负控（pytest 插件仅置于本机 Temp，仓库零新增文件）：篡改 workspace 前缀→2 子测试失败；关闭行动状态门控→失败于 `1 != 2`。
- 预算变更（用户授权 B 路径）：`devtools/verification.py` 的 `pytest_full` 上限由 1500 秒改为 **3600 秒**（依据：较重负载下实测 2609 秒、当天内部波动 26%，3600 秒留约 38% 余量）；检查集合、顺序、命令参数 `tests -v --tb=short --durations=30`、其他检查超时与 H4 排除边界全部不变，无 retry、无 sleep、无测试删减。同值同步的断言与文本为 `tests/test_release_script.py`（用例名与三处）、`tests/test_release_execution.py`、`tests/test_verify_profiles.py`、`tests/test_release_workflow.py` 各一处，以及 `docs/release-guide.md` 两处；`docs/releases/v0.6.10.md` 同步门禁预算表述并保持未跟踪。共享定义指纹由 `7d182f13…` 变为 `fdb57251…`；同进程 manifest 对照确认 11 项检查 id 与顺序不变、`changed_ids` 仅为 `pytest_full` 且 command 未变。
- 验证与边界：`tests/test_release_script.py` **22 passed, 21 subtests passed in 0.23s**；v4/v5 用例 **1 passed, 2 subtests passed**；原先被钉死 1500 的三条断言复跑 **3 passed**。本阶段 9 个 tracked 文件一次性本地提交；随后按 3600 秒预算执行 prepare。未消耗 reprepare、未推送、未打标签、未创建 Release、未改动三个受保护未跟踪数据项。

## 2026-09-11 16:15 · DSH

### v0.6.10 发布前全量门禁输出与过期测试夹具对齐（承接 code077-replacement-R058）

- 交接与写者轮换：上一 Codex 开发任务在 2026-09-11 12:22 取得租约、12:23—12:24 写入后中断，未提交、未记录、未释放；本轮按 canonical 跨运行时租约以 DSH 身份审计式 reclaim 过期租约（`f6fb039e…` → `4dd50123…`，HEAD 与 baseHead 均为 `41076df`，cached 为空），随后 renew 到 3600 秒。未使用 acquire、force，未删除或重建租约。
- 归因（只读）：11 文件差量与 11:21 提交同源，属 v0.6.10 发布前对齐的续作，分两组。① 全量 pytest 可观测性：`devtools/verification.py` 的 `pytest_full` 由 `tests -q --durations=30` 改为 `tests -v --tb=short --durations=30`（1500 秒预算与完整 `tests` 目标不变），`release.py` 仅同步描述文案，`docs/release-guide.md` 同步阶段说明，`tests/test_release_script.py` 同步命令断言并新增“共享定义命令变化必须改变指纹”的负向检查。② 过期测试与冻结证据对齐：`tests/test_agent_runtime.py` 采纳“模型可见定义 = 服务端定义 + 仅供展示的可选 `_actionStatus`（maxLength 80）”，并断言服务端 canonical 定义与 `required` 均不含该字段；`tests/test_p0_stability.py` 对齐 `resumePersistedRuns` 的 `let ctx;` 切片边界；`tests/test_reasoning_frontend.py` 补齐 `src/agent/system-prompt.js` 模块与 `followUpSubmissions` 切片，并新增普通消息不得派发、legacy 失败排队消息可重试的负向检查；`tests/test_server.py` 对齐更新代际守卫文案；两个 manual-compaction 证据夹具按真实重放重算网页／重放／套件／场景哈希。
- 验证结果（全部真实执行）：两个 manual-compaction 夹具测试 **13 passed, 38 subtests passed in 16.15s**；`tests/test_release_script.py` **22 passed, 21 subtests passed in 0.59s**；四个文件的直接相关定向用例 **6 passed, 466 deselected, 14 subtests passed in 1.97s**；`npm run verify:harness-replay` 通过（17 fixtures、124 events、25 checkpoints、25 checkpoint recoveries、4 explicit recoveries，Replay hash `166a8141…`）；`npm run check:frontend` 构建与 freshness 通过；`git diff --check` 退出 0。doctor 沿用本环境既有边界（esbuild／playwright／chromium 三项 30 秒超时），未安装依赖、未改动环境。
- 夹具哈希来源：仓库内不存在夹具写入或 `--update` 入口，两个证据结构分别由测试内 `collect_evidence()`／`collect_suite()` 驱动真实 node 重放计算。本次独立复算确认：`htmlHash` `8c7bc211…` 与 `replayHash` `1e573283…` 与夹具冻结值一致且两次采集逐字节相同；失败边界 `suiteHash` `e09df3a3…` 与 19 个 `scenarioHashes` 全部一致、`sourceFixtureSha256` 一致；夹具文件 SHA-256 `c0469cd8…` 与测试常量一致。冻结值对变异敏感（既有 mutation 差异路径断言通过），未手工改写任何冻结数值。
- 本阶段只暂存上述 11 个文件与本日志、索引共 13 个公共文件并创建单一本地提交；未推送、未打标签、未创建 Release，未改动三个受保护未跟踪数据项与 `docs/releases/v0.6.10.md`，未改动产品行为、协议、持久化或发布冻结基线。

## 2026-09-11 11:21 · Codex

### v0.6.10 发布前最小测试维护通过

- 在产品基线 `b8693081afd06eb7c8275f11ad0a1837c28d40b8` 上，仅对齐 `tests/test_frontend_modules.py` 中九项已定位的过期测试：五项 Agent 夹具加载真实 owner、tools 和消息展示依赖；原生工具固定 schema 黄金及三个属性列表纳入已批准的可选 `_actionStatus`；工具结果槽按中文 4em／英文 6em、共同条目几何与行动状态精确 selector 校验。产品、协议、持久化与验证预算均未修改。
- 原 CAS 保存后提交游标、竞争消息保留、用量回滚、runtime 不重复消费、结果去重与同游标恢复的 2／1／4 次 watch 断言保持。为未知无 code 异常保留明确失败，并增加真实 ownership 切换后的行动状态负向检查；ownership 限制此处的临时展示，不将其误述为所有持久事件投影都被拒绝。
- 原九项加新增检查共 10 项通过，相邻恢复／权限／布局检查 16 项通过；行动状态 JS 检查 16 项通过。新增负向检查在真实实现通过，隔离内存中移除 owner 条件后因旧 context 重新显示行动文案而失败，确认能检测该退化；产品文件没有为负控改写。语法解析及 `git diff --check` 通过。复用同环境已通过的 doctor，不机械重复运行，也未手工预跑发布全量。
- 本阶段只将测试文件、本日志与索引三文件独立本地提交。证据保存在本机临时目录 `code0610-r055-9_6bqxwy`；其中输出汇总遇到一次 Windows 控制台字符编码错误，实际相邻与 JS 测试均退出 0，完整 UTF-8 测试日志保留并完成核对。未修改私有计划、公开短期待办或受保护数据，也未进行真实模型调用、标签、推送或发布。
