# 2026-08-24

## 2026-08-24 12:21 · Codex 开发会话

### CODE-036 · 外链 favicon 安全同源代理（R004-R007，用户 PASS）

#### 完成内容

- 浏览器不再直连目标站、FaviconKit、Google 或 DuckDuckGo 图标地址，只向同源 `GET /api/favicon?scheme=...&host=...` 请求图标；失败继续显示既有主题化地球图标，外链 href、完整 URL tooltip、右键复制、图片/文件卡和 bundle/direct classic 入口保持不变。
- 服务端仅接受规范化 HTTP(S) scheme 与 host，拒绝任意 URL、路径、端口、userinfo、IP literal、localhost、单标签和 `.local`；候选顺序为经校验目标/父域的 `/favicon.ico` 与固定 allowlist provider，不抓取或解析目标 HTML，也不把早期“页面声明发现”方案写入实际实现。
- 每个目标和重定向都要求 DNS 返回集全部为公网地址，并把 TCP 连接固定到已验证地址；HTTPS 继续以原主机名完成 SNI/证书校验，拒绝私网、环回、链路本地、保留/组播、混合公网私网解析、peer 漂移、重定向循环、非默认端口及逐跳 HTTPS 降级。请求不携带 Cookie、Authorization 或 Referer。
- 出站请求采用总 deadline、单 socket timeout、并发上限、三次重定向、256 KiB、MIME 与 raster magic 双校验，拒绝 SVG/HTML；响应头及每次 body read 都按剩余总 deadline 收紧。合法 `Content-Length` 精确读满后立即完成并进入正缓存，提前 EOF/超额字节拒绝；无长度/chunked 响应仍逐次收紧，并在协议确认 closed 后正常完成。
- 解析结果使用 128 项有界内存 LRU、正缓存 6 小时、负缓存 5 分钟与同 host 请求合并；并发槽饱和属于临时容量拒绝，不写负缓存，非预期异常固定返回无内部细节的 `favicon unavailable`。未新增磁盘缓存、localStorage 第二缓存、依赖、凭据或持久化格式。

#### 验证与验收

- 最终 favicon 安全测试 `13/13 passed`，完整 `tests.test_server` `224/224 passed`；R004 同范围 server/frontend 非构建回归 `443 passed`。Python/Node 语法、loopback 端点、H4 discovery 与 `git diff --check` 通过。
- 用户普通终端完成 fresh frontend `build:frontend` / `verify:frontend`，bundle/direct classic `CJK bare URL` H4 `2 passed`；开发沙箱中的 esbuild 目录权限与 Chromium `spawn EPERM` 仅作为环境排除，未计为通过。
- 审批只读真实探针确认百度 `/favicon.ico` 返回合法 `image/x-icon`；修复精确 `Content-Length + Connection: close` 的多读误判后，用户在 Code Dev/direct classic 对扩展网站矩阵复核通过：合法站点显示真实图标，无效/失败站点稳定降级为地球图标，刷新与链接交互保持。

#### 兼容与有限边界

- 本阶段没有 HTML icon discovery、第三方并发竞速、磁盘/业务数据写入、Session/消息 JSONL、配置 schema、协议或线上配置变化；服务器只处理页面已出现外链的 host，代理本身不授予其他网络或数据权限。
- `CODE-036` 的中文标点裸链接边界与 favicon 安全代理、缓存、降级、双入口视觉/交互均已完成；事项从 TODO 删除，Code 最后分配编号游标保持 `CODE-038`，未自动启动下一事项。

## 2026-08-24 10:05 · Codex 开发会话

### CODE-038 · 会话列表相对时间与状态槽、Goal 会话切换隔离（R001-R011，用户 PASS）

#### 完成内容

- 会话标题右侧增加固定宽度状态槽，状态优先级为 inactive 会话的“等待回答 `?` → 等待确认 `!` → 运行环 → 未读圆点 → 空闲相对时间”。当前会话的问卷/授权仍由既有面板唯一提示，状态槽回落到运行、未读或时间，避免重复提示；中英文 `title` / `aria-label`、浅深主题与 `prefers-reduced-motion` 降级保持一致。
- 空闲时间只消费 `CODE-037` 已规范化的 `lastMessageTime`，缺失时回退 `updatedAt` / `createdAt`，未来值钳制为“刚刚 / now”；分钟 ticker 仅原地更新 idle 槽位，不整表重绘、不请求网络、不写持久化，也不改变会话排序。
- 异会话切换在通过既有 300ms 用户防抖后立即隐藏旧 Goal 面板，并以会话身份和请求序列丢弃迟到响应；目标加载成功后才投影目标 Goal，失败则恢复原会话正确面板。同会话点击和被防抖拒绝的切换不清空面板。
- 运行环保持 11px、2px 主题色边框和透明缺口，周期由 0.8 秒调整为 1.4 秒。状态槽同 kind 更新改为原地 patch；后台会话仍在 streaming 时只标记未读并局部刷新槽位，终态仍完整重绘列表，因此流式增量不会重建 indicator 或重启动画，最终未读、时间和排序仍同步。
- 编辑授权 H4 的路径身份契约统一兼容增强前 `data-path` 按钮与增强后 `title` 文件卡，并继续精确断言 waiting/terminal、Diff、刷新和 exactly-once；该修订未改变产品授权协议或写入次数。

#### 验证与验收

- 前端模块最终 `222/222 passed`；Node/Python 语法、前端构建 freshness、H4 discovery 与 `git diff --check` 通过。
- 用户普通终端在 fresh bundle 上运行 bundle/direct classic `session status slot` H4：`2 passed`；覆盖 active/inactive 运行态、跨两个流式 gate 的同一 indicator 身份、终态未读、返回 idle、一次模型请求、零工具与精确网络边界。
- 用户确认 Code Dev / direct classic 的相对时间、状态优先级、inactive-only 问卷/授权提示、Goal 切换隔离、1.4 秒连续运行环及桌面/移动、浅深主题观感人工验收 PASS。

#### 兼容与有限边界

- 本阶段未增加服务端、Session 索引、消息 JSONL、Goal/AgentRun 事件或持久化字段；会话排序和最后消息时间事实仍由 `CODE-037` 的服务端权威保存链负责。
- 状态槽只投影现有 streaming、未读、问卷与授权事实，不推断排队、网络、凭据或其他生命周期；截图中新发现的 Markdown 链接异常不属于本提交，未顺带处理或登记新事项。

## 2026-08-24 11:08 · Codex 开发会话

### CODE-036 · 中文标点相邻裸链接解析（R001-R003，用户 PASS）

#### 完成内容

- 在 `marked` 解析前增加有界普通文本投影：仅当 ASCII HTTP(S) 裸 URL 紧邻明确的 Unicode 标点时转换为标准 angle autolink，使 `yuanbao.tencent.com`、`xinghuo.xfyun.cn`、`mistral.ai` 在全角括号、顿号和句号之间分别生成独立链接；标点与说明文字仍由 Markdown 正常解析。
- 投影器以代码区和显式 Markdown 结构为边界，行内/围栏代码、标准链接、图片、既有 angle autolink、原始 HTML、ASCII query/fragment 和百分号编码保持不变；Unicode 边界从文字脚本收窄为 General Category `Punctuation`，合法 `/中文`、`/한글`、`/かな` 路径及无标点正文不被截断或猜测拆分。
- 外链 renderer 的 `target="_blank"`、`rel="noopener"`、域名别名、完整 URL tooltip、右键复制和既有 favicon/glyph 链保持不变；H4 只把已知 CDN 静态资源及精确域名/父域 favicon 候选列为受控 non-loopback GET，未修改产品 favicon 行为。

#### 验证与验收

- 确定性 Markdown 定向回归 `3/3 passed`；本地可用 `marked 17.0.5` 真实解析证明原复现句生成三个精确 href，Unicode 路径投影前后保持原字节。Node/Python 语法、H4 discovery 与 `git diff --check` 通过。
- 开发受限环境完整前端模块为 `223/224`，唯一阻断是 `esbuild` 无权遍历沙箱临时路径；该环境阻断未记作通过，也未通过放宽测试掩盖。
- 用户普通终端在 fresh bundle 上运行 bundle/direct classic `CJK bare URL` H4：`2 passed`；已覆盖独立 anchor 数量/href、中文标点、tooltip、右键复制、零模型/工具请求及显式网络白名单。合法 Unicode 路径边界由前述确定性测试覆盖；用户同时确认 Code Dev / direct classic 人工验收 PASS。

#### 兼容与有限边界

- 本阶段未修改 `marked` CDN 版本、favicon 获取实现、服务端、样式、Session/消息持久化、TODO 编号游标或线上配置；无标点中文正文中的 URL 歧义继续交给 `marked` 既有行为，不在本阶段猜测切分。
- `CODE-036` 只完成中文标点相邻裸链接解析阶段，整项仍保留为未完成；下一动作是另行实施已确认的 favicon 安全同源解析/代理，未在本提交中启动。
