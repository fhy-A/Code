# 2026-08-24

## 2026-08-24 21:50 · Codex 开发会话

### CODE-029 · 自动模式风险确认与派发门禁（用户 PASS）

#### 完成内容

- 每次从非 Auto 权限档切换到 Auto 都先显示风险确认；取消、关闭、`Esc` 或点击遮罩均保持原权限，不写确认记录，也不创建 Session、消息、queue、Goal 或 AgentRun。确认后才持久化 Auto 和版本化 `v1` acknowledgement；切离 Auto 会使 acknowledgement 失效，下次进入重新确认。
- 历史已保存 Auto 但缺少当前 acknowledgement 时，在下一次会启动模型工作的入口产生副作用前补确认；取消会安全回到 Accept 并保留草稿/附件。纯本地 `/goal status` 不触发弹窗，显式 `/goal <目标>` 在 Goal/Session/AgentRun 创建前受门禁，确认后的原意图只执行一次；同一已确认 Auto 状态下的续聊、刷新和恢复不按任务重复提示，既有 Run 的冻结权限不被追溯改写。
- 最终弹窗采用警示图标、简短导语、文件与编辑/命令与工具/网络访问三行能力面板及独立服务端保护说明；中英文和 HTML 中文 fallback 一致，中文导语使用“您”。移除重复启用小字和无用 i18n 键，不增加 checkbox 或外链；保留 `alertdialog`、默认聚焦“取消”、Tab/Esc、遮罩/关闭按钮和关闭后的焦点恢复。
- Auto 与 Accept 使用相同的当前允许工具集合；差异仅在 Auto 不再为这些允许操作逐项弹授权。越界路径、危险或系统级命令、破坏性 Git、工具预算与参数校验等服务端硬限制未弱化，权限协议、AgentRun/Session schema 和服务端拦截均未改变。

#### 验证与有限边界

- 定向权限、Goal、焦点、i18n 与 DOM 契约 `6/6 passed`；Node/Python 语法、双语 key parity、旧键/旧措辞扫描及 `git diff --check` 通过。完整前端模块为 `234/235 passed`，唯一未通过项是受限开发沙箱中 `esbuild` 无法读取仓库目录，该 build/freshness 项未记作通过。
- 原巨型 H4 被拆为选择确认、legacy 派发和无活动 Goal 的 acknowledgement reload 三类独立场景，各自覆盖 bundle/direct classic，避免活动 Goal 跨阶段污染。用户普通终端提供最终结果 `6 passed (18.6s)`，其中保持默认取消焦点、零副作用取消、显式 Goal exactly-once、隔离 fake connection/synthetic key、刷新后 plain dispatch 及零真实网络/真实凭据边界。
- 视觉与敬语修订后，用户明确回复“验收完毕”；该结论记录最终双入口人工验收，不把开发沙箱未完成的 `check:frontend` 冒充为精确通过。本阶段不执行 push、tag、release，也不自动启动后续 `CODE-013` 或 `CODE-026`。

## 2026-08-24 18:09 · Codex 开发会话

### CODE-042 · 项目级多标签预览工作区方案与待办登记（尚未开发）

- 按已确认的第一阶段产品行为分配 `CODE-042`，并在 [`docs/preview-workspace-multi-tab-development-guide.md`](../../preview-workspace-multi-tab-development-guide.md) 建立独立开发指导；Code 最后分配编号游标由 `CODE-041` 原子推进至 `CODE-042`，事项进入“可直接启动”，当前确认推进序列继续保持“无”。
- 只读核对当前实现：文件树和最终回答本地链接共用 `loadFile`，前端仍以唯一 `previewPath` 和全局 `code-preview-open` / `code-preview-path` / `code-preview-width` 恢复单文件预览；Session 导航切换项目根后没有项目级标签命名空间。专题指导据此定义项目级状态、同项目会话保留、跨项目原子隔离、普通单击复用、显式新标签、同路径去重、活动标签唯一渲染、旧键惰性迁移/回退，以及请求序列和资源释放边界。
- 第一阶段明确排除左右分栏、独立系统窗口、可编辑文件、标签固定/分组、Session JSONL、AgentRun/Goal、服务端文件协议和持久化变化；Codex 仅作为交互灵感，不作为内部实现或兼容承诺。

#### 验证与有限边界

- 本轮仅完成 TODO 与专题方案登记；检查范围为 ID/游标、状态/主域、现有代码事实、Markdown 本地链接、UTF-8、四文件白名单与 diff hygiene，不运行代码测试，也没有产品代码、测试、配置、协议或持久化变化。
- `CODE-042` 尚未实施或验收，不能从本文推定多标签已经可用；后续必须另行取得 STANDARD 实施授权，按专题指导完成自动与双入口人工验收后才能写入完成事实。

## 2026-08-24 15:55 · Codex 开发会话

### 旧 spec 删除与 Goal sidecar 忽略收口

- 正式提交删除 `Code-v0.5.28.spec` 至 `Code-v0.5.34.spec` 七个历史构建快照；`Code-v0.6.1.spec` 至 `Code-v0.6.4.spec` 均继续存在且受 Git 跟踪，当前 `VERSION=0.6.4`，`release.py` 为下一版本选择的最新模板仍是 `Code-v0.6.4.spec`。
- `.gitignore` 仅新增 `data/goal-workflows/`、`data/goals-v2/`、`data/goals/` 三个精确运行目录。三个 sidecar 实体目录原地保留；清理前后只按相对文件名、数量、大小和 UTC mtime 核对一致，没有读取、删除、移动或改写 JSONL、lock 等业务内容。
- ignore 边界检查确认三个 Goal 目录均命中规则，而受版本控制的 `data/memory`、`data/skills`、`data/project-summary.md` 均未被忽略；没有扩大到整个 `data/`，也没有改变发布脚本、VERSION、产品代码、TODO、协议、持久化或线上配置。

#### 验证与有限边界

- `tests/test_release_script.py` 与 `tests/test_release_workflow.py`：`39 passed + 229 subtests passed`；当前 spec 只读契约确认 `VERSION=0.6.4` 可读取、四个 v0.6.x spec 存在且下一版本模板选择为 `Code-v0.6.4.spec`。
- 组合前端契约在读取并通过当前 spec 静态断言后，因开发沙箱中的 `esbuild` 无权读取仓库路径而未完成构建，该项未记作通过；本次没有改前端或构建产物。UTF-8、ignore 精确边界、working/cached diff 与提交白名单另行通过。
- 本阶段只收口既有仓库噪声，不删除 Goal sidecar 实体数据，不启动下一 TODO，也不执行 push、tag 或 release。

## 2026-08-24 15:38 · Codex 开发会话

### Goal / DSH / pi 历史文档归档

- 正式归档 2026-08-14 的 DSH Goal 调研与顶部工具日志产品决策记录，并纳入 `docs/dsh-goal-mode-research-analysis.md`、`docs/pi-code-dsh-agent-comparison.md` 和 Goal 开发指导的历史差量；本阶段只处理六个文档，没有代码、TODO、协议、持久化或产品行为变化。
- 两份研究文档保留原始结论、版本、来源与有限验证边界，并在顶部明确标为 2026-08-14 历史快照：DSH 调研基于随附包 `0.1.0-rc.6` 源码/README，未做真实运行流程验证；pi 对照仅核验公开 README、包结构和 pi-web 文档，未读 pi 本体源码，stars 等时效字段仅代表当日截面。
- `goal-mode-development-guide.md` 的状态已与当前事实对齐：Goal v2 持久执行基线于 2026-08-16 完成；文档继续作为原始设计与后续演进约束，阶段路线和开放问题不视为当前 TODO。原有权限增补经当前代码复核成立：`goal_created` 持久化 `permissionProfile`，自动 successor 继承父 Run 的 `permission_profile`，持久 AgentRun 恢复读取同一字段。
- 定向权限证据、UTF-8、Markdown 本地链接、六文件白名单及 working/cached diff 检查通过；七个旧 spec 删除和三个 Goal sidecar 目录保持原样，未读取 sidecar 业务内容，也未修改 `.gitignore` 或无关活动 handoff。

#### 有限边界

- 历史研究文档不升级为当前产品事实，也不替代代码、测试、TODO 和开发日志；后续引用 pi/DSH 能力、版本或数量数据时必须重新核对。
- 本次归档不启动新的 Goal、DSH、pi 或顶部工具日志工作，不处理旧 spec 和 sidecar 忽略规则；这些边界由后续独立阶段决定。

## 2026-08-24 15:17 · Codex 开发会话

### CODE-041 · Windows 文件显示与资源管理器前台激活（R001-R003，用户 PASS）

#### 完成内容

- Windows 文件“在文件夹中显示”改为通过 `SHOpenFolderAndSelectItems` 打开父目录并选择精确文件；文件夹“用文件资源管理器打开”只打开精确目录。默认程序打开、目录终端、项目允许路径校验和非 Windows 回退继续保持原语义。
- 新增小型 Windows Explorer 集成层：以 `Shell.Application.Windows()` / `Document.Folder.Self.Path` 枚举 Shell 窗口，仅按规范化文件系统路径识别唯一目标 HWND，不使用窗口标题。Windows PowerShell 5.1 枚举结果显式写为无 BOM UTF-8 字节并由 Python 严格解码，中文、韩文、空格和长路径不依赖本地代码页。
- 目标窗口最小化时先以 `SW_RESTORE` 恢复，再尝试 `SetForegroundWindow` 并核对实际前台；系统拒绝普通激活时，仅对同一精确 HWND 做一次临时 topmost pulse，并在 `finally` 中立即撤销。多窗口精确匹配歧义、枚举失败、超时或窗口错配均不会激活其他 Explorer。
- 目标已不存在时只打开项目范围内最近存在的父目录，返回明确 `degraded` 且不伪报已选择；前端对成功保持静默，对降级显示双语 warning，对 HTTP、网络、无效 JSON 或不完整响应只显示通用“打开失败”，不投影服务端异常细节。

#### 验证与验收

- Windows Explorer 专项 `22/22 passed`，覆盖文件选择、目录打开、Unicode/空格/长路径、最近父目录、精确/歧义 HWND、最小化恢复、前台成功/拒绝、topmost 严格成对及异常清理；Explorer + 完整 server 回归 `246/246 passed`。
- P2/i18n `56/56 passed`，前端成功静默/degraded/HTTP 400/网络/无效 JSON/不完整响应定向 `2/2 passed`；Node/Python 语法、禁用窗口 API 扫描、H4 基础设施/discovery 与 `git diff --check` 通过。完整前端模块为 `230/231 passed`，唯一未通过项仍是开发沙箱无法启动 `esbuild` 的目录权限限制；该环境下 build/freshness 未计作通过。
- 用户普通终端运行 bundle/direct classic `Explorer route contract` H4：`2 passed (5.1s)`；H4 通过真实 `/api/open-file` 路由验证默认程序、文件选择、目录打开、成功静默、降级提示、零模型/工具副作用，并以测试 stub 隔离真实 Shell 外部动作。
- 用户完成 Windows 实机人工验收并确认通过：覆盖无既有 Explorer、最小化、被其他窗口遮挡、多窗口/多标签、中文与空格文件、目录、不存在目标和显式默认程序打开；目标选择/前台恢复符合预期，未观察到错误窗口激活或残留永久置顶。

#### 兼容与有限边界

- 本阶段未使用 `AppActivate`、`Alt/Tab`、`SendInput`、`SwitchToThisWindow`、`AttachThreadInput`、UIAccess、标题匹配、注册表、自定义协议、常驻 helper 或第三方依赖；没有 Session、AgentRun、Goal、持久化格式、安装器或线上配置变化。
- Explorer 前台仍受 Windows 系统策略约束；无法唯一识别或无法取得前台时会返回降级状态而不近似激活其他窗口。`CODE-041` 已完成并从 TODO 移除，Code 最后分配编号游标保持 `CODE-041`，当前推进序列恢复为“无”，未选择或启动下一事项。

## 2026-08-24 14:24 · Codex 开发会话

### CODE-040 · 大型文本文件内部截断预览回归修复（R001-R003，用户 PASS）

#### 完成内容

- 删除共享 `loadFile` 中按 350000 字符或 8000 行自动调用 `/api/open-file` 的隐式分支；文件树和最终回答本地文件链接现在都继续走右侧内部预览。350000 字符/8000 行只保留为免语法高亮的低成本渲染门槛，不再改变打开方式。
- 服务端既有 `MAX_PREVIEW_BYTES=1 MiB`、`truncated` 元数据与解码边界保持不变；前端只投影服务端返回的截断内容并显示既有“内容已截断 / Content truncated”状态，不传输或渲染完整超大文件。
- 显式右键“用默认程序打开”仍是文本文件启动系统程序的唯一入口；图片、PDF、二进制、路径越界、行号定位、复制和右键菜单语义均未改变。

#### 验证与验收

- 确定性前端契约覆盖超过 350000 字符和超过 8000 行的文本：两者都只请求 `/api/file`、进入内部 `code-preview`、保留截断提示且不请求 `/api/open-file`。直接相关测试 `5/5 passed`；完整前端模块本地可运行部分为 `228/229 passed`，唯一未通过项是开发沙箱无法启动 `esbuild` 的既有环境限制，未计作产品通过。
- Node/Python 语法、H4 discovery 与 `git diff --check` 通过；开发沙箱 Chromium `spawn EPERM` 作为环境排除。用户在普通终端运行 bundle/direct classic `large text preview` H4：`2 passed`，核心断言覆盖文件树、回答链接、截断标记、刷新恢复、零隐式系统打开及显式右键入口。
- 用户随后确认三项人工验收“均正常”：文件树大型文本在右侧截断预览、最终回答大型文本链接使用同一内部预览、显式“用默认程序打开”仍可用。

#### 兼容与有限边界

- 本阶段未提高服务端读取上限，未修改服务端、Markdown renderer、样式、i18n、Session/预览持久化或二进制/PDF/图片路由，也未引入完整大文件传输、虚拟化或新依赖。
- `CODE-040` 已完成并从 TODO 移除；Code 最后分配编号游标保持 `CODE-040`，未登记或启动 `CODE-041`。

## 2026-08-24 13:14 · Codex 开发会话

### CODE-039 · 最终回答结构化富文本可读性优化（R001-R003，用户 PASS）

#### 完成内容

- 最终回答的 GFM 任务清单直接消费 `marked` 的 `task` / `checked` token，以禁用原生 checkbox 和正文 `aria-labelledby` 保留已完成/未完成语义；点击不能改变消息内容，原始 Markdown 与消息复制结果不变。普通、紧凑和松散任务项统一投影，嵌套有序/无序列表继续由同一 token 树递归解析。
- 列表排版补齐层级缩进、段落间距、长文本换行和 checkbox/正文对齐；普通列表仍保留原 marker。旧会话继续从原 Markdown 重投影，没有消息 JSONL、Session、AgentRun、协议或迁移变化。
- GFM 表格保留每个表头/单元格的 `left` / `center` / `right` 对齐元数据，并在 CSS 中落实对应对齐；宽表只在独立 `.table-scroll` 容器内横向滚动。页面按真实 `scrollWidth > clientWidth` 原地标记溢出、启用键盘焦点并在滚动区下方显示不遮挡内容的提示，桌面和约 390px 窄屏均不产生页面级横向溢出。
- 普通本地文件卡从带背景、边框、圆角和大 padding 的卡片收敛为透明无框的单色类型图标 + 强调色文件名，并按正文基线对齐；完整路径 tooltip、点击打开/行号、右键菜单、二进制路由、越界降级和消息复制保持不变。图片路径仍使用原缩略图卡和边框，不随本阶段轻量化。

#### 验证与验收

- 结构化 Markdown 与普通文件卡定向契约最终 `4 passed`；完整可运行前端模块 `227 passed`，仅排除开发沙箱中已知无法启动 `esbuild` 的确定性构建节点。Node 语法、H4 discovery 与 `git diff --check` 通过，任务清单、表格、普通文件卡和图片卡边界均有确定性断言。
- 用户普通终端报告 bundle/direct classic `structured rich text` H4 `2 passed`；覆盖 checked/unchecked 只读任务、两级嵌套列表、`package.json` / `README.md` 无框文件展示、三列对齐、宽表局部滚动、桌面/移动无页面溢出、零模型/工具副作用及精确网络白名单。
- 用户确认 Code Dev / direct classic、明暗主题和约 390px 窄屏全部人工验收 PASS：任务/列表/表格清晰可读，普通文件链接视觉噪声降低，图片卡保持原样。

#### 兼容与有限边界

- 本阶段未新增依赖、第二套 Markdown 解析器、可编辑任务状态、表格排序、脚注、Mermaid、服务端或持久化字段，也未改变代码块、数学、Admonition、链接/favicon、图片、路径识别和右键交互。
- `CODE-039` 已完成并从 TODO 移除；Code 最后分配编号游标保持 `CODE-039`，未自动选择或启动下一事项。

## 2026-08-24 12:21 · Codex 开发会话

### CODE-036 · 外链 favicon 安全同源代理（R004-R007，用户 PASS）

#### 完成内容

- 浏览器不再直连目标站、FaviconKit、Google 或 DuckDuckGo 图标地址，只向同源 `GET /api/favicon?scheme=...&host=...` 请求图标；失败继续显示既有主题化地球图标，外链 href、完整 URL tooltip、右键复制、图片/文件卡和 bundle/direct classic 入口保持不变。
- 服务端仅接受规范化 HTTP(S) scheme 与 host，拒绝任意 URL、路径、端口、userinfo、IP literal、localhost、单标签和 `.local`；候选顺序为经校验目标/父域的 `/favicon.ico` 与固定 allowlist provider，不抓取或解析目标 HTML，也不把早期“页面声明发现”方案写入实际实现。
- 每个目标和重定向都要求 DNS 返回集全部为公网地址，并把 TCP 连接固定到已验证地址；HTTPS 继续以原主机名完成 SNI/证书校验，拒绝私网、环回、链路本地、保留/组播、混合公网私网解析、peer 漂移、重定向循环、非默认端口及逐跳 HTTPS 降级。请求不携带 Cookie、Authorization 或 Referer。
- 出站请求采用总 deadline、单 socket timeout、并发上限、三次重定向、256 KiB、MIME 与 raster magic 双校验，拒绝 SVG/HTML；响应头及每次 body read 都按剩余总 deadline 收紧。合法 `Content-Length` 精确读满后立即完成并进入正缓存，提前 EOF/超额字节拒绝；无长度/chunked 响应仍逐次收紧，并在协议确认 closed 后正常完成。
- 解析结果使用 128 项有界内存 LRU、正缓存 6 小时、负缓存 5 分钟与同 host 请求合并；并发槽饱和属于临时容量拒绝，不写负缓存，非预期异常固定返回无内部细节的 `favicon unavailable`。未新增磁盘缓存、localStorage 第二缓存、依赖、凭据或持久化格式。

#### 验证与验收

- 最终 favicon 安全测试 `13/13 passed`，完整 `tests.test_server` `224/224 passed`；R004 同范围 server/frontend 非构建回归 `443 passed`。Python/Node 语法、loopback 端点、H4 discovery 与 `git diff --check` 通过。
- 用户普通终端完成 fresh frontend `build:frontend` / `verify:frontend`，bundle/direct classic `CJK bare URL` H4 `2 passed`；开发沙箱中的 esbuild 目录权限与 Chromium `spawn EPERM` 仅作为环境排除，未计为通过。
- 审批只读真实探针确认百度 `/favicon.ico` 返回合法 `image/x-icon`；修复精确 `Content-Length + Connection: close` 的多读误判后，用户在 Code Dev/direct classic 对扩展网站矩阵复核通过：合法站点显示真实图标，无效/失败站点稳定降级为地球图标，刷新与链接交互保持。

#### 兼容与有限边界

- 本阶段没有 HTML icon discovery、第三方并发竞速、磁盘/业务数据写入、Session/消息 JSONL、配置 schema、协议或线上配置变化；服务器只处理页面已出现外链的 host，代理本身不授予其他网络或数据权限。
- `CODE-036` 的中文标点裸链接边界与 favicon 安全代理、缓存、降级、双入口视觉/交互均已完成；事项从 TODO 删除，Code 最后分配编号游标保持 `CODE-038`，未自动启动下一事项。

## 2026-08-24 10:05 · Codex 开发会话

### CODE-038 · 会话列表相对时间与状态槽、Goal 会话切换隔离（R001-R011，用户 PASS）

#### 完成内容

- 会话标题右侧增加固定宽度状态槽，状态优先级为 inactive 会话的“等待回答 `?` → 等待确认 `!` → 运行环 → 未读圆点 → 空闲相对时间”。当前会话的问卷/授权仍由既有面板唯一提示，状态槽回落到运行、未读或时间，避免重复提示；中英文 `title` / `aria-label`、浅深主题与 `prefers-reduced-motion` 降级保持一致。
- 空闲时间只消费 `CODE-037` 已规范化的 `lastMessageTime`，缺失时回退 `updatedAt` / `createdAt`，未来值钳制为“刚刚 / now”；分钟 ticker 仅原地更新 idle 槽位，不整表重绘、不请求网络、不写持久化，也不改变会话排序。
- 异会话切换在通过既有 300ms 用户防抖后立即隐藏旧 Goal 面板，并以会话身份和请求序列丢弃迟到响应；目标加载成功后才投影目标 Goal，失败则恢复原会话正确面板。同会话点击和被防抖拒绝的切换不清空面板。
- 运行环保持 11px、2px 主题色边框和透明缺口，周期由 0.8 秒调整为 1.4 秒。状态槽同 kind 更新改为原地 patch；后台会话仍在 streaming 时只标记未读并局部刷新槽位，终态仍完整重绘列表，因此流式增量不会重建 indicator 或重启动画，最终未读、时间和排序仍同步。
- 编辑授权 H4 的路径身份契约统一兼容增强前 `data-path` 按钮与增强后 `title` 文件卡，并继续精确断言 waiting/terminal、Diff、刷新和 exactly-once；该修订未改变产品授权协议或写入次数。

#### 验证与验收

- 前端模块最终 `222/222 passed`；Node/Python 语法、前端构建 freshness、H4 discovery 与 `git diff --check` 通过。
- 用户普通终端在 fresh bundle 上运行 bundle/direct classic `session status slot` H4：`2 passed`；覆盖 active/inactive 运行态、跨两个流式 gate 的同一 indicator 身份、终态未读、返回 idle、一次模型请求、零工具与精确网络边界。
- 用户确认 Code Dev / direct classic 的相对时间、状态优先级、inactive-only 问卷/授权提示、Goal 切换隔离、1.4 秒连续运行环及桌面/移动、浅深主题观感人工验收 PASS。

#### 兼容与有限边界

- 本阶段未增加服务端、Session 索引、消息 JSONL、Goal/AgentRun 事件或持久化字段；会话排序和最后消息时间事实仍由 `CODE-037` 的服务端权威保存链负责。
- 状态槽只投影现有 streaming、未读、问卷与授权事实，不推断排队、网络、凭据或其他生命周期；截图中新发现的 Markdown 链接异常不属于本提交，未顺带处理或登记新事项。

## 2026-08-24 11:08 · Codex 开发会话

### CODE-036 · 中文标点相邻裸链接解析（R001-R003，用户 PASS）

#### 完成内容

- 在 `marked` 解析前增加有界普通文本投影：仅当 ASCII HTTP(S) 裸 URL 紧邻明确的 Unicode 标点时转换为标准 angle autolink，使 `yuanbao.tencent.com`、`xinghuo.xfyun.cn`、`mistral.ai` 在全角括号、顿号和句号之间分别生成独立链接；标点与说明文字仍由 Markdown 正常解析。
- 投影器以代码区和显式 Markdown 结构为边界，行内/围栏代码、标准链接、图片、既有 angle autolink、原始 HTML、ASCII query/fragment 和百分号编码保持不变；Unicode 边界从文字脚本收窄为 General Category `Punctuation`，合法 `/中文`、`/한글`、`/かな` 路径及无标点正文不被截断或猜测拆分。
- 外链 renderer 的 `target="_blank"`、`rel="noopener"`、域名别名、完整 URL tooltip、右键复制和既有 favicon/glyph 链保持不变；H4 只把已知 CDN 静态资源及精确域名/父域 favicon 候选列为受控 non-loopback GET，未修改产品 favicon 行为。

#### 验证与验收

- 确定性 Markdown 定向回归 `3/3 passed`；本地可用 `marked 17.0.5` 真实解析证明原复现句生成三个精确 href，Unicode 路径投影前后保持原字节。Node/Python 语法、H4 discovery 与 `git diff --check` 通过。
- 开发受限环境完整前端模块为 `223/224`，唯一阻断是 `esbuild` 无权遍历沙箱临时路径；该环境阻断未记作通过，也未通过放宽测试掩盖。
- 用户普通终端在 fresh bundle 上运行 bundle/direct classic `CJK bare URL` H4：`2 passed`；已覆盖独立 anchor 数量/href、中文标点、tooltip、右键复制、零模型/工具请求及显式网络白名单。合法 Unicode 路径边界由前述确定性测试覆盖；用户同时确认 Code Dev / direct classic 人工验收 PASS。

#### 兼容与有限边界

- 本阶段未修改 `marked` CDN 版本、favicon 获取实现、服务端、样式、Session/消息持久化、TODO 编号游标或线上配置；无标点中文正文中的 URL 歧义继续交给 `marked` 既有行为，不在本阶段猜测切分。
- `CODE-036` 只完成中文标点相邻裸链接解析阶段，整项仍保留为未完成；下一动作是另行实施已确认的 favicon 安全同源解析/代理，未在本提交中启动。
