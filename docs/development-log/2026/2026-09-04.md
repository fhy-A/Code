# 2026-09-04 开发日志

## 2026-09-04 10:37 · Codex

### v0.6.8 发布门限阻断修复收口

- 对发布候选基线上的完整 Python 门禁进行逐组归因：原先的 `13 failed + 5 errors` 共 18 个红项均来自已经落后于当前实现的测试结构断言、VM 夹具能力或冻结证据派生值，不是产品运行时回归。修复仅更新测试 / 夹具合同：导入检查改为 AST 语义、前台恢复签名对齐当前 options 形态、terminal clear / subagent checkpoint 对齐 CODE-059 B2 的 CAS 回滚并继续抛错语义、项目布局补入 `data_dir_owner`，manual compaction VM 补齐被动 DOM 能力；没有修改产品代码或产品行为。
- D2 / D3 冻结证据只随 CODE-043 已提交的可访问性代码更新源码切片长度与派生哈希：D2 / D3 去除允许变化字段后的语义对象逐项等同于基线，D3 的 19 个 `scenarioHashes`、失败 / 恢复 / 副作用矩阵和全部 mutation 首差异路径保持不变。直接定向验证为 `13 passed + 38 subtests`，默认 harness replay 为 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`。
- 原 canonical `pytest_full` 共享超时为 360 秒；中间拟调至 600 秒后，健康全量套件仍在该边界外以 `2017 passed, 1 skipped, 1270 subtests` 于 633.20 秒完成，因此最终从提交基线的 360 秒调整为 1080 秒，按该实测提供约 1.7 倍有界余量且不改变 H4 的独立 600 秒合同；release script / workflow / verify profile 契约 `63 passed + 243 subtests`。随后真实 `python verify.py release` 全链通过：pytest 同一计数于 540.70 秒完成，profile 总耗时 561.353 秒，frontend freshness / bundle syntax、harness replay、diff check 与五项 Python syntax 均通过；显式 frontend build / freshness / bundle syntax 也再次通过。
- 本阶段只闭合本地发布门限与阻断测试，没有执行 `release.py --prepare`、publish、push、tag、EXE 构建或线上配置；`VERSION` 仍为 `0.6.7`，未跟踪的 `docs/releases/v0.6.8.md` 草稿保持原字节与 SHA-256 `8c96f5ea44ef9114ff4797027174e3e557d2affbc29a41e35ba8a4a44d53ea64`。受保护的 `data/generated-assets/` 与 `data/image-route-registry.json` 未读取、哈希、修改或暂存。

## 2026-09-04 03:21 · Codex

### CODE-043 · Code Web 视觉系统与组件尺度优化最终收口

- 最终有界批次完成 Theme picker 与应用内 Toast 收口：Theme picker 复用既有主题 token，统一 `18 / 14 / 12px` 信息层级、`40 / 32px` 控件尺度、卡片层级、选中态与键盘焦点，`390px` 下模式控件改为单列；原主题 ID、默认值、系统跟随解析和存储格式均未改变。Toast 保持原触发条件、严重级别、`1000～15000ms` 时长夹取、业务文案和淡出移除时序，新增 `alert / status`、`aria-live / aria-atomic`，并统一 `14px`、长文案换行、窄屏安全宽度和多 Toast 间距。
- 三个仍可到达且存在明确缺口的辅助弹层完成必要优化：新建文件夹移除内联固定宽度并将普通“创建”恢复为主操作视觉；压缩确认使用固定头尾与内部滚动；Key 同步在桌面保持三列、`390px` 收敛为名称 / 操作与 Key 两行布局。三者均补齐可访问名称、dialog 语义、Escape、初始焦点和关闭后焦点归还；其中新建文件夹 / 压缩确认修复 `visibility` 过渡导致显示初期不可聚焦，Key 同步显式保留请求前触发按钮，避免按钮临时禁用后焦点丢失。文件创建、压缩、Key 同步 / 复制及持久化业务语义均未改变。
- 只读盘点确认 platform-auth gate 已具备 dialog 语义、响应式宽度和一致层级，本批不修改；浏览器 / 系统原生 Notification 的视觉由操作系统控制，本批仅确认现有“API 存在且权限已授予才触发、异常降级为空”的合同，不修改权限请求、触发条件或系统通知语义。预览继续唯一归属 `CODE-042`，迁移 / 归档及既有已验收设置、消息和主界面表面未重复实施；至此 `CODE-043` 四阶段完成定义闭合，后续零散视觉问题不再回填本长期事项。
- 验证按真实失败边界留痕：初始单体 H4 先后因隔离宿主在附件目录尚未创建时采集 metrics 崩溃、以及鼠标交互后程序化 `focus()` 不满足 `:focus-visible` 的错误夹具假设而两轮停止，按熔断撤回且未记作产品失败；夹具只增加空附件目录证据降级，并把焦点验证改为真实 `Tab`。重新设计的单体链随后分别暴露并修复显示过渡焦点和 Key 同步触发按钮焦点两个真实缺口，再次达到单体路径熔断后未做第三次原样复跑，而是拆为互相独立的 `code043-final-core-selfcheck-v3` 与 `code043-final-key-sync-selfcheck-v3`。最终两条链均在 bundle / direct classic、light / dark、桌面 / `390px` 下通过，覆盖长文案、多 Toast、18 行长 Key 列表、选中 / 焦点态、可滚动弹层、Escape、可访问名称、零横向溢出、零业务写入及子进程 / 端口 / 临时根完整清理。
- 最终差量上 `python verify.py doctor` `10/10`、直接相关契约 / 行为测试、完整 `python -m unittest tests.test_frontend_modules` `453 tests`、frontend build / freshness / bundle syntax、`python verify.py quick` `6/6` 与 `git diff --check` 均通过。用户在实际 `http://localhost:3011/` 完成最终主观验收并明确回复 `PASS`；`CODE-043` 已从私有未完成清单移除，`CODE-058` 仅清理指向已完成事项的 related 链接，编号游标保持 `CODE-073`。本阶段未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未执行 push、tag、release 或线上配置。

## 2026-09-04 02:03 · Codex

### CODE-073 · 移除设置页模型 API Key 拖拽排序

- 设置页模型 API Key 行移除独立点阵、`draggable` 标记、`dragstart` / `dragover` / `dragleave` / `drop` / `dragend` 监听、拖放状态样式及 `dragSort` 中英文文案；行布局从三列收敛为“主内容 + 操作区”两列并释放左侧占位。查看、启停、编辑、添加、同步、删除与模型刷新入口均保留。
- `code-key-config` 继续按原数组 / DOM 顺序加载、收集与保存，不增加迁移、排序或规范化副作用；合成配置补充 workbar 同步顺序断言，既有两连接路由刷新 payload 顺序合同继续通过。默认 routing v2 的 `connectionId + modelId` 精确路由、Route Registry、服务端连接收集与旧 `CODE_ROUTING_V2=0` 回退均未修改。
- fail-first 合同先稳定命中旧 `key-drag-handle`，实现后直接相关 `6 tests` 与完整 `python -m unittest tests.test_frontend_modules` `451 tests` 通过；`npm run check:frontend`、最终 frontend freshness / bundle syntax、`python verify.py quick` `6/6` 和 `git diff --check` 通过。源文件与 bundle 均无 Key 点阵 / `dragSort`，direct classic 继续加载同一 `i18n.js` / `settings.js`。
- 新增的双入口隔离浏览器场景首轮把视觉隐藏 checkbox 错纳入可见矩形，第二轮又使用 Playwright `uncheck` 操作该隐藏原生控件；bundle / direct classic 均在同一夹具边界失败。按 H4 熔断规则未做第三次复跑，新增 helper / tests 已完整撤回，不把该场景记作产品通过或失败；失败时 `pageErrors=0`，第二轮端口、子进程、浏览器资源与临时根完整清理。
- 用户在实际 `http://localhost:3011/` 设置页完成最终主观验收并明确回复 `PASS`。本阶段未读取、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未执行 push、tag、release 或线上配置；`CODE-073` 已从私有未完成清单移除，其他事项未启动。

## 2026-09-04 01:31 · Codex

### CODE-071 Stage B2 · 桌面会话拖拽快捷入口闭环

- 桌面会话列表移除独立点阵手柄，改为会话主体整行可拖；更多操作、重命名输入等动作控件不参与拖拽，普通点击继续打开会话，键盘与触摸场景仍可使用既有项目迁移菜单。拖拽只向 `DataTransfer` 写入固定非敏感哨兵，Session 身份保留在页面内存；紧凑标题预览跟随指针，源行以透明度和轻量抬起反馈当前状态。
- 落点统一为可见的完整 `project-block`，包含项目标题、展开的子会话行与块内留白；临时“无项目”目标沿用同一语义。同项目为中性 no-op，无效目标弱化且不提交。指针在区块内直接命中；仅在会话列表水平范围内，对相邻区块垂直间隙提供最近边缘最多 `8px` 的稳定磁吸，等距按 DOM 顺序选择。高亮、`dragover`、自动滚动重算与最终 `drop` 共用同一解析结果。
- 指针靠近会话列表上下边缘时，仅该列表执行有界连续滚动，并在每帧按新矩形重算目标；到达边界、成功、失败、drop、dragend、Escape、窗口失焦或列表重绘时都会停止滚动并清除 ghost、临时目标与状态类。最终迁移继续只进入 Stage B1 的服务端权威预览、确认、revision / token 与原子提交管线，没有新增前端迁移判断或改变 Session、项目、文件树和既有 AgentRun 工作区语义。
- 可迁移项目使用主题适配底色与 `1px` 内描边，当前指针命中的可迁移项目提升为更强底色、`2px` 内描边和轻阴影；强调覆盖完整项目块，不改变尺寸或密度。no-op 保持中性，invalid 保持弱化，深浅主题均不使用错误红色，reduced-motion 合同与动画时序未改变。
- 自动证据按失败熔断保留真实边界：R003 完整前端 `448 tests`、项目 / Session 持久化 `180 tests` 与 quick `6/6` 通过；R004 最终完整前端 `449 tests`、direct classic 拖拽全链、build / freshness 与 quick `6/6` 通过，bundle 的新增指针命中矩阵在后续测试夹具断言前完成；R005 相关侧栏 `45 tests`、build / freshness 与 quick `6/6` 通过，并以同一隔离 H4 的聚焦视觉模式分别验证浅色 bundle 与深色 direct classic 的两级底色 / 描边、整块范围、零布局位移、no-op 区分和无横向溢出，`pageErrors=0`，子进程、端口与临时根均清理。未把因夹具熔断拆分的证据记作单次完整双入口 H4 全绿。
- 用户在实际 `http://localhost:3011/` 界面完成最终主观验收并明确回复 `PASS`。本阶段未读取、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未触碰无关活动 handoff，未执行 push、tag、release 或线上配置。`CODE-071` 仍保留为未完成事项，下一独立阶段仅为已批准的“每轮工作区权威提示”；`CODE-072` 与其他事项未启动。
