# 2026-09-04 开发日志

## 2026-09-04 23:40 · Codex

### CODE-074 Stage 2B1 · `skillLifecycle/v1` 核心与兼容适配

- AgentRun 外层继续使用 v5，在同一记录内新增由服务端独占构造、独立版本化的 `skillLifecycle/v1` 子合同。`activation` 冻结 explicit / automatic 意图类别、activated / none 结果、最多一个 owner 加一个 modifier，以及 installed 相对来源、Descriptor、Skill 正文、evidence、有效依赖 manifest 和资源合同的必要哈希 / 能力身份；`access/v1` 本阶段只保留空绑定容器，不提前改变工具行为。客户端提交的同名字段不受信任且不会进入记录；用户原文、disabled 明细、绝对路径、正文、完整 Registry、reference 内容和 diagnostics 均不持久化。无效 evidence 只保留 `invalid` 状态和内容哈希，不保存任意 sidecar 正文。
- 新 canonical Run 的 `activeSkillNames`、`activeSkillDependencies` 与 `skillEvidence` 由生命周期激活事实确定性投影，并与 lifecycle 在 `created` 事件持久化、worker 启动前写入同一 v5 记录；同一 `clientRequestId` 的并发或重启重试不重新读取 Skill。恢复只使用冻结记录，未知 lifecycle / activation / access 版本、畸形或部分结构、非 v5 / 非前台根 Run，以及 lifecycle 与任何兼容投影冲突均 fail-closed；运行中命令的既有重启归因仍可安全更新动态 evidence 结果。无 lifecycle 的旧 v1～v5 记录仅生成内存 legacy view，不写回、不迁移；flag-off、legacy、background、detached、child / subagent、公开 snapshot、Session、权限、工具结果与 runtime binding 语义保持不变。
- 生产范围严格为新建 `code_runtime/skill_lifecycle.py`、`code_runtime/skill_activation.py` 和 `server.py` 三个文件，净增恰好 `600` 行；Stage 2B1 直接测试净增恰好 `850` 行，覆盖 activated / none、owner / modifier、旧 v1～v5、未知 / 畸形 / 部分结构、投影冲突、无敏感载荷、同请求并发、持久化先于 worker、重启恢复、运行中工具恢复、sidecar 漂移、flag-off / legacy 字节与公开 snapshot。直接 lifecycle / activation 为 `37 passed`；AgentRuntime `137 passed`；server `273 passed, 1 skipped`；Skill Registry / dependency / resource `71 passed`；runtime binding / legacy recovery `19 passed`。
- 首次完整 `python verify.py runtime` 在 pytest 暴露两个既有测试合同 / 隔离问题及一次高负载 PPT 等待超时后停止；PPT 原用例隔离复验 `1 passed`。R006 仅在 `tests/test_project_layout.py` 与 `tests/test_skill_registry.py` 净增 `24` 行：布局清单补齐已存在的三个 Skill runtime 模块，bundled 语料和 Node cwd 改用测试文件解析出的仓库不变根，并以 decoy globals 回归证明不再受 `tests/test_routes.py` 遗留 globals 污染；定向结果 `4 passed + 7 subtests`，未修改 routes 生命周期或任何超时。
- 唯一最终 runtime 重跑中，frontend freshness / bundle syntax 通过，完整 pytest 为 `2087 passed, 1 skipped, 1347 subtests`（631.39 秒），Harness replay 为 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`，哈希保持 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；H4 infrastructure self-check 通过，但完整 `164 tests / 1 file / 1 worker` smoke 超过既有 `600s` 门限并以 timeout / exit 124 停止，因此不宣称 `verify.py runtime` 通过，也未二次重跑、放宽超时、跳过用例或降低覆盖。随后按审批有界例外仅运行一次 CODE-074 专项 H4：bundle / direct classic × canonical / legacy 四链全部 completed，canonical 均单次 create、前端完整 Skill 读取为 0，legacy 各读取 1 次，两代 child、双端口、临时根和 active child 全部清零；最终 `verify.py quick` `6/6`。完整 H4 容量与分段策略已独立登记为 `CODE-075`。
- `CODE_SKILL_ACTIVATION_V1` 继续默认关闭；Stage 2B2 尚未实施，下一阶段只可在新的 STRICT 门禁下落实已确认的 executable resource activation-pinned、普通文本 reference first-read-pinned，以及“关 flag 后排空活动 lifecycle Run”的降级语义。本阶段未运行真实模型、Office、外网、安装、push、tag、release 或线上配置，未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`。

## 2026-09-04 19:56 · Codex

### CODE-074 Stage 2A2 · 前端 capability 协商与权威 Skill 激活接入

- `app.js` 现在只在 heartbeat 精确公布 `skillActivationProtocol=canonical-v1` 时，为新前台 AgentRun 冻结 canonical 模式；请求 system prompt 在原 Skill 位置放置唯一激活 marker，并在初始工具包含 `task` 时以唯一 begin / end marker 包住既有委派规则。canonical 路径不再运行前端 Skill matcher、正文读取或旧工具预算，只发送 `schemaVersion / explicitSkill / disabledNames` 意图和版本化 `allowedTools.names` 上限；capability 缺失、未知、heartbeat 失败、flag-off、background、detached `/parallel`、child / subagent、已有 AgentRun 与恢复路径继续保持 legacy。
- `agent-runtime.js` 保留数组型 legacy `allowedTools` 和既有 `activeSkillName(s)` 请求字节，只在显式 canonical 请求存在时透传 intent-only envelope 并省略客户端预测身份。创建响应的 `activeSkillNames` 必须是最多两项、唯一且合法的规范数组；前端先以它替换预测状态并同步原始用户消息，再写入首个 running checkpoint，后续 watch snapshot 在同一 canonical Run 内继续按该严格合同保留最长 128 字符的权威名称，legacy snapshot 仍保持原 80 字符显示归一化。缺失、重复或非法响应，以及 `activation_protocol_disabled / unsupported` 等服务端拒绝均沿原错误链 fail-closed；即使请求含图片也不触发去图重试，不会重发 legacy 或创建第二个 Run。
- 新增单一专用 H4 自检并只为既有隔离宿主增加一个 opt-in 环境参数。最新 bundle 与 direct classic 在 flag-on 下均验证 heartbeat → 单次 create → 服务端消费唯一 marker / 注入选中正文 → 权威 `activeSkillNames` → checkpoint 的真实链路，且前端完整 Skill 正文读取为 `0`；同两入口 flag-off 对照均保持 legacy 请求并各读取正文 `1` 次。四条运行均 completed，外部静态 CDN 请求被网络围栏阻断，子进程退出、端口关闭、临时根删除与 active child 归零全部通过。
- 最终生产范围仅 `app.js +113/-23` 与 `agent-runtime.js +7/-2`，净增 `95` 行，未修改 `server.py`、`code_runtime/skill_activation.py`、AgentRun / Session schema、默认开关、权限、资源、依赖、runtime binding 或 evidence 语义。验证为 frontend `457 passed`、AgentRuntime `137 passed`、Skill / Registry / 依赖 / runtime binding / 资源组合 `131 passed`、route / model-route / server `354 passed, 1 skipped`（既有 Windows reparse 权限），合计 `1079 passed, 1 skipped`；frontend build / freshness / bundle syntax、专用 H4、H4 infrastructure self-check、`verify.py quick` `6/6` 与 diff hygiene 均通过。
- `CODE_SKILL_ACTIVATION_V1` 继续默认关闭，Stage 2A2 完成不等于自动启用生产切换；Stage 2B、Stage 3、`CODE-063` 及其他资源 / 依赖 / evidence 治理均需新的独立确认。本阶段未运行真实模型、Office、外网、安装、push、tag、release 或线上配置，未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`。

## 2026-09-04 18:13 · Codex

### CODE-074 Stage 2A1 · 默认关闭的服务端权威 Skill 激活核心

- 新增独立 `code_runtime/skill_activation.py`，把 Stage 1 的只读 Registry / resolver 接入一条仅供版本化直接 API 使用的 canonical 激活链：请求只接受 `schemaVersion / explicitSkill / disabledNames` 意图与独立的版本化 `allowedTools` 工具上限，不接受客户端提供 owner、正文或哈希。服务端在单次准入内完成有界 Registry、唯一 owner、可选 modifier、最多两项 metadata-only reference、选中包普通文件 / reparse / 大小 / 哈希复核、正文与输入预算、依赖能力和 evidence 捕获；owner 异常 fail-closed，modifier 超限或损坏只按稳定原因排除，未选正文不进入最终 prompt、AgentRun 或持久记录。
- 工具集合先受服务端 permission profile 约束，再按 Skill `allowed-tools` 逐项取交集；legacy `tools` 仍只表达偏好，`task` 只有 Skill 声明或用户明确要求委派时才保留且永不扩权。canonical 占位符与委派包络必须各自唯一、位于首个 system message，服务端按最终工具集合替换、解包或移除；畸形意图、工具包络、缺失 / 重复 marker、Registry 不可用、名称冲突、选中内容竞态或预算超限均在 Run / worker 创建前拒绝，不回退到 legacy。
- 同一 `clientRequestId` 在任何 Skill / Registry 可变读取前进入 64 槽有界准入锁；首个请求持锁直至 `created` 事件和 AgentRun v5 已持久化、worker 才可启动，并发或进程内缓存清空后的重试直接恢复同一 Run，不再次解析 Skill。最终 system prompt bytes、`activeSkillNames`、content hash evidence observer、依赖能力与工具预算继续使用既有 AgentRun v5 字段持久化和恢复，没有新增 receipt、Session schema、资源 / 依赖 / runtime binding / evidence 协议或迁移。
- 新能力由 `CODE_SKILL_ACTIVATION_V1` 显式开启，默认关闭；关闭时 heartbeat 响应不增加 capability 字段，canonical 请求明确拒绝，旧请求、旧 Run、background / subagent 与现有浏览器客户端继续原 legacy 路径。Stage 2A1 未修改 `app.js` 或 `agent-runtime.js`，因此即使服务端手工开启开关，当前客户端仍不会发送 canonical 意图、工具包络或 prompt marker；端到端协商属于待独立确认的 Stage 2A2。
- 范围门禁最终为两个生产文件：`server.py +181/-19`、新模块 `+538`，合计 `+719/-19`、净增 `700`，未超过批准上限；新增单一直接测试文件 `708` 行。确定性验证覆盖 explicit / implicit / no-match / disabled / 冲突 / 损坏 / owner-modifier-reference、未选正文隔离、marker、预算、工具与 task 只缩不扩、flag / HTTP additive contract、并发单解析 / 单持久 / 单 worker、v5 evidence / dependency 冻结及缓存清空恢复不重读。最终 Skill / 依赖 / runtime binding / 资源组合 `103 passed`，AgentRuntime `137 passed`，route / model-route / server `354 passed, 1 skipped`（仅本机 Windows reparse 权限），合计 `594 passed, 1 skipped`；`python verify.py quick` `6/6`，本任务同一环境 doctor `10/10`，diff / Python syntax 均通过。
- 本阶段未运行真实模型、Office、外网、安装、H4、push、tag、release 或线上配置；未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`。Stage 2A2 未启动，AgentRun 仍为 v5，CODE-063 / 064 / 068 与其他事项边界不变。

## 2026-09-04 16:18 · Codex

### CODE-074 Stage 1 R002 · Agent Skills 标准与真实生产基准修正

- 依据 [Agent Skills 官方规范](https://agentskills.io/specification) 修正 R001 把“可由 YAML 解析”近似成“标准合规”的错误：Descriptor 现在稳定区分 `agent-skills-standard / code-legacy / invalid`。标准 `name` 限 1～64 位小写字母、数字和单连字符且必须与目录同名，`description` 限 1～1024 字符，`compatibility` 必须为 1～500 字符字符串，`metadata` 必须为字符串到字符串映射，版本只从 `metadata.version` 取得；当前 Code 的顶层 `keywords / tools / version`、逗号式或 YAML list `allowed-tools`、嵌套 metadata、map compatibility 和未知顶层字段均继续可观察，但明确标为 `code-legacy` 而非标准。当前 31 个 bundled Skill 全部属于可执行的 Code legacy，未批量改写正文。
- 标准 `allowed-tools: Bash(git:*) Bash(jq:*) Read` 以空格切分并原样保留 token；workbar 不把 `Read` 或 `Bash(...)` 猜映射为 `read_file / run_command`，只有 token 与当前 permission profile 工具名逐字相等时才可参与交集窄化。Code 既有逗号字符串 / YAML list 作为显式 legacy adapter，顶层 / metadata `tools` 仍只表达偏好；官方标准、边界长度、连续连字符、目录不一致、legacy shape、无效 YAML 和不可读输入均有精确 fixture 与稳定 reason code。
- 删除只读审计入口中以 `server.match_skills` 冒充生产基准的比较。`POST /api/skills/shadow-resolution` 现在明确返回 `productionObservation.available=false / frontend_observation_required`，仍默认关闭且不进入生产链。生产差异证据改为测试中直接调用既有 `src/features/skills-memory.js` 导出的 `rankMatchedSkills` 和真实 `getSkillPromptSnapshot`：锁定 explicit-only 排除、disabled、最高分并列、最多 3 项、正文加载后的 body-length 排序、prompt 名称与 `activeSkillNames` 同一性，并对当前可读 bundled 样本生成 production-vs-shadow 精确矩阵。数学公式、XLSX / PDF / Word / PPT 和 code-review 的差异结论保持，但其生产侧顺序现在来自 JS 实测而非 Python 镜像。
- 复杂度复核后保留单文件实现：R001 的 `982` 行增至 `1118` 行，净增仅用于官方字段分类、标准 / legacy allowed-tools 解析及外部观察比较重命名；服务端反而移除了 Python mirror comparison。现有体量按职责约为安全 YAML 与标准格式 `327` 行、Descriptor / sidecar / health `364` 行、来源 Registry 与工具窄化 `197` 行、shadow resolver / comparison `230` 行；28 个顶层定义均服务于这四个已批准职责，没有 AgentRun、激活、持久化、迁移或 UI 预实现。继续拆文件只会分散同一不可变 snapshot 合同，故本轮不作机械行数压缩。
- R002 聚焦验证：`tests/test_skill_registry.py` 最终 `28 passed + 9 subtests`；与既有真实前端 matcher / prompt snapshot、Skill server 定向、builtin 与 bundled sync 合并为 `56 passed + 198 subtests`；`python verify.py quick` `6/6` 与 `git diff --check` 通过。未重复无关 LibreOffice、AgentRuntime 扩大组或 H4；未修改前端生产源码、Skill 正文 / 资源、EXE / 安装合同、API 既有语义、AgentRun / Session、工具、依赖、resource、evidence 或 UI。
- 本轮未访问 `claude-skills`，未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未运行真实模型、外网、依赖安装、push、tag 或 release。`CODE-074` 仍停在 Stage 1 复核，Stage 2 未授权。

## 2026-09-04 15:42 · Codex

### CODE-074 Stage 1 · Skill Descriptor / Registry / Resolver 影子基础层

- 新增隔离的 `code_runtime/skill_registry.py`：使用现有开发门禁已要求的 PyYAML 安全解析真实 YAML frontmatter，与 Memory 的简化 parser 完全分离；规范化 `name / description / license / compatibility / metadata / allowed-tools`，将既有 `keywords / tools` 明确标记为 legacy adapter。`allowed-tools` 只可与 permission profile 取交集，legacy `tools` 只表达偏好，均不能扩权。描述符不包含正文或绝对路径，只冻结来源、名称、可选版本、正文 / 内容哈希、三份既有 sidecar 的只读摘要、健康状态与稳定 reason code。
- Registry Snapshot 同时保留 installed 与 bundled 的独立身份，不合并同名不同来源；可区分 shared-development、当前 bundled 副本、custom、legacy-ambiguous、tombstone、bundled 未安装、无效 / 不可读 / unsafe、空正文及 sidecar 身份冲突。既有 sidecar 不迁移、不借用，未知旧副本不猜来源；目录枚举、重启和绝对路径迁移保持确定性 snapshot/hash。
- resolver v2 只产生影子 `owner / modifier / reference / action`、排除项和稳定诊断。办公格式默认单一专家 owner，`office-files` 仅作读取 reference，`document-design` 只在明确视觉意图时作为 modifier；显式 Skill 精确优先，disabled、冲突、无效和 legacy-ambiguous 不成为可执行候选。代表语料精确证明：数学公式讲解不再误选 xlsx；自然 XLSX 审计为 xlsx owner 且 office-files 不竞争 owner；XLSX 视觉任务为 xlsx owner + document-design modifier；PDF、Word、PPT 与普通 code-review 的 legacy-vs-shadow 差异均可复核，额外候选会使测试失败。
- 新增默认关闭的 `CODE_SKILL_REGISTRY_SHADOW` 与只读 `POST /api/skills/shadow-resolution` 审计入口；请求正文不进入 URL，响应仅保留消息哈希 / 长度。关闭时该路径维持原 404，现有 `rankMatchedSkills`、`match_skills`、system prompt、`activeSkillNames`、AgentRun / Session、工具、依赖、资源、evidence 和 UI 均不读取影子结果；调用点审计确认新 Registry 只可由该 opt-in 入口或测试到达。未改变 EXE / 安装合同，未新增外部依赖。
- 自动证据：doctor `10/10`；新增矩阵最终 `24 passed + 9 subtests`，Registry / bundled sync / ppt-master 静态组合 `39 passed + 9 subtests`；server `272 passed, 1 skipped + 50 subtests`；旧 AgentRun / 协议恢复 `16 passed + 178 subtests`；Skill / 依赖 / 资源扩大组除既有 LibreOffice 渲染一次 60 秒超时外为 `99 passed + 210 subtests`，该测试进程已退出且精确临时根已清理。AgentRuntime 扩大组在主机负载下仅有既有 `0.12s` 首响应超时断言分别耗时 `0.781s / 0.578s` 超出 `0.5s`，其余最终 `136 passed + 81 subtests`；该失败用例两次隔离复验均通过，未修改无关运行时或测试阈值。`python verify.py quick` `6/6` 与 `git diff --check` 通过，无需 H4 / 用户视觉验收。
- 本阶段未运行真实模型、外网、依赖安装或持久迁移，未修改任何 Skill 正文 / 资源，未进入 CODE-054 / 063 / 064 / 068 或 CODE-058 实施。受保护的 `data/generated-assets/` 与 `data/image-route-registry.json` 未读取、哈希、修改或暂存。一次范围过宽的依赖文件名枚举只显示了 `claude-skills` 下两个 `requirements.txt` 路径，未读取内容、未哈希且未作为证据；随后所有检查均限定到批准目录。`CODE-074` 保留为五阶段总事项，Stage 2 仍需新的独立 STRICT relay。

## 2026-09-04 10:37 · Codex

### v0.6.8 发布门限阻断修复收口

- 对发布候选基线上的完整 Python 门禁进行逐组归因：原先的 `13 failed + 5 errors` 共 18 个红项均来自已经落后于当前实现的测试结构断言、VM 夹具能力或冻结证据派生值，不是产品运行时回归。修复仅更新测试 / 夹具合同：导入检查改为 AST 语义、前台恢复签名对齐当前 options 形态、terminal clear / subagent checkpoint 对齐 CODE-059 B2 的 CAS 回滚并继续抛错语义、项目布局补入 `data_dir_owner`，manual compaction VM 补齐被动 DOM 能力；没有修改产品代码或产品行为。
- D2 / D3 冻结证据只随 CODE-043 已提交的可访问性代码更新源码切片长度与派生哈希：D2 / D3 去除允许变化字段后的语义对象逐项等同于基线，D3 的 19 个 `scenarioHashes`、失败 / 恢复 / 副作用矩阵和全部 mutation 首差异路径保持不变。直接定向验证为 `13 passed + 38 subtests`，默认 harness replay 为 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`。
- 原 canonical `pytest_full` 共享超时为 360 秒；中间拟调至 600 秒后，健康全量套件仍在该边界外以 `2017 passed, 1 skipped, 1270 subtests` 于 633.20 秒完成，因此最终从提交基线的 360 秒调整为 1080 秒，按该实测提供约 1.7 倍有界余量且不改变 H4 的独立 600 秒合同；release script / workflow / verify profile 契约 `63 passed + 243 subtests`。随后真实 `python verify.py release` 全链通过：pytest 同一计数于 540.70 秒完成，profile 总耗时 561.353 秒，frontend freshness / bundle syntax、harness replay、diff check 与五项 Python syntax 均通过；显式 frontend build / freshness / bundle syntax 也再次通过。
- 本阶段只闭合本地发布门限与阻断测试，没有执行 `release.py --prepare`、publish、push、tag、EXE 构建或线上配置；`VERSION` 仍为 `0.6.7`，未跟踪的 `docs/releases/v0.6.8.md` 草稿保持原字节与 SHA-256 `8c96f5ea44ef9114ff4797027174e3e557d2affbc29a41e35ba8a4a44d53ea64`。受保护的 `data/generated-assets/` 与 `data/image-route-registry.json` 未读取、哈希、修改或暂存。

## 2026-09-04 03:21 · Codex

### CODE-043 · Code Web 视觉系统与组件尺度优化最终收口

- 最终有界批次完成 Theme picker 与应用内 Toast 收口：Theme picker 复用既有主题 token，统一 `18 / 14 / 12px` 信息层级、`40 / 32px` 控件尺度、卡片层级、选中态与键盘焦点，`390px` 下模式控件改为单列；原主题 ID、默认值、系统跟随解析和存储格式均未改变。Toast 保持原触发条件、严重级别、`1000～15000ms` 时长夹取、业务文案和淡出移除时序，新增 `alert / status`、`aria-live / aria-atomic`，并统一 `14px`、长文案换行、窄屏安全宽度和多 Toast 间距。
- 三个仍可到达且存在明确缺口的辅助弹层完成必要优化：新建文件夹移除内联固定宽度并将普通“创建”恢复为主操作视觉；压缩确认使用固定头尾与内部滚动；Key 同步在桌面保持三列、`390px` 收敛为名称 / 操作与 Key 两行布局。三者均补齐可访问名称、dialog 语义、Escape、初始焦点和关闭后焦点归还；其中新建文件夹 / 压缩确认修复 `visibility` 过渡导致显示初期不可聚焦，Key 同步显式保留请求前触发按钮，避免按钮临时禁用后焦点丢失。文件创建、压缩、Key 同步 / 复制及持久化业务语义均未改变。
- 只读盘点确认 platform-auth gate 已具备 dialog 语义、响应式宽度和一致层级，本批不修改；浏览器 / 系统原生 Notification 的视觉由操作系统控制，本批仅确认现有“API 存在且权限已授予才触发、异常降级为空”的合同，不修改权限请求、触发条件或系统通知语义。预览继续唯一归属 `CODE-042`，迁移 / 归档及既有已验收设置、消息和主界面表面未重复实施；至此 `CODE-043` 四阶段完成定义闭合，后续零散视觉问题不再回填本长期事项。
- 验证按真实失败边界留痕：初始单体 H4 先后因隔离宿主在附件目录尚未创建时采集 metrics 崩溃、以及鼠标交互后程序化 `focus()` 不满足 `:focus-visible` 的错误夹具假设而两轮停止，按熔断撤回且未记作产品失败；夹具只增加空附件目录证据降级，并把焦点验证改为真实 `Tab`。重新设计的单体链随后分别暴露并修复显示过渡焦点和 Key 同步触发按钮焦点两个真实缺口，再次达到单体路径熔断后未做第三次原样复跑，而是拆为互相独立的 `code043-final-core-selfcheck-v3` 与 `code043-final-key-sync-selfcheck-v3`。最终两条链均在 bundle / direct classic、light / dark、桌面 / `390px` 下通过，覆盖长文案、多 Toast、18 行长 Key 列表、选中 / 焦点态、可滚动弹层、Escape、可访问名称、零横向溢出、零业务写入及子进程 / 端口 / 临时根完整清理。
- 最终差量上 `python verify.py doctor` `10/10`、直接相关契约 / 行为测试、完整 `python -m unittest tests.test_frontend_modules` `453 tests`、frontend build / freshness / bundle syntax、`python verify.py quick` `6/6` 与 `git diff --check` 均通过。用户在实际 `http://localhost:3011/` 完成最终主观验收并明确回复 `PASS`；`CODE-043` 已从私有未完成清单移除，`CODE-058` 仅清理指向已完成事项的 related 链接，编号游标保持 `CODE-073`。本阶段未读取、哈希、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未执行 push、tag、release 或线上配置。

## 2026-09-04 02:03 · Codex

### CODE-073 · 移除设置页模型 API Key 拖拽排序

- 设置页模型 API Key 行移除独立点阵、`draggable` 标记、`dragstart` / `dragover` / `dragleave` / `drop` / `dragend` 监听、拖放状态样式及 `dragSort` 中英文文案；行布局从三列收敛为“主内容 + 操作区”两列并释放左侧占位。查看、启停、编辑、添加、同步、删除与模型刷新入口均保留。
- `code-key-config` 继续按原数组 / DOM 顺序加载、收集与保存，不增加迁移、排序或规范化副作用；合成配置补充 workbar 同步顺序断言，既有两连接路由刷新 payload 顺序合同继续通过。默认 routing v2 的 `connectionId + modelId` 精确路由、Route Registry、服务端连接收集与旧 `CODE_ROUTING_V2=0` 回退均未修改。
- fail-first 合同先稳定命中旧 `key-drag-handle`，实现后直接相关 `6 tests` 与完整 `python -m unittest tests.test_frontend_modules` `451 tests` 通过；`npm run check:frontend`、最终 frontend freshness / bundle syntax、`python verify.py quick` `6/6` 和 `git diff --check` 通过。源文件与 bundle 均无 Key 点阵 / `dragSort`，direct classic 继续加载同一 `i18n.js` / `settings.js`。
- 新增的双入口隔离浏览器场景首轮把视觉隐藏 checkbox 错纳入可见矩形，第二轮又使用 Playwright `uncheck` 操作该隐藏原生控件；bundle / direct classic 均在同一夹具边界失败。按 H4 熔断规则未做第三次复跑，新增 helper / tests 已完整撤回，不把该场景记作产品通过或失败；失败时 `pageErrors=0`，第二轮端口、子进程、浏览器资源与临时根完整清理。
- 用户在实际 `http://localhost:3011/` 设置页完成最终主观验收并明确回复 `PASS`。本阶段未读取、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未执行 push、tag、release 或线上配置；`CODE-073` 已从私有未完成清单移除，其他事项未启动。

## 2026-09-04 01:31 · Codex

### CODE-071 Stage B2 · 桌面会话拖拽快捷入口闭环

- 桌面会话列表移除独立点阵手柄，改为会话主体整行可拖；更多操作、重命名输入等动作控件不参与拖拽，普通点击继续打开会话，键盘与触摸场景仍可使用既有项目迁移菜单。拖拽只向 `DataTransfer` 写入固定非敏感哨兵，Session 身份保留在页面内存；紧凑标题预览跟随指针，源行以透明度和轻量抬起反馈当前状态。
- 落点统一为可见的完整 `project-block`，包含项目标题、展开的子会话行与块内留白；临时“无项目”目标沿用同一语义。同项目为中性 no-op，无效目标弱化且不提交。指针在区块内直接命中；仅在会话列表水平范围内，对相邻区块垂直间隙提供最近边缘最多 `8px` 的稳定磁吸，等距按 DOM 顺序选择。高亮、`dragover`、自动滚动重算与最终 `drop` 共用同一解析结果。
- 指针靠近会话列表上下边缘时，仅该列表执行有界连续滚动，并在每帧按新矩形重算目标；到达边界、成功、失败、drop、dragend、Escape、窗口失焦或列表重绘时都会停止滚动并清除 ghost、临时目标与状态类。最终迁移继续只进入 Stage B1 的服务端权威预览、确认、revision / token 与原子提交管线，没有新增前端迁移判断或改变 Session、项目、文件树和既有 AgentRun 工作区语义。
- 可迁移项目使用主题适配底色与 `1px` 内描边，当前指针命中的可迁移项目提升为更强底色、`2px` 内描边和轻阴影；强调覆盖完整项目块，不改变尺寸或密度。no-op 保持中性，invalid 保持弱化，深浅主题均不使用错误红色，reduced-motion 合同与动画时序未改变。
- 自动证据按失败熔断保留真实边界：R003 完整前端 `448 tests`、项目 / Session 持久化 `180 tests` 与 quick `6/6` 通过；R004 最终完整前端 `449 tests`、direct classic 拖拽全链、build / freshness 与 quick `6/6` 通过，bundle 的新增指针命中矩阵在后续测试夹具断言前完成；R005 相关侧栏 `45 tests`、build / freshness 与 quick `6/6` 通过，并以同一隔离 H4 的聚焦视觉模式分别验证浅色 bundle 与深色 direct classic 的两级底色 / 描边、整块范围、零布局位移、no-op 区分和无横向溢出，`pageErrors=0`，子进程、端口与临时根均清理。未把因夹具熔断拆分的证据记作单次完整双入口 H4 全绿。
- 用户在实际 `http://localhost:3011/` 界面完成最终主观验收并明确回复 `PASS`。本阶段未读取、修改或暂存受保护的 `data/generated-assets/` 与 `data/image-route-registry.json`，未触碰无关活动 handoff，未执行 push、tag、release 或线上配置。`CODE-071` 仍保留为未完成事项，下一独立阶段仅为已批准的“每轮工作区权威提示”；`CODE-072` 与其他事项未启动。
