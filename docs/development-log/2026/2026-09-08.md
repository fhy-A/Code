# 2026-09-08 开发日志

## 2026-09-08 19:21 · Codex

### CODE-058 用户视觉验收通过

- 用户对R123单项 / 批量授权展示明确回复“可以”，已获视觉PASS。复用31项回归、80组真实bundle/classic展示与合成决策验证及构建/freshness证据，仅核对差量和提交卫生，独立本地提交收口。
- 公共差量为app.js、i18n、单项CSS、新增H4专项、开发日志及索引，共6个文件。私有清单移除已完成CODE-058，ID游标保持CODE-086；无推送、标签、发布、日常服务或保护数据操作。

## 2026-09-08 19:15 · Codex

### CODE-058 单项与批量授权展示：实现及自动验证

- 已按用户视觉PASS先独立提交CODE-076：`ae8a67902e0d6d6b387c93a92e7b6b82a2d0df99`，5个公共文件，未推送。随后在同一开发任务进入已批准的CODE-058；本条之后的未提交差量不混入CODE-076提交。
- 单项改为“需要确认 / 拒绝 / 批准”，中英文走现有i18n；隐藏单项总数、类型数量摘要及选择框，保留操作类型、目标路径 / 命令、Diff查看及子Agent来源。多项保留原数量摘要、来源分组、行选择、全选、全部拒绝及批准所选(N)，补行选择框可访问名称；单项CSS去掉选择框网格列。
- 单项操作在点击时对应当前唯一pending且非提交中请求，即使该项此前在批量态被取消选择也能操作；不把隐式单项选择写回selected。旧单项按钮遇到替换请求或新批量、旧批量按钮遇到单项时仅刷新展示，避免用过期展示操作新对象。提交中按钮禁用，继续使用原resolveAuthorization的_finishing去重和失败恢复，仍按原authorizationId提交，后端、权限、队列、持久格式与恢复协议未改。
- 相关前端 / 权限 / 授权回归 `31 passed in 2.45s`，前端构建、bundle语法与freshness通过；没有运行全量或重复doctor。新增`node tests/e2e/h4/code058-authorization-display-selfcheck.cjs`，加载未改写的真实bundle/classic，通过既有导航回调注入合成请求、调用实际渲染及恢复；浏览器层履行全部授权与会话保存响应，真实模型 / 工具 / AgentRun请求和执行均为0。
- 专项最终通过两种运行态 × 中英文 × 深浅 × 1280px / 390px × 单主、单子、多主、主子混合、后台共80组展示检查；包含折叠/展开、键盘Tab焦点环、选择框名称、来源和目标、窄屏可用宽度及无面板横向溢出。两种运行态各9个合成决策，覆盖单项取消选择遗留、提交中重复点击、1↔多切换、替换项旧按钮、批量选择与全部拒绝、同ID失败重试、真实页面reload后已有保存请求恢复回调及重复恢复去重。
- 首轮夹具遗漏PUT会话保存合成响应，围栏按预期阻止请求；补齐精确合成路径后通过。随后纠正窄屏未收侧栏与指针模式下程序化focus的样本问题，以侧栏收起和真实Tab导航完成最终验证。未放宽产品断言或修改产品行为来迁就夹具。浏览器/上下文关闭，隔离子进程退出、两端口关闭、临时根删除均通过。
- 可复核证据在本机临时目录`code058-display-pDb3zu`：`result.json`、40份真实bundle截图和`index.html`选择预览。已实际查看宽屏浅色单主文件、窄屏深色单子命令及英文混合批量图。CODE-058仍待用户视觉确认、保持未提交，CODE-083未启动；私有TODO维护对应未完成差量，ID游标保持CODE-086，保护数据与日常服务未动。

## 2026-09-08 18:58 · Codex

### CODE-076 用户视觉验收通过

- 用户对 R122 更新效果明确回复“没问题”，工具轨迹外间距、名称常规字重及用时标题下方10px分隔已获视觉 PASS。复用 R121/R122 的真实自动验证与截图证据，只补差量和提交卫生检查；本阶段完成，按独立本地提交收口。
- 公共差量为样式、原间距断言、真实 bundle/classic 首项间距验证及开发日志/索引，共5个文件。私有清单移除已完成 CODE-076，ID游标保持CODE-086。未推送、打标签、发布或修改日常服务及保护数据。

## 2026-09-08 18:53 · Codex

### 工具轨迹视觉修正：用时标题与首个可见工具块

- 根据 R122 用户截图反馈，只补充两条局部 CSS：展开时首个可见工具块上 margin 为0，保留轨迹 body 的10px padding；收起时首个可见 persistent 工具块上 margin 为10px。选择器按可见候选序列识别首项，跳过 hidden 元素及收起态非 persistent 内容，不要求工具是 DOM first-child。
- 完整用时标题与轨迹 body 的前后合成样本，在深浅主题与1000px / 390px下测得标题底部到首工具顶部：展开直接工具及前置hidden思考均由26px降为10px，收起前置隐藏思考的persistent工具由16px降为10px。可见思考后工具、后续工具和工具下方仍为16px；名称400、正文 / 思考16px、工具14px、颜色与折叠语义保持。
- 扩展既有真实 bundle/classic 折叠专项，在两种运行态 × 深浅 × 1280px / 390px × 六种首项结构共48种组合中通过：直接工具、前置hidden思考、前置可见思考、收起隐藏思考、收起可见persistent思考、普通工具完全收起。原点击、键盘焦点、长路径和主题断言继续通过；模型 / 工具 / 产品写请求为0，进程、端口和临时根目录清理通过。
- 直接相关pytest `10 passed in 1.28s`；复用 R121 未变的其它字号 / 消息证据。8份新前后样本及计算样式检查通过，实际查看窄屏深色前后和宽屏浅色截图。本轮未运行全量、重启日常服务或改写数据。新证据目录为本机临时目录 `code-ui-r122-01a08005`，包含生成脚本、`index.html`、截图与 `visual-results.json`；真实运行态JSON保留在前一证据目录的 `r122-runtime.json`。
- 本阶段仍未提交，等待用户对标题下方留白的视觉确认；未关闭 CODE-076，未启动其它排队事项。

## 2026-09-08 18:42 · Codex

### 工具轨迹外间距与名称字重：实现及自动验证

- 按 R121 已批准的 CODE-076 范围，将所有工具块上下外间距统一为 `16px`，单工具名称、工具组标题及组内工具名称使用 `400` 常规字重；原阶段思考后 `8px`、已完成工具块后 `6px` 的紧凑分隔由工具块外间距替代。工具组内部排布、正文 / 思考 `16px`、工具轨迹 `14px` 和现有主题颜色规则保持。
- 产品差量仅为 CSS；同步更新原间距回归断言。定向消息 / 轨迹 / 字号 / 折叠回归 `23 passed in 1.48s`，前端构建与 freshness 通过。真实 bundle 与 classic 的 compact-disclosures final-state 专项通过：折叠点击、键盘焦点、长路径截断、无横向溢出及深浅主题有效；模型、工具与产品写请求均为0，隔离子进程退出、端口关闭、临时根目录清理通过。
- 使用产品工具渲染器、完整 CSS 与主题引擎生成合成长短步骤、单工具及工具组样本，完成深浅主题 × 1000px / 390px × 修改前后共8份截图。计算样式确认正文 / 思考16px、工具14px、名称400；运行 / 等待 / 失败 / 取消 / 中断 / 完成名称状态均保持400，颜色值与基线一致，外间距16px、无页面横向溢出。宽屏浅色和窄屏深色前后截图已实际查看。
- 样本首次缺少 `.pi-shell` 字号作用域，断言拒绝通过；补齐样本容器后8份全部通过。此问题只涉及临时样本，未改产品实现。未重跑全量或 doctor；本任务沿用既有 doctor 清理超时边界，本轮隔离浏览器及上下文正常关闭。
- 本条只记录已实现和已验证事实；主观视觉验收尚未完成，阶段保持未提交，未宣称 CODE-076 完成。可复核样本与生成脚本位于本机临时目录 `code-ui-r121-01a08005`，包含 `index.html`、8份截图和 `visual-results.json`。未改滚动、轨迹结构、执行协议、运行时数据或日常服务。

## 2026-09-08 18:14 · Codex

### 发布流程优化单次冻结回归与布局清单修正

- 在实现提交 `a01fb99b18026e0a75345f748bae21b41063a2c2` 上只运行一次 `python -B verify.py release`：前9项非发布检查通过；完整pytest真实结果为 `1 failed, 2836 passed, 1 skipped, 4 warnings, 1453 subtests passed in 994.79s`。完整命令耗时1001.003秒，pytest进程997.188秒，退出码1；不是1500秒超时，也不构成全量通过。
- 已读取完整stdout、stderr、失败栈、警告和最慢30项。唯一失败为项目布局测试的 `DEVTOOL_MODULES` 清单漏列本轮新增的 `release_inputs`。修正仅补该名称，目录严格相等、模块归属、无根目录副本及其余全部断言保留；完整布局模块随后 `3 passed in 1.04s`。
- 没有再次运行全量或增加预算；冻结之后发布实现和生产代码未改变，仅布局清单及完成记录有差量。原全量失败与逐阶段日志保持，不把3项定向通过替换为全量成功。R120实现与指定验证流程完成，后续正式发布仍须另行授权及其对应门禁。
- 阶段A收益仍仅为同环境专项83.70→76.10秒、单次约9.1%；本次全量与R112的代码/测试数量和环境负载不同，不能把1067.26→994.79秒直接归因于本次优化。真实新EXE打包/发布体验未验证；本轮只作合成隔离打包集成。v0.6.9 EXE及原例外审计哈希复核未变，未操作日常服务或保护数据，临时日志输出环境变量已恢复。

## 2026-09-08 17:52 · Codex

### 发布流程优化阶段B实现与定向验证

- R120完成最小单候选v2凭证：仅尚未发布时支持本版本正文允许区域或Git/gh版本变化的显式刷新，其他源代码、任意测试、实际打包输入、未知变化及构建环境漂移均拒绝复用。旧v1保持原严格读取/续发，不静默补字段获得细粒度复用；原全量验证时间保留，刷新记录具体检查与原凭证审计。
- 新增显式同版本重新准备：要求完整可验证来源、旧版本与HEAD关系可追溯、cached为空、无本地/远端对象冲突。先保存原审计并标为不可发布的 `reprepare_pending`，成功原子替换；失败恢复进入时四文件内容并保留同一重试入口，不复活旧凭证。publishing/published拒绝刷新或重准备，原对象继续冻结。
- 新prepare与恢复入口在昂贵验证前拒绝四发布文件之外的未提交改动和未跟踪的实际发布输入，防止EXE包含tag之外的产品变化；原三项不打包运行数据排除且不读取内容。新矩阵验证了拒绝时不运行门禁/打包、不修改凭证，以及保护数据不被误拒绝。
- release打包可传入本次精确前端证明，入口与PyInstaller前复核输入、输出和实际工具链；独立build仍自足。esbuild实际入口及二进制哈希、Node/npm、Python构建包版本与发行包版本摘要在门禁前采集，门禁后及封印前再次比较。实际Node解析的合成同版本二进制变化、门禁期间变化均被拒绝，未只依赖模拟环境字典。
- 旧helper非dry-run直接执行明确拒绝，删除了删除重建tag旁路；兼容CLI继续统一入口。Release创建绑定已存在tag与目标commit，创建/续发/已发布核验均检查非draft、非prerelease、有效发布时间及uploaded资产状态；缺失或冲突不自动修改远端。
- 发布相关回归 `131 passed, 243 subtests passed in 189.25s`；最终实际工具链/候选绑定7项 `7 passed in 36.28s`。前端确定性及隔离构建专项1项通过，quick六项与freshness通过。合成打包入口验证独立自足、复用时两次核验与明确版本/输入参数，没有运行真实PyInstaller或读取真实记忆。
- 本提交固定实现与上述定向事实，完整pytest及非发布冻结检查尚未执行，不能宣称全量通过。R117浏览器关闭超时边界保留，未重跑doctor；v0.6.9版本、EXE与原例外审计哈希未变，未push/tag/release或操作日常服务。

## 2026-09-08 17:07 · Codex

### 发布流程优化阶段A：指南去重与慢测试职责分离

- R118从 `1ce7ecd` 开始，只调整发布指南、协作说明和测试结构；生产Skill实现、发布脚本、prepared格式与失效规则、版本号及v0.6.9标签均未修改，未推送或正式发布。R117的doctor浏览器关闭超时作为既有能力边界保留，未重跑doctor或冒充通过。
- 指南删除“先手动全量，再运行会再次全量的release.py”步骤，正常入口统一为完整发布或prepare/publish-prepared二选一，发布中断走resume。原手动改版本/提交/推送/上传清单收敛为只读故障边界；如实说明当前同版本prepared失效后的版本号拒绝问题，未用文档宣称恢复实现已经完成。四份协作规则与canonical协议仅补充已核验证据的差量复用，原授权与独立复核合同不变。
- `test_skill_management_read_view.py` 默认仍为9项：两项31-Skill场景保留真实bootstrap/转换/逐项选择、每项独立副本、原完整性/计数、无跨请求缓存和并发断言；启停的停用/启用两方向均验证返回快照、generation+1与enabled。只将五次无独立断言的历史预热、旧实现31/95次读取对照和逐函数计时移到显式性能专项，其余七个场景及准备fixture/helper的AST不变。原两项测量函数在 `tests/performance/skill_management_read_view.py` 逐字保留；[覆盖对照](../../../tests/performance/README.md)列明默认与专项职责。
- 同一环境、相同从零合成准备的前后完整模块测量均9项通过：修改前 `83.70s`，修改后 `76.10s`，单次顺序样本减少 `7.60s`（约9.1%）；外部命令时间分别85.175/77.522秒。修改后真实准备仍占51.90秒，收益有限，不能外推完整发布耗时或统计稳定收益；R112原全量失败与例外记录不变。
- 相邻管理API/事务模块 `22 passed in 17.64s`。性能目录默认发现0项（pytest收集退出码5），显式文件发现2项；专项函数已在修改前原模块真实通过，移动后只核对相同源码和显式入口收集，没有再次重复计时。diff检查与成对规则同步通过；未运行产品全量、前端构建或EXE，前端/打包输入没有变化。
- 四组真实离线安装涉及v6/v7与正常绑定/持久化失败恢复及继续执行，全部保留原实现和组合；可共享的小型wheel准备收益不足，未引入共享可写环境、跨测试缓存或并行调度。根目录规则不属Git仓库，继续作为本地变更单独交付；阶段B仅提出设计，尚未实施。

## 2026-09-08 16:37 · Codex

### 本地会话文件大小分级检查与轮换规则

- 按 R117 确认的 100 / 300 / 500 MiB 建议阈值，新增 `scripts/session_file_size.py`：按精确任务 UUID 定位本地 `sessions/` 与 `archived_sessions/` 的 rollout JSONL，仅枚举名称和读取文件大小、修改时间等元数据，不打开会话正文。支持 `--json` 与显式 `--codex-home`，默认使用 `CODEX_HOME` 或 `~/.codex`。
- 小于100 MiB正常复用，100至小于300 MiB观察，300至小于500 MiB建议阶段结束后轮换，500 MiB及以上强烈建议轮换后再承接新阶段。分级按原始字节计算，显示值可四舍五入；阈值不是性能上限，也不自动中断运行或授权创建任务。
- 缺失、重复匹配、枚举/元数据读取失败或链接导致搜索不完整时返回“无法判断”、空大小及退出码2，不挑选最新/最小副本，不把未知当作零。已知大小各等级退出码均为0。脚本不保存提醒状态，不修改真实会话、个人技能、Codex配置或后台任务。
- 根目录与 `code` 的四份 AGENTS / CLAUDE 已同步同一规则块，canonical 协议已修正“发布完成即轮换”旧表述。审批/开发独立检查，在接手、阶段结束和承接新阶段前执行；同阶段同等级依已有对话记录只提醒一次。新任务仍需用户明确授权，在安全阶段边界完成事实归档、未完成事项整理、旧写者释放租约或旧审批停止发送，紧凑交接不复制全部历史。
- 合成专项最终 `26 passed in 15.64s`，覆盖100/300/500 MiB边界前后一个字节、两种存储目录、精确/重复/缺失匹配、读取失败、重解析点及CLI退出码；禁止内容打开调用的检查与文件字节/修改时间对比证明只读。大尺寸合成文件均已清理。四份规则块一致、成对协作文件同步及diff检查通过；当前开发任务仅元数据检查为正常复用。
- 本任务一次doctor中Python与所需依赖检查通过；Chromium关闭阶段报告 `cleanup_failed / DOCTOR_TIMEOUT`（5000ms），不能宣称doctor全部通过。本专项不依赖浏览器，未追加浏览器修复、产品全量或构建。根目录不属于Git仓库，两份根规则作为本地修改单独交付；本地提交仅包含 `code` 内本阶段文件。没有相关私有TODO活动项需收口，其他待办、保护数据和已发布v0.6.9保持不动，未推送。

## 2026-09-08 16:11 · Codex

### v0.6.9 一次性差量验收发布完成

- 经 R116 授权，从修复候选 `3aff527586a8ff92df224a919560b2b398b37804` 发布既有产物；发布提交 `a4dccc7b61fd34550b5941a361badec016d1e8d6` 严格只包含 `README.md`、`VERSION`、`file_version_info.txt` 和 `docs/releases/v0.6.9.md`，其父提交及四文件 Git blob 与独立例外审计一致。
- 已非强制推送 `master` 与 `v0.6.9` 标签，远端二者均指向该发布提交；[GitHub Release](https://github.com/fhy-A/Code/releases/tag/v0.6.9) 已公开，Release ID 为 `384545474`，非草稿、非预发布，中文正文与已审核文件一致。
- 唯一发布资产 `Code-v0.6.9.exe` 的 Asset ID 为 `550115138`，大小 `32996097 bytes`；GitHub 返回的 SHA-256 为 `05bcda135918974feee3cf987c931490d78dc0f2c80aec35a12b951be088e019`，与本地 EXE 和原例外审计一致。PE 的 ProductVersion / FileVersion 均为 `0.6.9`，OriginalFilename 为 `Code-v0.6.9.exe`；本轮没有启动 EXE。
- 验收仍为用户明确批准的一次性差量例外：原 R112 完整 pytest 的 `1 failed, 2751 passed, 1 skipped, 4 warnings, 1453 subtests passed in 1067.26s` 如实保留；修复后两个原参数分支各10次、共20次通过。本轮直接复用这些结果和既有构建，没有重跑测试、doctor 或构建，不宣称全量通过，不签发标准 prepared 凭证，默认门禁未修改。
- 原例外审计及全部绑定证据保持不变，另存本次独立发布审计，记录授权、候选、四文件、发布提交、标签、Release 与资产 ID / 摘要及逐步远端核验结果。未使用 force、clobber、删除重建或自动登录安装；受保护差量、真实运行数据、日常服务和其它活动交接未操作。
- 已知启停性能、自然加载样本及 Office 渲染证据限制仍按发布说明保留；发布完成不代表相关延期事项关闭。本次完成记录独立本地提交，暂不另行推送，不移动发布标签，不启动下一周期。

## 2026-09-08 15:46 · Codex

### v0.6.9 同步测试修复与一次性差量验收

- R112 在候选 `592dee0` 的完整 pytest 真实结果为 `1 failed, 2751 passed, 1 skipped, 1453 subtests passed`，耗时 1067.26 秒；前10项发布检查通过，完整 prepare 失败且未构建 EXE。该失败结果及原始日志保留，不被本次定向补验替换为全量成功。
- 唯一失败发生在取消/超时测试观察合成副作用文件时：文件已创建但内容尚未写完。R113 控制窗口验证确认该竞态，True 分支修正后的成对复跑在第7对暴露未修正 False 分支的同一窗口，因此停止；这些旧复跑不计入本次结果。
- 用户通过 R114 明确允许两个原参数分支共享内容就绪等待；净差量仅为 `test_cancel_and_timeout_after_effect_do_not_claim_rollback` 中一行 while 条件。原参数、断言、超时、worker 退出检查、函数外内容、其它测试及生产代码完全不变，已保存原源对比证明。
- R114 全新连续10对原参数测试全部通过，即 cancel=True 与 cancel=False 各10次，合计20次，总耗时34.063秒；R115恢复时直接复用该证据，没有重复测试。完整输出、逐次退出码、差量及 preservation-proof 保存在本地任务证据目录。
- 用户明确批准本版本采用一次性差量验收例外：以保留的 R112 完整失败结果和上述定向补验为依据，另行构建本地产物与独立例外审计。本提交只完成测试修复与验证事实记录，尚未构建或发布；不运行完整 prepare、全量 pytest 或 skip-tests，不签发标准 prepared 凭证，常规发布门禁保持不变。

## 2026-09-08 14:43 · Codex

### v0.6.9 完整测试预算与耗时报告

- 用户明确确认R112：完整pytest上限由1080秒调整为1500秒（25分钟），增加 `--durations=30`；保留全部测试、断言、原11项检查集合与顺序、其他超时及H4排除边界。没有分片、并行化、夹具重构或生产功能修改，失败后不自动继续加时。
- 修改仅作用于共享 `pytest_full` 定义、对应断言及发布指南。实测manifest逐项对比确认只有该项的command和timeout改变；新指纹为 `f7c819387c963f6d2368505890c9193f2b537f2642499c58df156fae343eba1f`，旧凭证不允许复用，schema未改变。
- 仅运行预算/参数实际传入、共享检查集合与超时、指纹变化、旧凭证拒绝及合成prepared绑定的7项直接相关测试，`7 passed in 0.25s`；diff检查通过。没有重复既有101/95/28项专项或doctor。
- 此提交记录预算变更事实；完整prepare尚未执行，不能据定向测试宣称全量或发布通过。后续按新提交冻结后仅运行一次正式prepare，成功仍需发布复核，失败或超时停止；不会修改已生成凭证绑定的白名单外文件。

## 2026-09-08 14:15 · Codex

### v0.6.9 完整prepare失败与旧生命周期夹具修正

- R110在冻结候选 `9d89688` 上仅执行一次canonical prepare。版本同步和前10项共享检查通过；Harness replay通过17项fixture、124个事件、25个checkpoint及其恢复和4个显式恢复。完整pytest在1080.031秒超时，prepare退出1、总耗时1092.43秒，未进入EXE构建或签发凭证。
- 本次完整stdout/stderr与退出诊断已保留，进度到约97%后的输出仍可核对；没有最终pytest汇总，因此不能把进度或未显示失败的用例当作完整通过。超时后本轮Job收尾，版本元数据按canonical流程回滚至0.6.8，没有修改日常实例或运行数据。
- 对现有进度与只收集顺序作定位，30个失败标记映射到旧生命周期/访问协议集成类；独立复现两个代表用例，均在创建运行时被新默认准入以 `Immutable Skill admission is unavailable` 拒绝，尚未进入原断言。这两组夹具只建立旧可变目录，预期包括v5记录，却未显式选择旧模式。
- 仅在 `test_skill_lifecycle.py` 和 `test_skill_lifecycle_access.py` 的旧协议setUp中明确关闭新准入与主模型加载，原tearDown恢复开关；生产默认值、准入拒绝逻辑、协议、全部原断言与测试数量均不变。保留R109发布流程及既有合成基线优化。
- 两个完整测试文件及新默认模式测试共 `95 passed, 119 subtests passed in 28.08s`。此次只确认夹具修复及默认值未被改变；整体1080秒耗时问题尚未验证解除，没有再执行第二轮prepare、提高超时、跳测试或对外发布。修正提交形成待复核的新候选基线。

## 2026-09-08 13:44 · Codex

### v0.6.9 发布前分层检查、实时诊断与统一入口

- 用户明确将发布流程优化纳入v0.6.9，R109以 `10b8b10` 为基线实施。原11项发布检查的命令、标签和超时逐项保持，顺序改为diff/语法、前端、replay、完整pytest；共享manifest仍是唯一事实源，顺序变化产生新指纹，旧凭证不能静默复用。1080秒上限、H4排除和完整发布门禁均未放宽。
- prepare先核对说明正文及现有构建工具，再作远端只读预检；正文旧输入格式继续支持。验证环境前后比对，候选、发布文件、定义、产物与环境仍按原schema绑定。发布说明和指南明确日常按变更影响运行定向及相关验证，不为每个开发阶段机械全量。
- 公开验证/构建命令实时输出，并在系统临时目录保留完整UTF-8 stdout、stderr及阶段、耗时、退出诊断。Windows复用现有Job容器，命令仅在绑定成功后放行，结束时确认本轮进程树退出；quiet身份与凭据查询保持quiet。超时、取消及未运行项不会被标为通过。
- 默认完整入口统一复用原prepare、publish-prepared及resume流程：fresh只准备一次，有效prepared严格校验后复用，publishing审计续接同一候选，published只作只读审计。已发布分支、标签、Release或资产缺失/漂移时拒绝重建；坏凭证和未知状态不能回退成一次新的发布。版本优先、兼容别名、dry-run、skip-tests及交互确认边界保留。
- R108中断前的F通过现有输出、只收集顺序和相关子集定位为布局测试清单漏列既有 `skill_loading.py`；仅补齐名称，严格目录相等断言保持。此前合成基线优化继续保留，不修改Skill产品行为。
- 最终相关回归 `101 passed, 250 subtests passed in 34.32s`；覆盖旧凭证漂移/冲突/响应丢失、入口兼容、早期失败、真实stdout/stderr、非零、超时、实时取消、Windows批处理、Job绑定失败及本轮后代退出，独立无关进程保持存活。真实quick profile六项及diff检查通过。初轮Windows换行和旧测试位置/编码问题均已修正并复验。
- 本阶段未运行新的完整prepare、EXE构建或对外发布，版本保持0.6.8，无prepared凭证；原1080秒全量超时是否解除仍待后续冻结候选门禁。未重启日常实例、调用付费模型、安装依赖或改动保护数据、其它TODO及WB-003交接。

## 2026-09-08 13:11 · Codex

### v0.6.9 发布阻断定位与合成测试准备复用

- R108 按用户重新发布及允许定向修复阻断的授权，从 `c6c3e3f95b0ed609db801c6c7386ebf3d1e8aeee` 执行 canonical prepare。版本同步、前端构建、freshness及语法检查通过，完整 pytest 在原1080秒上限超时；命令退出1，脚本自行恢复版本元数据至0.6.8，没有生成 EXE 或 prepared 凭证，也未进入 replay 或对外发布。
- 此次实时输出和退出码已保存。合成测试目录时间线显示，两项31-Skill用例分别占据约205.75秒和187.89秒的目录创建间隔；这些间隔用于定位重复准备成本，不是精确的测试耗时或唯一根因证明。超时时测试已推进到后续启动用例。
- 仅调整 `tests/test_skill_management_read_view.py`：31项、217文件的真实 bootstrap、转换及逐项选中历史改为模块内构建一次；每项测试复制到独立目录后继续原启停、读取和并行操作。新增复制内容逐字节相等和共享模板未被修改断言，既有外部合成基线入口保留。产品代码、安全验证、测试数量、门禁定义及1080秒上限均未修改。
- 定向运行整个模块，`9 passed in 119.21s`；含基线准备的启停测试99.31秒，复用副本的读取测试10.54秒，原有正确性及完整对象检查断言全部保留并通过。该结果不能代替完整发布门禁；修正提交后须重新冻结候选并重新 prepare。
- 原受保护和运行时数据、CODE-084及WB-003活动交接未修改；不恢复延期的性能、自然加载或Office专项，不重启日常实例，不调用真实模型。
