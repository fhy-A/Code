# 2026-08-25

## 2026-08-25 15:50 · Codex 开发会话

### CODE-013 · 终态问卷恢复与 stale 输入对账（用户 H4 PASS）

#### 完成内容

- 服务端 AgentRun 问卷继续接受任意非空文本答案，不根据问题 ID 或文案猜测 URL 等额外格式；健康的 `waiting_user_input` 与同一 `requestId` 保持部分进度、刷新恢复和 exactly-once 续行，提交端点与既有 answers body 兼容。
- 已持久化问卷在完整 Session 加载后，以关联 AgentRun 的终态、缺失或请求身份不匹配作为权威证据对账；取消、失败或完成的 stale 问卷会清除前端面板并将既有 Session 终态投影持久保存。暂时查询失败仍保留门禁并允许重试，不会误清理有效问卷。
- 取消 AgentRun 继续按既有契约产生唯一一次 `tool_completed` 取消闭合；刷新对账不会新增事件、工具执行、消息、input/resume/tool 请求或替代 AgentRun。清理后的会话仍可创建并完成后续普通任务，原始取消运行和后续完成运行按 AgentRun 身份独立核对。
- 同批已验收的新手任务体验微调保持：首次任务建议只填充输入框，中屏完成反馈只在实时未完成→完成时播放一次；模型连接身份统一称为 workbar，不展开或猜测其底层网关实现。

#### 验证与有限边界

- 用户在 fresh frontend 上运行 bundle/direct classic `terminal questionnaire` 四场景，结果为 `4 passed (11.0s)`：健康路径证明“你帮我找”等任意非空文本与第二个文本答案一次提交并完成；取消 stale 路径证明刷新后 Session 为无问卷的暂停投影、AgentRun 生命周期无新增副作用，且后续普通任务完成。
- 本次只收口已经验收的 CODE-013 差量，不修改 TODO，不启动 `CODE-026`，不触碰活动 handoff、真实 Session/AgentRun 数据、浏览器数据或发布状态；未执行 push、tag、release。

## 2026-08-25 11:11 · Codex 开发会话

### CODE-013 · 登录后三步新手任务（用户 PASS）

#### 完成内容

- 登录后的显式新手任务收口为三步顺序流程：主动验证 workbar 连接、主动同步/添加或确认可用 Key、在引导内确认模型并成功创建首次前台 AgentRun。被动登录状态、静默 Key 同步、普通模型选择或直接发送均不推进进度；三条示例只填入并聚焦输入框，不会自动发送。
- 任务卡只投影在无 Session、无消息的空白欢迎页；普通消息发送后立即隐藏且保留未完成进度，新建空白会话时恢复。完成后只显示一次轻量恭喜反馈，`prefers-reduced-motion` 使用静态反馈；设置页“重新打开新手任务”仅重置引导并切回空白欢迎页，不创建或删除 Session。
- `localStorage` 继续使用独立键 `code-onboarding-tasks-v1`，当前 `v2` 只保存 `version`、有序 `completedTaskIds` 和 `completed`。旧 `v1` 四步状态按合法前缀迁移：模型步不冒充新第三步完成，旧四步全完成保持完成；损坏/未知版本和写入失败均不伪完成。已有会话历史的老用户默认豁免，可从设置主动重新开启；不保存 Key、Token、模型、消息或账号数据。
- 欢迎页 composer 与任务卡修正为 `composerStack` 下的同宽直接兄弟节点，工具预设与停止按钮保持在 stack 外；单列 grid 及直接子节点使用可收缩的 `minmax(0, 1fr)`、`border-box` 和零最小宽度，桌面及约 390px 窄屏均保持配置间距、无重叠和无横向溢出。没有改变 Session、AgentRun、Goal、权限、Key 存储或服务端协议。

#### 验证与有限边界

- onboarding 状态、显式成功边界、HTML 层级和窄屏收缩的定向契约通过；Node 语法、UTF-8 与 diff hygiene 通过。用户在普通终端完成 fresh frontend build 与 `verify:frontend`，随后 bundle/direct classic `explicit onboarding tasks` H4 为 `2 passed (11.3s)`，覆盖三步顺序、直聊不推进、示例零派发、真实前台 AgentRun、设置重开以及 1280px/390px 几何边界。
- 本阶段完成 CODE-013 并从 TODO 移除；Code 编号游标保持 `CODE-042`。确认序列只保留尚未启动的 `CODE-026`，不由本阶段自动开始。未执行 push、tag、release，也未触碰活动 handoff 或浏览器中的其他业务数据。
