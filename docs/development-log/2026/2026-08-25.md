# 2026-08-25

## 2026-08-25 23:32 · Codex 开发会话

### 问卷 v2、流式跟随、Goal 取消与可信模型路由（全量门禁 PASS）

#### 完成内容

- 新 `request_user_input` 调用固定为 `single` / `multiple`，每题提供 2–3 个明确选项、始终允许自定义文本，并且恰有一个带非空理由的推荐项；格式不合法时作为同一 AgentRun 内可修复的普通工具失败处理。旧持久化的 text、无推荐标记或 `allowOther=false` 问卷继续按原数据恢复和提交，不迁移 Session JSONL。问卷面板在 chat form 内完整隔离 Enter：文本、自定义输入和选项键盘确认均只解析当前题，连续两份问卷各自 exactly-once 提交并续行同一 AgentRun，不再触发外层空提交或取消；用户已完成人工滚动/问卷 Enter 验收。
- 消息滚动以可续期的用户输入所有权覆盖 wheel 惯性、touch、相关键盘与滚动条拖动；真实用户上滚立即优先，离开底部后停止 following，仅在用户真实回到底部或点击“回到最新”时恢复。流式 DOM patch、ResizeObserver、图片/表格迟到布局只在无活动用户滚动意图时保持底部或既有阅读位置，不改变会话隔离与 reading-anchor。
- 前台 Agent 新增内部 `goal_cancel` 元数据工具，复用 Goal v2 既有 `goal_cancelled` 事件、revision、owner/sourceRunId 和幂等语义，可直接终结 draft 0/0、active、paused/gated 与兼容的非终态 Goal。取消不等同于 DELETE AgentRun 或停止当前输出；终态 Goal 的活动 composer 投影隐藏，历史来源与事件保留，刷新继续呈现 cancelled 事实，既有人工 `propose_change(type=cancel)` 路径保持兼容。
- 模型目录缓存升级为可信模型→Key 路由：仅持久化绑定 Base URL 与当前启用 Key 集合的稳定非明文身份，不保存 Key；派发只使用 `/proxy/models` 明确验证支持目标模型的 Key。缺失或过期映射先受控刷新一次，失败则在创建 AgentRun 前 fail-closed；中英文“模型无可用渠道/路由”复用 `model_access_denied` 分类并使对应缓存失效，普通 5xx、网络、限流或泛化 distributor 文本不误判。浏览器直连与服务端 AgentRun 客户端使用同一分类器。
- 模型请求投影只从 assistant `meta.toolCalls` 建立 API pending call；steer 插入时已完成结果继续与原 assistant call 原子配对。旧会话中的孤立 tool-result 仅在请求投影层降级为普通上下文文本，不改写 JSONL、不伪造 tool call，也不丢失用户消息。
- 全量回归暴露的测试证据已与上述 canonical 行为对齐：Goal completed/cancelled 终态、queue 异步可信 Key 门禁、startup 异步 v3 缓存恢复、questionnaire v2 合法样本及 classic onboarding import 均保留原安全/顺序断言。手动压缩两套 Node VM 统一补入 `getFallbackKeys`；source slice 只因可信路由调用从 21221 变为 21270 字符，19 个 failure scenario hash 全部保持不变，仅同步直接派生的 slice、fixture、replay 与 suite 哈希。

#### 验证与有限边界

- 用户普通 PowerShell 全量门禁：`1440 passed`、`1053 subtests`、`145.18s`，仅有 3 条预期的损坏 TIFF 警告。此前定向证据为：前端模块 `245 tests OK`、Agent runtime `115 tests OK`；questionnaire/stream bundle+classic H4 `14 passed`；draft Goal cancel `2 passed`；trusted model route/tool protocol `4 passed`；manual compaction `13 passed + 38 subtests`；合并回归 `141 passed + 60 subtests`。本地 `npm.cmd run verify:frontend` 同步确认 bundle freshness 与语法通过。
- 兼容边界保持：不迁移或重写旧 Session/AgentRun/Goal sidecar，不把 Key 写入模型缓存或证据，不更改 New API 线上分组/渠道，不把 Goal 取消耦合到传输取消，也不把旧孤立工具结果修写回历史。`CODE-026` 仍未启动；未执行 push、tag 或 release。

## 2026-08-25 15:50 · Codex 开发会话

### CODE-013 · 终态问卷恢复与 stale 输入对账（用户 H4 PASS）

#### 完成内容

- 服务端 AgentRun 问卷继续接受任意非空文本答案，不根据问题 ID 或文案猜测 URL 等额外格式；健康的 `waiting_user_input` 与同一 `requestId` 保持部分进度、刷新恢复和 exactly-once 续行，提交端点与既有 answers body 兼容。
- 已持久化问卷在完整 Session 加载后，以关联 AgentRun 的终态、缺失或请求身份不匹配作为权威证据对账；取消、失败或完成的 stale 问卷会清除前端面板并将既有 Session 终态投影持久保存。暂时查询失败仍保留门禁并允许重试，不会误清理有效问卷。
- 取消 AgentRun 继续按既有契约产生唯一一次 `tool_completed` 取消闭合；刷新对账不会新增事件、工具执行、消息、input/resume/tool 请求或替代 AgentRun。清理后的会话仍可创建并完成后续普通任务，原始取消运行和后续完成运行按 AgentRun 身份独立核对。
- 同批已验收的新手任务体验微调保持：首次任务建议只填充输入框，中屏完成反馈只在实时未完成→完成时播放一次；模型连接身份统一称为 workbar，不展开或猜测其底层网关实现。

#### 验证与有限边界

- 用户在 fresh frontend 上运行 bundle/direct classic `terminal questionnaire` 四场景，结果为 `4 passed (11.0s)`：健康路径证明“你帮我找”等任意非空文本与第二个文本答案一次提交并完成；取消 stale 路径证明刷新后 Session 为无问卷的暂停投影、AgentRun 生命周期无新增副作用，且后续普通任务完成。
- 本次只收口已经验收的 CODE-013 差量，不修改 TODO，不启动 `CODE-026`，不触碰活动 handoff、真实 Session/AgentRun 数据、浏览器数据或发布状态；未执行 push、tag、release。

## 2026-08-25 11:11 · Codex 开发会话

### CODE-013 · 登录后三步新手任务（用户 PASS）

#### 完成内容

- 登录后的显式新手任务收口为三步顺序流程：主动验证 workbar 连接、主动同步/添加或确认可用 Key、在引导内确认模型并成功创建首次前台 AgentRun。被动登录状态、静默 Key 同步、普通模型选择或直接发送均不推进进度；三条示例只填入并聚焦输入框，不会自动发送。
- 任务卡只投影在无 Session、无消息的空白欢迎页；普通消息发送后立即隐藏且保留未完成进度，新建空白会话时恢复。完成后只显示一次轻量恭喜反馈，`prefers-reduced-motion` 使用静态反馈；设置页“重新打开新手任务”仅重置引导并切回空白欢迎页，不创建或删除 Session。
- `localStorage` 继续使用独立键 `code-onboarding-tasks-v1`，当前 `v2` 只保存 `version`、有序 `completedTaskIds` 和 `completed`。旧 `v1` 四步状态按合法前缀迁移：模型步不冒充新第三步完成，旧四步全完成保持完成；损坏/未知版本和写入失败均不伪完成。已有会话历史的老用户默认豁免，可从设置主动重新开启；不保存 Key、Token、模型、消息或账号数据。
- 欢迎页 composer 与任务卡修正为 `composerStack` 下的同宽直接兄弟节点，工具预设与停止按钮保持在 stack 外；单列 grid 及直接子节点使用可收缩的 `minmax(0, 1fr)`、`border-box` 和零最小宽度，桌面及约 390px 窄屏均保持配置间距、无重叠和无横向溢出。没有改变 Session、AgentRun、Goal、权限、Key 存储或服务端协议。

#### 验证与有限边界

- onboarding 状态、显式成功边界、HTML 层级和窄屏收缩的定向契约通过；Node 语法、UTF-8 与 diff hygiene 通过。用户在普通终端完成 fresh frontend build 与 `verify:frontend`，随后 bundle/direct classic `explicit onboarding tasks` H4 为 `2 passed (11.3s)`，覆盖三步顺序、直聊不推进、示例零派发、真实前台 AgentRun、设置重开以及 1280px/390px 几何边界。
- 本阶段完成 CODE-013 并从 TODO 移除；Code 编号游标保持 `CODE-042`。确认序列只保留尚未启动的 `CODE-026`，不由本阶段自动开始。未执行 push、tag、release，也未触碰活动 handoff 或浏览器中的其他业务数据。
