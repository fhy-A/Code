# 2026-08-26 开发日志

## 2026-08-26 10:31 · Codex 开发会话

### 问卷五题上限与设置页新手入口删除（用户 H4 PASS）

#### 完成内容

- `request_user_input` 的前端工具 schema、服务端工具 schema、服务端执行校验和前端运行时标准化统一支持最多 5 题；模型指导仍要求默认 1 题、通常不超过 3 题，仅在同阶段确有 4～5 个独立且必要的决定时使用更高题数。每题 2～3 个选项、允许自定义输入、唯一推荐项及非空推荐理由的既有约束不变，6 题继续 fail-closed，不做静默截断或自动拆分。
- 修复了运行时标准化层遗留的 `.slice(0, 3)`：合法五题现在按原顺序完整进入 Session、前端问卷和逐题进度，不再出现模型工具参数含 5 题但持久化与 UI 仅保留 3 题。旧 1～3 题、旧持久化问卷、刷新恢复、Enter/点击提交和 exactly-once 续接不需要迁移，答案协议与 Session/AgentRun 格式未改变。
- 删除设置页中的“新手任务”导航、面板、重开按钮及设置专用接线和文案；欢迎页三步新手任务、最小进度持久化、旧用户豁免和完成语义保持不变。隔离测试改由明确准备 onboarding 存储状态，不保留隐藏的生产重开入口。

#### 验证与有限边界

- 用户普通 PowerShell 首次运行同组 bundle/direct classic H4 时，已有 6 个场景通过，2 个五题场景失败；证据定位到唯一运行时 `.slice(0, 3)`。修复后用户复跑五题 bundle/direct classic，结果为 `2 passed (12.6s)`，覆盖五题逐题进度、刷新恢复、一次 input/resume 与完成链路。
- 定向前端标准化/工具 schema 单元为 `2 tests OK`，Node/Python 语法与 `git diff --check` 通过；schema、validator 与 normalizer 的五题上限及六题拒绝边界完成静态复核。
- 本轮没有运行或宣称全量 pytest；当前结论限定于上述定向契约与用户 H4。未修改 TODO、持久化协议、答案格式或下一推进序列，未启动 `CODE-026`，未执行 push、tag 或 release。
