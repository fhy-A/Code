# 2026-08-31 开发日志

## 2026-08-31 15:39 · Codex

### CODE-043 · 会话搜索、归档视觉与来源标记批次完成

- 会话搜索与归档辅助表面统一为 `18 / 14 / 12px` 的标题、主要内容与辅助信息层级，搜索 / 归档输入为 `40px`，恢复、永久删除与重试操作为 `32px`；归档卡片间距与内边距收紧，用户 Browser 实测代表行高由约 `80px` 收敛到 `61px`。桌面操作保持右对齐，`720px` 下的自然换行、长标题和零横向溢出由确定性 CSS / DOM 合同覆盖。
- 侧栏与未归档搜索共用一个来源标记投影：只在 `sourceBadgeVisible=true` 且来源为 Codex / Claude Code 时显示标题滚动区之外的紧凑标记，并继续使用既有“首次在 Code 中继续后隐藏”提示和只提示一次的生命周期；原生 Code 与已继续会话不显示。归档列表不再为 Code 保留冗余来源行，Codex / Claude Code 则无论是否已经继续，都把既有本地化完整来源名合并进“项目 · 归档时间”辅助行；没有改变来源字段、导入、搜索、归档、导航、恢复或删除协议。
- 永久删除保持原 `.archived-session-delete`、二次确认、当前 `archiveToken`、pending / confirming 防重与删除后刷新所有权，但按钮改为复用共享 `trash` 线性 SVG 的 `32×32px` 图标控制。默认透明、弱边框与灰色，只在 hover / `focus-visible` 使用克制危险色和浅红反馈；删除 pending 时禁用并切换既有“正在删除”title / aria-label、`aria-busy=true` 与忙碌指示，醒目的最终危险强调继续由确认弹窗承担。

### 自动证据、Browser / 用户验收与边界

- R002 fail-first 稳定得到 `4 failures`，锁定共享来源渲染、搜索来源、归档辅助行合并及长标题 / `720px` 边界；R003 fail-first 为 `2 failures`，锁定图标化 DOM、title / aria-label / busy、默认中性与 intent-danger 样式。修正后直接相关组为 `23 passed`，阶段冻结时完整 `tests/test_frontend_modules.py` 为 `385 passed`；`npm run check:frontend` 的 build / freshness、`app.js` / `sessions.js` / `settings.js` JavaScript 语法及 Git diff 检查均通过。
- 审批任务在现有 3011 Browser 复核 Notion 深色与浅色的搜索、归档、默认垃圾桶、hover、键盘焦点及最终确认弹窗，确认 `61px` 归档行和原有搜索 / 侧栏没有视觉回归；用户随后明确 `PASS`。当前真实数据全部来自 Code，因此 Browser 没有外部来源真实样本；Codex / Claude Code 的显示与 `sourceBadgeVisible` 生命周期由 Code / Codex / Claude Code 确定性夹具验证。
- 本批次没有执行真实永久删除，也没有在 Browser 制造删除 pending；pending 无障碍名、忙碌态和防重由确定性测试覆盖。`720px` 只记录为自动合同，不声称 Browser 实测。开发任务未操作 3010 / 3011 或内置 Browser，未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。
- `CODE-043` 继续保持开放：第三阶段的设置其余表面、预览正文、通知与 onboarding，以及第四阶段桌面 / 窄屏、bundle / direct classic、一致性与可访问全面审计仍待用户另行选择；本阶段未自动启动后续实现。

## 2026-08-31 14:15 · Codex

### CODE-043 · 普通消息渲染可访问性完成

- 普通助手消息中的可操作文件卡改为原生 `button`；图片路径卡继续区分“缩略图查看图片”和“文件名打开文件”，两者分别为原生控制。Markdown 内联预览图片获得等价按钮语义、带可见文件名或完整路径的本地化名称及 Enter / Space 键盘委托，重复按键事件不会重复激活；文件和图片动作继续复用既有 `openReferencedPath` / `showImageOverlay`，没有新建路径信任或预览协议。
- Markdown 表头增加 `scope="col"`。普通助手消息的表格滚动容器只在 `scrollWidth > clientWidth + 1` 时进入 `tabindex="0"`，并获得 `region` 与本地化区域名称；不再溢出时同步恢复 `tabindex="-1"` 并移除 region / aria / i18n 标记，范围外表格保留旧焦点策略。文件 / 图片控制和可滚动表格的 `focus-visible` 只作用于普通助手消息。

### 自动证据、Browser / 用户验收与边界

- fail-first 稳定得到 `4 failures + 1 error`，分别锁定表头列头语义、动态 region、原生卡片控制、键盘转发及局部焦点样式；修正后直接组为 `7 passed`，完整 `tests/test_frontend_modules.py` 为 `376 passed`。`npm run check:frontend` 的 build / freshness、`app.js` / `markdown.js` / `i18n.js` JavaScript 语法、Python 语法及 Git diff 检查均通过。
- 审批任务在现有 3011 Browser 实测文件 / 图片按钮的原生语义、可访问名称、焦点反馈，鼠标图片预览 / 文件打开、tooltip 与中英文标签；宽表格可横向滚动，实测 `clientWidth / scrollWidth ≈ 243.3 / 258` 且具有 region，两个普通表格均为 `tabindex="-1"` 且无 region。验收后已恢复中文、Notion 深色、原样本会话并关闭预览面板。
- Browser 键盘驱动连无关设置页原生按钮也无法激活，属于验收工具限制，因此本条不声称审批 Browser 自动观测到 Enter / Space 或右键菜单。用户收到明确清单后，手动复核 Enter / Space 每次只触发一次既有动作及右键菜单保持，随后明确 `PASS`。
- 本批次只修改前端渲染、局部样式、i18n 与确定性测试，没有改变鼠标点击、contextmenu、tooltip、路径归一 / 越界降级、图片预览、Markdown 数据、协议、持久化或状态模型。未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。`CODE-043` 继续开放，第三 / 第四阶段剩余表面等待用户另行选择，本阶段没有自动启动后续实现。

## 2026-08-31 13:08 · Codex

### CODE-043 · Markdown 排版与跨主题对比度优化完成

- 普通用户与助手消息的首个 Markdown 直接子元素顶距统一归零，避免首段或首标题重复叠加顶部留白；admonition 标题 / 正文统一为 `14px`，分别保持 `600` 字重与 `1.5 / 1.6` 行高。引用、外链、代码头 / 行号 / 复制 / 宽表提示及消息时间、角色、用量等辅助信息改用局部 Markdown 对比度令牌，`copy-code` / `table-overflow-hint` 明确为 `12px` 且不再依赖透明度降低可读性。
- 令牌只接入普通消息范围，不扩展到预览、Diff、工具、设置或其他后续表面。深色入口以运行时真实使用的 `html[data-theme-mode="dark"]` 为权威，同时保留 `body.theme-dark` 兼容；浅色 muted 收紧为 `#5c6674`，深色 muted / link 分别为 `#adb5c3 / #89b9ff`，其余正文、DOM、JavaScript、i18n、协议和状态未变。

### 自动证据、Browser / 用户验收与边界

- R001 的直接合同先稳定锁定首子元素、admonition、引用 / 外链 / 代码辅助文字和局部范围；审批 Browser 随后发现真实页面以 `html[data-theme-mode="dark"]` 挂载主题、`body` 并无 `theme-dark`，导致 Notion 深色误套浅色令牌并将候选打回。R002 改用真实入口并保留旧入口兼容，测试直接执行 `theme-engine` 的 15 个浅色与 26 个深色 palette，固定 Notion 深色 `#191919 / #262626 / #282828` 背景事实；全部背景组合的最弱对比度为浅色 `4.809`、深色 `4.888`，均不低于 `4.7`。
- 修正后的普通消息与 Markdown 相关组为 `11 passed`，阶段冻结时完整 `tests/test_frontend_modules.py` 为 `373 passed`；`npm run check:frontend` 的 build / freshness、Python 语法及 Git diff 检查均通过。审批任务在现有 3011 Browser 复核 Notion 深色与 Codex 浅色的普通 Markdown、admonition、引用、链接和代码辅助文字，最终恢复 Notion 深色并取得用户明确 `PASS`。开发任务自身未操作 3010 / 3011 或内置 Browser。
- 本批次只修改 scoped CSS、确定性测试及统一事实文档，没有读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，没有覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。`CODE-043` 继续保持开放，下一独立批次为已获授权但尚未启动的渲染可访问性；本阶段没有自动开始该实现。

## 2026-08-31 10:44 · Codex

### CODE-043 · 用户长消息单行底部操作区完成

- 仅真实溢出的普通用户消息进入局部两列 grid：纯文本消息把操作区固定在正文后的第 `2` 行，图片+长文本固定在第 `3` 行；展开全文 / 收起始终位于左下，时间戳与 `28×28px` 复制按钮位于同一行右下，不再形成 toggle 与元信息两层操作区。
- 折叠与展开复用控制器持续维护的 `.is-overflowing`，因此操作行位置稳定；右侧元信息仍默认隐藏，只在消息 hover 或 `focus-within` 时显示并恢复点击。正文 / 图片继续跨完整两列并右对齐，容器保持 `fit-content` 且受 `min(660px, 92%)` 约束，左列可收缩、展开文字可省略，图片+长文本和窄宽不新增横向溢出。
- 短消息不命中新布局，继续使用既有绝对定位时间 / 复制元信息；折叠高度、渐隐、完整内容、展开页内状态、切会话复位及 `aria-controls` / `aria-expanded` / 键盘语义均未改变。背景 / 排队 / 执行轨迹、助手消息、问卷、Diff、工具和授权界面保持原样。

### 自动证据、Browser / 用户验收与边界

- 单一 footer、折叠 / 展开共用行、短消息隔离、hover / 焦点、图片+长文本和窄宽合同先稳定得到 `4 failures`，修正后长消息 / 普通消息定向组为 `14 passed`；阶段冻结时完整 `tests/test_frontend_modules.py` 为 `368 passed`。`npm run check:frontend` 的 build / freshness 与 bundle 语法、`node --check app.js`、`node --check src/ui/messages.js`、Python 语法及 Git diff 检查均通过。
- 审批任务在现有 3011 Browser 抽查真实溢出长消息的折叠 / 展开、hover 与键盘焦点，以及普通短消息：左下 toggle 和右下时间 / 复制保持同一水平行，右侧元信息显隐正确，短消息位置未变；用户随后明确 `PASS`。开发任务自身未操作 3010 / 3011 或内置 Browser。
- 本批次只修改 scoped CSS 和确定性前端测试，没有改变消息 DOM / 协议、Markdown、折叠阈值、持久状态、刷新 / 切会话语义或其他折叠组件。未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。
- `CODE-043` 继续保持开放；Markdown 排版对比度与渲染可访问性两个后续独立批次已获用户授权但尚未实施，必须分别通过后续 STANDARD relay 推进，本阶段没有自动启动其中任何一项。

## 2026-08-31 10:09 · Codex

### CODE-043 · 普通消息最终视觉与无障碍审计完成

- 普通用户消息（含图片+文本）与普通助手正文统一为 `16px`、`1.68` 行高，并适度收紧段落、列表和标题节奏；助手 `H1 / H2 / H3` 保持 `20 / 18 / 16px`，用户 Markdown 标题最高 `18px`，普通代码与表格单元统一为 `14px`。助手角色 / 模型为 `14px`，时间、用量和展开等辅助信息为 `12px`；普通消息内容列继续为 `760px`，用户气泡继续右对齐且最大 `620px`。
- 普通用户与助手复制按钮统一为 `28×28px`、SVG `16px`；用户元信息在 hover 与 `focus-within` 下均可见可操作。用户消息底部不再把消息间距和元信息占位相加，而以两者较大值形成 `30px` 跟随间距。长消息完整折叠 / 展开、宽表横向滚动、流式与阅读锚点保持原有所有权。
- 普通助手正文中的 `.path-file-card.answer-local-path` 文件名改为继承所属正文的字号与行高，用户验收样本 `Coding-Agent架构设计分析报告.md` 与同段正文层级一致；全局文件名和图片预览名称仍保持紧凑辅助尺度，路径识别、点击和 Markdown 协议未变。

### 自动证据、Browser / 用户验收与边界

- R001 的正文 / 标题 / 元信息 / 复制 / 间距合同先稳定得到 `5 failures`，修正后直接组为 `11 passed`；R002 的本地文件名继承合同先因 scoped 规则缺失失败，修正后定向组为 `8 passed`。阶段冻结时完整 `tests/test_frontend_modules.py` 为 `364 passed`；`npm run check:frontend` 的 build / freshness 与 bundle 语法、相关 JS / Python 语法和 Git diff 检查均通过。
- 审批任务在现有 3011 深浅主题 Browser 复核了普通纯文本、图片+文本、Markdown 标题 / 代码 / 宽表、长消息、用户与助手复制按钮、hover / 键盘焦点、约 `30px` 消息节奏及本地文件链接字号；工具过程、Diff 编辑建议、授权、问卷、生成图片和图片预览名称保持原层级。用户随后对包含 R002 文件链接修正的最终候选明确 `PASS`。开发任务自身未操作 3010 / 3011 或内置 Browser。
- 本批次只修改普通消息 scoped 样式和确定性前端测试，没有改变 DOM / ARIA、消息 / Markdown 协议、路径行为、流式锚点、API、持久化或特殊消息业务语义。未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。
- `CODE-043` 继续保持开放；普通消息最终审计已完成，第三阶段其余辅助表面与第四阶段桌面 / 窄屏、双入口一致性和可访问全面审计仍需用户另行选择并通过 STANDARD 门禁，本阶段未自动启动后续工作。

## 2026-08-31 09:01 · Codex

### CODE-043 · 编辑项目弹窗与侧栏文件区高度边界优化完成

- “编辑项目”弹窗宽度收紧为约 `560px`，使用 `14px` 圆角与 `22px` 内边距；标题 / 可见“项目名称”标签 / 输入文字分别为 `18 / 12 / 16px`。名称容器与输入行约 `40px`，静止使用中性边框，仅 `focus-within` 使用 accent；窄窗口通过卡片内部纵向滚动保证操作可达，横向溢出保持关闭。
- 源文件夹名称与常规操作文字为 `14px`，主文件夹等辅助标识为 `12px`；文件夹行与添加行约 `40px`，移除控件 `28×28px`，footer 按钮 `34px` 高 / `14px` 文字，删除仍独立在左，取消 / 保存保持在右。关闭、文件夹、添加、移除与删除全部复用现有 `uiIcon` 的 `close / folder / folderPlus / trash` 共享路径，统一为 `16px` / 约 `1.8px` 线宽，编辑弹窗内不再保留字符 `×`、emoji 或局部原始 SVG。
- 侧栏文件区的高度上限不再使用固定 `sidebar.clientHeight - 260`，而是以侧栏实时 `clientHeight` 减去 header、新建区、项目栏、splitter、side-footer 的实测外部高度和会话区既有最小高度；下限继续保留文件区 header + cwd 的既有最小空间。拖拽、窗口 resize、旧超限 `code-session-height` 恢复和折叠后重新展开共用同一夹紧并回写安全值，会话列表最小高度、文件树滚动与项目编辑业务语义保持。

### 自动证据、Browser / 用户验收与边界

- fail-first 的弹窗尺度 / 共享图标 / 无字符叉号合同，以及文件区真实高度预算合同为 `2 failed`；修正后直接组 `4 passed`。完整 `tests/test_frontend_modules.py` 首轮只余旧“index 内联 folder-plus path”测试未对齐共享 registry，改为验证 `folderPlus` 共享路径与投影后最终为 `358 passed`。`npm run check:frontend`、最终 `verify:frontend` freshness / bundle 语法、`node --check app.js`、Python 语法与 Git diff 检查通过。
- 确定性高度样本中，`sidebar=800 / fixed=194 / sessionsMin=80 / explorerMin=80` 时，请求 `2000 / 20 / 420 / 0` 分别夹紧为 `526 / 80 / 420 / 230`；`sidebar=420 / request=9999` 夹紧为 `146`，预留高度为 `274`。
- 审批任务在现有 3011 深色 Browser 核对了约 `560px` 卡片、`18 / 12 / 16 / 14px` 文字层级、`40 / 28 / 34px` 控件、共享 `16px` SVG 和输入静止 / 聚焦边框；折叠再展开后设置按钮距侧栏底部约 `10px` 并完整可见。审批自动化未成功执行真实 splitter 极限拖拽；用户在收到“手动拖到最上端与最下端，同时确认视觉”的明确验收要求后回复 `PASS`。本条不把该真实 drag 写成审批 Browser 自动实测。开发任务本身未操作 3010 / 3011 或内置 Browser。
- 本批次没有改变项目编辑 API / 数据 / 删除语义，会话或文件树业务逻辑，i18n 字典与持久化格式也未变。未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。
- `CODE-043` 继续保持开放；下一动作恢复为等待用户选择后另行审批的第二阶段普通消息最终审计，或其他尚未启动的独立 STANDARD 批次，本阶段未自动启动后续工作。

## 2026-08-31 08:30 · Codex

### CODE-043 · 项目侧栏标题、置顶与操作反馈优化完成

- 项目名称复用独立的标题 marquee owner：静止时只在真实溢出后保留右缘渐隐，名称悬停 `420ms` 后以固定 `24px/s` 线性滚动一次，按实际“新建会话”操作面左缘保留约 `4px` 可见终点；结束去 mask、移出复位与 `prefers-reduced-motion` 边界保持。完整项目根路径收入 toggle 的可访问名称，不恢复浏览器默认路径 tooltip。
- 项目置顶图标保持普通 flex 项，以 `13px` / `1.9px` 轮廓并旋转 `38deg` 统一视觉方向。项目行只保留右侧“新建会话”操作，按钮绝对覆盖而不占用标题静态宽度；原“更多”按钮及监听已移除，编辑 / 置顶继续仅由项目右键菜单提供。
- 可访问结构收敛为非交互 `.project-header` 容器，内部原生 `.project-toggle` 独立承载箭头、图钉、名称和 `aria-expanded`，“新建会话”为其同级真实按钮；原生 Enter / Space 展开收起，`Shift+F10` / `ContextMenu` 打开菜单，`Escape` 关闭并回焦 toggle。鼠标右键仍使用实际指针坐标，菜单在 append 后按实测尺寸以 `8px` 边界夹紧、靠底向上翻转，宽度保持内容驱动。悬停 / `focus-visible` 反馈改为由已定义且不透明的 `--text` 与 `--panel-2` 计算，深浅主题均可辨。

### 自动证据、Browser 验收与剩余边界

- R001–R003 的直接合同覆盖独立标题 owner、固定速率与真实操作面终点、项目图钉隔离、单一可见操作、菜单指针 / 键盘定位、视口夹紧、非嵌套交互语义与主题反馈；阶段冻结时完整 `tests/test_frontend_modules.py` 为 `356 passed`。`npm run check:frontend` 的 build / freshness、bundle 语法，`node --check app.js`、`node --check src/features/sessions.js`、Python 语法与 Git diff 检查均通过。
- 审批任务在现有 3011 Browser 核对 `.project-header` 为非交互容器、toggle 与新建按钮同级，深色菜单悬停 / 焦点反馈可见，并验证右键、`Shift+F10`、`ContextMenu`、`Escape` 回焦与鼠标点击路径。长项目名实测约 `24px/s`，终点距操作面约 `4.1px` 且末字完整；用户随后明确 `PASS`。开发任务本身未操作 3010 / 3011 或内置 Browser。
- 本批次未改变 server / API、项目持久化、排序 / 置顶业务规则、会话层交互、权限或协议。未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。
- `CODE-043` 继续保持开放：“编辑项目”弹窗的字号、图标与控件尺度优化仅登记为下一个待确认 STANDARD 子阶段，本批次没有启动该实现；普通消息最终审计和第三 / 第四阶段其余表面仍保留。

## 2026-08-31 01:56 · Codex

### CODE-043 · 会话侧栏状态、标题与置顶标识精修完成

- 会话列表收敛为单一权威状态位，优先级固定为等待回答、等待确认、等待 Skill 证据、运行中、上次运行失败、未读、空闲相对时间；`paused` / `cancelled` 不误判为失败，新运行可覆盖旧失败。空闲时间按内容宽度占位，图形状态保持 `14px`，标题与状态总缓冲为 `8px`。
- 真实溢出标题在静止状态常驻右缘渐隐；悬停 `420ms` 后以固定 `24px/s` 线性滚动一次，完成后移除渐隐，移出复位。滚动终点按悬停时标题尾部、标题容器与更多操作遮挡面的实际矩形一次测量，读取同一 `--session-more-cover-inset` 样式事实并保留 `4px` 安全间距；静止不溢出但悬停会被操作面遮住的临界标题只在悬停时滚动。`prefers-reduced-motion`、根级有界宽度刷新与既有点击 / ARIA / 会话切换所有权保持。
- 右侧更多操作由单一不透明主题表面同时遮住状态和按钮区域，按钮本体透明、无独立边框 / 阴影 / 色块，仍保留既有点击热区与 `focus-visible`。会话置顶图钉移入左侧 gutter，不再占标题 flex 宽度；会话专属图标为 `13px`、`1.9px` 轮廓并朝左下，置顶与普通标题起点一致。项目级图钉与共享 SVG 未变。

### 自动证据、浏览器验收与剩余边界

- R001-R010 的直接合同分别锁定状态优先级、失败投影、静止渐隐、固定速度、真实可见终点、操作面连续性、图钉隔离与方向；阶段冻结时完整 `tests/test_frontend_modules.py` 为 `353 passed`。`npm run check:frontend` 的 build / freshness、`node --check app.js`、`node --check src/features/sessions.js` 与 Git diff 检查均通过。
- 审批任务在现有 3011 深色页面复核用户样本“帮我生成一个二次元女孩图片”与长标题：滚动结束距操作面分别约 `4.2px`、`4.7px`，两者实测均约 `24px/s`，末字完整可见；标题 / 状态 `8px` 缓冲、无接缝更多操作面及朝左下图钉保持。用户随后明确 `PASS`，本批次无需再次请求复跑。开发任务自身未操作 3010 / 3011 或内置 Browser。
- 本批次只修改前端投影、局部样式、i18n、会话纯控制器与直接测试；没有改变 server / API、Session / AgentRun / Goal、持久化、权限、协议、会话菜单或标题内容。未读取、哈希、修改或暂存 `data/generated-assets/` 与 `data/image-route-registry.json`，未覆盖无关 `upstream-channel-formal-admission` handoff，未 push、tag 或 release。
- `CODE-043` 继续保持开放：下一分步为另行审批的 3B 会话侧栏操作反馈；第二阶段普通消息最终审计，以及第三 / 第四阶段其余辅助表面、桌面 / 窄屏、双入口一致性和可访问全面审计仍未完成，本阶段均未启动。
