# 2026-09-12 开发日志

## 2026-09-12 14:27 · DSH

### 修复：打包版与源码版启动序列分叉——收敛为共享后置步骤（含 launcher 与 dev 第三入口）

- **结构性根因（本机独立核验）**：打包版入口是 `launcher.py`（`build_exe.py:191` 传给 PyInstaller 的入口参数），它自建 `ThreadingHTTPServer` 与托盘，**从不调用** `server.run_server()`；源码入口 `run_server`（`server.py:31118`）只由 `server.__main__` 与测试调用。于是 `run_server` 里的「监听器就绪之后」序列在**安装版从不执行**——这是本日第二起同类缺陷（第一起是 launcher 不转发 `underlying`），根因是**打包版与开发版有两条分叉的启动路径，而测试只覆盖开发版那条**。
- **本起缺陷的直接证据（现场观察 + 本机独立核验）**：`_cleanup_old_update_images` 全仓唯一调用点在 `run_server` 内（旧 `server.py:31193`），因此 0.6.12 发布说明承诺的「旧版本 EXE 有界清理」在所有安装版里是死代码。本机只读核对：`~/.code` 仍保留 `Code-v0.6.9/0.6.10/0.6.11/0.6.12` 四个镜像，而 `update.log` 共 54 行、`cleanup` 出现 **0 次**；正在运行的 0.6.12 恰是唯一含该功能的版本 ⇒ 与审批侧判断一致。
- **逐项核实结论（读调用图，不按函数名推断）**：`_restore_update_jobs`（`def 14585`）、三项迁移（`16940` / `16990` / `17086`）、两项派生索引预热（`2042` / `2342`）与 `_cleanup_old_update_images`（`13295`）在打包路径上**均缺失**。其中：
  - 两项索引预热有请求期自愈触发（`2078`/`2093`/`2146` 与 `2375`/`2390`/`2437`），属幂等的派生缓存 ⇒ **纳入**；
  - `_restore_update_jobs` 默认 `start_workers=True`，会在启动期联网取 trusted descriptor（`12753`→`12763`）并后台续传未完成下载（`14713`），且兼容别名 `_complete_orphaned_parts`（`31089`）**全仓无调用者** ⇒ **不纳入**；
  - 三项迁移会移动/重写用户数据（会话文件移动到 `YYYY/MM/DD/`、`projects.json` 与全部会话元数据重写），且本机打包档两个 flag（`.codex_projects_migrated`、`.codex_project_roots_migrated`）**均不存在** ⇒ 启用即首次改写用户数据，**不纳入**，见私有 `CODE-093`。
- **共享函数形态**：新增 `server.py:31118 _startup_after_listener(owner, server_obj, *, frozen=None, owner_is_current=True)`——① best-effort 预热两项派生索引；② 以原语义计算 `service_ready`（owner 持有 + 监听器已绑定 + handoff owner 未变更）；③ `frozen and service_ready` 时 `_cleanup_old_update_images(_update_target_dir(), log_path=DATA_DIR/"update.log")`，异常一律吞掉。**唯一实现、三处调用**：`run_server`（`server.py:31220`，在「Code is running」打印之后、`serve_forever` 之前）、`launcher._main`（`launcher.py:392`，监听器绑定与 worker 线程启动之后、托盘之前）、`dev_server.run_dev_server`（监听器绑定与 `start_tray` 之后、`serve_forever` 之前），并移除了 dev 与 run_server 里各自重复的索引预热两行。唯一顺序变化：索引预热由 `run_server` 的 bind 前移到 bind 后（与打包路径一致）；**清理时序未变**（owner < tray < cleanup < serve）。
- **回归（只跑直接相关 + py_compile，未跑全量门禁）**：`tests/test_server.py` + `tests/test_dev_server.py` + `tests/test_bundled_skill_sync.py` + `tests/test_skill_default_enablement.py` **383 passed / 1 skipped / 53 subtests**；新增打包路径用例（走真实 `launcher._main` 替身流程）断言 `_cleanup_old_update_images` 恰好被调用 1 次、参数与顺序正确；新增反向用例覆盖「非 frozen 不回收」「owner/socket/owner_is_current 未就绪不回收」「预热与回收同时抛错也不影响启动与退出码」；dev 入口新增用例走**真实共享函数**（断言预热发生、非 frozen 下不回收）；原结构性守卫**加强**而非放宽（新增 launcher 侧调用断言与「调用位于共享函数内」的断言）。
- **证据等级分开标注**：本机独立核验＝代码/调用图/`update.log` 计数与 `~/.code` 目录清单（只读）；现场观察＝真机（第三方机器）上旧镜像仍在，本轮未直接核验该机。
- **未纳入清单（待产品决策）**：`_restore_update_jobs`、`_migrate_sessions_to_hierarchy`、`_migrate_codex_project_sessions_support`、`_migrate_project_root_paths` —— 一行未动，已登记为私有 `CODE-093`。

## 2026-09-12 14:10 · DSH

### 0.6.12 正式发布完成（发布提交 `ae23fc12`，标签 `v0.6.12`）

- **本机独立核验**：HEAD = origin/master = `ae23fc1243d5b2da9be87a8b9ad3018afcf522e8`，ahead = 0；标签 `v0.6.12` → `ae23fc12…`（远端 `git ls-remote origin refs/tags/v0.6.12` 同值）；发布提交 4 files +52/−8（`README.md` 2/2、`VERSION` 1/1、`docs/releases/v0.6.12.md` 44/0 新建、`file_version_info.txt` 5/5）；工作区仅剩三个既存受保护未跟踪项（`data/generated-assets/`、`data/image-route-registry.json`、`data/skill-store-v1/`）。
- **Release**：https://github.com/fhy-A/Code/releases/tag/v0.6.12 （`isDraft=false`、`isPrerelease=false`）；资产 `Code-v0.6.12.exe`，**33179009 字节**，`digest sha256:cf472212f5e9002aa0e6552b1435901fed9f6f9b2878bc1e2df2ff1cf5888a75`，state `uploaded`；与本地 EXE、prepared 凭证三方一致。下载地址 https://github.com/fhy-A/Code/releases/download/v0.6.12/Code-v0.6.12.exe。
- **门禁（11/11 CHECK_PASS）**：`git_diff_check`、5 项语法检查、前端 build / freshness / bundle syntax、`harness_replay`、`pytest_full`；其中 `pytest_full` **1509.469s（25:09）**，`build_exe.py` 60.734s，EXE 元数据 0.656s；`--reprepare` 墙钟约 **26 分 42 秒**（2026-09-12T05:28:32Z → 05:55:14Z）。
- **真机验证（证据等级＝用户报告）**：用户在其第三方机器（`C:\Users\Administrator\.code`，存在卡住的旧 64 位 journal 与旧长名 staging）上运行 prepared 候选（33179009 字节 / `cf472212…`）**正常启动**；**审批侧与开发侧均未直接核验该机器**，本条不表述为本机核验结果。
- **凭证**：`.git/code-release/v0.6.12.json` state = `published`，`publication.commit = ae23fc12…`，`lastCompleted = asset`，completedAt 2026-09-12T06:09:27+00:00；`baseline.head` 仍为 `5276d85b…`，artifact 摘要未漂移。
- **本周期真实范围（`5276d85 → ae23fc12`）**：① 应用内更新后新版本无法启动的修复（更新器三条语义恢复）；② 启动失败可见性与诊断（对话框与旁路日志写入底层 `errno`/`winerror`/`filename`，**含 launcher 打包入口透传**）；③ 较长 Windows 用户名下技能库启动失败（路径超 260，提交 `dca21e7`：暂存目录名与 operation id 解耦 + 就地迁移 + 管理通道寻址）；④ 夹具自检守卫按进程树归属收窄（提交 `cd276b2`，消除第三方桌面弹窗假阳性）。
- **证据等级分开标注**：本机独立核验覆盖发布提交、标签、远端资产摘要与凭证据绑定；"那台机器正常启动"仅为用户报告。

## 2026-09-12 13:28 · DSH

### 修复：夹具自检守卫改为"进程树归属"，消除第三方桌面弹窗假阳性（提交 `cd276b2`，**未发布**）

- **背景（本机独立核验）**：R006 的 `python release.py 0.6.12 --reprepare --yes` 在 `pytest_full` 阶段失败，耗时 **2261.8s（0:37:36）**，`1 failed, 3491 passed, 1 skipped`，失败项为 `tests/test_server.py::TestUpdaterHelpers::test_module_fixture_guards_are_installed_and_the_batch_is_clean`（`assertEqual(_visible_product_dialog_count(), 0)` → `1 != 0`）与 `TestConnectionCentricRouteApi::test_workbar_collector_honors_enabled_platform_token_ids` 的 teardown ERROR（同一原因：`fixture self-check failed: windows=1 processes=0 dirs=[] bats=[]`）。
- **根因不是夹具泄漏，而是守卫自身的假阳性**：`_visible_product_dialog_count()` 用 `EnumWindows` 统计**整个桌面**的所有可见窗口，命中条件为 `class == "#32770"` 或标题以产品标题开头。`#32770` 是 Windows 标准对话框类，因此**任何第三方程序的对话框**（游戏启动器、安装器、安全软件）都会被判成"我们泄漏的产品对话框"。实测被计数的窗口属于 `wegame.exe`（PID 28584，class `#32770`，title `no_active_wnd`，创建于 2026-09-12 12:13:10 本地）。反证不是夹具泄漏：`processes=0 / dirs=[] / bats=[]`，且该 guard 测试的前三条断言（对话框桩已装、`_DIALOG_GUARD` 非空、`mkstemp` 已被跟踪）全部通过 —— 产品自身的对话框出口在整个模块内已被 stub，`#32770` 只能来自测试进程之外。
- **修复（提交 `cd276b2`，`tests/test_server.py` +167/−15）**：归属判据收窄到**本测试进程及其后代**——新增 `_product_process_pids()`（`CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS)` 取进程→父进程映射并从 `os.getpid()` 闭包展开；失败或非 Windows 时退化为 `{os.getpid()}`，**绝不回退桌面级**）、`_enumerate_visible_windows()`（逐窗口取 pid/class/title，异常返回空表，保持原 "0 on any failure"），`_visible_product_dialog_count(*, owner_pids=None, windows=None)` 只在 **PID ∈ 本进程树**时进入 class/标题判定。两个参数仅作测试注入缝，生产调用不传参；`fixture_selfcheck: visible_product_dialogs=N` 打印语义与 `assertEqual(..., 0)` 断言均未放宽。
- **新增 5 项用例（全部通过，同组 6 passed）**：外部 PID 的可见 `#32770` → 计 0；本进程可见 `#32770` → 计 1（仍会失败）；外部 PID 的产品标题 → 不计；**屏外真实 `#32770` 窗口自证**（`CreateWindowExW` + `WS_POPUP`，位置 `-32000,-32000`，测试内 `DestroyWindow`）在**真实路径无注入**下断言 1 → 0；真起子进程验证后代 PID 在归属集合内。全模块 `tests/test_server.py` **310 passed / 1 skipped / 50 subtests passed in 33.86s**。
- **in-vivo 反证**：修复后的用例是在第三方 `#32770` 窗口**仍然存在**时通过的（探针与用例在同一轮执行中先后运行）；修复前同一断言正因该窗口失败。
- **教训（本轮重点）**：**夹具自检的判据必须绑定"由本测试进程创建/派生"这一可证明归属，不得使用桌面级或系统级匹配。** `#32770` 是 Windows 标准对话框类，任何第三方弹窗都会把 25–40 分钟的门禁判成失败并误导根因分析——本轮 R006 就是这样白跑 2261s，且上一轮我们还曾把 `windows=N` 误当成"夹具泄漏"证据。产品侧的同类检查（`server._running_code_image_processes` 叠加 `%TEMP%` 归属过滤）之所以正确，正是因为它有可证明的归属条件。同守卫其它判据已审查并如实记录：`_fixture_process_count()` 已是 `%TEMP%` 区域判据（保留；收紧为进程树会漏掉孤儿化残留 EXE）、`_fixture_temp_dirs()` / `_temp_update_bats()` 为快照差分 + 产品专有命名（保留）。
- **相关状态**：本轮**未跑完整门禁**；`--reprepare` 凭证仍为 `state=reprepare_pending`（3450 字节，未清理、未改状态）；`dist/Code-v0.6.12.exe.prep_r005`（33177588 / `24b8af7f…`）未被覆盖。

## 2026-09-12 12:42 · DSH

### 修复续：暂存目录名与 operation id 解耦并就地迁移（提交 `dca21e7`，**未发布**）

- **证据等级 A｜用户机器实测**：已发布候选 `Code-v0.6.12.exe`（33177588 字节，SHA-256 `24b8af7f…`）在 15 字符用户名 `Administrator` 的机器上**仍然启动失败**，`underlying=FileNotFoundError: errno=2`，filename 仍是 `…\.code\skill-store-v1\staging\op1_<64hex>\…\content\vendor\scripts\pptx_shapes\data\LICENSE-APACHE-2.0.txt`（**266 > 260**）。该 store 的 `transactions/` 与 `staging/` 目录名**都是旧 64 位 id** —— 说明 `9a204c3` 只缩短了“新建”的 id，**恢复既有旧 store 时暂存目录名仍沿用 68 字符**，因此新二进制也会失败，且失败在**恢复路径**。
- **证据等级 B｜本机独立核验（复现方法与原始输出要点）**：本机 `LongPathsEnabled=1`，无法直接复现，故用 `_MaxPath` 补丁把 `builtins.open` 与 `os.lstat/stat/rename/replace` 包装为“超过限值即抛 `FileNotFoundError(2, …)`”，并手工构造 legacy journal（`op1_<64hex>`）+ legacy staging 目录 + 长数据根，跑真实 `inspect_startup_state` + `bootstrap`；同一脚本只切换产品代码：
  - 修复前（`9a204c3^` 即 0.6.11 模块）：首个越界路径 **263** 字符，位于 `staging\op1_b4e2…\objects\sha256\5e\…\content\vendor\scripts\pptx_shapes`，平台抛 `FileNotFoundError errno=2`，被 store 包装为 `SkillStoreError(store_path_unsafe)`；
  - 修复后（同一磁盘布局）：`inspect_startup_state -> staged-verified`、`bootstrap -> RECOVERED`、`BLOCKED 0 paths`。
  - 真机与复现的差别仅在于“第一个越界访问”落在该暂存子树中的深浅（263 vs 266），**属同一缺陷**。
- **同时发现并一并修掉的两条独立越界**：
  ① `_atomic_json` 临时文件名内嵌完整 operation id（`.<name>.<68字符id>.<32hex>.tmp`，实测文件名 **180**（journal）/ **120**（registry）字符，在 81 字符 store 根下即 **261** 字符路径）→ 改用 `_operation_key`（**132 / 72** 字符）；
  ② `skill_store_management.py` 有 **5 处**仍以完整 op id 直接拼暂存/临时路径（`_stage_check`、`inspect_managed_state`、`_cleanup_terminal`、`_claim_prepared_temps`、`_clean_temps`），主修复后它们会让管理恢复/清理**继续失败**（10 套相关用例曾出现 **57 项失败**）→ 全部改走 store 的寻址解析。
- **改动文件与行数**：`code_runtime/skill_store.py` +67/−9；`code_runtime/skill_store_management.py` +16/−8；`tests/test_skill_store.py` +183/−1；`tests/test_skill_store_management_recovery.py` +2/−1（合计 268/19）。提交 `dca21e7`（4 文件，未 push）。
- **实测长度**：用户布局 staging 目录 **120 → 72** 字符、上报文件 **266 → 218**；本机复现布局最深 content **291 → 243**；仓库内最长内置资源（78 字符相对路径）**290 → 242**（`291→234` 不是实测值，实测为 243）。规律：暂存目录名一律少 **48** 字符（68 → 20）。
- **新增四项长路径用例（全部通过，同组 9 项通过）**：① legacy journal + 长数据根 → 恢复成功；② legacy staging 残留 + 长数据根 → 启动不被 brick；③ 迁移幂等，重复启动成功且**不重复 bootstrap**（receipt 的 operationId 保持旧值）；④ 注入“迁移 rename 失败”（仅对暂存脚手架 rename 注入 OSError）→ 启动仍成功。
- **教训（本轮补记）**：**兼容用例必须覆盖路径长度**。上一轮的 `test_legacy_operation_ids_stay_readable_and_recover_in_place` 使用 pytest 默认短 `tmp_path`（约 70 字符），路径从未接近 260 —— 它验证的是旧 id“可读/可恢复”，**没有覆盖路径长度**；新用例把 `_MaxPath` 限值校准在旧长名与短名布局之间（`limit = len(legacy_path) - 1`，并断言 `legacy > limit >= short`），因此与 checkout 实际路径长度无关，在 260 语义下必然触发边界。
- **回归面（本机）**：`tests/test_skill_store.py` **111 passed**；10 套直接相关用例 **348 passed**；全部 27 个 `test_skill_*.py` **749 passed / 2 failed**（1044.29s）；`git diff --check` 通过。
- **如实声明的门禁前状态与既存失败**：本阶段**未跑完整门禁**。上述 2 项失败为 `tests/test_skill_registry.py::TestSkillShadowServerIsolation` 的两项（`test_disabled_endpoint_preserves_existing_not_found_behavior`、`test_enabled_endpoint_is_read_only_audit`），失败点 `server.py:27240 _guard_legacy_skill_http` —— 因工作区存在未跟踪的 `code/data/skill-store-v1/`（最后写入 2026-09-06，**早于本阶段**）使 `has_store()` 为真而提前返回 `management_client_upgrade_required`；把 `CODE_DATA_DIR` 指向空目录后该文件 **29/29 全通过**。本阶段**未修改** `server.py` 与该测试文件，**未放宽任何断言**。若重新跑门禁时再次出现，按 release-guide 既有处置停下并如实附原始输出。
- **兼容/迁移与回退**：持久化 `operationId` 一律不改写（receipt 的 operationId 参与 `_state_hash`/`registryHash`）；journal 与 registry 字节不变；已实测持久化文件（`root.json`、`registry.json`、`transactions/op1_*.json`、`registry.lock`）字节中**不含 `staging`**，证明暂存目录名从未被持久化，`os.rename` 安全。`inspect_startup_state` 现在唯一可能的写操作就是这个受 `write_enabled` 约束的 best-effort rename。回退：代码 `git checkout` 回退即可；对已 committed 的 store 无影响，若回退时恰有 mid-flight 旧 journal 而暂存目录已被改短名，旧代码会报 `bootstrap_recovery_capture_missing`，此时先完成恢复、或把 `staging\<短名>` 手工改回 `staging\<68字符id>`（一次 rename，不改任何持久化文件）。
- **后续**：`dist/Code-v0.6.12.exe` 的 sealed prepared 凭据因产品代码变更**必然失效**，需重新 `--prepare` 并重新封印；本阶段未做任何 `release.py` 动作、未重建产物、未 push/tag/Release。

## 2026-09-12 00:58 · DSH

### 修复：第三方机器技能库启动失败＝Windows MAX_PATH 溢出（提交 `9a204c3`，**未发布**）

- 根因（用户机器实测 + 本机独立核验，证据分级见同日 00:52 条目）：技能库暂存区目录名 `op1_<64hex>` 使完整路径达 **266 > 260**，真机报 `FileNotFoundError: errno=2`。
- 修复（`code_runtime/skill_store.py`，+91/−23）：
  ① `_operation_id()` 改为 **16 位派生形式**（`op1_<16hex>`；同一输入仍是同一 id，**幂等性不变**），并新增 `_operation_id_legacy()` 保留 64 位旧形式；
  ② 三处读取**双格式兼容**：`_OP_ID`（已持久化 receipt 的 operationId）、`_STORE_OP_ID`（transactions/staging 目录名）、`_TEMP`（临时文件名）；新增 `_operation_key()` 归一化为「前缀 + 前 16 位」用于比较；
  ③ `_normalize_journal` 接受新旧任一 id；`_build_target(..., operation_id=…)` 在恢复旧 journal 时**沿用该 journal 的 id** —— 否则重建 target 会换成新 id 并触发 `bootstrap_recovery_source_conflict`，这是「升级即坏」的关键一环；
  ④ 旧 staging 残留容错：`_inspect_layout` 不再因暂存目录数量 >1 失败；`inspect_startup_state` 的活动/已提交两分支都容忍合法命名的残留；`_bootstrap_from_catalog_loader` 改为 **best-effort 回收**（`_discard_stale_staging`：只回收合法命名且无对应 journal 的目录，任何异常都吞掉、**绝不影响启动**）；`_clean_temps` 亦按归一化 id 比较；
  ⑤ `objects/sha256/xx/<64hex>/content` 落盘布局**未改**（留待二期）；启动语义与失败闭合未变。
- **实测新路径长度**（15 字符用户名 `Administrator`）：报告案例 **266 → 218**；本仓库最长内置资源相对路径（78 字符）**290 → 242**；两者都 ≤ 260，**不再依赖 `LongPathsEnabled`**。
- 用例（`tests/test_skill_store.py` 新增 6 条，全部通过）：① 短 id 幂等与双格式正则；② **旧 64 位 operationId 的 store 仍可读并原地恢复**（receipt id 保持旧值、不重跑 bootstrap）；③ 旧 staging 残留不 brick 启动并被尽力回收；④ **回收失败时启动仍成功**（patch `_remove_safe_tree` 抛 OSError）；⑤ **边界 258/259/260 精确断言**（不依赖本机用户名，并用旧 id 精确复现 266 > 260）；⑥ 最长内置资源在 15 字符用户名下 ≤ 260 的上界断言。
- 回归：`tests/test_skill_store*.py` + `test_skill_runtime_startup.py` **247 passed**（145.46s）；`tests/test_server.py -k "skill or startup or immutable or handoff or launch"` **36 passed / 270 deselected**；`py_compile` 通过。**未跑完整门禁**（按本阶段口径，门禁与发布另行下发）。
- 提交：`9a204c3`（`code_runtime/skill_store.py` 与 `tests/test_skill_store.py`，2 文件 +197/−23）；本条日志与索引随后单独提交。发布说明与版本文件**未提交**（按口径留给后续 `--prepare`）。

## 2026-09-12 00:52 · DSH

### 第三方机器技能库启动失败＝Windows MAX_PATH 溢出（根因确认，**仅记录、未修复**）

- **证据等级 A｜用户机器实测**（用户报告，非本机核验）：在**修好 launcher 透传后**的 `Code-v0.6.12.exe` 上，对话框与旁路日志第一次给出底层原因：
  ```
  underlying=FileNotFoundError: errno=2
  filename=C:\Users\Administrator\.code\skill-store-v1\staging\op1_48b02cdcacb27a836fda8fbd93dff6049546beab549a3abb96c102b4d4c37c15\objects\sha256\6a\6aeacaa3728766cdb76d16dba3723f060aaadde5d9593c8ec46a46d7f8fbe98f\content\vendor\scripts\pptx_shapes\data\LICENSE-APACHE-2.0.txt
  ```
  这同时说明：**诊断增强在真机上第一次生效，并直接定位到根因**（`c1b62b0` 的输出能力 + R003 修好的 launcher 透传，两者缺一不可）。
- **证据等级 B｜本机独立核验**（开发任务实测）：
  1. 该路径长度 **266 > 260**（传统 MAX_PATH 上限），超出 6 个字符；
  2. 把 `Administrator` 换成本机 profile 名 `Admin` 后为 **258 ≤ 260**，差值恰 **8** —— 所以**本机永远复现不出来，用户机器必现**；
  3. 长度分解（本机实测，求和 = 266）：`C:\Users\Administrator\.code\` 29 + `skill-store-v1\` 15 + `staging\op1_<64hex>\` **77** + `objects\sha256\xx\<64hex>\content\` **91** + 技能包相对路径 54；即**脚手架占 212/266**，真正属于技能包的只有 54 字符，其中两段 64 位 hex 合计 128 字符。
  4. 本机 `LongPathsEnabled = 1`（长路径已启用）→ 本机不受影响；用户机器按推断为默认 0。
  5. 相关代码位置：`code_runtime/skill_store.py:26` `_OP_ID`（校验已持久化的 `operationId`）、`:27` `_STORE_OP_ID`（校验 `staging/` 下目录名，`:205` 不匹配即 `store_layout_unknown`）、`:86` `_operation_id`、`:358-360` `_object_path(..., staging=...)`。
  6. 一处与审批侧口径的差异（如实记录）：审批侧给出「脚手架 204 + 包 55」，二者之和为 259，与实测总长 266 不符；按本机分解为**脚手架 212 + 包 54 = 266**（212 = 44 前缀 + 168 脚手架）。
- **影响面**：老路径在 `C:\Users\<较长用户名>\` 下普遍可能超限——`Administrator`、域账号、中文用户名都可能中招。这是**真实产品缺陷**，不是环境问题。
- **本轮状态：仅记录，未修复、未改任何代码**。修复方向与必须先回答的兼容/回退问题（缩短暂存区目录名 `op1_<64hex>` → `op1_<16hex>`；`_OP_ID`/`_STORE_OP_ID` 对旧 64 位格式的兼容；旧残留目录清理不得让启动失败；是否压缩 `objects/sha256/xx/<64hex>/content` 层级；失败路径与回退；一条能在 260 以内必然复现该边界的用例）已登记到私有待办，**待用户确认方案后再实施**。

## 2026-09-12 00:09 · DSH

### 已排除「第三方机器 `.code` 目录只读导致启动失败」的假设（**本机独立核验**）

- 背景（用户报告）：第三方机器 `C:/Users/Administrator/.code` 的启动错误日志中出现 9 次 `skill_runtime_startup_io_failed`；用户怀疑该目录/文件的「只读」属性是启动失败原因。
- **结论：该假设已排除。** 依据（**本机独立核验**，与第三方机器现场的用户报告分开标注）：
  1. `C:\Users\Admin\.code` 整棵树 **10912 个条目中带 R（只读）属性者 0 个**；
  2. 隔离探针实证：对标记了 R 的目录，**新建文件、新建子目录、删除文件全部成功**——Windows 的目录 R 标志对文件系统写入**无约束**；
  3. 更直接：那台机器既然能写出 **42 行** `.code.startup-error.log`，本身即证明该目录**可写**；
  4. Explorer 该复选框的标签本身就写着「仅应用于文件夹中的文件」，属装饰性遗留标志。
- **意义**：该假设被否掉后，`skill_runtime_startup_io_failed` 更可能来自权限（ACL）、安全软件（含受控文件夹访问）拦截或异常条目；后续定位依赖本阶段已实现的诊断增强输出（`underlying=… filename=…`，见本日 immutable-skill 诊断条目与提交 `c1b62b0`），无需再重复调查「只读属性」这一路径。

## 2026-09-12 00:02 · DSH

### i18n 口径定稿：原生对话框文案暂不抽模块（护栏二改为可判定触发），当前基线＝正文 3 条

- 本阶段**定稿（审批侧）**：**现在不抽模块、不新增文件**。理由：护栏二是预防性规则、不是用户需求也不是缺陷；抽模块属零行为变化的纯搬迁，对用户零可见价值，而代价真实（新增一个仓库文件作为模块边界、与 `server.py` 形成导入耦合、再配一条结构性测试）；按项目约定，阶段收口时不得静默扩大范围。
- **护栏一仍有效且当前满足**：成对中英双语只由 `server.py:12960 _startup_occupancy_guidance()` 这一个命名 helper 构造，无第二处语言分支，且两段语义等价。
- **护栏二口径收紧为可判定触发**（取代原「≥3 条」）：以**原生对话框正文／消息条数**计数，**当前＝3**；当新增文案使正文**达到 4 条**时，**必须**抽到 `code/startup_messages.py` 再维护，届时由审批侧在阶段开始一并授权该新增文件与配套测试；**标题不单独计数**（与所属正文同属一个对话框）。
- 本轮**不做任何代码改动**；将来任何新增原生对话框文案，仍须集中在单一命名 helper 内，且中英两段语义等价。

## 2026-09-12 00:00 · DSH

### 教训正例：测试脚手架不得降低对产品的检查强度（并复核 immutable-skill 诊断增强已落地，**未发版**）

- **审批侧单独点出并接受的一条自catch**：批次自检首跑抓到 1 个未回收的临时 `.bat` 后，我不仅修「回收」，还发现**自己的跟踪方式本身有害**——原先以「替换 `server._build_update_script`」实现跟踪，使 `inspect.getsource(server._build_update_script)` 返回包装函数（实测 `WRAPPER_INSPECTED=True`），从而**削弱**了 `test_update_script_is_diagnosable_and_never_touches_other_images` 这类依赖源码的不变量检查。随后把跟踪点移到**创建点**（包装 `tempfile.mkstemp`，只记录 `prefix="code-update-"`），复测 `WRAPPER_INSPECTED=False`、`REAL_BUILDER_INSPECTED=True`，并用结构性断言把「更新脚本工厂未被替换」固定下来（提交 `b9c57b3`／`07daa0c`）。
- **教训（记为本次事故的正例）**：**测试脚手架不得降低对产品的检查强度**。为把某个副作用收干净而替换产品函数，代价可能是让既有检查退化为空转——这比「没回收文件」更危险，因为它让绿灯失去意义。今后凡为夹具替换产品对象，必须先确认不存在依赖该对象真实身份的检查（源码检查、签名、`__module__` 等），并在测试里显式断言其未被替换。
- **附带复核（提交 `c1b62b0`）**：immutable-skill 启动失败的诊断缺口已补齐——`server._underlying_startup_error()` 把底层 `errno/winerror/filename/error` 写入**对话框正文**与**旁路日志**（`detail=<code> underlying=<…>`）；**未改** `code_runtime/skill_runtime_startup.py`（其 `raise … from cause` 早已保留异常链），**未改启动语义、未把该失败降级**（仍 `return 1`）。注入验收（真实 owner + 真实 `except OSError` 分支）：`RUN_SERVER_RESULT=1`、`LISTENER_CALLED=False`，对话框与旁路日志均含 `errno=2 filename=…\skills\registry.json`。
- **补充事实（用户报告）**：用户确认那台第三方机器 **C 盘剩余 41.2 GB / 共 125 GB，磁盘空间不是原因**；因此那 9 次 `skill_runtime_startup_io_failed` 更可能来自权限、安全软件拦截或异常条目。增强后的诊断输出带 `filename=…`，下一次失败可直接定位到具体路径与操作，不必再靠推断。
- 数字口径更正（审批侧指出）：我先前提「本阶段累计 9 个提交」有误，以 `git rev-list --count origin/master..HEAD` 为准——当时为 10，当前为 13（含后续证据与诊断提交）。

