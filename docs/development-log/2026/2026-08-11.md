# 2026-08-11

## 2026-08-11 14:24 · Codex

### Harness H4-8E 问卷等待态与单项队列刷新顺序收口

- H4-8E 已为固定 required single-choice 问卷与唯一纯文本 queue follow-up 补齐真实浏览器组合证据，专题见 [H4-8E 问卷等待态与单项队列刷新顺序](../../harness/h4-8e-questionnaire-queue-refresh-order.md)。本阶段只修改隔离假上游和 H4 浏览器测试，不是生产修复，也没有改变 API、协议、Session 格式、持久化或队列算法。
- 主 AgentRun 处于 `waiting_user_input` 时，真实 composer 的一次 `Control+Enter` 只 enqueue 一条消息，`/steer` 为 0；等待态完整刷新保持同一主 Run、问卷 `requestId`、queue id、`clientRequestId` 与 pending 顺序，AgentRun/Runtime/chat、`/input`、`/resume` 和 promotion 的业务增量均为 0。
- 用户真实选择 B 并确认一次后，`/input`/`/resume` 为 `1/1`；主 Run 的终态 checkpoint 清理后，queue 才唯一提升一次并创建第二个 AgentRun。终态为 2 个 AgentRun、3 个 Runtime、3 次 upstream chat，enqueue/promotion 为 `1/1`。问卷原生 durable interaction execution 恰好为 1；registered tool delegation/execution 为 `0/0`，两类计数没有混同。
- 主 Run 精确 12 个事件，queued Run 精确 4 个事件，Runtime cursor 为 `[3,3,3]`。主任务第二轮模型请求不含 queued marker；queued Run 请求中 questionnaire user、主 final 与 queued user 顺序稳定，且主 final 位于 queued user 之前。
- 终态 Session 八条消息顺序为 initial user → tool owner → questionnaire call → input summary → questionnaire result → main final → queued user → queued final；DOM 六段顺序为 main user → tool process → input summary → main final → queued user → queued final。终态完整刷新保持两 Run、三 Runtime、Session/DOM 与 queue 身份，业务重放增量为 0。
- bundle、direct classic 与两轮完整 H4 门禁的 12 项冻结哈希一致；相关回归为 `415 passed, 247 subtests passed`，H4-8A/B 邻接为 `4 passed (14.3s)`。两轮 H4 smoke 矩阵均为 `63 passed (3.7m)`、单 worker、零 retry、各 63 条 cleanup；第一轮由标准入口先行运行 infra，第二轮先独立通过 infra 再调用内层 smoke。完整 pytest 为 `1126 passed, 751 subtests passed, 3 warnings in 97.39s`；replay 保持 `17/124/25/25/4` 与哈希 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。
- 第一轮完整 H4 曾因外层 npm 参数未传入内层 Playwright 而使用默认 output，五项既有 ignored questionnaire 失败诊断已无法恢复，用户已接受；第二轮改用专用 output，默认 output 未再变化。该诊断损失不改写为“仍保留”。
- H4-8E 关闭第一轮完成线中的“问卷等待态 + 队列刷新顺序”固定证据项，但 Harness 第一轮仍未完成：授权请求失败后的安全重试、`/parallel` 编辑授权恢复只读审计/条件实施，以及最终文件树上的整体验收仍待后续独立阶段。
- 证明边界只覆盖单问卷、单队列项、单标签页/actor 与同进程刷新；不外推到多队列、optional/mixed 新组合、取消/编辑答案、失败重试、parallel/background、服务重启/跨进程、真实模型/外网、Firefox/WebKit、主观视觉或通用 exactly-once。

## 2026-08-11 12:00 · Codex

### workbar 文档仓库公开、开发部署手册与提交归属纠正完成

- GitHub 仓库 `https://github.com/fhy-A/workbar-docs` 已按用户确认从私有切换为公开，默认分支仍为 `main`；公开状态经 GitHub API 实时核对，未修改仓库内容、Actions、服务器、DNS、证书、Nginx 或 workbar 线上后台配置。
- 公开后，普通开发无需仓库写权限即可读取源码，并可通过 Fork、分支和 Pull Request 协作；仓库当前尚未配置 `main` 分支保护或 Ruleset，是否启用“必须 PR、必须通过 Check and package、禁止 force-push/删除”等规则继续由用户确认后实施。
- 在 `../workbar-docs/DEVELOPER_DEPLOYMENT.md` 新增面向服务器开发的一页式部署交接手册，并从根 `README.md` 建立入口。手册固定了“已进入 main 的完整 SHA + main push 成功 Actions + 同 SHA Artifact/部署脚本”发布契约，明确 PR Artifact 不得上线。
- 手册将执行边界收口为：可信开发机下载、验签、确认 SHA 属于 `origin/main`，只上传 `dist/` 和同提交 `deploy/` 到 VPS；服务器显式传入实际 Nginx 组后原子发布。后续内容更新无需 reload Nginx 或重启 New API，回滚只切换 `/srv/workbar-docs/current`。
- 手册同时列出首次 DNS/TLS/Nginx 部署、Artifact 30 天过期处理、发布后状态码/缓存/安全头验收、回滚和开发回传证据。`npm --prefix preview run check`、相对链接、敏感信息扫描和 `git diff --check` 通过。
- 首次提交曾因本地 Git 邮箱归属到另一 GitHub 账号而显示错误作者。经公开提交记录确认用户指定邮箱归属 `fhy-A` 后，仅在 `workbar-docs` 仓库设置本地作者信息；以保留文件树和提交顺序的方式重写两条提交，并使用带精确旧 SHA 的 `--force-with-lease` 更新 `main`。当前根提交为 `1482fa0 docs: 建立 workbar 官方文档仓库`，部署手册提交及远端 `main` 为 `c23bbd8 docs: 添加开发部署交接手册`；GitHub API 已确认两条提交的作者与提交者均映射到 `fhy-A`。
- 新 `main` 的 GitHub Actions 运行 `31457002313` 已成功，Artifact 为 `workbar-docs-c23bbd827a0492d90ac23a67bd5873d11dc45a08`；旧 SHA 及旧构建产物不再作为部署依据。服务器、DNS、证书、Nginx 和 workbar 线上后台配置仍未操作。

## 2026-08-11 11:35 · Codex

### Harness H4-8A～H4-8D 与第一轮完成线阶段补账

- 本条中央日志按已提交专题与 Git 提交补记 Harness 阶段事实；其中 H4-8A 的实际提交日期为 2026-08-09，本次只补齐中央日志，不将其记作 2026-08-11 实施。
- H4-8B、H4-8C、Harness 第一轮完成线与 H4-8D 已在 2026-08-11 分别形成独立本地提交。当前只说明这些已提交阶段及其验收边界，不把后续只读方案写成完成，也不宣称 Harness 第一轮已经完成。

### 补记 H4-8A：单题问卷刷新恢复

- 提交：`b8349ade4ed45eadebd9f2bef9f03bfb3e3c62fc`，实际提交日期为 2026-08-09；专题见 [H4-8A 问卷刷新恢复](../../harness/h4-8a-request-user-input-refresh-resume.md)。
- 固定 required single-choice 问卷在 `waiting_user_input` 时完成整页刷新，恢复同一 AgentRun、唯一待答请求、Session 与 DOM；真实选择 B 后只提交一次 `/input` 和一次 `/resume`，同一 AgentRun 进入第二个 Runtime 并完成，终态刷新不重放模型、交互或提交副作用。
- 生产修复只在 `finishServerAgentUserInputRequest()` 已有的 awaited Session 保存边界增加 `persistMessages:true`，使已追加的 `user-input-summary` 与 runState 同边界耐久；没有新增 API、协议、schema 或迁移。
- 对应最终文件树下，bundle/direct classic 九项语义哈希一致；连续两轮标准 H4 各 `53 passed`，单 worker、零 retry；定向回归为 `371 passed, 75 subtests`，完整 pytest 为 `1131 passed, 751 subtests`。
- 该阶段只覆盖固定单题 required single-choice，不证明混合问卷、队列组合、请求失败重试、服务重启或跨进程恢复。

### H4-8B：混合问卷渐进恢复

- 提交：`68d8165cc27695d7e3f13fe7b68341c6f42bc058`；专题见 [H4-8B 混合问卷渐进恢复](../../harness/h4-8b-mixed-questionnaire-progress-refresh.md)。
- 固定三题 all-required 问卷依次覆盖 single B、multiple A+C+Other 与 text marker；Q1/Q2 的已确认进度耐久，Q2 完成且 Q3 尚未填写时整页刷新只恢复唯一 Q3，最终仍只有一次 `/input` 和一次 `/resume`，同一 AgentRun 完成后终态刷新零业务重放。
- 该阶段复用 H4-8A 的 begin/complete/reload transport 原语，但使用独立的领域投影与九项哈希；H4-8A 旧哈希未重基线。没有生产、协议或持久化格式变化。
- 对应最终文件树下，bundle/direct classic 九项语义哈希一致；连续两轮标准 H4 各 `55 passed`，问卷/Agent/route/frontend 定向为 `9 passed`，完整 pytest 为 `1131 passed, 751 subtests`，并保留既有损坏 TIFF 负测警告。
- 该阶段不覆盖 optional/cancel/back-edit、未确认草稿、队列组合、失败重试、服务重启或跨进程恢复。

### H4-8C：编辑授权刷新恢复

- 提交：`282636a9297054a47db311b2e47c73f2d92b6450`；专题见 [H4-8C 编辑授权刷新恢复](../../harness/h4-8c-edit-authorization-refresh-resume.md)。
- 固定单一 `propose_edit` 在等待授权时整页刷新；真实 approve/reject 各一次，均保持同一父 AgentRun 继续并在终态刷新后零业务重放。approve 分支的 production apply/write/backup 为 `1/1/1`，文件从 initial 变为 target；reject 分支为 `0/0/0`，文件保持 initial。
- 生产修复只给前台 waiting save 与 decision save 两个既有 Session 保存边界增加 `persistMessages:true`；background 控制流、授权协议、JSONL schema 与迁移均未改变。
- approved/rejected 各九项冻结哈希在 bundle/direct classic 间对等；定向回归为 `425 passed, 247 subtests`，H4-8A/B 邻接四例为 `4 passed (26.5s)`，连续两轮标准 H4 均为 `59 passed (3.7m)`，完整 pytest 为 `1131 passed, 751 subtests`。
- replay 基线保持 `17 fixtures / 124 events / 25 checkpoints / 25 checkpoint recoveries / 4 explicit recoveries`，哈希为 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`。
- 该阶段只覆盖固定单 proposal 的前台 approve/reject，不证明陈旧冲突、授权失败重试、多 proposal、并发 actor、background 或 `/parallel` 授权恢复。

### Harness 第一轮工程完成线

- 提交：`908fa26d2b807a823abd959bd6ffc541ebdba2ad`；专题见 [Harness 第一轮完成线](../../harness/harness-round-1-completion-line.md)。
- 完成线把第一轮限定为有限、可自动验收的工程边界，并将 H4-8A/B/C 作为后续组合证据的既有基线；文档本身不构成“第一轮已完成”的声明。
- H4-8D 已在随后提交中关闭其中一个必选项，但问卷等待态与队列刷新顺序、授权失败安全重试、`/parallel` 编辑授权恢复审计以及所有新增项完成后的最终集成门禁仍未闭合。

### H4-8D：批准后的陈旧编辑冲突保护

- 提交：`0e349e3d34264263567d1297732552af5f1f587e`；专题见 [H4-8D 批准后陈旧编辑冲突](../../harness/h4-8d-approved-stale-edit-conflict.md)。
- 该阶段是对生产既有 stale-edit 冲突保护的真实浏览器证据补全，不是生产修复。固定单文件、单 proposal 在等待授权刷新后，由测试控制面把 initial 精确转换为 third-party 一次；用户真实决定仍是 approved。
- production apply 调用一次，但 raw result 为 `ok=false`、`action=apply_edit`、`conflict=true`、`applied=false` 且 `currentMtime>0`；`rejected`、`replayed` 与 `backupPath` 字段缺失。当前 UI/Session 的 `rejected=true` / `is-rejected` 只表示“批准后冲突、未应用”，不等于用户拒绝。
- production apply/write/backup 为 `1/0/0`，third-party SHA 在批准、冲突、父 Run 终态和终态刷新后保持不变。场景固定为 1 个 AgentRun、2 个 Runtime、2 次 upstream chat，authorization/resume 各一次，12 个 AgentRun 事件与 Runtime cursor `[4,3]`；冲突原因仅在真实展开 trace→stage→item 后可见，并在终态和刷新恢复后各精确出现一次。
- H4-8D bundle/direct classic 十项冻结哈希对等，H4-8C 十八项旧哈希未漂移。冻结单例、低层冲突测试与邻接 H4-8A/B 分别为 H4-8D bundle `1 passed (6.4s)`、classic `1 passed (5.0s)`、低层 `10 passed in 1.71s`、邻接 `4 passed (14.3s)`。
- 当前 H4-8D 最终文件树下，标准 H4 连续两轮为 `61 passed (3.6m)` 与 `61 passed (3.5m)`，均为单 worker、零 retry、infra 先行且每轮 61 条 cleanup；严格 `tests` 目录完整 pytest 为 `1126 passed, 751 subtests passed, 3 warnings in 95.98s`，三条 warning 均来自既有损坏 TIFF 负测。这里的 `1126` 是明确的 `tests` 目录口径，不与 H4-8A/B/C 阶段的 `1131` 统计混算。
- replay 继续保持 `17/124/25/25/4` 与哈希 `166a8141c50e8cf17748a04e2b6aa994323c563e9d8d22d3aa4f6d17682030c2`；frontend、Node/Python 语法、diff 与资源清理门禁通过，临时环境恢复，既有诊断保留。
- 该证据只证明隔离项目内固定单文件、单 proposal、一次受控第三方转换和 approved stale conflict；不证明任意文件或内容、多 proposal、并发 actor、多标签页、mtime-only/hash-only 冲突、changed-to-target replay、失败重试、崩溃窗口、服务重启/跨进程、background/queue/`/parallel`、真实外部编辑器/模型/网络/凭据、Firefox/WebKit、专属冲突 UX、发布或 OS syscall exactly-once。

### Harness 第一轮剩余门禁

- 补齐“问卷等待态 + 队列刷新顺序”的固定、隔离、自动浏览器证据；复用 H4-8A/B 与现有 queue 证据，不重建问卷状态机，也不重基线旧哈希。
- 补齐授权请求失败后的安全重试，证明不会重复 decision、resume、apply、write 或 backup；如果现有入口不能自动证明且需要新的产品语义，必须先停在方案门禁。
- 对 background / detached `/parallel` 编辑授权恢复完成只读价值与可行性审计；仅在不引入新产品决策、不扩大协议、持久化或安全边界且无需人工验收时实施，否则把实施明确延期且不阻塞第一轮。
- 上述新增项完成后，再执行最终 bundle/direct classic 对等、标准 H4 连续两轮、相关回归、完整 pytest、Harness replay、frontend/build/语法/diff、资源归零、专题文档与独立本地提交总门禁。H4-8D 当前的两轮矩阵是已提交阶段基线，不替代未来最终文件树上的整体验收。

### 本次补账边界

- 本次只把已提交的 Harness 阶段事实写入中央开发日志并补充索引，没有修改生产代码、测试、协议、持久化格式、统一 TODO 或既有 2026-08-09 日志。
- 当前 Harness 第一轮仍未完成；本条不把正在讨论或只读审计中的后续方案记为完成，也不外推到真实模型、外网、凭据、其他浏览器、主观视觉/无障碍验收或通用副作用 exactly-once。
