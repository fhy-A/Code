# 2026-08-22

## 2026-08-22 13:22 · Codex

### Gemini-GCP Goal 工具 schema 兼容修复

- **根因**：Code 的 `goal_revise_plan` 工具在参数对象上使用 `anyOf` 表达 objective/steps 至少一项。ApiNebula-Gemini-Antigravity 可以接受该 OpenAI 工具 schema，但 ApiNebula-Gemini-GCP 转交 Google `functionDeclarations` 后先拒绝缺少显式类型的分支，补齐分支类型后又明确要求 `any_of` 必须是所在 schema 的唯一字段。
- **最终修复**：移除 `goal_revise_plan.parameters.anyOf`，改用普通对象 schema 的 `minProperties: 1`；Goal v2 既有协议层仍独立强制 `plan_revised` 必须包含 objective 和/或 steps，因此没有放宽服务端真实语义。新增回归断言，防止重新引入 provider 不可移植的组合 schema。
- **自动验证**：`tests/test_goal_v2_protocol.py + tests/test_goal_v2_agentrun.py` 共 `46 passed`；`tests/test_agent_runtime.py + tests/test_goal_v2_agentrun.py` 共 `136 passed + 60 subtests`；`python -m py_compile server.py` 与 `git diff --check` 通过。仅出现 `server.py` 既有无效转义 `SyntaxWarning`，与本次改动无关。
- **真实复测**：用户重启 Code 后以 Gemini-GCP、`gemini-3.6-flash` 发送要求读取根目录 `TODO.md` 的兼容请求，GCP 不再返回 schema 配置错误，并准确返回 `WB-003 · 渠道正式准入与持续评估`。本结论只证明该工具声明兼容链恢复，不提升渠道正式准入等级。
- **旁支发现**：多 Key 场景中 `modelKeyMap` 会让第一个声称支持某模型的 Key 取得该模型路由；本轮曾使 Gemini 模型使用 `DeepSeek-官方` Token。该问题未混入本次补丁，另以 `CODE-035` 跟踪。
- **范围与状态**：未修改 Workbar 线上渠道、模型、分组或 Key，未读取真实 Key，未处理 ApiNebula-Claude-Kiro 和国产模型旁支；owner lease 在两轮实现及收口写入前均取得并在阶段结束释放。由于共享工作区已有无关日志/文档改动，本阶段未自动提交。
