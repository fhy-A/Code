# 2026-09-10 开发日志

## 2026-09-10 04:46 · Codex

### CODE-087 预算反馈与七工具说明结束复核通过

- R031结束复核通过：同轮length+JSON解析失败的预算/未执行提示、原诊断与旧失败签名兼容、七工具模型说明的获批范围完成。审批核对两产品冻结，独立AST比较全部服务端工具结构保持且仅七工具description变化，并复核57项后端、46项前端及签名负控、隔离SSE纠正只执行一次、预算/恢复不增加调用、doctor/构建与清理证据。
- 本次复用已通过门禁，不修改产品/测试、不重复运行。2产品、2测试文件、1参数结构夹具、2公共日志共7文件作单个本地提交；父提交 `b1fab3b7e1074026688cc75bd042857f3a741e11`，产品磁盘冻结及Git正常换行归一化字节均核对一致。
- 私有CODE-087清理已完成预算反馈/说明执行差量，保留读取行窗口设计以待下一合同，并保留auto预算、搜索和路径未批准方向。读取窗口及任何预算/授权/恢复策略未实施；无关待办、handoff与保护data保持。接手会话26.837MiB，正常复用；未读写真实数据、重启服务、推送/标签/发布或自动开始下一实现。

## 2026-09-10 04:42 · Codex

### CODE-087 R030 同轮预算终止反馈与文件工具说明

- 用户在审批任务确认先实施预算终止反馈与常用文件工具说明。本轮只修改 `server.py` 现有参数归一化/失败消息路径与服务端模型说明，以及 `src/agent/tools.js` 的对应说明；没有改变max_tokens、用户设置、工具执行/授权/自动重试、读取/搜索/路径实现或持久化结构。
- 归一化接收该工具所属模型轮的已有finishReason：正常模型完成传当前model_result，恢复的Skill完成批次传其对应已保存轮。仅 `finishReason=length` 且 `json.JSONDecodeError` 时，在原decoder诊断后追加固定提示，说明该轮到输出Token上限、本次无效JSON调用未执行、应重新给完整合法参数而非猜测补尾。缺finish的旧调用、stop/tool_calls的普通解析错误、合法JSON但字段不合法、非对象JSON不猜预算；length但完整合法参数仍按原规则执行。生图原有错误脱敏不放宽。
- 提示沿用 `parseError → invalid_tool_arguments → result.error` 和既有模型工具回执/用户工具轨迹，errorCode与fieldErrors保持。针对现有UI将错误摘要限制为220字符的行为，典型未闭合字符串的原诊断、length和未执行事实均在前220字符内，完整提示留于模型回执；没有另造前端翻译字典或UI状态。
- 现有相同失败识别包含error文本。审批确认仅对 `invalid_tool_arguments` 去掉本次**精确固定提示后缀**再计算旧签名，避免提示文字重置旧失败累计；用户/模型看到的error不删减。没有通用清洗、改重试次数、改指纹字段或迁移历史。最终负控证明其他errorCode、后面还有其他文字的非精确后缀均保持原全文签名；旧/新/混合历史签名和累计一致。
- 七种模型工具说明完成同步：list_files深度/200项与不可读限制，glob_files路径glob/零层匹配/根回查，search_files的query-regex-glob区别与1MiB/100文件/通常10处限制，read_file当前前512KiB预览再切行及附件/图片差异，propose_edit整文件与片段组合/混用优先/唯一上下文/现行模糊首命中，write_file完整UTF-8、JSON转义/换行与空内容，delete_file空目录限制及精确授权范围。两端说明明确公共resolver的home/output语义，不声称强项目沙箱；删除说明只禁止绕过授权或扩大未确认范围，其他既有工具仍按原合同处理。
- 以 `b1fab3b` 为基线固化七工具参数结构夹具 `tests/fixtures/file-tool-parameter-schemas.json`。真实服务端定义、前端nativeTools以及实际发给隔离上游的七工具定义逐项验证：只改description，字段名/type/required/additionalProperties/其他约束保持；两语言例子可解析且通过现有校验，写入例子解码后为真实换行，空content仍合法。整包前端定义hash因说明改变而更新，独立结构冻结避免把文案更新误作schema迁移。
- 最终相关后端/Runtime/编辑回归 **57 passed, 150 deselected, 22 subtests passed in 10.38s**（本机Temp `code087-r030-backend-final.txt`），包括5个本阶段定向测试、既有普通解析/别名冲突、重复失败限制与改参、授权写/拒绝删、编辑冲突/备份/重放、Runtime分块聚合/协议400/无凭据快照与Skill批次恢复。之后只给签名测试补负控，单项再次通过（`code087-r030-signature.txt`）；产品未再改变。前端工具/diff/edit回归 **46 passed, 418 deselected, 48 subtests passed in 3.72s**（`code087-r030-frontend-final.txt`）。doctor、构建/freshness、Node语法、真实server模块加载与diff检查通过；没有机械全量重跑。
- 隔离上游真实SSE片段场景：第一轮length+残缺write_file参数被拒绝且无operationId/授权决定；第二轮故意仍标length但完整合法参数成功，仅写隔离目录一次，第三轮stop，实际模型HTTP请求固定3、executor固定1，max_tokens始终4096。下一轮模型工具回执含预算/未执行提示，恢复保存记录仍保留该反馈且无新请求。另3次相同残缺read后正常结束，0执行/4模型HTTP请求、失败计数1/2/3和既有retryLimitReached信号保持；真实写入失败的第四次阻止仍由既有回归覆盖，不扩大为所有解析错误的硬重试保证。全部请求是本机假上游，无付费模型；每批Python导入前使用临时CODE_DATA_DIR，夹具及导入根清理通过，未碰真实记录/保护data。
- 实施中说明字典的JSON布尔中间态在真实模块测试前纠正为Python字面量，并保留原紧凑定义格式；合成分块初版重复工具name、以及把私有operationId当公开字段的测试假设已改正，最终断言核对真实私有执行记录。未因此改产品协议或请求用户复跑。
- 原真实样本的4096+length证据仍只证明所记运行，未重新读取原始用户记录或补做真实分块；无法确定当时auto或显式4096的边界保持。文本有界行窗口的下一阶段设计只写私有CODE-087并交审批，不在本轮实现。最终两产品冻结于Temp `code087-r030-freeze.json`，基线 `b1fab3b7e1074026688cc75bd042857f3a741e11`；2产品、2测试文件、1结构夹具、2公共日志共7文件未提交。会话26.747MiB，正常复用；无重启3010/3011、推送/标签/发布或下一阶段写入。

## 2026-09-10 04:13 · Codex

### CODE-087 失败显示修复与文件工具诊断结束复核通过

- R029结束复核通过：无有效提案的失败/孤立拒绝不生成虚假文件卡，真实提案失败与批准/拒绝/已应用事实、旧显示兼容均完成。审批独立核对两产品冻结、14组直接检查、29项相关回归、8组页面与清理及最终截图；并精确读取同AgentRun元数据，确认4096输出预算、length终止、后续完整参数成功，源码与内存合成诊断矩阵一致。
- 本次收口不修改产品/测试、不重跑已通过证据。2产品、3测试/夹具、2公共日志共7个公共文件作单个独立本地提交，父提交 `17307ca4291dabc4565682043675fd0a70bcf831`；两产品磁盘冻结与Git正常换行归一化后的提交字节均核对一致，server.py保持不变。
- 私有CODE-087移入待决策/调研，清理已完成UI与诊断执行差量，保留四项未批准优化及其失败/兼容/回退边界。没有实施预算、参数契约、读取、路径、执行或重试方案；诊断原始私有数据未进入公共提交。CODE-045、无关handoff与三项保护data保持。接手会话26.031MiB，正常复用；未操作真实会话/目标文件、重启服务或推送/打标签/发布，未自动开始下一优化。

## 2026-09-10 04:09 · Codex

### CODE-087 R027 失败工具显示修复与 R028 文件工具只读审计

- 用户确认先修正失败编辑建议显示，再定位参数失败原因，并追加查找、读取、写入、编辑、删除的只读工具审计；后续方案确认统一由审批任务对接。新事项按私有游标分配CODE-087，未归入已完成的CODE-045。
- 基线真实函数的最小内存复现确认：`write_file` 返回 `invalid_tool_arguments` 时，前端仍生成pendingEditId，渲染“未命名文件/等待批准/+1 lines”，错误文字被当作文件内容（本机Temp `code087-r027-before.json`）。本轮仅修改 `app.js` 与 `src/ui/diff.js`：无实际提案的失败或孤立拒绝只进入普通工具轨迹；有真实提案的执行失败保留提案和失败状态，不再以失败推断用户拒绝或以既有批准推断已应用。真实成功、显式拒绝与已应用事实保持。旧错误投影只在显示时过滤，不迁移或改写用户历史。
- 直接加载实际完成投影与renderer的检查最终 **14组通过**（`code087-r027-unit-final.txt`），覆盖解析/校验错误、I/O和编辑/读取失败、无提案拒绝、真实提案批准前后失败、旧误标拒绝、成功与纠正后成功、真实用户拒绝、已应用事实、真实待批准及重复事件/历史无修改。相关前端diff/edit/tool-process/server-agent回归 **29 passed, 435 deselected in 1.07s**（`code087-r027-related-final.txt`）；doctor、构建/freshness、JS语法与diff检查通过，无新i18n字段或固定文案。
- bundle/direct classic×深浅主题×1280/390宽共 **8组页面通过**（`code087-r027-ui-EEmvSZ/result.json`）。每组7个合成工具结果在重复完成事件后仍各一条，新投影与旧JSON快照重放均仅保留3个有事实依据的编辑卡；失败详情展开可查、无未命名/待批准假卡和错误文本的“+1 lines”，真实失败/已应用/拒绝分别显示。重放前后消息内容不变。中英文及代表截图已核验；未将用户文件或模型调用用作测试。
- 首轮UI夹具因 `:not([open])` 动态集合在点击中收缩而超时，改为稳定阶段集合和精确open判断；审批补充发现孤立拒绝样本仍有假行数，随后同时修正产品的无提案拒绝与历史过滤，并将真实拒绝夹具补为实际授权ID+diff，新增孤立拒绝断言。最终结果含这项修正，早前13组/8组结果不冒充最终版本。最终页面errors为空，模型/工具/AgentRun均0，自有context/browser、宿主进程、两端口与临时根清理通过，activeChildren=0。

### 指定写入失败样本的确认事实

- 只读用户指定Session（8条、51459字节）和其精确关联AgentRun，不扫描其他会话。模型为 `deepseek-v4-flash-vision-exp`；该运行请求 `max_tokens=4096`。第一轮03:23:27回报 `finishReason=length`、`completion_tokens=4096`，工具参数长度1403且JSON未闭合，规范化阶段拒绝；无operationId或授权决定，未进入文件执行。第二轮03:23:48参数6679、JSON完整，`finishReason=tool_calls`、completion_tokens=2447，写入成功；第三轮stop结束。已有模型纠错循环在此样本有效，没有必要再叠加自动写入重试。
- 第一、二轮参数在model-completed、tool-started和execution的耐久副本逐字一致，Session中的调用也与错误/成功回执对应。证据支持本次参数不完整与单轮输出预算耗尽有关，不是磁盘拒绝写入；但未取得原始逐段SSE，不能据此证明全部分块无缺失或归责某个网络环节。精确关联3个Runtime的只读GET均404；现有Runtime为内存记录并设终态TTL，404的具体原因不从代码直接推断。只保留关联ID、结束原因、时间、计数/长度及是否相等的脱敏摘要于Temp `code087-r028-sample.json`，没有落盘完整原文或凭据。
- `getEffectiveMaxTokens` 的auto分支按模型名选择8192/16384，否则4096；此模型名会落默认4096。运行仅留最终4096，**无法区分当时auto回退还是用户显式选择4096**，未读取其他设置猜测；也未证明第一轮Token消耗的细分构成或其他模型上限。

### 各工具审计事实矩阵

| 工具/阶段 | 触发与证据 | 是否进入执行及反馈 | 已有恢复与边界 |
|---|---|---|---|
| 公共参数入口 | JSON解析失败；未知字段/缺字段；仅read_file对file_path做窄别名归一化，冲突明确拒绝。`server.py:6101/6331/9453` | 解析/校验失败在工具执行前返回invalid_tool_arguments；字段错误有具体名称，JSON错误主要为decoder文案 | 失败回执进入模型下一轮，3次相同失败设重试限制；本次第二轮纠正成功。不能把所有失败都当模型错误或未执行 |
| 列目录/查文件名 | list_files最大深度3、最多200项，跳过SKIP_DIRS；目录迭代OSError被吞掉。glob_files子目录无匹配时回到根目录搜索。`23919/24165` | 内存假目录PermissionError确实返回ok=true/count=0/truncated=false，不能区别空目录和不可读；glob根回退可能改变搜索范围，未作真实用户样本验证 | 不存在目录有错误；200项上限有truncated。当前没有目录跳过错误计数，不能把0匹配视作穷尽查找 |
| 内容搜索 | schema要求query，regex默认false，pattern别名被schema拒绝；contextAround无minimum，合成-5可通过。单文件内容上限1MiB、结果100、每文件默认10。`24612/24080` | 正则语法错误/空query/无路径可报告；逐文件读取异常被忽略。纯字面查询疑似正则且无结果时已有hint | 现有测试覆盖递归glob根目录匹配与regex提示；本轮不重跑实际目录搜索或声明用户搜索问题已定位 |
| 读取 | 先read_bytes全量读入，再截512KiB并按行切片。`23969:24054–24068` | 内存样本完整文件2行、第二行位于512KiB后，请求第2行却报endLine小于startLine；空文件请求1–1也报同样错误。均未操作真实文件 | 行范围、truncated、二进制/图片回退已存在；该错误可能误导模型继续改范围，且现有结果预算不等于内存读取上限 |
| 创建/写入 | 完整content必填；路径空、目标目录、读取旧文件失败都会拒绝；采用备份、锁、原子替换和写后核验。`19982/20069` | 指定JSON失败未执行；但通用I/O错误可能发生于备份前、替换时或写后核验，不能统一声称没有写入 | 内容相同已有replayed，operationId关联写入；不以UI失败标签决定重复写。没有证据说明本次需放宽文件写入安全检查 |
| 编辑/应用 | schema仅required path；合成仅path通过schema，却在builder报缺newContent或oldText/newText；同时给两种模式优先片段替换。模糊匹配已有空白归一化及0.65相似度回退，替换首个命中。`24900/25356/25410` | 片段不匹配提示重读；同内容无diff报错；应用核对提案hash、当前hash/mtime，冲突要求重读 | 已有最终内容hash一致的安全重放。不能再以降低匹配阈值或无条件覆盖来减少失败；本轮未验证真实编辑样本的匹配质量 |
| 删除 | 只文件/空目录；非空目录拒绝且文案包含rmdir建议；缺失路径只有同operationId的耐久receipt存在才视为重放成功。`20005/20111` | 非空/不存在属于必要前置失败；备份/receipt/删除阶段可能出现I/O错误，需区分副作用状态 | 已有删除receipt与重启重放测试；本轮没有执行删除，也不把不存在一律吞成成功 |
| 公共路径解释 | 模型schema主要表述project-relative，resolver实际接受home内路径、对其他路径回退项目output/同名文件，并可能创建fallback父目录。`23693` | 这是代码事实和文档语义差异，不是本次真实样本的根因；读工具也经过该resolver，不能未经验证称其绝无持久副作用 | 现有路径测试允许home/redirect，单凭测试名称“rejected”不能当强项目沙箱证明；未调用真实resolver去试越界路径，权限/兼容调整另行审议 |

- 上述合成证据使用AST提取当前函数和内存假文件/目录，不执行真实文件工具（Temp `code087-r028-synthetic.json`，8项观察）。初次schema片段夹具遗漏类型helper，补齐实际helper后取得最终校验结果；不把夹具异常算产品缺陷。现有 `test_routes.py`、`test_edit_pipeline.py`、`test_agent_runtime.py` 与H4-6I说明仅作源码/历史证据审阅，不记作本轮重新运行通过。
- 本轮只实现显示修复；预算策略、参数契约、行范围读取、路径解析、错误结构、执行或重试方案均未改动，未经批准的建议只保留私有CODE-087与审批任务。本阶段2产品、3测试/夹具、2公共日志共7个公共文件未提交；最终产品冻结于Temp `code087-r027-freeze.json`，基线 `17307ca4291dabc4565682043675fd0a70bcf831`，server.py保持。真实Session/目标文件/其他记录未写入，未调用收费模型、重启服务或推送发布；开发会话25.820MiB，正常复用。

## 2026-09-10 03:19 · Codex

### CODE-045 流式图标即时加载与尺寸对齐结束复核通过

- R026结束复核通过：实际流式增量即时绑定、局部重绘缓存复用与占位/成功图标16px对齐的获批范围已完成。审批独立核对三产品冻结、8组实际增量页面的14次补丁/慢加载无重启/缓存同步/结束转换、14组直接检查、16项相关回归、同文本Range与外框坐标及行高相等、截图和清理证据；本次收口复用通过结果，不修改产品/测试或重复运行。
- 三产品与R025冻结一致，server.py保持原样；本阶段3产品、3测试/夹具、2公共日志共8个公共文件创建独立本地提交，父提交为 `9c1978ff68264310be07e196c09bf69637dcc8af`。核对Git正常换行归一化后的提交字节与冻结工作区内容一致。
- 私有CODE-045移回待决策/调研，只保留真实品牌覆盖、网络环境和未来新样本的决策入口；已完成流式与对齐执行差量移除，无关待办/交接和保护data保持。既有Qwen通用图、安全能力差异、同尺寸不等于同墨迹面积及离线夹具边界均不扩大解释。接手会话23.782MiB，正常复用；未推送、打标签、发布、重启服务或自动启动下一事项。

## 2026-09-10 03:15 · Codex

### CODE-045 R025 真实流式增量即时绑定与图标尺寸对齐

- 用户确认将流式图标接入遗漏与替换偏移一起修复。旧 `patchStreamingAssistantMessage` 直接替换答案HTML，没有调用仅全量渲染时执行的图标绑定；地球SVG为12×12，成功canvas为16×16，外框均20×20。
- 产品修改前，在bundle深色390宽和classic浅色1280宽的实际 `updateAssistantMessage → scheduleStreamingAssistantPatch → requestAnimationFrame → patchStreamingAssistantMessage` 链保留复现：初始答案无链接，随后完整链接出现并连续7次增量更新，仍为streaming=true、4地球、0图标请求。没有用循环renderMessages替代增量更新。证据为本机Temp `code045-r025-ui-sbcHJ8/result.json` 和前后截图。
- 同文本、稳定布局后的独立尺寸控制实验才手动调用旧绑定并放行合成图片：已加载项内框12→16，图标外框向上2px；段落文字Range坐标和行高不变。表格已加载行高度39→38.6875px，下一行文字随整体行位置上移0.3125px，但文字相对单元格的坐标不变。两个入口观察一致；不把这些结果扩大为所有文字跳动或真实品牌墨迹尺寸一致。
- 本轮产品仅 `app.js`、`styles.css`、`src/ui/markdown.js`：绑定函数支持默认document或指定局部根节点；流式输出节点替换后同步绑定该节点内的新消费者，再清理旧消费者，已解码图标同一补丁同步绘制，全量调用保持兼容。SVG标记和SVG/canvas样式统一16×16，保留20×20外框、5px间距、既有色彩/地球语义和成功图标背景。没有调整来源、4秒/3秒预算、输入过滤、并发/cache/取消策略、后端协议、Session保存或滚动行为。
- 新增局部绑定直接检查，验证只扫描指定变化区域、无关链接不绑定、新消费者先于清理注册、慢请求不中止重开与缓存同步绘制；最终直接函数 **14组通过**（`code045-r025-unit.txt`）。相关favicon/tooltip/Markdown/外链/流式投影回归 **16 passed, 447 deselected in 4.78s**（`code045-r025-related.txt`）；doctor、构建/freshness、JS语法和diff检查通过。复用已通过的无关基线，不重跑全量和旧H4初始化阻塞。
- 修复后实际增量链在bundle/classic×深浅主题×1280/390宽共 **8组通过**（`code045-r025-ui-zPlBo6/result.json`，16张截图）。每组14次schedule对应14次真实rAF补丁；快速图加载后，慢图被夹具保持未完成并跨越8次增量，两个origin仍各请求一次、无取消，8个补丁内均同步观察到2个缓存canvas，期间全量render计数不增加。随后放行慢图，新增域与重复域、双失败稳定地球及结束转换通过；streaming=true时已显示5图/6槽，整组4个origin共5候选请求，结束转换不增加请求。
- 同样文本的加载前后，段落/表格SVG与canvas内框均16×16、外框均20×20，外框与文字Range坐标、文字行高及父行高度逐项相等；后续新增链接和最终答案的槽位尺寸也一致。目视bundle浅色1280和classic深色390截图布局正常；内框相同不表示各品牌实际墨迹面积相同。
- 复用的隔离宿主本身采用离线marked原样HTML解析，初始夹具直接传Markdown文本未生成链接，诊断后改为实际Markdown link renderer生成固定段落/表格token的HTML，增量调度/替换/绑定仍使用真实产品函数。该样本不宣称完整marked语法解析或真实SSE/模型传输覆盖；初版尺寸控制误将旧坐标不等视作失败，随后先记录旧差量并等待几何稳定，再对修复版执行相等断言。未扩修框架、请求用户复跑或添加网络依赖。
- 合成PNG/no-store、原站测试query、隔离宿主关闭路由v2和关闭前清空唯一测试内存Session ID的边界沿用R023；这些不是生产修改。最终8组页面errors为空，模型/工具/AgentRun均0，自有context/browser、宿主进程、两端口、临时根清理通过，活动子进程0；旧版本两组有效复现也清理通过。
- 三个产品SHA256冻结于本机Temp `code045-r025-freeze.json`，基线 `9c1978ff68264310be07e196c09bf69637dcc8af`，server.py不变。本轮3产品、3测试/夹具、2日志共8个公共文件未提交，私有CODE-045只记录结束复核和真实覆盖限制。开发会话23.697MiB，正常复用；未重启3010/3011、操作真实用户会话/模型/保护data或无关交接，未推送/打标签/发布。

## 2026-09-10 02:45 · Codex

### CODE-045 浏览器直载阶段结束复核通过与本地收口

- R024结束复核通过：用户确认的Google优先、精确原站次选、稳定地球及有界缓存/重绘范围已完成。审批独立核对冻结源码、13组直接检查、15项相关回归、8组实际应用页面与清理、no-store请求前后计数、24品牌/Qwen通用图截图，以及Playwright原站请求夹具适配依据；复用这些证据，不修改产品/测试或重跑已通过检查。
- 收口前 `app.js` 与R023冻结SHA256一致，`server.py` 与基线相同，父提交核对为 `770b92648391aef418f125b02bf8459c71859323`。本阶段1产品、4测试/夹具、2公共日志共7个公共文件独立本地提交；私有CODE-045移回待决策/调研，移除完成执行差量，只保留真实品牌覆盖限制及未来新样本的决策入口。
- Qwen通用图、任意站点/跨网络不可保证、浏览器默认凭据与原代理安全能力差异，以及隔离图片/内存Session/原站测试query的证据边界保持。旧H4初始化阻塞不记通过，真实服务和用户会话未操作；未推送、打标签、发布或自动开始额外优化。接手会话23.022MiB，正常复用。

## 2026-09-10 02:41 · Codex

### CODE-045 R020–R022 真实诊断与 R023 浏览器图标链实现验收

- 用户依据普通浏览器环境下的25站点结果，确认采用浏览器 Google S2 优先、精确原站 `/favicon.ico` 次选、最终地球的两候选方案。真实截图中 **24个可辨识品牌图案、Qwen为通用地球**，不能写成25个品牌命中；不增加Qwen特例、16像素占位启发式、HTML发现或第三来源。
- R023只修改产品 `app.js`：Google参数固定为 `domain=encoded normalized origin&sz=32`，等待最多4秒；失败后精确origin的 `/favicon.ico` 最多3秒。每个候选每周期至多一次，均失败后保留地球，无第三次、周期轮询或错误弹窗。原20×20槽位、链接href/target/rel、右键复制、键盘与统一tooltip保持。
- 静态归一化在任何外部图片请求前拒绝IPv4及URL可归一化的非常规写法、IPv6、localhost及子域、单标签、`.local`、非默认端口、userinfo及无效主机；只允许HTTP(S)有效域名和IDNA归一化结果，向Google传origin，不传路径、查询或fragment。图片使用 `no-referrer` 和浏览器默认凭据策略，**no-referrer不代表禁用Cookie**。静态检查无法识别域名最终解析到私网，也不提供原代理的DNS固定连接、逐跳重定向、字节大小/MIME/魔数校验；这些是用户已确认的浏览器路径边界，不声称安全能力等同。
- `server.py` 与 `/api/favicon` 保持原样作为回退基线；没有更改全局COEP/COOP/CSP、代理、DNS/TLS或持久化结构。回退只需恢复本阶段前端实现并重建前端产物，既有会话和服务端接口无需迁移。
- 同origin共享、4槽FIFO、128项LRU、成功6小时/失败5分钟缓存保持；一槽覆盖两候选，过期后由新订阅触发新周期。同步流式重绘先注册新消费者，不重置在途额度；无消费者取消、迟到回调隔离、淘汰与最终pagehide清理、bfcache保留通过直接检查。成功图片按origin保留一个已解码资源，各槽仅canvas绘制；没有跨源像素读取或导出。此举避免no-store响应在复制图片节点和重绘时再次请求，缓存重绘也不重置淡入动画。
- 独立no-store实验证据 `code045-r023-nostore-trk03lpt/`：8个同origin槽、5次重绘，修正前累计请求 `[2,3,4,5,6]`，资源复用后 `[1,1,1,1,1]`。全部请求由隔离夹具响应，外网请求0，context/browser已关闭；这是重复请求证据，不是真实品牌图样验收。
- 最终确定性函数检查 **13组通过**（本机Temp `code045-r023-unit-final.txt`），覆盖静态拒绝与origin隐私、主备成功/失败、4+3秒超时和迟到事件、1像素拒绝但不凭16像素猜占位、4槽FIFO、同源共享和流式重绘、取消代际、解码资源复用、容量/正负缓存过期与pagehide。相关前端favicon/tooltip/Markdown/外链回归 **15 passed, 448 deselected in 5.24s**（`code045-r023-related.txt`）；doctor、前端构建/freshness、语法和diff检查通过。未机械重跑无关全量或旧H4会话初始化阻塞。
- 实际bundle/direct classic应用以原 `renderMessages`、Markdown renderer和绑定函数验收，深浅主题×1280/390宽共 **8组通过**（`code045-r023-ui-cmo4jx/result.json`）。主源失败后原槽恢复、双失败地球、6槽20×20无横向溢出、20次流式重绘及流结束无额外请求、统一提示、右键复制、Enter激活和刷新均通过，代表截图已目视核验。每组刷新前5个origin共9次候选请求，重绘后仍9次；刷新创建新周期，不能把跨刷新累计次数称作第三次恢复。
- UI夹具使用合成PNG与no-store响应，隔离宿主仅在夹具关闭路由v2，内存Session不代表完整Session创建/保存链。Playwright会在用户route前自动中止恰好以 `/favicon.ico` 结尾的请求，因此仅返回给隔离页的源码为原站候选加固定测试query；生产仍是精确无query路径，由直接检查验证。首次适配前原站被测试框架中止，后续发现内存Session在reload触发unload beacon，已在测试reload/关闭前清空该测试ID；最终业务断言、写入审计和清理全部通过，未改产品保存逻辑或扩修框架。旧CJK用例同步了浏览器图标断言与准确证据字段，仅做语法检查，不冒充旧H4通过。
- 最终8组页面错误为空，模型/工具/AgentRun均0；自有context/browser、宿主进程、两端口与临时根清理通过，活动子进程0。真实3010/3011、用户会话、模型、保护data与无关交接未操作。
- 产品SHA256冻结于本机Temp `code045-r023-freeze.json`，基线 `770b92648391aef418f125b02bf8459c71859323`。本轮1产品、4测试/夹具、2公共日志共7个公共文件未提交，私有CODE-045只保留结束复核与覆盖限制；本次不提交、推送、打标签或发布。开发会话元数据22.941MiB，正常复用。

### R020–R022 已完成诊断事实与适用范围

- R020旧同源链25域样本完成24域：10个通过解码校验的资源、14个临时失败，Gemini因24秒worker上限未返回，结果未知。10个成功域暖查没有新增候选；14个失败暖查仍失败。已返回结果内共113次候选fetch、57个含重定向HTTP响应，Google33次与DuckDuckGo24次均未取得图片；未知worker内部计数不在其中。失败涉及连接/DNS/TLS、403/404、HTML响应和根favicon缺失，不等于网站没有图标。
- R020成功资源中通义父域 `aliyun.com/favicon.ico` 是32×32全透明图，灰色来自展示背景；其余9个有可见图案，其中8个精确站点、1个文心百度父域替代。3011返回的app.js/bundle原始字节与磁盘一致，服务进程启动晚于基线提交，但未证明用户页面缓存或当前闭包版本；CUA入口失败后未绕过接口检查用户浏览器。
- R020实际renderMessages的6域正常连续30次重绘共6请求、0取消，流结束1.885秒有4图、2.482秒6图；人为制造消费者短暂消失才出现7请求/1取消，未证明真实流式路径存在该间隙。按已测时延合成的25域回放在1.910秒流结束时1成功/4加载/20排队，48.675秒全部终结、40请求/0取消；Gemini按6秒未知失败代入。此回放说明旧候选链占满4槽可拖延后续图标，不是用户精确耗时或可与后续实测计算加速倍数的基准。证据 `code045-r020-zpgn3bw2/`。
- R021固定6域、原站与Google共12个完全一致URL，对照实际用户Chrome152与Python安全客户端。浏览器两轮各9/12加载、Google6/6；Python两轮各仅DeepSeek和Anthropic原站成功、Google全部连接失败。确认路径结果差异，但不能单独归因代理/DNS/TLS/UA/缓存。该诊断页crossOriginIsolated=true且含COEP credentialless/COOP same-origin，不直接代表普通产品环境；首次观察/同页重复也不是严格冷缓存/缓存命中证明。证据 `code045-r021-tg7oypor/`。
- R022随后以无COEP/COOP/CSP响应头、crossOriginIsolated=false的实际Chrome152复测25域，两轮均25/25图片加载且全部来自Google；首轮最后完成4276.1ms、同页重复14.9ms，仍不作严格冷缓存或性能倍率声明。Qwen16×16通用地球、Moonshot28×28，其余32×32；用户截图确认24个品牌图案与Qwen限制，通义已有可辨识图案。证据 `code045-r022-l_pd_fpn/`。R021/R022自有诊断宿主按时退出、端口关闭和租约释放已核对；三轮诊断未修改产品、测试、日志或TODO，本日统一补录事实。
