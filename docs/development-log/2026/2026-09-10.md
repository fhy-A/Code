# 2026-09-10 开发日志

## 2026-09-10 03:19 · Codex

### CODE-045 流式图标即时加载与尺寸对齐结束复核通过

- R026结束复核通过：实际流式增量即时绑定、局部重绘缓存复用与占位/成功图标16px对齐的获批范围已完成。审批独立核对三产品冻结、8组实际增量页面的14次补丁/慢加载无重启/缓存同步/结束转换、14组直接检查、16项相关回归、同文本Range与外框坐标及行高相等、截图和清理证据；本次收口复用通过结果，不修改产品/测试或重复运行。
- 三产品与R025冻结一致，server.py保持原样；本阶段3产品、3测试/夹具、2公共日志共8个公共文件创建独立本地提交，父提交为 `9c1978ff68264310be07e196c09bf69637dcc8af`。核对Git正常换行归一化后的提交字节与冻结工作区内容一致。
- 私有CODE-045移回待决策/调研，只保留真实品牌覆盖、网络环境和未来新样本的决策入口；已完成流式与对齐执行差量移除，无关待办/交接和保护data保持。既有Qwen通用图、安全能力差异、同尺寸不等于同墨迹面积及离线夹具边界均不扩大解释。接手会话23.782MiB，正常复用；未推送、打标签、发布、重启服务或自动启动下一事项。

## 2026-09-10 03:15 · Codex

### CODE-045 R025 真实流式增量即时绑定与图标尺寸对齐

- 用户确认将流式图标接入遗漏与替换偏移一起修复。旧 `patchStreamingAssistantMessage` 直接替换答案HTML，没有调用仅全量渲染时执行的图标绑定；地球SVG为12×12，成功canvas为16×16，外框均20×20。
- 产品修改前，在bundle深色390宽和classic浅色1280宽的实际 `updateAssistantMessage → scheduleStreamingAssistantPatch → requestAnimationFrame → patchStreamingAssistantMessage` 链保留复现：初始答案无链接，随后完整链接出现并连续7次增量更新，仍为streaming=true、4地球、0图标请求。没有用循环renderMessages替代增量更新。证据为本机Temp `code045-r025-ui-sbcHJ8/result.json` 和前后截图。
- 同文本、稳定布局后的独立尺寸控制实验才手动调用旧绑定并放行合成图片：已加载项内框12→16，图标外框向上2px；段落文字Range坐标和行高不变。表格已加载行高度39→38.6875px，下一行文字随整体行位置上移0.3125px，但文字相对单元格的坐标不变。两个入口观察一致；不把这些结果扩大为所有文字跳动或真实品牌墨迹尺寸一致。
- 本轮产品仅 `app.js`、`styles.css`、`src/ui/markdown.js`：绑定函数支持默认document或指定局部根节点；流式输出节点替换后同步绑定该节点内的新消费者，再清理旧消费者，已解码图标同一补丁同步绘制，全量调用保持兼容。SVG标记和SVG/canvas样式统一16×16，保留20×20外框、5px间距、既有色彩/地球语义和成功图标背景。没有调整来源、4秒/3秒预算、输入过滤、并发/cache/取消策略、后端协议、Session保存或滚动行为。
- 新增局部绑定直接检查，验证只扫描指定变化区域、无关链接不绑定、新消费者先于清理注册、慢请求不中止重开与缓存同步绘制；最终直接函数 **14组通过**（`code045-r025-unit.txt`）。相关favicon/tooltip/Markdown/外链/流式投影回归 **16 passed, 447 deselected in 4.78s**（`code045-r025-related.txt`）；doctor、构建/freshness、JS语法和diff检查通过。复用已通过的无关基线，不重跑全量和旧H4初始化阻塞。
- 修复后实际增量链在bundle/classic×深浅主题×1280/390宽共 **8组通过**（`code045-r025-ui-zPlBo6/result.json`，16张截图）。每组14次schedule对应14次真实rAF补丁；快速图加载后，慢图被夹具保持未完成并跨越8次增量，两个origin仍各请求一次、无取消，8个补丁内均同步观察到2个缓存canvas，期间全量render计数不增加。随后放行慢图，新增域与重复域、双失败稳定地球及结束转换通过；streaming=true时已显示5图/6槽，整组4个origin共5候选请求，结束转换不增加请求。
- 同样文本的加载前后，段落/表格SVG与canvas内框均16×16、外框均20×20，外框与文字Range坐标、文字行高及父行高度逐项相等；后续新增链接和最终答案的槽位尺寸也一致。目视bundle浅色1280和classic深色390截图布局正常；内框相同不表示各品牌实际墨迹面积相同。
- 复用的隔离宿主本身采用离线marked原样HTML解析，初始夹具直接传Markdown文本未生成链接，诊断后改为实际Markdown link renderer生成固定段落/表格token的HTML，增量调度/替换/绑定仍使用真实产品函数。该样本不宣称完整marked语法解析或真实SSE/模型传输覆盖；初版尺寸控制误将旧坐标不等视作失败，随后先记录旧差量并等待几何稳定，再对修复版执行相等断言。未扩修框架、请求用户复跑或添加网络依赖。
- 合成PNG/no-store、原站测试query、隔离宿主关闭路由v2和关闭前清空唯一测试内存Session ID的边界沿用R023；这些不是生产修改。最终8组页面errors为空，模型/工具/AgentRun均0，自有context/browser、宿主进程、两端口、临时根清理通过，活动子进程0；旧版本两组有效复现也清理通过。
- 三个产品SHA256冻结于本机Temp `code045-r025-freeze.json`，基线 `9c1978ff68264310be07e196c09bf69637dcc8af`，server.py不变。本轮3产品、3测试/夹具、2日志共8个公共文件未提交，私有CODE-045只记录结束复核和真实覆盖限制。开发会话23.697MiB，正常复用；未重启3010/3011、操作真实用户会话/模型/保护data或无关交接，未推送/打标签/发布。

## 2026-09-10 02:45 · Codex

### CODE-045 浏览器直载阶段结束复核通过与本地收口

- R024结束复核通过：用户确认的Google优先、精确原站次选、稳定地球及有界缓存/重绘范围已完成。审批独立核对冻结源码、13组直接检查、15项相关回归、8组实际应用页面与清理、no-store请求前后计数、24品牌/Qwen通用图截图，以及Playwright原站请求夹具适配依据；复用这些证据，不修改产品/测试或重跑已通过检查。
- 收口前 `app.js` 与R023冻结SHA256一致，`server.py` 与基线相同，父提交核对为 `770b92648391aef418f125b02bf8459c71859323`。本阶段1产品、4测试/夹具、2公共日志共7个公共文件独立本地提交；私有CODE-045移回待决策/调研，移除完成执行差量，只保留真实品牌覆盖限制及未来新样本的决策入口。
- Qwen通用图、任意站点/跨网络不可保证、浏览器默认凭据与原代理安全能力差异，以及隔离图片/内存Session/原站测试query的证据边界保持。旧H4初始化阻塞不记通过，真实服务和用户会话未操作；未推送、打标签、发布或自动开始额外优化。接手会话23.022MiB，正常复用。

## 2026-09-10 02:41 · Codex

### CODE-045 R020–R022 真实诊断与 R023 浏览器图标链实现验收

- 用户依据普通浏览器环境下的25站点结果，确认采用浏览器 Google S2 优先、精确原站 `/favicon.ico` 次选、最终地球的两候选方案。真实截图中 **24个可辨识品牌图案、Qwen为通用地球**，不能写成25个品牌命中；不增加Qwen特例、16像素占位启发式、HTML发现或第三来源。
- R023只修改产品 `app.js`：Google参数固定为 `domain=encoded normalized origin&sz=32`，等待最多4秒；失败后精确origin的 `/favicon.ico` 最多3秒。每个候选每周期至多一次，均失败后保留地球，无第三次、周期轮询或错误弹窗。原20×20槽位、链接href/target/rel、右键复制、键盘与统一tooltip保持。
- 静态归一化在任何外部图片请求前拒绝IPv4及URL可归一化的非常规写法、IPv6、localhost及子域、单标签、`.local`、非默认端口、userinfo及无效主机；只允许HTTP(S)有效域名和IDNA归一化结果，向Google传origin，不传路径、查询或fragment。图片使用 `no-referrer` 和浏览器默认凭据策略，**no-referrer不代表禁用Cookie**。静态检查无法识别域名最终解析到私网，也不提供原代理的DNS固定连接、逐跳重定向、字节大小/MIME/魔数校验；这些是用户已确认的浏览器路径边界，不声称安全能力等同。
- `server.py` 与 `/api/favicon` 保持原样作为回退基线；没有更改全局COEP/COOP/CSP、代理、DNS/TLS或持久化结构。回退只需恢复本阶段前端实现并重建前端产物，既有会话和服务端接口无需迁移。
- 同origin共享、4槽FIFO、128项LRU、成功6小时/失败5分钟缓存保持；一槽覆盖两候选，过期后由新订阅触发新周期。同步流式重绘先注册新消费者，不重置在途额度；无消费者取消、迟到回调隔离、淘汰与最终pagehide清理、bfcache保留通过直接检查。成功图片按origin保留一个已解码资源，各槽仅canvas绘制；没有跨源像素读取或导出。此举避免no-store响应在复制图片节点和重绘时再次请求，缓存重绘也不重置淡入动画。
- 独立no-store实验证据 `code045-r023-nostore-trk03lpt/`：8个同origin槽、5次重绘，修正前累计请求 `[2,3,4,5,6]`，资源复用后 `[1,1,1,1,1]`。全部请求由隔离夹具响应，外网请求0，context/browser已关闭；这是重复请求证据，不是真实品牌图样验收。
- 最终确定性函数检查 **13组通过**（本机Temp `code045-r023-unit-final.txt`），覆盖静态拒绝与origin隐私、主备成功/失败、4+3秒超时和迟到事件、1像素拒绝但不凭16像素猜占位、4槽FIFO、同源共享和流式重绘、取消代际、解码资源复用、容量/正负缓存过期与pagehide。相关前端favicon/tooltip/Markdown/外链回归 **15 passed, 448 deselected in 5.24s**（`code045-r023-related.txt`）；doctor、前端构建/freshness、语法和diff检查通过。未机械重跑无关全量或旧H4会话初始化阻塞。
- 实际bundle/direct classic应用以原 `renderMessages`、Markdown renderer和绑定函数验收，深浅主题×1280/390宽共 **8组通过**（`code045-r023-ui-cmo4jx/result.json`）。主源失败后原槽恢复、双失败地球、6槽20×20无横向溢出、20次流式重绘及流结束无额外请求、统一提示、右键复制、Enter激活和刷新均通过，代表截图已目视核验。每组刷新前5个origin共9次候选请求，重绘后仍9次；刷新创建新周期，不能把跨刷新累计次数称作第三次恢复。
- UI夹具使用合成PNG与no-store响应，隔离宿主仅在夹具关闭路由v2，内存Session不代表完整Session创建/保存链。Playwright会在用户route前自动中止恰好以 `/favicon.ico` 结尾的请求，因此仅返回给隔离页的源码为原站候选加固定测试query；生产仍是精确无query路径，由直接检查验证。首次适配前原站被测试框架中止，后续发现内存Session在reload触发unload beacon，已在测试reload/关闭前清空该测试ID；最终业务断言、写入审计和清理全部通过，未改产品保存逻辑或扩修框架。旧CJK用例同步了浏览器图标断言与准确证据字段，仅做语法检查，不冒充旧H4通过。
- 最终8组页面错误为空，模型/工具/AgentRun均0；自有context/browser、宿主进程、两端口与临时根清理通过，活动子进程0。真实3010/3011、用户会话、模型、保护data与无关交接未操作。
- 产品SHA256冻结于本机Temp `code045-r023-freeze.json`，基线 `770b92648391aef418f125b02bf8459c71859323`。本轮1产品、4测试/夹具、2公共日志共7个公共文件未提交，私有CODE-045只保留结束复核与覆盖限制；本次不提交、推送、打标签或发布。开发会话元数据22.941MiB，正常复用。

### R020–R022 已完成诊断事实与适用范围

- R020旧同源链25域样本完成24域：10个通过解码校验的资源、14个临时失败，Gemini因24秒worker上限未返回，结果未知。10个成功域暖查没有新增候选；14个失败暖查仍失败。已返回结果内共113次候选fetch、57个含重定向HTTP响应，Google33次与DuckDuckGo24次均未取得图片；未知worker内部计数不在其中。失败涉及连接/DNS/TLS、403/404、HTML响应和根favicon缺失，不等于网站没有图标。
- R020成功资源中通义父域 `aliyun.com/favicon.ico` 是32×32全透明图，灰色来自展示背景；其余9个有可见图案，其中8个精确站点、1个文心百度父域替代。3011返回的app.js/bundle原始字节与磁盘一致，服务进程启动晚于基线提交，但未证明用户页面缓存或当前闭包版本；CUA入口失败后未绕过接口检查用户浏览器。
- R020实际renderMessages的6域正常连续30次重绘共6请求、0取消，流结束1.885秒有4图、2.482秒6图；人为制造消费者短暂消失才出现7请求/1取消，未证明真实流式路径存在该间隙。按已测时延合成的25域回放在1.910秒流结束时1成功/4加载/20排队，48.675秒全部终结、40请求/0取消；Gemini按6秒未知失败代入。此回放说明旧候选链占满4槽可拖延后续图标，不是用户精确耗时或可与后续实测计算加速倍数的基准。证据 `code045-r020-zpgn3bw2/`。
- R021固定6域、原站与Google共12个完全一致URL，对照实际用户Chrome152与Python安全客户端。浏览器两轮各9/12加载、Google6/6；Python两轮各仅DeepSeek和Anthropic原站成功、Google全部连接失败。确认路径结果差异，但不能单独归因代理/DNS/TLS/UA/缓存。该诊断页crossOriginIsolated=true且含COEP credentialless/COOP same-origin，不直接代表普通产品环境；首次观察/同页重复也不是严格冷缓存/缓存命中证明。证据 `code045-r021-tg7oypor/`。
- R022随后以无COEP/COOP/CSP响应头、crossOriginIsolated=false的实际Chrome152复测25域，两轮均25/25图片加载且全部来自Google；首轮最后完成4276.1ms、同页重复14.9ms，仍不作严格冷缓存或性能倍率声明。Qwen16×16通用地球、Moonshot28×28，其余32×32；用户截图确认24个品牌图案与Qwen限制，通义已有可辨识图案。证据 `code045-r022-l_pd_fpn/`。R021/R022自有诊断宿主按时退出、端口关闭和租约释放已核对；三轮诊断未修改产品、测试、日志或TODO，本日统一补录事实。
