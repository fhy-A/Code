# 2026-08-03

## 2026-08-03 06:04 · Codex

### 同轮引导协议阶段一地基

- 为运行中后续消息新增同一 AgentRun 的耐久引导协议：`POST /api/agent/runs/{id}/steer` 支持纯文本与多模态用户消息、`clientRequestId` 幂等、同 ID 异内容拒绝、终态冲突和最多 32 条待处理保护；前端运行时客户端已提供对应方法，但本阶段尚未改变输入框发送行为。
- AgentRun 记录升级为 v4，新增待引导消息与不含正文的幂等回执；引导在下一模型边界原子消费一次，完成提交前到达的引导会阻止旧回答把 Run 提前置为终态。服务重启后未消费引导可恢复，旧 v1～v3 记录按空引导状态兼容加载，会话 JSONL 与经典前端未修改。
- 新增 `steer_submitted / steer_consumed` 两类无正文耐久事件，并同步规范 reducer、独立旧影子适配器、H0 事实清单与 v4 兼容夹具。两类事件不改变旧状态、轮次、工具、计时或可见消息投影，也不直接产生界面提示。
- 定向协议、运行时、兼容与前端客户端验证为 `275 passed, 208 subtests passed`，冻结投影契约补测为 `23 passed, 164 subtests passed`，完整回归为 `978 passed, 468 subtests passed in 97.13s`；`npm run check:frontend`、Python/JavaScript 语法、Harness 夹具和 `git diff --check` 通过。只保留既有 `server.py` 无效转义 `SyntaxWarning`。
- 下一阶段把运行中普通发送默认接为同轮引导，同时保留显式排队入口；用户已确认两种消息都按普通用户消息展示，不增加专门状态、徽标或提示文案。

**涉及文件**：`server.py`、`agent-runtime.js`、`agent_protocol.py`、`src/agent/run-reducer.js`、`src/agent/run-projection-shadow.js`、`tests/`、`docs/harness/h0-1-fact-inventory.*`、`docs/harness/same-run-steer-protocol.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 05:15 · Codex

### Harness H2-3 工具运行中连续刷新真实验证

- 在“接受编辑”权限下启动约 15 秒的只读 Node 命令，授权后于命令执行中连续刷新 Code Dev 两次。最后页面从恢复游标继续观察 5 个事件和 1 个快照直至总游标 14，6 次投影比较均零差异，无观察器错误或丢弃诊断。
- 完整 AgentRun 耐久记录为 14 个事件、2 个模型轮和 1 个工具执行：只有一次 `authorization_required`、一次批准、一次 `command_started` 和一次 `tool_completed(succeeded)`，进程约运行 16 秒且退出码为 0；两次页面刷新没有重复授权、重复启动命令或创建第二个工具执行。
- H2-3 目前尚余排队消息、`/parallel` 后台、模型阶段取消及命令阶段取消/失败四类真实轨迹，另需为关键路径补重复样本后再收口 H2。

**涉及文件**：`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 05:05 · Codex

### Harness H2-3 命令授权拒绝真实验证

- Code Dev 在“接受编辑”权限下完成命令授权拒绝路径：单个前台 Run 结束于游标 12，12 个事件、4 个快照和 16 次投影比较均零差异，无观察器错误或丢弃诊断。
- 服务端耐久事件核对为一次 `authorization_required`、一次 `authorization_submitted(rejected)` 和一次 `tool_completed(failed)`；全程没有 `command_started`，说明操作系统命令没有启动。拒绝后只追加一轮模型回答并进入 `completed`，没有重试相同命令、替代工具或补充问卷。
- H2-3 目前已真实通过普通多工具、问卷、命令授权接受/拒绝和模型运行中刷新恢复；仍需工具运行中连续刷新、排队、后台、模型取消与命令取消/失败，并为关键路径补重复样本后才能收口 H2。

**涉及文件**：`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 04:59 · Codex

### Harness H2-3 命令授权接受与轮次投影修复

- Code Dev 的真实命令授权接受验收首先暴露严重回归：未提交的 Completion Guard 实验因脱敏工具回执缺少具体命令和授权匹配事实，把已经成功的 `git status --short` 误判为“缺少工具证据”，再以 `tool_choice=required` 强制重试；工具调用又清空恢复计数，最终形成两次成功命令、一次被重复保护拦截的命令和三次问卷循环。候选回答还在 Guard 判定前进入公开 `model_completed` 投影，导致中间过程提前显示正式回答与 Token 统计。
- 该实验尚未提交且不满足稳定性边界，已从 `server.py` 与 `tests/test_agent_runtime.py` 完整撤回，两文件恢复为当前 `HEAD`，开发实例不再启动隐藏 Guard 模型调用、强制工具轮或循环恢复；会话 JSONL、既有 AgentRun 和 `data/` 未被改写。后续 Completion Guard 必须按“评价不得拥有执行权、成功副作用不可重放、候选回答先内置后公开、独立有界恢复预算”重新设计并单独确认，不能沿用本次实现。
- 同一真实 Run 在游标 24 暴露一条 `modelRoundCount` 差异：服务端 `model_pending` 表示下一轮已经排队，但规范 reducer 和旧影子观察器提前把它计入已开始轮次，旧可见投影只在 `model_started` 时计数。现已统一为只有 `model_started / model_completed` 或快照确认的轮次才增加计数，`model_pending` 仅切换运行状态；契约文档同步明确该语义。
- 新增授权等待、批准、凭据恢复、命令完成、下一轮排队与真正开始的纯回归，分别比较排队前后的轮次及完整影子摘要。定向 Harness/前端模块为 `172 passed, 30 subtests passed`，完整回归为 `978 passed, 465 subtests passed in 95.38s`；`npm run check:frontend`、经典回退、JavaScript 语法与 `git diff --check` 通过。完整回归只保留既有 `server.py` 无效转义 `SyntaxWarning`，本阶段未新增警告。
- 重启 Code Dev 后人工复验通过：授权只出现一次、命令只执行一次、中间不再出现正式回答或 Token 统计、最终回答只出现一次；单个前台 Run 结束于游标 14，14 个事件、5 个快照、19 次比较均零差异且无观察器错误。H2-3 仍需继续授权拒绝、工具运行中连续刷新、排队、后台及失败/取消真实验证，当前不提前收口 H2。

**涉及文件**：`src/agent/run-reducer.js`、`src/agent/run-projection-shadow.js`、`tests/test_run_projection.py`、`docs/harness/h2-1-run-projection-contract.md`、`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 04:10 · Codex

### Harness H2-3 前台单观察者所有权修复

- 首轮真实多工具摘要发现同一前台 AgentRun 被两个页面上下文同时观察：两条摘要均结束于游标 18，其中一个从游标 1 续接并观察 17 个事件，另一个从游标 0 观察全部 18 个事件；先结束的上下文会清理共享 `run.modelRound`，使另一个上下文在终态产生一条 `modelRoundCount` 差异。服务端耐久记录只有一个 AgentRun、两轮模型和五次工具调用，没有重复执行工具；问题位于前端观察所有权与终态清理竞态。
- 新增身份安全的前台上下文 claim/owns/release 边界。普通发送、继续任务和刷新恢复必须先取得同一会话的唯一所有权；恢复上下文不再无条件覆盖 `_activeCtx`，运行循环拒绝第二个观察者，计时、轮次和活动上下文也只有当前所有者可以清理。改动只作用于页面内存与前端编排，不修改 AgentRun、会话 JSONL、工具协议或持久化结构。
- 增加独立所有权回归，直接执行真实辅助函数验证第二上下文无法抢占或清理第一上下文，并对普通发送、继续任务、恢复入口和运行循环做结构约束。定向验证为 `58 passed, 15 subtests passed`，前端模块为 `158 passed`，完整回归为 `975 passed, 473 subtests passed in 105.10s`；`npm run check:frontend`、JavaScript 语法与 `git diff --check` 通过。
- Code Dev 人工验证通过两条前台路径：普通多工具任务只产生 1 条摘要，游标 19、19 个事件、25 次比较均零差异；任务中刷新后只产生 1 条恢复摘要，从持久化游标 6 继续至游标 26，观察 20 个事件、24 次比较均零差异。用户同时确认计时没有明显回退且最终回答只出现一次。
- 本轮另发现工具组在事件更新时偶发大面积内容闪烁，已作为独立渲染待办记录，不阻塞单观察者所有权修复。H2-3 仍需继续验证问卷、授权、队列、后台和失败/取消路径，当前不提前宣告 H2 收口。

**涉及文件**：`app.js`、`tests/test_foreground_run_ownership.py`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 03:25 · Codex

### Harness H2-3 只读摘要提取准备

- 为 H2-3 真实 Code Dev 验证增加浏览器内存级 `window.Code.agent.projectionShadowDiagnostics.snapshot()` 入口；它只在影子开关启用时返回最近 32 条已归档 Run 摘要，关闭时返回空报告，不新增服务端路由、网络上传、浏览器存储或持久化字段。
- 导出边界不直接暴露页面状态对象，而是重新按固定结构构造副本并再次执行枚举白名单。报告只保留版本、计数、前台/后台类别、规范状态、事件游标、白名单诊断代码和比较字段；伪造的 Key、Run/会话 ID、工具参数及任意未知字符串不会进入结果，调用方修改返回对象也不会污染原摘要。
- 自动验证覆盖启用/关闭、有界截断、聚合计数、返回副本和敏感字段注入；定向结果为 `165 passed, 15 subtests passed`，完整回归为 `976 passed, 473 subtests passed in 98.42s`，`npm run check:frontend`、JavaScript 语法与 `git diff --check` 通过。
- 该阶段只完成真实验证所需的安全提取能力，未把它记录成 H2-3 验收完成。普通多工具、问卷/授权、连续刷新、队列、后台和失败/取消仍需由用户在 Code Dev 中逐项运行并回传脱敏报告，稳定零差异后才收口 H2。

**涉及文件**：`app.js`、`src/agent/run-projection-shadow.js`、`tests/test_run_projection.py`、`tests/test_frontend_modules.py`、`docs/harness/h2-2-frontend-shadow-projection.md`、`docs/harness/h2-3-shadow-validation.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 03:13 · Codex

### Harness H3-1 统一离线轨迹回放器

- 在不改变生产 AgentRun、会话 JSONL、工具协议或前端投影的前提下，新增独立 `replay-agent-traces` 入口，复用 H2-1 纯 reducer 与 View Model 离线回放仓库内脱敏合成轨迹；runner 不读取 `data/sessions/`，不调用模型、工具、网络或浏览器。
- 现有 15 条 H0 轨迹、106 个事件和 17 个检查点已统一接入；每条轨迹验证连续序号、合法状态、冻结检查点、终态、重复投递幂等和确定性 SHA-256 状态哈希。全部 17 个检查点均经过 JSON 往返恢复，另覆盖 4 个页面刷新、轮询断线和服务重启显式恢复点。
- CLI 支持按轨迹名或标签定向运行、列出场景及 JSON 输出；差异会报告首个相关事件序号、状态路径、期望值和实际值。自动测试故意删除事件、交换事件顺序及篡改检查点，分别准确定位到 `$.events[*].seq` 和 `$.status`。
- 定向验证为 `30 passed, 171 subtests passed`；完整回归为 `975 passed, 473 subtests passed in 111.35s`。`npm run verify:harness-replay`、`npm run check:frontend`、JavaScript 语法与 `git diff --check` 通过，整套回放哈希为 `50026eaa82cf059158910896bcaf63c65415343d85b9652715c7ed2b794bfe90`。
- 本阶段只是提前完成 H3 的 runner 地基，不将 H0 的 15 条代表性轨迹误报为优化计划第六节 15 类最终完整覆盖，也不替代 H2-3 真实开发实例验证。下一步仍先收口 H2-3，再补充近期跨层 Bug、排队/并行、Child AgentRun、图片降级和旧记录恢复等 replay 夹具并接入正式发布门禁。

**涉及文件**：`scripts/replay-agent-traces.cjs`、`tests/test_harness_replay.py`、`package.json`、`docs/harness/h3-1-replay-runner.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 01:40 · Codex

### Harness H2-2 前端只读影子投影

- 在 H2-1 纯契约之上新增独立 `run-projection-shadow`：规范 reducer 与旧投影适配器在同一 AgentRun 事件/快照边界并行只读计算，现有消息投影、工具组、状态栏、授权和问卷仍是唯一用户可见实现；观察器异常 fail-open，不影响旧投影、游标推进、模型或工具执行。
- 前台普通任务、排队/刷新恢复及后台 AgentRun 均完成接线。前台事件先进入新 reducer，旧投影成功后才推进独立旧适配器并比较；快照在事件全部投影完成后比较七类冻结字段。旧时间线拥有独立类别、状态与引用映射，不复用新 reducer 映射，也不从 DOM 文案反向解析领域事实。
- 增加 `CODE_AGENT_PROJECTION_SHADOW`：开发实例默认开启、正式实例默认关闭，显式真/假值可覆盖，无法识别时回到实例默认值；心跳只返回布尔能力，前端收到明确 `true` 前保持关闭。关闭时不创建观察状态、不执行 reducer 或比较，无需迁移旧 AgentRun、JSONL 或配置。
- 差异只记录 `code / field / cursor / status` 与计数；单 Run 最多保留 64 条样本，当前页面最多保留 32 个 Run 摘要，全部仅在内存中。诊断不包含 Run/会话 ID、模型或思考正文、工具参数/结果、命令输出、文件内容、diff、错误原文或凭据，也不写持久数据、UI 或 API 业务响应。
- 自动验证覆盖零差异轨迹、故意差异、观察器异常、64 条上限、超限计数、敏感参数/结果不进入摘要、前后台接线、开发/正式默认值与显式覆盖。定向结果为 `193 passed, 15 subtests passed` 和 `176 passed, 212 subtests passed`；完整回归为 `966 passed, 450 subtests passed in 66.78s`，`npm run check:frontend`、JavaScript/Python 语法和 `git diff --check` 通过。
- 本阶段尚未把自动测试结果冒充为真实任务零差异。下一步 H2-3 在开发实例观察普通多工具、授权/问卷、连续刷新、队列、后台及失败/取消轨迹，修正真实差异后再决定 H2 是否收口。

**涉及文件**：`app.js`、`server.py`、`src/agent/run-projection-shadow.js`、`src/core/state.js`、`src/frontend-entry.js`、`scripts/build-frontend.mjs`、`tests/test_run_projection.py`、`tests/test_frontend_modules.py`、`tests/test_agent_runtime.py`、`tests/test_routes.py`、`tests/fixtures/harness/compatibility/classic-frontend.json`、`docs/harness/h2-1-run-projection-contract.md`、`docs/harness/h2-2-frontend-shadow-projection.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`

## 2026-08-03 01:00 · Codex

### Harness H2-1 Run 投影契约与纯函数边界

- 在 H1 收口后新增独立 `run-reducer` 和 `run-view-model`：规范输入显式区分 H1 归一事件与服务端/前端快照事实，状态可 JSON 序列化并从检查点继续重放；两个模块不访问 DOM、网络、文件、存储或系统当前时间，也不执行模型和工具副作用。
- 冻结首批新旧投影比较字段：Run 状态、终态、模型轮数、按 `toolCallId` 去重的工具数量、待授权/问卷/凭据类型、显式参考点计时与结构化时间线。时间线只保留事件序号、类型、类别、状态和引用 ID，不收集模型正文、思考、工具参数/结果、命令输出、文件内容、diff、错误原文或凭据。
- 22 类 H1 事件均有稳定类别和阶段映射；重复序号幂等、缺口和未知未来事件可诊断并继续、终态不可回流。活动计时只在调用方明确提供参考时间时累计，终态由显式完成时间收口，避免纯投影读取墙钟造成刷新回退或测试漂移。
- 两个纯模块已进入默认 bundle 和经典回退脚本顺序，但未接入 `projectAgentEvent()`、Agent 轮询、旧状态、消息 DOM 或用户界面；未修改 AgentRun、会话 JSONL、工具协议、服务端 API 和开发/正式实例开关，可以通过移除模块与入口引用整体回退。
- 15 条 H0 合成轨迹的全部检查点状态、终态和时间线均与冻结基线一致，检查点 JSON 往返继续重放与从头重放相同。定向验证为 `169 passed, 21 subtests passed` 和 `115 passed, 212 subtests passed`，完整回归为 `961 passed, 442 subtests passed in 89.29s`；`npm run check:frontend` 通过默认 bundle、经典回退、构建新鲜度和 JavaScript 语法检查。
- 下一阶段 H2-2 才在现有事件/快照边界加入可关闭的只读影子计算与旧投影比较适配器；差异只允许使用同一显式计时参考点，保存脱敏、有界、内存级代码和计数，不写入持久数据或 UI。

**涉及文件**：`src/agent/run-reducer.js`、`src/ui/run-view-model.js`、`src/frontend-entry.js`、`scripts/build-frontend.mjs`、`tests/test_run_projection.py`、`tests/test_frontend_modules.py`、`tests/fixtures/harness/compatibility/classic-frontend.json`、`docs/harness/h1-4-shadow-credential-diagnostics.md`、`docs/harness/h2-1-run-projection-contract.md`、`TODO.md`、`docs/development-log/README.md`、`docs/development-log/2026/2026-08-03.md`
