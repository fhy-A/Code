# 2026-08-14 开发日志

## 2026-08-14 21:29 · Codex

### CODE-025 编辑 Diff 默认收起与授权定位完成

- 编辑建议卡片的 Diff 正文首次展示与完整刷新后默认收起，文件路径、增删统计、状态和授权操作持续可见；多个编辑建议按当前页面内的独立瞬态状态展开，同轮流式、授权状态和完成投影更新不会反复收起，超长 Diff 仍保留原有二级“展开全部”保护。
- 授权面板“查看”会展开并定位对应编辑建议，提供短暂低强调高亮，同时保持授权面板、勾选和批准/拒绝语义不变；普通重绘不抢历史阅读滚动，完整刷新或重新打开会话恢复默认收起。
- 重复同源提案的前端实例身份按 `authorizationId`、历史兼容的 `agentRunId + toolCallId`、最终 `pendingEditId` 依次推导，并贯穿 DOM、展开集合、授权查看与结果投影；完成事件按 run 与 tool call 隔离，因此第一次拒绝与第二次应用各自保持正确卡片和状态。
- 本阶段不改变服务端 deterministic proposal ID、授权 API、apply 幂等、Session JSONL、AgentRun、协议或持久化；旧数据只在前端投影时推导实例键，不迁移、不改写。
- 自动门禁沿用最终人工验收现场：统一 UI 为 `224 passed`，bundle/direct classic 编辑授权 H4 为 `6 passed`；`npm run check:frontend`、bundle freshness、Node 语法和 `git diff --check` 均通过，真实双轮证据为两次提案、两次授权、一次 apply、一次写入、一次备份且无重复副作用。用户随后分别确认 Code Dev 与 direct classic 人工验收 PASS。
- 有限边界：只读会话证据显示，同一 AgentRun 中第一次授权被拒绝且未写入后，模型可主动重提并产生不同 `authorizationId`；第二次仍需用户再次明确批准，最终只应用和写入一次。这不是自动越过拒绝，不阻断 CODE-025，也不扩展为新的拒绝策略或 TODO。
- `CODE-025` 已按 canonical TODO 契约移出未完成清单，确认推进序列缩短为 `CODE-002 → CODE-032 → CODE-031 → CODE-019`；未启动下一事项。回退只需撤销本阶段独立提交；未执行 push、tag 或 release。

## 2026-08-14 11:31 · Codex

### CODE-005 工具组内容闪烁与折叠交互稳定性完成

- 工具生命周期普通更新改为按稳定身份协调执行轨迹、工具流程组与工具项节点；标题、状态、参数和结果在原节点更新，避免整组销毁重建造成的大面积闪烁，并保持工具顺序与内容唯一。
- 折叠状态由真实前台任务生命周期统一拥有：活动任务中跨模型说明、相邻工具轮、失败后继续以及授权/问卷等待均保留用户展开态；用户主动收起不会被后续工具强制重开；真实成功、失败或取消终态只自动折叠一次，完整刷新仍默认折叠。
- 人工验收期间依次修正三类同一交互面的回归：折叠控制标题的主键操作不再建立跨工具正文、Diff 或阶段说明的异常 Selection；指针来源焦点显式抑制浏览器白色原生轮廓；跨 Code Dev / direct classic 失焦恢复后继续抑制主题绿色轮廓。正文、Diff 与阶段说明仍可选择复制，Tab/Shift+Tab 的键盘 `focus-visible`、Enter/Space 和 `aria-expanded` 保持可用。
- 自动验证沿用最终人工验收现场：direct classic 与重建 bundle 的跨入口 H4 均 PASS，统一 UI 为 `221 passed`；`npm run check:frontend`、Node 语法、bundle freshness 与 `git diff --check` 均通过。验证资源已清理：隔离端口关闭、临时根移除、活动子进程为 `0`。
- 用户先后复验工具组无闪烁、Selection、白色/主题色焦点轮廓，以及 Code Dev → direct classic → 返回 Code Dev 的真实跨入口序列，最终明确确认 PASS。`CODE-005` 已按 canonical TODO 契约移出未完成清单；`CODE-025` Diff 默认收起、工具组流光与 `CODE-026` 不同会话 Agent 通信等相邻事项仍保持未完成且未启动。
- 本阶段不改变服务端、Session JSONL、AgentRun、API、协议、持久化或滚动所有权；未执行 push、tag、release，也未选择或启动下一项 TODO。

## 2026-08-14 07:50 · DSH

### CODE-002 顶部工具日志产品决策：按"移除"收口

- 用户确认产品决策：移除顶部工具日志面板，后续升级方向（per-run 轨迹视图 + 副作用 manifest 的调试/回退审计）另行立项，不在本决策内实现。
- 决策依据：现状 `renderToolLog()`（`app.js`）仅从当前会话 `state.messages` 过滤 `tool-call`/`tool-result` 派生，是对话流工具组的降级副本，且面板未实现其"顶部全局"定位（无跨任务/后台/并行聚合）；失败判定依赖 `startsWith(t("toolExecFailed"))` 文本前缀，与 i18n 文案耦合；目标受众为开发者调试/企业回退时，action/detail/pill 三元组信息过薄，无法支撑调试与回退。
- 与 DSH Goal 调研同期对照确认：DSH 会话轨迹设计（单一事件真源 + 派生投影 + 结构化 outcome + 呈现意图分离）可作为后续"调试/回退审计"升级方向的参照，但 DSH 无"顶部全局工具日志面板"对应物，去留属于 Code 自己的产品决策。
- `TODO.md` 的 `CODE-002` 已按 canonical 契约更新：保留稳定 ID，标题改为"顶部工具日志移除"，类型/风险改为 UI 移除 / STANDARD，写入决策理由、移除实施清单（`app.js`/`index.html`/`src/ui/panels.js`/`src/core/i18n.js` zh-en 成对清理及回归测试）与验收定义（移除后"唯一错误或进行中状态"由对话流工具组承担，bundle/direct classic、i18n、旧会话读取门禁通过）。
- 本次仅更新 `TODO.md`，未改动任何代码；未创建 git 提交。移除功能本身属 STANDARD 级 UI 改动，等待方案确认后另行实施。

## 2026-08-14 07:31 · DSH

### DSH Goal 模式调研分析文档落盘

- 新建 `docs/dsh-goal-mode-research-analysis.md`（v1.0，2026-08-14 07:27），未修改 `docs/goal-mode-development-guide.md`；原设计文档仍是 Code Goal 的规范性依据，调研文档只记录 DSH 实测设计供实施对照。
- 调研基线为本机 DSH 部署随附包 `0.1.0-rc.6`：`dsh-goal`、`dsh-goal-round-driver`、`dsh-tool-goal` 三个包源码全文逐行阅读并交叉核对 README；`dsh-command-goal` 仅读 README，未逐行核对命令分发实现；未做真实运行验证，局限已在文档第十一节如实标注。
- 已核实的 DSH 关键事实：持久层只存 phase/revision/rounds 等事实，`activation`（续行授权）进程本地且绝不持久化，每次 `agent/session-start`/fork 一律 disarm，必须人类显式 `resume` 重新武装；goal 变更以全量快照 `goal/change` 事件 + tombstone 进会话日志；CAS（精确 `{id, revision}`，stale 拒绝）；工具 `get_goal`/`create_goal`/`update_goal` 三层权限（精确活跃实例 + 开放轮次 + create/edit/pause/resume 需直接人类、complete/blocked 接受精确 Goal Round）；blocked 机械门槛默认 3 轮（`blockedAfterConsecutiveRounds`），人类直接授权可立即 block；续跑驱动器 round-limit/queue-failed/prompt-rejected 三场景自动 block。
- 调研文档与 `goal-mode-development-guide.md` 逐条对照，为第十四节五个开放问题给出 DSH 落地答案（存储选会话事件扩展、窄工具、3 轮默认门槛、三档权限）；同时列出 DSH 明确不做的差异清单（步骤计划、证据分层、预算），由 Code 自行设计。
- 本次仅新增一个未跟踪文档，未改动任何代码；按用户指示记录日志但不创建 git 提交。无 TODO 项变更，无活动 handoff 关联。

## 2026-08-14 01:24 · Codex

### CODE-003 新消息阅读锚点与流式滚动完成

- 真实普通前台轮建立仅存在于当前页面的阅读锚点；用户消息顶部目标为桌面约 `32px`、宽度不超过 `900px` 时约 `20px`。该状态不写入 Session JSONL、AgentRun、服务端 API、协议或持久化数据。
- 锚点按当前视口动态生成临时底部空白。短回复结束后可以暂留剩余阅读空间；回答、工具或图片等真实内容增长与用户向上滚动只会单调消耗空白，同一轮不会因普通重绘、布局重算、缩放或 composer 高度变化重新增加。
- 用户向上滚动后由既有集中控制器立即交出自动跟随。首次真实向下滚动或点击“一键到底”会一次释放全部剩余空白，随后按真实消息底部计算滚动目标，避免先滚入空白再发生第二次跳转。
- 滚动控制器把两个距离明确分离：滚动所有权距离保留已消耗量以维持防回弹语义；按钮显隐只使用排除剩余临时空白、已消耗量和布局补偿后的真实内容距离。真实内容距离 `159px` 时隐藏、达到既有 `160px` 阈值才显示，回到既有 `2px` 容差内隐藏并恢复跟随。短回复视口底部仍处于临时空白时不再误显按钮。
- queue 只在消息真正开始前台执行时建立锚点；有效 steer 被服务端接受后替换当前锚点，`409` 入队等待真实执行；恢复中的 steer 与 detached `/parallel` 不建立或替换当前前台锚点。刷新或切换会话会清除临时状态并继续定位真实最新消息。
- 自动验证：滚动控制器定向 `5 passed`；统一 `python verify.py ui` 为 `219 passed`；`npm run check:frontend` 的 bundle/direct classic 构建、freshness 与 bundle 语法通过；`node --check` 和 `git diff --check` 通过。
- 用户最终确认 Code Dev 与 direct classic 两个入口人工验收均 PASS。人工确认范围聚焦短回复、按钮误显修正与两个入口的最终体验；未把所有工具/图片组合或全部移动设备写成已人工覆盖。
- `CODE-003` 已按 canonical TODO 契约移出未完成清单。回退只需撤销本阶段独立本地提交；本阶段未 push、tag、release，也未选择或启动下一项 TODO。
