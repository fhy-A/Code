# Code 开发日志索引

> 本目录是公共完成事实源：记录已经完成的重要改动、产品决策和验证结果。内部未完成事项与活动交接分别以 sibling 私有 `../../../workbar-private/TODO.md`、`../../../workbar-private/development-handoff.md` 为唯一事实源；公开 `../../TODO.md` 只是用户批准的脱敏短期摘要，`../development-handoff.md` 仍是兼容 stub。

## 阅读方式

1. 开始任务时先阅读本索引。
2. 默认只读取最新日期文件，以及与当前任务直接相关的日期文件。
3. 只有需要追溯早期实现或决策时，才读取历史归档；不要在常规交接中加载完整归档。
4. 内部工作区读取 `../../../workbar-private/TODO.md`，并在项目根目录运行 `git status --short`、`git log -8 --oneline`；公开摘要是非执行信息，Agent 不得据此自动选择或启动任务。私有事实源缺失的外部 clone 只按用户显式范围工作，不得恢复或重建内部计划。
5. 检查 `../../../workbar-private/development-handoff.md` 是否存在；用户要求继续 / 接手或任务范围重叠时读取，无关任务不得覆盖。
6. Git 现场、公共日志、私有 TODO 和私有活动交接冲突时，以 Git 现场和可复现结果为准，并修正私有交接。

新会话可以直接接收用户任务并按以上流程自助入场，不要求上一会话为已经完成的阶段重复生成交接消息。

## 写入规则

- 新日志按本地日期写入 `YYYY/YYYY-MM-DD.md`；当天文件不存在时新建。
- 同一天可以记录多个阶段，每条使用 `## YYYY-MM-DD HH:mm · 协作方` 标题并按时间倒序排列。
- 只记录已经完成且有事实依据的内容；未完成事项只写入私有 TODO。
- 公开短期待办摘要仅在用户明确批准后人工更新，不得从私有 TODO 自动同步，也不表达优先级、排期或发布承诺。
- 阶段任务尚未完成、需要暂停或切换 Codex / Claude Code 等 Agent 时，按 `../development-handoff-template.md` 创建或更新私有 handoff。
- 阶段完成后，将长期事实写入公共日志、剩余事项写入私有 TODO，然后移除私有 handoff 中对应活动差量。
- 每次新增日期文件或改变当日主题时，同步更新下方索引。
- 发布说明、更新摘要和开发日志默认使用中文，仅保留必要的英文技术字段。
- `archive/CHANGELOG-through-2026-07-29.md` 是冻结的历史原文，不再追加或改写。

## 日志索引

| 日期或范围 | 摘要 | 详细记录 |
| 2026-09-02 | CODE-065 PPTX clean-room 与可迁移 Skill 资源闭环完成：pptx / xlsx 资源合同以服务端白名单返回受身份、普通文件 / reparse、大小与哈希约束的精确 runtime resources；已知旧 bundled 身份只读 fallback，默认同根 Code Dev 的显式 custom manifest 也可使用自己的可移动资源与 local dependency，未知 / 篡改 / 冲突均 fail-closed。当前 bundled audit 无 finding，真实 LibreOffice PPTX 渲染、R003 资源合同 / PPTX / 静态复跑 35 tests、server/security/Skill 410 passed、此前 AgentRuntime 137 passed + 81 subtests、frontend 417 passed、doctor 10/10 与非发布 PyInstaller 通过；CODE-055 评测仍暂停，须用户新的 STRICT 授权 | [查看](2026/2026-09-02.md) |
|---|---|---|
| 2026-09-02 | `CODE-062` XLSX Skill 自有重算资源与路径闭环完成：服务端白名单返回经身份 / tombstone / reparse / 哈希校验的精确资源路径，新安装使用 installed 资源，已知旧安装只读回退 bundled 且不覆写；公式能力严格验证真实 LibreOffice，重算具备外部引用门禁、有限超时与原子发布。直接 `19 tests`、Skill 依赖 / runtime `86 tests`、AgentRuntime / P2 `151 tests`、frontend `417 tests`、server / security / P2 `406 tests`、doctor `10/10` 与非发布 PyInstaller 通过。此前 `CODE-061` 自动模式范围引导与风险校准也已完成；`CODE-055` bypass 评测继续暂停，待独立复核与用户再次授权 | [查看](2026/2026-09-02.md) |
| 2026-09-01 | `CODE-043` 新手任务界面单层紧凑收敛：标题旁进度、内容驱动三步轨、仅当前步骤、短标签 / 完整 prompt 分离、共享 SVG 与无内滚动完成；absolute 锚定、`inset:auto` 和 `10px` 间距修复水平 / 纵向回归。doctor `10/10`、onboarding `5 passed`、完整 frontend / P2 `473 passed + 4 subtests`、build / freshness / 语法 / diff 通过；两套 H4 各两轮失败均按熔断停止且未记为通过，同源 ignored 审计页完成中英文 / 深浅 / 状态 / 布局验收后用户 `PASS` | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 发布与验证支持模块归位 `devtools/`：`release_state` / `verification` 以 Git rename 迁入轻量包，根 release / verify 用户命令不变且无 shim；verification ROOT、检查顺序、profile、definition fingerprint 与 manifest 哈希迁移前后逐项一致。布局、release script/workflow、verify profiles `66 tests`、doctor `10/10`、frontend freshness、AST / diff 通过，published 凭证与发布边界不变，未运行真实 release | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 后端实现模块归位 `code_runtime/`：13 个当前运行模块以 Git rename 迁入轻量包，入口 / first-party tests / mock patch 全部切换且无根 shim，PPT APP_ROOT 与全部资源目标保持。布局 `2 tests`、直接 `231 tests`、入口兼容 `666 tests`、完整 Python `1809 tests`、frontend build/freshness、doctor `10/10` 通过；非发布 PyInstaller 成功收集新包，用户原 EXE 按原哈希恢复，frontend / release notes 哈希不变，临时根与 build 清理 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 未接入运行时的 Goal v1 移除：调用图确认三个旧模块仅由三份专属测试消费后删除全部六文件且不留 shim；`data/goals/` 旧 sidecar 继续原地保留、不读不迁移。布局合同以 AST / 动态导入形态阻止运行时回流，fail-first `1 failure` 后 Goal v2、AgentRun、frontend、server / Session 组合回归 `363 tests` 与 diff 检查通过 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 重复品牌图标与 H4 配置收口：删除与根图标字节相同且无产品消费者的 `assets/code-icon.png`，导出器重跑后四个保留 PNG / ICO 哈希不变且副本未重建；Playwright 配置归位 `tests/e2e/h4/`，以显式路径保持原 test/output 位置和全部运行参数。fail-first `2 failures` 后定向 `2 tests`、完整前端 `416 tests`、H4 infra 与新配置 smoke 列表 `164 tests`、语法 / JSON / diff 通过，未运行真实 H4 或 Browser | [查看](2026/2026-09-01.md) |
| 2026-09-01 | Code 品牌设计源收敛：最终 7 个 stacked-circles 构造稿与导出器无损迁入唯一 `design/code-logo-final/`，增加目录说明，其余 68 个 tracked 历史候选从当前树删除且 Git 历史可追溯；导出器重跑后五个 PNG / ICO 哈希逐项不变、无生成资产差量，定向 `1 test`、完整前端 `415 tests`、AST / diff 与唯一目录清单通过，未改写历史日志或触碰发布 / 用户 data | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 发布构建移除版本化 PyInstaller spec：七份内容等价的 `Code-v0.6.1`～`v0.6.7.spec` 从当前树删除，`build_exe.py --specpath build/` 成为唯一生成路径，release 的未来元数据白名单 / 凭证 / 暂存不再含 spec，既有 published 凭证不变。fail-first `2 failures + 3 errors` 后 release / workflow / frontend `458 tests`、frontend freshness、语法 / diff 与一次非发布 PyInstaller 构建通过；EXE 仍为 v0.6.7 且唯一 spec 位于 ignored build | [查看](2026/2026-09-01.md) |
| 2026-09-01 | Code 项目目录保守整理第一阶段：七个精确白名单空目录 / Python / pytest 缓存经 containment、reparse、内容类型及 tracked=0 审计后删除，共 273 个文件、18,050,456 字节；新增唯一 `.pytest_cache/` 忽略规则。两个无引用的一次性 PPT 脚本以原字节迁入 `scripts/legacy/presentation/`，哈希与 AST 解析保持，README 明确非产品入口；未改变运行、构建、发布或用户 data | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 公开短期待办摘要边界测试加固：精确锁定唯一“近期方向”章节的四项与顺序，全文拒绝嵌入式 CODE / WB ID、渠道、供应商、安全缺口和具体日期，同时允许现有禁止承载字段说明；7 个纯内存对抗样本命中预期违规类别，定向 `6 tests` 与 diff 检查通过。公开 TODO blob、私有 TODO 哈希及全部规则 / handoff 均未改变 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 公开短期待办摘要与私有计划边界：公开 TODO 仅列用户批准的四个高层方向，明确顺序非优先级、非排期 / 发布承诺且不可自动启动；摘要仅经用户明确批准人工更新，禁止从私有 TODO 自动同步。内部未完成事项仍只以仓库外 `workbar-private/TODO.md` 为 canonical，私有 TODO / handoff 字节与 SHA-256 不变，公开 handoff 仍为 marker stub；边界测试 `4 failures → 5 passed`，规则同步与 diff 检查通过，未运行产品验证或外部操作 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 私有计划事实源前向隔离：基线完整 TODO 与活动 handoff 先以原 SHA-256 / 字节 / 行数无损复制到公共子仓库之外的 `workbar-private/`，31 个稳定 ID、双域游标和活动差量均保留，且私有目录不是 Git 仓库；公开 TODO / handoff 改为 marker stub，根与 code 规则、README、日志规范、模板、发布和 approval-relay 指向私有源。私有源缺失的外部 clone 仍可按用户显式任务工作但不得重建公开计划；边界回归 `5 failures → 5 passed`、doctor `10/10` 通过 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 执行轨迹 Skill 标记与模型未选提醒：当前用户轮次的可选 `activeSkillNames` 元数据投影为标题轻量芯片并保留完整可访问名单，旧 Session / Tool 分组 / 协议不变；新消息提交前按刷新中、未选择、目录为空、路由不可用给出中英文无副作用提醒，停止与跟进队列不受影响。定向 `3 tests`、完整前端 `415 tests`、build / freshness / 语法 / diff 与 bundle/classic model-free H4 通过；3011 Browser 点击 / Enter 均保留草稿和 45 条会话，用户截图确认 `Skill · imagegen` 位置与顶部“请选择模型”提示并完成视觉 `PASS`。Skill 在 AgentRun 启动时冻结，同轮不动态追加；自动多 Skill 沿用最多三项并以首项 `+N` 展示 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | Bundled Skill 升级补齐与删除持久化：launcher 逐目录只补缺失且未 tombstone 的 bundled Skill，29→31 明确补入 ppt-master/imagegen，现有 bundled/local/custom 字节不变；copy staging/delete quarantine 位于 skills 同级受控工作区，状态写前完整回滚，tombstone 提交后即使部分清理失败也不恢复残缺 Skill，所有残留不可枚举。R002 对抗 `2 errors → 10 tests`、Skills CRUD/Launcher `18 tests`、image runtime/builtin `49 tests`、doctor `10/10` 通过；imagegen 目录缺失不影响合法 generate_image 工具 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | Skill 系统依赖指引与 Git Bash 无重启重检：Windows 缺失 bash/qpdf 且无 manifest 显式提示时派生精确 winget 协作命令，显式值仍最高优先；bash 在当前/注册表 PATH 后从 Git cmd 根及 Program Files、x86、LocalAppData 六个常见位置解析，每次检查读取新鲜环境且不改 PATH。fail-first `7 failures + 2 errors`、新增检查 `11 tests`、完整 Skill dependencies `33 tests`、doctor `10/10`、语法/diff 通过；未执行安装或改变 manifest/binding 身份 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | 发版元数据自动同步：修正 v0.6.7 README badge 陈旧 alt，并让 canonical 更新/一致性门禁统一同步和校验 badge URL、`Version X.Y.Z` alt 与 `Code-vX.Y.Z.exe`；只作用于精确版本徽章和具体 EXE 名，其他图片 alt / 链接 / 正文不变，相同版本幂等。fail-first `11 failures`、直接 `3 tests`、两套 release 定向 `42 tests`、doctor `10/10`、语法/diff 通过；未修改已发布 tag/Release/EXE 或运行发布命令 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | v0.6.7 manual-compaction Harness UI 证据受控重基线：首次 prepare 的 `6 failed` 均定位为前端视觉演进后的冻结 UI 哈希漂移；D2 只刷新 `htmlHash/replayHash`，D3 只刷新 UI / pending UI 与派生 scenario / suite 链，v0.6.6 独立导出的 19 个非 UI 场景哈希逐项一致。D2+D3 定向连续两轮各 `13 tests`、全部 Harness `110 tests` 通过；未重新 prepare / publish，发布说明草稿保持冻结未暂存 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | `CODE-043` 全局表单焦点样式统一：独立 input / textarea / select 收敛为单一 `1px` 强调边框，搜索 / 项目编辑由外壳唯一描边，System Prompt 仅底线，composer 只保留中性表面阴影；按钮、checkbox / radio、drag / disabled 及语义状态不变。fail-first `4 failures`、直接兼容组 `15 tests`、完整前端 `413 tests`、build / freshness / bundle 语法 / diff 通过；3011 Browser 在 Notion 深浅主题验证代表控件的单层边界与零外扩 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | `CODE-043` 浅色输入框表面与工具栏分割线收口：空 / 普通会话底部工具栏顶边归零，Codex / Notion 浅色 composer 与页面同底色并仅保留轻阴影，focus / drag / disabled 层级与 Notion 深色原表面不变。直接组 `6 tests`、完整前端 `409 tests`、build / freshness / bundle 语法 / diff 通过；3011 Browser 完成三主题、空 / 普通会话的底色、边线、阴影与零布局位移复验 | [查看](2026/2026-09-01.md) |
| 2026-09-01 | `CODE-043` 导入会话窗口优化完成：约 `760px` 固定头尾单窗口、共享 close / refresh SVG、无胶囊来源计数、精简筛选与条件 x/y 摘要、普通 available 行无徽标、`14 / 12px` 行层级及单一“导入所选”主按钮完成收敛；首次 5 行骨架、缓存重扫/失败只读锁定、generation 原子替换与 tab/tabpanel/焦点归还闭合，导入 API/协议不变。doctor `10/10`、导入组 `19 passed`、完整前端/P2 `464 passed + 4 subtests`、build/freshness/语法/diff 通过；中英文、Notion 深浅与 `1686 / 740 / 390px` Browser H4 零溢出且未执行真实导入 | [查看](2026/2026-09-01.md) |
| 2026-08-31 | `CODE-043` 顶部工具信息栏最终审计完成：侧栏开关收敛为单一共享 SVG，桌面保留完整标签与统计、中等宽度图标化、窄宽状态摘要；Session Info / 分支补齐 disclosure、互斥、外部点击、Escape 焦点归还和切会话同步。doctor `10/10`、完整前端 `404 passed`、build / freshness / 语法 / diff 通过；Notion 深浅、中英文及 `1686 / 740 / 390px`（390px 折叠侧栏）Browser H4 零溢出并恢复中文 Notion 深色现场 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | 预览工作区任务归属统一：用户决定预览侧栏 / 正文的视觉、交互、响应式、renderer 与多标签工作区均由 `CODE-042` 唯一承接，不再作为 `CODE-043` 独立批次；2026-08-24 已确认指导及点击复用、显式新标签、项目隔离、兼容、安全与分阶段合同不变，`CODE-042` 仍未实施，未引入未确认补充方案 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` workbar 账号设置页视觉优化完成：分散身份 / 用量 / 详情卡收敛为单一概览，紧凑身份与连接状态、三项无边框用量指标、扁平账户信息及自然同步 / 重试状态完成统一，登出仅在 hover / 焦点强化危险反馈，原认证、刷新、登出、用量格式、API 与持久化语义不变。doctor `10/10`、定向 `3 passed`、完整前端 `402 passed`及 build / freshness / 语法 / diff 通过；深浅主题、中英文、桌面 / `740px` Browser 复核后用户 `PASS`，真实登出与未登录态变更未执行 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` Skills 高密度设置页完成：可搜索 `240px` 主从工作区、独立启用 / 依赖状态与扁平详情；启用、编辑、删除统一为 `40×40px` 纯图标并保留 pressed / tooltip / 焦点，关键词 / 工具 / 路径全宽纵向。新建 / 编辑 modal 固定头尾且仅中部滚动，高级依赖默认折叠并清理空提示条，CRUD / rename / 依赖 payload 不变。完整前端 `401 passed`、build / freshness / 语法 / diff 通过；深浅主题、中英文、桌面与 `740px` Browser 复核后用户 `PASS` | [查看](2026/2026-08-31.md) |
| 2026-08-31 | Skill Runtime Binding v1 完成：AgentRun 冻结 active Skill / capability 并持久化可选能力级 binding，普通命令在授权前门禁、启动前实时重检，只向子进程注入可信受管 Python / Node 环境；stale、冲突、越界 / reparse 均零启动。受管安装继续要求用户授权且不使用未就绪 runtime，安装后须重新检查再绑定。最终绑定 / 依赖 `41 passed + 10 subtests`、AgentRuntime `137 passed + 81 subtests`、server `263 passed + 47 subtests`，此前恢复 / security 与 doctor `10/10` 证据保持；未使用真实用户 Skill、运行时、网络或 Browser，Skills 设置页仍未启动 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 模型与生图模型高密度设置页完成：模型页统一为连接 / 可用模型 / 模型参数三分区，workbar 次级操作与 `920px` 主体右沿对齐；生图页使用单层连接卡、轻量 focus 和统一 `40 / 32px` 控件，默认区最终为“标题+状态 / select+应用配置”两行并保留原保存+refresh+routeRef 语义。完整前端 `394 passed`，bundle / classic build、freshness、语法与 diff 通过；Notion 深色与 Codex 浅色经多轮 Browser 返修后用户 `PASS`。Skills 保留为下一独立阶段，`760px` 仅有确定性合同 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 设置共同壳与轻量旧页批次完成：统一 `18 / 14 / 12px` 信息层级与 `40 / 32px` 控件尺度，System Prompt、编辑器和更新页使用轻量结构；记忆页重设计为列表 / 表单互斥的单工作区，R003 修正 form 模式页头新建按钮的 `hidden` 级联。完整前端 `389 passed`、R003 定向 `4 passed`，bundle / classic build、freshness、语法与 diff 通过；Notion 深色与 Codex 浅色 Browser 复核后用户 `PASS`。`760px` 未作 Browser 实测；高密度设置页、workbar / 其余辅助表面和第四阶段仍待独立门禁 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 会话搜索 / 归档视觉与来源标记批次完成：以 `18 / 14 / 12px`、`40 / 32px` 统一层级与控件，归档代表行由约 `80px` 收敛到 `61px`；侧栏 / 搜索仅为仍可见的 Codex / Claude Code 导入快照显示紧凑来源，归档把外部完整来源并入辅助行且 Code 不显示来源。永久删除改为默认中性、hover / focus 才显危险色的 `32px` 线性垃圾桶，确认 / token / 防重不变。完整前端 `385 passed`、build / freshness 与语法 / diff 通过；深浅主题 Browser 复核后用户 `PASS`。真实数据全为 Code，外部来源由夹具验证，未执行真实删除或 pending Browser，`720px` 仅为确定性合同；CODE-043 继续开放 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 普通消息渲染可访问性完成：可操作文件卡及图片缩略图 / 文件名使用原生控制，内联预览图具备本地化等价按钮语义与 repeat 防护；文件 / 图片继续复用既有打开路径。表头增加 `scope=col`，仅真实横向溢出的普通助手表格获得 `tabindex=0`、region 与本地化名称，非溢出同步移除；focus-visible 保持局部。fail-first `4 failures + 1 error`、直接组 `7 passed`、完整前端 `376 passed`、build / freshness 与语法 / diff 通过。审批 Browser 验证语义、焦点、鼠标动作、tooltip、中英文及宽 / 非宽表格；其键盘驱动无法激活任何原生按钮，Enter / Space 单次动作和右键由用户手测后 `PASS`。CODE-043 保持开放，第三 / 第四阶段等待用户选择 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` Markdown 排版与跨主题对比度完成：普通消息首 Markdown 直接子元素顶距归零，admonition 标题 / 正文为 `14px`，引用、外链、代码辅助文字和消息元信息使用局部安全令牌，复制 / 宽表提示为 `12px`。审批 Browser 首轮发现 `body.theme-dark` 与真实 `html[data-theme-mode]` 挂载不符并打回；修正后 15 个浅色 / 26 个深色 palette 最弱对比度分别为 `4.809 / 4.888`，完整前端 `373 passed`、build / freshness 通过，Notion 深色与 Codex 浅色复核后用户 `PASS`；CODE-043 保持开放，渲染可访问性为下一独立批次 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 用户长消息单行底部操作区完成：仅真实溢出的普通用户消息使用局部 grid，纯文本 / 图片+长文本的 footer 分别固定在第 `2 / 3` 行，左下展开 / 收起与右下时间 / `28×28px` 复制同处一行，hover / `focus-within` 显隐、折叠 / 展开位置、短消息旧布局和窄宽边界保持。fail-first `4 failures`、定向 `14 passed`、完整前端 `368 passed`，build / freshness 与语法 / diff 通过；审批 Browser 抽查折叠、展开、悬停、焦点和短消息，用户 `PASS`。Markdown 排版对比度与渲染可访问性后续批次已获授权但尚未实施 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 普通消息最终视觉与无障碍审计完成：普通用户（含图片+文本）与助手正文为 `16px / 1.68`，助手标题 `20 / 18 / 16px`，代码 / 表格 `14px`，角色 / 模型 `14px`、辅助信息 `12px`；复制按钮 `28×28px` / SVG `16px`，用户元信息支持 hover / `focus-within`，底部间距以非叠加式 `30px` 收敛，正文本地文件名继承内容尺度。完整前端 `364 passed`，build / freshness 与语法 / diff 通过；审批 Browser 复核深浅主题、宽表 / 长消息 / 键盘焦点和特殊消息边界，用户对含文件链接修正的最终候选 `PASS`，后续阶段未启动 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 编辑项目弹窗与侧栏文件区高度边界完成：弹窗收紧为约 `560px` / `14px` 圆角 / `22px` 内边距，以 `18 / 12 / 16 / 14px` 对齐标题、字段标签、输入与常规文字，共享 `16px` SVG 替代字符 / 局部图标；文件区按侧栏实时固定区、footer 和会话 / 文件最小空间计算高度，统一夹紧拖拽、resize、旧值和折叠恢复。fail-first `2 failed`、直接组 `4 passed`、完整前端 `358 passed`，build / freshness 与语法 / diff 通过；审批 Browser 验证弹窗尺度与折叠再展开后设置按钮距底约 `10px`，自动化未完成真实极限 drag；用户在收到手动两端拖拽验收要求后明确 `PASS`，后续普通消息或其他 STANDARD 批次未启动 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 项目侧栏标题 / 置顶 / 操作 / 右键菜单批次完成：项目名在真实溢出时渐隐，悬停 `420ms` 后以 `24px/s` 单次滚动并按新建操作面保留约 `4px` 终点；项目图钉尺度 / 方向统一，行结构收敛为非交互容器内的原生 toggle 与同级新建按钮，编辑 / 置顶只由支持指针夹紧、键盘打开 / 回焦和深浅主题反馈的右键菜单提供。完整前端 `356 passed`、build / freshness 与语法 / diff 通过，审批 Browser 实测长标题终点约 `4.1px`、末字完整且用户 `PASS`；“编辑项目”弹窗视觉尺度只登记为下一个待确认 STANDARD 子阶段 | [查看](2026/2026-08-31.md) |
| 2026-08-31 | `CODE-043` 会话侧栏状态批次完成：单一状态位覆盖等待回答 / 确认 / Skill 证据、运行、失败、未读与空闲相对时间；溢出标题静止渐隐、`420ms` 后以固定 `24px/s` 单次滚动，并按真实操作面边界保留约 `4px` 可见终点。空闲槽内容驱动、标题 / 状态间距 `8px`，更多操作面无接缝，会话图钉移入左侧 gutter 并朝左下。完整前端 `353 passed`、build / freshness 与语法 / diff 通过，审批 Browser 两个样本终点约 `4.2px / 4.7px` 且用户 `PASS`；3B 操作反馈、普通消息审计与后续阶段未启动 | [查看](2026/2026-08-31.md) |
| 2026-08-30 | 会话停止并归档语义完成：Goal `draft` / `active` / `ready_for_acceptance` 不再阻断无活动工作的直接归档，恢复后 sidecar 字节不变且可继续；真实活动前台 / 后台 / Child Run、交互与队列返回专用冲突，经用户确认后以目标会话 fence、锁外 `8s` 有界取消、终态持久化与原事务归档完成。失败区分未能停止与已停止但归档失败，前端提供可访问的“取消 / 停止并归档”确认和独立 `10s` 兜底。Session 99、frontend 348、AgentRuntime 137、Goal v2 66、cancel + server 275 全部通过，build / freshness 与静态检查通过，用户 `PASS`；doctor 仅 9 项通过，Chromium `context.close()` 超时为 `cleanup_failed`，临时根已删除且无本轮进程残留 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | 十天前 Code 会话数据清理完成：冻结 cutoff `2026-08-20T20:09:13.844497+08:00`，R001 因两个未结束 Goal 在零删除预检停止；R002 固定保留它们后，从摘要一致的 176 个旧 Code 候选中以现有 archive → 本次 token 永久删除事务清理 174 个。后置 active meta 为 120，保留 19 paused、2 Goal、4 imported、95 recent 与 1 recent archive，目标 active/archive 均为 0。两个保留的旧 Claude meta 不在现有 session index，磁盘核心仍在且未擅自修复；未读取生成资产内容或 image route registry | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-043` 人工介入界面扁平化完成：授权队列仅在存在非主任务来源时分组并保留批量选择；请求用户问卷合并标题层级、使用两栏布局，“其他”输入一至三行自适应；多选标题只显示模型 prompt。长回答按答案总字符 `>240` 或任一答案至少三行折叠为三个视觉行，提供本地化可访问展开且仅保留当前页状态。直接组 `17 passed`、完整前端 `345 passed`、build / freshness 与语法 / diff 通过，用户 `PASS`；普通消息最终审计与后续阶段仍未启动 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-043` Diff 编辑建议扁平化完成：PC Web 的建议外壳与标题行去除边框 / 背景 / 阴影，代码区成为唯一持久表面；文件名 / 状态控制 / Diff 代码分别为 `14 / 12 / 14px`，状态仅保留语义文字色，展开与授权定位反馈继续可见。直接合同 `6 failed → 6 passed`，完整前端 `328 passed`，build / freshness、Python AST 与 diff 通过；开发任务未运行 Browser / H4 或操作 3010 / 3011，用户在现有 3011 手动刷新后 `PASS`。授权队列、普通消息与后续阶段仍未启动 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-043` API Key 连接名称语义优化：名称继续可选且仅作显示标签，空名称在刷新 payload、公开 route state 与持久目录中保持 `""`，“未命名连接”仅在前端按当前语言投影，重名继续以稳定 connectionId 后缀区分；显式重命名可刷新目录，语言切换不刷新路由，旧本地化占位收敛后 routeRef / 选择 / 可用性保持。完整前端 `322 passed`、Route Registry `11 passed + 5 subtests`、build / freshness 与语法 / diff 通过；未运行 Browser / H4 或真实网络，收口前用户报告 429 已恢复但本提交未处理或复验 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-043` 三层折叠控件交互统一：执行轨迹 / 阶段 / 工具项使用紧凑 `fit-content`、`32px` 点击高度与统一方向箭头，长路径安全省略；最外层箭头收敛为 `6×6px` / `1.4px`。完整前端 `319 passed`，独立 transition-disabled final-state selfcheck 仅运行一次并通过 bundle / classic；原场景两轮失败后熔断且未第三跑，R003 仅运行直接 `7 passed` 与 build / freshness；用户在 3011 Browser 复核后 `PASS`，后续内容卡片、辅助表面与全面审计仍未启动 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-043` 主界面字号层级第一批完成：以 `12 / 14 / 16 / 18 / 20px` 统一辅助信息、常规界面、主要阅读 / 输入、区块与页面标题；普通用户 / 助手正文、模型名和 composer 输入为 `16px`，思考 / 工具及主界面控件为 `14px`，metadata 为 `12px`，SVG 图标保持尺寸例外。富文本 admonition 首段 leading-BR 同步修复；完整前端 `314 passed`、build / freshness 与语法 / diff 通过。新字号 selfcheck 两轮失败后熔断并删除，未记为通过；现有 3011 Browser 实测关键字号并由用户 `PASS`，辅助表面第二批仍未启动 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-043` Code Web 视觉系统第一阶段完成：PC 高频外壳局部接入视觉尺度与 11 个 SVG，保持既有焦点、ARIA 和交互所有权；预览按实时 workbench 夹紧并至少保留 `520px` 会话区 / `250px` 预览区，中英文欢迎语与 composer 提示改为通用任务表达。前端 `307 passed`、build / freshness 与静态门禁通过，R004 model-free selfcheck 的历史通过与原 H4 / 后续窄屏 selfcheck 熔断边界已区分记录；用户完成 PC 深浅主题和预览极限 Browser `PASS`，390px / 手机窄屏留到第四阶段 | [查看](2026/2026-08-30.md) |
| 2026-08-30 | `CODE-035` 多分组路由待办完成映射：既有 `CODE-046` Route Registry 与提交 `6655055` 已以 `connectionId + modelId` 生成不透明 `routeRef`，为每个 Workbar `platformTokenId` 建立独立 `wc1_` 连接，并闭合同名模型精确选择、reload 持久化及禁用连接零跨组回退；后续 Key 删除 / 禁用即时撤销证据补强 fail-closed 边界。`modelKeyMap` 仅保留为 `routing_v1` 回退兼容，不是 `routing_v2` 权威事实；本次只做文档收口，未使用真实 Key、模型或外网 | [查看](2026/2026-08-30.md) |
| 2026-08-29 | `CODE-051` 更新下载状态恢复与重复任务防护完成：官方 Release 固定资产的 name/URL/size/SHA-256 形成服务端可信描述，同版本/目标 single-flight 与版本化原子 sidecar 保证设置页/服务重启接管同一 job；严格 Range/Content-Range/ETag 有界续传，size/digest/PE/非 reparse 单链接路径校验后才发布，restart 只接受已验证 job 且只启动一次。前端 generation 清除旧 poll 并投影下载、失败重试、完成、安装及 installed 状态；直接矩阵 `23 passed`、前端 `295 passed`、server/security/dev-server `362 passed`，构建/freshness 与语法/diff 通过 | [查看](2026/2026-08-29.md) |
| 2026-08-29 | Code 开发任务复用与命名规则正式收口：小任务、单个阶段和正式发布完成继续复用当前开发任务；只有累计多个中大型任务并出现真实上下文/恢复问题时才建议轮换，新建仍需用户明确授权。开发任务标题固定为 `code开发·[目前具体任务]`，阶段切换只重命名原任务；根目录与 `code` 四份协作规则已同步，当前任务继续承接 `CODE-051` | [查看](2026/2026-08-29.md) |
| 2026-08-29 | `CODE-052` 子 Agent 固定轮次语义收敛：持久 Child 的兼容 `maxRounds` 改为原样继承父 Run，不再专属夹到 8；普通主/子 worker 均无固定模型轮次终止，旧 5/8 记录无需迁移且可在重启后跨界继续。全仓调用图证明旧同步 `run_subagent` 5 轮实现无产品/API/插件调用者后删除；AgentRuntime `137 passed + 81 subtests`，Goal/server/Session/协议/取消相关扩大回归 `398 passed + 245 subtests`，doctor `10/10` | [查看](2026/2026-08-29.md) |
| 2026-08-29 | Code v0.6.6 发布 blocker test/evidence correction：manual compaction VM 适配 routing-v2 并冻结 dispatch identity；legacy AgentRun suite 升级为 credential-free v5 与双派生索引前置；P0、canonical/alias context、图片 Session 删除安全终态断言对齐现行生产合同。三组 harness `18 passed + 58 subtests`，default replay `17/124/25/25/4`，完整 pytest `1717 passed + 1193 subtests`；生产与发布实现、未跟踪 v0.6.6 notes、用户 data 均未改动，未重新运行 prepare 或任何外部发布 | [查看](2026/2026-08-29.md) |
| 2026-08-29 | `CODE-049` 用户验收通过：用户实际打开 PPT Master 离线运行时验收样例后明确回复 `PASS`；最终实现提交 `5f25cb1`、加固提交 `92b3a3d`，样例 SHA-256 `d67134c3092114c46e910e2c2600bb0925d2b4d8cb8d48639384c7a6b1916997`。供应链准入、静态共存、受管依赖、离线运行时、安全加固与人工验收均完成，canonical TODO 中的 CODE-049 主项已整项移除 | [查看](2026/2026-08-29.md) |
| 2026-08-29 | `CODE-049` 离线运行时最终复核加固：工具投影/capability/execute 前锁定核验 exact receipt、dist-info/RECORD 与 23 个安装文件；final/staging 所有允许文件在读取前检查 containment 及 reparse；段落+列表确定性分区，表格/图表混排明确拒绝；外层 PPTX 与原生图表嵌套 workbook 使用 part/Content-Type/关系 allowlist 拒绝宏、OLE、ActiveX、媒体及其他未批准部件；部分安装失败的 cleanup 必须精确恢复 before distribution/RECORD 基线。五组红测修正后为 `38 passed + 212 subtests`，相关模块隔离扩大回归合计 `603 passed + 399 subtests`；五页样例哈希不变并通过更严格 OOXML 校验 | [查看](2026/2026-08-29.md) |
| 2026-08-29 | `CODE-049` 完成 PPT Master 离线运行时试点：固定并审计 PyPI 官方 `skia-pathops==0.9.2` / `uharfbuzz==0.50.0` Windows wheels，只安装到 Skill 受管 Python 且完成精确回滚/再安装演练；新增显式 `/ppt-master` 的结构化、离线、可取消工具，只允许 inline Markdown 或项目内 `.md`/`.txt` 生成固定新 PPTX，纳入 file-mutation 授权、AgentRun 幂等/恢复、30 秒进程树取消、staging 回滚与原子发布。五页 CJK 开发态 deck 经 OOXML、artifact-tool、LibreOffice 与逐页图检，文本/形状/表格/原生图表可编辑且无明显溢出；默认 `/pptx`、用户 data 与被排除的图片/模板/网络/媒体/服务保持不变。`CODE-009` 会话归档产品面与无秘密派生索引完成；`CODE-044` 完成活动/归档双域搜索及 model-free 验收。独立提交 `65f6815` 修复 HTTP/1.1 归档请求体消费边界；前端 `294 passed`、build/freshness、语法/diff 与 Browser 单层焦点环用户 `PASS` | [查看](2026/2026-08-29.md) |
| 2026-08-28 | `CODE-009` 归档命名空间兼容修复：新物理归档写入带所有权标记的 `session-archive/code-v1/`；无 manifest/所有权的 pre-CODE-009 root-level 旧备份安全忽略且字节不变，有效 root-level v1 bundle/journal/staging 继续兼容恢复，受管损坏与双位置仍 fail-closed。Session `71 passed + 25 subtests`，相关扩大回归 `657 passed + 205 subtests`，doctor `10/10`；UI H4 产品阶段保持暂停 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-009` 物理可恢复归档服务端地基：Session core 进入独立 `data/session-archive/<sessionId>/` bundle，manifest 固定同 ID canonical/legacy 路径、摘要、mtime 与状态令牌；持久 journal/staging 闭合 archive/restore/permanent-delete 崩溃窗口，双位置与篡改全局 fail-closed。归档态所有活动读写/Goal/import/AgentRun 入口统一拒绝，archive-only token delete 事务清理 Goal/assets/终态 AgentRun 并保留 legacy DELETE/压缩快照语义。Session `64 passed + 25 subtests`，相关扩大回归 `557 passed + 180 subtests`；doctor 仅测试 Chromium close cleanup timeout，阶段无 UI/H4，事项继续等待设置页产品面 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 生成资产历史身份模型专用投影：后续同 Session AgentRun 在原 assistant/tool 配对中获得权威 assetId 与有界 MIME/尺寸/字节元数据，旧/畸形安全回退，不改写 Session/schema/UI，不泄露 URL/base64/缓存路径。两份 Skill 改为直接使用回执身份并经授权转存后做确定性本地处理。相关回归 `523 passed + 106 subtests`，bundle/direct classic H4 `2 passed`，两入口均为两次 count=1 上游、第三个 combine 零重复生图/零问卷、两次转存授权与真实拼接、reload 零重放；build/freshness、Skill validator、语法/diff 通过，doctor 的隔离 Chromium close timeout 无进程或临时根残留 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 受控图片转存 Child 隔离纠正：background subagent 在前端删除 `generate_image` 和 `manage_generated_image`，服务端在显式/parent/depth 推导 Child 的 create 与 v5 非破坏恢复中独立 fail-closed 过滤，public allowed/available tools 不再暴露两个图片工具。foreground/Goal/queue 的 Accept/Bypass 继续允许转存，Read/Plan 继续排除。定向 `3 passed + 5 subtests`，相关回归 `481 passed + 91 subtests`，build/freshness、语法/diff 通过 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 生图数量约束与受控图片转存完成：`generate_image` 仅当前冻结用户消息明确要求 2–4 张时允许 `count>1`，其余默认 1 并在授权/上游前拒绝模型自行加量。新 `manage_generated_image` 以 file_mutation 授权、冻结项目根、路径/符号链接/所有权/完整性校验和持久 receipt，将 Session 资产按需转存/重命名于 `output/generated-images`；同 Session 可直接用 generated_asset 分阶段编辑，新 Session 可用受控 workspace_image，内部缓存不暴露且 Session 删除不删用户转存副本。直接/扩大回归 `673 passed + 146 subtests`，资产工作流与四图批次 bundle/direct classic H4 各 `2 passed`，build/freshness、Skill validator、语法/diff 通过；doctor 的三个 JS/浏览器内部探针 timeout 按环境边界记录，真实 build/H4 能力已闭环 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-012` 上下文预算设置语义收敛：设置页改为“最大上下文窗口 / Maximum context window”，显式值仅按全局 1,024..2,000,000 规范化，不再依赖当前模型、route 或能力估算，也不再显示高于估算/按模型上限调整提示；Max Tokens Auto 不做模型输出猜测。服务端继续以 hard metadata 与 connection+model 校准静默决定实际 `contextLimit`，desired 200K 保留在 `contextBudgetTokens`。相关回归 `498 passed + 113 subtests`、bundle/direct classic 权威 soft400K/hard128K H4 `2 passed`，build/freshness、语法/diff 与资源清理通过；`CODE-012` broader override TODO 继续保留 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 多图批次恢复加固：单一 validator/sanitizer 在执行、重启与公开投影前校验确定性 batchId、0..N-1 child/operation identity、2–4 请求、并发 2 及有界状态/result/asset 字段；无效记录稳定降级 `image_batch_state_invalid + outcomeUnknown + notReplayed` 且零上游。`assets_persisted` 的缺失/partial/corrupt/unavailable 资产不再投影陈旧成功，改为真实 partial/失败并停止后续 admission；旧 completed count>1 仍原样复用，reference edit 与 admission 间隙取消闭合。定向 `58 passed + 41 subtests`、扩大回归 `918 passed + 195 subtests`、bundle/direct classic H4 `2 passed`，build/freshness、语法/diff 与资源清理通过 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 单图上游批次与并发多图完成：`generate_image count=1..4` 由权威运行时拆为 N 个独立 `n=1` child operation，固定并发 2、逐项持久派发与唯一幂等键，按请求序公开资产并支持部分成功；systemic/unknown 停止后续 admission，取消、重启、旧记录与 exactly-once 边界保持。bundle/direct classic page-rebind H4 `2 passed`，两入口均完成 4/4 与 3/4 partial、峰值并发 2、乱序完成有序 gallery，reload/restart 的 AgentRun/upstream/assets 零增量；扩大回归 `859 passed + 166 subtests`，doctor、build/freshness、语法、Skill validator 与 diff 门禁通过 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | API Key 删除/禁用后的聊天模型路由即时撤销完成：空 connections 权威清空服务端 route/credentials，禁用/删除目标连接即时从客户端目录和 selection 消失，整体刷新失败只撤销 runtime bindings 并 fail-closed；同模型唯一剩余 route 可在权威刷新后迁移，旧 routeRef、并发旧刷新、reload/restart 与已删除 workbar token 均不复活。routing v2 的非空 prompt/图片/queue 只有 enabled+credentialed 当前 route 才可 Send，routing v1 与 Stop/Cancel/既有恢复不变。定向 `34 passed + 9 subtests`、相关扩大回归 `484 passed + 86 subtests`、bundle/direct classic H4 `2 passed`，build/freshness、语法/diff 与资源清理通过，无真实模型/外网/Key | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 生图结果一等输出展示完成：成功资产作为 standalone gallery 位于 compact trace 后/final answer 前，trace 无重复图片且默认折叠，单图大图、多图响应网格且无内部滚动，受控 metadata/download/overlay 与 reload 去重保持。bundle/direct classic 等价 H4 `2 passed`：两入口 reload 零模型/工具/上游重放，edit/disabled 合同不变，权威 Session PUT 后唯一 DELETE=200；删除后同一 keep-alive socket 上 stale PUT=410、紧随资产 GET=404，两资产均 404。前端 `280 passed`、build/freshness、语法/diff 证据闭合，未使用 Browser/真实模型/外网/Key | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` Session 删除后 keep-alive 请求消费修复：陈旧 Session PUT 命中 tombstone 410 和 deleted/missing messages POST 早退前均精确消费当前 body 一次，不再让残留 JSON 污染同一 HTTP/1.1 连接的下一请求。fail-first 真实单连接从 `JSON + GET` 拼接 400 修正为 410 → 资产 404，并列 messages 链修正为 410/404 → Session 列表 200；Session persistence/routes/server 回归 `352 passed + 73 subtests`，compile/diff 通过，未重跑展示 H4/Browser | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` Session 删除准备失败边界补齐：路径解析、Session/JSONL/index/Goal/分支与资产快照全部纳入 `SessionDeleteError`；快照完成前的 PermissionError/OSError 不执行删除或无依据回滚，仅返回脱敏 `session_delete_failed`/503。含绝对路径/秘密哨兵的 fail-first 从 400 修正为 503，全部事实字节不变；相关扩大回归 `547 passed + 166 subtests` | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` Session 删除一致性修复：Windows 并发读取下旧 `Path.unlink()` 触发 `PermissionError/winerror=32`，且会先删 Goal/资产、泄露临时路径并允许迟到 save 复活。现以 Session 生命周期锁、可回滚 JSON/JSONL/index/资产快照与稳定 `session_delete_failed` 脱敏失败统一 save/Goal/资产删除边界；成功 DELETE 保持幂等，迟到 PUT 与付费生图均不复活 Session/资产。相关扩大回归 `546 passed + 166 subtests`，compile/diff/doctor 通过；未运行已熔断展示 H4，未使用内置 Browser 或真实数据 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 生图上游共享总预算从 60 秒提升至 180 秒：默认与最大 override 均固定为 180 秒，上游 POST、JSON 响应读取 socket、响应处理和受控 URL 回退共享同一 deadline；超过边界仍为 `image_upstream_timeout + outcomeUnknown + notReplayed`，同一 AgentRun 零自动重试、零第二 operationId/派发。fake clock 证明 66 秒完成可唯一保存资产，响应读取与 URL 回退的 179/181 秒边界闭合；image runtime/AgentRun `39 passed + 24 subtests`，相关 server/Goal/route 6 项、Python compile 与 diff 门禁通过，无真实模型/外网/凭据 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | 协作手册新增 Codex 桌面内置 Browser 标签页程序化关闭禁令：根目录与 `code` 的 `AGENTS.md` / `CLAUDE.md` 同步禁止 `tab.close()` 等动作，尤其不得关闭最后一个内置标签页；验收后保持打开并导航复用或交由用户手动关闭。该禁令不影响测试夹具自行创建的隔离 Playwright/Chromium page、context 与 browser process 按合同正常关闭清理 | [查看](2026/2026-08-28.md) |
| 2026-08-28 | `CODE-006` 独立生图模型 API 正式完成并继续按 Codex 官方 imagegen 分层原则收紧：第一阶段以独立无秘密 `ImageRouteRegistry`、稳定 routeRef、`generate_image`/`image_generation` 权限、OpenAI-compatible generation/edit、受控下载、Session-scoped 资产和付费调用 exactly-once/outcome-unknown 建立安全地基；第二阶段完成独立设置/默认模型、route 冻结、Child 不继承、`imagegen` Skill、脱敏授权卡和权威资产预览/下载，验收修正移除单参考图编辑 capability 开关。hardening 使 Skill 只组织生成/编辑意图与 prompt，新模型工具不再公开 size/quality/outputFormat，legacy 控制由服务端统一忽略为 auto/auto/png，默认上游请求不发送 size/quality；同 AgentRun 在已派发的非重试或未知结果后，变参尝试也在授权/上游前持久熔断为 `image_retry_blocked + notReplayed`，新 AgentRun 才可再试一次。最新重启恢复修正使新 AgentRun 创建前的 image route 目录错误至多按原 routeRef/connection/model 一次 refresh/rebind，以原消息和 clientRequestId 唯一创建；路由消失、禁用、刷新失败或第二次失败继续 fail-closed，已有 AgentRun 不重绑，timeout 只声明送达与结果未知并保持不重放。相关扩大回归 `801 passed + 161 subtests`；重绑定定向证据另为 image/AgentRuntime `35 passed + 20 subtests`、frontend `280 passed`、补充 AgentRuntime/routes/server `10 passed + 3 subtests`，bundle/direct classic 重绑定 H4 `2 passed`，build/freshness、语法/diff 通过。未读取真实 Key、调用真实模型/外网，也未调研 Claude Desktop；事项保持从 TODO 删除。同日 `CODE-014` 正式完成：observer v1 服务端冻结实际 Skill 集合与权威证据，显式 `/code-review` opt-in 以 `waiting_skill_evidence` 门禁支持同 AgentRun Continue、零模型 Skip 和幂等 Cancel；Session `interactionState` 只投影脱敏等待枚举，bundle/direct classic H4 与应用内 Browser 已验收，`CODE-049` 未启动 | [查看](2026/2026-08-28.md) |
| 2026-08-27 | `CODE-014` 第一阶段 Skill 执行证据 observer v1 及自动匹配多 Skill 完成复核修正：请求级 prompt snapshot 同时冻结实际注入正文与 `activeSkillNames`，显式为单项，自动匹配保持既有排名/禁用排除/最多 3 项，未注入 Skill 的后台子 Agent 为空；服务端逐项冻结已安装 Skill 的名称、内容哈希与可选 `evidence.json` v1 合同，合同只能引用 AgentRun 已实际允许的工具。观察只用唯一 execution/result 与成功文件产物回执，模型自述和重复事件不算证据；公开 `activeSkills` / `skills` 保留全部激活项并兼容 `5987436` 单项形状和旧无字段 v5，五态不改变 AgentRun 终态、权限、授权或重试。AgentRuntime/协议 `137 passed + 227 subtests`、路由 `69 passed + 15 subtests`、前端 `270 passed`、subagent `29 passed`；首阶段扩大回归排除三份基线已陈旧冻结 harness 后 `1499 passed + 1017 subtests`，build/freshness/语法/diff 通过。无浏览器观察面，使用真实 AgentRun API 持久化链闭环；事项仍保留在 TODO，下一阶段需另行 STRICT 审批代表性 Skill 的可配置强制校验试点 | [查看](2026/2026-08-27.md) |
| 2026-08-27 | `CODE-050` 最终回答本地链接绝对路径完成：新回答仅将规范化完整绝对本地路径投影为可点击 identity，覆盖盘符、UNC、正反斜杠、CJK/空格、目录与行号；项目说明仍可用相对路径/行内代码，但不可点击且不按项目根猜测，旧会话不迁移。最终回答与工具/文件树入口隔离，工具参数继续项目相对语义；文本和 1.2 MB 大文本仍内部预览，项目内绝对目录经 `/api/files` 确认后复用 Explorer 目录合同，失效目标 fail-closed，项目外 absolute code/link/image 在助手回答插入 DOM 前降级为非交互内容且零请求，用户消息保持原通用 renderer；外部 HTTPS、二进制和安全边界不变。相关回归 `387 passed + 28 subtests`，bundle/direct classic H4 `6 passed`，每入口目录打开请求 1、默认程序/模型/工具请求为 0，build/freshness、语法和 diff 门禁通过；未启动 `CODE-049` | [查看](2026/2026-08-27.md) |
| 2026-08-27 | `CODE-048` AgentRun 中途故障恢复与无固定轮次上限完成：Auto 上下文以服务端 connection+model 能力为权威，legacy 128K 不再收紧 1M family 能力；前端 200、服务端 50、Goal 40 固定终止已移除，旧 `maxRounds` 只读兼容。首轮仅在 120 秒零有效输出时硬中断；已有进展后的断网、流中断、超时或重启持久进入同 routeRef 的 `waiting_recovery`，复用权威 execution/result 且不重放 completed tool。自动压缩使用专用最终摘要、一次空摘要重试、持久退避和原子缩小替换。重设计 bundle/direct classic H4 `2 passed`，扩大回归（排除 3 个基线已陈旧冻结 harness）`1487 passed + 1009 subtests`，前端 build/freshness 与 diff 门禁通过；旧服务进程浏览器场景两轮后按熔断停止，由确定性服务重启测试加页面 reload H4 分层闭合，未调用真实外部模型，也未启动 `CODE-049` | [查看](2026/2026-08-27.md) |
| 2026-08-27 | `CODE-047` 并行视觉工具协议与错误分类正式完成：提交 `cd3427d` 已使同一 assistant 的全部 tool 回执按声明顺序连续、exactly-once 持久化，完整工具块后才追加视觉；旧 v4/v5 仅在请求内存投影中安全恢复，冲突证据 fail-closed，明确工具协议 400 独立为非瞬态 `tool_protocol_error`。在已加载修复的新鲜服务上，真实 `Code-QA-国产模型 / deepseek-v4-pro` 会话 `523c1c6cd75243d8` 与独立严格 OpenAI-compatible `Code-QA-gpt codex / gpt-5.6-sol` 会话 `e4b4eba0c0924d9b` 均首次形成双 `read_file`，持久序列严格为 `assistant(A,B) → tool(A) → tool(B) → visual(A) → visual(B)`；每个 execution/start/completed 各一次，刷新后 AgentRun、轮次与执行计数不增加，UI 无 `config_error` 或 Key/授权误导。结合 bundle/direct classic 隔离 H4 与相关自动回归，完成定义闭合，事项已从 TODO 删除 | [查看](2026/2026-08-27.md) |
| 2026-08-27 | Code 工作流优化方案 B 两阶段完成：第一阶段新增独立 `python verify.py doctor`，聚合核对当前/H4 Python、四项测试依赖、Node/npm、真实内存 esbuild transform、Playwright 与受控 Chromium 启动/清理，不进入四档 profile 或 release 定义；第二阶段将一次预检、验证阶梯、H4 内部优先与用户两轮熔断、每阶段三个协调点及阶段边界开发任务轮换同步到根/code 协作规则和仓库审批协议，明确不弱化风险、授权、兼容、安全、lease 或发布门禁。真实预检 exit 0，Chromium 临时根前后归零，当前解释器下 release fingerprint 保持 `7be91010...e3485b`。`CODE-046` 首条消息即时反馈与连接中心 Route Registry 完成：以稳定非秘密 `connectionId + modelId` 和服务端不透明 `routeRef` 精确绑定连接，公开目录按连接名称分组且不暴露 Key、Authorization 或内部 baseUrl；首条消息先乐观持久化/展示，再异步 fail-closed 路由并支持不重复消息/AgentRun 的安全重试。服务端多后端部分失败隔离、前端按连接快照/意图 singleflight 与有效路由不等待后台刷新闭合；新 `routing_v2` 与旧缓存/AgentRun 只读兼容。用户 H4 以 `10 passed` 加修正后定向 `4 passed (14.5s)` 组合覆盖完整 14 场景；扩展回归真实漂移 `151/151`、registry/server `250/250`、前端非构建 `261/261`。`CODE-045` 同日保留同源 favicon 安全代理、1×1 拒绝、临时失败恢复、4 槽 FIFO 与统一 tooltip，相关单元 `22 passed`、既有 bundle/direct classic H4 `4 passed`；真实 10+ 站点仅部分改善，新增公平队列 H4 因夹具不稳定撤回，事项转回待决策而非完成。早先记录的缺依赖/esbuild 沙箱红项不再外推为当前新任务环境；本阶段未运行全量 pytest/build | [查看](2026/2026-08-27.md) |
| 2026-08-26 | 问卷五题上限与设置页新手入口删除完成：前后端 schema、服务端 validator 与运行时 normalizer 统一支持最多 5 题，默认仍为 1 题且通常不超过 3 题，6 题 fail-closed；修复遗留三题截断后，五题可完整进入 Session/UI 并保持刷新与 exactly-once。设置页新手入口已移除，欢迎页三步任务与最小进度语义保留。用户普通 PowerShell 首轮同组 H4 为 6 passed/2 个五题失败，修复后五题 bundle/direct classic `2 passed (12.6s)`；本轮未声称全量回归 | [查看](2026/2026-08-26.md) |
| 2026-08-25 | `CODE-013` 登录后三步新手任务及后续交互闭环完成：欢迎页显式三步、最小 `v2` 进度与旧用户豁免保持；问卷 v2 固定 2–3 选项、自定义输入和唯一推荐理由，同时兼容旧持久化问卷，Enter/连续问卷 exactly-once；流式滚动以真实用户意图为所有权边界；前台 Agent 可用内部 `goal_cancel` 终结 draft/active/gated Goal而不取消传输；模型→Key v3 缓存只保存非明文可信身份，缺失映射在 AgentRun 前 fail-closed，中英文无渠道错误统一失效；旧孤立 tool-result 仅在请求投影层安全降级。用户普通 PowerShell 全量 `1440 passed + 1053 subtests`（仅 3 条预期损坏 TIFF 警告），questionnaire/stream H4 `14 passed`、Goal cancel `2 passed`、trusted route/tool protocol `4 passed`，manual compaction 与兼容证据同步闭合 | [查看](2026/2026-08-25.md) |
| 2026-08-24 | `CODE-029` 自动模式风险确认完成：非 Auto → Auto 先确认，取消/关闭/Esc/遮罩保持原权限且零派发副作用；`v1` acknowledgement、切离失效、legacy 新模型任务前补确认、纯本地 `/goal status` 豁免及显式 Goal exactly-once 均闭合。弹窗以三能力面板和服务端保护说明准确区分 Auto 与 Accept，保留 alertdialog/默认取消焦点并完成中英文敬语；定向 `6/6`，用户 bundle/direct classic H4 `6 passed (18.6s)` 并最终确认验收完毕 | [查看](2026/2026-08-24.md) |
| 2026-08-24 | `CODE-042` 项目级多标签预览工作区完成方案与待办登记（尚未开发）：第一阶段限定单侧栏和活动标签唯一渲染，按项目隔离并在同项目会话间保留；普通单击复用浏览标签，双击、Ctrl/中键或明确入口新建，同路径只激活既有标签。专题指导覆盖旧 `code-preview-*` 兼容迁移/回退、异步竞态、资源释放、分阶段实施与验收；Code 游标推进至 `CODE-042`，当前推进序列仍为“无” | [查看指导](../preview-workspace-multi-tab-development-guide.md) · [查看日志](2026/2026-08-24.md) |
| 2026-08-24 | 历史仓库噪声收口：正式删除 `Code-v0.5.28.spec` 至 `Code-v0.5.34.spec` 七个旧构建快照，保留并验证 `Code-v0.6.1` 至 `v0.6.4` 与当前发布模板；仅以三条精确规则忽略 Goal sidecar 运行目录，实体数据和其他受跟踪 `data/` 资产保持不变。release 定向 `39 passed + 229 subtests`，当前 spec 与模板选择契约通过 | [查看](2026/2026-08-24.md) |
| 2026-08-24 | Goal / DSH / pi 历史文档归档：纳入 2026-08-14 DSH Goal 调研、顶部工具日志决策、pi/Code/DSH 架构对照与 Goal 指导增补；两份研究明确为当日历史快照和有限验证，不冒充当前事实。Goal 指导状态对齐 2026-08-16 已实现的 Goal v2 基线，并以持久 `permissionProfile`、successor 继承与 AgentRun 恢复事实校准权限说明；无代码、TODO、sidecar 或产品行为变化 | [查看](2026/2026-08-24.md) |
| 2026-08-24 | `CODE-041` Windows 文件显示与 Explorer 前台激活完成：文件使用 Shell 原生父目录精确选择，目录打开精确位置；仅按路径识别唯一 HWND，支持最小化恢复、普通前台激活和必撤销的临时 topmost 降级，Unicode 枚举固定 UTF-8。专项 `22/22`、Explorer+server `246/246`、P2/i18n `56/56` 通过；用户 bundle/direct classic H4 `2 passed (5.1s)`，并确认多窗口/多标签、遮挡/最小化、中文空格路径、目录、不存在目标与默认程序实机验收通过 | [查看](2026/2026-08-24.md) |
| 2026-08-24 | `CODE-040` 大型文本内部截断预览回归修复完成：文件树与最终回答链接统一继续走右侧 `loadFile`，沿用服务端前 1 MiB 与 `truncated` 状态，350000 字符/8000 行仅关闭高亮；隐式 `/api/open-file` 已移除，显式默认程序打开及图片/PDF/二进制/行号边界保持。用户 bundle/direct classic H4 `2 passed`，并确认文件树、回答链接和显式系统打开三项人工验收均正常 | [查看](2026/2026-08-24.md) |
| 2026-08-24 | `CODE-039` 最终回答结构化富文本可读性优化完成：任务清单以禁用 checkbox 保留只读语义，嵌套列表层级/换行统一，GFM 表格左中右对齐及宽表自身滚动/非遮挡提示闭合；普通本地文件改为无框图标+文件名，图片卡保持。前端模块 `227 passed`，用户 bundle/direct classic H4 `2 passed`，双入口、明暗主题与约 390px 窄屏人工 PASS | [查看](2026/2026-08-24.md) |
| 2026-08-24 | `CODE-036` 两阶段完成：先在 `marked` 前为紧邻 Unicode 标点的 HTTP(S) 裸 URL 建立有界 autolink，保留代码、显式链接/图片、query/fragment、百分号编码与 Unicode 路径；再将 favicon 改为同源安全代理，只访问经全量公网 DNS 校验并固定连接地址的目标 `/favicon.ico`、固定 provider 和父域候选，不解析 HTML。三次重定向、逐跳 HTTPS 降级拒绝、总 deadline、256 KiB/MIME/magic、128 项 LRU/正负 TTL/同 host 合并及地球图标降级闭合；最终安全测试 `13/13`、server `224/224`、同范围回归 `443 passed`，用户 fresh bundle H4 `2 passed` 并确认 Code Dev/direct classic 扩展网站矩阵与链接交互人工 PASS | [查看](2026/2026-08-24.md) |
| 2026-08-24 | `CODE-038` 会话列表相对时间与状态槽、Goal 会话切换隔离完成：固定右侧槽按 inactive 等待回答/确认、运行、未读、空闲时间投影现有事实，active 交互由既有面板唯一接管；分钟 ticker 不重排，切换期间旧 Goal 零跨会话帧。运行环调整为 1.4 秒并以同 kind 原地 patch、后台 streaming 局部刷新保持跨增量 DOM 身份。前端模块 `222/222 passed`，用户 fresh bundle 上 bundle/direct classic H4 `2 passed`，双入口视觉与交互人工 PASS | [查看](2026/2026-08-24.md) |
| 2026-08-23 | `CODE-037` 历史会话最后对话时间排序完成：可选 `lastMessageTime`、旧索引锁内惰性回填、导入/分支兼容、naive 本地时间到 UTC 的 API 边界归一、续聊后权威时间即时重排及刷新/首次切换防抖闭合；用户 bundle/direct classic H4 `2 passed (6.7s)`，排序、活跃时间与短消息时间戳单行显示人工 PASS | [查看](2026/2026-08-23.md) |
| 2026-08-23 | 最终回答渲染与交互（answer-render R001-R017）：markdown 扩展（admonition/表格滚动/锚点/语言标签/行号引用）、路径别名+自定义 tooltip+点击定位行+右键菜单、预览路由矩阵（binary/超限外部打开）、外链 favicon 左置+glyph 兜底+多源+父域回退+缓存+近白底衬+主题适配、悬浮去重、系统提示防编造；211 passed、本地提交（未 push） | [查看](2026/2026-08-23.md) |
| 2026-08-23 | 输入框图片大图左右切换（image-overlay-nav R001-R003）：多图箭头/键盘 ←→/索引/边界禁用/删除联动/单图降级；缩略图提示 i18n；根因=新模块未入构建入口（frontend-entry 37→38）；210 passed、用户 PASS | [查看](2026/2026-08-23.md) |
| 2026-08-22 | Gemini-GCP Goal 工具 schema 兼容修复：`goal_revise_plan` 从 GCP 不接受的同层 `anyOf` 改为普通对象 `minProperties: 1`，Goal v2 服务端语义保持不变；定向 `46 passed`、AgentRuntime/Goal `136 passed + 60 subtests`、编译与 diff 检查通过，用户重启后真实 Gemini-GCP 请求准确读取并返回 TODO 首个 P0 标题。多分组 Key 首个认领导致的错误 Token 路由另记 `CODE-035` | [查看](2026/2026-08-22.md) |
| 2026-08-21 | v0.6.4 完整 pytest 发布阻断收口：同步 AgentRun/Goal/manual compaction/frontend build 等回归夹具事实，frozen evidence 仅更新经语义不变量证明的 sourceSlice 与派生哈希；用户 clean TEMP/TMP 全量 `1356 passed + 1021 subtests`（214.44 秒）。共享 `pytest_full` 预算按当前规模由 180 秒调整为 360 秒，命令、顺序、fail-fast、prepared 指纹和 H4 排除边界不变；无产品代码变化，未运行 prepare/publish | [查看](2026/2026-08-21.md) |
| 2026-08-21 | `CODE-012` 模型上下文上限事实源 A+B、官方目录 C1+C2 与 D-lite 完成：统一 resolver、可信 metadata/cache、用户预算/Max Tokens、Run/Session 冻结恢复及自动压缩内部投影闭合；官方目录覆盖 7 provider/37 variant（31 active + 6 pending），`official`/`stale_official` 为 soft estimate。D-lite 仅从明确 context 错误学习实际失败 Key+连接+模型的较低 cap，以同 Key 单次压缩重试、成功前 store 事务、失败/取消零学习、30/7 天 TTL 和最小值合并保证安全；同秒 Run 排序与 follow-up 孤立消息竞态一并修复。用户 context calibration bundle/direct classic H4 `2 passed (7.3s)`；D2 `9/9`、竞态 `4/4`、protocol+Runtime `24/24`、replay `17/124/25/25/4` 通过，AgentRuntime `107/109` 与前端 `208/209` 的各自既有环境红项未重基线。`CODE-012` 仅剩 connection-specific verified override 与校准检查/显式重置入口待决策 | [查看](2026/2026-08-21.md) |
| 2026-08-20 | `CODE-012` 模型上下文能力与用户预算详细开发指导完成（未实施）：拆分硬能力、Code估算、用户预算、输出预留/安全余量与最终冻结值；确定服务端唯一 resolver、metadata/cache v2、normalized baseUrl+model、多 Key最小合并、旧 Run继承、只向下校准和设置页预算预设。首阶段限定 A+B，专家覆盖/校准后置；CODE-012 仍在 TODO | [查看指导](../context-window-budget-development-guide.md) · [查看日志](2026/2026-08-20.md) |
| 2026-08-20 | `CODE-033` 复杂 Goal 用户补充后跨 AgentRun 续接验收完成：bundle/direct classic 真实 H4 覆盖 waiting_user 前后 reload、切 Session、普通消息唯一 successor、同 Session/Goal/revision/步骤/权限、单轮 usage 与成功工具不重放，并以 bundle 上游 502 证明失败保持门禁后可安全恢复。用户终端 `3 passed (33.7s)`；Session/Goal control/前端投影/AgentRun 定向 `21+12+7+5 passed`，replay `17/124/25/25/4`、frontend freshness/语法/diff 通过；仅测试与事实文档，无生产协议变化 | [查看](2026/2026-08-20.md) |
| 2026-08-20 | `CODE-034` 跨运行时 owner lease 完成：新增 Codex/DSH 共用的 tracked Python CLI，以 worktree Git-dir 内 OS 文件锁 + 原子状态实现唯一写者；schema 绑定 runtime、审批/开发身份、stage、HEAD、relay/lease ID 与 TTL，过期只允许审计 HEAD/cached 后显式 reclaim，损坏/中断 fail-closed，history 有界且 Git status 零污染。专用 unittest `12 passed`、`verify.py quick` 六项 PASS；四份协作规则统一引用 canonical 协议，TODO 游标推进至 `CODE-034`，未启动 `CODE-033` | [查看](2026/2026-08-20.md) |
| 2026-08-20 | `CODE-027` 文件树递归搜索 + `CODE-028` 文件树键盘导航完成（审批 relay file-tree-search-nav R001-R013）：流式渐进搜索（当前目录优先逐级向上、按 path 去重增量渲染、进度行、全局 500 上限、目录粒度缓存 TTL 3s、服务端回退检测）；roving tabindex 键盘导航（↑/↓、Enter、Alt+↑/Ctrl+↑/Backspace、Esc 清空，进入目录自动聚焦）；根因=glob_files 无索引全扫 + 子树无匹配回退全项目根（Desktop 27k ≈7s）；SKIP_DIRS 补全 10 项。前端模块 206 passed、构建 fresh、用户 3011 复测 PASS；本地提交（未 push） | [查看](2026/2026-08-20.md) |
| 2026-08-20 | `CODE-030` 顶部用量统计与会话信息面板增加缓存命中率完成：口径 cache/input；normalizeResponseUsage 新增 hasCacheReported、ledger cacheReported 标志 + cache>0 推断（兼容服务端持久化 stats）；顶部纯百分比圆点加粗、面板独立字段行（无数据显示 —）。前端模块 203 passed、构建 fresh、用户验收 PASS；提交 dfc5b64 | [查看](2026/2026-08-20.md) |
| 2026-08-16（发布流程） | 两阶段发布流程完成：新增 `prepare` / `publish-prepared` / `resume`，以 Git 内部封印凭证精确绑定候选、共享门禁、发布说明和 EXE；发布提交、master、tag、Release 与资产按“一致跳过、缺失补做、不同停止”恢复，禁止覆盖/重建。定向 `51 passed + 229 subtests`、共享 release `1304 passed + 765 subtests`、replay `17/124/25/25/4`、legacy dry-run 零修改通过；未执行真实 push/tag/Release，H4 未加入 release | [查看](2026/2026-08-16.md) |
| 2026-08-16 | `CODE-019` Goal v2 持久执行闭环完成：采用 Session 隔离的 append-only v2 事件、严格 reducer、revision CAS/幂等/跨进程锁/损坏 fail-closed 与 tombstone；显式 `/goal` 和模型自主 Goal 共用普通前台 AgentRun 与受控 Goal 操作，身份和权限由服务端绑定。计划、逐步证据、用户门禁、最后一步直达完成及 40 轮软交接/50 轮硬熔断已闭合；composer 紧凑进度条、三态详情、显式来源/完成标记、内部工具隐藏、同轮跨 Run 唯一用量、公开过程折叠、工具终态和最终总结单一出口在 bundle/direct classic 一致。最终定向 `33 passed`、相关回归 `400 passed + 66 subtests`、统一 UI `231 passed`、replay `17/124/25/25/4`、Goal H4 `2+7 passed`，build/freshness/语法/diff 通过，用户 Code Dev 与 direct classic 多轮验收最终 PASS。用户选择方案 B，不要求兼容旧 Goal；旧 v1/workflow 与真实 sidecar 原地保留、未读取迁移且未纳入提交。Goal v2 尚未发布，v0.6.3 另行冻结 | [查看](2026/2026-08-16.md) |
| 2026-08-15 | `CODE-002` 顶部工具日志移除完成：删除顶部入口、面板及重复的渲染/开关/样式/i18n 死依赖，保留对话流内联工具组、Session 工具统计、Branches、Preview 和公共顶部面板。`CODE-032` 工具组运行提示与展开层级完成：高光只作用于当前工具标题文字，当前工具结束即停；工具间隙保持当前标题，组终态再汇总，首次展开外层组不再自动展开首个工具详情。最终定向契约 `196 passed`、统一 UI `225 passed`、bundle/direct classic H4 `6 passed`，前端 build/freshness/语法/diff 均通过，用户双入口验收 PASS；无服务端、AgentRun、Session JSONL、协议或持久化变化。同日为 canonical TODO 固化 workbar/Code 独立编号游标，初始化为最后已分配的 `WB-012`/`CODE-032`；新增事项按主域重读、加一并与游标原子同改，完成后不回退、不复用。`CODE-031` 随后完成系统提示词 11 段组装、首次持久化 AgentRun 任务快照、图片重试复用、本地 IANA 时区标签和 Child 中文控制提示词，并迁入 bundle/generated classic 共用的独立模块；定向 `5+2 passed`、统一 UI `227 passed`、前端 build/freshness/语法与 replay `17/124/25/25` 通过。runtime 6 项和 H4 21 项红项均在纯净 HEAD 同现，当前分支无独有失败，未重基线。`CODE-019` 第一阶段随后建立独立 Goal sidecar 事件事实层、严格三态 reducer、revision CAS/幂等、跨进程锁、损坏安全降级、tombstone、只读查询及 Session 删除/归档/分支兼容；定向 `40 passed + 6 subtests`、相关回归 `119 passed + 60 subtests`、replay `17/124/25/25` 通过。runtime 为 `1191 passed + 754 subtests + 7` 个纯 HEAD 同现失败，H4 为 `48 passed + 21` 个纯 HEAD 同现失败，本阶段无独有红项且未重基线。第二阶段增加 per-Session `/goal` 查询、草案确认与显式控制，零消息 Goal Session 正确进入会话布局，重复同一待确认草案只重新显示确认且不重复写入；前端模块 `201 passed`、统一 UI `230 passed`、bundle/direct classic Goal H4 `2 passed`，build/freshness/语法/diff 通过。用户确认 Session 隔离、欢迎态与重复草案修复，但模型规划和普通 AgentRun 的 Goal 感知尚未实现；`CODE-019` 仍未完成，下一步为受限模型规划草案和新前台 AgentRun 的有界只读 Goal 投影 | [查看](2026/2026-08-15.md) |
| 2026-08-14 | `CODE-003` 新消息阅读锚点与流式滚动完成：真实前台轮使用非持久化锚点为首段回答保留稳定空间，回答增长和用户向上滚动单调消耗临时空白，首次向下或“一键到底”一次释放并转向真实消息底部；按钮显隐的真实内容距离与防回弹的滚动所有权距离分离，保持 2px/160px 契约。queue、steer、detached `/parallel` 与刷新/切会话边界已闭合；控制器 5 passed、统一 UI 219 passed、前端构建/freshness/语法/diff 通过，用户在 Code Dev 与 direct classic 最终验收均 PASS。随后 DSH 完成 Goal 模式调研分析文档落盘（`docs/dsh-goal-mode-research-analysis.md`，基线 DSH 四包 0.1.0-rc.6 源码核对，未改 `goal-mode-development-guide.md`、未提交）。同日 `CODE-002` 顶部工具日志产品决策按"移除"收口：现状面板是对话流工具组降级副本且未实现"全局"定位、失败判定依赖 i18n 文本前缀，目标受众为开发者调试/企业回退时信息过薄；升级方向（per-run 轨迹 + 副作用 manifest 的调试/回退审计）另行立项，`TODO.md` 的 `CODE-002` 已改为"顶部工具日志移除"（UI 移除 / STANDARD，未提交）。`CODE-005` 随后完成工具组内容闪烁与折叠交互稳定性收口：三层节点按稳定身份原位更新，活动任务保持用户展开/收起选择，真实终态折叠且刷新默认折叠；异常 Selection、鼠标白色原生轮廓及跨入口恢复后的主题绿色轮廓均已修正，同时保留正文/Diff 可复制和键盘焦点。direct classic 与重建 bundle 跨入口 H4、统一 UI 221 passed、前端构建/freshness/语法/diff 均通过，用户 Code Dev → direct classic → 返回 Code Dev 最终复验 PASS。`CODE-025` 随后完成编辑建议 Diff 正文默认收起、页面内独立展开、授权“查看”展开定位、重复同源提案实例隔离和刷新复位；统一 UI 224 passed、bundle/direct classic 编辑授权 H4 6 passed，前端构建/freshness/语法/diff 通过，用户双入口验收 PASS。同轮拒绝后模型可发起不同 `authorizationId` 的新授权，但第一次拒绝未写入、第二次仍需明确批准且仅应用一次；该有限观察不扩展为新策略。相邻 `CODE-032`、`CODE-026` 等仍未完成且未启动 | [查看](2026/2026-08-14.md) |
| 2026-08-13 | `CODE-004` 用户长文本省略与收起完成：长草稿和已发送用户长文本仅在真实溢出时提供不损失原文语义的展开/收起，普通重绘保持当前页状态，刷新或切换会话恢复默认收起；控制器、统一 UI、i18n/P2、前端构建与语法门禁及用户实机均通过。`CODE-024` 随后完成对话终态后文件树静默刷新：前台成功/失败/取消、真实执行后的 queue/steer 主轮及 detached `/parallel` 自身终态才触发，授权/问卷/仅入队不提前触发；保留当前项目/目录、搜索、预览选中和滚动位置，以 32ms 合并、同轮去重、latest-wins、过期响应丢弃和失败保留旧树防止闪动与回写。定向 3 passed、统一 UI 217 passed、前端构建/freshness/语法/diff 门禁通过，Code Dev 与 direct classic 双入口人工验收均 PASS。用户另明确确认 `WB-010` 企业域名邮箱开发已完成，该事项据此移出 TODO；具体服务商、域名、DNS、账号及安全验收细节未在本阶段核对，不作已验证声明。其他待办未启动 | [查看](2026/2026-08-13.md) |
| 2026-08-12 | Harness H4-8G 已收口固定同 Session 的 detached `/parallel` 编辑授权完整刷新恢复：第二 Runtime consumer fence 后进入 background waiting authorization，主任务先完成，current/last-active Session 完整 reload 复用同一 background Run/auth，真实 approve 只产生一次 apply/write/backup；终态为 3 AgentRun/5 Runtime/5 chat、create transport 4 次，follow-up 实际模型上下文与七项固定编辑 artifact 均无 detached 泄漏，bundle/direct classic 十三项哈希一致。随后以冻结证据树 `618f876` 综合核对 H4-8D/E/F/G 与第一轮五项完成定义；最终两轮 67 例 H4、相关回归、完整 pytest、replay、frontend/语法/diff 和资源门禁全部闭合，完成线、TODO、工程总览与统一日志已同步，Harness 第一轮按有限工程边界完成。本结论不外推为通用 exactly-once、授权恢复完整覆盖或并发/浏览器/真实外网场景穷举，也不启动下一 Harness 阶段。Code P1 阶段 A 同日完成输入框相对主题层级与稳定文件拖放反馈；阶段 B 随后完成历史消息“一键到底”和集中滚动接管：2px 到底容差、160px 显示阈值、运行中水平等距三点/空闲箭头、动态安全区与授权抑制已在 bundle/direct classic 统一，wheel/touch 首次意图及 `awaitingUserScroll` 保护短会话轻微溢出，完整 pytest 为 1138 passed/751 subtests，用户人工验收最终 PASS。相邻“新消息发送后的阅读锚点与流式滚动”仍作为独立未完成事项保留。Goal 复杂长任务指导随后落盘：固化三态顶层计划、普通消息驱动控制、跨 AgentRun 安全续跑、证据验收、最小 UI、恢复/幂等与权限边界；旧 Agent 重构 Stage 3 的五态和控制台式交互已明确让位，P2 事项仍保持未实施。Workflow v2 同步引入 QUICK/STANDARD/STRICT 风险分级、差量 relay、发布冻结队列和 v1 恢复兼容，长期未跟踪审批手册已迁入个人 Skill并移出仓库；统一 `verify.py` 随后完成四档只读验证、稳定摘要与单一共享定义，`release.py` 保持原门禁顺序/超时及 dry-run/skip-tests 语义，H4 仅属于 runtime；真实 quick/ui/runtime/release、67 例 H4、最终 1146 passed/751 subtests、replay 与 release dry-run 全部通过。唯一 TODO 同日迁移为 workbar/Code 双域、六种执行状态和 35 个稳定 ID，跨域仅以依赖/关联连接；`WB-003` 是唯一当前进行项并唯一关联活动 handoff，`CODE-010` 外部等待且单向依赖 `WB-003`；四份协作规则同步引用 canonical 维护契约，未启动任何待办 | [查看](2026/2026-08-12.md) |
| 2026-08-11 | `workbar-docs` 已从私有仓库切换为公开，开发可无需写权限通过 Fork/PR 协作；一页式开发部署交接手册已随 `c23bbd8` 推送到 `main`，固定完整 SHA、成功 Actions、同 SHA Artifact 与部署脚本的发布契约，并收口可信开发机验签、VPS 原子发布、Nginx 组、回滚及验收证据。两条提交的作者信息已纠正为用户指定且归属 `fhy-A` 的邮箱，根提交为 `1482fa0`；GitHub API 映射与新 Actions 运行 `31457002313` 均验证成功，旧 SHA/Artifact 停用。`main` 分支保护仍待用户确认。Harness 中央日志补记 H4-8A 历史完成事实，并归档 H4-8B 混合问卷渐进恢复、H4-8C 编辑授权刷新恢复、第一轮工程完成线、H4-8D approved stale conflict 保护、H4-8E 问卷等待态与单项队列刷新顺序，以及 H4-8F 授权请求到达服务端前失败与单次安全重试；H4-8F 固定窗口的 transport 为 2/1/1/1，bundle/direct classic 十项哈希一致，完整 `tests` pytest 为 1126 passed/751 subtests，两轮标准 H4 各 65 passed、1 worker、0 retry，replay 保持 17/124/25/25/4。该证据不覆盖 durable decision 后响应丢失或通用服务端幂等；第一轮尚余 `/parallel` 授权恢复只读审计/条件实施及最终整体收口 | [查看](2026/2026-08-11.md) |
| 2026-08-09 | workbar 官方用户文档完成内容规划、导航、文档首页、13 篇正文和第一轮全量精修；已统一 Code 密钥同步与手动恢复、自动分组、计费奖励、账号安全、工具地址和三类兼容接口，并明确 Claude Code 当前未提供 `/v1/messages/count_tokens` 的兼容边界。首批线上脱敏配图已接入；随后新增可重复构建的本地单页 HTML 文档站，覆盖 14 篇内容、5 组导航、搜索、文章目录、代码复制、配图放大、深浅色和移动端布局，并将正文、配图、表格与代码块统一到 `820px` 内容列；桌面端与 390×844 移动端实测通过，宽表格保留容器内横向滚动，页面无横向溢出。Code 下载、模型广场和钱包三张实际 JPEG 截图已改为正确 `.jpg` 扩展名，当前引用图片均以正确 MIME 返回。正式架构已确定为 `docs.workbar.ai` 独立静态站，并完成 14 篇真实目录路由的生产 `dist/`、哈希资源、搜索与 SEO/404 文件、构建安全校验，以及同一 VPS 的 Nginx、DNS/TLS、缓存、安全头、原子发布和回滚资料；本地 HTTP 与浏览器桌面/移动交互回归通过，自定义首页两个文档入口已改为正式域名。随后 `workbar-docs` 已建立独立 GitHub 仓库并推送当前根提交 `1482fa0`，Node 20 的 GitHub Actions 检查成功，SHA artifact 与 SHA256 交付链完成下载复核；截图密钥整栏遮挡、旧敏感图片清理和 Actions 产物部署说明均已完成，线上 DNS、证书、Nginx、后台链接与必要配图继续后置。H4-6O 完成同指纹 `read_file` 三次真实失败、第四次阻断与第五轮 forced-final 固定 HTTP 502 的 failed 终态：AgentRun 24 事件、Runtime `[4,3,3,3,0]`，Session 回滚为 user 与唯一错误 assistant；failed 主任务耗时改由同一持久化边界写入既有 `meta._responseTime`，bundle/direct classic 九项哈希对等且刷新零重放。最终文件哈希下连续两轮标准 H4 均为 49 passed，完整回归为 1129 passed/751 subtests | [查看](2026/2026-08-09.md) |
| 2026-08-08 | H4-6N 完成同指纹 `read_file` 的 `missing→success→missing` 成功打断失败链：outcome 为 failed→succeeded→failed，失败签名 A→absent→A、计数 1→absent→1，bundle/direct classic 九项哈希对等且刷新零增量；最终文件哈希下连续两轮标准 H4 均为 47 passed，完整回归为 1127 passed/751 subtests。H4-6M 同日完成 `present→missing→present` 错误签名交替隔离，H4-6L 完成不同 arguments 身份隔离并补强 Q1/TIFF Harness 确定性门禁；H4-7C 完成 detached `/parallel` 固定 HTTP 502 失败隔离；H4-7B 完成耗时唯一投影并通过人工验收；H4-7A 完成 TIFF 派生预览与页面缓存；H3-2D3 人工验收确认压缩失败保存恢复后原历史和唯一失败 marker 保持 | [查看](2026/2026-08-08.md) |
| 2026-08-07 | Harness H4-2 真实 Code Dev 补充验收修正模型目录阻塞 AgentRun/Runtime 重附着，并修正 D3 HTML 投影跨日固定时钟缺口而不改既有哈希；H4-3 为 classic fallback 直接入口补齐三条同进程流式刷新证据；H4-4 为默认根入口补齐 bundle-load/bundle-init 两条自动降级及降级后任务闭环，以严格命令身份/退出分类修正正常 exit(0) 竞态，并为一次未复现、根因未定位的 stop-time metrics 超时增加白名单阶段诊断；H4-5A 以真实不同 PID 的进程 A→B 边界证明 completed 纯文本 AgentRun 从同一受控持久化根重载时身份、事件、Session JSONL 与 DOM 唯一；H4-5B1 进一步证明含唯一只读工具轨迹的 terminal AgentRun 跨进程重载且 B 零工具执行，并移除生产 CodeHandler 访问日志与控制 JSONL 共用 stdout 的不安全测试边界；H4-6A 补齐默认 bundle 单只读工具在运行中重绘保持展开、前端终态自动折叠、完成态三层真实查看与完整刷新默认折叠/零重执行；H4-6B 复用相同参数化流程，为 direct classic 补齐同一工具详情生命周期与刷新零重执行对等证据；H4-6C 为默认 bundle 补齐同轮双受限 read_file 的执行闸门、11 事件、单组双项重绘保持与刷新零重执行，并明确结构自动证据不等于主观闪烁验收；H4-6D 再以同一参数化流程为 direct classic 补齐双工具活动态、终态与刷新零重执行对等，八类语义哈希与 bundle 一致；H4-6E 为默认 bundle 补齐合法 path 加额外字段的生产 schema 校验失败、第二轮失败回执、父 Run completed、两相失败 DOM 与刷新零执行证据；H4-6F 将同一流程参数化到 direct classic，保持八类语义哈希、失败 DOM 与刷新零执行对等，并将一次无 Playwright 摘要的外层 exit 124 明确排除出通过轮，随后以不改内部门禁的 240 秒外层预算取得连续两轮 25 passed；H4-6G 以 schema-valid 行范围参数真实进入 read_file 生产执行器并在其内部失败，闭合 bundle/direct classic 的九事件、失败 DOM、刷新零执行和八类对等哈希，随后取得连续两轮 27 passed；active receipt consumption、工具副作用 exactly-once、其他文件系统/执行器失败、取消/长输出及其他真实 DOM 生命周期仍保留 | [查看](2026/2026-08-07.md) |
| 2026-08-06 | 定价采集与计划任务操作报告优化完成验收归档；Harness H3-2C1 新增独立上游失败与 non-action evidence suite，以本地假上游核对 401/429/502/首响应超时分类及 empty/reasoning-only 当前事件顺序；H3-2C2 为既有 v1～v4 最小 AgentRun 建立 loader/snapshot/v4 persisted 分层恢复与两次临时磁盘加载契约；H3-2D1 以七格式矩阵闭合图片 MIME 生产链；H3-2D2/D3 闭合手动压缩成功路径、完整可见历史及 19 个失败/持久化边界；H3 最终完成第 6.3 节十五类证据映射及独立 30 秒 replay 发布门禁；H4-1 引入隔离 Chromium E2E 基础设施和三条首批冒烟；H4-2 完成默认 bundle 的同进程模型流式刷新恢复，覆盖首增量前刷新、两段后续流及刷新后取消，并冻结同一 AgentRun/Runtime、DOM 前缀不回退、无重复请求与清理归零证据；classic 刷新、服务重启/跨进程恢复和其余 DOM 生命周期继续归后续 H4 | [查看](2026/2026-08-06.md) |
| 2026-08-05 | 定价采购体系新增 CC Switch 本地官方价变化探测、按厂商复核/7 天完整审计回退、结构化可操作计划任务报告与三类任务差异化提示；Harness H3-2B2 增加兼容 v1/v2 的 multi-run runner 与独立 Child AgentRun fixture v2：1 场景、3 Run、21 事件、21 schedule 步骤、6 检查点/恢复，固化静态父子身份闭合、Child 终态/父工具结果独立顺序、每 Run 生产投影、复合恢复与幂等，且 B1 与单 Run 基线/哈希不变；H3-2B1 独立 queue/parallel 多 Run 关系基础设施及 H3-2A 两条单 Run 轨迹收口；H2-3 非刷新关键路径重复零差异样本收口，刷新恢复限制延后至 H4 或未来控制面重构 | [查看](2026/2026-08-05.md) |
| 2026-08-04 | Code v0.5.33 正式发布；Harness H2-3 已验收路径收口、Runtime 投影单消费者与 metadata-only 首增量检查点；首个模型增量前刷新明确延后；命令失败及同轮多工具取消真实影子零差异 | [查看](2026/2026-08-04.md) |
| 2026-08-03 | Harness H2-3 流式首帧投影、部分正文暂停保留、模型取消零差异与命令取消工具终态闭合；前后台并行影子隔离、后台轮次时序统一与 `/parallel` 真实零差异；排队消息双前台 Run 真实零差异；同轮引导前端接线、显式排队入口、执行轨迹同轮时序与新会话首轮排队自动接续；同轮引导协议阶段一地基、AgentRun v4 持久化与旧版本恢复兼容；Harness H2-3 工具运行中连续刷新单次执行、命令授权接受/拒绝真实零差异、`model_pending` 轮次修复、危险 Completion Guard 实验撤回、前台单观察者所有权、普通/问卷/刷新恢复真实零差异与只读摘要提取、二次白名单脱敏；Harness H3-1 统一离线轨迹回放、全检查点恢复、确定性哈希与首差异定位；Harness H2-2 前端只读影子投影、独立旧投影适配、开发/正式开关与脱敏内存诊断；Harness H2-1 纯 Run reducer、规范 View Model、七类投影比较字段与确定性检查点恢复 | [查看](2026/2026-08-03.md) |
| 2026-08-02 | Harness H1-4 影子凭据诊断分层、真实 v1 Run 重放与 H1 收口；运行中连续刷新计时单调接力与状态栏首帧恢复；Harness H1-3 开发实例事件 v1 灰度写入、真实 UTC 时间与前端旧版/未来版兼容；Harness H1-2 服务端事件协议影子验证、开发/正式实例开关、脱敏有界诊断与恢复游标续接；Harness H1-1 Agent 事件 v1 契约、v0 兼容适配、凭据拒绝与状态不变量纯函数基线；Harness H0-2 脱敏轨迹、v1/v2/v3 与旧 JSONL/经典前端兼容夹具、确定性测量基线；Harness H0-1 事件状态事实清单、脱敏轨迹候选与回归基线；Code Harness 工程优化路线立项；用户图片批次横排、统一视觉高度与图上文下布局；工具阶段标题时序与展开状态保持；前端图片附件格式归一、入口等待与异常兜底；模型图片请求边界转换、旧历史污染保护与恢复任务计时修正；发送前置提示与新会话标题 i18n 收口；定价 Codex 计划任务与确定性调度入口部署；定价监控固定任务离线补跑与小时探测不补跑；定价监控确定性触发状态机与调度/通知契约；定价流水线唯一运行手册与跨 Agent 自助接手收口；workbar Key 同步状态、配置保护与弹窗布局验收；模型列表热刷新缓存、失败保留及设置页同步验收；Workbar 未显式缓存价按输入价回退的毛利修正及三类价格变化处理规则；上游缓存支持语义与定价总表逐维毛利修正；定价原始响应历史归档及模型/渠道双视图精简总表；UU API 5/5 启用范围校正及 Kimi 渠道级显式映射、登录价格响应复核；ByteCatCode 原始价格响应确定性导入；12 条上游渠道确定性价格采集与官方/上游一键流水线；GPT Terra/Luna 基础价与九个分组倍率联动分析、Terra 分阶段调价及模型广场反算验收；app.js 阶段 6D-1/4 临时全局清理、正式版经典回退决策与阶段 6 收口；九家官方来源 35 个模型全量价格采集验证；手动 `/compact` 独立状态、完整历史保留与图文兼容；导入会话结果反馈区布局与状态条验收；状态文案 i18n 与斜杠命令发现性验收；Qwen 限时折扣仅保留候选 | [查看](2026/2026-08-02.md) |
| 2026-08-01 | Code v0.5.32 正式更新、端口与 bundle/回退人工验收；OpenAI、DeepSeek 与 xAI 官方价格采集器首批验证；Code v0.5.32 首个 bundle 正式版本发布；workbar 全模型价格监控首批只读引擎；workbar 官方用户文档方向与逐主题协作方式；app.js 阶段 6A 至 6C-3 esbuild 构建链、开发默认 bundle、自动重建、经典回退、EXE 资源与发布门禁，第五阶段 5G 与阶段 5 正式收口；问卷工具执行轨迹去重与本地化；Hermes PATH 冲突诊断与 Code Dev 托盘恢复；v0.5.31 端口隔离人工确认与发布前收口；AgentRun 90% 自动上下文压缩、任务中途续行与执行轨迹投影；workbar 用户端与品牌部署整合；`/compact` 手动压缩命令与命令/Skills 分组 | [查看](2026/2026-08-01.md) |
| 2026-07-31 | app.js 第四阶段拆分、第五阶段 5A/5B 收口、5C 权限策略、5D 问卷收口与 5E-1/2a 子 Agent 纯边界拆分；新会话首次发送性能优化；Codex 风格执行轨迹与稳定任务状态 | [查看](2026/2026-07-31.md) |
| 2026-07-30 | 开发日志结构调整；渠道首轮筛查；新会话自助启动与跨 Agent 活动交接；消息流垂直间距统一 | [查看](2026/2026-07-30.md) |
| 截至 2026-07-29 | 原 `CHANGELOG.md` 完整历史，保持迁移前原文 | [查看归档](archive/CHANGELOG-through-2026-07-29.md) |
