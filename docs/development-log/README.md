# Code 开发日志索引

> 本目录与 `TODO.md` 共同构成项目状态事实源：这里记录已经完成的重要改动、产品决策和验证结果，`TODO.md` 只保留尚未完成的项目事项。`../development-handoff.md` 仅在阶段任务未完成或需要跨 Agent 接力时存在，只记录进行中差量。

## 阅读方式

1. 开始任务时先阅读本索引。
2. 默认只读取最新日期文件，以及与当前任务直接相关的日期文件。
3. 只有需要追溯早期实现或决策时，才读取历史归档；不要在常规交接中加载完整归档。
4. 阅读 `../../TODO.md`，并在项目根目录运行 `git status --short`、`git log -8 --oneline`。
5. 检查 `../development-handoff.md` 是否存在；用户要求继续/接手或任务范围重叠时读取，无关任务不得覆盖。
6. Git 现场、日志、TODO 和活动交接冲突时，以 Git 现场和可复现结果为准，并修正活动交接。

新会话可以直接接收用户任务并按以上流程自助入场，不要求上一会话为已经完成的阶段重复生成交接消息。

## 写入规则

- 新日志按本地日期写入 `YYYY/YYYY-MM-DD.md`；当天文件不存在时新建。
- 同一天可以记录多个阶段，每条使用 `## YYYY-MM-DD HH:mm · 协作方` 标题并按时间倒序排列。
- 只记录已经完成且有事实依据的内容；未完成事项写入项目根目录 `TODO.md`。
- 阶段任务尚未完成、需要暂停或切换 Codex / Claude Code 等 Agent 时，按 `../development-handoff-template.md` 创建或更新 `../development-handoff.md`。
- 阶段完成后，将长期事实写入日志、剩余事项写入 `TODO.md`，然后移除对应活动交接。
- 每次新增日期文件或改变当日主题时，同步更新下方索引。
- 发布说明、更新摘要和开发日志默认使用中文，仅保留必要的英文技术字段。
- `archive/CHANGELOG-through-2026-07-29.md` 是冻结的历史原文，不再追加或改写。

## 日志索引

| 日期或范围 | 摘要 | 详细记录 |
|---|---|---|
| 2026-08-07 | Harness H4-2 真实 Code Dev 补充验收修正模型目录阻塞 AgentRun/Runtime 重附着，并修正 D3 HTML 投影跨日固定时钟缺口而不改既有哈希；H4-3 为 classic fallback 直接入口补齐三条同进程流式刷新证据；H4-4 为默认根入口补齐 bundle-load/bundle-init 两条自动降级及降级后任务闭环，以严格命令身份/退出分类修正正常 exit(0) 竞态，并为一次未复现、根因未定位的 stop-time metrics 超时增加白名单阶段诊断；H4-5A 以真实不同 PID 的进程 A→B 边界证明 completed 纯文本 AgentRun 从同一受控持久化根重载时身份、事件、Session JSONL 与 DOM 唯一；H4-5B1 进一步证明含唯一只读工具轨迹的 terminal AgentRun 跨进程重载且 B 零工具执行，并移除生产 CodeHandler 访问日志与控制 JSONL 共用 stdout 的不安全测试边界，同时保留 active receipt consumption、工具副作用 exactly-once 及历史控制响应超时根因未闭合边界 | [查看](2026/2026-08-07.md) |
| 2026-08-06 | 定价采集与计划任务操作报告优化完成验收归档；Harness H3-2C1 新增独立上游失败与 non-action evidence suite，以本地假上游核对 401/429/502/首响应超时分类及 empty/reasoning-only 当前事件顺序；H3-2C2 为既有 v1～v4 最小 AgentRun 建立 loader/snapshot/v4 persisted 分层恢复与两次临时磁盘加载契约；H3-2D1 以七格式矩阵闭合图片 MIME 生产链；H3-2D2/D3 闭合手动压缩成功路径、完整可见历史及 19 个失败/持久化边界；H3 最终完成第 6.3 节十五类证据映射及独立 30 秒 replay 发布门禁；H4-1 引入隔离 Chromium E2E 基础设施和三条首批冒烟；H4-2 完成默认 bundle 的同进程模型流式刷新恢复，覆盖首增量前刷新、两段后续流及刷新后取消，并冻结同一 AgentRun/Runtime、DOM 前缀不回退、无重复请求与清理归零证据；classic 刷新、服务重启/跨进程恢复和其余 DOM 生命周期继续归后续 H4 | [查看](2026/2026-08-06.md) |
| 2026-08-05 | 定价采购体系新增 CC Switch 本地官方价变化探测、按厂商复核/7 天完整审计回退、结构化可操作计划任务报告与三类任务差异化提示；Harness H3-2B2 增加兼容 v1/v2 的 multi-run runner 与独立 Child AgentRun fixture v2：1 场景、3 Run、21 事件、21 schedule 步骤、6 检查点/恢复，固化静态父子身份闭合、Child 终态/父工具结果独立顺序、每 Run 生产投影、复合恢复与幂等，且 B1 与单 Run 基线/哈希不变；H3-2B1 独立 queue/parallel 多 Run 关系基础设施及 H3-2A 两条单 Run 轨迹收口；H2-3 非刷新关键路径重复零差异样本收口，刷新恢复限制延后至 H4 或未来控制面重构 | [查看](2026/2026-08-05.md) |
| 2026-08-04 | Code v0.5.33 正式发布；Harness H2-3 已验收路径收口、Runtime 投影单消费者与 metadata-only 首增量检查点；首个模型增量前刷新明确延后；命令失败及同轮多工具取消真实影子零差异 | [查看](2026/2026-08-04.md) |
| 2026-08-03 | Harness H2-3 流式首帧投影、部分正文暂停保留、模型取消零差异与命令取消工具终态闭合；前后台并行影子隔离、后台轮次时序统一与 `/parallel` 真实零差异；排队消息双前台 Run 真实零差异；同轮引导前端接线、显式排队入口、执行轨迹同轮时序与新会话首轮排队自动接续；同轮引导协议阶段一地基、AgentRun v4 持久化与旧版本恢复兼容；Harness H2-3 工具运行中连续刷新单次执行、命令授权接受/拒绝真实零差异、`model_pending` 轮次修复、危险 Completion Guard 实验撤回、前台单观察者所有权、普通/问卷/刷新恢复真实零差异与只读摘要提取、二次白名单脱敏；Harness H3-1 统一离线轨迹回放、全检查点恢复、确定性哈希与首差异定位；Harness H2-2 前端只读影子投影、独立旧投影适配、开发/正式开关与脱敏内存诊断；Harness H2-1 纯 Run reducer、规范 View Model、七类投影比较字段与确定性检查点恢复 | [查看](2026/2026-08-03.md) |
| 2026-08-02 | Harness H1-4 影子凭据诊断分层、真实 v1 Run 重放与 H1 收口；运行中连续刷新计时单调接力与状态栏首帧恢复；Harness H1-3 开发实例事件 v1 灰度写入、真实 UTC 时间与前端旧版/未来版兼容；Harness H1-2 服务端事件协议影子验证、开发/正式实例开关、脱敏有界诊断与恢复游标续接；Harness H1-1 Agent 事件 v1 契约、v0 兼容适配、凭据拒绝与状态不变量纯函数基线；Harness H0-2 脱敏轨迹、v1/v2/v3 与旧 JSONL/经典前端兼容夹具、确定性测量基线；Harness H0-1 事件状态事实清单、脱敏轨迹候选与回归基线；Code Harness 工程优化路线立项；用户图片批次横排、统一视觉高度与图上文下布局；工具阶段标题时序与展开状态保持；前端图片附件格式归一、入口等待与异常兜底；模型图片请求边界转换、旧历史污染保护与恢复任务计时修正；发送前置提示与新会话标题 i18n 收口；定价 Codex 计划任务与确定性调度入口部署；定价监控固定任务离线补跑与小时探测不补跑；定价监控确定性触发状态机与调度/通知契约；定价流水线唯一运行手册与跨 Agent 自助接手收口；workbar Key 同步状态、配置保护与弹窗布局验收；模型列表热刷新缓存、失败保留及设置页同步验收；Workbar 未显式缓存价按输入价回退的毛利修正及三类价格变化处理规则；上游缓存支持语义与定价总表逐维毛利修正；定价原始响应历史归档及模型/渠道双视图精简总表；UU API 5/5 启用范围校正及 Kimi 渠道级显式映射、登录价格响应复核；ByteCatCode 原始价格响应确定性导入；12 条上游渠道确定性价格采集与官方/上游一键流水线；GPT Terra/Luna 基础价与九个分组倍率联动分析、Terra 分阶段调价及模型广场反算验收；app.js 阶段 6D-1/4 临时全局清理、正式版经典回退决策与阶段 6 收口；九家官方来源 35 个模型全量价格采集验证；手动 `/compact` 独立状态、完整历史保留与图文兼容；导入会话结果反馈区布局与状态条验收；状态文案 i18n 与斜杠命令发现性验收；Qwen 限时折扣仅保留候选 | [查看](2026/2026-08-02.md) |
| 2026-08-01 | Code v0.5.32 正式更新、端口与 bundle/回退人工验收；OpenAI、DeepSeek 与 xAI 官方价格采集器首批验证；Code v0.5.32 首个 bundle 正式版本发布；workbar 全模型价格监控首批只读引擎；workbar 官方用户文档方向与逐主题协作方式；app.js 阶段 6A 至 6C-3 esbuild 构建链、开发默认 bundle、自动重建、经典回退、EXE 资源与发布门禁，第五阶段 5G 与阶段 5 正式收口；问卷工具执行轨迹去重与本地化；Hermes PATH 冲突诊断与 Code Dev 托盘恢复；v0.5.31 端口隔离人工确认与发布前收口；AgentRun 90% 自动上下文压缩、任务中途续行与执行轨迹投影；workbar 用户端与品牌部署整合；`/compact` 手动压缩命令与命令/Skills 分组 | [查看](2026/2026-08-01.md) |
| 2026-07-31 | app.js 第四阶段拆分、第五阶段 5A/5B 收口、5C 权限策略、5D 问卷收口与 5E-1/2a 子 Agent 纯边界拆分；新会话首次发送性能优化；Codex 风格执行轨迹与稳定任务状态 | [查看](2026/2026-07-31.md) |
| 2026-07-30 | 开发日志结构调整；渠道首轮筛查；新会话自助启动与跨 Agent 活动交接；消息流垂直间距统一 | [查看](2026/2026-07-30.md) |
| 截至 2026-07-29 | 原 `CHANGELOG.md` 完整历史，保持迁移前原文 | [查看归档](archive/CHANGELOG-through-2026-07-29.md) |
