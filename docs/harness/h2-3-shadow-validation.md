# Harness H2-3：真实影子投影验证

当前状态：进行中

## 1. 验证目标

在 Code Dev 的真实 AgentRun 中观察规范 reducer 与现有前端投影是否一致，覆盖普通多工具、授权/问卷、连续刷新恢复、队列、后台任务和失败/取消。H2-3 只有在关键轨迹重复观察为零差异后才能收口；自动测试不能替代真实任务证据。

## 2. 只读摘要入口

开发实例默认启用 `CODE_AGENT_PROJECTION_SHADOW`。任务进入终态后，可在浏览器开发者工具 Console 中执行：

```javascript
window.Code.agent.projectionShadowDiagnostics.snapshot()
```

复制格式化结果：

```javascript
copy(JSON.stringify(window.Code.agent.projectionShadowDiagnostics.snapshot(), null, 2))
```

返回报告只存在于当前页面内存，最多包含最近 32 条已经归档的前台/后台 Run 摘要。刷新页面会清空此前页面中的摘要，不会写入会话 JSONL、AgentRun、浏览器存储或服务端。

## 3. 摘要边界

报告只允许以下字段：

- 开关、版本、数量和前台/后台计数；
- 观察到的事件、快照、比较、差异、观察器错误和丢弃诊断数量；
- 白名单诊断代码、比较字段、事件游标和八类规范状态。

导出边界会再次构造白名单副本。即使页面内存对象被意外加入 Run ID、会话 ID、模型正文、工具参数/结果、命令输出、文件内容、diff、错误原文、Key 或请求头，这些字段也不会出现在报告中。关闭影子开关时，入口只返回 `enabled: false` 的空报告。

## 4. 第一批真实测试顺序

每次只执行一个场景并在任务完整结束后提取报告，避免多个问题混在同一摘要中：

1. 普通前台多工具任务（已通过）；
2. `request_user_input` 问卷等待、提交和继续（已通过）；
3. 文件编辑或命令授权接受（命令路径已通过）；
4. 文件编辑或命令授权拒绝（命令路径已通过）；
5. 模型运行中刷新一次并恢复（已通过）；
6. 工具运行中连续刷新并恢复（已通过）；
7. 排队消息（已通过）；
8. `/parallel` 后台任务（已通过）；
9. 模型阶段取消（已通过，包含部分流式正文保留）；
10. 命令阶段取消或失败（取消已通过，包含同轮多工具终态闭合与影子零差异；仍待失败路径）。

每个场景记录：页面行为是否正常、报告中的新增 `summaryCount`、`runKind`、`status`、`mismatches`、`observerErrors`、`diagnosticCounts` 和 `diagnostics`。任何差异先保存脱敏字段与复现步骤，再决定修复哪一侧，不根据 UI 文字猜测领域状态。

## 5. 本阶段已完成的准备

- 新增只读 `projectionShadowDiagnostics.snapshot()` 页面内存入口；
- 影子开关关闭时不返回历史摘要；
- 导出结果有独立 32 条上限和严格字段/枚举白名单；
- 返回全新副本，调用方修改报告不会污染页面中的原始摘要；
- 自动测试覆盖有界、关闭、聚合、复制和伪造敏感字段过滤；
- 默认 bundle 与经典回退页均能通过现有构建门禁。

## 6. 尚未完成

- 命令授权接受首轮曾暴露 `model_pending` 提前计入下一模型轮次的真实差异，以及未提交 Completion Guard 实验错误强制重复执行已成功命令、提前公开候选回答与 Token 统计的问题；危险实验已完整撤回，轮次投影已修正并加入授权恢复回归；
- 修复后命令授权接受复验为单次授权、单次命令和单次最终回答，14 个事件、5 个快照、19 次比较均零差异；命令授权拒绝也已确认只有一次拒绝、没有 `command_started`、没有重试或问卷，12 个事件、4 个快照、16 次比较均零差异；此前普通多工具、问卷和模型运行中刷新恢复也已取得单前台 Run、零差异样本；
- 工具运行中连续刷新也已通过：两次页面刷新后从恢复游标继续观察至总游标 14，最后页面观察 5 个事件、1 个快照和 6 次比较均零差异；完整耐久 Run 只有一次授权、一次 `command_started` 和一次成功工具完成，没有重复执行；
- 排队消息已通过：新会话首轮前台任务结束后无需刷新即自动启动持久化排队项；两个前台 Run 均为 `completed`，分别观察 29/4 个事件、12/2 个快照和 41/6 次比较，合计 33 个事件、14 个快照、47 次比较，差异、观察器错误与丢弃诊断均为 0；
- `/parallel` 后台任务已通过：首轮发现子上下文按引用继承父任务影子，导致前后台两条独立耐久 Run 写入同一观察器并产生 91 条镜像差异；隔离投影私有状态后只余后台 `model_started` 时事件已开始轮次领先于快照已完成轮数的两条差异，统一取两类已观察轮次最大值后，最终前台/后台分别观察 10/11 个事件，合计 21 个事件、17 个快照和 38 次比较均零差异且无观察器错误或丢弃诊断；
- 模型阶段取消已通过：暂停立即生效、计时与暂停说明稳定，部分流式正文保留；对应脱敏影子摘要为 3 个事件、2 个快照和 5 次比较，`cancelled` 终态零差异且无观察器错误或诊断丢弃；
- 命令阶段取消已通过：服务端会在 Run `cancelled` 前闭合同一模型轮的全部待处理工具，正在执行的命令沿用现有取消结果且只产生一次 `tool_completed`，从未开始的调用写入带 `cancelledBeforeStart: true` 的耐久取消结果且不产生 `tool_started / command_started`；取消后 `pendingToolCalls`、`pendingInput` 与 `pendingAuthorization` 均为空，重复取消、并发取消和持久化重载回归通过。真实双工具人工验收中命令与未开始的 `VERSION` 读取均显示“已取消”，脱敏影子摘要观察 8 个事件、3 个快照和 11 次比较，`mismatches`、`observerErrors` 与 `diagnosticsDropped` 均为 0；命令失败路径仍尚未执行；
- 尚未形成真实零差异重复样本；
- 尚未把真实差异或近期跨层时序问题转换为 replay 夹具；
- 因此 H2 仍处于未收口状态，新投影仍不能接管现有 UI。
