# Code TODO

_上次更新：2026-07-27_

本清单只保留尚未完成的工作；已交付内容统一查阅 `CHANGELOG.md`。

---

## P0 · workbar 上线后收口

- ~~workbar品牌统一为小写字母~~ ✅ Code 部分已完成
- ~~确定正式 API 上游、模型组合与定价方案~~ ✅ BoxYing + ByteCatCode，9 款模型
- 扩充 workbar 可售模型池：基于用户需求、编程与通用能力、工具调用兼容性、上下文规格、响应速度、供应稳定性和实际成本筛选候选模型；先完成小流量实测与分层定位，再决定正式上架、默认推荐和下架策略。
- 调整 workbar DeepSeek 模型定价：以 DeepSeek 官方输入、输出和缓存定价为基准统一乘以 `1.15`，核对 New API 模型倍率、用户侧价格展示和计费测试，避免只修改展示值而实际扣费未同步。
- 建立上游供应商准入与持续评估：除 DeepSeek 外覆盖 Claude、Gemini 等主要模型，比较真实 API 稳定性、并发能力、首 Token/总延迟、SSE 粒度、工具调用兼容性、错误率、价格、结算与故障切换条件，不能以供应商后台“连通测试”替代普通用户令牌路径的实测。
- 单独核算 DeepSeek 官方“高峰期 2 倍扣费”：确认适用时段、时区、模型、Token 类型、缓存计费、倍率叠加顺序及上游是否同步传导；建立常态/高峰成本与毛利情景，再决定 workbar 使用固定价、分时价、成本缓冲或渠道调度。
- ~~更新重启机制修复~~ ✅ v0.5.23 改为 batch 方式，待 v0.5.24 端到端验证
- 创建令牌时选择模型限制加入供应商级联下拉框
- 确认 workbar 服务器国内可访问性
- ~~开发推送 `GENERATE_DEFAULT_TOKEN` + `QuotaForNewUser` 配置~~ ✅ 用户确认已完成
- 寻找 Gemini 替代渠道（LinkAI 不可用）
- ~~`tests/test_new_skills_routing.py` `sys.exit(0)` 导致 pytest 无法收集用例~~ ✅ 已加守卫 + 同步修复 updater 测试签名
- 完成 workbar 品牌 Logo、首页内容与用户文档。
- workbar登录/注册页改造
- 配置并启用企业域名邮箱。
- 完成生产环境运维闭环：备份、监控、告警、安全检查与恢复演练。
- 完成商业主体、经营许可和后续支付渠道升级准备。


---

## P1.5 · 会话导入收尾

- ~~完成 Codex / Claude Code 重复导入与前端状态闭环：稳定来源识别、无变化跳过、未续聊时原地更新、Code 已续聊时安全创建更新快照、项目和 `cwd` 保留、来源移动兼容、缓存强制复检、批量选择时序锁定、分类结果与部分失败反馈、无整页刷新的列表更新。~~ ✅ 已完成自动测试、浏览器验证与人工确认
- ~~完成导入批次的主动停止和失败项一键重试：停止请求允许当前项确定性完成、阻止后续项目且不回滚成功项；结果保留稳定 `errorCode / retryable`，仅重跑可重试失败项，并补齐进度、失败详情、中英文切换和刷新收尾状态。~~ ✅ 已完成自动测试、浏览器时序验证与人工确认
- ~~完成扫描范围与异常来源路径审计：覆盖权限拒绝、来源移动/删除、读取中变化、无效 UTF-8、损坏/未写完 JSONL、大体积临时落盘、根目录越界，以及同一来源会话 ID 多文件的确定性去重。~~ ✅
- ~~建立导入消息兼容矩阵并修复导入后兼容性：覆盖 Codex / Claude Code 的文本、reasoning、工具调用与结果、系统/摘要消息、附件、模型名、时间戳、usage 和异常 JSONL；验证导入会话在渲染、保存、继续对话、分支、压缩和再次加载后不会丢失、错序或退化。~~ ✅ 已完成消息协议、安全迁移边界、不可重放工具轨迹、压缩/导出保护及真实样本与人工验证
- ~~在新侧栏中把 `source` 仅作为来源徽标/筛选信息，不再生成 Codex / Claude Code 二级分组~~ ✅
- Codex 导入的模型名提取（turn_context.model） ✅
- 导入时更新时间戳为当前时间 ✅
- Codex 导入内容块类型兼容（input_text/output_text/text） ✅
- ~~完成导入窗口选择器与操作收口：来源数量与重新扫描、默认“可导入”及“全部”筛选、本地搜索、跨搜索保留选择、当前结果全选、底部选择数量、忙碌状态锁定、键盘/焦点语义、中英文即时切换和窄屏布局。~~ ✅ 已完成人工验收
- 低优先级审计导入会话的累计 usage：评估旧导入快照的安全重算或显式刷新策略、Claude Code 同一助手响应的重复 usage 去重，以及更多上游缓存写入字段的兼容；不得静默改写已经在 Code 继续过的历史会话。
- 低优先级增加“从文件导入”：允许用户选择任意位置的兼容 JSONL，先以只读方式自动识别并预检现有 Codex / Claude Code 格式，再复用迁移边界、来源标记、重复导入、更新快照、取消与失败重试闭环；不得接受前端任意磁盘路径或猜测转换未知/混合格式。第三方通用格式另行制定带版本头的 Code JSONL interchange 协议。

## P1 · Code 核心能力

- 修复 `release.py` 的发布说明自动化：当前 Phase 5 会重写预先编辑的说明，`--yes` 又会自动通过确认；应支持保留预写正文或显式传入说明文件，在提交、打标签和创建 GitHub Release 前阻止占位文案，并补充自动测试。
- 深入排查 workbar 网关/上游渠道稳定性与兼容差异：Code 已能区分模型授权失败和瞬时上游故障，并按 Key 的可见模型优先路由；但 Claude 普通令牌路径在最小流式请求、关闭思考和关闭工具后仍可能经历渠道内部重试并返回 502，而相同渠道的后台连通测试可以成功。后续需对照渠道测试与普通请求的账号/令牌选择、请求入口、超时、重试、模型映射和上游路由，不在 Code 侧叠加未经验证的重复重试。
- ~~完成 Agent 工具轮次的前端表达收口：第一轮与后续轮分别显示“等待响应 / 等待模型继续”，明确右侧为任务总耗时；把模型随工具调用返回的普通 `content` 从“思考摘要”改为紧凑过程更新，将工具调用、结构化成功/失败结果和重复次数组合展示，最终回答保持独立，并覆盖刷新恢复、中英文即时切换、停止任务和 SSE 增量不重复。~~ ✅ 已完成自动回归与真实 Claude 内容拦截分支人工验收
- ~~修复 AgentRun”过程性承诺被误判为完成”~~ ✅ v0.5.29 三分类逻辑：空文本+有思考→continue、空承诺检测（限2次）、error_code 体系
- ~~复核 Agent 真实场景中的三类无反馈异常：模型空回复；只给出“接下来会执行”之类承诺却没有后续动作；首条思考摘要出现过慢、用户无法判断任务是否正常进行。三类结果共享一次自动续行预算，工具/有效回答重置预算，终态与计数可持久恢复，并提供立即等待、25 秒延迟、60 秒慢上游和自动续行状态。~~ ✅ 已完成自动回归与真实模型人工验收
- 继续排查上游流式粒度：Code 已为上游大块纯文本增加有界的显示级平滑，并对首个正文、思考或工具调用设置 120 秒服务端硬截止（SSE 心跳与 usage 不会续期）；剩余问题是部分 Claude 请求为何在首个有效 SSE 内容前长期等待或直接 502，以及渠道原始分块是否稳定，需要与 workbar 普通请求路径的上游调查一起验证。
- 实现统一委托 `Context Envelope`：为模型子任务与用户并行任务提供受限、可预算、可审计的上下文。
- ~~增强错误分类与恢复建议~~ ✅ v0.5.29 后端 error_code 7 分类 + 前端 _errorCodeDisplay 映射 + _formatAgentError 差异化展示
- 重做登录后的新手指引，使 workbar 授权、Key 获取、模型选择与首次任务形成连续流程；旧版指引已移除。
- 加强 Skill 执行证据校验，避免只输出结论而未实际完成 Skill 要求。
- ~~**完成 Codex 风格项目/会话界面第二阶段**：移除 `Project > Source Group > Session` 和 `__unclassified__`；改为 `Project > Session`，每个项目默认显示 3 条最近会话，当前会话始终可见，并提供“显示全部/收起”~~ ✅
- ~~**完成会话运行目录闭环**：项目选择、当前会话、文件树根目录和 Agent 工具 `cwd` 必须保持一致；未归属会话继续使用自身 `cwd`~~ ✅
- ~~为上述界面补齐中英文即时切换、窄屏布局和来源徽标，并按人工测试步骤验证后提交~~ ✅
- ~~明确区分顶部工具栏的“导入会话”和“导出会话”：使用统一实心文件夹轮廓与向下/向上箭头区分方向，增大到 18 px，补齐独立悬停、提示文案、中英文、无障碍标签并移除多余分隔线。~~ ✅ 已完成人工验收
- ~~优化长会话时间线体验：改为 Codex 风格短线和主节点/上下两级次节点悬停阶梯；按 70% 可用高度动态限制窗口并支持时间线滚轮浏览，空间不足时自动隐藏；支持多个当前可见轮次、用户消息与模型最终回答纯文本预览、三个非空内容行，以及左右侧栏拖动期间的重排性能优化。~~ ✅ 已完成自动测试、真实长会话验证与人工确认
- 继续拆分 `app.js` 第四阶段，迁移剩余高耦合的会话与运行时逻辑。
- 补全 workbar 用量体验：在不暴露敏感信息的前提下展示账号、会话和任务维度的 Token / 成本信息。
- ~~**EXE 安装路径与桌面快捷方式**~~ ✅✅✅ v0.5.27→v0.5.28 端到端更新重启验证通过

---

## P2 · 规划与自动化

- 在现有 AgentRun / Child AgentRun 上实现结构化计划：步骤依赖、并发槽位、预算、验证证据和恢复状态。
- 结构化计划稳定后，再评估 Cron、Monitor、Workflow 与 worktree 隔离。
- 评估 macOS / Linux 支持与原生客户端形态。
- 准备公开发布所需的产品演示、使用案例和推广素材。

---

详细 Agent 路线：

- [`docs/agent-refactoring-execution-plan.md`](docs/agent-refactoring-execution-plan.md)
- [`docs/codex-claude-code-agent-design-analysis.md`](docs/codex-claude-code-agent-design-analysis.md)
