# workbar / Code 执行清单

_上次更新：2026-08-24_

本文件是整个 workbar 项目唯一的产品级未完成事项清单；完成事实与可复核证据统一进入 [`docs/development-log/README.md`](docs/development-log/README.md) 及其日期日志。

## Canonical 维护契约

### 双域编号游标

- **workbar 最后已分配编号**：`WB-012`
- **Code 最后已分配编号**：`CODE-038`
- 新增事项前必须重新读取对应主域游标并核对现存事项标题编号；以游标和现存最大编号中的较大值为基准加一，按三位数字补零，并在同一次修改中原子更新游标与新条目。并行修改已推进游标时按更大值重新计算，禁止产生重复 ID。
- 游标记录最后已分配编号，不等同于当前未完成事项的最大编号；事项完成并移除后，游标不得回退，既有编号不得复用或改义。workbar 与 Code 游标彼此独立、各自单调递增。

- 一级域固定为“workbar 运营与上线”和“Code 产品与工程”；每个未完成事项只能有一个主域、一个执行状态和一个稳定 ID。`WB-` / `CODE-` ID 一经分配不得改义或复用。
- 域内状态固定为：`当前进行`、`可直接启动`、`待决策/调研`、`外部等待`、`持续运营/观察`、`长期路线`。状态只表示执行可用性，不表示产品优先级；移动事项时保留 ID。
- 每项至少维护类型、优先级、风险、下一动作和完成定义。只有真实依赖才写 `depends-on`，非阻塞联系写 `related`；跨域事项不得复制，只能引用稳定 ID。
- 新事项先分配唯一主域/状态/ID；更新只写仍未完成的差量，不在 TODO 堆积已完成历史。完成后删除整项，并把完成事实、验证和提交写入开发日志。
- 活动 handoff 只记录进行中差量，并且只引用本清单的任务 ID/链接；不得复制清单正文、另建长期 TODO 或改变事项主域。当前活动 handoff 仅关联 [`WB-003`](#wb-003--渠道正式准入与持续评估)。
- Agent 不得从清单顺序自行选择或启动下一项；产品方案、外部授权和高风险边界仍按项目风险门禁确认。

---

# workbar 运营与上线

## 当前进行

### WB-003 · 渠道正式准入与持续评估

- **类型 / 优先级 / 风险**：渠道准入 / P0 / STRICT
- **下一动作**：在活动 handoff `upstream-channel-formal-admission` 中，对已启用 12 条渠道执行跨时段稳定性、并发、首 Token/总延迟、SSE、Code 工具闭环、错误率和账单一致性验证，并核对结算与故障切换。
- **完成定义**：按模型、渠道、时间段保存可追溯证据并作出正式准入决定；后台连通或单次短测不算准入。
- **活动 handoff**：`upstream-channel-formal-admission`（只关联本 ID，不在此复制交接正文）

## 可直接启动

### WB-007 · 国内可访问性确认

- **类型 / 优先级 / 风险**：上线验收 / P0 / STANDARD
- **下一动作**：从国内代表性网络核对 workbar 域名、页面与关键 API 的连通、延迟和错误表现。
- **完成定义**：形成可复核的地域/网络样本，阻断问题已有修复或明确回退与运维处置。

### WB-009 · Code 专题页连接文案校正

- **类型 / 优先级 / 风险**：内容与产品衔接 / P0 / STANDARD
- **下一动作**：把“填入/复制 API Key”的旧流程更新为“连接 workbar”自动同步账号 API Key 和模型列表，再为官方《快速开始》补对应截图。
- **完成定义**：使用步骤与开始使用区符合现行授权流程，截图无敏感信息且与真实界面一致。
- **related**：[`WB-008`](#wb-008--docsworkbarai-正式上线)

### WB-011 · 生产环境运维闭环

- **类型 / 优先级 / 风险**：生产运维 / P0 / STRICT
- **下一动作**：分别定义并实施备份、监控、告警、安全检查和恢复演练的可操作流程。
- **完成定义**：五类能力均有责任边界、运行证据、告警处置和至少一次可复核恢复演练。

## 待决策/调研

### WB-006 · DeepSeek 高峰期双倍扣费策略

- **类型 / 优先级 / 风险**：定价调研 / P0 / STRICT
- **下一动作**：确认适用时段、时区、模型、Token/缓存类型、倍率叠加顺序及上游传导，建立常态/高峰成本与毛利情景。
- **完成定义**：基于证据明确采用固定价、分时价、成本缓冲或渠道调度，并经定价授权后实施；不得在结论前改价。

## 外部等待

### WB-004 · GPT 分阶段调价剩余复核

- **类型 / 优先级 / 风险**：定价复核 / P0 / STRICT
- **下一动作**：随正常使用补证 Terra 缓存读取；取得 BoxYing Plus/Premium 的 `gpt-5.6-luna` 实扣并确认共享分组策略。Terra 当前价格与 `GPT-Codex-优质=0.09`、`GPT-Codex=0.06` 保持不变，DeepSeek 保持 `1.18`。
- **完成定义**：Luna 输入/输出/缓存实扣和分组毛利闭合，形成经授权的保持或调价决定；阿里折扣不并入本项。

### WB-008 · docs.workbar.ai 正式上线

- **类型 / 优先级 / 风险**：文档上线 / P0 / STRICT
- **下一动作**：仅在自然安全状态补必要 Code 配图；由开发按部署手册发布同 SHA artifact、配置 DNS/TLS 并执行 `nginx -t`，管理员再将后台文档链接设为 `https://docs.workbar.ai/`；逐项判断默认页脚中项目介绍、功能、入门、安装和 API 等旧链接语义。
- **完成定义**：真实缓存、安全头、404、桌面/移动、入口链接和回退均通过；不得制造模型调用/支付/凭据状态，不把文档打进 New API 镜像，不使用 `FRONTEND_BASE_URL`，也不得把不同页脚入口机械指向同一首页。
- **证据**：公开仓库根提交 `1482fa0`、部署手册提交 `c23bbd8`、成功 Actions `31457002313`；`main` 分支保护仍待确认。

### WB-012 · 商业主体、许可与支付升级准备

- **类型 / 优先级 / 风险**：合规与商业准备 / P0 / STRICT
- **下一动作**：按所在地与目标支付渠道确认商业主体、经营许可、材料和升级前置条件。
- **完成定义**：形成经专业复核的材料清单和执行路径，所需主体/许可就绪，支付升级可在另行授权后启动。

## 持续运营/观察

### WB-001 · 可售模型池维护

- **类型 / 优先级 / 风险**：产品运营 / P0 / STANDARD
- **下一动作**：持续基于用户需求、编程/通用能力、工具调用、上下文、响应速度、供应稳定性与实际成本评估模型。
- **完成定义**：每次上架、默认推荐、观察或下架都有证据、影响范围和可回退决定；本项为持续职责，不因单次调整关闭。

### WB-002 · 定价监控剩余闭环

- **类型 / 优先级 / 风险**：定价运营与工程 / P0 / STRICT
- **下一动作**：按 7 天有效期更新 ByteCatCode 脱敏快照；用供应商账户实扣确认 APINebula、BoxYing、Unity2、UU API、ByteCatCode 采购价并重点复核 `ApiNebula-Codex / gpt-5.4-mini` 显式缓存价；另行实现独立通知器及通知成功后的 `ack`。
- **完成定义**：快照和实扣证据在有效期内，通知/ack 可确定性重试且不误替换已确认缓存；每次调整前保留来源、快照、计算过程、影响范围和回滚值，基础价或分组倍率仅在明确授权后调整。既有每日/每周补最近到期槽、小时探测 15 分钟窗口、CC Switch 变化探测/168 小时九厂商审计、来源失败回退、operator report、UU API 映射与 5 模型范围继续作为运行契约。

### WB-005 · 阿里云百炼限时折扣观察

- **类型 / 优先级 / 风险**：价格观察 / P0 / STANDARD
- **下一动作**：持续保存 `qwen3.7-max` 当前 50% 优惠、`qwen3.7-plus` 基础/长上下文 20% 优惠的原价、折扣价、适用条件与到期回退信号。
- **完成定义**：折扣变化或到期有可靠提醒和候选建议；未经新授权不进入基础价修改。

---

# Code 产品与工程

> **当前确认推进序列（2026-08-24）**：无。清单顺序不构成自动启动授权，后续事项仍须由用户另行选择并按自身风险等级确认。

## 可直接启动

### CODE-013 · 登录后新手指引

- **类型 / 优先级 / 风险**：产品引导 / P1 / STANDARD
- **下一动作**：设计 workbar 授权、Key 获取、模型选择和首次任务的连续引导，替代已移除的旧版流程。
- **完成定义**：新用户可在不中断主流程的情况下完成连接和首次任务，失败/跳过/恢复路径及中英文、移动端可用。

### CODE-014 · Skill 执行证据校验

- **类型 / 优先级 / 风险**：Agent 可靠性 / P1 / STRICT
- **下一动作**：定义 Skill 要求、实际工具/产物证据和最终结论的确定性对应，识别只输出结论未执行的情况。
- **完成定义**：代表性成功、失败、部分执行和伪完成场景可判定，敏感信息不进入证据，旧 Skill 行为可兼容或安全降级。

### CODE-016 · workbar 用量体验

- **类型 / 优先级 / 风险**：产品能力 / P1 / STANDARD
- **下一动作**：定义账号、会话与任务维度的 Token/成本事实源、展示范围与脱敏规则。
- **完成定义**：三层用量可理解且一致，不暴露 Key、Authorization 或内部敏感账单字段，刷新与历史会话表现稳定。

### CODE-017 · 导入会话累计 usage 审计

- **类型 / 优先级 / 风险**：兼容审计 / P1.5 / STRICT
- **下一动作**：评估旧导入快照的安全重算/显式刷新、Claude Code 同一助手响应重复 usage 去重及更多上游缓存写入字段。
- **完成定义**：导入 usage 可解释且无重复，旧数据有兼容/回退；不得静默改写已在 Code 继续使用的历史会话。

### CODE-018 · 从文件导入会话

- **类型 / 优先级 / 风险**：导入能力 / P1.5 / STRICT
- **下一动作**：设计任意位置文件的只读识别和预检，复用 Codex/Claude Code 迁移、来源标记、重复导入、更新、取消和失败重试；第三方格式另定带版本头的 interchange 协议。
- **完成定义**：兼容文件可安全导入且无重复/越权路径访问；未知或混合格式拒绝而不猜测转换，旧会话与附件保持完整。

### CODE-036 · 外链 favicon 准确获取、显示与主题适配优化

- **类型 / 优先级 / 风险**：产品体验与性能 + 后端代理 / P1 / STRICT
- **下一动作**：启动第二阶段 favicon 安全解析：按 STRICT 门禁提供同源图标解析/代理，只访问经校验的公网 HTTP(S) 目标，限制重定向、内网/DNS 重绑定、超时、响应大小和 MIME；按页面声明确定性选择图标、解析相对 URL，以有界 host TTL 缓存和 favicon/glyph 降级保证失败安全。本阶段不引入第三方并发竞速，也不新增 localStorage 第二缓存；安全解析通过后再按 STANDARD 门禁验收图标尺寸、对齐和深浅主题表现。
- **完成定义**：常见与冷门站点可稳定取得自身声明的清晰图标；无声明、拒绝、超时、越界或资源异常时确定性降级且不泄漏内网或敏感信息，缓存有界、刷新不重复冷抓取。视觉阶段完成后，外链与本地资源图标在深浅主题下大小统一、对齐清晰，并且图片卡、预览、右键菜单和悬浮交互无回归。
- **来源**：answer-render R014–R017 对 cache 型 miss、1×1 图标、尺寸和主题表现的实测；favicon 安全解析与后续视觉验收边界已由用户确认，尚未实施。

## 外部等待

### CODE-010 · workbar 令牌与渠道测试兼容差异

- **类型 / 优先级 / 风险**：运行时诊断 / P1 / STRICT
- **下一动作**：等待 `WB-003` 形成已启用 12 条渠道的正式准入证据；若证据仍显示普通令牌与后台渠道测试存在兼容差异，再对照账号/令牌选择、请求入口、超时、重试、模型映射、原始分块和上游路由，聚焦部分 Claude 请求的渠道内重试、首个有效 SSE 前长等待和 502。
- **完成定义**：两条路径差异有脱敏可复核证据，根因和责任边界明确，修复/准入方案经确认并验证；不得在 Code 侧叠加未经验证的重复重试。
- **depends-on**：[`WB-003`](#wb-003--渠道正式准入与持续评估)

## 待决策/调研

### CODE-035 · 多分组 API Key 的模型路由与分组一致性

- **类型 / 优先级 / 风险**：凭据与模型路由 / P0 / STRICT
- **下一动作**：设计并确认 Key、Workbar Token 分组、模型可用范围与用户所选模型/分组的唯一可信映射，修复多个分组 Key 同时启用时 `modelKeyMap` 由首个 Key 任意认领重叠模型的问题；覆盖同步后刷新、Key 排序/禁用、模型缓存失效、同模型多分组及失败回退，且不得读取、展示或持久化泄漏真实 Key。
- **完成定义**：Gemini 等重叠模型始终使用与用户选择一致的 Token 分组；单 Key 旧行为兼容，多 Key 刷新/重启/切模型结果确定，错误 Key 不再产生“目标分组无可用渠道”，失败回退不跨越未经授权的价格分组且有可复核自动与真实验收证据。
- **related**：[`WB-003`](#wb-003--渠道正式准入与持续评估)

### CODE-012 · 模型上下文上限事实源

- **类型 / 优先级 / 风险**：能力与配置 / P1 / STRICT
- **下一动作**：按 [`docs/context-window-budget-development-guide.md`](docs/context-window-budget-development-guide.md) 对尚未实现的 connection-specific verified override 另行决策/调研：明确可信配置入口、权限、同 scope 冲突合并、删除与回退；同时决定现有自动校准记录的用户可见检查和显式重置入口。D-lite 自动向下校准已经完成，未经新审批不扩展为人工提高或强制覆盖。
- **完成定义**：连接级覆盖有唯一可信入口、权限和可解释的冲突/删除回退；用户可在不暴露 Key、指纹、原始错误或内部路径的前提下检查校准来源/有效期并显式重置，删除、过期或重置后安全回到 metadata → official → family/unknown 能力链。

### CODE-029 · 启用自动模式的风险确认弹窗

- **类型 / 优先级 / 风险**：权限安全交互 / P1 / STANDARD
- **下一动作**：先确认选择“自动”权限档（bypass）时的重复确认策略，再设计创建任务前的风险确认弹窗；文案需说明自动执行全部允许操作、不弹授权，同时如实标注既有拦截边界，并明确“自动”与“接受编辑”的差异及中英文呈现。
- **完成定义**：经确认的提示频率与作用范围被确定性执行，拒绝确认前不创建任务；弹窗文案与既有硬拦截行为一致（对照 i18n `permBypassTip` 现状修正），bundle/direct classic、i18n 双语、无项目/切换权限/旧会话场景通过，既有权限档行为零回归。

### CODE-006 · 独立生图模型 API

- **类型 / 优先级 / 风险**：新能力设计 / P1 / STRICT
- **下一动作**：确认生图服务地址、协议、模型、Key 路由、能力元数据、费用/授权/审计及文生图和参考图编辑边界，统一普通、队列、并行、Child、Skill 与工具入口。
- **完成定义**：唯一服务端边界覆盖格式、尺寸、质量、数量、超时、取消和错误；Key 不进入 JSONL/上下文/事件/日志，未配置或失败安全回退且不借用聊天配置。

### CODE-007 · Agent 剪贴板工具

- **类型 / 优先级 / 风险**：能力调研 / P1 / STRICT
- **下一动作**：调研 Windows、Codex 桌面端与浏览器能力，确认显式触发、授权、数据类型、工具协议、审计和失败降级。
- **完成定义**：形成是否加入工具列表的可实施结论；任何实现不得静默读取，也不得把 Key、密码等敏感内容写入日志、会话或上下文。

### CODE-008 · 语音输入

- **类型 / 优先级 / 风险**：新交互与服务 / P1 / STRICT
- **下一动作**：先验证 `getUserMedia`、`MediaRecorder`、MIME、3010/3011、bundle/classic 与打包环境；再确定有时长/大小上限的浏览器录音、本地 `/api/transcribe` 和可配置服务/本地引擎，`SpeechRecognition` 仅作可选快路径。
- **完成定义**：显式授权后录音，转写只进入可编辑草稿不自动发送；原始音频默认不进入 Session JSONL、日志或模型上下文；拒绝/无设备/不支持/启停取消/中英/队列并行通过，音轨与临时音频清理，旧会话和纯文本回退不变。

### CODE-009 · 会话可恢复归档

- **类型 / 优先级 / 风险**：数据生命周期 / P1 / STRICT
- **下一动作**：评估与压缩历史快照分离的软删除存储、归档列表、恢复、永久删除、附件/分支和保留期限；方案确认前维持现有活动会话硬删除。
- **完成定义**：兼容旧会话并提供清晰恢复/永久删除语义、迁移与回退，数据不会无提示丢失或与压缩快照混淆。

### CODE-011 · 统一委托 Context Envelope

- **类型 / 优先级 / 风险**：Agent 架构 / P1 / STRICT
- **下一动作**：为模型子任务与用户并行任务定义受限、可预算、可审计的上下文边界、继承规则和敏感信息过滤。
- **完成定义**：普通/并行/Child 的上下文最小且可解释，预算、授权和隔离可验证，旧 AgentRun/会话兼容并可回退。

### CODE-020 · AgentRun 生命周期 Hook

- **类型 / 优先级 / 风险**：内部扩展机制 / P2 / STRICT
- **下一动作**：先设计版本化、脱敏、非阻塞、只读的运行/模型/工具/授权/终态事件观察，包含超时隔离和失败降级；稳定后再评估受限执行前策略。
- **完成定义**：渠道证据、通知、审计和验收可可靠订阅，严禁记录 Access Token/API Key/Authorization；在安全兼容恢复验证前不开放任意脚本、参数改写或自动批准。

### CODE-026 · 不同会话 Agent 通信能力调研

- **类型 / 优先级 / 风险**：Agent 协作调研 / P2 / STRICT
- **下一动作**：比较同主机与跨主机场景下的会话身份、发现与绑定、用户主动消息语义和授权边界，并研究消息投递幂等/回执、最小上下文裁剪、失败恢复、循环防护与可审计性；本项只形成调研结论，不预先承诺协议或实现。
- **完成定义**：形成覆盖身份与授权、传递语义、幂等和恢复、安全隔离及审计的分阶段设计，明确兼容、失败降级、回退和独立验收边界；任何实现须另行按 STRICT 门禁确认。
- **related**：[`CODE-011`](#code-011--统一委托-context-envelope)、[`CODE-020`](#code-020--agentrun-生命周期-hook)

### CODE-022 · macOS / Linux 与原生客户端形态

- **类型 / 优先级 / 风险**：平台调研 / P2 / STRICT
- **下一动作**：盘点 Windows 专属依赖、托盘/文件/命令/浏览器能力和打包发布差异，比较跨平台 Web、桌面壳与原生形态。
- **完成定义**：形成平台优先级、兼容矩阵、分阶段成本与回退方案，不以未验证的“跨平台兼容”承诺替代实测。

## 持续运营/观察

### CODE-015 · Windows PATH 与解释器所有权观察

- **类型 / 优先级 / 风险**：开发环境观察 / P1 / STANDARD
- **下一动作**：持续检查第三方 Agent/工具安装器是否把内部 venv 置于用户 PATH 前部；巡检 `where python/pythonw/hermes`、pytest、`pystray/PIL`、3011 托盘和 Office 命令冒烟。
- **完成定义**：发生漂移时优先恢复解释器所有权并记录根因/恢复证据，不向第三方内部 venv 安装项目依赖；本项为持续观察职责。
- **证据**：Hermes 专用启动器与 PATH 后移已恢复当前现场。

## 长期路线

### CODE-001 · Harness 后续轮次边界

- **类型 / 优先级 / 风险**：质量工程路线 / P1 / STRICT
- **下一动作**：另行确认下一轮目标、自动验收和安全边界后，才可从三类剩余中选择阶段：① active worker 跨进程消费 completed receipt 的 explicit/auto resume 与是否允许新模型请求；② 异构多工具、required/解析、权限/编码/大文件/执行器失败、错误交替、其他模型/classic、取消与长输出；③ 压缩/图片/队列并行/background/Child、多 proposal、多标签、重启/crash、Firefox/WebKit、真实外网/模型/凭据及主观生命周期。
- **完成定义**：被选阶段有独立完成线和冻结证据；不得由第一轮 terminal 跨进程证据推定 active 恢复，也不外推通用 exactly-once 或授权完整覆盖。
- **证据**：[Harness 第一轮完成线](docs/harness/harness-round-1-completion-line.md)，冻结树 `618f876fb7d09f821b302eaf5a731f367fd894ac`；H0～H4-8G 已完成事实只从日志/专题查阅。

### CODE-021 · Cron、Monitor、Workflow 与 worktree 隔离

- **类型 / 优先级 / 风险**：自动化路线 / P2 / STRICT
- **下一动作**：等待 Goal/结构化计划稳定，再评估定时、监控、工作流编排和隔离 worktree 的所有权、恢复与权限模型。
- **完成定义**：前置能力稳定，方案覆盖重复执行、并发、恢复、授权和资源回收，并按独立阶段验收。
- **已满足前置**：`CODE-019`（Goal v2 持久执行闭环）已完成；本项仍须另行确认后启动。

### CODE-023 · 公开发布演示与推广素材

- **类型 / 优先级 / 风险**：产品发布路线 / P2 / STANDARD
- **下一动作**：在产品与发布范围稳定后，规划脱敏演示、代表性使用案例和推广素材。
- **完成定义**：素材只展示已发布能力，案例可复现、无敏感信息，文案与当前版本事实一致并通过发布审核。

---

## 长期专题索引

- [Harness 工程优化计划](docs/harness-engineering-optimization-plan.md)
- [Agent 重构执行计划](docs/agent-refactoring-execution-plan.md)
- [Codex / Claude Code Agent 设计分析](docs/codex-claude-code-agent-design-analysis.md)
