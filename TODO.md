# Code TODO

_上次更新：2026-08-07_

本清单只保留尚未完成的工作；已交付内容统一从 [`docs/development-log/README.md`](docs/development-log/README.md) 索引查阅。

---

## P0 · workbar 上线后收口

- 持续维护 workbar 可售模型池：基于用户需求、编程与通用能力、工具调用兼容性、上下文规格、响应速度、供应稳定性和实际成本，继续决定新模型的正式上架、默认推荐、观察和下架策略。
- 完成模型定价每日监控剩余阶段：脱敏记录、标准快照、语义差异、来源故障续用、中文报告、九家官方适配器、12 条启用渠道采集器、ByteCatCode 原始 JSON 确定性导入器、无需 AI 的官方/上游一键流水线、原始响应历史归档、模型/渠道双视图精简总表、缓存支持状态、统一分组倍率与逐维毛利影响分析，以及 P0/P1/P2 确认、来源重试/恢复、到期/未来价格、事件去重、并发合并重跑和离线恢复的确定性触发状态机已经完成；固定每日/每周任务只补最近一个到期槽，小时探测超过默认 15 分钟执行窗口不补跑。三个本地 Codex 计划任务已经启用，统一调用确定性调度入口；小时探测复用官方记录、只实时采集上游。每日任务已增加 CC Switch 本地社区缓存严格校验与 35 模型变化探测：无变化复用已确认官方价，变化时只复核受影响厂商，异常时完整回退，并在距离上一次成功完整采集达到 168 小时时强制完成九厂商官方审计；失败运行不替换已确认缓存或重置审计周期。调度输出已拆分本轮新增/历史持续通知并生成可操作 `operator_report`。UU API 已固化 `kimi-k2.6 -> kimi-k2.6x` 渠道级显式映射并拆分上游可访问范围与 workbar 当前 5 模型启用范围。下一步按 7 天有效期更新 ByteCatCode 本地脱敏快照，用供应商账户实际扣费确认 APINebula、BoxYing、Unity2、UU API 与 ByteCatCode 的采购价，并重点复核 `ApiNebula-Codex / gpt-5.4-mini` 的显式缓存价；另行实现独立通知器和通知成功后的动作 `ack`。每次调整前继续保存来源、快照、计算过程、影响范围和回滚值，实际修改基础价或分组倍率仍需明确授权。
- 完成 GPT 分阶段调价的剩余复核：用户已将 `gpt-5.6-terra` 更新为基础档 `13.4 / 80.4 / 1.34`、长上下文档 `26.8 / 120.6 / 2.68` CNY/MTok，并将 `GPT-Codex-优质` 从 `0.08` 调至 `0.09`；`GPT-Codex` 保持 `0.06`。模型广场全部维度反算一致，BoxYing-Codex-Premium 首笔 Terra 输入/输出实扣确认实际毛利约 17.07%；连续两次未观测到缓存读取，后续随正常使用补证，不阻断当前倍率。`gpt-5.6-luna` 继续保持旧价，待 BoxYing Plus/Premium 实扣与共享分组策略确认。DeepSeek 本轮保持 `1.18`，阿里限时折扣仍只列候选。
- 持续监控但暂不调整阿里云百炼限时折扣：`qwen3.7-max` 当前限时优惠较 2026-07-29 workbar 快照低 50%，`qwen3.7-plus` 基础档和长上下文档均低 20%。用户已确认限时折扣只列入变更候选，不进入当前基础价修改范围；继续保存原价、折扣价、适用条件和到期回退信号。
- 完成当前渠道的正式准入与持续评估：执行跨时段稳定性、并发能力、首 Token/总延迟、SSE 流式行为、Code 工具闭环、错误率和 Workbar 账单一致性验证，核对结算与故障切换条件，并按模型、渠道、时间段保存可追溯证据；供应商后台连通测试或单次质量短测不得直接视为正式准入。
- 单独核算 DeepSeek 官方“高峰期 2 倍扣费”：确认适用时段、时区、模型、Token 类型、缓存计费、倍率叠加顺序及上游是否同步传导；建立常态/高峰成本与毛利情景，再决定 workbar 使用固定价、分时价、成本缓冲或渠道调度。
- 确认 workbar 服务器国内可访问性
- 逐篇完成面向普通 AI 工具用户和初学者的 workbar 官方用户文档，并部署为独立文档站：每次只推进一个主题，核对真实操作后按“目标、准备、步骤、成功标志、常见问题、下一步”编写，必要时补脱敏操作截图，用户验收后再进入下一篇。第一篇为《5 分钟快速开始》，后续依次覆盖文档首页、密钥管理、workbar Code、Claude Code、其他兼容工具、模型与自动分组、余额费用、故障排查、账号安全及 API 参考；品牌 Logo、首页和登录/注册页已经完成并合入 `new-api-source`。
- 配置并启用企业域名邮箱。
- 完成生产环境运维闭环：备份、监控、告警、安全检查与恢复演练。
- 完成商业主体、经营许可和后续支付渠道升级准备。

---

## P1 · Code 核心能力

- 按 [`Harness 工程优化指导`](docs/harness-engineering-optimization-plan.md) 继续完成 H4 剩余浏览器生命周期；H0～H3、replay 发布门禁、H4-1 浏览器基础设施与首批冒烟、H4-2 默认 bundle 刷新恢复、H4-3 classic fallback 直接入口刷新兼容、H4-4 自动 bundle 失败降级、H4-5A completed AgentRun 跨进程终态重载、H4-5B1 终态工具轨迹跨进程零重执行、H4-6A 默认 bundle 工具详情生命周期、H4-6B direct classic 工具详情对等、H4-6C 默认 bundle 同轮双工具生命周期及 H4-6D direct classic 同轮双工具对等均已收口，长期事实见对应专题和开发日志。
  - H4-2 已完成默认 bundle 的同进程模型流式刷新恢复：首增量前刷新、已有两段后刷新继续追加、已有两段后刷新再取消均复用同一 AgentRun/Runtime，未创建第二上游请求或重复 assistant；活动 Session JSONL 不保存部分正文及 `streaming/_streamProjection`，成功终态才保存完整正文。
  - 2026-08-07 真实 Code Dev 复验发现并修正了启动阶段的模型目录阻塞：AgentRun/Runtime 恢复现在位于 `refreshModels()` 之前，但仍在平台同步与认证失效检查之后。慢目录闸门回归证明目录阻塞期间已发起 Runtime GET 并恢复前缀/取消；人工结果是刷新后短暂等待即恢复正常流式，当前基本满足需求。这不声称正文瞬时恢复；后续若追求更低延迟，须单独度量 Session/平台同步、AgentRun GET、Runtime GET 和首个 DOM 追赶的时间线，不得通过新建第二刷新状态机或把临时流式字段写入 JSONL 规避。
  - H4-3 已完成 classic fallback 直接入口的同进程刷新兼容：三条 classic 场景复用 H4-2 的相同恢复流程、闸门和断言，保持同一 AgentRun/Runtime、DOM 前缀不回退及取消终态闭合。
  - H4-4 已完成默认根入口的自动失败降级：真实 Chromium 分别注入单次 bundle-load 与 bundle-init 故障，固定对应 fallback 原因、单次 classic 初始化和降级后纯文本任务闭环；隔离 host 同时以严格命令身份/退出分类关闭 shutdown 响应与正常 exit(0) 的竞态，不会吞掉非零、signal 或非 shutdown 错误。提交前曾独立出现一次 stop-time metrics 超时；阶段按门禁停止并增加白名单阶段 breadcrumb，随后一次定向与累计三轮标准 11 例矩阵连续通过，但历史超时仍未复现、根因未定位，诊断不代表放宽 5 秒上限、stop-time metrics 或严格清理门禁。
  - H4-5A 已完成 completed/terminal 纯文本 AgentRun 的真实跨进程重载：进程 A 完成持久化并退出、端口关闭后，进程 B 以新 PID、新随机端口和新 origin 复用同一受控数据根；浏览器通过生产 Session 列表/UI 打开同一 Session，B 读取同一 completed AgentRun，旧 Runtime 返回 404 且不创建新 Run、Runtime、chat 或工具执行，Session JSONL 与 DOM user/assistant/final 保持唯一。普通前台 `clientRequestId` 当前为空，本阶段只验证 A/B 保存同一实际值。
  - H4-6A 已完成默认 bundle 单只读工具的真实详情生命周期：运行中用户展开在第二轮正文重绘后保持，同一稳定 key 不变；服务端 completed 后等待前端 banner、stop 和 active/completed trace 四项收敛，再证明工具组自动折叠。完成态通过父 trace→工具组→单项三层真实点击查看参数与格式化结果；完整刷新后三层默认折叠且 AgentRun POST、Runtime POST、chat、工具执行增量均为 0，展开偏好不持久化。
  - H4-6B 已完成 direct classic 的单只读工具详情对等：复用 H4-6A 相同生产模块、双闸门和参数化流程，保持运行中重绘展开、前端终态折叠、完成态三层交互、完整刷新默认折叠与四项增量为 0；classic 精确标记为 `classic-fallback` 且无 bundle ready，本阶段不属于自动 bundle 故障降级。
  - H4-6C 已完成默认 bundle 的同轮双只读工具详情生命周期：T1/T2 均受限为 `read_file("fixture.txt")`，第二调用只增加 `startLine=1/endLine=1`；测试侧闸门固定 T1 completed/T2 running 的执行边界，终态闭合 11 事件、Session 两对 tool-call/tool-result、单组双项 T1→T2、重绘保持展开、刷新默认折叠及四项零增量。自动证据只证明结构稳定性，不证明人眼感知的整片闪烁、布局跳动或滚动问题已解决。
  - H4-6D 已完成 direct classic 的同轮双只读工具对等：参数化复用 H4-6C 的同一双工具任务、三个闸门及 AgentRun/Runtime/Session/DOM 链，保持 11 事件、T1→T2、单组双项重绘与刷新四项零增量；classic 精确标记为 `classic-fallback` 且无 bundle ready，不属于自动故障降级场景，八类领域哈希与 bundle 完全相同。
  - H4-2/H4-3 取消只保证已有正文保留、唯一暂停、唯一 DELETE、cancelled 且无成功终答；本地合成上游可能有一个在途片段先于取消收敛落盘，不承诺点击后绝无新字节。H4-5A/H4-5B1 不证明 active Run 自动续流、部分正文跨进程保留、同 origin/原标签页重连、Runtime 持久化或工具副作用 exactly-once。
  - 真实剩余事项：active worker 跨进程消费已完成 receipt 的产品语义门禁（须先明确 explicit/auto resume 及是否允许新模型请求），异构多工具、工具失败/取消/长输出详情，以及问卷/授权、压缩、图片、队列/并行/Child 等真实 DOM 生命周期；后续阶段确认前不得把 HTML 投影、离线 replay、自动降级、同进程模型流式刷新、terminal 工具轨迹零重执行或 H4-6A～H4-6D 证据扩张为这些路径已通过。H1～H4 稳定后才扩大 Context Envelope、结构化计划、生命周期 Hook 与 Workflow。
- 调整消息输入框与页面主题背景的层级：深色主题下让输入框使用略浅的中性主题底色（如灰色），避免输入区域与页面背景融为一体；浅色主题也应通过统一设计令牌保持可辨识但不过度突出。实现时一并核对默认、悬停、聚焦、禁用、拖放附件和多行展开状态，以及开发构建、正式构建与经典回退页面，保证文本、占位符、边框和操作按钮的对比度不回归。
- 重新评估顶部“工具日志”的产品定位：当前单个任务内的阶段工具组已承担工具时序、当前动作、展开详情和完成统述，顶部全局工具记录总览存在信息重复且独立阅读价值不明确。先盘点它是否仍服务于跨任务观察、后台/并行任务、失败定位或快速跳转；若没有不可替代职责则移除，并同步清理状态、事件订阅和空白布局；若保留则扩展为明确的全局活动/异常入口，不再重复逐条展示当前任务已有记录。验收需覆盖普通任务、队列、并行与 Child AgentRun、任务切换、失败/取消、刷新恢复和无工具任务，且不得丢失唯一可见的错误或进行中状态。
- 优化新消息发送后的阅读锚点与流式滚动：当用户原本位于会话底部并发送新消息时，临时把该用户消息定位在输入框上方一段稳定距离，为模型首段回答和后续工具/思考轨迹预留屏幕中部空间，避免内容从最底部起步及流式增长造成频繁视口抖动；用户已经向上浏览历史时不得强制抢回滚动位置。方案需先对照当前 Codex 的实际消息布局、发送后锚定、流式跟随和用户接管滚动行为；[OpenAI 官方更新日志](https://learn.chatgpt.com/docs/changelog#codex-2026-06-16-app)目前只公开提到改进了 assistant message actions 与 transcript layout，未披露具体滚动算法，因此实现依据必须来自当前版本的可复现实测，而不是根据描述推测。优先评估稳定锚点/动态占位与按帧合并滚动更新，明确占位何时释放、用户滚动何时接管，并覆盖短/长回答、工具组展开折叠、图片、窗口缩放、移动尺寸、刷新恢复、队列和并行任务；验收以发送后首帧位置稳定、流式过程无明显闪动、用户手动滚动不被打断为准。
- 增加历史消息“一键到底”入口：当用户向上滚动并离开会话底部一定距离时，在输入框上方靠近底部的位置显示轻量悬浮按钮，点击后直接回到最新消息；位于底部时自动隐藏，不能遮挡输入框、附件、问卷、授权面板或消息操作。按钮显隐应基于真实滚动距离并做稳定阈值处理，避免流式输出、图片加载、工具组展开折叠和输入框高度变化时反复闪现；用户主动浏览历史时不得被自动滚动抢回底部。验收覆盖短/长会话、持续流式输出、图片、工具详情、窗口缩放、移动尺寸、刷新恢复、队列和后台消息，并与后续“新消息阅读锚点”共用一致的滚动所有权规则。
- 排查运行中工具组偶发的大面积内容闪烁：真实多工具与刷新恢复验收中，计时、单观察者所有权和最终回答均正常，但工具事件更新时偶尔会看到整片工具详情短暂闪现。先用脱敏事件序列区分整组 `innerHTML` 重建、`details[open]` 展开状态恢复时序、运行中/完成态标题切换和自动滚动造成的视觉跳变，再决定采用稳定节点复用、帧内批量更新或渲染前恢复展开状态；不得通过隐藏工具内容掩盖问题。验收覆盖普通多工具、用户主动展开、刷新恢复、工具失败、长命令输出、队列和后台任务，并确认最终折叠规则与用户手动展开状态不回归。
- 增加独立的生图模型 API 配置，并统一所有图片生成场景的调用规则：设置中将生图服务与普通对话模型解耦，明确服务地址、协议/端点、模型、Key 路由和能力元数据，区分文生图、参考图编辑、支持的输入图片格式、尺寸/质量/数量、超时、取消、费用与错误映射；普通会话、排队消息、后台并行任务、Child AgentRun、Skill 和工具触发的生图需求必须经过同一个服务端调用边界，不在前端或各功能中分散直连，也不得把生图 Key 写入会话 JSONL、模型上下文、事件或日志。需要定义显式触发、授权与审计规则，避免模型在用户未要求时自动产生高成本调用；未配置、模型不支持、请求失败或关闭新逻辑时应给出可理解提示并安全回退，不得静默借用当前聊天模型配置。方案与验收需覆盖文生图、单图/多图参考编辑、普通/队列/并行/子任务路径、中英文提示、刷新与取消、旧配置兼容、凭据脱敏，以及后续更换供应商或协议时仍保持唯一事实源。
- 调研 Agent 读取剪贴板的现有实现方式，并评估是否加入 Code 工具列表：明确 Windows、Codex 桌面端与浏览器环境的能力边界、用户授权和显式触发方式、文本与其他数据类型、工具协议、审计记录及失败降级；不得静默读取剪贴板，不得把 API Key、密码或其他敏感内容写入日志、会话或模型上下文。
- 分阶段增加语音输入：在消息输入框旁提供麦克风入口，必须由用户显式点击并授权后才可采集；录音、转写、成功、失败和取消状态需清晰可见，转写结果只合并为可编辑草稿并保留原文字与光标位置，不得自动发送。第一阶段先验证 `getUserMedia`、`MediaRecorder`、可用 MIME 与 3010/3011、默认构建、经典回退页及打包启动环境的兼容性；正式 MVP 优先采用有时长和大小上限的浏览器录音，加本地同源 `/api/transcribe` 转写代理及一个可配置的转写服务或本地引擎，浏览器 `SpeechRecognition` 仅作为能力探测后的可选快速路径，不得成为唯一实现。后端需补齐模型/Key 路由、格式归一、超时、取消和错误映射；默认不把原始音频写入 JSONL、日志或模型上下文，成功、失败、取消和重启后均释放音轨并清理临时数据。旧会话格式保持不变，能力不可用或关闭功能时回退为纯文本输入。验收覆盖拒绝权限、无麦克风、不支持 API/MIME、连续启停与取消、转写合并、中文/英文、普通发送/队列/并行任务不回归，以及临时音频无残留。
- 低优先级评估将会话删除改为可恢复归档：软删除归档必须与上下文压缩生成的历史快照分开存储和表达，提供归档列表、恢复及明确的永久删除入口，并定义附件、分支关系、保留期限和旧会话兼容策略；在该方案完成前继续保持现有活动会话硬删除行为。
- 深入排查 workbar 普通令牌路径与后台渠道测试的兼容差异：Code 已能区分模型授权失败和瞬时上游故障，并按 Key 的可见模型优先路由，也已为大块纯文本增加有界的显示级平滑和 120 秒首个有效内容硬截止；剩余问题是部分 Claude 请求仍会在渠道内部重试、首个有效 SSE 内容前长期等待或直接返回 502。后续需对照两条请求路径的账号/令牌选择、请求入口、超时、重试、模型映射、原始分块和上游路由，不在 Code 侧叠加未经验证的重复重试。
- 实现统一委托 `Context Envelope`：为模型子任务与用户并行任务提供受限、可预算、可审计的上下文。
- 完善模型上下文上限的事实源，使占用百分比与 90% 自动压缩阈值不再只依赖模型名称推断；当前自定义别名或渠道实际限制可能低于按名称判定的 1M，导致百分比偏低、预防性压缩触发过晚。可执行方案按以下顺序评估：
  1. 首选扩展模型列表/网关接口，返回规范化的 `contextWindowTokens` 能力元数据，由前后端读取同一事实源；需要区分模型理论上限与具体渠道可用上限。
  2. 增加模型及“渠道 + 模型”显式覆盖配置，建议优先级为渠道模型覆盖 > 模型元数据 > 模型族启发式 > 未知模型保守默认 128K，并提供来源展示、合法范围校验和一键恢复默认值。
  3. 保留运行时上下文超限后的单次压缩重试，并可从上游明确返回的最大 Token 数仅向下校准本次或带有效期的实际限制；该结果需记录来源、适用渠道和失效时间，不得根据模糊 400 错误自动学习，也不得自动提高上限。
  验收需覆盖 128K、200K、1M、自定义别名、同模型不同渠道、缺失/错误元数据与旧 AgentRun 恢复；前端百分比、服务端阈值和持久化值必须一致。新增字段保持可选，旧会话无需迁移；关闭能力元数据或删除覆盖配置后应能回退到现有保守规则。
- 重做登录后的新手指引，使 workbar 授权、Key 获取、模型选择与首次任务形成连续流程；旧版指引已移除。
- 加强 Skill 执行证据校验，避免只输出结论而未实际完成 Skill 要求。
- 观察第三方 Agent / 开发工具安装器对 Windows 用户 PATH 与解释器解析顺序的影响：本机安装 Hermes Agent 后，其内部 `venv\Scripts` 被置于用户 PATH 前部，导致裸 `python` / `pythonw` 命中裁剪过的 Hermes 虚拟环境，已出现 pytest 不可用、Office 工具依赖缺失，以及 Code Dev 3011 服务正常但托盘因缺少 `pystray` 而不显示。当前已通过 Hermes 专用启动器并将内部 Scripts 路径后移恢复；后续继续观察其他安装器、Hermes 更新或系统重启是否重新置顶路径或引发同类工具消失。巡检信号包括 `where python` / `where pythonw` / `where hermes` 的顺序、`python -m pytest --version`、`python -c "import pystray, PIL"`、3011 Code Dev 托盘和 Office `run_command` 冒烟；若再次发生，优先恢复解释器所有权，不向第三方内部虚拟环境安装项目依赖。
- 补全 workbar 用量体验：在不暴露敏感信息的前提下展示账号、会话和任务维度的 Token / 成本信息。

---

## P1.5 · 会话导入后续

- 低优先级审计导入会话的累计 usage：评估旧导入快照的安全重算或显式刷新策略、Claude Code 同一助手响应的重复 usage 去重，以及更多上游缓存写入字段的兼容；不得静默改写已经在 Code 继续过的历史会话。
- 低优先级增加“从文件导入”：允许用户选择任意位置的兼容 JSONL，先以只读方式自动识别并预检现有 Codex / Claude Code 格式，再复用迁移边界、来源标记、重复导入、更新快照、取消与失败重试闭环；不得接受前端任意磁盘路径或猜测转换未知/混合格式。第三方通用格式另行制定带版本头的 Code JSONL interchange 协议。

---

## P2 · 规划与自动化

- 在现有 AgentRun / Child AgentRun 上实现结构化计划：步骤依赖、并发槽位、预算、验证证据和恢复状态。
- 基于现有持久 AgentRun 事件流设计内部生命周期 Hook / 订阅机制：第一阶段只支持脱敏、非阻塞、只读的事件观察，用于渠道测试证据采集、任务与授权通知、审计记录和结束验收；事件至少覆盖运行、模型轮、工具轮、授权及终态，并提供稳定的版本化结构、超时隔离和失败降级，严禁记录 workbar Access Token、API Key 或 Authorization 头。完成安全、兼容和恢复测试后，再评估受限的执行前策略能力；在此之前不开放任意本地脚本、工具参数改写或自动批准权限。
- 结构化计划稳定后，再评估 Cron、Monitor、Workflow 与 worktree 隔离。
- 评估 macOS / Linux 支持与原生客户端形态。
- 准备公开发布所需的产品演示、使用案例和推广素材。

---

详细 Agent 路线：

- [`docs/harness-engineering-optimization-plan.md`](docs/harness-engineering-optimization-plan.md)
- [`docs/agent-refactoring-execution-plan.md`](docs/agent-refactoring-execution-plan.md)
- [`docs/codex-claude-code-agent-design-analysis.md`](docs/codex-claude-code-agent-design-analysis.md)
