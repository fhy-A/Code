# Code Claude Code 协作入口

## 新会话自助启动

先读本入口；内部工作区还须读根目录协作规则（同级 AGENTS/CLAUDE 校验一致后不重复展示共同正文）。外部 clone 没有根规则或私有源时仍按用户显式任务和本入口工作，不重建内部计划。

1. 读 [日志索引](docs/development-log/README.md)的阅读规则/最近条目、最新日志与当前任务相关段落；读可用的私有 `../workbar-private/TODO.md` 顶部维护契约及相关稳定 ID/依赖。无关历史不默认全文加载。
2. 在本目录运行 `git status --short`、`git log -8 --oneline`，核对 HEAD、tracked/cached、共享差量。可用的私有 `../workbar-private/development-handoff.md` 在接手或范围重叠时必读，无关活动不得覆盖。
3. 只按任务需要读取实现、规范和原始证据。已读且未变化的同任务只查差量，漂移/冲突再追原文；项目按需读取规则不覆盖实际使用技能的强制阅读要求。
4. **唯一写者**：共享工作区任何写入、暂存、提交或有持久副作用的测试前，依 [canonical 租约协议](docs/approval-relay-protocol.md#角色与强制顺序)使用唯一 helper `scripts/workspace_owner_lease.py` 取得并维护 owner lease。审批只读且不申请；其他 holder、所有权不明或租约失效立即停止，`none` 不证明 DSH 已停止。

**事实源与归档**：Git/文件/可复现证据优先；整个 workbar 已完成事实统一写入 `docs/development-log/YYYY/YYYY-MM-DD.md` 并更新索引（本地时间 `YYYY-MM-DD HH:mm`、同日倒序）。未完成事项只在私有 TODO，遵守顶部维护契约，稳定 ID、唯一主域/状态、跨域仅引用；不另建平行计划。公共 `TODO.md` 是用户批准的公开、脱敏、非执行摘要；不得据此自动选择或启动任务，仅在用户明确批准后人工更新，不得从私有 TODO 自动同步。私有源缺失时不从公开摘要、stub 或历史恢复内部路线。

私有 handoff 仅保留进行中差量：冲突以现场为准并先修正；阶段完成后归档事实、整理私有 TODO，再移除对应活动差量；暂停/换手用 [交接模板](docs/development-handoff-template.md)。公共 `docs/development-handoff.md` 只是兼容 stub。完整读取/归档规则见 [事实源与按需读取](docs/approval-relay-protocol.md#事实源与按需读取)。

## 开发任务预检、验证、接力与轮换

- **按阶段分工**：风险等级决定授权/验证；另按不确定性、可验证性、交接成本选执行方。DSH/Claude Code 可日常承担清晰阶段；Codex/Astra 优先关键设计、高不确定性诊断、必要独立复核，复杂任务已有连续上下文时可完整闭环，不机械换手。参见 [任务分配与介入](docs/approval-relay-protocol.md#任务分配与介入)。
- 用户可直接交付清楚的小任务；分流不是发送、新建、模式切换或操作授权，不新增调度会话/程序、后台监控或路由持久状态。每阶段一个执行者、按需一个复核者，不默认第二层审批。
- 新开发任务只读接手后、环境变化或重要运行时改动后运行一次 `python verify.py doctor`；同环境微调不重复。失败报告能力边界，不自动安装依赖。
- 验证按 doctor（适用时）→定向确定性证据→相关回归/构建/freshness→适用扩大/全量推进。保留兼容、恢复、安全、重复副作用证据；不为文档或小改机械跑不相关全量。H4 优先内部闭环；环境受限时用户首次失败后审计完整断言链，最多再请求一次最终复跑，第二次失败禁止第三次。详见 [验证规则](docs/approval-relay-protocol.md#验证按证据阶梯推进)。
- **内置 Browser 标签页禁止程序化关闭**，验收后保持打开（包括禁止 `tab.close()`）；隔离夹具创建的 page/context/browser 仍必须清理。
- 批准边界内连续实现、修正、验证和事实收口；两轮实质尝试未缩小根因、范围扩大或证据/断言/覆盖可信度问题时升级。产品、协议、数据、安全、交互或外部状态变化仍须停下重新确认。
- 复用开发任务并命名 `code开发·[目前具体任务]`。接手、阶段结束、承接新阶段前分别以自己的精确 ID 执行 `python -B scripts/session_file_size.py <task-id> --json`，只读元数据；未知不能记为零。大小只作建议，轮换/新建仍需用户授权，阈值、去重与交接见 [会话检查](docs/approval-relay-protocol.md#本地会话大小检查与阶段边界轮换)。

## 分阶段协作与交付

### 风险分级与实施确认门禁

先只读检查/必要复现和轻量价值预检，按最高影响分级，不确定上调；诊断/解释/评审/报告不授权修复。[完整风险门禁](docs/approval-relay-protocol.md#risk-levels-and-workflow-gates)：

- **QUICK**：小而确定的文档/修复，无协议、持久化、并发、权限、安全、迁移、发布、不可逆或主观 UI 边界；直接修改请求可授权明确范围，不重复确认，不默认独立 Astra 复核。
- **STANDARD**：UI/交互、多文件功能、非平凡重构；先说明产品行为、影响、取舍、边界和验收，取得一次明确方案确认，保留适用验收。
- **STRICT**：协议、持久化/格式、并发、授权/安全/凭据、进程生命周期、迁移、发布、删除或难复现且有重要副作用；完整合同包含事实、兼容/迁移、失败、回退、阶段和停止条件，取得方案/操作授权并独立复核。

### 授权、测试与兼容性边界

- 一次只推进一个获批阶段；旁支仅在阻塞本阶段验收时处理，否则按私有维护契约登记，不自动启动下一项。风险/范围/产品行为变化停止重新确认，不因预算降低门禁。
- 按 [阶段收口与共同约束](docs/approval-relay-protocol.md#阶段收口与共同约束)完成适用检查、必要用户 PASS、事实日志与私有 TODO 整理，再只暂存本阶段文件并创建本地提交；已知失败不提交。交付报告提交、测试/未覆盖边界、保留的共享差量和临时状态恢复，不将日志汇总冒充本轮独立复验。
- push、tag、release、删除数据、线上配置及高风险外部操作须用户明确授权；租约只保护写入互斥。旧数据/协议须兼容、可迁移/回退，不得无提示失效。
- 同级 AGENTS/CLAUDE 共同规则同步；全局规则还要核对根入口。新增用户文案遵守 [README 的 i18n 约定](README.md#界面文案与-i18n)，完成规定自动检查与中英文切换，不在 `app.js` 或业务文件另建局部语言字典。

## 发版与发布

只在明确授权后按 [release-guide](docs/release-guide.md) 使用 `python release.py <版本> --yes`，不得手改版本号。脚本负责版本同步、测试、EXE、元数据、发布正文、提交/标签、推送/Release，失败即停；`--dry-run` 仅预演。

提前准备无占位的中文发布正文，Phase 5 保留正文并刷新日期/版本/大小/SHA-256；正文空或有占位时即使 `--yes` 也必须在提交/标签/Release 前停下。正文只描述上个标签到本次候选的实际改动。`gh` 未安装/未登录不得绕过；推送/Release 失败如实报告部分外部状态和补救步骤。按 [发布冻结](docs/approval-relay-protocol.md#release-freeze)锁定候选与范围，新需求入队列，完成/中止后不自动开始下一周期。

## 品牌与命名约定

平台品牌始终小写 **workbar**（UI、i18n、文档、注释、测试、提交信息）；URL 为 `https://workbar.ai`。常量如 `WORKBAR_URL` 遵循语言惯例，CSS 类如 `.key-workbar-btn` 保持小写；新增/修改涉及品牌的内容提交前检查。
