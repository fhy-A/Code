# Code Claude Code 协作入口

## 新会话自助启动

用户可以在新会话中直接提出任务，不需要为已经完成的阶段提供人工交接消息。开始任何开发、测试或评审任务前，Agent 自行完成：

1. 阅读 `docs/development-log/README.md`，按索引读取最新日期文件，以及与当前任务直接相关的日期文件；历史归档仅在追溯时按需读取。
2. 阅读 `TODO.md`，确认未完成事项、优先级和当前任务边界。
3. 运行 `git status --short` 和 `git log -8 --oneline`，核对分支、提交和共享工作区现场。
4. **跨运行时写入租约**：Codex/DSH Developer Agent 在对共享 `code` 工作区执行任何写入、暂存、提交或会产生持久副作用的测试前，必须按 canonical `code/docs/approval-relay-protocol.md` 使用唯一 helper `code/scripts/workspace_owner_lease.py` 取得并维护 owner lease；Approval Agent 保持只读且不申请。租约仅提供同一物理 worktree 写入互斥，不授予 push、tag、release、delete 或线上配置权限。
5. 按任务需要读取实现、测试、规范和专题文档，不默认加载完整历史。
6. 检查 `docs/development-handoff.md` 是否存在。该文件只代表尚未完成的活动任务：用户要求“继续/接手”或当前任务与其范围重叠时必须读取并与 Git、日志、TODO 核对；当前任务无关时不得覆盖，并在可能冲突时提示用户。

已完成事实以开发日志和可复现结果为准，未完成项目事项以 `TODO.md` 为准，代码现场以 Git 和实际文件为准。活动交接只记录进行中差量，不能成为第四份长期状态源。

本目录同时是整个 workbar 项目的产品级状态事实源。即使实现改动发生在根目录、`new-api-source` 或其他兄弟目录，已经完成的重要事实、产品决策和真实验证结果仍统一写入本目录的 `docs/development-log/YYYY/YYYY-MM-DD.md` 并更新索引，未完成事项统一写入本目录的 `TODO.md`。实现所在子仓库只保留与自身实现或部署直接相关的专题文档，不得另建平行的通用开发日志或项目 TODO。

新增、更新、移动或完成产品级事项时，必须遵守 `TODO.md` 顶部的 canonical 维护契约：保留稳定 ID、唯一主域与唯一执行状态，跨域只写依赖/关联引用，完成事实进入开发日志，活动 handoff 只引用任务 ID/链接；不要在协作规则中复制字段模板。

任务结束时：

- 阶段已经完成：将重要改动和真实验证结果写入 `docs/development-log/YYYY/YYYY-MM-DD.md`，更新日志索引，将剩余事项写入 `TODO.md`，并移除已经收口的 `docs/development-handoff.md`。
- 阶段尚未完成、需要暂停或切换 Codex / Claude Code 等 Agent：按 `docs/development-handoff-template.md` 创建或更新 `docs/development-handoff.md`，写清现场、验证、阻塞和准确下一步。
- 新增日志必须写明本地具体时间戳，格式为 `YYYY-MM-DD HH:mm`；既有历史归档无需追溯修改。
- 不要在本文件复制项目状态，避免形成多个事实源。

新增或修改用户可见界面文案时，必须遵守 `README.md` 中的“界面文案与 i18n”约定，并在任务结束前完成其中的自动检查与中英文切换检查。不要在 `app.js` 或其他业务文件中建立新的局部语言字典。

## 开发任务预检、验证、接力与轮换

- **边界预检，不机械重复**：新开发任务完成只读接手后、解释器/依赖/浏览器环境变化时，或重要运行时改动完成后，在 `code` 目录运行一次 `python verify.py doctor`。同一任务和未变化环境中的微小修正不得重复运行；doctor 不自动安装依赖，失败只形成明确能力边界和处置入口。
- **验证证据阶梯**：按“doctor（适用时）→ 直接相关的确定性测试 → 相关回归与构建/freshness → 仅在阶段冻结、重要运行时改动或发布时扩大/全量”的顺序验证。不得以分层为由跳过直接相关、兼容、恢复、安全或重复副作用证据，也不得为微小修正机械重跑不相关全量门禁。
- **H4 优先内部闭环，用户复跑有熔断**：环境允许时，H4 必须由开发任务内部通过后再交付；环境受限时先完成可执行的确定性覆盖，正常情况下只请求一次用户复跑。首次失败后必须完整审计全部附件、诊断和剩余断言链，修复后最多再请求一次最终复跑；同一场景第二次仍失败时禁止第三次，必须重设计、撤回不稳定测试或明确阻塞。
- **Codex 内置 Browser 标签页禁止程序化关闭**：使用 Codex 桌面应用内置 Browser 验收时，严禁调用 `tab.close()` 或任何程序化关闭标签页的动作，尤其不得关闭最后一个内置标签页；已确认此类动作可能销毁并重启整个 `OpenAI.Codex` AppX 容器，从而中断当前任务。验收结束后保持内置标签页打开，可导航复用或交由用户手动关闭。此禁令仅适用于应用内置 Browser；测试夹具自行创建的隔离 Playwright/Chromium page、context 和 browser process 仍必须按原合同正常关闭与清理，不得混同。
- **每阶段默认三个协调点**：开始时批准控制合同，只有真实范围/风险变化时重新审批，结束时完成复核。已批准边界内的实现修正和测试夹具修正由开发任务在同一 turn 自行闭环；产品行为、协议、持久化、安全、数据或外部操作边界变化仍必须停下并重新确认。
- **在阶段边界轮换开发任务**：正式发布完成、发生上下文压缩、历史已混入多个无关阶段，或无法仅靠 Git/开发日志/TODO 快速恢复现场时，旧开发任务停止作为写者和接收新 relay；按用户已有的任务创建授权绑定新任务，缺少授权时只建议轮换并等待确认。新任务按本文件自助接手，不复制已完成历史或受保护差量。

## 分阶段协作与交付

### 风险分级与实施确认门禁

- 新需求、优化建议或 Bug 先进行与风险相称的只读检查和必要复现，再按最高影响边界分为 `QUICK`、`STANDARD` 或 `STRICT`；不确定时上调一级。风险分级只调整确认深度和验证范围，不降低事实核对、共享工作区保护、兼容、安全或外部操作授权门禁。
- **QUICK（快速）**：适用于 TODO、日志、文档，或目标与验收明确、范围小且确定性的修复；不得涉及协议、持久化、并发、权限、安全、迁移、发布、不可逆操作或需要主观取舍的 UI。用户已经直接要求“修改/修复/更新/实现”时，该请求在明确范围内即构成实施授权；简短说明理解、边界和验收后可以实施，不得要求用户重复确认同一方案。用户只要求检查、解释、评审、诊断或报告时仍保持只读。
- **STANDARD（标准）**：适用于 UI、交互、多文件功能和非平凡重构。先说明产品行为、影响范围、主要取舍、实施边界与验收，取得一次明确方案确认后再实施；具体函数、选择器、文件拆分和测试组织由开发 Agent 在已确认边界内自主决定。
- **STRICT（严格）**：适用于协议、持久化、数据格式、并发、授权、安全、凭据、进程生命周期、迁移、发布、删除或难复现且可能产生重要副作用的问题。必须完成事实源、兼容/迁移、失败路径、回退、阶段边界和停止条件的完整审批，并取得对应方案与操作授权。
- 实施中如果出现产品歧义、明显扩大范围，或需要改变既定行为、数据结构、协议、安全边界、交互方式或外部状态，立即暂停并重新确认；不得用原有低风险授权覆盖新发现的高风险边界。

### 授权、测试与兼容性边界

- **操作授权**：阶段验收通过后可按适用的子项目规则自动创建本地 `git commit`；`git push`、打标签、发布版本、删除数据和修改线上配置必须由用户明确提出，不能从“完成开发”中自动推断。
- **诊断与修复分离**：用户只要求检查、分析、解释原因、评审或报告状态时，默认保持只读；不得把诊断请求扩展为代码修复，修复仍需经过方案确认。
- **临时测试改动必须恢复**：测试中调整的超时、模型参数、渠道配置、环境变量或调试开关，结束前必须恢复并核对；交付时报告恢复结果，不得让临时值进入提交或发布。
- **协作文档同步**：同一级的 `AGENTS.md` 与 `CLAUDE.md` 中，共同协作规则必须同步修改；根目录与子项目存在相同全局规则时，也要检查是否需要同步，避免不同 Agent 执行不同标准。
- **发布文档规范**：发布说明、更新摘要和开发日志默认使用中文，仅保留必要的英文技术字段；发布前必须确认不存在占位文案，且内容只描述上一标签至当前标签之间实际包含的改动。
- **数据与协议兼容性**：涉及会话格式、持久化数据、工具协议或配置结构时，方案必须说明向后兼容、迁移和回退方式，并验证旧数据或旧协议不会在无提示情况下失效。

1. **一次只推进一个阶段**：先从 `TODO.md` 和现有实现确认当前阶段的目标、边界与验收条件；阶段完成前不混入下一阶段功能。发现旁支问题时，只有会阻塞当前验收才在本阶段修复，否则记入 `TODO.md`。
2. **先验证，后提交**：验证必须与风险等级匹配。`QUICK` 默认运行定向测试、相关语法/一致性检查和 `git diff --check`；`STANDARD` 增加相关回归、前端构建/freshness 及必要的用户人工 PASS；`STRICT` 按影响增加旧数据/旧协议、恢复、失败、安全和重复副作用测试，并在阶段冻结、重要运行时改动或发布时运行扩大回归或完整 pytest。不得为微小修正机械重复不相关的全量门禁，也不得以风险分级为由漏掉直接相关测试。测试失败时先修复并重新验证，不提交已知失败的阶段。
3. **区分自动与人工验收**：能够由自动化测试充分覆盖的阶段，由协作 Agent 自行完成验证、日志和提交；涉及视觉效果、流式交互、焦点、时序、浏览器差异或其他需要观察的行为时，向用户提供简短明确的测试步骤和通过标准，等待用户确认正常后再提交。
4. **人工测试发现问题就暂停收口**：用户反馈异常时，保持当前阶段未提交，先定位、修复并重新给出验证方式；只有用户确认或等价自动化证据通过后，才将阶段标记完成。
5. **每阶段独立记录并提交**：验收通过后，将完成内容和真实测试结果写入当天的 `docs/development-log/YYYY/YYYY-MM-DD.md` 并更新索引，将新发现且未完成的事项写入 `TODO.md`，移除其中已经完成的条目并保持清单简洁；随后只暂存本阶段文件，创建含义明确的 Git 提交，再开始下一阶段。
6. **保护共享工作区**：默认工作区可能包含用户或其他协作者的未提交修改。提交前检查 `git status` 和暂存区，只提交本阶段确认过的文件；不得覆盖、清理、回退或顺带提交无关改动。
7. **交付信息必须可核验**：阶段结束时报告完成结果、测试结论、提交哈希和仍保留的未提交文件；不能把未运行的测试、未观察的界面或推测性结果描述为已验证。

## 发版与发布

发版使用项目根目录的自动化脚本，**不得手动修改版本号**：

```powershell
python release.py 0.5.8           # 人工发版（有交互确认）
python release.py 0.5.8 --yes     # AI Agent 发版（跳过交互确认）
python release.py 0.5.8 --dry-run # 预演：只检查不改任何文件
```

脚本按 7 个阶段自动完成：版本号同步 → 测试 → 构建 EXE → 元数据校验 → 发布说明 → Git 提交打标签 → 推送 + GitHub Release。任何步骤失败立即停止并报告原因。

详细说明见 [`docs/release-guide.md`](docs/release-guide.md)。

Agent 发版注意事项：
- 必须使用 `--yes` 参数跳过交互确认。
- Agent 应提前写好无占位的中文发布正文；Phase 5 会保留已有正文并刷新日期、版本号、文件大小和 SHA-256。正文为空或仍含占位文案时，即使使用 `--yes` 也必须在 Git 提交、标签和 Release 创建前停止。
- `gh` 未安装或未登录时脚本会退出并给出补救命令，Agent 不应尝试绕过。
- 推送或 Release 创建失败时，代码和标签可能已部分推送，Agent 应向用户报告具体失败原因和补救步骤。
- **发布冻结队列**：用户授权进入正式发布门禁后，以当时的候选版本、基线提交和已确认范围作为冻结集合。门禁期间出现的新需求、截图、样本或文档补充统一进入 next-cycle 队列，不得静默改写候选版本；只有阻断发布的缺陷、安全问题或用户明确改变范围时才停止并重新冻结。发布完成或中止后报告冻结基线和队列，但不得自动开始下一周期。

## 品牌与命名约定

- **平台品牌 "workbar" 必须始终使用全小写**，包括所有用户可见界面文案、i18n 翻译、文档、注释、测试和提交信息。URL `https://workbar.ai` 自然为小写。
- 代码中的常量名（如 `WORKBAR_URL`）使用大写是 JavaScript/Python 标准惯例，不受此规则约束。CSS 类名（如 `.key-workbar-btn`）也保持小写不变。
- 新增或修改任何包含平台品牌名的内容时，必须先检查是否为全小写 "workbar"，再提交。
